# M4-B03 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Vanilla's pathfinding node-expansion budget is `maxVisitedNodes = floor(FOLLOW_RANGE_blocks x 16)`. | `PathNavigation` ctor, PathNavigation.java:59-62 — `createPathFinder(Mth.floor(mob.getAttributeBaseValue(Attributes.FOLLOW_RANGE) * 16.0))`; `updatePathfinderMaxVisitedNodes` lines 69-72 with `getMaxPathLength()` lines 79-81 recomputes it as `floor(max(FOLLOW_RANGE value, requiredPathLength=16.0) * 16.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `recomputePath()` never runs more than once per 20 ticks per entity (`MAX_TIME_RECOMPUTE`). | PathNavigation.java:34 (`MAX_TIME_RECOMPUTE = 20`); `recomputePath` lines 101-110 — `gameTime - timeLastRecompute <= 20L` defers via `hasDelayedRecomputation`, so the real minimum gap is 21 ticks | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Every vanilla `Mob` owns two independent `GoalSelector` instances -> a behavior `goalSelector` and an attack/interaction-target-only `targetSelector` -> each an ordered list of `(priority, Goal)` pairs where a lower priority number means higher precedence. | Mob.java:134-135 (fields) and 150-151 (both constructed); `WrappedGoal.canBeReplacedBy` WrappedGoal.java:16-18 — a challenger needs a strictly smaller priority number. Container shape note: `GoalSelector.availableGoals` is an insertion-ordered `ObjectLinkedOpenHashSet` (GoalSelector.java:25), never a list sorted by priority | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's goal-flag `EnumSet` has exactly four flags: MOVE, LOOK, JUMP, and TARGET. | Goal.java:68-76 — `enum Flag { MOVE, LOOK, JUMP, TARGET }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A vanilla `Goal`'s default `canContinueToUse()` implementation simply returns `canUse()`. | Goal.java:17-19 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A vanilla `Goal`'s default `isInterruptable()` value is `true`. | Goal.java:21-23 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `GoalSelector.tick()` runs a fixed four-pass algorithm each call, in order: a cleanup pass, a flag-lock cleanup pass, a start pass, and a tick pass. | `GoalSelector.tick`, GoalSelector.java:69-97 — cleanup 73-77, flag-lock cleanup 79, start 83-93, tick pass via `tickRunningGoals(true)` at 96 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `GoalSelector` cleanup pass stops any running goal whose flags now intersect the disabled flags, or whose `canContinueToUse()` now returns false. | GoalSelector.java:73-77 with `goalContainsAnyFlags` 49-57 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `GoalSelector` flag-lock cleanup pass drops any of the four flag locks whose owning entry is no longer running. | GoalSelector.java:79 — `lockedFlags.entrySet().removeIf(e -> !e.getValue().isRunning())` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `GoalSelector` start pass, evaluated in declaration order, starts any non-running goal whose needed flags are unlocked or held only by interruptable goals of strictly lower precedence (a numerically greater priority number), and whose `canUse()` returns true, evicting and stopping whatever previously held each needed flag. | GoalSelector.java:83-93 with `goalCanBeReplacedForAllFlags` 59-67 and `WrappedGoal.canBeReplacedBy` WrappedGoal.java:16-18 (`isInterruptable() and goal.getPriority() < this.getPriority()`); eviction is `currentGoal.stop()` then `lockedFlags.put(flag, goal)` at 86-88 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `GoalSelector` tick pass runs every currently-running goal, either every tick (for a goal that needs continuous updating) or only on the caller's full-tick pass. | `GoalSelector.tickRunningGoals`, GoalSelector.java:99-110 — `forceTickAllRunningGoals or goal.requiresUpdateEveryTick()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's goal-selector start pass breaks ties between equal-priority goals by insertion order, matching the underlying `ObjectLinkedOpenHashSet`. | GoalSelector.java:25 and 83 — `availableGoals` is an `ObjectLinkedOpenHashSet` iterated in insertion order and never sorted; priority enters only through `canBeReplacedBy` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `Mob.serverAiStep()` throttles a full `GoalSelector.tick()` (the cleanup+start passes) to run only every other tick, gated by `(tickCount + entityId) % 2`; on the intervening ("off") tick only `tickRunningGoals(false)` runs, while any already-running goal that needs continuous updating still ticks unconditionally on every tick regardless of this key. | `Mob.serverAiStep`, Mob.java:716-737 — `idBasedTickCount = tickCount + getId()`; `idBasedTickCount % 2 != 0 and tickCount > 1` takes the `tickRunningGoals(false)` branch, else the full `tick()`; continuous ticking comes from `requiresUpdateEveryTick` at GoalSelector.java:104. The extra `tickCount > 1` guard is not in the blueprint's wording but does not change the every-other-tick key | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla's Brain system, `forget_outdated_memories` (memory-TTL expiry) runs first, every brain tick, before any sensor runs. | `Brain.tick`, Brain.java:388-393 — `forgetOutdatedMemories()` at 389 precedes `tickSensors()` at 390; `forgetOutdatedMemories` itself at 401-403 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla's Brain system, `Brain.tickSensors` dispatches every registered `Sensor` each brain tick, but each `Sensor`'s own work is throttled by a per-instance scan-rate countdown (`Sensor.tick` is final and gated by `timeToTick`) -> a sensor's `doTick` runs only once every `scanRate` ticks, the no-arg default being 20, staggered by a randomized start delay; only the outer dispatch loop is unthrottled. | `Brain.tickSensors` (Brain.java:395-399) is unthrottled, but `Sensor.tick` (Sensor.java:44-50) is `final` and gated — `if (--this.timeToTick <= 0L) { this.timeToTick = this.scanRate; ...; this.doTick(...); }` with `DEFAULT_SCAN_RATE = 20` (Sensor.java:14) used by the no-arg constructor (Sensor.java:36-38). Correction: a default sensor's `doTick` runs once every 20 brain ticks, not every tick; each sensor type supplies its own `scanRate`, and `randomlyDelayStart` (Sensor.java:40-42) staggers the first fire. Only the outer dispatch loop is unthrottled | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A vanilla `Behavior`'s default minimum and maximum duration are both 60 ticks. | Behavior.java:12 (`DEFAULT_DURATION = 60`), 19-25 — the one-arg constructor forwards 60, the two-arg one sets `minDuration = maxDuration = timeOutDuration` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Activity/Behavior system registry has 26 entries. | Activity.java:7-32 — 26 `register` calls (CORE, IDLE, WORK, PLAY, REST, MEET, PANIC, RAID, PRE_RAID, HIDE, FIGHT, CELEBRATE, ADMIRE_ITEM, AVOID, RIDE, PLAY_DEAD, LONG_JUMP, RAM, TONGUE, SWIM, LAY_SPAWN, SNIFF, INVESTIGATE, ROAR, EMERGE, DIG); cross-checked against datagen `reports/registries.json`, `minecraft:activity` = 26 entries | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Brain tick executes exactly FOUR phases, in order -> memory forgetting, sensors, behavior starting, and behavior stopping/ticking; activity selection is not one of them, running instead outside `Brain.tick` from the entity's own AI step and from a schedule-update behavior the brain's own packages register. | `Brain.tick`, Brain.java:388-393. Correction: the brain tick has exactly FOUR phases, in order `forgetOutdatedMemories()`, `tickSensors()`, `startEachNonRunningBehavior()`, `tickEachRunningBehavior()`. Activity selection is NOT one of them — it is driven from outside `Brain.tick`, by the entity's own `customServerAiStep` (e.g. `Villager.customServerAiStep` calling `brain.updateActivityFromSchedule(...)`, Brain.java:328-336) and by the `UpdateActivityFromSchedule` behavior the Idle package itself registers at priority 99 (VillagerGoalPackages.java:185-224) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Brain activity-selection method (`setActiveActivityToFirstValid`) scans a caller-supplied activity list in order and switches to the first whose every `ActivityRequirement` is satisfied by the brain's current memory state, but does nothing at all when none match -> there is no Idle fallback on that path; the `useDefaultActivity`/`Activity::Idle` fallback belongs to a different entry point entirely. | `Brain.setActiveActivityToFirstValid`, Brain.java:338-345; `Brain.setActiveActivityIfPossible` 298-304; `Brain.useDefaultActivity` 284-286; `defaultActivity` field Brain.java:49. Correction: `setActiveActivityToFirstValid(List<Activity>)` walks the caller-supplied list, sets the first activity whose `activityRequirementsAreMet` holds and `break`s — when NONE match it does nothing at all; there is no Idle fallback on that path. The `useDefaultActivity()` fallback belongs to the different entry point `setActiveActivityIfPossible(activity)`, which falls back only when that one named activity's requirements fail. `defaultActivity` is a per-brain field initialised to `Activity.IDLE` and settable via `setDefaultActivity` (Brain.java:347-349), not a hard-coded Idle | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Brain starts every not-yet-running behavior across all currently-active activity packages, in priority-ascending order, whenever its required memories are met and its extra start conditions pass. | `Brain.startEachNonRunningBehavior`, Brain.java:413-428 — iterates `availableBehaviorsByPriority.values()`, which is a `Maps.newTreeMap()` keyed by Integer priority (Brain.java:43) and therefore ascending; skips activities not in `activeActivities` (line 419); starts only `Behavior.Status.STOPPED` entries via `tryStart`, whose gate is `hasRequiredMemories(body) and checkExtraStartConditions(level, body)` (Behavior.java:44-54) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Brain stops a currently-running behavior when its `can_still_use` check fails or its own randomized minimum-to-maximum-tick duration has elapsed, and otherwise ticks it -> checked in one flat pass in priority-ascending order across EVERY registered behavior whose status is running, including behaviors of an activity no longer active, not only the currently-active packages. | `Brain.tickEachRunningBehavior` Brain.java:430-435, `Brain.getRunningBehaviors` 267-282, `Behavior.tickOrStop` Behavior.java:60-66. The stop condition itself is exactly right (`!timedOut(timestamp) and canStillUse(...)` else `doStop`, with the duration drawn as `minDuration + random.nextInt(maxDuration + 1 - minDuration)` at Behavior.java:47). Correction to the ordering clause: the flat pass is NOT independent of priority order — `getRunningBehaviors` builds its list by walking the same ascending `Maps.newTreeMap()`-backed `availableBehaviorsByPriority` (Brain.java:43, 271), so the tick or stop pass runs in priority-ascending order; and it spans EVERY registered behavior whose status is RUNNING, including ones belonging to an activity no longer in `activeActivities`, not only the active packages | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's brain schedule-update throttle (`SCHEDULE_UPDATE_DELAY`) is 20 ticks. | Brain.java:40 (`SCHEDULE_UPDATE_DELAY = 20`), used at `updateActivityFromSchedule` line 329 as `gameTime - lastScheduleUpdate > 20L` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla's pathfinding, the `PathType` values `Blocked`, `PowderSnow`, `Fence`, `Lava`, `UnpassableRail`, `DoorWoodClosed`, `DoorIronClosed`, `Leaves`, and `Damaging` each carry a default malus of -1 (impassable). | PathType.java:4 BLOCKED, 9 POWDER_SNOW, 11 FENCE, 12 LAVA, 16 UNPASSABLE_RAIL, 22 DOOR_WOOD_CLOSED, 23 DOOR_IRON_CLOSED, 25 LEAVES, 20 DAMAGING — all `-1.0F` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla's pathfinding, the `PathType` values `Water`, `WaterBorder`, `FireInNeighbor`, `DamagingInNeighbor`, and `StickyHoney` each carry a default malus of 8. | PathType.java:13 WATER, 14 WATER_BORDER, 17 FIRE_IN_NEIGHBOR, 19 DAMAGING_IN_NEIGHBOR, 26 STICKY_HONEY — all `8.0F` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla's pathfinding, the `PathType` value `Fire` carries a default malus of 16. | PathType.java:18 — `FIRE(16.0F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla's pathfinding, the `PathType` values `Breach` and `BigMobsCloseToDanger` each carry a default malus of 4. | PathType.java:24 `BREACH(4.0F)`, 30 `BIG_MOBS_CLOSE_TO_DANGER(4.0F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla's pathfinding, the `PathType` values `Open`, `Walkable`, `WalkableDoor`, `Trapdoor`, `OnTopOfPowderSnow`, `Rail`, `DoorOpen`, `Cocoa`, `DamageCautious`, and `OnTopOfTrapdoor` each carry a default malus of 0. | PathType.java:5 OPEN, 6 WALKABLE, 7 WALKABLE_DOOR, 8 TRAPDOOR, 10 ON_TOP_OF_POWDER_SNOW, 15 RAIL, 21 DOOR_OPEN, 27 COCOA, 28 DAMAGE_CAUTIOUS, 29 ON_TOP_OF_TRAPDOOR — all `0.0F`; together with the three preceding rows this accounts for all 27 enum constants | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's pathfinding `NodeEvaluator` system has four evaluator types: Walk, Fly, Amphibious, and Swim. | `net/minecraft/world/level/pathfinder`: WalkNodeEvaluator.java:31, FlyNodeEvaluator.java:15 (extends Walk), AmphibiousNodeEvaluator.java:10 (extends Walk), SwimNodeEvaluator.java:17 (extends NodeEvaluator). Two entity-private subclasses exist outside the package and add no new family — `Frog.FrogNodeEvaluator` (Frog.java:351) and `Creaking.HomeNodeEvaluator` (Creaking.java:592) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's neighbor generation for pathfinding produces the 4 cardinal neighbors first (North, East, South, West, in that fixed order), then the 4 diagonal neighbors (NE, SE, SW, NW), and a diagonal neighbor is only emitted if both of its adjacent cardinal neighbors are themselves valid and the diagonal node itself is also independently valid, preventing the path from cutting a solid corner (`isDiagonalValid`). | `WalkNodeEvaluator.getNeighbors`, WalkNodeEvaluator.java:124-164; `Direction.Plane.HORIZONTAL` Direction.java:577. Correction: the cardinal iteration order is NORTH, EAST, SOUTH, WEST — `HORIZONTAL(new Direction[]{Direction.NORTH, Direction.EAST, Direction.SOUTH, Direction.WEST}, ...)` — not North, South, East, West. Everything else holds: the four cardinals are emitted first (lines 135-143), then the four diagonals (145-161) formed as `direction` paired with `direction.getClockWise()`, i.e. NE, SE, SW, NW; a diagonal is only emitted when `isDiagonalValid(pos, ew, ns)` passes for both adjacent cardinals (170-185) and `isDiagonalValid(diagonalNode)` passes for the diagonal itself (187-193). Separate detail: the cardinals are stored into `reusableNeighbors` indexed by `Direction.get2DDataValue()` (SOUTH=0, WEST=1, NORTH=2, EAST=3), so the scratch-array slot order is not the emission order | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's pathfinding neighbor generation is not a first-valid-wins scan over three vertical placements -> the same-elevation placement is tried first, and the step-up and the downward scan are then mutually exclusive alternatives selected by the candidate's own `PathType` (a failed step-up never falls through to a downward scan), after an earlier outright rejection when the candidate's elevation rise exceeds the mob's own jump height. | `WalkNodeEvaluator.findAcceptedNode`, WalkNodeEvaluator.java:232-272, with `tryJumpOn` 300-336, `tryFindFirstNonWaterBelow` 337-351, `tryFindFirstGroundNodeBelow` 353-371. Correction: the three placements are not a first-valid-wins scan. Same-elevation is tried first (lines 242-244, accepted when the mob's malus for that PathType is `>= 0`); the step-up and the downward scan are then MUTUALLY EXCLUSIVE `else if` branches selected by the candidate's own PathType (lines 253-266) — `tryJumpOn` runs only when the same-level node was missing or negative-malus AND `jumpSize > 0` AND the type is not FENCE (unless the mob can walk fences), UNPASSABLE_RAIL, TRAPDOOR or POWDER_SNOW; a failed `tryJumpOn` returns null and does NOT fall through to a downward scan. The downward scan runs only for WATER (non-amphibious, non-floating) or OPEN. A separate early rejection applies first: `maxYTarget - nodeHeight > getMobJumpHeight()` returns null outright (lines 237-240, `getMobJumpHeight` 274-276 = `max(1.125, maxUpStep())`) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's pathfinding step-up size (`jumpSize`) is `floor(max(1.0, step_height))` blocks, and with the `STEP_HEIGHT` attribute's default value of 0.6, this evaluates to 1 block. | `WalkNodeEvaluator.getNeighbors`, WalkNodeEvaluator.java:130 — `jumpSize = Mth.floor(Math.max(1.0F, this.mob.maxUpStep()))`; `Attributes.STEP_HEIGHT` default 0.6 at Attributes.java:98-100, so `floor(max(1.0, 0.6)) = 1` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's pathfinding disables the step-up (Y+1) candidate entirely whenever the block directly above the current node has a negative-malus `PathType`. | `WalkNodeEvaluator.getNeighbors`, WalkNodeEvaluator.java:127-131 — `jumpSize` stays 0 unless `this.mob.getPathfindingMalus(blockPathTypeAbove) >= 0.0F`. Additional condition the claim omits: the same guard also requires `blockPathTypeCurrent != PathType.STICKY_HONEY`, so standing in honey disables the step-up too | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's pathfinding computes a candidate neighbor's cost as the straight-line distance from the current node (1.0 for a cardinal move, the square root of 2 for a diagonal move) plus the destination's `PathType` malus. | `PathFinder.findPath`, PathFinder.java:111-113 with `PathFinder.distance` 149-151 (`from.distanceTo(to)`, Euclidean — 1.0 cardinal, sqrt(2) diagonal); the malus is the destination node's `costMalus`, set from the mob's per-PathType malus in `WalkNodeEvaluator.getNodeAndUpdateCostToMax` (WalkNodeEvaluator.java:278-282) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's pathfinding treats a negative `PathType` malus (`costMalus < 0`, impassable) as never crossable for a new candidate node, only for the node the search currently occupies (`isNeighborValid`). | `WalkNodeEvaluator.isNeighborValid`, WalkNodeEvaluator.java:166-168 — `neighbor != null and !neighbor.closed and (neighbor.costMalus >= 0.0F or current.costMalus < 0.0F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's real pathfinding vertical-placement search scans further than a fixed short downward range under certain conditions, rather than using one constant bounded descent depth. | `WalkNodeEvaluator.tryFindFirstGroundNodeBelow`, WalkNodeEvaluator.java:353-371 — descends to `mob.level().getMinY()` and is bounded by the per-entity `mob.getMaxFallDistance()` (`LivingEntity.getMaxFallDistance` LivingEntity.java:1777-1784), not a fixed constant; `tryFindFirstNonWaterBelow` (337-351) descends to `getMinY()` with no depth bound at all | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's A* pathfinding heuristic multiplier (`FUDGING`) is 1.5. | PathFinder.java:23 — `private static final float FUDGING = 1.5F;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's A* pathfinding search computes `g` as the accumulated edge cost from the start node and `h` as `FUDGING x straight_line_distance(node, nearest target)`, with `f = g + h`. | `PathFinder.findPath`, PathFinder.java:111-121 — `tentativeGScore = current.g + distance + neighbor.costMalus` (113), `neighbor.h = this.getBestH(neighbor, targets) * 1.5F` (117), `neighbor.f = neighbor.g + neighbor.h` (121); `getBestH` 153-163 is the min straight-line distance over all targets. One exception the claim does not name: the START node's own `h` is set unfudged (`from.h = this.getBestH(from, targets)`, lines 72-74) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's A* pathfinding search terminates the instant any target comes within Manhattan `reach_range` of the current best node, or once `max_visited_nodes` expansions have been used, whichever comes first. | `PathFinder.findPath`, PathFinder.java:83-100 — `if (++count >= maxVisitedNodesAdjusted) break;` at the loop head (84-86), then `current.distanceManhattan(target) <= reachRange` marks targets reached (91-96) and `if (!reachedTargets.isEmpty()) break;` (98-100). The expansion-budget check is evaluated before the pop and before the reach test in each iteration | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's pathfinding falls back to a best-effort route toward the closest-approached node when a search never reaches any target, rather than returning no path at all. | `PathFinder.findPath`, PathFinder.java:129-135 — when `reachedTargets` is empty it reconstructs from every target's `getBestNode()` with `reached=false` and picks `min(getDistToTarget, then getNodeCount)`; `getBestH` and `Target.updateBest` (PathFinder.java:153-163) is what tracks the closest-approached node | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `Path` type applies no further geometric smoothing to the raw pathfinding node sequence beyond storing nodes and advancing between them. | Path.java as a whole — the node list is stored verbatim from `PathFinder.reconstructPath` (PathFinder.java:165-176) and the type only exposes `advance()` (Path.java:30-32), indexing and `getEntityPosAtNode` (72-78); there is no smoothing or string-pulling step. Note: corner-cutting shortcuts do exist, but in the navigation layer (`PathNavigation.shouldTargetNextNodeInDirection`, PathNavigation.java:264-292), not in `Path` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's real per-node path-advancement radius scales with the entity's own bounding-box width, rather than using one fixed distance threshold for every entity. | `PathNavigation.followThePath`, PathNavigation.java:247-262 — `maxDistanceToWaypoint = mob.getBbWidth() > 0.75F ? mob.getBbWidth() / 2.0F : 0.75F - mob.getBbWidth() / 2.0F` (line 249), recomputed every call; the 0.5F field initialiser at line 48 is only a pre-first-tick placeholder | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's navigation recompute throttle (`MAX_TIME_RECOMPUTE`) is 20 ticks. | PathNavigation.java:34 (`MAX_TIME_RECOMPUTE = 20`), used as the literal `20L` in `recomputePath` at line 102 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's navigation recompute throttle still resets its cooldown window even when a path search fails to find a route, so a failed search consumes the same throttle period as a successful one. | `PathNavigation.recomputePath`, PathNavigation.java:101-110 — `this.path = this.createPath(...)` at 105-106 may return null, and `this.timeLastRecompute = this.level.getGameTime()` at 107 runs unconditionally on that branch | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's navigation stuck-check interval (`STUCK_CHECK_INTERVAL`) is 100 ticks. | PathNavigation.java:35 (`STUCK_CHECK_INTERVAL = 100`), used as the literal `100` in `doStuckDetection` at line 295 (`this.tick - this.lastStuckCheck > 100`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's navigation stuck-detection distance factor (`STUCK_THRESHOLD_DISTANCE_FACTOR`) is 0.25. | PathNavigation.java:36 (`STUCK_THRESHOLD_DISTANCE_FACTOR = 0.25F`), used as the literal `0.25F` in `doStuckDetection` at line 297 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's navigation stuck check flags an entity as stuck when the squared distance it moved since the last check is below `(effective_speed x 25.0) squared`, where `effective_speed` is the mob's own current speed value (squared when below 1.0) and 25.0 is `STUCK_CHECK_INTERVAL x STUCK_THRESHOLD_DISTANCE_FACTOR` (100 x 0.25), not a flat `x 20.0` multiplier applied outside the square. | `PathNavigation.doStuckDetection`, PathNavigation.java:294-307. Correction: the reference computes `effectiveSpeed = mob.getSpeed() >= 1.0F ? mob.getSpeed() : mob.getSpeed() * mob.getSpeed()` (296), then `thresholdDistance = effectiveSpeed * 100.0F * 0.25F` (297) — that is STUCK_CHECK_INTERVAL (100), not 20.0 — and flags stuck when `mobPos.distanceToSqr(lastStuckCheckPos) < thresholdDistance * thresholdDistance` (298). So the threshold is `(effectiveSpeed * 25.0)^2`, the 0.25 factor sits INSIDE the squared term rather than multiplying it, and the input is `Mob.getSpeed()` (the move-control-set speed field) squared when below 1.0 — not the raw MOVEMENT_SPEED attribute | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `MoveControl.MAX_TURN` constant is 90 degrees per tick. | MoveControl.java:18 (`protected static final int MAX_TURN = 90;`), applied as the literal `90.0F` cap passed to `rotlerp` at line 96; `rotlerp` clamps the wrapped delta to plus or minus max at lines 132-140 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's yaw convention computes a desired yaw as `atan2(dz, dx)` in degrees, minus 90.0. | `MoveControl.tick`, MoveControl.java:95 — `float yRotD = (float)(Mth.atan2(zd, xd) * 180.0F / (float)Math.PI) - 90.0F;`, matched by `LookControl.getYRotD` LookControl.java:102 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's look-control pitch convention computes pitch as the negative of `atan2(dy, horizontal_distance)` in degrees (a down-positive pitch convention). | `LookControl.getXRotD`, LookControl.java:94 — `Optional.of((float)(-(Mth.atan2(yd, sd) * 180.0F / (float)Math.PI)))` where `sd` is the horizontal distance | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's look-control pitch turn rate is a separate, smaller constant than the yaw turn rate in some contexts, rather than sharing one shared per-tick turn-rate limit. | LookControl.java:11-12 (`yMaxRotSpeed` and `xMaxRotAngle` are two separate fields), applied separately at 54-55. Contexts where the pitch limit is the smaller of the two exist — e.g. `setLookAt(target, 60.0F, 30.0F)` and `setLookAt(pos, 180.0F, 20.0F)`. Note for the implementer: the DEFAULT pairing is the other way round — `LookControl.setLookAt(x, y, z)` (LookControl.java:34-36) uses `mob.getHeadRotSpeed()` = 10 (Mob.java:776-778) for yaw and `mob.getMaxHeadXRot()` = 40 (Mob.java:759-761) for pitch | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla, a mob's full one-block pathfinding step-up is resolved by a discrete jump impulse -> `MoveControl` fires the jump control when the required rise exceeds the mob's own `STEP_HEIGHT` attribute (default 0.6, exceeded by a 1.0 rise); continuous ground/step-height contact only resolves rises up to that attribute's own value. | `MoveControl.tick`, MoveControl.java:101-108. Correction: a full one-block rise IS resolved by a discrete jump impulse. The branch is `if (yd > this.mob.maxUpStep() and xd*xd + zd*zd < Math.max(1.0F, this.mob.getBbWidth()) or <collision-shape clause>) { this.mob.getJumpControl().jump(); this.operation = Operation.JUMPING; }`, and with `STEP_HEIGHT`'s default 0.6 (Attributes.java:98-100) a `yd` of 1.0 exceeds `maxUpStep()`, so `JumpControl.jump()` fires. Continuous step-height contact only resolves rises up to `maxUpStep()` (slabs, stairs, single carpet-height steps); the pathfinder's `jumpSize = 1` (WalkNodeEvaluator.java:130) deliberately admits candidates the mob must jump to reach | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla, the per-tick sensing seen/unseen line-of-sight cache is cleared every tick, before any line-of-sight check that tick. | `Sensing.tick`, Sensing.java:19-22 — clears both the `seen` and `unseen` IntSets; `Mob.serverAiStep` calls it at Mob.java:719-721, before either goal selector and before `navigation.tick()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's real line-of-sight test uses a block's exact partial `VoxelShape` for occlusion, not full-cube-only opacity. | `LivingEntity.hasLineOfSight`, LivingEntity.java:3328-3330 — clips with `ClipContext.Block.COLLIDER`, i.e. the block's own `getCollisionShape` VoxelShape rather than a full-cube opacity test (the transparent-block variant at LivingEntity.java:1767-1769 selects `ClipContext.Block.VISUAL` instead) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `AttributeModifierOperation` enum has three values in this order: `AddValue` = 0, `AddMultipliedBase` = 1, `AddMultipliedTotal` = 2. | `AttributeModifier.Operation`, AttributeModifier.java:41-44 — `ADD_VALUE("add_value", 0)`, `ADD_MULTIPLIED_BASE("add_multiplied_base", 1)`, `ADD_MULTIPLIED_TOTAL("add_multiplied_total", 2)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's attribute-modifier `addOrReplacePermanentModifier` semantics replace any existing modifier sharing the same modifier id, rather than adding a duplicate. | `AttributeInstance.addOrReplacePermanentModifier`, AttributeInstance.java:95-99 — `removeModifier(modifier.id()); addModifier(modifier); permanentModifiers.put(modifier.id(), modifier);`, versus `addPermanentModifier` (101-104) which omits the removal | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla keys attribute modifiers by a namespaced string identifier rather than the older UUID-based scheme. | `AttributeModifier` record header, AttributeModifier.java:14 — `record AttributeModifier(Identifier id, double amount, Operation operation)`; the wire and codec forms both carry that `Identifier` (AttributeModifier.java:15-32) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla attribute modifiers marked permanent survive an NBT save/reload, while transient (potion-effect-style) modifiers do not. | `AttributeInstance.pack`, AttributeInstance.java:186-188 — the packed form carries `List.copyOf(this.permanentModifiers.values())` only; `addTransientModifier` (AttributeInstance.java:91-93) never touches `permanentModifiers`, and `apply` (194-201) re-adds only what was packed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's attribute value calculation runs its four stages -> AddValue, AddMultipliedBase, AddMultipliedTotal, then clamping -> in that fixed order. | `AttributeInstance.calculateValue`, AttributeInstance.java:148-165 — ADD_VALUE loop 151-153, ADD_MULTIPLIED_BASE loop 157-159, ADD_MULTIPLIED_TOTAL loop 161-163, `sanitizeValue` at 165 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's attribute value calculation begins from the base value and adds every `AddValue` modifier's amount. | AttributeInstance.java:149-153 — `double base = this.getBaseValue();` then `base += modifier.amount();` for each ADD_VALUE modifier | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's attribute value calculation applies every `AddMultipliedBase` modifier by adding `base x amount` computed against the original base value each time, so multiple such modifiers are mutually additive rather than compounding against each other. | AttributeInstance.java:155-159 — `double result = base;` then `result += base * modifier.amount();`, `base` being the post-ADD_VALUE value captured once before the loop | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's attribute value calculation applies every `AddMultipliedTotal` modifier by multiplying the running result by `1.0 + amount`, sequentially, so multiple such modifiers do compound against each other. | AttributeInstance.java:161-163 — `result *= 1.0 + modifier.amount();` inside the loop, so successive modifiers compound | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's attribute value calculation clamps its final result to the attribute's `[min, max]` range. | AttributeInstance.java:165 with `RangedAttribute.sanitizeValue` RangedAttribute.java:35-37 — `Double.isNaN(value) ? this.minValue : Mth.clamp(value, this.minValue, this.maxValue)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla applies a per-instance `FOLLOW_RANGE` individuality bonus at mob spawn time, drawn from a triangular distribution and consuming the region's shared RNG in a fixed relative order. | `Mob.finalizeSpawn`, Mob.java:1083-1096 — `RandomSource random = level.getRandom();` then, when `RANDOM_SPAWN_BONUS_ID` is not already present, `followRange.addPermanentModifier(new AttributeModifier(RANDOM_SPAWN_BONUS_ID, random.triangle(0.0, 0.11485000000000001), Operation.ADD_MULTIPLIED_BASE))`, followed by `this.setLeftHanded(random.nextFloat() < 0.05F)` — a fixed two-draw order off the level's shared RNG | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `MAX_HEALTH` attribute has a registry default value of 20.0 with range [1, 1024]. | Attributes.java:68-70 — `new RangedAttribute("attribute.name.max_health", 20.0, 1.0, 1024.0).setSyncable(true)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `MOVEMENT_SPEED` attribute has a registry default value of 0.7 with range [0, 1024]. | Attributes.java:77-79 — `new RangedAttribute("attribute.name.movement_speed", 0.7, 0.0, 1024.0).setSyncable(true)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `FOLLOW_RANGE` attribute has a registry default value of 32.0 with range [0, 2048]. | Attributes.java:51 — `new RangedAttribute("attribute.name.follow_range", 32.0, 0.0, 2048.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `ATTACK_DAMAGE` attribute has a registry default value of 2.0 with range [0, 2048]. | Attributes.java:17 — `new RangedAttribute("attribute.name.attack_damage", 2.0, 0.0, 2048.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `ATTACK_KNOCKBACK` attribute has a registry default value of 0.0 with range [0, 5]. | Attributes.java:18 — `new RangedAttribute("attribute.name.attack_knockback", 0.0, 0.0, 5.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `KNOCKBACK_RESISTANCE` attribute has a registry default value of 0.0 with range [-2, 1]. | Attributes.java:61-63 — `new RangedAttribute("attribute.name.knockback_resistance", 0.0, -2.0, 1.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `ARMOR` attribute has a registry default value of 0.0 with range [0, 30]. | Attributes.java:13 — `new RangedAttribute("attribute.name.armor", 0.0, 0.0, 30.0).setSyncable(true)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `ARMOR_TOUGHNESS` attribute has a registry default value of 0.0 with range [0, 20]. | Attributes.java:14-16 — `new RangedAttribute("attribute.name.armor_toughness", 0.0, 0.0, 20.0).setSyncable(true)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `STEP_HEIGHT` attribute has a registry default value of 0.6 with range [0, 10]. | Attributes.java:98-100 — `new RangedAttribute("attribute.name.step_height", 0.6, 0.0, 10.0).setSyncable(true)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `JUMP_STRENGTH` attribute has a registry default value of 0.42 with range [0, 32]. | Attributes.java:58-60 — `new RangedAttribute("attribute.name.jump_strength", 0.42F, 0.0, 32.0).setSyncable(true)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie has a `MAX_HEALTH` attribute value of 20.0. | `Zombie.createAttributes`, Zombie.java:131-138 — MAX_HEALTH is not overridden, so it inherits the registry default 20.0 through `Monster.createMonsterAttributes` (Monster.java:119-121) and `LivingEntity.createLivingAttributes` (LivingEntity.java:332-334) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie has a `MOVEMENT_SPEED` attribute value of 0.23. | `Zombie.createAttributes`, Zombie.java:134 — `.add(Attributes.MOVEMENT_SPEED, 0.23F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie has a `FOLLOW_RANGE` attribute value of 35.0. | `Zombie.createAttributes`, Zombie.java:133 — `.add(Attributes.FOLLOW_RANGE, 35.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie has an `ATTACK_DAMAGE` attribute value of 3.0. | `Zombie.createAttributes`, Zombie.java:135 — `.add(Attributes.ATTACK_DAMAGE, 3.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager has a `MAX_HEALTH` attribute value of 20.0. | `Villager.createAttributes`, Villager.java:227-229 — MAX_HEALTH not overridden, inherits the registry default 20.0 via `Mob.createMobAttributes` (Mob.java:166-168) and `LivingEntity.createLivingAttributes` (LivingEntity.java:332-334) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager has a `MOVEMENT_SPEED` attribute value of 0.5. | `Villager.createAttributes`, Villager.java:227-229 — `Mob.createMobAttributes().add(Attributes.MOVEMENT_SPEED, 0.5)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager has a `FOLLOW_RANGE` attribute value of 16.0, not 32.0 -> `Mob.createMobAttributes` overrides the registry default of 32.0 with 16.0 for every Mob, and Villager never re-adds `FOLLOW_RANGE`. | `Villager.createAttributes`, Villager.java:227-229 and `Mob.createMobAttributes`, Mob.java:166-168. Correction: the Villager's FOLLOW_RANGE base value is 16.0, not 32.0 — `Mob.createMobAttributes()` is `LivingEntity.createLivingAttributes().add(Attributes.FOLLOW_RANGE, 16.0)`, overriding the registry default of 32.0 for every Mob, and Villager does not override it again. The only per-instance addition is the `finalizeSpawn` triangular ADD_MULTIPLIED_BASE bonus (Mob.java:1087-1092) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager has no `ATTACK_DAMAGE` attribute at all, so there is no 0.0 value -> `ATTACK_DAMAGE` is added only by `Monster.createMonsterAttributes`, and querying an absent attribute throws rather than yielding a default; the villager's own lack of a melee attack follows from this absence, not from a zero value. | `Villager.createAttributes`, Villager.java:227-229; `Mob.createMobAttributes` Mob.java:166-168; `LivingEntity.createLivingAttributes` LivingEntity.java:332-359; `Monster.createMonsterAttributes` Monster.java:119-121. Correction: the Villager has NO ATTACK_DAMAGE attribute at all, so there is no 0.0 value — ATTACK_DAMAGE is added only by `Monster.createMonsterAttributes`, and `AttributeMap.getValue` (AttributeMap.java:58-61) falls through to `AttributeSupplier.getValue`, which throws `IllegalArgumentException("Can't find attribute ...")` for an unregistered attribute (AttributeSupplier.java:17-28). The "no melee attack" half of the claim is right; the mechanism is absence of the attribute, not a zero value | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow has a `MAX_HEALTH` attribute value of 10.0. | `AbstractCow.createAttributes`, AbstractCow.java:56-58 — `.add(Attributes.MAX_HEALTH, 10.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow has a `MOVEMENT_SPEED` attribute value of 0.2. | `AbstractCow.createAttributes`, AbstractCow.java:56-58 — `.add(Attributes.MOVEMENT_SPEED, 0.2F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow has a `FOLLOW_RANGE` attribute value of 16.0, not 32.0 -> `Mob.createMobAttributes` overrides the registry default of 32.0 with 16.0 for every Mob, and neither `Animal` nor `AbstractCow` re-adds `FOLLOW_RANGE`. | `AbstractCow.createAttributes` AbstractCow.java:56-58 to `Animal.createAnimalAttributes` Animal.java:55-57 to `Mob.createMobAttributes` Mob.java:166-168. Correction: the Cow's FOLLOW_RANGE base value is 16.0, not 32.0 — `Mob.createMobAttributes()` overrides the registry default 32.0 with 16.0 for every Mob, and neither `Animal` nor `AbstractCow` overrides it again | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow has no `ATTACK_DAMAGE` attribute at all, so there is no 0.0 value -> the animal attribute chain never adds it, and querying an absent attribute throws rather than yielding a default. | `AbstractCow.createAttributes` AbstractCow.java:56-58, `Animal.createAnimalAttributes` Animal.java:55-57, `LivingEntity.createLivingAttributes` LivingEntity.java:332-359. Correction: the Cow has NO ATTACK_DAMAGE attribute at all — the animal chain never adds it (only `Monster.createMonsterAttributes`, Monster.java:119-121, does), so querying it throws `IllegalArgumentException` via `AttributeMap.getValue` (AttributeMap.java:58-61) and `AttributeSupplier.getValue` (AttributeSupplier.java:17-28) rather than yielding 0.0 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie entity hitbox is 0.6 blocks wide by 1.95 blocks tall. | `EntityTypes.ZOMBIE`, EntityTypes.java:1089-1096 — `.sized(0.6F, 1.95F)` with `.eyeHeight(1.74F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager entity hitbox is 0.6 blocks wide by 1.95 blocks tall. | `EntityTypes.VILLAGER`, EntityTypes.java:1004-1005 — `.sized(0.6F, 1.95F).eyeHeight(1.62F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow entity hitbox is 0.9 blocks wide by 1.4 blocks tall. | `EntityTypes.COW`, EntityTypes.java:306-308 — `.sized(0.9F, 1.4F).eyeHeight(1.3F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's clientbound `Update Attributes` (`update_attributes`) packet has id `0x83`. | datagen `reports/packets.json`, `play.clientbound.minecraft:update_attributes` = `{"protocol_id": 131}`, and 131 = 0x83 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `Update Attributes` packet layout is, in order: `entity_id` (VarInt), `count` (VarInt), then per entry: `attribute_id` (VarInt registry id), `base_value` (Double), `modifier_count` (VarInt), then per modifier: `id` (Identifier, VarInt-length-prefixed UTF-8 `namespace:path`), `amount` (Double), `operation` (VarInt: 0=AddValue, 1=AddMultipliedBase, 2=AddMultipliedTotal). | `ClientboundUpdateAttributesPacket.STREAM_CODEC`, ClientboundUpdateAttributesPacket.java:20-26 (VAR_INT entity id, then `AttributeSnapshot.STREAM_CODEC.apply(ByteBufCodecs.list(128))` = VarInt count then entries) and `AttributeSnapshot.STREAM_CODEC` 71-79 (`Attribute.STREAM_CODEC` = `ByteBufCodecs.holderRegistry(Registries.ATTRIBUTE)`, Attribute.java:14, a VarInt registry id; then DOUBLE base; then `MODIFIER_STREAM_CODEC.apply(ByteBufCodecs.collection(...))` = VarInt count then entries) and `MODIFIER_STREAM_CODEC` 62-70 (`Identifier.STREAM_CODEC`, DOUBLE amount, `Operation.STREAM_CODEC` = `ByteBufCodecs.idMapper` which reads and writes a VarInt, ByteBufCodecs.java:546-559, over the ids 0/1/2 of AttributeModifier.java:42-44) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie goal selector registers no `FloatGoal` and no priority-0 goal at all -> `Zombie.registerGoals` never calls into the shared empty base registration; a zombie deliberately sinks and walks along the bottom rather than floating. | `Zombie.registerGoals` Zombie.java:112-117 and `Zombie.addBehaviourGoals` Zombie.java:119-129 — the complete goal registration for a Zombie. Correction: the Zombie registers NO FloatGoal at any priority in 26.2, and there is no priority-0 entry at all; `Zombie.registerGoals` does not call `super.registerGoals()`, and the inherited `Mob.registerGoals` (Mob.java:163-164) is empty. `FloatGoal` itself exists (FloatGoal.java:7-31, flags `{JUMP}`, `requiresUpdateEveryTick() == true`, jumps with probability 0.8 per tick) and is registered by 28 other entity classes, but a Zombie deliberately sinks and walks along the bottom (the drowning-to-Drowned path) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie goal selector registers `ZombieAttackGoal` at priority 3, not 2, with the MOVE and LOOK flags -> priority 2 in the same block is held by a ranged `SpearUseGoal`. | `Zombie.addBehaviourGoals`, Zombie.java:121. Correction: `ZombieAttackGoal` is registered at priority 3, not 2 — `this.goalSelector.addGoal(3, new ZombieAttackGoal(this, 1.0, false));`. Priority 2 is held by `new SpearUseGoal<>(this, 1.0, 1.0, 10.0F, 2.0F)` (Zombie.java:120). The flag half of the claim is right: `ZombieAttackGoal` (ZombieAttackGoal.java:5) extends `MeleeAttackGoal`, whose constructor sets `EnumSet.of(Goal.Flag.MOVE, Goal.Flag.LOOK)` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie goal selector registers `WaterAvoidingRandomStrollGoal` at priority 7 with the MOVE flag. | `Zombie.addBehaviourGoals`, Zombie.java:123 — `this.goalSelector.addGoal(7, new WaterAvoidingRandomStrollGoal(this, 1.0));`; `WaterAvoidingRandomStrollGoal` (WaterAvoidingRandomStrollGoal.java:8) extends `RandomStrollGoal`, whose constructor sets `EnumSet.of(Goal.Flag.MOVE)` (RandomStrollGoal.java:33) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie goal selector registers `LookAtPlayerGoal` and `RandomLookAroundGoal` both at priority 8, mutually exclusive via the priority tie, but `RandomLookAroundGoal`'s own flag set is MOVE and LOOK together, not LOOK alone -> it also contends for the MOVE lock against the priority-7 stroll goal; only `LookAtPlayerGoal` is LOOK-only. | `Zombie.registerGoals`, Zombie.java:114-115 — both are registered at priority 8, and with equal priorities `WrappedGoal.canBeReplacedBy` (WrappedGoal.java:16-18) can never fire in either direction, so they are mutually exclusive. Correction to the flag half: `RandomLookAroundGoal` sets `EnumSet.of(Goal.Flag.MOVE, Goal.Flag.LOOK)` (RandomLookAroundGoal.java:14), not LOOK alone — so it also contends for the MOVE lock against `WaterAvoidingRandomStrollGoal` at priority 7, which the LOOK-only reading would miss. `LookAtPlayerGoal` alone is LOOK-only (`setFlags(EnumSet.of(Goal.Flag.LOOK))`) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie target selector registers `HurtByTargetGoal` at priority 1 with the TARGET flag. | `Zombie.addBehaviourGoals`, Zombie.java:124 — `this.targetSelector.addGoal(1, new HurtByTargetGoal(this).setAlertOthers(ZombifiedPiglin.class));`; `HurtByTargetGoal` sets `EnumSet.of(Goal.Flag.TARGET)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie target selector registers `NearestAttackableTargetGoal<Player>` at priority 2 with the TARGET flag. | `Zombie.addBehaviourGoals`, Zombie.java:125 — `this.targetSelector.addGoal(2, new NearestAttackableTargetGoal<>(this, Player.class, true));`; `NearestAttackableTargetGoal` sets `EnumSet.of(Goal.Flag.TARGET)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `RandomStrollGoal`-family goals have roughly a 1-in-120 chance per tick to start when there is no current walk target. | `RandomStrollGoal`, RandomStrollGoal.java:10 (`DEFAULT_INTERVAL = 120`), 20-22 (no-arg interval defaults to 120) and `canUse` 36-50 — the gate is `this.mob.getRandom().nextInt(reducedTickDelay(this.interval)) != 0` (line 47), i.e. `nextInt(Mth.positiveCeilDiv(120, 2)) = nextInt(60)`, a 1-in-60 draw per goal-selector start pass; because `Mob.serverAiStep` only runs the start pass every other tick (Mob.java:722-737), that is about 1-in-120 per game tick. The reference's other preconditions are `!mob.hasControllingPassenger()` and, when `checkNoActionTime`, `mob.getNoActionTime() < 100` — there is no explicit walk-target check, the goal simply is not re-evaluated while it is running | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Zombie `LookAtPlayerGoal` activates when a player is within roughly 8 blocks of the entity. | `Zombie.registerGoals`, Zombie.java:114 — `new LookAtPlayerGoal(this, Player.class, 8.0F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow goal selector registers `FloatGoal` at priority 0 with the JUMP flag. | `AbstractCow.registerGoals`, AbstractCow.java:41 — `this.goalSelector.addGoal(0, new FloatGoal(this));`; `FloatGoal` sets `EnumSet.of(Goal.Flag.JUMP)` (FloatGoal.java:12) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow goal selector registers `PanicGoal` at priority 1 with the MOVE flag. | `AbstractCow.registerGoals`, AbstractCow.java:42 — `this.goalSelector.addGoal(1, new PanicGoal(this, 2.0));`; `PanicGoal` sets `EnumSet.of(Goal.Flag.MOVE)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow goal selector registers `BreedGoal` at priority 2, but its own flag set is MOVE and LOOK together, not MOVE alone -> it also takes and holds the LOOK lock while running. | `AbstractCow.registerGoals`, AbstractCow.java:43 — priority 2 is right. Correction: `BreedGoal`'s flag set is `EnumSet.of(Goal.Flag.MOVE, Goal.Flag.LOOK)`, not MOVE alone, so it also takes and holds the LOOK lock while running | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow goal selector registers `TemptGoal` at priority 3, but its own flag set is MOVE and LOOK together, not MOVE alone. | `AbstractCow.registerGoals`, AbstractCow.java:44 — priority 3 is right (`new TemptGoal(this, 1.25, i -> i.is(ItemTags.COW_FOOD), false)`). Correction: `TemptGoal`'s flag set is `EnumSet.of(Goal.Flag.MOVE, Goal.Flag.LOOK)`, not MOVE alone | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow goal selector registers `FollowParentGoal` at priority 4, but it never calls `setFlags` at all -> its flag set is EMPTY, so it locks nothing and is never blocked by a flag lock, materially different from holding MOVE. | `AbstractCow.registerGoals`, AbstractCow.java:45 — priority 4 is right (`new FollowParentGoal(this, 1.25)`). Correction: `FollowParentGoal` never calls `setFlags` (FollowParentGoal.java:16-19), so its flag set is EMPTY — it locks nothing and is never blocked by a flag lock, which is materially different from holding MOVE | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow goal selector registers `WaterAvoidingRandomStrollGoal` at priority 5 with the MOVE flag. | `AbstractCow.registerGoals`, AbstractCow.java:46 — `this.goalSelector.addGoal(5, new WaterAvoidingRandomStrollGoal(this, 1.0));`, flags `{MOVE}` inherited from `RandomStrollGoal` (RandomStrollGoal.java:33) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow goal selector registers `LookAtPlayerGoal` at priority 6 with the LOOK flag. | `AbstractCow.registerGoals`, AbstractCow.java:47 — `this.goalSelector.addGoal(6, new LookAtPlayerGoal(this, Player.class, 6.0F));`, flags `{LOOK}` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow goal selector registers `RandomLookAroundGoal` at priority 7, but its own flag set is MOVE and LOOK together, not LOOK alone -> it also contends for the MOVE lock against the priority-5 stroll goal. | `AbstractCow.registerGoals`, AbstractCow.java:48 — priority 7 is right. Correction: `RandomLookAroundGoal`'s flag set is `EnumSet.of(Goal.Flag.MOVE, Goal.Flag.LOOK)` (RandomLookAroundGoal.java:14), not LOOK alone, so at priority 7 it also contends for MOVE against the priority-5 stroll goal | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Cow is never hostile and has no target-selector goals. | `AbstractCow.registerGoals`, AbstractCow.java:40-49 — the method registers eight `goalSelector` entries and zero `targetSelector` entries, and does not call `super.registerGoals()`; the inherited `Mob.registerGoals` (Mob.java:163-164) is empty, so a Cow's `targetSelector` stays empty | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager brain has a Core activity (always active) running `SwimBehavior` and `LookAtTargetSink`. | `Villager.BRAIN_PROVIDER`, Villager.java:156 — `ActivityData.create(Activity.CORE, VillagerGoalPackages.getCorePackage(profession, 0.5F))`; CORE is always active because `Brain`'s constructors set `setCoreActivities(ImmutableSet.of(Activity.CORE))` (Brain.java:101, 110) and `setActiveActivity` always re-adds `coreActivities` (Brain.java:309-311). `getCorePackage` (VillagerGoalPackages.java:35-71) does contain `new Swim(0.8F)` and `new LookAtTargetSink(45, 90)`, both at priority 0. Scope note for the implementer: the real Core package holds 17 entries, of which these two are the M4 subset | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager brain has an Idle default-fallback activity running a `WaterAvoidingRandomStrollGoal`-equivalent stroll behavior and an `InteractWithNearestVillager` behavior. | `Villager.BRAIN_PROVIDER`, Villager.java:163 — `ActivityData.create(Activity.IDLE, VillagerGoalPackages.getIdlePackage(0.5F))`; IDLE is the default-fallback because `Brain.defaultActivity` is initialised to `Activity.IDLE` (Brain.java:49) and Villager never calls `setDefaultActivity`. `getIdlePackage` (VillagerGoalPackages.java:185-224) holds both named behaviors inside one priority-2 `RunOne` weighted gate — `InteractWith.of(EntityTypes.VILLAGER, 8, MemoryModuleType.INTERACTION_TARGET, speedModifier, 2)` at weight 2 and `VillageBoundRandomStroll.create(speedModifier)` at weight 1 — alongside five other weighted entries and six further priority-3-and-99 behaviors | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager brain has a Panic activity carrying NO memory requirement at all -> it is entered by a trigger behavior in the Core package whenever `HurtBy` or `NearestHostile` is present, running `VillagerCalmDown` plus two separate flee behaviors, one fleeing `NearestHostile` and one fleeing `HurtByEntity`. | `Villager.BRAIN_PROVIDER`, Villager.java:164, and `ActivityData.create(Activity, ImmutableList)` ActivityData.java:44-48. Correction: the PANIC activity carries NO memory requirement — the two-argument `create` sets `conditions = ImmutableSet.of()`, so `activityRequirementsAreMet` is satisfied by an empty requirement list; panic is entered by the `VillagerPanicTrigger` behavior in the Core package (VillagerGoalPackages.java:42), not by a HurtBy ActivityRequirement. The behavior half is right in spirit: `getPanicPackage` (VillagerGoalPackages.java:231-240) runs `VillagerCalmDown.create()` at priority 0 and TWO flee behaviors at priority 1 — `SetWalkTargetAwayFrom.entity(MemoryModuleType.NEAREST_HOSTILE, runawaySpeed, 6, false)` and `SetWalkTargetAwayFrom.entity(MemoryModuleType.HURT_BY_ENTITY, runawaySpeed, 6, false)` — plus a priority-3 `VillageBoundRandomStroll` and a minimal look behavior | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager panic-flee behavior multiplies the goal package's own speed modifier by 1.5, not the `MOVEMENT_SPEED` attribute directly -> `MoveControl` then multiplies that resulting walk-target speed modifier by the `MOVEMENT_SPEED` attribute as usual, netting 0.75x the attribute for a villager's own 0.5 package modifier, never 1.5x the attribute. | `VillagerGoalPackages.getPanicPackage`, VillagerGoalPackages.java:231-240. Correction: the 1.5 factor multiplies the PACKAGE's speed modifier, not the MOVEMENT_SPEED attribute — `float runawaySpeed = speedModifier * 1.5F;` with `speedModifier` = 0.5F as passed from `Villager.BRAIN_PROVIDER` (Villager.java:164), giving a walk-target speed modifier of 0.75. `MoveControl.tick` then computes the actual speed as `speedModifier * mob.getAttributeValue(Attributes.MOVEMENT_SPEED)` (MoveControl.java:97), i.e. 0.75x the attribute, not 1.5x. The direction half is right: `SetWalkTargetAwayFrom.entity(MemoryModuleType.HURT_BY_ENTITY, runawaySpeed, 6, false)` flees the hurt-by entity's position at radius 6 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager brain has `Work` and `Meet` activities gated by the `JobSite` and `MeetingPoint` memories respectively, but `Rest` carries no memory requirement at all -> there is no `Home` gate on the REST activity in vanilla. | `Villager.BRAIN_PROVIDER`, Villager.java:147-164, with `ActivityData.create` overloads at ActivityData.java:44-48 and 50-56. Correction: only WORK and MEET are gated. WORK carries `ImmutableSet.of(Pair.of(MemoryModuleType.JOB_SITE, MemoryStatus.VALUE_PRESENT))` (Villager.java:148-152) and MEET carries `ImmutableSet.of(Pair.of(MemoryModuleType.MEETING_POINT, MemoryStatus.VALUE_PRESENT))` (Villager.java:158-160). REST is registered with the two-argument `ActivityData.create(Activity.REST, VillagerGoalPackages.getRestPackage(0.5F))` (Villager.java:162), which sets an EMPTY condition set — there is no HOME requirement on the REST activity in 26.2 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager brain uses NINE sensor types in total, not eight -> the omitted one is `NearestLivingEntitySensor`, which populates the nearest-living-entities memories several of the villager's other sensors read from. | `Villager.BRAIN_PROVIDER` sensor list, Villager.java:130-140. Correction: the Villager registers NINE sensor types, not eight. The list is, in declaration order, `SensorType.NEAREST_LIVING_ENTITIES`, `NEAREST_PLAYERS`, `NEAREST_ITEMS`, `NEAREST_BED`, `HURT_BY`, `VILLAGER_HOSTILES`, `VILLAGER_BABIES`, `SECONDARY_POIS`, `GOLEM_DETECTED`. The blueprint's eight names are all real members; the omitted one is `NEAREST_LIVING_ENTITIES`, the sensor that populates the NEAREST_LIVING_ENTITIES and NEAREST_VISIBLE_LIVING_ENTITIES memories every other villager sensor and behavior reads from | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Brain system has 116 distinct memory module types in total, not roughly 90. | `MemoryModuleType.java` — 116 `public static final MemoryModuleType` declarations; cross-checked against datagen `reports/registries.json`, `minecraft:memory_module_type` = 116 entries. Correction: the count is 116, not roughly 90 (for reference, the sibling registries are `minecraft:sensor_type` = 24 and `minecraft:activity` = 26) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
