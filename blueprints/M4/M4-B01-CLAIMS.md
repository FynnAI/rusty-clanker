# M4-B01 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Vanilla's Java entity class hierarchy is Entity -> LivingEntity -> Mob -> PathfinderMob -> concrete type, with parallel Item/Projectile/Vehicle branches. | net/minecraft/world/entity/Entity.java:163; LivingEntity.java:141 (extends Entity); Mob.java:99 (extends LivingEntity); PathfinderMob.java:13 (extends Mob); item/ItemEntity.java:39 (extends Entity); projectile/Projectile.java:39 (extends Entity); vehicle/VehicleEntity.java:20 (extends Entity) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Every vanilla entity's NBT root actually carries a Pos field (world position), even though MECH-D30's own field list omits it. | Entity.saveWithoutId, net/minecraft/world/entity/Entity.java:2078-2084 - `Pos` is stored unconditionally for every saved entity (vehicle-relative x/z only when the entity is a passenger) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key Pos is a List<Double> of 3 elements [x, y, z], world-absolute position. | net/minecraft/world/entity/Entity.java:2081-2083 (`store("Pos", Vec3.CODEC, this.position())`); world/phys/Vec3.java:19-24 (`Codec.DOUBLE.listOf()` fixed to size 3, encoded as `[x, y, z]`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key Motion is a List<Double> of 3 elements [dx, dy, dz], measured in blocks/tick. | net/minecraft/world/entity/Entity.java:2086 (`store("Motion", Vec3.CODEC, this.getDeltaMovement())`); Vec3.java:19-24; delta movement is per-tick displacement in blocks | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key Rotation is a List<Float> of 2 elements [yaw, pitch], in degrees. | net/minecraft/world/entity/Entity.java:2087 (`new Vec2(this.getYRot(), this.getXRot())`); world/phys/Vec2.java:18-20 (`Codec.FLOAT.listOf()` fixed to size 2) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key FallDistance is of type Float (moderate confidence; long-stable as Float in every version cross-referenced). | net/minecraft/world/entity/Entity.java:2088 (`output.putDouble("fall_distance", this.fallDistance)`), :2168 (`input.getDoubleOr("fall_distance", 0.0)`), field decl :245 (`public double fallDistance`); util/datafix/fixes/EntityFallDistanceFloatToDoubleFix.java | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key Fire is a Short holding the remaining fire-tick count, where -1 means not on fire and not yet eligible to be set alight this tick (moderate confidence). | net/minecraft/world/entity/Entity.java:2089 (`putShort("Fire", (short)this.remainingFireTicks)`), :2169 (read default `(short)0`), :253 (`private int remainingFireTicks`, Java default 0), :656-658 (`clearFire()` = `min(0, current)`), :540 and :561 ("on fire" test is `remainingFireTicks > 0`) | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key Air is a Short holding remaining breath, with a default TOTAL_AIR_SUPPLY of 300. | net/minecraft/world/entity/Entity.java:2090 (`putShort("Air", (short)this.getAirSupply())`), :194 (`TOTAL_AIR_SUPPLY = 300`), :2805-2807 (`getMaxAirSupply()` returns 300), :319 (metadata default) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key OnGround is a Boolean. | net/minecraft/world/entity/Entity.java:2091 (`putBoolean("OnGround", this.onGround())`), :2171 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key Invulnerable is a Boolean. | net/minecraft/world/entity/Entity.java:2092 (`putBoolean("Invulnerable", this.invulnerable)`), :2172 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key PortalCooldown is an Int. | net/minecraft/world/entity/Entity.java:2093 (`putInt("PortalCooldown", this.portalCooldown)`), :2173 (`getIntOr`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key UUID is stored as an IntArray of 4 elements: vanilla packs a UUID as four big-endian i32 chunks of the 128-bit value, most-significant chunk first, not as a string. | net/minecraft/world/entity/Entity.java:2094 (`store("UUID", UUIDUtil.CODEC, ...)`); core/UUIDUtil.java:23-24 (`Codec.INT_STREAM` fixed to size 4), :67-69 (`leastMostToIntArray` emits msb>>32, msb, lsb>>32, lsb - most-significant chunk first) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key CustomName is an optional String stored as the raw JSON text-component string. | net/minecraft/world/entity/Entity.java:2095 (`storeNullable("CustomName", ComponentSerialization.CODEC, ...)`); network/chat/ComponentSerialization.java:36 and :109-118 - over NbtOps a component that collapses to plain text encodes as a bare NBT TAG_String holding the literal text, anything richer as a TAG_Compound | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key CustomNameVisible is a Boolean. | net/minecraft/world/entity/Entity.java:2096-2098 (`putBoolean("CustomNameVisible", ...)`, written only when true), :2186 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key Silent is a Boolean. | net/minecraft/world/entity/Entity.java:2100-2102 (`putBoolean("Silent", ...)`, written only when true), :2187 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key NoGravity is a Boolean. | net/minecraft/world/entity/Entity.java:2104-2106 (`putBoolean("NoGravity", ...)`, written only when true), :2188 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key Glowing is a Boolean. | net/minecraft/world/entity/Entity.java:2108-2110 (`putBoolean("Glowing", true)`, written only when the glowing tag is set), :2189 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key TicksFrozen is an Int. | net/minecraft/world/entity/Entity.java:2112-2115 (`putInt("TicksFrozen", ...)`, written only when > 0), :2190 (`getIntOr`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key HasVisualFire is a Boolean. | net/minecraft/world/entity/Entity.java:2117-2119 (`putBoolean("HasVisualFire", ...)`, written only when true), :2191 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key Tags is a List<String> of entity scoreboard tags. | net/minecraft/world/entity/Entity.java:2121-2123 (`store("Tags", TAG_LIST_CODEC, ...)`), :196 (`TAG_LIST_CODEC = Codec.STRING.sizeLimitedListOf(1024)`), :2194 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The NBT key Passengers is a List<Compound>; vanilla nests a full recursive entity compound per passenger. | net/minecraft/world/entity/Entity.java:2130-2142 (`output.childrenList("Passengers")`, one child compound per passenger via `saveAsPassenger`, which itself calls `saveWithoutId` recursively), :2059-2069 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| LivingEntity's Health NBT field is a Float. | net/minecraft/world/entity/LivingEntity.java:747 (`output.putFloat("Health", this.getHealth())`), :813 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| LivingEntity's HurtTime NBT field is a Short. | net/minecraft/world/entity/LivingEntity.java:748 (`output.putShort("HurtTime", (short)this.hurtTime)`), :814 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| LivingEntity's DeathTime NBT field is a Short. | net/minecraft/world/entity/LivingEntity.java:749 (`output.putShort("DeathTime", (short)this.deathTime)`), :815 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Arrow count, stinger count, sleeping bed position, and potion-particle state are metadata-only fields in vanilla at the LivingEntity rung -> they have no independent NBT key of their own. | net/minecraft/world/entity/LivingEntity.java:185-189 (arrow count, stinger count and sleeping position are all SynchedEntityData accessors) but :145 and :759 and :825 - the sleeping bed position DOES have its own NBT key, `sleeping_pos`, stored via `BlockPos.CODEC`; arrow count and stinger count genuinely have none; the effect-particle/ambience metadata is derived from `active_effects` (:908-909) | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Potion effects persist to NBT under the ActiveEffects key in vanilla. | net/minecraft/world/entity/LivingEntity.java:143 (`TAG_ACTIVE_EFFECTS = "active_effects"`), :755 (`store("active_effects", MobEffectInstance.CODEC.listOf(), ...)`), :805 (read) | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's item entity NBT key Item is a Compound whose id field is a namespaced string such as "minecraft:diamond". | item/ItemEntity.java:323 (`output.store("Item", ItemStack.CODEC, this.getItem())`), :334; world/item/ItemStack.java:110-120 (`MAP_CODEC` field `id` via `Item.CODEC_WITH_BOUND_COMPONENTS`, a registry-by-name codec, plus `count` and `components`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The item entity's PickupDelay NBT field is a Short. | item/ItemEntity.java:319 (`output.putShort("PickupDelay", (short)this.pickupDelay)`), :331 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The item entity's Age NBT field is a Short implementing MECH-D51's 6000-tick despawn timer. | item/ItemEntity.java:318 (`output.putShort("Age", (short)this.age)`), :330, :43 (`LIFETIME = 6000`), :188 (`this.age >= 6000` discards the entity) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Villager NBT key VillagerData is a Compound of the form { type: String, profession: String, level: Int }. | npc/villager/Villager.java:478 (`output.store("VillagerData", VillagerData.CODEC, ...)`), :494; npc/villager/VillagerData.java:20-34 (`type` and `profession` via `holderByNameCodec()` - namespaced strings - and `level` via `Codec.INT`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity metadata payload (Set Entity Data packet) is a sequence of entries, each (index: u8, type: VarInt, value: type-specific), terminated by a single sentinel byte 0xFF (255) in the index position, with no outer count prefix. | network/protocol/game/ClientboundSetEntityDataPacket.java:15 (`EOF_MARKER = 255`), :24-40 (`pack`/`unpack`: entries written back-to-back, then a single byte 255; no count prefix); network/syncher/SynchedEntityData.java:225-234 (`DataValue.write`: `writeByte(id)`, `writeVarInt(serializerId)`, then the serializer's own payload) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776 a Spawn Entity packet itself carries no inline metadata; a Set Entity Data packet immediately follows for any entity whose fields differ from their type's own defaults. | server/level/ServerEntity.java:280-284 (`getAddEntityPacket` broadcast first, then a separate `ClientboundSetEntityDataPacket` only when `trackedDataValues != null`), :87 and :345 (`trackedDataValues = entityData.getNonDefaultValues()`); SynchedEntityData.java:92-106 (`getNonDefaultValues` returns null when every item is at its default); ClientboundAddEntityPacket.write :105-116 carries no metadata | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 0 to Byte (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:162 (`registerSerializer(BYTE)` - the 1st call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 1 to VarInt (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:163 (`registerSerializer(INT)` - the 2nd call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 2 to VarLong (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:164 (`registerSerializer(LONG)` - the 3rd call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 3 to Float (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:165 (`registerSerializer(FLOAT)` - the 4th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 4 to String (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:166 (`registerSerializer(STRING)` - the 5th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 5 to TextComponent (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:167 (`registerSerializer(COMPONENT)` - the 6th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 6 to OptionalTextComponent (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:168 (`registerSerializer(OPTIONAL_COMPONENT)` - the 7th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 7 to Slot (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:169 (`registerSerializer(ITEM_STACK)` - the 8th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 8 to Boolean (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:170 (`registerSerializer(BOOLEAN)` - the 9th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 9 to Rotations (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:171 (`registerSerializer(ROTATIONS)` - the 10th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 10 to Position (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:172 (`registerSerializer(BLOCK_POS)` - the 11th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 11 to OptionalPosition (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:173 (`registerSerializer(OPTIONAL_BLOCK_POS)` - the 12th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 12 to Direction (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:174 (`registerSerializer(DIRECTION)` - the 13th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 13 to OptionalLivingEntityReference (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:175 (`registerSerializer(OPTIONAL_LIVING_ENTITY_REFERENCE)` - the 14th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 14 to BlockState (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:176 (`registerSerializer(BLOCK_STATE)` - the 15th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 15 to OptionalBlockState (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:177 (`registerSerializer(OPTIONAL_BLOCK_STATE)` - the 16th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 16 to Particle (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:178 (`registerSerializer(PARTICLE)` - the 17th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 17 to Particles (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:179 (`registerSerializer(PARTICLES)` - the 18th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 18 to VillagerData (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:180 (`registerSerializer(VILLAGER_DATA)` - the 19th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 19 to OptionalVarInt (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:181 (`registerSerializer(OPTIONAL_UNSIGNED_INT)` - the 20th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 20 to Pose (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:182 (`registerSerializer(POSE)` - the 21th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 21 to CatVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:183 (`registerSerializer(CAT_VARIANT)` - the 22th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 22 to CatSoundVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:184 (`registerSerializer(CAT_SOUND_VARIANT)` - the 23th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 23 to CowVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:185 (`registerSerializer(COW_VARIANT)` - the 24th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 24 to CowSoundVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:186 (`registerSerializer(COW_SOUND_VARIANT)` - the 25th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 25 to WolfVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:187 (`registerSerializer(WOLF_VARIANT)` - the 26th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 26 to WolfSoundVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:188 (`registerSerializer(WOLF_SOUND_VARIANT)` - the 27th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 27 to FrogVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:189 (`registerSerializer(FROG_VARIANT)` - the 28th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 28 to PigVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:190 (`registerSerializer(PIG_VARIANT)` - the 29th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 29 to PigSoundVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:191 (`registerSerializer(PIG_SOUND_VARIANT)` - the 30th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 30 to ChickenVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:192 (`registerSerializer(CHICKEN_VARIANT)` - the 31th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 31 to ChickenSoundVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:193 (`registerSerializer(CHICKEN_SOUND_VARIANT)` - the 32th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 32 to ZombieNautilusVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:194 (`registerSerializer(ZOMBIE_NAUTILUS_VARIANT)` - the 33th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 33 to OptionalGlobalPosition (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:195 (`registerSerializer(OPTIONAL_GLOBAL_POS)` - the 34th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 34 to PaintingVariant (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:196 (`registerSerializer(PAINTING_VARIANT)` - the 35th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 35 to SnifferState (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:197 (`registerSerializer(SNIFFER_STATE)` - the 36th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 36 to ArmadilloState (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:198 (`registerSerializer(ARMADILLO_STATE)` - the 37th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 37 to CopperGolemState (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:199 (`registerSerializer(COPPER_GOLEM_STATE)` - the 38th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 38 to WeatheringCopperState (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:200 (`registerSerializer(WEATHERING_COPPER_STATE)` - the 39th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 39 to Vector3 (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:201 (`registerSerializer(VECTOR3)` - the 40th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 40 to Quaternion (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:202 (`registerSerializer(QUATERNION)` - the 41th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 41 to ResolvableProfile (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:203 (`registerSerializer(RESOLVABLE_PROFILE)` - the 42th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Java Edition protocol's entity-metadata type-ID table assigns id 42 to HumanoidArm (moderate confidence, from a live wiki fetch, not yet reconciled against a real packets.json). | net/minecraft/network/syncher/EntityDataSerializers.java:204 (`registerSerializer(HUMANOID_ARM)` - the 43th call in the static block at :161-205, which appends to an incremental id map, so registration order is the wire id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity-metadata Byte value's wire payload is 1 byte. | net/minecraft/network/syncher/EntityDataSerializers.java:53 (`BYTE = EntityDataSerializer.forValueType(ByteBufCodecs.BYTE)`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity-metadata VarInt value's wire payload is a VarInt. | net/minecraft/network/syncher/EntityDataSerializers.java:54 (`INT = ... forValueType(ByteBufCodecs.VAR_INT)`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity-metadata Float value's wire payload is 4 bytes, big-endian. | net/minecraft/network/syncher/EntityDataSerializers.java:56 (`FLOAT = ... forValueType(ByteBufCodecs.FLOAT)`); netty `writeFloat` is a 4-byte big-endian IEEE-754 write | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity-metadata String value's wire payload is VarInt-length-prefixed UTF-8. | net/minecraft/network/syncher/EntityDataSerializers.java:57 (`STRING = ... forValueType(ByteBufCodecs.STRING_UTF8)`) - VarInt byte-length prefix then UTF-8 bytes | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity-metadata OptionalTextComponent value's wire payload is a 1-byte present flag followed by the text payload as network-NBT bytes when present. | net/minecraft/network/syncher/EntityDataSerializers.java:59-61 (`OPTIONAL_COMPONENT = ... ComponentSerialization.TRUSTED_OPTIONAL_STREAM_CODEC`); network/codec/ByteBufCodecs.java:373-386 (`optional`: 1-byte boolean flag then the payload) and :356-372 (`fromCodecWithRegistries` writes the value as a network NBT `Tag`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity-metadata Boolean value's wire payload is 1 byte, 0x00 or 0x01. | net/minecraft/network/syncher/EntityDataSerializers.java:88 (`BOOLEAN = ... forValueType(ByteBufCodecs.BOOL)`) - netty `writeBoolean` emits 0x00 or 0x01 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity-metadata OptionalPosition value's wire payload is a present-flag bool followed by the packed-Position i64 when present. | net/minecraft/network/syncher/EntityDataSerializers.java:95-97 (`OPTIONAL_BLOCK_POS = ... BlockPos.STREAM_CODEC.apply(ByteBufCodecs::optional)`); ByteBufCodecs.java:373-386; core/BlockPos.java:38-46 (`FriendlyByteBuf.writeBlockPos` - the packed 64-bit position) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity-metadata Pose value's wire payload is a VarInt holding the pose enum's own ordinal. | net/minecraft/network/syncher/EntityDataSerializers.java:117 (`POSE = ... forValueType(Pose.STREAM_CODEC)`); world/entity/Pose.java:33 (`ByteBufCodecs.idMapper(BY_ID, Pose::id)` - a VarInt of the declared ordinal) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity-metadata VillagerData value's wire payload is three VarInts in the order kind, profession, level. | net/minecraft/network/syncher/EntityDataSerializers.java:105 (`VILLAGER_DATA = ... forValueType(VillagerData.STREAM_CODEC)`); npc/villager/VillagerData.java:36-44 (holderRegistry VILLAGER_TYPE, holderRegistry VILLAGER_PROFESSION, `ByteBufCodecs.VAR_INT` level, in that order); ByteBufCodecs.java:588-590 (`holderRegistry` writes the plain registry id as a VarInt) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The entity-metadata Slot value's wire payload begins with a VarInt item count (0 = empty, encoding nothing further); when nonzero, vanilla continues with a VarInt item id, then a VarInt count of components to add, then a VarInt count of components to remove. | net/minecraft/network/syncher/EntityDataSerializers.java:62-71 (`ITEM_STACK` uses `ItemStack.OPTIONAL_STREAM_CODEC`); world/item/ItemStack.java:124 and :169-190 (VarInt count, `<= 0` yields EMPTY and reads nothing further, else `Item.STREAM_CODEC` registry-id VarInt then the patch); core/component/DataComponentPatch.java:80-95 (VarInt positive/add count, then VarInt negative/remove count) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Pose enum assigns ordinal 0 to Standing and ordinal 2 to Sleeping. | world/entity/Pose.java:12-14 (`STANDING(0, "standing")`, `FALL_FLYING(1, ...)`, `SLEEPING(2, "sleeping")`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 0 to the status-flags byte (fire/sneak/sprint/swim/invisible/glowing/elytra bits), kind Byte, default 0 (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/Entity.java:260 (`DATA_SHARED_FLAGS_ID`, the first `defineId` on Entity, so index 0, serializer BYTE), :318 (default `(byte)0`), :261-267 (bit constants) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 1 to air ticks, kind VarInt, default 300 (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/Entity.java:268 (`DATA_AIR_SUPPLY_ID`, second `defineId`, index 1, serializer INT which is VarInt on the wire), :319 (default `this.getMaxAirSupply()`), :2805-2807 (300) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 2 to custom name, kind OptionalTextComponent, default None (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/Entity.java:269-271 (`DATA_CUSTOM_NAME`, third `defineId`, index 2, serializer OPTIONAL_COMPONENT), :322 (default `Optional.empty()`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 3 to custom name visible, kind Boolean, default false (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/Entity.java:272 (`DATA_CUSTOM_NAME_VISIBLE`, fourth `defineId`, index 3, serializer BOOLEAN), :320 (default false) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 4 to silent, kind Boolean, default false (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/Entity.java:273 (`DATA_SILENT`, fifth `defineId`, index 4, serializer BOOLEAN), :323 (default false) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 5 to no gravity, kind Boolean, default false (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/Entity.java:274 (`DATA_NO_GRAVITY`, sixth `defineId`, index 5, serializer BOOLEAN), :324 (default false) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 6 to pose, kind Pose, default Standing (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/Entity.java:275 (`DATA_POSE`, seventh `defineId`, index 6, serializer POSE), :325 (default `Pose.STANDING`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 7 to freeze/ticks-frozen, kind VarInt, default 0 (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/Entity.java:276 (`DATA_TICKS_FROZEN`, eighth `defineId`, index 7, serializer INT which is VarInt on the wire), :326 (default 0) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 8 to hand states (active-hand/riptide bits), kind Byte, default 0 (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/LivingEntity.java:179 (`DATA_LIVING_ENTITY_FLAGS`, the first `defineId` on LivingEntity, so index 8, serializer BYTE), :323 (default `(byte)0`), :176-178 (bit 1 is-using, bit 2 off-hand, bit 4 spin-attack/riptide) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 9 to health, kind Float, default type-specific MAX_HEALTH (20.0 unless overridden) (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/LivingEntity.java:180 (`DATA_HEALTH_ID`, index 9, serializer FLOAT) but :328 - the synched default is `1.0F`, not a type-specific MAX_HEALTH and not 20.0; MAX_HEALTH is an attribute (`createLivingAttributes`, :331-334) applied through `setHealth` at construction, never the metadata default the wire diff is taken against | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 12 to arrow count, kind VarInt, default 0 (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/LivingEntity.java:185 (`DATA_ARROW_COUNT_ID`, index 12, serializer INT which is VarInt on the wire), :326 (default 0) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 13 to bee-stinger count, kind VarInt, default 0 (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/LivingEntity.java:186 (`DATA_STINGER_COUNT_ID`, index 13, serializer INT which is VarInt on the wire), :327 (default 0) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The base+LivingEntity metadata index table assigns index 14 to sleeping bed position, kind OptionalPosition, default None (moderate confidence, from a live wiki fetch). | net/minecraft/world/entity/LivingEntity.java:187-189 (`SLEEPING_POS_ID`, index 14, serializer OPTIONAL_BLOCK_POS), :329 (default `Optional.empty()`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Indices 10 and 11 in the entity metadata index table are reserved for potion-effect particles and the ambient flag. | net/minecraft/world/entity/LivingEntity.java:181-183 (`DATA_EFFECT_PARTICLES`, index 10, serializer PARTICLES) and :184 (`DATA_EFFECT_AMBIENCE_ID`, index 11, serializer BOOLEAN); :908-909 sets both from the active effect list | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's shared entity-flags metadata byte (DATA_SHARED_FLAGS_ID, index 0 above) assigns bit 0 to on-fire, bit 1 to sneaking, bit 3 to sprinting, bit 4 to swimming, bit 5 to invisible, bit 6 to glowing, and bit 7 to elytra-flying. | net/minecraft/world/entity/Entity.java:260-267 (`DATA_SHARED_FLAGS_ID` plus `FLAG_ONFIRE = 0`, `FLAG_SHIFT_KEY_DOWN = 1`, `FLAG_SPRINTING = 3`, `FLAG_SWIMMING = 4`, `FLAG_INVISIBLE = 5`, `FLAG_GLOWING = 6`, `FLAG_FALL_FLYING = 7`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Mob rung contributes zero additional synced metadata indices beyond what LivingEntity already defines. | world/entity/Mob.java:100 (`DATA_MOB_FLAGS_ID`, serializer BYTE) and :269-272 (`defineSynchedData` calls `super` then defines it) - Mob adds exactly one synced index, which lands at 15 because LivingEntity ends at 14; consequently Villager's own `DATA_VILLAGER_DATA` (npc/villager/Villager.java:98) starts at 16, not 15 | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla, a Mob's PersistenceRequired and CanPickUpLoot fields are internal-only bookkeeping, neither independently persisted to NBT nor synced via entity metadata. | world/entity/Mob.java:124 and :126 (`TAG_CAN_PICK_UP_LOOT = "CanPickUpLoot"`, `TAG_PERSISTENCE_REQUIRED = "PersistenceRequired"`), :370-371 (both written as NBT Booleans in `addAdditionalSaveData`), :396-397 (both read back) - neither is synced via entity metadata, which is the only half of the claim that holds | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Item entity's Slot metadata occupies index 8, since Item is Entity-direct rather than LivingEntity-rung and index 8 is the first free slot after Entity's own indices 0-7. | item/ItemEntity.java:39 (`ItemEntity extends Entity`, so its own indices start at 8) and :40 (`DATA_ITEM` is its only `defineId`, hence index 8) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| EntityType.clientTrackingRange defaults to 5 chunks in vanilla. | world/entity/EntityType.java:475 (`private int clientTrackingRange = 5` in `EntityType.Builder`), :424-426 and :579-582 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| EntityType.updateInterval defaults to 3 ticks in vanilla. | world/entity/EntityType.java:476 (`private int updateInterval = 3` in `EntityType.Builder`), :428-430 and :584-587 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's zombie entity type overrides the tracking-range default to 8 chunks (moderate confidence). | net/minecraft/world/entity/EntityTypes.java:1089-1097 (`ZOMBIE` builder, `.clientTrackingRange(8)`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's villager entity type overrides the tracking-range default to 8 chunks (moderate confidence). | net/minecraft/world/entity/EntityTypes.java:1004-1006 - the `VILLAGER` builder declares `.clientTrackingRange(10)`, not 8 | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's cow entity type overrides the tracking-range default to 8 chunks (moderate confidence). | net/minecraft/world/entity/EntityTypes.java:306-309 - the `COW` builder declares `.clientTrackingRange(10)`, not 8 | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's item entity type overrides the tracking-range default to 6 chunks (moderate confidence). | net/minecraft/world/entity/EntityTypes.java:558-566 (`ITEM` builder, `.clientTrackingRange(6)`; it also sets `.updateInterval(20)`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's item entity (minecraft:item) is not a Mob. | item/ItemEntity.java:39 (`public class ItemEntity extends Entity implements TraceableEntity`) - it never reaches the LivingEntity or Mob rung | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's item entity (minecraft:item) is never naturally spawned. | net/minecraft/world/entity/EntityTypes.java:560 (`MobCategory.MISC`); world/level/NaturalSpawner.java:62 (`SPAWNING_CATEGORIES` filters MISC out) and :254 (spawn attempt rejected for a MISC type); world/level/biome/MobSpawnSettings.java:130 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's own client-tracking system re-discovers an entity that re-enters a player's tracking range as a fresh spawn, with no memory that it was previously tracked and despawned. | server/level/ChunkMap.java:1412-1434 (`TrackedEntity.updatePlayer`: in range it does `seenBy.add(player.connection)` and, on a fresh add, `serverEntity.addPairing(player)`, which re-sends the full pairing data; out of range it calls `removePlayer`, dropping the entry entirely - no residual per-player memory), :1454-1458 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's zombie (minecraft:zombie) uses the legacy GoalSelector AI system and belongs to the MONSTER mob category. | monster/zombie/Zombie.java:71 (`extends Monster`) and :112 (`registerGoals`, the legacy GoalSelector path; no `makeBrain` override); net/minecraft/world/entity/EntityTypes.java:1091 (`MobCategory.MONSTER`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's villager (minecraft:villager) uses the Brain AI system and belongs to the CREATURE mob category. | npc/villager/Villager.java:191-209 (`getBrain`/`makeBrain`/`registerBrainGoals`, the Brain path) - correct; but net/minecraft/world/entity/EntityTypes.java:1005 declares `MobCategory.MISC` for `VILLAGER`, not CREATURE | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's cow (minecraft:cow) uses the legacy GoalSelector AI system and belongs to the CREATURE mob category. | animal/cow/Cow.java:32 (`extends AbstractCow`) and animal/cow/AbstractCow.java:40 (`registerGoals`, the legacy GoalSelector path); net/minecraft/world/entity/EntityTypes.java:308 (`MobCategory.CREATURE`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776 the Spawn Entity packet is client-bound with packet ID 0x01 and fields, in wire order: entity_id (VarInt), uuid (16 raw bytes, big-endian), entity_type (VarInt), x/y/z (f64), pitch then yaw (each a 1-byte Angle, pitch before yaw), head_yaw (1-byte Angle), data (VarInt), velocity_x/velocity_y/velocity_z (i16 fixed-point). | network/protocol/game/ClientboundAddEntityPacket.java:105-116 (`write`) and :91-102 (the decoding constructor); reports/packets.json play.clientbound `minecraft:add_entity` protocol_id 1. Real order: entity_id VarInt, uuid 16 bytes, entity_type VarInt, x/y/z doubles, then the movement vector via `Vec3.LP_STREAM_CODEC` (network/LpVec3.java:44-66, a variable-length quantized encoding, NOT three i16 fixed-point fields), then xRot byte, yRot byte, yHeadRot byte, and `data` VarInt LAST. The claim puts `data` before the velocity and ends with three i16 velocity fields | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Spawn Entity packet orders pitch before yaw, the reverse of every other entity-rotation packet. | ClientboundAddEntityPacket.java:113-114 (`writeByte(xRot)` then `writeByte(yRot)`); contrast ClientboundMoveEntityPacket.java:145-152 (PosRot writes yRot then xRot), :181-186 (Rot writes yRot then xRot), and world/entity/PositionMoveRotation.java:12-22 (yRot then xRot) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla uses the Spawn Entity packet's data field for a kind-specific spawn payload, e.g. a thrown potion's effect id. | The kind-specific role is real (ClientboundAddEntityPacket.java:36-49, the `data` constructor parameter), but no 26.2 entity uses it for a thrown potion's effect id. Actual users: projectile/Projectile.java:346-348 and projectile/FishingHook.java:545-547 (owner entity id), decoration/ItemFrame.java:402-403 and decoration/painting/Painting.java:197-198 (direction 3D data value), monster/warden/Warden.java:138-139 (emerging flag) | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776 the Set Entity Data packet is client-bound with packet ID 0x63. | reports/packets.json play.clientbound `minecraft:set_entity_data` protocol_id 99 = 0x63; ClientboundSetEntityDataPacket.java:49-51 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776 the Update Entity Position packet is client-bound with packet ID 0x35, carrying entity_id (VarInt), a per-axis fixed-point position delta as i16 (round((new - old) * 4096)) valid only for a per-axis delta within +/-8 blocks, and on_ground (bool). | reports/packets.json `minecraft:move_entity_pos` protocol_id 53 = 0x35 and ClientboundMoveEntityPacket.java:104-111 confirm the id and the shape (VarInt id, three i16, bool). The formula differs: network/protocol/game/VecDeltaCodec.java:14-17 and :33-43 compute `Math.round(new * 4096.0) - Math.round(old * 4096.0)`, not `round((new - old) * 4096)` - the two disagree by one unit at rounding boundaries. The plus/minus 8 block bound is right (32767/4096 approximately 7.99998; server/level/ServerEntity.java:152-160 switches to entity_position_sync once any axis leaves the i16 range) | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776 the Update Entity Position and Rotation packet is client-bound with packet ID 0x36, carrying entity_id, the same i16 position delta, yaw/pitch as 1-byte Angles, and on_ground. | reports/packets.json `minecraft:move_entity_pos_rot` protocol_id 54 = 0x36; ClientboundMoveEntityPacket.java:145-152 (VarInt id, three i16, yRot byte, xRot byte, bool) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776 the Update Entity Rotation packet is client-bound with packet ID 0x38, carrying entity_id, yaw/pitch as 1-byte Angles, and on_ground. | reports/packets.json `minecraft:move_entity_rot` protocol_id 56 = 0x38; ClientboundMoveEntityPacket.java:181-186 (VarInt id, yRot byte, xRot byte, bool) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776 the Teleport Entity (entity_position_sync) packet is client-bound with packet ID 0x23, carrying entity_id, x/y/z (f64), velocity_x/y/z (f64), yaw/pitch as full 4-byte Float degrees (not the 1-byte Angle encoding), and on_ground. | reports/packets.json `minecraft:entity_position_sync` protocol_id 35 = 0x23; ClientboundEntityPositionSyncPacket.java:12-20 with world/entity/PositionMoveRotation.java:12-22 (Vec3.STREAM_CODEC position, Vec3.STREAM_CODEC deltaMovement - each three raw doubles per world/phys/Vec3.java:25-35 - then `ByteBufCodecs.FLOAT` yRot and xRot, then `ByteBufCodecs.BOOL`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776 the Set Head Rotation packet is client-bound with packet ID 0x53, carrying entity_id and head_yaw as a 1-byte Angle. | reports/packets.json `minecraft:rotate_head` protocol_id 83 = 0x53; ClientboundRotateHeadPacket.java:30-33 (`writeVarInt(entityId)`, `writeByte(yHeadRot)`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776 the Set Entity Velocity packet is client-bound with packet ID 0x65, carrying entity_id and velocity_x/y/z as i16 fixed-point values. | reports/packets.json `minecraft:set_entity_motion` protocol_id 101 = 0x65 - id correct. But ClientboundSetEntityMotionPacket.java:12-18 composes `ByteBufCodecs.VAR_INT` with `Vec3.LP_STREAM_CODEC`; network/LpVec3.java:44-66 is a quantized variable-length encoding (two bytes plus an int, plus an optional VarInt scale continuation), not three i16 fixed-point components | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776 the Remove Entities packet is client-bound with packet ID 0x4D, carrying a list of entity ids where each id is individually VarInt-encoded (not a single length-prefixed array of plain i32). | reports/packets.json `minecraft:remove_entities` protocol_id 77 = 0x4D; ClientboundRemoveEntitiesPacket.java:26-32 with network/FriendlyByteBuf.java:146-160 (`writeIntIdList`: VarInt count then one VarInt per id) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's Angle wire encoding is a single byte computed as round(degrees / 360.0 * 256.0). | util/Mth.java:185-187 - `packDegrees` is `(byte)floor(angle * 256.0F / 360.0F)`, floor and not round; the inverse `unpackDegrees` at :189-191 is `rot * 360 / 256.0F` | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's entity-velocity wire encoding is round(v * 8000.0), clamped to [-32768, 32767], stored as i16 (derived from Entity.getVelocityUpdatePacket / Mth.clamp). | No such encoding exists at 26.2: there is no `Entity.getVelocityUpdatePacket` anywhere under net/, and the constant 8000 appears nowhere in the velocity path. Entity velocity is encoded by network/LpVec3.java:29-66 (15 bits per axis around a shared integer scale, packed into six bytes plus an optional VarInt scale continuation), used by both ClientboundAddEntityPacket.java:111 and ClientboundSetEntityMotionPacket.java:15 | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| WORLD-D29 fixes the entities/ region-file schema as entities/r.<X>.<Z>.mca with root compound { DataVersion: Int, Position: [x, z] as a 2-element IntArray, Entities: List<Compound> }, one compound per entity in vanilla's own generic { id: String, ...entity fields } shape. | world/level/chunk/storage/EntityStorage.java:102-104 (`NbtUtils.addCurrentDataVersion` root, `Entities` ListTag, `Position` via `ChunkPos.CODEC`) and :60 and :74; world/level/ChunkPos.java:20-22 (`Codec.INT_STREAM` fixed to size 2, hence a 2-element IntArray); server/level/ServerLevel.java:248-249 (the `entities` directory); world/level/chunk/storage/RegionFileStorage.java:20 and :45 (`r.<X>.<Z>.mca`); net/minecraft/world/entity/Entity.java:2064-2069 (`output.putString("id", id)` per entity compound) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| This engine's entity persistence uses DataVersion stamp 4903 (WORLD-D16). | C:\Users\krank\mc-research\26.2\src\version.json - `"world_version": 4903` (with `"protocol_version": 776`); world/level/chunk/storage/EntityStorage.java:102 stamps exactly this value through `NbtUtils.addCurrentDataVersion` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla assigns a freshly-spawned entity's UUID via java.util.UUID.randomUUID(), which is SecureRandom-backed, not java.util.Random-backed. | net/minecraft/world/entity/Entity.java:284 (`protected UUID uuid = Mth.createInsecureUUID(this.random)`) and :251 (`protected final RandomSource random = RandomSource.create()`); util/Mth.java:341-345 (`createInsecureUUID` takes two `nextLong()` draws from that java.util.Random-family source and patches the version/variant bits). `java.util.UUID.randomUUID()` is not used for entity spawn UUIDs | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla re-assigns a fresh network entity id and internal object identity to an entity on every load; only UUID is load-stable across a save/load cycle. | net/minecraft/world/entity/Entity.java:312 (`this.id = level.getNextEntityId()` in the constructor, run afresh on every load-time reconstruction) and :2078-2142 (`saveWithoutId` persists no network id and no object identity); only `UUID` is restored, at :2174-2177 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
