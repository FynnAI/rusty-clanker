# M4-B05 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| AttributeInstance's value calculation stage 1: base = base_value, then add every AddValue modifier's amount to base. | `world/entity/ai/attributes/AttributeInstance.java:148-153` (`calculateValue`) - `double base = getBaseValue()`, then `base += modifier.amount()` over the `ADD_VALUE` bucket | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| AttributeInstance's value calculation stage 2: result = base; for every AddMultipliedBase modifier, add base * amount to result; multiple AddMultipliedBase modifiers are additive against each other, against the original base, not the running result. | `AttributeInstance.java:155-159` - `double result = base;` then `result += base * modifier.amount()` per `ADD_MULTIPLIED_BASE` modifier, each against the unchanged `base` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| AttributeInstance's value calculation stage 3: for every AddMultipliedTotal modifier, multiply result by (1 + amount) sequentially, so multiple AddMultipliedTotal modifiers compound with each other since each applies to the already-updated running total. | `AttributeInstance.java:161-163` - `result *= 1.0 + modifier.amount()` per `ADD_MULTIPLIED_TOTAL` modifier, applied to the running `result` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| AttributeInstance's value calculation stage 4: clamp the result to [min, max]. | `AttributeInstance.java:165` (`sanitizeValue(result)`) plus `RangedAttribute.java:34-37` - `Double.isNaN(value) ? minValue : Mth.clamp(value, minValue, maxValue)`. Nuance not in the claim: NaN collapses to `min`, not to a clamp | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Modifier application order within the AddMultipliedBase/AddMultipliedTotal buckets is hash-table slot order keyed by each modifier's own identifier, not registration or insertion order; AddMultipliedTotal's result *= (1 + amount) is mathematically commutative across modifiers, so only IEEE-754 double rounding, not the running-total structure, makes iteration order observable. | `AttributeInstance.java:53-55` and `:168-170`. The per-operation buckets are `Object2ObjectOpenHashMap` (a fastutil open-addressing HASH map), and `getModifiersOrEmpty` iterates `map.values()` - so iteration order inside a bucket is hash-table slot order keyed by the modifier `Identifier`, NOT registration or insertion order. Only `modifierById` and `permanentModifiers` are `Object2ObjectArrayMap` (insertion-ordered), and neither is used by `calculateValue`. Second half of the claim is also mis-reasoned: `result *= (1+a)` IS mathematically commutative across modifiers; only IEEE-754 double rounding makes the order observable, not the running-total structure | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| AttackDamage attribute default is 2.0, range [0.0, 2048.0]; the Zombie override is 3.0. | `world/entity/ai/attributes/Attributes.java:17` - `new RangedAttribute("attribute.name.attack_damage", 2.0, 0.0, 2048.0)`; `world/entity/monster/zombie/Zombie.java:135` - `.add(Attributes.ATTACK_DAMAGE, 3.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| AttackKnockback attribute default is 0.0, range [0.0, 5.0]; the Zombie override is also 0.0. | `Attributes.java:18` - `RangedAttribute(..., 0.0, 0.0, 5.0)`; `Zombie.java:131-138` adds no `ATTACK_KNOCKBACK` override, and `LivingEntity.java:352` registers it value-less, so the zombie keeps the 0.0 default | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| AttackSpeed attribute default is 4.0, range [0.0, 1024.0]; unused by mobs, which have no attack-cooldown charge curve. | `Attributes.java:9,19-21` - `DEFAULT_ATTACK_SPEED = 4.0`, `RangedAttribute(..., 4.0, 0.0, 1024.0)`. The charge curve lives only on `Player` (`Player.java:1816-1828`); `Mob.doHurtTarget` (`Mob.java:1385-1406`) reads no attack-speed term | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Armor attribute default is 0.0, range [0.0, 30.0]; Zombie's own override is 2.0, Cow and Villager keep the 0.0 default. | `Attributes.java:13` gives the correct default and range (`0.0, 0.0, 30.0`), and Cow and Villager do keep 0.0 - but the Zombie override is NOT 0.0: `Zombie.java:136` is `.add(Attributes.ARMOR, 2.0)`. Correction: Zombie `Armor` = 2.0. This propagates into every zombie golden row of the armor-absorption matrix (`total_armor = 2`, not 0) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| ArmorToughness attribute default is 0.0, range [0.0, 20.0], identical across Zombie, Cow, and Villager. | `Attributes.java:14-16` - `RangedAttribute(..., 0.0, 0.0, 20.0)`; none of `Zombie.createAttributes` (`Zombie.java:131-138`), `AbstractCow.createAttributes` (`AbstractCow.java:56-58`), `Villager.createAttributes` (`Villager.java:227-229`) overrides it | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| KnockbackResistance attribute default is 0.0, range [-2.0, 1.0], identical across Zombie, Cow, and Villager. | `Attributes.java:61-63` - `RangedAttribute(..., 0.0, -2.0, 1.0)`; no override in the three suppliers named above | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| MaxHealth attribute default is 20.0, range [1.0, 1024.0]; Zombie 20.0, Cow 10.0, Villager 20.0. | `Attributes.java:68-70` - `RangedAttribute(..., 20.0, 1.0, 1024.0)`; `AbstractCow.java:57` - `.add(Attributes.MAX_HEALTH, 10.0)`; Zombie and Villager add no override, so both keep 20.0 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| SafeFallDistance attribute default is 3.0, range [-1024.0, 1024.0]; Zombie, Cow, and Villager are all 3.0. | `Attributes.java:86-88` - `RangedAttribute(..., 3.0, -1024.0, 1024.0)`; no override in any of the three suppliers | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| FallDamageMultiplier attribute default is 1.0, range [0.0, 100.0]; Zombie, Cow, and Villager are all 1.0. | `Attributes.java:44-47` - `RangedAttribute(..., 1.0, 0.0, 100.0)`; no override in any of the three suppliers | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| SweepingDamageRatio attribute default is 0.0, range [0.0, 1.0]. | `Attributes.java:104-106` - `RangedAttribute("attribute.name.sweeping_damage_ratio", 0.0, 0.0, 1.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The per-hit damage pipeline runs, in exact order: shield block, freezing/damaged-helmet multipliers, the invulnerability top-up gate, armor absorption, the Resistance status-effect reduction, the enchantment-protection-factor reduction, then absorption hearts. | `world/entity/LivingEntity.java:1198-1231` (`hurtServer`: `applyItemBlocking` -> freeze x5 -> helmet x0.75 -> invulnerability gate) then `LivingEntity.java:1954-1972` (`actuallyHurt`: `getDamageAfterArmorAbsorb` -> `getDamageAfterMagicAbsorb` -> absorption), with Resistance and then EPF inside `getDamageAfterMagicAbsorb` (`LivingEntity.java:1911-1952`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The freezing damage multiplier is x5.0. | `LivingEntity.java:1203-1205` - `if (source.is(DamageTypeTags.IS_FREEZING) and this.is(EntityTypeTags.FREEZE_HURTS_EXTRA_TYPES)) damage *= 5.0F;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The damaged-helmet damage multiplier is x0.75. | `LivingEntity.java:1207-1210` - `if (source.is(DamageTypeTags.DAMAGES_HELMET) and head slot non-empty) { hurtHelmet(...); damage *= 0.75F; }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| If invulnerable_time > 10, not > 0, and the new damage is <= last_hurt, the hit is fully absorbed and produces no effect. | `LivingEntity.java:1217-1220` - `if (this.invulnerableTime > 10.0F and !source.is(BYPASSES_COOLDOWN)) { if (damage <= this.lastHurt) return false; ... }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| If invulnerable_time > 10 and the new damage is > last_hurt, only the delta (damage - last_hurt) is actually applied, last_hurt is updated to the new damage value, and invulnerable_time is NOT reset. | `LivingEntity.java:1222-1224` - `actuallyHurt(level, source, damage - this.lastHurt); this.lastHurt = damage; tookFullDamage = false;` - no write to `invulnerableTime` on this branch | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| If invulnerable_time <= 10, the hit applies its full damage, sets last_hurt to that damage, resets invulnerable_time to 20, and sets hurt_time to 10. | `LivingEntity.java:1225-1231` - `lastHurt = damage; invulnerableTime = 20; actuallyHurt(level, source, damage); hurtDuration = 10; hurtTime = hurtDuration;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| invulnerable_time decrements by 1 every tick. | `LivingEntity.java:471-473` decrements it once per `baseTick` for every non-`ServerPlayer`; `server/level/ServerPlayer.java:584-585` does the same decrement for players in `doTick`. Two sites, same per-tick step of 1 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The BYPASSES_COOLDOWN damage-type tag is empty in vanilla at version 26.2, so no damage type bypasses the invulnerability window. | `tags/DamageTypeTags.java:12` declares `BYPASSES_COOLDOWN = create("bypasses_cooldown")`, and the datagen tag directory `data/minecraft/tags/damage_type/` contains no `bypasses_cooldown.json` - the tag has zero members, so the `!source.is(...)` guard at `LivingEntity.java:1217` is always true | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Armor-absorption formula: toughness = 2.0 + armor_toughness/4.0; real_armor = clamp(total_armor - damage/toughness, total_armor*0.2, 20.0); armor_fraction = clamp(real_armor/25.0, 0.0, 1.0); damage = damage * (1.0 - armor_fraction). | `world/damagesource/CombatRules.java:19-35` (`getDamageAfterAbsorb`) - `toughness = 2.0F + armorToughness / 4.0F`; `realArmor = Mth.clamp(totalArmor - damage / toughness, totalArmor * 0.2F, 20.0F)`; `damage * (1.0F - armorFraction)`. Shape nuance: the `[0,1]` clamp at `:28` wraps the enchantment-modified fraction, not the raw `realArmor / 25.0F` at `:24`; with `Armor` bounded `[0,30]` and `realArmor` already clamped to `<= 20`, the fraction is always in `[0, 0.8]`, so the clamp is numerically vacuous and the blueprint's placement gives identical results for every reachable input | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| total_armor used in the armor-absorption formula is floor(AttributeMap[Armor]), the armor value is int-floored before use even though the rest of the formula is float. | `LivingEntity.java:1878-1880` - `getArmorValue()` returns `Mth.floor(this.getAttributeValue(Attributes.ARMOR))` (an `int`), which is what `getDamageAfterArmorAbsorb` (`LivingEntity.java:1902-1909`) passes as `totalArmor` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Enchantment-protection-factor formula: epf_sum is the uncapped sum of protection_epf, fire_protection_epf, blast_protection_epf, projectile_protection_epf, and feather_falling_epf; real_epf = clamp(epf_sum, 0.0, 20.0); damage = damage * (1.0 - real_epf/25.0). | `world/item/enchantment/EnchantmentHelper.java:169-175` (`getDamageProtection` accumulates a `MutableFloat` over all equipment with no cap) and `CombatRules.java:37-40` - `realArmor = Mth.clamp(totalMagicArmor, 0.0F, 20.0F); return damage * (1.0F - realArmor / 25.0F);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Protection's EPF is 1.0 per level, max level 4, max EPF 4.0. | `data/minecraft/enchantment/protection.json` - `max_level: 4`, `minecraft:damage_protection` add of `linear{base 1.0, per_level_above_first 1.0}`; `LevelBasedValue.Linear.calculate` (`world/item/enchantment/LevelBasedValue.java:172-174`) is `base + perLevelAboveFirst * (level - 1)`, so level 4 gives 4.0 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Fire Protection's, Blast Protection's, and Projectile Protection's EPF are each 2.0 per level, max level 4, max EPF 8.0 each. | `data/minecraft/enchantment/fire_protection.json`, `blast_protection.json`, `projectile_protection.json` - each `max_level: 4` with `damage_protection` add of `linear{base 2.0, per_level_above_first 2.0}` -> 2.0 per level, 8.0 at level 4 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Feather Falling's EPF is 3.0 per level, max level 4, max EPF 12.0. | `data/minecraft/enchantment/feather_falling.json` - `max_level: 4`, `damage_protection` add of `linear{base 3.0, per_level_above_first 3.0}` -> 12.0 at level 4 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Feather Falling's EPF applies specifically to Fall damage, and this still applies even though Fall damage otherwise bypasses the armor-absorption step entirely. | `feather_falling.json` requirement is `damage_source_properties` with tag `minecraft:is_fall` expected true; `data/minecraft/tags/damage_type/is_fall.json` contains `fall`; `bypasses_armor.json` also contains `fall`, but the EPF step is skipped only for `bypasses_effects` (`starve` only) and `bypasses_enchantments` (`sonic_boom` only) per `LivingEntity.java:1911-1938` - so fall damage is armor-bypassing yet fully EPF-subject | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Mob absorption-hearts path: original = damage; damage = max(damage - absorption, 0.0); absorption -= (original - damage); then if damage != 0.0, health -= damage AND absorption -= damage again, a second subtraction, mob path only. | `LivingEntity.java:1954-1972` (`actuallyHurt`) - `originalDamage = dmg; dmg = Math.max(dmg - getAbsorptionAmount(), 0.0F); setAbsorptionAmount(getAbsorptionAmount() - (originalDamage - dmg));` then inside `if (dmg != 0.0F)`: `setHealth(getHealth() - dmg); setAbsorptionAmount(getAbsorptionAmount() - dmg);` - the second subtraction is present exactly as claimed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Player absorption-hearts path: original = damage; damage = max(damage - absorption, 0.0); absorption -= (original - damage), only one subtraction; then if damage != 0.0, trigger food exhaustion for the damage type and health -= damage. | `world/entity/player/Player.java:748-771` (`Player.actuallyHurt`) - identical prologue, one absorption subtraction only, and inside `if (dmg != 0.0F)`: `causeFoodExhaustion(source.getFoodExhaustion())` first, then `setHealth(getHealth() - dmg)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In both the mob and player absorption-hearts paths, absorption is clamped to >= 0.0, never negative, after the subtraction(s). | `LivingEntity.java:3398-3400` - `setAbsorptionAmount` is `internalSetAbsorptionAmount(Mth.clamp(absorptionAmount, 0.0F, this.getMaxAbsorption()))`, so it can never go negative. Nuance not in the claim: the clamp is two-sided against the `minecraft:max_absorption` attribute (default 0.0, `Attributes.java:65-67`), which the blueprint's `AttributeKind` table omits | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| attack_strength_delay(ticks) = 1.0 / attack_speed_attribute * 20.0, which for the default AttackSpeed of 4.0 yields 5 ticks. | `Player.java:1816-1818` - `getCurrentItemAttackStrengthDelay()` returns `(float)(1.0 / this.getAttributeValue(Attributes.ATTACK_SPEED) * 20.0)`; with the 4.0 default that is 5.0 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| charge_scale(ticker, offset) = clamp((ticker + offset) / attack_strength_delay, 0.0, 1.0). | `Player.java:1826-1828` - `getAttackStrengthScale(a)` returns `Mth.clamp((this.attackStrengthTicker + a) / this.getCurrentItemAttackStrengthDelay(), 0.0F, 1.0F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| base_damage_scale_factor() = 0.2 + charge_scale(ticker, 0.5)^2 * 0.8. | `Player.java:1207-1210` - `baseDamageScaleFactor()` is `0.2F + attackStrengthScale * attackStrengthScale * 0.8F` with `attackStrengthScale = getAttackStrengthScale(0.5F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| attack_strength_ticker increments by 1 every player tick and resets to 0 whenever on_attack() fires, which happens at the start of every successful Interact{Attack} dispatch regardless of whether the swing dealt damage. | `Player.java:268` - `this.attackStrengthTicker++` once per `Player.tick`; `Player.java:1840-1843` `onAttack()` calls `resetOnlyAttackStrengthTicker()` (`:1845-1847`, `attackStrengthTicker = 0`); `Player.java:959` calls `this.onAttack()` unconditionally, before the `deflectProjectile`/`baseDamage > 0` branches, so it fires whether or not the swing lands | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Player melee damage assembly order: base_damage = AttributeMap[AttackDamage]; charge_scale = charge_scale(ticker, 0.5); magic_boost = charge_scale * (enchanted_damage(base_damage, enchants) - base_damage), computed against the UNSCALED base_damage; base_damage is then multiplied by base_damage_scale_factor(), the charge curve is applied after magic_boost is computed; total_damage = base_damage + magic_boost. | `Player.java:953-958,977` - `baseDamage = getAttributeValue(ATTACK_DAMAGE)`; `attackStrengthScale = getAttackStrengthScale(0.5F)`; `magicBoost = attackStrengthScale * (getEnchantedDamage(entity, baseDamage, damageSource) - baseDamage)` against the unscaled `baseDamage`; then `baseDamage *= baseDamageScaleFactor()`; `totalDamage = baseDamage + magicBoost`. Not named by the claim: `Player.java:971` inserts `baseDamage += attackingItemStack.getItem().getAttackDamageBonus(...)` between the charge scale and the crit multiply (the Mace/stab hook the blueprint declares out of scope) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| full_strength = charge_scale > 0.9, the exact threshold. | `Player.java:962` - `boolean fullStrengthAttack = attackStrengthScale > 0.9F;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| knockback_attack = is_sprinting && full_strength, which adds +0.5 knockback and triggers the KNOCKBACK sound. | `Player.java:964-969` - `if (this.isSprinting() and fullStrengthAttack) { playServerSideSound(SoundEvents.PLAYER_ATTACK_KNOCKBACK); knockbackAttack = true; }`; the +0.5 is at `Player.java:987-989` inside the `causeExtraKnockback` argument | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| critical = full_strength && can_critical_attack(...); if critical, base_damage is multiplied by 1.5. | `Player.java:972-975` - `boolean criticalAttack = fullStrengthAttack and this.canCriticalAttack(entity); if (criticalAttack) baseDamage *= 1.5F;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| can_critical_attack = fall_distance > 0.0 && !on_ground && !on_climbable && !in_water && target_is_living && !is_sprinting; this check is deterministic and consumes no RNG. | `Player.java:1032-1041` (`canCriticalAttack`). The real conjunction is `fallDistance > 0.0 and !onGround() and !onClimbable() and !isInWater() and !isMobilityRestricted() and !isPassenger() and entity instanceof LivingEntity and !isSprinting()` - eight terms, not five. The blueprint's Context already declares `isMobilityRestricted`/`isPassenger` excluded (no such state modelled), but the target-is-a-`LivingEntity` term is not declared anywhere and must be added (an attack against a non-living target, e.g. M4-B01's `Item` kind, can never crit). The 'deterministic, no RNG' half of the claim is CONFIRMED - the method draws nothing | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| sweep = full_strength && !critical && !knockback_attack && on_ground && horizontal_speed_sq < (movement_speed * 2.5)^2 && main_hand_is_sword. | `Player.java:1043-1053` (`isSweepAttack`) - `fullStrengthAttack and !criticalAttack and !knockbackAttack and this.onGround()`, then `getKnownMovement().horizontalDistanceSqr() < Mth.square(this.getSpeed() * 2.5)`, then `getItemInHand(MAIN_HAND).is(ItemTags.SWORDS)`. `getSpeed()` (`LivingEntity.java:2722-2724`) returns the `speed` field, which `Player.aiStep` sets from the `MOVEMENT_SPEED` attribute at `Player.java:456` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| enchanted_damage(base, enchants) = base + sharpness_bonus(sharpness_level) + smite_bonus(smite_level) if target is undead else 0.0, + bane_bonus(bane_of_arthropods_level) if target is arthropod else 0.0. | `server/level/ServerPlayer.java:2211-2214` -> `EnchantmentHelper.modifyDamage` (`EnchantmentHelper.java:177-183`), which folds every `minecraft:damage` effect of the weapon's enchantments into the base. `sharpness.json` adds unconditionally; `smite.json` is gated on `#minecraft:sensitive_to_smite`, which is `["#minecraft:undead"]`; `bane_of_arthropods.json` is gated on `#minecraft:sensitive_to_bane_of_arthropods`, which is `["#minecraft:arthropod"]` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Sharpness's enchant-bonus formula is linear as base + per_level*(level-1) for level >= 1 else 0.0, with base 1.0 and per-level 0.5. | `data/minecraft/enchantment/sharpness.json` - `minecraft:damage` add of `linear{base 1.0, per_level_above_first 0.5}`; `LevelBasedValue.java:172-174` evaluates that as `1.0 + 0.5 * (level - 1)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Smite's enchant-bonus formula is linear as base + per_level*(level-1) for level >= 1 else 0.0, with base 2.5 and per-level 2.5. | `data/minecraft/enchantment/smite.json` - `minecraft:damage` add of `linear{base 2.5, per_level_above_first 2.5}` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Bane of Arthropods's enchant-bonus formula is linear as base + per_level*(level-1) for level >= 1 else 0.0, with base 2.5 and per-level 2.5. | `data/minecraft/enchantment/bane_of_arthropods.json` - `minecraft:damage` add of `linear{base 2.5, per_level_above_first 2.5}` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| sweep_ratio = sweeping_edge_level / (sweeping_edge_level + 1) if sweeping_edge_level > 0, else 0.0. | `data/minecraft/enchantment/sweeping_edge.json` - an `add_value` modifier on `minecraft:sweeping_damage_ratio` whose amount is `fraction{numerator linear{1.0, 1.0}, denominator linear{2.0, 1.0}}` = `level / (level + 1)`; `LevelBasedValue.Fraction.calculate` (`LevelBasedValue.java:129-133`) returns 0.0 on a zero denominator | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| sweep_base = 1.0 + sweep_ratio * base_damage, where base_damage is the post-charge, post-critical value. | `Player.java:1167` (`doSweepAttack`) - `float var12 = 1.0F + (float)this.getAttributeValue(Attributes.SWEEPING_DAMAGE_RATIO) * baseDamage;` with `baseDamage` the value passed from `Player.java:991`, i.e. post-charge-scale and post-crit (sweep only fires when `!criticalAttack`, so the x1.5 never applied on this path) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A sweep attack hits every LivingEntity within the primary target's own AABB inflated by (1.0, 0.25, 1.0) whose attacker-to-candidate squared distance is < 9.0, excluding the attacker itself, the primary target, entities allied to the attacker, and marker ArmorStands. | `Player.java:1169` - the box is the PRIMARY TARGET's, not the attacker's: `this.level().getEntitiesOfClass(LivingEntity.class, entity.getBoundingBox().inflate(1.0, 0.25, 1.0))`, where `entity` is the entity being attacked. The `< 9.0` squared-distance test at `:1174` is `this.distanceToSqr(nearby)` (attacker to nearby) and is correctly stated. Two further exclusions the claim omits: `!this.isAlliedTo(nearby)` (`:1172`) and `!(nearby instanceof ArmorStand armorStand and armorStand.isMarker())` (`:1173`) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Each sweep-hit nearby target takes enchanted_damage(sweep_base, enchants) * charge_scale. | `Player.java:1175` - `float enchantedDamage = this.getEnchantedDamage(nearby, var12, damageSource) * attackStrengthScale;` - the enchant formula is re-evaluated per nearby target against the sweep base, then scaled by the charge | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| On a successful sweep hit, the nearby target receives a knockback impulse directed by the attacker's yaw with a flat magnitude of 0.4 and no enchant term. | `Player.java:1176-1183` - on `nearby.hurtServer(...)` returning true, `nearby.knockback(0.4F, Mth.sin(yRot * PI/180), -Mth.cos(yRot * PI/180), damageSource, enchantedDamage)` - flat 0.4, attacker-yaw direction, no enchantment term | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Mob melee damage = AttributeMap[AttackDamage] + enchant_bonus(target); this is fully deterministic with no critical hits, no sweep, no charge curve, and no RNG. | `world/entity/Mob.java:1385-1392` (`doHurtTarget`) - `dmg = getAttributeValue(ATTACK_DAMAGE)`, then `EnchantmentHelper.modifyDamage(...)`, then `+= weaponItem.getItem().getAttackDamageBonus(...)`, then `target.hurtServer(level, damageSource, dmg)`. No charge scale, no crit branch, no sweep branch, no RNG draw anywhere in the method | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| On a mob attack that deals damage, the target receives a knockback impulse with magnitude get_knockback(attacker, target), directed by the attacker's yaw. | `Mob.java:1394` - `this.causeExtraKnockback(target, this.getKnockback(target, damageSource), oldMovement, damageSource, dmg, true)` inside `if (wasHurt)`; `LivingEntity.causeExtraKnockback` (`LivingEntity.java:2735-2749`) directs it with `Mth.sin(yRot * PI/180), -Mth.cos(yRot * PI/180)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Knockback impulse #1 fires on the fresh-hit branch of the invulnerability gate only (never the top-up branch, even when it deals damage) whenever the damage type is not no_knockback-tagged; its direction is the normalized XZ-plane vector from the victim's position to the source's position, and its magnitude is a flat 0.4. | `LivingEntity.java:1290-1306` (`dealDefaultKnockback`) and its call site `LivingEntity.java:1247-1249`. Two corrections. (a) Direction sign: the reference computes `xd = source.getSourcePosition().x() - this.getX()`, `zd = source.getSourcePosition().z() - this.getZ()` - the vector FROM the victim TO the source (the claim says 'from the source's position to the victim's position', which is the negated vector); it is then subtracted in `knockback` (`new_vx = old_vx/2 - dx`), which is what pushes the victim away. The blueprint's own Context pseudocode has the correct sign; only this claim's prose is inverted. (b) Firing condition: it is NOT 'any hit that dealt nonzero damage'. The call sits inside `if (tookFullDamage)` and is further gated by `if (!source.is(DamageTypeTags.NO_KNOCKBACK))` - so it never fires on the invulnerability top-up branch even when that branch does deal damage, and it DOES fire on a fresh hit whose damage armour reduces to zero. Magnitude 0.4 and XZ normalisation are correct | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Fall and Starve are both members of the no_knockback set, modeled explicitly as a per-DamageTypeKind flag, so knockback impulse #1 never fires for them; their self-inflicted, positionless source is incidental, not the mechanism. | `data/minecraft/tags/damage_type/no_knockback.json` lists both `minecraft:fall` and `minecraft:starve`, and `LivingEntity.java:1247` skips `dealDefaultKnockback` entirely for any `NO_KNOCKBACK`-tagged source. The claim's stated mechanism is the wrong one and is not even structurally safe: were `dealDefaultKnockback` reached with no source position, `xd`/`zd` stay 0.0 (`LivingEntity.java:1291-1292`) and `knockback` (`LivingEntity.java:1647-1650`) would enter the degenerate-direction loop, drawing RNG and still halving the victim's velocity - a real effect, not a no-op. Correction: model the `no_knockback` tag membership explicitly for `Fall` and `Starve` rather than relying on the absent source position | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Knockback impulse #2 fires strictly after impulse #1 has already mutated velocity; its direction is (sin(attacker_yaw_rad), -cos(attacker_yaw_rad)); its magnitude is get_knockback(attacker, target) + 0.5 if knockback_attack is true, else get_knockback(attacker, target), for players, mobs never have knockback_attack true. | Ordering: `Player.java:985` `entity.hurtOrSimulate(...)` (which runs `hurtServer` -> `dealDefaultKnockback`) precedes `Player.java:987-989` `causeExtraKnockback(...)`. Direction and magnitude: `Player.causeExtraKnockback` (`Player.java:1130-1138`) uses `Mth.sin(yRot * PI/180), -Mth.cos(yRot * PI/180)` with `getKnockback(entity, damageSource) + (knockbackAttack ? 0.5F : 0.0F)`; the mob path (`Mob.java:1394`) passes `getKnockback(...)` with no bonus term | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| get_knockback(attacker, target) = (attacker.AttributeMap[AttackKnockback] + knockback_enchant_bonus(level)) / 2.0. | `LivingEntity.java:1540-1546` (`getKnockback`) - `knockback = (float)getAttributeValue(Attributes.ATTACK_KNOCKBACK)` then `EnchantmentHelper.modifyKnockback(level, getWeaponItem(), target, damageSource, knockback) / 2.0F` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| knockback_enchant_bonus(level) = 1.0 + 1.0*(level-1) if level > 0, else 0.0. | `data/minecraft/enchantment/knockback.json` - `max_level: 2`, `minecraft:knockback` add of `linear{base 1.0, per_level_above_first 1.0}`, i.e. `1.0 + 1.0 * (level - 1)`; the effect is only present when the enchantment is on the item at level >= 1 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In the knockback-impulse-application algorithm, power *= (1.0 - target.AttributeMap[KnockbackResistance]) is applied first, and if the resulting power <= 0.0, velocity is returned unchanged before any halving is applied. | `LivingEntity.java:1642-1643` - `power *= 1.0 - this.getAttributeValue(Attributes.KNOCKBACK_RESISTANCE);` then `if (!(power <= 0.0)) { ... }`, so a non-positive power leaves `deltaMovement` untouched before any halving. Nuance: the negated form means a NaN power falls through into the impulse body | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In the knockback-impulse-application algorithm, while the direction vector's squared XZ magnitude is below the double-widened float threshold 9.999999747378752e-6 (not the literal 1e-5), a fallback direction is re-drawn as xd = (rng.next_double() - rng.next_double()) * 0.01 and zd = (rng.next_double() - rng.next_double()) * 0.01, consuming 4 RNG draws per iteration, unbounded, until the sample clears the threshold. | `LivingEntity.java:1647-1650` uses a `while` loop, not an `if`: `while (xd * xd + zd * zd < 1.0E-5F) { xd = (random.nextDouble() - random.nextDouble()) * 0.01; zd = (random.nextDouble() - random.nextDouble()) * 0.01; }`. It re-draws until the sample clears the threshold, so the RNG call count is 4 per iteration and unbounded, not a fixed 4 - which matters under MECH-D5's call-count-and-order rule. Second correction: the threshold literal is the float `1.0E-5F` widened to double, i.e. 9.99999974737875E-6, not exactly 1e-5 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In the knockback-impulse-application algorithm, the normalized direction scaled by power gives (dx, dz), and new_vx = old_vx/2.0 - dx; new_vz = old_vz/2.0 - dz; new_vy = min(0.4, old_vy/2.0 + power) if on_ground, else old_vy unchanged. | `LivingEntity.java:1652-1657` - `Vec3 deltaVector = new Vec3(xd, 0.0, zd).normalize().scale(power); setDeltaMovement(deltaMovement.x / 2.0 - deltaVector.x, this.onGround() ? Math.min(0.4, deltaMovement.y / 2.0 + power) : deltaMovement.y, deltaMovement.z / 2.0 - deltaVector.z);` - the `power` in the y term is the resistance-scaled value | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The knockback impulse function is called exactly twice per hit with two different (power, direction) inputs and its results are never merged into a single call, reproducing vanilla's own velocity-halving-per-call behavior. | `LivingEntity.java:1247-1249` (impulse #1 via `dealDefaultKnockback` -> `knockback(0.4F, ...)`) and `Player.java:987-989` / `Mob.java:1394` (impulse #2 via `causeExtraKnockback` -> `knockback(amount, ...)`) are two distinct `LivingEntity.knockback` invocations, each performing its own `/ 2.0` halving; nothing merges them. Nuance not in the claim: each call is separately gated, so a hit can produce fewer than two - impulse #1 is skipped on the top-up branch or for a `no_knockback` source, and impulse #2 is skipped when `knockbackAmount > 0.0F` fails (`Player.java:1130`, `LivingEntity.java:2738`), which is the default case with `AttackKnockback` 0.0 and no sprint | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| fall_power = fall_distance + 1e-6 - AttributeMap[SafeFallDistance]. | `LivingEntity.java:1855-1857` (`calculateFallPower`) - `return fallDistance + 1.0E-6 - this.getAttributeValue(Attributes.SAFE_FALL_DISTANCE);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| fall_damage = floor(fall_power * damage_modifier * AttributeMap[FallDamageMultiplier]), an integer result, with damage_modifier fixed at vanilla's own default of 1.0. | `LivingEntity.java:1846-1853` (`calculateFallDamage`) - `Mth.floor(baseDamage * damageModifier * this.getAttributeValue(Attributes.FALL_DAMAGE_MULTIPLIER))`, returning `int`; the default `damageModifier` is the `1.0F` that `Block.fallOn` (`world/level/block/Block.java:489-491`) passes | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Fall damage is skipped entirely, not computed or applied at all, when the resulting fall_damage is <= 0, rather than being applied as a zero-value hit. | `LivingEntity.java:1802-1811` (`causeFallDamage`) - `int dmg = calculateFallDamage(...); if (dmg > 0) { ... this.hurt(damageSource, dmg); return true; } else return damaged;` - no hurt call at all when the computed damage is not positive | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla, landing on a Bed halves fall distance. | `world/level/block/BedBlock.java:131-133` - `super.fallOn(level, state, pos, entity, fallDistance * 0.5)`, i.e. the fall DISTANCE is halved before the default modifier-1.0 path runs | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla, landing on a Hay Bale sets the fall-damage modifier to 0.2 while passing the fall distance through unscaled, so SafeFallDistance still subtracts from the full distance before the 0.2 multiplies. | `world/level/block/HayBlock.java:25-27` - `entity.causeFallDamage(fallDistance, 0.2F, level.damageSources().fall())`. The 0.2 is the `damageModifier`, and the fall DISTANCE is passed through unchanged. That is not the same computation: `SafeFallDistance` is subtracted from the full distance first and the 0.2 multiplies afterwards, so vanilla gives `floor((d + 1e-6 - 3.0) * 0.2 * FallDamageMultiplier)`, whereas scaling the distance would give `floor((0.2d + 1e-6 - 3.0) * ...)`. Correction: hay bale sets `damage_modifier = 0.2`, it does not scale fall distance | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla, landing on a Slime Block sets the fall-damage modifier to 0.0 in both branches; sneaking (isSuppressingBounce) instead skips the fall-damage call entirely, suppressing the bounce rather than restoring fall damage. | `world/level/block/SlimeBlock.java:22-27` - `if (!entity.isSuppressingBounce()) { entity.causeFallDamage(fallDistance, 0.0F, level.damageSources().fall()); }`. Two corrections. (a) The 0.0 is the `damageModifier`, not a fall-distance multiplier (same distinction as the hay bale). (b) Sneaking does NOT restore fall damage: `isSuppressingBounce()` is `isShiftKeyDown()` (`world/entity/Entity.java:2699-2701`), and when it is true the override returns without calling `causeFallDamage` at all, so the sneaking player takes no fall damage either. A slime block negates fall damage in both branches; sneaking only suppresses the bounce | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla, landing in Powder Snow grants full fall-damage immunity. | `world/level/block/PowderSnowBlock.java:101-107` - the `fallOn` override only plays a landing sound and never calls `causeFallDamage`, so the default `Block.fallOn` modifier-1.0 path is fully bypassed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| When the target is a player whose GameModeState.instabuild is true, the damage pipeline returns Invulnerable immediately, with no invulnerability-window consumption, no animation, and no packets sent, matching vanilla's own abilities.invulnerable short-circuit. | `Player.java:684-686` - `if (this.abilities.invulnerable and !source.is(DamageTypeTags.BYPASSES_INVULNERABILITY)) return false;`, placed after `isInvulnerableTo` and before the difficulty scaling at `:694`, so nothing downstream (invulnerability window, hurt animation, damage event broadcast) runs. `bypasses_invulnerability.json` holds only `out_of_world` and `generic_kill`, neither of which is a `DamageTypeKind` this blueprint declares | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Difficulty scaling is applied to incoming player damage only, strictly before any of the pipeline's item-blocking/freeze/helmet-multiplier logic, and only for damage types whose scaling is WhenCausedByLivingNonPlayer when the attacker is a non-player living entity, or Always unconditionally. | `Player.java:694-706` places the whole scaling block inside `Player.hurtServer` before the `super.hurtServer` call at `:708` that contains item blocking, freeze and helmet logic; `world/damagesource/DamageSource.java:92-98` (`scalesWithDifficulty`) is `NEVER -> false`, `WHEN_CAUSED_BY_LIVING_NON_PLAYER -> causingEntity instanceof LivingEntity and !(causingEntity instanceof Player)`, `ALWAYS -> true` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Peaceful difficulty sets incoming player damage to 0.0. | `Player.java:695-697` - `if (level.getDifficulty() == Difficulty.PEACEFUL) damage = 0.0F;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Easy difficulty sets incoming player damage to min(damage/2.0 + 1.0, damage). | `Player.java:699-701` - `if (level.getDifficulty() == Difficulty.EASY) damage = Math.min(damage / 2.0F + 1.0F, damage);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Hard difficulty sets incoming player damage to (damage * 3.0) / 2.0 — a multiply followed by a divide, not a single multiply by 1.5; the two forms agree everywhere except above f32::MAX/3, where the multiply-then-divide form overflows to infinity while a single ×1.5 stays finite. | `Player.java:703-705` - the reference writes `damage = damage * 3.0F / 2.0F`, not `damage * 1.5F`. These are not the same float expression: `x * 3.0F` rounds once and the following `/ 2.0F` is exact, whereas `x * 1.5F` rounds once against a different exact product, so the two disagree in the last ulp for some inputs. Under this project's bit-identical-parity rule and this blueprint's own f32-bit-for-bit golden-table gate, the multiply-then-divide form is the one to implement | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Normal difficulty leaves incoming player damage unchanged. | `Player.java:694-706` contains branches for PEACEFUL, EASY and HARD only; NORMAL falls through with `damage` untouched | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| If difficulty scaling reduces the damage to exactly 0.0, the whole hit short-circuits with no invulnerability-window consumption. | `Player.java:708` - `return damage == 0.0F ? false : super.hurtServer(level, source, damage);` - `super.hurtServer` is never entered, so `invulnerableTime`/`lastHurt` are not touched | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's own default world difficulty is Normal. | `world/level/LevelSettings.java:62` - `LevelSettings.DifficultySettings.DEFAULT = new LevelSettings.DifficultySettings(Difficulty.NORMAL, false, false)`. The `Difficulty` enum ordinals (`world/Difficulty.java:13-16`) are PEACEFUL 0, EASY 1, NORMAL 2, HARD 3, matching the blueprint's declaration order | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| On mob death, vanilla broadcasts an Entity Event with event_id 3, death animation, to every viewer tracking that mob. | `LivingEntity.java:1477` (inside `die`) - `this.level().broadcastEntityEvent(this, (byte)3);`, and `world/entity/EntityEvent.java:6` declares `public static final byte DEATH = 3;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Health reaching <= 0.0 marks the target as dead and the pipeline outcome is Died rather than Dealt. | `LivingEntity.java:1171-1173` - `isDeadOrDying()` is `this.getHealth() <= 0.0F or this.dead`; `hurtServer` (`LivingEntity.java:1252-1260`) tests it right after `actuallyHurt` and calls `die(source)`. Note: vanilla has no Died-vs-Dealt outcome enum (`hurtServer` returns a plain boolean) - that split is this blueprint's own modelling of the same branch | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A dying mob's dropped item entities receive vx = rng.next_double()*0.2 - 0.1, vy = the constant 0.2 (not a random draw), vz = rng.next_double()*0.2 - 0.1, consuming exactly two next_double() calls per item, x then z. | `world/entity/item/ItemEntity.java:61-66` - `this.setDeltaMovement(this.random.nextDouble() * 0.2 - 0.1, 0.2, this.random.nextDouble() * 0.2 - 0.1)`. The y component is the CONSTANT 0.2, not a random draw, so the spawn consumes exactly TWO `nextDouble()` calls (x then z), not three. Under MECH-D5's call-count-and-order rule the extra draw would desynchronise every subsequent value on that stream. Correction: `vx = rng.next_double()*0.2 - 0.1; vy = 0.2; vz = rng.next_double()*0.2 - 0.1` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla villagers drop no items on death. | `data/minecraft/loot_table/entities/villager.json` - the table declares only `type` and `random_sequence` and carries no `pools` key at all, so a villager death yields no items | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A vanilla zombie drops 0 to 2 rotten flesh on death, a uniform random count. | `data/minecraft/loot_table/entities/zombie.json`, pool 1 - a single `minecraft:rotten_flesh` entry with `minecraft:set_count` of `uniform{min 0.0, max 2.0}` (plus a Looting `enchanted_count_increase` of `uniform{0,1}` per level, and a separate zombie-horse-vehicle-conditional red mushroom pool that never applies to a plain zombie) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A vanilla cow drops 1 to 3 beef and, independently, 0 to 2 leather on death. | `data/minecraft/loot_table/entities/cow.json` - `minecraft:beef` with `set_count` `uniform{min 1.0, max 3.0}` and `minecraft:leather` with `set_count` `uniform{min 0.0, max 2.0}`, each in its own pool with `rolls: 1.0`. Note for the blueprint's `FixedTierTwoLoot`: the LEATHER pool is declared first and the beef pool second, so a parity-faithful roll order draws leather before beef - the reverse of the blueprint's stated field order | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The default player entity-interaction range is approximately 3.0 blocks. | `Attributes.java:41-43` - `ENTITY_INTERACTION_RANGE = new RangedAttribute("attribute.name.entity_interaction_range", 3.0, 0.0, 64.0)`; it is registered on every living entity by `LivingEntity.createLivingAttributes` (`LivingEntity.java:346`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Zombie's and Villager's hitbox dimensions are 0.6 wide x 1.95 tall. | `world/entity/EntityTypes.java:1089-1098` - Zombie `.sized(0.6F, 1.95F)`; `EntityTypes.java:1004-1006` - Villager `.sized(0.6F, 1.95F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Cow's hitbox dimensions are 0.9 wide x 1.4 tall. | `world/entity/EntityTypes.java:306-309` - Cow `.sized(0.9F, 1.4F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Item's hitbox dimensions are 0.25 wide x 0.25 tall. | `world/entity/EntityTypes.java:558-566` - Item `.sized(0.25F, 0.25F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Landing a melee attack costs a flat 0.1 exhaustion, EXHAUSTION_ATTACK. | `Player.java:998` - `this.causeFoodExhaustion(0.1F)` as the last statement of the `if (wasHurt)` branch of `Player.attack`; `causeFoodExhaustion` (`Player.java:1584-1590`) forwards to `foodData.addExhaustion(amount)` unless the player is `abilities.invulnerable` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| PlayerAttack damage costs 0.1 exhaustion, scales WhenCausedByLivingNonPlayer, and does not bypass armor. | `data/minecraft/damage_type/player_attack.json` - `exhaustion: 0.1`, `scaling: when_caused_by_living_non_player`; `minecraft:player_attack` is absent from `data/minecraft/tags/damage_type/bypasses_armor.json` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| MobAttack damage costs 0.1 exhaustion, scales WhenCausedByLivingNonPlayer, and does not bypass armor. | `data/minecraft/damage_type/mob_attack.json` - `exhaustion: 0.1`, `scaling: when_caused_by_living_non_player`; absent from `bypasses_armor.json` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Fall damage costs 0.0 exhaustion, scales WhenCausedByLivingNonPlayer, and bypasses armor. | `data/minecraft/damage_type/fall.json` - `exhaustion: 0.0`, `scaling: when_caused_by_living_non_player`; `minecraft:fall` is a member of `bypasses_armor.json` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Starve damage costs 0.0 exhaustion, scales WhenCausedByLivingNonPlayer, and bypasses armor. | `data/minecraft/damage_type/starve.json` - `exhaustion: 0.0`, `scaling: when_caused_by_living_non_player`; `minecraft:starve` is a member of `bypasses_armor.json` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A joining player's food stats start at food_level 20, range 0..=20, saturation 5.0, vanilla's own join default, and exhaustion 0.0 with a ceiling of 40.0. | `world/food/FoodData.java:14-17` - `foodLevel = 20`, `saturationLevel = 5.0F`, `exhaustionLevel` default 0.0F; the `0..=20` food range is enforced by `FoodData.add` (`:22-25`, `Mth.clamp(food + foodLevel, 0, 20)`) and the 40.0 exhaustion ceiling by `addExhaustion` (`:103-105`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| add_exhaustion adds the given amount to exhaustion, clamped to a ceiling of 40.0. | `FoodData.java:103-105` - `this.exhaustionLevel = Math.min(this.exhaustionLevel + amount, 40.0F);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Exhaustion-decay branch: if exhaustion > 4.0, subtract 4.0 from exhaustion, then reduce saturation by 1.0, floored at 0.0, if saturation > 0.0, else reduce food_level by 1, floored at 0, if difficulty is not Peaceful. | `FoodData.java:38-45` - `if (exhaustionLevel > 4.0F) { exhaustionLevel -= 4.0F; if (saturationLevel > 0.0F) saturationLevel = Math.max(saturationLevel - 1.0F, 0.0F); else if (difficulty != Difficulty.PEACEFUL) foodLevel = Math.max(foodLevel - 1, 0); }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Fast, saturation-driven, natural regeneration: if saturation > 0.0 and the entity is hurt and food_level >= 20, a regen timer increments each tick and at 10 ticks heals health by min(saturation, 6.0)/6.0, capped at max_health, spends that same amount as exhaustion, and resets the timer. | `FoodData.java:48-55` - `saturationLevel > 0.0F and player.isHurt() and foodLevel >= 20`, `tickTimer++`, at `>= 10` heal `Math.min(saturationLevel, 6.0F) / 6.0F`, `addExhaustion(saturationSpent)`, `tickTimer = 0`; the max-health cap comes from `setHealth`'s clamp (`LivingEntity.java:1167-1169`). Nuance the blueprint declares as a deliberate simplification: vanilla also requires the `NATURAL_HEALTH_REGENERATION` gamerule (`FoodData.java:47-48`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Slow, food-driven, natural regeneration: if food_level >= 18 and the entity is hurt, and the fast-regen branch did not fire, a regen timer increments each tick and at 80 ticks heals 1.0 health, capped at max_health, adds 6.0 exhaustion, and resets the timer. | `FoodData.java:56-62` - `else if (naturalRegen and foodLevel >= 18 and player.isHurt())`, `tickTimer++`, at `>= 80` `player.heal(1.0F)`, `addExhaustion(6.0F)`, `tickTimer = 0`; same gamerule nuance as the previous row | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Starvation: if food_level <= 0, and neither regen branch fired, a regen timer increments each tick and at 80 ticks applies 1.0 Starve damage if health > 10.0, or difficulty is Hard, or health > 1.0 and difficulty is Normal; the timer then resets regardless. | `FoodData.java:63-71` - `else if (foodLevel <= 0)`, `tickTimer++`, at `>= 80` `if (player.getHealth() > 10.0F or difficulty == HARD or player.getHealth() > 1.0F and difficulty == NORMAL) player.hurtServer(level, damageSources().starve(), 1.0F);` and `tickTimer = 0` outside that inner condition | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| is_hurt(health, max_health) is defined as health < max_health && health > 0.0. | `Player.java:1608-1610` - `public boolean isHurt() { return this.getHealth() > 0.0F and this.getHealth() < this.getMaxHealth(); }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Interact packet's payload has no interaction_type discriminator: Attack is its own server-bound packet, ID 0x01, a single entity_id (VarInt) field; Interact is server-bound ID 0x1A with fields, in wire order, entity_id (VarInt), hand (VarInt: 0=main hand, 1=off hand), location (LpVec3), using_secondary_action (bool) — a flat, unconditional four-field layout with no conditional field groups. | The packet id is right (`reports/packets.json`, `play.serverbound.minecraft:interact` = 0x1A) but the payload is entirely different in 26.2, and Attack is no longer part of it. `network/protocol/game/ServerboundInteractPacket.java:11-23` is `record ServerboundInteractPacket(int entityId, InteractionHand hand, Vec3 location, boolean usingSecondaryAction)` with `STREAM_CODEC = composite(VAR_INT entityId, InteractionHand.STREAM_CODEC hand, Vec3.LP_STREAM_CODEC location, BOOL usingSecondaryAction)` - a flat, unconditional four-field layout with NO `interaction_type` discriminator, no 0/1/2 enum, and no conditional field groups. Attacking has its own packet: `play.serverbound.minecraft:attack` = 0x01, `ServerboundAttackPacket.java:9-12`, a record of a single `entityId` encoded as one VarInt. Correction for the blueprint: implement `Attack` as packet 0x01 with one VarInt field, and `Interact` as 0x1A with `entity_id VarInt, hand VarInt, location LpVec3, using_secondary_action bool` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Set Health packet is client-bound with packet ID 0x68 and fields, in wire order: health (f32), food (VarInt), saturation (f32). | `reports/packets.json`, `play.clientbound.minecraft:set_health` = 0x68; `network/protocol/game/ClientboundSetHealthPacket.java:28-32` writes `writeFloat(health); writeVarInt(food); writeFloat(saturation);` in that order | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Update Attributes packet is client-bound with packet ID 0x83 and fields, in wire order: entity_id (VarInt), then a VarInt-count-prefixed sequence of entries each shaped attribute_id (VarInt), base_value (f64), modifier_count (VarInt). | `reports/packets.json`, `play.clientbound.minecraft:update_attributes` = 0x83; `ClientboundUpdateAttributesPacket.java:20-26` is `composite(VAR_INT entityId, AttributeSnapshot.STREAM_CODEC.apply(ByteBufCodecs.list(128)) attributes)` (a VarInt-count-prefixed list), and each snapshot (`:71-79`) is `Attribute.STREAM_CODEC` + `DOUBLE base` + a VarInt-count-prefixed modifier collection. `Attribute.STREAM_CODEC` is `ByteBufCodecs.holderRegistry(Registries.ATTRIBUTE)` (`Attribute.java:14`), which `ByteBufCodecs.java:588-590` routes to a plain `idMapper` - a raw VarInt registry id, matching the blueprint's numeric-id convention | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Damage Event packet is client-bound with packet ID 0x19 and fields, in wire order: entity_id (VarInt), source_type_id (VarInt), source_cause_id (VarInt, network entity id + 1, 0 = none), source_direct_id (VarInt, network entity id + 1, 0 = none), has_source_position (bool), then source_x/source_y/source_z (each f64) only if has_source_position is true. | `reports/packets.json`, `play.clientbound.minecraft:damage_event` = 0x19; `ClientboundDamageEventPacket.java:53-63` writes `writeVarInt(entityId)`, `DamageType.STREAM_CODEC` (a `holderRegistry` VarInt registry id, `DamageType.java:24`), `writeOptionalEntityId` twice (`:45-47`, `writeVarInt(id + 1)`, so 0 encodes 'none'), then `writeOptional(sourcePosition, ...)` = a bool followed, when true, by three doubles | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Entity Event packet is client-bound with packet ID 0x22 and fields, in wire order: entity_id as a plain Int, not a VarInt, unlike every other entity-id field in this packet set, then event_id (u8); event_id 2 is KINETIC_HIT, a weapon-hit-sound cue this blueprint never constructs, and event_id 3 is the death-animation event, the only value this blueprint ever sends. | The wire shape is right - `reports/packets.json`, `play.clientbound.minecraft:entity_event` = 0x22, and `ClientboundEntityEventPacket.java:28-31` writes `output.writeInt(this.entityId)` (a plain 4-byte Int, confirming the claimed asymmetry) followed by `output.writeByte(this.eventId)`. The event-id semantics are not: `world/entity/EntityEvent.java:4-6` declares `JUMP = 1`, `KINETIC_HIT = 2`, `DEATH = 3`. Id 3 is the death animation as claimed, but id 2 is KINETIC_HIT, NOT a generic hurt/hurt-animation event - the hurt animation is carried by the dedicated `Damage Event` packet (0x19) instead. Correction: this blueprint must not construct entity event 2 for a hurt animation | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Player Combat Kill packet is client-bound with packet ID 0x44 and fields, in wire order: player_id (VarInt), then message. Vanilla encodes message as an NBT-backed chat Component, not a length-prefixed string; this blueprint deliberately sends a plain length-prefixed string as a documented stand-in, a real, client-visible wire-shape divergence from vanilla, not a parity bug to fix. | The id and first field are right (`reports/packets.json`, `play.clientbound.minecraft:player_combat_kill` = 0x44; `ClientboundPlayerCombatKillPacket.java:12-18` starts with `ByteBufCodecs.VAR_INT playerId`), but the message field is not a length-prefixed string: it is `ComponentSerialization.TRUSTED_STREAM_CODEC`, an NBT-encoded chat `Component`. A raw prefixed string is not a decodable payload for a vanilla client here. The blueprint's Context declares the plain-string stand-in deliberately (mirroring M4-B01's `OptionalTextComponent`), so this row records the wire-shape divergence rather than a fixable transcription error - but the claim as worded asserts vanilla's shape and is wrong about it | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| After a hit applies both knockback impulses, only the single final post-both-impulses velocity is broadcast via one Set Entity Velocity packet, matching vanilla's own single velocity-update-packet-per-hit behavior even though the server-side math applies two separate impulses. | `server/level/ServerEntity.java:225-228` - `if (this.entity.hurtMarked) { this.entity.hurtMarked = false; this.synchronizer.sendToTrackingPlayersAndSelf(new ClientboundSetEntityMotionPacket(this.entity)); }` sends exactly one motion packet per tick built from the entity's current (post-both-impulses) delta movement; `Player.causeExtraKnockback` (`Player.java:1152-1157`) does the same one-packet send for a `ServerPlayer` target | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Health is broadcast via the entity metadata field at index 9, Set Entity Data, to viewers other than the entity's own owning player, who instead receive Set Health directly. | Synched-data index arithmetic over `world/entity/Entity.java:260-276` (eight `defineId(Entity.class, ...)` accessors, indices 0-7: SHARED_FLAGS, AIR_SUPPLY, CUSTOM_NAME, CUSTOM_NAME_VISIBLE, SILENT, NO_GRAVITY, POSE, TICKS_FROZEN) followed by `world/entity/LivingEntity.java:179-180` (`DATA_LIVING_ENTITY_FLAGS` = 8, `DATA_HEALTH_ID` = 9). `ServerEntity` sends metadata to tracking players, while the owning player receives `ClientboundSetHealthPacket` (`ServerPlayer.doTick`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
