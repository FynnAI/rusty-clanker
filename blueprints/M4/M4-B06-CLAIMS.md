# M4-B06 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| A fluid cell's `FluidState` is hosted entirely through its `BlockState`'s legacy `LEVEL` property, ranged [0,15] (`BlockStateProperties.LEVEL`). | `LiquidBlock.java:53` (`LEVEL = BlockStateProperties.LEVEL`); `BlockStateProperties.java:116` (`IntegerProperty.create("level", 0, 15)`); `LiquidBlock.createBlockStateDefinition:245-248` adds only `LEVEL`; `LiquidBlock.getFluidState:123-127` derives the FluidState from that single property | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Water and lava each occupy a contiguous 16-wide `BlockStateId` range (one id per `LEVEL` value 0-15). | reports/blocks.json: `minecraft:water` ids 86..101 (default 86), `minecraft:lava` ids 102..117 (default 102); 16 states each, sole property `level` with values "0".."15" ascending in id order | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The legacy-level formula is: `legacy_level = if is_source { 0 } else { (8 - amount.min(8)) + if falling { 8 } else { 0 } }`. | `FlowingFluid.getLegacyLevel:464-466` - `isSource() ? 0 : 8 - Math.min(getAmount(), 8) + (getValue(FALLING) ? 8 : 0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A non-source flowing state at full amount (8), non-falling, also encodes to legacy level 0, indistinguishable from a genuine source once placed. | `FlowingFluid.getLegacyLevel:464-466` - for a non-source, non-falling, amount-8 state: `8 - min(8,8) + 0 == 0`, identical to the source encoding | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The inverse mapping (BlockStateId -> FluidState) always resolves legacy level 0 to Source, never to Flowing{amount:8, falling:false}, matching `LiquidBlock.getFluidState` deriving FluidState from the stored BlockState. | `LiquidBlock` ctor:66-78 builds `stateCache` as index 0 = `fluid.getSource(false)`, 1..7 = `getFlowing(8-level, false)`, 8 = `getFlowing(8, true)`; `LiquidBlock.getFluidState:123-127` returns `stateCache.get(Math.min(level, 8))`. Levels 9..15 additionally all clamp onto index 8 (flowing, amount 8, falling) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `FluidState.getAmount()` for a source is hardcoded 8 and not itself stored. | `WaterFluid.Source.getAmount:159-162` and `LavaFluid.Source.getAmount:267-270` both `return 8`; only the `Flowing` subclasses add `LEVEL` to the fluid state definition (`WaterFluid.Flowing:138-142`, `LavaFluid.Flowing:246-250`), so a source stores no amount | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `getOwnHeight(state) = amount as f32 / 9.0f32` using float division (source height 8/9 approx 0.889). | `FlowingFluid.getOwnHeight:477-480` - `return fluidState.getAmount() / 9.0F;` (int divided by a float literal, float result) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `getHeight(state)` is exactly 1.0f32 whenever the cell directly above holds the same fluid type (any amount/falling combination), else it falls back to `getOwnHeight`. | `FlowingFluid.getHeight:472-475` with `hasSameAbove:468-470` (`fluidState.getType().isSame(level.getFluidState(pos.above()).getType())`) - returns `1.0F` on a match, else `fluidState.getOwnHeight()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `Direction.Plane.HORIZONTAL` iterates in the order NORTH, EAST, SOUTH, WEST, and this exact order is used by `getFlow`, `getNewLiquid`, `sourceNeighborCount`, `getSpread`, and `getSlopeDistance`. | `Direction.java:577` - `HORIZONTAL(new Direction[]{NORTH, EAST, SOUTH, WEST}, ...)`; iterated at `FlowingFluid.getFlow:62` and `:92`, `getNewLiquid:168`, `sourceNeighborCount:359`, `getSpread:375`, `getSlopeDistance:289` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The lava+water contact-conversion scan uses a second, independent order: UP, NORTH, SOUTH, WEST, EAST (`LiquidBlock.POSSIBLE_FLOW_DIRECTIONS` combined with `.getOpposite()`). | `LiquidBlock.POSSIBLE_FLOW_DIRECTIONS:56-58` = DOWN, SOUTH, NORTH, EAST, WEST; `LiquidBlock.shouldSpreadLiquid:218-222` reads `pos.relative(direction.getOpposite())`, giving the scanned position order UP, NORTH, SOUTH, WEST, EAST | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `getNewLiquid` looks only at the 4 horizontal neighbors and the cell above, never at the cell's own current contents, accumulating a highest-amount value and a source count. | `FlowingFluid.getNewLiquid:163-198` - accumulates `highestNeighbor` and `neighbourSources` over `Direction.Plane.HORIZONTAL` (:168-179), then reads only DOWN (:182) and UP (:189). Nuance: the cell's own BlockState is passed in and used solely as the source side of `canPassThroughWall`; its own FluidState is never read | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| If at least 2 qualifying source neighbors are found and the relevant gamerule allows source conversion, and the cell below is solid (an isSolid-equivalent heuristic: a force-solid-on/off override, else collision-shape mean edge length >= 0.7291666666666666 or Y-extent >= 1.0 — not a full-cube test, so a bottom slab qualifies) or an already-source cell of the same kind, the recompute returns Source; the floor check is a logical OR, either alone qualifies. | `FlowingFluid.getNewLiquid:181-187`. The `>= 2` count, the gamerule gate and the genuine OR are exact, but the floor test is `belowState.isSolid()`, NOT a full-cube test: `BlockBehaviour.BlockStateBase.isSolid:548-550` returns the cached `legacySolid` computed by `calculateSolid:484-503` - forceSolidOn wins, else forceSolidOff loses, else collision shape non-empty AND (`bounds.getSize() >= 0.7291666666666666` OR `bounds.getYsize() >= 1.0`). A slab, and any other large partial shape, therefore qualifies as a source-conversion floor | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| If the cell directly above holds the same fluid kind and can pass through the wall upward, the recompute returns Flowing{amount:8, falling:true}. | `FlowingFluid.getNewLiquid:189-194` - `return this.getFlowing(8, true);` guarded by `!aboveFluid.isEmpty() && aboveFluid.getType().isSame(this) && canPassThroughWall(Direction.UP, ...)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Otherwise the new amount is `highest - drop_off(kind)`; if that is <= 0 the result is Empty, else Flowing{amount, falling:false}. | `FlowingFluid.getNewLiquid:196-197` - `int amount = highestNeighbor - this.getDropOff(level); return amount <= 0 ? Fluids.EMPTY.defaultFluidState() : this.getFlowing(amount, false);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The `water_source_conversion` gamerule defaults to true. | `GameRules.java:92` - `WATER_SOURCE_CONVERSION = registerBoolean("water_source_conversion", GameRuleCategory.UPDATES, true)`; read by `WaterFluid.canConvertToSource:78-81` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The `lava_source_conversion` gamerule defaults to false, so two adjacent lava sources do not spontaneously create a third by default. | `GameRules.java:47` - `LAVA_SOURCE_CONVERSION = registerBoolean("lava_source_conversion", GameRuleCategory.UPDATES, false)`; read by `LavaFluid.canConvertToSource:201-204` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `spread` checks the cell below first (down-before-sideways preference): if the block below can maybe pass through and can be replaced with and can hold the fluid, the fluid spreads down there. | `FlowingFluid.spread:120-137` - `canMaybePassThrough(..., Direction.DOWN, ...)`, then `belowFluid.canBeReplacedWith(..., Direction.DOWN)` and `canHoldSpecificFluid(...)`, then `spreadTo(..., Direction.DOWN, newBelowFluid)`, all before any sideways handling | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| After `spread` flows down into the cell below, if at least 3 source neighbors surround the origin cell it also tops up sideways in the same tick, even though the fluid already flowed down. | `FlowingFluid.spread:131-133` - `if (this.sourceNeighborCount(level, pos) >= 3) this.spreadToSides(level, pos, fluidState, state);` placed after the down `spreadTo` and before the `return` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `spread` runs unconditionally every scheduled tick, including for source cells and immediately after `on_scheduled_tick`'s own recompute. | `FlowingFluid.tick:445-462` - `this.spread(...)` at :461 sits outside the `if (!fluidState.isSource())` block, so it runs on every scheduled tick and immediately after the recompute's own `setBlock`. `spread` itself only short-circuits on an empty fluid state (:121) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A non-source cell that finds an open shaft below but does not fall into it this tick (already full from a prior tick) skips sideways spreading entirely. | `FlowingFluid.spread:139-141` - sideways runs only `if (fluidState.isSource() or !this.isWaterHole(...))`. With the same fluid already full below, the down branch fails at `WaterFluid.canBeReplacedWith:119-122` (incoming water is rejected) while `isWaterHole:312-320` still returns true, so the sideways call is skipped entirely | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `spread_to_sides`, the neighbor gate is 7 when the cell is falling (overriding the amount-based value), else `state.amount() - drop_off(kind)`; sideways spread happens only when this gate is > 0. | `FlowingFluid.spreadToSides:145-151` - `int neighbor = fluidState.getAmount() - this.getDropOff(level); if (fluidState.getValue(FALLING)) { neighbor = 7; } if (neighbor > 0) { ... }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `getSpread` scans neighbors in order North, East, South, West; a strictly shorter slope distance found at a later direction clears all previously recorded tied candidates, while equal-or-shorter distances are kept (ties preserved). | `FlowingFluid.getSpread:370-409` - loop over `Direction.Plane.HORIZONTAL` (:375), `if (distance < lowest) { result.clear(); }` (:393-395), then `if (distance <= lowest)` (:397) keeping equal-distance entries | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `getSpread`'s `lowest` distance is updated to the newly measured distance even when the candidate is rejected by the replace-with check. | `FlowingFluid.getSpread:397-403` - `lowest = distance;` is placed outside the inner `if (testFluidState.canBeReplacedWith(...))` guard, so a rejected candidate still lowers `lowest` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `is_hole(pos)` requires that the wall below can be passed through downward AND (the existing fluid below is already the same kind, OR the cell below could structurally hold that kind at all). | `FlowingFluid.isWaterHole:312-320` - `if (!canPassThroughWall(Direction.DOWN, ...)) return false;` then `bottomState.getFluidState().getType().isSame(this) ? true : canHoldFluid(level, bottomPos, bottomState, this.getFlowing())` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `get_slope_distance` is a greedy depth-first probe that returns immediately (short-circuits) on the first hole found at a given depth, in North, East, South, West order, never stepping directly back the direction it came from, and never performing a true shortest-path search. | `FlowingFluid.getSlopeDistance:284-310` - `lowest = 1000`, loop over `Direction.Plane.HORIZONTAL` skipping `direction == from` (:290), `if (context.isHole(testPos)) return pass;` (:295-297) before any sibling direction is examined, recursion only while `pass < this.getSlopeFindDistance(level)` (:299) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `slope_find_distance` for water is 4, always. | `WaterFluid.getSlopeFindDistance:94-97` - `return 4;` with no condition | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `slope_find_distance` for lava is 4 if the dimension has fast lava (Nether-like), else 2 in a normal dimension. | `LavaFluid.getSlopeFindDistance:152-155` - `return isFastLava(level) ? 4 : 2;`; `LavaFluid.isFastLava:238-240` reads `EnvironmentAttributes.FAST_LAVA` off the dimension | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A full-cube shape at the target position makes a wall impassable to fluid flow. | `FlowingFluid.canPassThroughWall:209-212` - `if (targetShape == Shapes.block()) return false;` (the source side is rejected the same way at :214-217) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| An empty shape at the target position makes a wall passable to fluid flow only when the source position's shape is also empty; a full-cube source still blocks flow into an empty target, and a full-cube target is rejected outright regardless of the source shape. | `FlowingFluid.canPassThroughWall:219-221` - the empty fast path is `if (sourceShape == Shapes.empty() && targetShape == Shapes.empty()) return true;`, requiring BOTH shapes empty. An empty target behind a full-cube source returns false at :214-217, and an empty target with a partial source falls through to `!Shapes.mergedFaceOccludes(sourceShape, targetShape, direction)` at :241. An empty target shape alone is therefore not sufficient to make the wall passable | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `canBeReplacedWith` against an empty/no existing fluid target is unconditionally true. | `EmptyFluid.canBeReplacedWith:24-27` - `return true;` unconditionally. Nuance: there is no shared base default - `Fluid.canBeReplacedWith:73` is abstract, so EMPTY is the only implementation returning an unconditional true | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `canBeReplacedWith` against existing water is true only if the incoming direction is Down and the incoming kind is not water. | `WaterFluid.canBeReplacedWith:119-122` - `return direction == Direction.DOWN && !other.is(FluidTags.WATER);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `canBeReplacedWith` against existing lava is true only if the existing lava's `get_height` is >= 0.44444445f32 (approximately 4/9, i.e. amount >= 4 for flowing, always true for a source at 8/9) and the incoming kind is water, with no direction restriction. | `LavaFluid.canBeReplacedWith:172-175` - `return state.getHeight(level, pos) >= 0.44444445F && other.is(FluidTags.WATER);` with no direction term; the constant is also named at `LavaFluid.MIN_LEVEL_CUTOFF:34` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `getFlow` accumulates flow_x/flow_z as f64 by iterating neighbors in North, East, South, West order, testing whether the neighbor fluid is none or the same kind (affectsFlow). | `FlowingFluid.getFlow:57-102` - `double flowX = 0.0; double flowZ = 0.0;` accumulated over `Direction.Plane.HORIZONTAL` (:62), gated by `affectsFlow:104-106` = `neighbourFluid.isEmpty() or neighbourFluid.getType().isSame(this)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `getFlow`, when a neighbor's own height is 0.0 and that neighbor does not block motion (an isSolid-equivalent heuristic excluding cobweb/bamboo sapling by kind, not a full-cube test), the algorithm looks one cell further down; if that cell's fluid is none or the same kind and has height > 0.0, distance is computed as `own_height(state) - (bh - 0.8888889f32)` using the literal f32 constant 0.8888889, not a computed 8.0f32/9.0f32. | `FlowingFluid.getFlow:68-78`. The structure and the constant are exact - one cell down (`blockPos.below()`), the deeper cell re-tested with `affectsFlow`, `neighborHeight > 0.0F` required, and the literal `0.8888889F` rather than `8.0F / 9.0F`. But the gate is `!level.getBlockState(blockPos).blocksMotion()`, not "not a full cube": `BlockBehaviour.BlockStateBase.blocksMotion:542-545` is "block is not COBWEB and not BAMBOO_SAPLING and `isSolid()`", where `isSolid()` is the `legacySolid` heuristic of `calculateSolid:484-503` and is true for many non-full shapes | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| When a neighbor's own height is > 0.0, distance is `own_height(state) - neighbor_height`. | `FlowingFluid.getFlow:79-81` - `else if (neighborHeight > 0.0F) { distance = fluidState.getOwnHeight() - neighborHeight; }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Flow accumulation multiplies the direction offset by distance in f32, then widens the product to f64 before adding. | `FlowingFluid.getFlow:83-86` - `flowX += direction.getStepX() * distance;`: `getStepX()` is an `int` and `distance` a `float`, so the product is evaluated in float and only then widened by `+=` into the `double` accumulator | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| If the fluid state is falling, and any of the 4 horizontal neighbors (or the cell above that neighbor) presents a solid face, the flow vector is normalized and then has 6.0 subtracted from its y component; the first matching direction wins and remaining directions are unchecked. | `FlowingFluid.getFlow:91-99` - `if (this.isSolidFace(level, blockPos, direction) or this.isSolidFace(level, blockPos.above(), direction)) { flow = flow.normalize().add(0.0, -6.0, 0.0); break; }`, the `break` leaving later directions unchecked | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `normalize_or_zero` treats a vector with length below 1.0e-5f32 (compared after widening to f64) as exactly zero; otherwise divides each component by the length. | `Vec3.normalize:83-86` - `double dist = Math.sqrt(x*x + y*y + z*z); return dist < 1.0E-5F ? ZERO : new Vec3(x / dist, y / dist, z / dist);` - the float literal is widened to double for the comparison | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Lava+water contact conversion (reaction A) is triggered synchronously from a lava cell being placed or a neighbor changing; a shape update never triggers it, since vanilla's own shape-update hook only re-arms a fluid tick. | `LiquidBlock.onPlace:149-153` and `LiquidBlock.neighborChanged:195-201` both call `shouldSpreadLiquid:215-239` synchronously, so the place and neighbor-change triggers are exact. The shape-update trigger does not exist: `LiquidBlock.updateShape:169-189` never calls `shouldSpreadLiquid` - it only schedules a fluid tick when the cell's own or the neighbor's fluid is a source (:180-182) and handles the bubble column (:184-186) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Reaction A scans exactly 5 positions in the fixed order Up, North, South, West, East, a different order from the horizontal-only neighbor order used elsewhere. | `LiquidBlock.shouldSpreadLiquid:218-235` iterates the 5 entries of `POSSIBLE_FLOW_DIRECTIONS:56-58` and takes `direction.getOpposite()` (:222), giving UP, NORTH, SOUTH, WEST, EAST - distinct from `Direction.Plane.HORIZONTAL`'s NORTH, EAST, SOUTH, WEST used everywhere else | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In reaction A, the first scanned position that is water converts the lava cell's own position to obsidian if the lava is a source, else to cobblestone. | `LiquidBlock.shouldSpreadLiquid:223-227` - `Block convertToBlock = level.getFluidState(pos).isSource() ? Blocks.OBSIDIAN : Blocks.COBBLESTONE; level.setBlockAndUpdate(pos, convertToBlock.defaultBlockState());` written at the lava cell's own `pos` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Reaction A returns immediately once the first water match is found; remaining scan positions are never checked that call. | `LiquidBlock.shouldSpreadLiquid:227` - `return false;` executes inside the loop body immediately after the conversion write and the fizz, so no later scan position is read | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In reaction A, the water test and the (caller-configured) soul-soil-gated blue-ice test both run against each of the 5 scan positions in a single interleaved pass, not a separate rescan; at each position the water test fires first, and only if it does not, the blue-ice test is checked at that same position, so a blue-ice match at an earlier position beats a water match at a later one, and a match converts the cell to basalt. | `LiquidBlock.shouldSpreadLiquid:215-239` makes ONE interleaved pass, not two. `isOverSoulSoil` is computed once before the loop (:217), and inside each single iteration the water test (:223-228) is followed immediately by the blue-ice test for that same direction (:230-234), returning on whichever fires first. There is no "scan all 5 for water, then rescan all 5 for blue ice" ordering - a blue-ice neighbor at an earlier scan position beats a water neighbor at a later one and yields basalt | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Reaction A is not gated by lava's own slow tick delay; it fires the same game tick the triggering neighbor-state-change is observed. | `LiquidBlock.onPlace:149-153` and `LiquidBlock.neighborChanged:195-201` invoke `shouldSpreadLiquid` on the dispatching tick, and the conversion writes happen inside it (`setBlockAndUpdate` at :225 and :231) before any `scheduleTick`; `LavaFluid.getTickDelay:177-180` is never consulted on that path | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Reaction B (downward-spread conversion) applies only when direction == Down: if lava spreads downward into a cell whose existing fluid is water, the target becomes plain stone, never cobblestone or obsidian, and lava is never actually placed there. | `LavaFluid.spreadTo:206-221` - gated on `direction == Direction.DOWN` (:208), tests the target's own fluid `level.getFluidState(pos)` for `FluidTags.WATER` (:209-210), writes `Blocks.STONE.defaultBlockState()` (:212) and returns without ever calling `super.spreadTo`. Nuance: the stone write is itself guarded by `state.getBlock() instanceof LiquidBlock` (:211), so a waterlogged non-LiquidBlock target is left unchanged; either way the method fizzes and returns without placing lava | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Sideways spread of lava into a water cell can never reach reaction B, because water's own `canBeReplacedWith` already rejects any non-Down replacement attempt. | Structural consequence of `WaterFluid.canBeReplacedWith:119-122` (`direction == Direction.DOWN` is required) applied at `FlowingFluid.getSpread:398`, the only gate through which `spreadToSides:154-159` can reach `spreadTo` with a horizontal direction | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Fluid tick delay values: water is always 5 ticks; lava is 30 ticks when the dimension's `fast_lava` is false, and 10 ticks when `fast_lava` is true. | `WaterFluid.getTickDelay:114-117` - `return 5;`; `LavaFluid.getTickDelay:177-180` - `return isFastLava(level) ? 10 : 30;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Drop-off values: water is 1; lava is 2 when `fast_lava` is false, 1 when `fast_lava` is true. | `WaterFluid.getDropOff:109-112` - `return 1;`; `LavaFluid.getDropOff:167-170` - `return isFastLava(level) ? 1 : 2;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Slope-find distance values: water is always 4; lava is 2 when `fast_lava` is false, 4 when `fast_lava` is true. | `WaterFluid.getSlopeFindDistance:94-97` - `return 4;`; `LavaFluid.getSlopeFindDistance:152-155` - `return isFastLava(level) ? 4 : 2;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `get_spread_delay` for lava rolls a 4-sided random check (`rng.roll_next_int(4)`) only when the old and new states are both non-empty, both non-falling, and the new state's height is strictly greater than the old's (lava "rising"); when the roll result is not 0 (a 75% chance), the delay is the table value multiplied by 4. | `LavaFluid.getSpreadDelay:182-195` - the exact conjunction `!oldFluidState.isEmpty() && !newFluidState.isEmpty() && !oldFluidState.getValue(FALLING) && !newFluidState.getValue(FALLING) && newFluidState.getHeight(level, pos) > oldFluidState.getHeight(level, pos) && level.getRandom().nextInt(4) != 0`, then `result *= 4` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Water's spread delay always uses the flat table value with no randomized multiplier. | `FlowingFluid.getSpreadDelay:440-442` - `return this.getTickDelay(level);`; `WaterFluid` never overrides it (only `LavaFluid:182-195` does), so water always uses the flat 5 from `WaterFluid.getTickDelay:114-117` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The RNG stream used for lava's rising-delay roll (vanilla's `Level.random`) is shared and non-deterministically seeded across every random-tick consumer in a tick, and is distinct from the per-chunk-per-tick determinism stream. | `Level.java:122` - `protected final RandomSource random = RandomSource.create();`, exposed by `Level.getRandom:1048` and shared by every consumer; `RandomSource.create:14-16` seeds from `RandomSupport.generateUniqueSeed()`, not from the world seed. `LavaFluid.getSpreadDelay:190` draws from that one shared instance | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Block ticks drain completely before fluid ticks begin in every Stage-4 pass (`drain_due_block_ticks` fully processed, then `drain_due_fluid_ticks`). | `ServerLevel.java:388-391` - `profiler.push("blockTicks"); this.blockTicks.tick(tick, 65536, this::tickBlock); profiler.popPush("fluidTicks"); this.fluidTicks.tick(tick, 65536, this::tickFluid);`, over the two separate `LevelTicks` instances declared at :209-210 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's 3-argument `scheduleTick(pos, fluid, delay)` overload used throughout the fluid-dynamics code defaults its tick priority to NORMAL (moderate confidence, standard default, not independently re-verified against decompiled source). | `ScheduledTickAccess.java:31-33` - the 3-arg fluid `scheduleTick` calls `createTick(pos, type, tickDelay)`; `LevelAccessor.createTick:34-37` builds the 4-arg `ScheduledTick` record constructor, which `ScheduledTick.java:36-38` delegates to the 5-arg one with `TickPriority.NORMAL`. Fully settled against decompiled source - the blueprint's "moderate confidence, not independently re-verified" caveat can be dropped | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's fixed denylist of blocks that can never hold any fluid regardless of shape includes doors, signs, ladder, sugar cane, bubble column, nether portal, end portal, end gateway, and structure void. | `FlowingFluid.canHoldAnyFluid:411-428` - exactly `DoorBlock`, `BlockTags.SIGNS`, `Blocks.LADDER`, `SUGAR_CANE`, `BUBBLE_COLUMN`, `NETHER_PORTAL`, `END_PORTAL`, `END_GATEWAY`, `STRUCTURE_VOID`, reached only when the state is not a `LiquidBlockContainer` (:413-414, which passes unconditionally) and does not `blocksMotion()` (:416-417) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `is_solid_face` in real vanilla is false when the fluid at the position is the caller's own fluid kind (a same-fluid short-circuit), else false for ice, else a genuine per-face sturdiness test (`SupportType.FULL` against the block's own support shape, which defaults to its collision shape) — not a full-cube test; this blueprint implements that sturdiness test for real, via a pure `is_face_sturdy_shape` function checked against the same tier-1/tier-2 shape table every other occlusion check already resolves, rather than approximating it with `is_full_cube(pos)`. | `FlowingFluid.isSolidFace:108-118`. The ice exception is real (`state.getBlock() instanceof IceBlock ? false : ...` at :116), but the rest is not a full-cube test: it first returns false when the fluid at `pos` is the same fluid (:111-112), then returns true unconditionally for `direction == Direction.UP` (:113-114, dead inside `getFlow`, which only passes horizontal directions), and otherwise returns `state.isFaceSturdy(level, pos, direction)` - a per-face `SupportType.FULL` sturdiness test, defaulting to the `isFaceSturdy(level, pos, direction, SupportType.FULL)` overload (`BlockBehaviour.BlockStateBase.isFaceSturdy:885-891`); `SupportType.FULL.isSupporting` (`SupportType.java:12-16`) is `Block.isFaceFull(state.getBlockSupportShape(level, pos), direction)`, where `isFaceFull` (`Block.java:345-348`) projects the shape onto that face and checks full [0,1]x[0,1] coverage; `getBlockSupportShape` (`BlockBehaviour.java:291-293`, wrapper at `:693-695`) defaults to `getCollisionShape` unless a block overrides it - verified against the reference that no block in this blueprint's own tier-1/tier-2 placeable set (wire, torch, repeater, comparator, piston, chest, furnace, hopper, terrain) overrides it, so support shape and collision shape coincide for all of them | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5); support-shape citations added for M4-B06 decision 16, claude-sonnet-5 | 2026-09-03 |
| The neighbor_gate value in `spread_to_sides` only gates whether sideways spreading happens at all; the actual fluid amount placed at each spread-to neighbor comes independently from that neighbor's own `get_new_liquid` recompute inside `get_spread`, never from the gate value itself. | `FlowingFluid.spreadToSides:145-160` - `neighbor` is consumed only by `if (neighbor > 0)` (:151); the states actually placed are the map values of `getSpread`, which `getSpread:380` fills from `this.getNewLiquid(level, testPos, testState)` per target | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `getFlow` reads each neighbor's height via `getOwnHeight`, not `getHeight`, so the same-fluid-directly-above adjustment to 1.0 never applies when computing a neighbor's contribution to the flow vector. | `FlowingFluid.getFlow:66` and `:73` both call `getOwnHeight()`; `getHeight` with its `hasSameAbove` 1.0F adjustment (`FlowingFluid.getHeight:472-475`) is never invoked on a neighbor inside `getFlow` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| This project's `FluidVariant` carries a falling bit on both the Source and Flowing variants, though it is only meaningful — and only ever `true` — for Flowing here; vanilla's own `FluidState` also allows a falling Source (e.g. a waterlogged copper grate's `Fluids.WATER.getSource(true)`, which does take `getFlow`'s falling redirect), a case this crate's own encoding simply never produces. | `FlowingFluid.createFluidStateDefinition:51-54` adds `FALLING` to every `FlowingFluid`, source variants included (only the `Flowing` subclasses additionally add `LEVEL`) - so the first half is exact. "Only ever true on the flowing variant" is false: `WaterloggedTransparentBlock.getFluidState:57-60` returns `Fluids.WATER.getSource(true)`, a SOURCE state with FALLING set, and `FlowingFluid.getFlow:91` reads `fluidState.getValue(FALLING)` without checking the variant, so such a source does take the -6.0 y redirect path | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| When `getSpread`'s candidate for a target resolves to no fluid state, `spread_to` writes air at that position rather than a fluid block, matching `EMPTY.createLegacyBlock()`'s own vanilla behavior. | `FlowingFluid.spreadTo:270-280` writes `target.createLegacyBlock()` (:278); `FluidState.createLegacyBlock:99-101` delegates to the type and `EmptyFluid.createLegacyBlock:59-62` returns `Blocks.AIR.defaultBlockState()`. `getNewLiquid:197` can yield `Fluids.EMPTY.defaultFluidState()`, which `getSpread:380-400` can store as a candidate | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `spread_to` checks whether the target can hold the incoming fluid as a waterlogged container before ever hard-overwriting a non-air target, matching `FlowingFluid.spreadTo`'s own `instanceof LiquidBlockContainer` check ordering; a target that accepts it is waterlogged in place instead of being destroyed and replaced. | `FlowingFluid.spreadTo:270-280` - the `state.getBlock() instanceof LiquidBlockContainer container` branch runs `container.placeLiquid(...)` and the destroy-then-`setBlock` else-branch is reached only for non-containers. Two nuances: at this point vanilla tests container-ness with a bare `instanceof` and does NOT call `canPlaceLiquid` (that acceptance test already ran in `canHoldSpecificFluid:434-436` at the caller); and when `placeLiquid` returns false nothing is written at all - there is no fall-through to the hard overwrite | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `SimpleWaterloggedBlock`'s shared default implementation accepts only water, never lava, as the fluid it can hold. | `SimpleWaterloggedBlock.canPlaceLiquid:19-22` - `return type == Fluids.WATER;`; `placeLiquid:24-36` additionally requires `fluidState.is(Fluids.WATER)` and `WATERLOGGED == false` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Placing a fluid into a waterloggable container via spread schedules a new fluid tick at that same position; in vanilla this re-arm belongs to the container's own `placeLiquid` implementation, not to `FlowingFluid.spreadTo`, which schedules nothing at all. | The tick is scheduled, but not by `FlowingFluid.spreadTo:270-280`, which schedules nothing at all. The re-arm lives in the container implementation: `SimpleWaterloggedBlock.placeLiquid:25-32` does `level.setBlock(pos, state.setValue(WATERLOGGED, true), 3);` followed by `level.scheduleTick(pos, fluidState.getType(), fluidState.getType().getTickDelay(level));` - server side only, and only when the block was not already waterlogged | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `willTickThisTick(pos, type)` guard, used before scheduling a duplicate tick, returns true only when `pos` is among the tick entries in the batch currently being processed this tick, not merely whenever any tick is pending, due or not. | `LevelTicks.willTickThisTick:207-211` - `this.calculateTickSetIfNeeded(); return this.toRunThisTickSet.contains(ScheduledTick.probe(type, pos));`, over the set built from `toRunThisTick` for the tick currently being processed (`calculateTickSetIfNeeded:213-217`). The coarse "any entry queued" query is the separate `LevelTicks.hasScheduledTick:201-205` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| This blueprint's own, project-added `willTickThisTick`-equivalent guard — vanilla's own fluid path applies no such guard at any site — applies only at neighbor-changed, shape-update, and waterlog-placement re-arm sites; it never suppresses a scheduled tick's own unconditional self-reschedule after that same tick changes its own state. | Vanilla's fluid path applies no `willTickThisTick` guard at any of those three sites: `LiquidBlock.neighborChanged:199-201`, `LiquidBlock.updateShape:180-182` and `SimpleWaterloggedBlock.placeLiquid:29` all call `scheduleTick` unconditionally. A repo-wide search finds `willTickThisTick` called only from `ComparatorBlock.java`, `DiodeBlock.java`, `RedstoneTorchBlock.java` and `TestBlockEntity.java` - never from fluid code. The second half of the claim does hold trivially: `FlowingFluid.tick:457` also schedules unconditionally. The guard placement described here is therefore this project's own design decision, not a vanilla fact | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A source cell's own scheduled tick never invokes `get_new_liquid` on itself; only a non-source cell's tick recomputes its state, while a source cell always proceeds directly to spreading with its existing state unchanged. | `FlowingFluid.tick:445-462` - the `getNewLiquid` recompute and everything derived from it sit inside `if (!fluidState.isSource())` (:446-459); a source falls straight through to `this.spread(level, pos, blockState, fluidState)` at :461 with its unchanged state | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| When a non-source cell's recompute yields a state identical to its current one, no self-reschedule occurs on that tick; the cell ticks again only once a future neighbor-changed event re-arms it. | `FlowingFluid.tick:453-458` - the rewrite-and-`scheduleTick` path is `else if (newFluidState != fluidState)`, a reference comparison over interned `FluidState` instances; an unchanged recompute takes neither branch and schedules nothing | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The passability check used by the down-spread and slope-search paths additionally rejects a target that is already a source of the same fluid kind, independent of `canBeReplacedWith`'s own asymmetric water/lava rules. | `FlowingFluid.canMaybePassThrough:336-348` opens with `!this.isSourceBlockOfThisType(testFluidState)` (`isSourceBlockOfThisType:350-352` = same type AND `isSource()`), independent of any `canBeReplacedWith` call. It gates the down-spread (`spread:125`), `getSpread:379`, and via `canPassThrough:322-334` the slope search (`getSlopeDistance:294`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `can_hold_specific_fluid` in real vanilla defers to the target's own waterloggable-container acceptance check when the target is registered as a fluid container, and otherwise defaults to true, with no denylist of its own; vanilla's fixed denylist lives in the separate `can_hold_any_fluid`, combined with the specific-fluid check only inside `can_hold_fluid`, which `is_hole` uses. | `FlowingFluid.canHoldSpecificFluid:434-436` - `state.getBlock() instanceof LiquidBlockContainer container ? container.canPlaceLiquid(null, level, pos, state, newFluid) : true`. The container and default-true halves are exact, but "subject only to the denylist" is not: this function contains no denylist and no `blocksMotion` test. The denylist, the `blocksMotion` rejection and the "any LiquidBlockContainer passes unconditionally" shortcut all live in the separate `canHoldAnyFluid:411-428`; the two are combined only in `canHoldFluid:430-432`, used by `isWaterHole:318` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| On a lava cell's neighbor-changed dispatch, the contact-conversion check runs first; a fluid-tick re-arm is only considered if that check does not fire a reaction. | `LiquidBlock.neighborChanged:195-201` - `if (this.shouldSpreadLiquid(level, pos, state)) { level.scheduleTick(pos, state.getFluidState().getType(), this.fluid.getTickDelay(level)); }`; `shouldSpreadLiquid:215-239` returns false exactly when it fired a conversion, which suppresses the re-arm | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
