# M4-B07 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| World bottom is at Y = -64 (`WORLD_MIN_Y = -64`). | `datagen/generated/data/minecraft/dimension_type/overworld.json`, `"min_y": -64` (overworld only — `the_nether.json`/`the_end.json` both carry `"min_y": 0`, `"height": 256`); `LevelHeightAccessor.getMinY()` is the dimension-type value | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The world spans 384 blocks vertically (`WORLD_HEIGHT = 384`). | `datagen/generated/data/minecraft/dimension_type/overworld.json`, `"height": 384` (and `"logical_height": 384`); overworld only, as above | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| There are 24 real block sections per column (`SECTION_COUNT = 24`). | `LevelHeightAccessor.getSectionsCount()` lines 15-25: `getMaxSectionY() - getMinSectionY() + 1` with `getMinSectionY() = blockToSectionCoord(minY)`; for the overworld `blockToSectionCoord(319) - blockToSectionCoord(-64) + 1 = 19 - (-4) + 1 = 24` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's block-index formula within one section is `block_index(x, local_y, z) = (local_y << 8) bitwise-or (z << 4) bitwise-or x` (4096 entries per section, vanilla's own axis order). | `net/minecraft/world/level/chunk/Strategy.java:69-71` — `getIndex(x,y,z)` is `(y shifted left by bitsPerAxis, bitwise-or z), shifted left by bitsPerAxis, bitwise-or x`, with `bitsPerAxis = 4` for a 16-cubed section, i.e. `y<<8` or-ed with `z<<4` or-ed with `x`; `DataLayer.getIndex` (`world/level/chunk/DataLayer.java:39-41`) uses the identical expression for light nibbles; 16 cubed = 4096 entries | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's light-section layout pads the tracked column with one extra section below the lowest real block section and one above the highest, giving 26 light sections total (`SECTION_COUNT + 2`), matching `LevelLightEngine.LIGHT_SECTION_PADDING`'s own hard edge. | `LevelLightEngine.java:13` `LIGHT_SECTION_PADDING = 1`; `:157-167` `getLightSectionCount() = getSectionsCount() + 2`, `getMinLightSection() = getMinSectionY() - 1`, `getMaxLightSection() = getMinLightSection() + getLightSectionCount()`; 24 + 2 = 26 for the overworld | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Each light section's 4096 light values are packed as 4-bit nibbles, two nibbles per byte, in a 2048-byte array. | `world/level/chunk/DataLayer.java:9-11` (`LAYER_COUNT = 16`, `LAYER_SIZE = 128`, `SIZE = 2048`), `:13` `NIBBLE_SIZE = 4`, `:26-29` constructor rejects any array whose length is not 2048, `:44-68` `getByteIndex(index) = index >> 1` and `getNibbleIndex(index) = index & 1` with a 4-bit shift and a 15 mask | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The `WorldSurface` heightmap value for a column is (highest non-air block's Y) + 1, using a `!is_air` predicate only — opacity and transparency play no role, so a transparent non-air block such as glass or water is itself counted as the topmost block and the recorded Y sits one block above it. | `Heightmap.java:148` — `WORLD_SURFACE` is declared with the `NOT_AIR` predicate (`:30`, `input -> !input.isAir()`), and `Heightmap.primeHeightmaps` (`:56-67`) records `y + 1` for the highest Y satisfying that predicate. CORRECTION: the value is exactly (highest non-air block's Y) + 1. Opacity/transparency plays no part in `WORLD_SURFACE` — a transparent but non-air block (glass, water) satisfies `NOT_AIR`, so the recorded Y sits one block *above* such a block, never at it; the "air-or-transparent" gloss would place the boundary at the glass's own Y | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Sky light and block light are two independent channels that share one propagator algorithm but run separately, with no shared queue and no shared state between the two channels. | `LevelLightEngine.java:16-22` holds a separate `BlockLightEngine` and `SkyLightEngine` instance, `:48-59` runs `runLightUpdates()` on each in turn (block first, then sky); `LightEngine.java:28-30` declares `blockNodesToCheck`, `decreaseQueue`, `increaseQueue` as per-instance fields, so the two channels share no queue and no storage. The shared algorithm is the abstract base (`LightEngine.runLightUpdates`/`propagateIncreases`/`propagateDecreases`/`QueueEntry`); each channel supplies its own `checkNode`/`propagateIncrease`/`propagateDecrease` (`:219-223`), and `SkyLightEngine` adds an extra empty-section fast path (`propagateFromEmptySections`, `SkyLightEngine.java:231-268`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla maintains exactly one increase queue and one decrease queue per light channel for the whole level (never per chunk), with each queue entry carrying an absolute packed block position; the per-chunk queue split is this blueprint's own WORLD-D9 parallelization design, not a vanilla fact. | `LightEngine.java:29-30` — `decreaseQueue`/`increaseQueue` are two `LongArrayFIFOQueue` fields of the *engine*, i.e. one pair per channel for the whole level, shared by every chunk; entries carry an absolute packed block position, and `getChunk`/`lastChunkPos` (`:31-33`, `:87-106`) exists precisely because consecutive entries in one queue may belong to different chunks. CORRECTION: vanilla has exactly one increase and one decrease queue per channel per level, never per chunk — the per-chunk split is this blueprint's own parallelization design (WORLD-D9), not a vanilla fact | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The propagation algorithm is push-based: a queued item names a position whose value just became authoritative, and processing it pushes further work onto neighbors only when a neighbor's value must actually change, never by pulling from neighbors. | `BlockLightEngine.propagateIncrease` (`:46-79`) and `propagateDecrease` (`:82-109`): both dequeue a `fromNode` whose own stored level is authoritative, iterate `PROPAGATION_DIRECTIONS`, and only `setStoredLevel`/`enqueue*` at a neighbor when the neighbor's stored value must change (`newToLevel > toLevel`, `toLevel <= oldFromLevel - 1`); no code path reads a set of neighbors to recompute the dequeued position's own value | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In the block-light channel's `check_node`, when a block's emission decreases below the position's currently stored light level, vanilla zeroes the stored value and enqueues a decrease entry carrying the pre-zeroing stored level with all 6 directions. | `BlockLightEngine.checkNode:30-34` — `oldLevel = storage.getStoredLevel(node)`; `if (lightEmission < oldLevel) { storage.setStoredLevel(node, 0); enqueueDecrease(node, QueueEntry.decreaseAllDirections(oldLevel)); }`; `QueueEntry.decreaseAllDirections` (`LightEngine.java:241-243`) sets the full 6-bit direction mask 1008 and the given level | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In the block-light channel's `check_node`, the decrease-vs-pull-request branch is chosen by a single comparison between the block's current emission and the position's currently stored light level: if emission is less than the stored level, vanilla takes the decrease branch; otherwise (emission greater than or equal to the stored level — regardless of whether only opacity or shape changed) vanilla enqueues a synthetic decrease-queue "pull request" entry with `from_level: 1` and all 6 directions, letting already-lit neighbors probe whether they can push a brighter value back into the position (the restatement of vanilla's `PULL_LIGHT_IN_ENTRY` sentinel). | `LightEngine.java:23` `PULL_LIGHT_IN_ENTRY = QueueEntry.decreaseAllDirections(1)`; `BlockLightEngine.checkNode:35-37` enqueues it on the decrease queue — the sentinel's shape (level 1, all 6 directions, decrease queue) and its effect (`propagateDecrease:102-104` takes the `else` branch for every lit neighbor and enqueues a single-direction increase probe back toward the position) are exact. CORRECTION: the branch condition is misstated. Vanilla tests only `lightEmission < oldLevel`, where `oldLevel` is the position's *currently stored light level* (not its previous emission); the sentinel is the `else` of that single comparison, i.e. it fires exactly when the block's current emission is greater than or equal to the stored level. An opacity-or-shape-only change at a position lit by a neighbor (emission 0, stored 10) therefore takes the *decrease* branch, not the pull branch — "or only its opacity/shape changed" is false as written | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In the block-light channel's `check_node`, when a block's new emission is greater than 0, vanilla additionally enqueues an increase entry at that emission's magnitude with all 6 directions and `increase_from_emission` set. | `BlockLightEngine.checkNode:39-41` — `if (lightEmission > 0) enqueueIncrease(node, QueueEntry.increaseLightFromEmission(lightEmission, isEmptyShape(state)))`; `QueueEntry.increaseLightFromEmission` (`LightEngine.java:245-253`) sets the full direction mask 1008 plus the `FLAG_INCREASE_FROM_EMISSION` bit 2048 (and the `FLAG_FROM_EMPTY_SHAPE` bit 1024 when the source state is an empty shape). Note the emission read is `getEmission` (`BlockLightEngine.java:111-114`), which reports 0 while the section's light is off | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The sky-light channel's `check_node` differs structurally from the block-light channel's: it first (conditionally) runs a whole-column sky-source-boundary maintenance pass that writes stored levels and enqueues work on its own; for a source position it enqueues both a decrease entry and an increase entry at `from_level: 15` with `directions: all_except(Up)` (five directions, not all six) and no `increase_from_emission` flag on the increase entry; and for a non-source position the decrease-vs-pull-request branch is chosen by whether the position's current stored level is greater than 0, not by any emission-style comparison. | `SkyLightEngine.checkNode:48-73` differs structurally from `BlockLightEngine.checkNode:26-43` in four ways. (1) It first runs a whole-column source maintenance pass, `updateSourcesInColumn`/`removeSourcesBelow`/`addSourcesAbove` (`:75-136`), which itself writes stored levels and enqueues work — block light has no analogue. (2) For a source position it enqueues *both* `REMOVE_SKY_SOURCE_ENTRY` and `ADD_SKY_SOURCE_ENTRY` (`:61-62`), which are `decreaseSkipOneDirection(15, UP)` and `increaseSkipOneDirection(15, false, UP)` (`:18-19`) — five directions, skipping UP, not all 6. (3) That increase entry does **not** carry the `INCREASE_FROM_EMISSION` flag, so there is no lazy bump; the level 15 is written by `addSourcesAbove:129`. (4) For a non-source position the decrease-vs-pull branch is chosen by `oldLevel > 0` (`:64-70`), not by any emission comparison | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_increase_step`, when an entry is marked `increase_from_emission` and the position's current stored level is still below the entry's `from_level`, vanilla bumps the stored value up to `from_level` before propagating further (lazy materialization of emission). | `LightEngine.propagateIncreases:160-165` — `if (QueueEntry.isIncreaseFromEmission(increaseData) && fromLevel < fromTargetLevel) { storage.setStoredLevel(fromNode, fromTargetLevel); fromLevel = fromTargetLevel; }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_increase_step`, if a dequeued entry's `from_level` no longer matches the position's current stored level (after any lazy bump), the entry is stale and is discarded without further propagation. | `LightEngine.propagateIncreases:167-169` — `if (fromLevel == fromTargetLevel) this.propagateIncrease(...)`; there is no `else`, so a mismatch drops the entry silently | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_increase_step`, for each direction being propagated, the maximum value that can reach the neighbor is `from_level.saturating_sub(1)`, and propagation in that direction stops immediately if that value is 0. | `BlockLightEngine.propagateIncrease:54-55` (and `SkyLightEngine.propagateIncrease:148-149`) — `int maxPossibleNewToLevel = fromLevel - 1; if (maxPossibleNewToLevel > toLevel) { ... }`. Vanilla carries no separate zero test; the guard subsumes it, since a stored level is never negative, so `maxPossibleNewToLevel == 0` can never satisfy the comparison and the direction is skipped before the neighbor's state is read | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_increase_step`, propagation toward a neighbor is skipped early, without even reading the neighbor's block properties, whenever the computed `max_possible` is not strictly greater than the neighbor's current stored value. | `BlockLightEngine.propagateIncrease:53-57` — `toLevel` is read from light storage first, the `maxPossibleNewToLevel > toLevel` test gates everything, and `getState(toNode)` is only called inside that branch (same shape at `SkyLightEngine.propagateIncrease:147-151`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_increase_step`, the new value written at a neighbor is `new_level = from_level.saturating_sub(get_opacity(neighbor_properties))`, written only if `new_level` is strictly greater than the neighbor's current stored value. | `BlockLightEngine.propagateIncrease:58-67` — `int newToLevel = fromLevel - this.getOpacity(toState); if (newToLevel > toLevel) { ... storage.setStoredLevel(toNode, newToLevel); }`, with `getOpacity` = `Math.max(1, state.getLightDampening())` (`LightEngine.java:77-79`). The write is additionally gated on `!shapeOccludes(fromState, toState, direction)` (`:66`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_increase_step`, when a neighbor's new value exceeds 1, vanilla enqueues a further increase entry that fans out to every direction except the one just walked in from — propagation never immediately bounces straight back toward its own source direction. | `BlockLightEngine.propagateIncrease:68-72` — `if (newToLevel > 1) enqueueIncrease(toNode, QueueEntry.increaseSkipOneDirection(newToLevel, isEmptyShape(toState), propagationDirection.getOpposite()))`; `increaseSkipOneDirection` (`LightEngine.java:255-262`) clears exactly the named direction bit from the full 1008 mask (same shape at `SkyLightEngine.propagateIncrease:162-166`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_decrease_step`, a dequeued entry's `from_level` field records the value the position just dropped from, not the position's current value. | `BlockLightEngine.propagateDecrease:83` reads `oldFromLevel = QueueEntry.getFromLevel(decreaseData)`; every producer supplies the pre-zeroing value — `checkNode:33-34` zeroes the stored level then enqueues `decreaseAllDirections(oldLevel)`, and `propagateDecrease:94-96` sets the neighbor to 0 then enqueues `decreaseSkipOneDirection(toLevel, ...)` with the neighbor's former level | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_decrease_step`, if a neighbor's current stored value is 0, the step does nothing further for that neighbor. | `BlockLightEngine.propagateDecrease:89-90` — `int toLevel = storage.getStoredLevel(toNode); if (toLevel != 0) { ... }` (same shape at `SkyLightEngine.propagateDecrease:186-187`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_decrease_step`, if a neighbor's current stored value is less than or equal to `from_level.saturating_sub(1)`, the neighbor's value could only have derived from the position being processed, so vanilla zeroes the neighbor's stored value. | `BlockLightEngine.propagateDecrease:91-94` — `if (toLevel <= oldFromLevel - 1) { ... storage.setStoredLevel(toNode, 0); ... }` (same test at `SkyLightEngine.propagateDecrease:188-189`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_decrease_step`, after zeroing such a neighbor, if the neighbor's own baseline source strength is less than its former stored value, vanilla cascades the darkness further by enqueueing another decrease entry at the neighbor carrying its former current value. | `BlockLightEngine.propagateDecrease:93-97` — `int toEmission = this.getEmission(toNode, toState); ... if (toEmission < toLevel) enqueueDecrease(toNode, QueueEntry.decreaseSkipOneDirection(toLevel, propagationDirection.getOpposite()))`; the cascade skips the direction back toward the position being processed. The sky channel has no source comparison at all — `SkyLightEngine.propagateDecrease:190` enqueues that cascade unconditionally, because sky sources are re-established by the column-source pass instead | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_decrease_step`, after zeroing such a neighbor, if the neighbor's own baseline source strength is greater than 0, vanilla does not write that source strength back at the neighbor immediately — it enqueues a full-fan-out increase entry with `increase_from_emission` set, and the neighbor's own baseline glow is materialized lazily once that entry is actually propagated, so the neighbor reclaims it without waiting for a separate change event but without an immediate write either. | `BlockLightEngine.propagateDecrease:99-101` — `if (toEmission > 0) enqueueIncrease(toNode, QueueEntry.increaseLightFromEmission(toEmission, isEmptyShape(toState)))`. The full-fan-out part is right (`increaseLightFromEmission` uses the full 1008 direction mask). CORRECTION: vanilla does **not** write the source strength at the neighbor here — the neighbor's stored level stays 0 after `:94`, and the value is materialized later, lazily, by `LightEngine.propagateIncreases:162-165`, precisely because the entry carries the `INCREASE_FROM_EMISSION` flag. The blueprint's variant (immediate write plus an entry with `increase_from_emission: false`) is a deviation: any later decrease in the same drain that re-zeroes that position makes the flagless entry stale and it is discarded at `:167`, whereas vanilla's flagged entry re-bumps the value | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In `propagate_decrease_step`, if a neighbor's current stored value is strictly greater than `from_level.saturating_sub(1)`, the neighbor has an independent, still-valid, stronger source, and vanilla enqueues a single-direction "probe" onto the increase queue, fanning out in exactly the one direction back toward the darkened position, never a full 6-direction fan-out. | `BlockLightEngine.propagateDecrease:102-104` — the `else` of `toLevel <= oldFromLevel - 1` enqueues `QueueEntry.increaseOnlyOneDirection(toLevel, false, propagationDirection.getOpposite())`; `increaseOnlyOneDirection` (`LightEngine.java:264-272`) starts from a zero mask and sets exactly the one direction bit (same shape at `SkyLightEngine.propagateDecrease:192-193`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Within one chunk's one round, the decrease queue is drained to empty completely before the increase queue is touched at all; decreases may enqueue increases, but increases never enqueue decreases. | `LightEngine.runLightUpdates:137-153` — every pending `checkNode` runs first, then `propagateDecreases()` (`:175-184`, drains `decreaseQueue` to empty), then `propagateIncreases()` (`:155-173`). `propagateDecrease` enqueues onto both queues (`BlockLightEngine.java:96`, `:100`, `:103`); `propagateIncrease` only ever calls `enqueueIncrease` (`:69-71`), never `enqueueDecrease`. Vanilla drains per engine run, not per chunk — the per-chunk round is this blueprint's own scheduling layer | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Scalar light opacity (vanilla's `getLightDampening` value) ranges 0..=15. | `BlockBehaviour.getLightDampening:299-305` is the only default and yields 15, 0 or 1; the only two overrides in the whole tree are `LeavesBlock.getLightDampening:84-86` (1) and `TintedGlassBlock.getLightDampening:25-27` (15), so no vanilla block state produces a value outside 0..=15 (in fact only 0, 1 and 15 occur). The 4-bit storage (`DataLayer`, `QueueEntry.LEVEL_MASK = 15`) and `MAX_LEVEL = 15` (`LightEngine.java:21`) bound what a larger value could mean; the sentinel 16 appears only as `LightEngine.getLightDampeningInto`'s "fully occluded" return (`:50-60`), never as a block's own dampening | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A fully solid-rendering block has opacity 15. | `BlockBehaviour.getLightDampening:299-301` — `if (state.isSolidRender()) return 15;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A non-full block that "propagates skylight down" has opacity 0. | `BlockBehaviour.getLightDampening:302-303` — the non-solid-render branch returns `state.propagatesSkylightDown() ? 0 : 1` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Every other non-full block has opacity 1. | `BlockBehaviour.getLightDampening:302-303` — same expression, the `: 1` arm | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The effective opacity used by propagation is `props.opacity.max(1)`, a minimum opacity floor of 1, so every propagation hop costs at least 1 light level regardless of a block's declared opacity. | `LightEngine.getOpacity:77-79` — `return Math.max(1, state.getLightDampening());`, with `MIN_OPACITY = 1` declared at `:22`; both engines subtract exactly this value (`BlockLightEngine.propagateIncrease:58`, `SkyLightEngine.propagateIncrease:152`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's shape-occlusion veto for a propagation step in direction `dir` fires if either the source block's face in `dir`, or the destination block's face in `dir.opposite()`, is declared to fully occlude the shared face between them — either side alone is sufficient. | `LightEngine.shapeOccludes:81-85` — `Shapes.faceShapeOccludes(getOcclusionShape(fromState, direction), getOcclusionShape(toState, direction.getOpposite()))`; `Shapes.faceShapeOccludes:290-296` returns `true` immediately when *either* argument is the full block shape, so either side alone is indeed sufficient. Vanilla's complete rule is broader than the claim states but never narrower: when neither side is a full face it tests whether the OR-union of the two partial faces covers the whole face (`:294`), so two partial faces can occlude jointly — the boolean-per-face model of Context §3 is a deliberate sufficient-condition simplification, which the blueprint's own prose already flags | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| For an ordinary block that does not opt into shape-based occlusion (the vanilla default), every face is non-occluding and the shape-occlusion veto never fires, so only the scalar opacity subtraction governs; shape-accurate occlusion is opted into by only a handful of blocks. | `BlockBehaviour.useShapeForLightOcclusion:227-229` returns `false` by default; `LightEngine.isEmptyShape:66-68` is `!state.canOcclude() or !state.useShapeForLightOcclusion()`, so a default block yields `Shapes.empty()` from `getOcclusionShape:62-64` and `Shapes.faceShapeOccludes(empty, empty)` is `false` (`Shapes.java:294`). Exactly 14 blocks override `useShapeForLightOcclusion` (daylight detector, dirt path, enchanting table, end portal frame, farmland, lectern, piston base, piston head, sculk sensor, sculk shrieker, shelf, slab, snow layer, stairs, stonecutter — grep of the whole tree) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's air block is the default light-property state: fully transparent (zero opacity) and non-emitting (zero block-light emission). | Air is not solid-render and its shape is empty with no fluid, so `BlockBehaviour.propagatesSkylightDown:397-399` is true and `getLightDampening:299-305` yields 0; emission defaults to `state -> 0` (`BlockBehaviour.Properties.lightEmission`, `:983`) and air never calls `lightLevel(..)`. `LightEngine.propagateIncrease` itself substitutes `Blocks.AIR.defaultBlockState()` for an empty-shape source (`BlockLightEngine.java:61-63`). Note the engine's fallback for a *missing* chunk is bedrock, not air (`LightEngine.getState:70-75`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Block-light emission is a value 0..=15 (`MAX_LEVEL = 15`). | `LightEngine.java:21` `MAX_LEVEL = 15`; emission defaults to 0 (`BlockBehaviour.Properties.lightEmission`, `:983`) and is stored per state at `:463`/`:581`. A level is physically 4 bits everywhere it travels — `QueueEntry.FROM_LEVEL_BITS = 4` with `LEVEL_MASK = 15` (`LightEngine.java:226-228`) and `DataLayer`'s 4-bit nibbles — so 15 is the representable maximum; no vanilla `lightLevel(..)` call exceeds it | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Sky light has no per-block emission field of its own; its source strength derives from column position instead. | `getEmission` exists only on the block channel (`BlockLightEngine.java:111-114`); `SkyLightEngine` decides source status purely from the column's lowest-source Y (`:37-40`, `:53-59` `isSource = y >= lowestSourceY`, backed by `ChunkSkyLightSources`) and uses the fixed magnitude 15 (`isSourceLevel:33-35`, `:129`, `:17-19`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A block's `propagates_skylight_down` property follows vanilla's default rule (non-full shape and no fluid) only for blocks that do not override it; vanilla overrides the rule in both directions for 15 block classes — five, including the transparent-glass family, return `true` unconditionally regardless of shape (a full-cube glass block still propagates skylight down), four return `false` unconditionally, four test only the fluid condition, and two test only whether the block is waterlogged. | That is the *default* implementation only — `BlockBehaviour.propagatesSkylightDown:397-399`, `!Block.isShapeFullBlock(state.getShape(EmptyBlockGetter.INSTANCE, BlockPos.ZERO)) && state.getFluidState().isEmpty()`. CORRECTION: 15 overriding classes break the stated rule in both directions, not 13. `TransparentBlock.propagatesSkylightDown:35-37`, `BambooStalkBlock:57-59`, `HangingMossBlock:55-57`, `MossyCarpetBlock:99-101` and `VineBlock:74-76` return `true` unconditionally — glass and its family have a full cube shape yet propagate skylight down (which is exactly how a glass roof passes sky light, the case Context §6 relies on); `TintedGlassBlock:20-22`, `ShulkerBoxBlock:161-163`, `LiquidBlock:114-116` and `PipeBlock:59-61` return `false` unconditionally; `BarrierBlock:40-42`, `VegetationBlock:50-52`, `LightBlock:71-73` and `GlowLichenBlock:47-49` test only the fluid condition; `CrossCollisionBlock:71-73` and `WallBlock:261-263` test only whether the block is waterlogged | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla packs a light propagation queue entry's 6-direction fan-out metadata into bit-flags inside one packed `u64` field. | `LightEngine.QueueEntry:225-326` — a queue entry is a `long`; `DIRECTION_BITS = 6`, `DIRECTIONS_MASK = 1008` (bits 4..9), `withDirection`/`withoutDirection`/`shouldPropagateInDirection` shift by `direction.ordinal() + 4` (`:311-325`), with the level in bits 0..3 and the two flag bits 1024/2048 above the direction field. Each queue holds two longs per entry, the packed position then the packed metadata (`:186-194`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A light value's magnitude is capped at 15 and decays by at least 1 level per propagation hop, so a single light-affecting change can never travel more than 15 blocks from its origin in any direction. | `LightEngine.java:21` `MAX_LEVEL = 15` and `:22` `MIN_OPACITY = 1`, enforced by `getOpacity:77-79` (`Math.max(1, ...)`); the per-hop value is `fromLevel - getOpacity(toState)` and is only written when strictly greater than the neighbor's own value (`BlockLightEngine.propagateIncrease:58-67`), and no further entry is enqueued once the value reaches 1 (`:68`). 15 successive hops therefore exhaust the magnitude | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A chunk is 16 blocks wide horizontally, so a light value can cross at most one chunk boundary before fully decaying, barring a corner case that can cross two boundaries near a chunk corner. | `SectionPos.java:18` `SECTION_SIZE = 16`, the same 16-wide horizontal extent used by `ChunkSkyLightSources` (`SIZE = 16`, `index(x,z) = x + z*16`, `:19`, `:188-190`) and by every 16-cubed section index (`Strategy.getIndex:69-71`). The travel bound is arithmetic from that width plus the 15-level cap and 1-per-hop decay confirmed above; the reference states no such derived rule of its own | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A position `(x, world_y, z)` is a sky-light source (level 15, no decay needed) if and only if its world Y is at or above the column's sky-source boundary Y. | `SkyLightEngine.checkNode:59` — `boolean isSource = y >= lowestSourceY;`, with `lowestSourceY` from `ChunkSkyLightSources.getLowestSourceY` (`:37-40`); `addSourcesAbove:123-131` writes stored level 15 for every Y from the boundary upward, and `isSourceLevel:33-35` defines a source as exactly the value 15. A column with no occluder at all returns `Integer.MIN_VALUE` (`ChunkSkyLightSources.extendSourcesBelowWorld:184-186`), making every Y a source | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's sky-source boundary Y scan does not use any heightmap: it starts at the top of the chunk's highest non-empty section, and its per-pair stop test is `isEdgeOccluded` — the lower block's light dampening being nonzero, or a two-sided face-occlusion test between the adjacent pair — stopping at, and returning, the upper block's Y of the first occluding pair (one above the occluder), or the world floor minus one if the scan reaches the bottom without one; the result is maintained in a dedicated per-chunk `BitStorage`, updated incrementally on every block change, not derived from `WorldSurface` or any other heightmap. | `ChunkSkyLightSources.findLowestSourceY:47-75` and `isEdgeOccluded:140-148`. CORRECTION on all three points. (a) The scan starts at the top of the chunk's highest non-empty section (`sectionToBlockCoord(getSectionYFromSectionIndex(topSectionIndex) + 1)`, `:48`), not at a heightmap value; the `WORLD_SURFACE` heightmap is never read by the lighting package, and sky sources live in their own per-chunk 256-entry `BitStorage` (`:26-31`, `fillFrom:33-45`), maintained incrementally by `update`/`updateEdge`/`findLowestSourceBelow` (`:77-138`). (b) The stop test is `isEdgeOccluded(topState, bottomState)` = `bottomState.getLightDampening() != 0` OR `Shapes.faceShapeOccludes(occlusionShape(top, DOWN), occlusionShape(bottom, UP))` — it is the lower block's *light dampening* plus a two-sided face-occlusion test between the adjacent pair, and `propagatesSkylightDown` is not consulted anywhere in `net/minecraft/world/level/lighting/`. (c) The returned boundary is the *upper* block's Y of the first occluding pair (`:64`, `:129`) — i.e. the lowest Y that is still a source, one above the occluder; a scan that reaches the bottom returns `minY = level.getMinY() - 1` (`:27`, `:74`, `:137`) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla retains a chunk's persisted/saved light data on chunk load rather than recomputing it, when the persisted light status was already recorded as correct. | `SerializableChunkData.parse:125` reads the NBT flag `isLightOn` into `lightCorrect` (written back at `:461-463`); `:241-256` feeds every saved section's `BlockLight`/`SkyLight` nibble array back into the engine via `lightEngine.retainData(pos, true)` + `queueSectionData(..)`, and `:293` restores `chunk.setLightCorrect(..)`. `ChunkStatusTasks.isLighted:33-35` is `persistedStatus.isOrAfter(ChunkStatus.LIGHT) && chunk.isLightCorrect()`, and `ThreadedLevelLightEngine.lightChunk:164-180` skips `propagateLightSources(pos)` entirely when that flag is set | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At protocol 776, the `Level Chunk with Light` packet's id is `0x2D`. | `datagen/generated/reports/packets.json`, `play.clientbound["minecraft:level_chunk_with_light"].protocol_id = 45`; protocol version 776 confirmed at `SharedConstants.java:20` (`RELEASE_NETWORK_PROTOCOL_VERSION = 776`). CORRECTION: the id is 45 decimal = `0x2D`, not `0x2C`. For reference, `minecraft:light_update` is 48 decimal = `0x30`, and the neighbouring ids are `minecraft:forget_level_chunk` 37 (`0x25`) and `minecraft:chunks_biomes` 13 (`0x0D`) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The `Level Chunk with Light` packet's light-relevant fields are, in order: `sky_light_mask` (BitSet), `block_light_mask` (BitSet), `empty_sky_light_mask` (BitSet), `empty_block_light_mask` (BitSet), a VarInt-count-prefixed `sky_light_arrays` whose own entries each additionally carry their own VarInt length prefix (always 2048) before their 2048 bytes, and a VarInt-count-prefixed `block_light_arrays` with the identical two-level prefixing. | `ClientboundLightUpdatePacketData.write:59-66` — `writeBitSet(skyYMask)`, `writeBitSet(blockYMask)`, `writeBitSet(emptySkyYMask)`, `writeBitSet(emptyBlockYMask)`, `writeCollection(skyUpdates, DATA_LAYER_STREAM_CODEC)`, `writeCollection(blockUpdates, DATA_LAYER_STREAM_CODEC)`, embedded after the chunk data by `ClientboundLevelChunkWithLightPacket.write:42-47`. The field list and its order are exact. CORRECTION to the entry shape: `DATA_LAYER_STREAM_CODEC` is `ByteBufCodecs.byteArray(2048)` (`:18`), whose encoder is `FriendlyByteBuf.writeByteArray` (`ByteBufCodecs.java:251-265`) — so each list entry is itself a VarInt length (always 2048) followed by the 2048 bytes, not 2048 bare bytes; the outer VarInt count prefixes the list as the claim says. A future wire-integration blueprint that emits bare 2048-byte entries would desynchronise the stream | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The standalone `Update Light` packet carries the same six light fields as `Level Chunk with Light`, preceded by `chunk_x: VarInt` and `chunk_z: VarInt` (moderate confidence — this packet's own numeric id is not pinned in this project's corpus). | `ClientboundLightUpdatePacket.write:37-41` — `writeVarInt(x)`, `writeVarInt(z)`, then the identical `ClientboundLightUpdatePacketData.write` body used by `Level Chunk with Light`. The id can now be pinned: `datagen/generated/reports/packets.json`, `play.clientbound["minecraft:light_update"].protocol_id = 48` (`0x30`). Note the coordinates differ between the two packets — `Level Chunk with Light` writes `x`/`z` as plain big-endian ints (`ClientboundLevelChunkWithLightPacket.write:42-44`), `Update Light` as VarInts | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A `LightColumn` section that is untracked (`None`) contributes to neither its mask nor its empty mask, and no array entry is sent for it. | `ClientboundLightUpdatePacketData.prepareSectionData:68-86` — `DataLayer data = lightEngine.getLayerListener(layer).getDataLayerData(..); if (data != null) { ... }`; a null layer (the section is not tracked by that channel's storage) sets neither `mask` nor `emptyMask` and appends nothing to `updates` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's tracked-section empty test is purely structural (no backing array ever allocated, implicit fill value zero) — it never scans the 4096 nibbles, so a tracked section whose backing array is allocated is never reported empty, however many of its nibbles happen to be zero; this blueprint's own `Option`-based `LightSection` has no separate "allocated but still implicit" state, so every tracked (`Some`) section always contributes to the corresponding non-empty mask with its full bytes sent, regardless of content. | `ClientboundLightUpdatePacketData.prepareSectionData:78-84` sets the empty mask when `data.isEmpty()`. CORRECTION: `DataLayer.isEmpty:141-143` is `this.data == null && this.defaultValue == 0` — a purely structural test ("no backing array has been allocated and the implicit fill value is zero"), never a scan of the 4096 nibbles. A tracked section whose backing array *is* allocated but happens to hold all zeros is therefore **not** empty: it sets the non-empty mask bit and its full 2048 zero bytes go on the wire. Conversely a `fill(15)` layer has `data == null` yet `defaultValue == 15`, so it is also not empty (`DataLayer.fill:71-74`). The client-side effect is the same either way, but the emitted bytes are not | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A tracked section with at least one nonzero nibble contributes to the corresponding non-empty mask and has its full `[u8; 2048]` array sent, in ascending section-index order matching the mask's own bit order. | `ClientboundLightUpdatePacketData.prepareSectionData:81-84` — `mask.set(sectionIndex); updates.add(data.copy().getData());`; the driving loop `:39-47` runs `sectionIndex` from 0 to `getLightSectionCount()` ascending, so the array list order matches the mask bit order exactly, and the section index maps to `getMinLightSection() + sectionIndex` (`:77`). A section holding a nonzero nibble necessarily has an allocated array or a nonzero default, so it is never `isEmpty()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's own "border-only broadcast" optimization sends full section light state on a chunk's initial send. | `PlayerChunkSender.java:77` — the initial send is `new ClientboundLevelChunkWithLightPacket(chunk, level.getLightEngine(), null, null)`, and null section filters make `ClientboundLightUpdatePacketData`'s constructor loop (`:39-47`) prepare *every* light section of the column, so the full light state ships. That send goes to the loading player regardless of border status; the "border-only" part of the name belongs to the update path, not to this one | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| On a later in-place light change, vanilla's own "border-only broadcast" optimization restricts who receives the packet — only players for whom the chunk sits on the edge of their own tracked/view-distance area — not which sections are in it: every section whose light actually changed that tick is always sent, with no notion of border-adjacency among the sections themselves; every other viewer relies on their own bit-identical client-side local light engine to fill in the rest. | `ChunkHolder.broadcastChanges:174-188`. CORRECTION: "border" is a *recipient* filter, not a section filter. The sections sent are every section whose light actually changed this tick — `skyChangedLightSectionFilter`/`blockChangedLightSectionFilter`, set per changed light section by `ChunkHolder.sectionLightChanged:142-160` and passed straight into `ClientboundLightUpdatePacket` (`:180-183`) — with no notion of border adjacency among them. What is restricted is who receives the packet: `this.playerProvider.getPlayers(this.pos, true)` (`:178`), whose `borderOnly` argument selects `ChunkMap.isChunkOnTrackedBorder(player, x, z)` (`ChunkMap.getPlayers:1135-1146`), i.e. only players for whom this chunk sits on the edge of their tracked/view-distance area; every other viewer relies on their own client light engine. The ordinary block-change broadcast right below uses `getPlayers(pos, false)` (`:191`) for contrast | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
