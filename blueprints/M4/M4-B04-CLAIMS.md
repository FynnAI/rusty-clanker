# M4-B04 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Villagers are never a `NaturalSpawner` biome-list entry in real vanilla. | `data/minecraft/worldgen/biome/*.json` (all 66 biome files): no `spawners` list of any category contains `minecraft:villager`; `EntityTypes.java:1004-1006` declares `VILLAGER` with `MobCategory.MISC`, and `NaturalSpawner.java:62` (`SPAWNING_CATEGORIES`) filters `MISC` out entirely | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In real vanilla, villagers are never naturally spawned; they arrive via structure generation (the village jigsaw villagers pieces, plus the igloo basement), breeding, and zombie-villager curing, never through the per-tick natural spawn cycle. | `ZombieVillager.java:231-233` (`finishConversion` calls `convertTo(EntityTypes.VILLAGER, ...)`) - curing a zombie villager is a third in-game villager source the claim's "only" omits. The claim's second half is exact: `EntityTypes.java:1004-1006` gives `VILLAGER` `MobCategory.MISC`, excluded from `NaturalSpawner.java:62`'s `SPAWNING_CATEGORIES`, so villagers never enter the per-tick cycle | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `finalizeSpawn`'s individuality-bonus RNG cost is 3 calls: a follow-range triangle sample (2 calls) plus a left-handed roll (1 call). | `Mob.java:1083-1096` (`finalizeSpawn`): `random.triangle(0.0, 0.11485000000000001)` for the `FOLLOW_RANGE` `RANDOM_SPAWN_BONUS_ID` modifier, then `this.setLeftHanded(random.nextFloat() < 0.05F)`; `RandomSource.java:59-61` defines `triangle(double, double)` as `mean + spread * (nextDouble() - nextDouble())` - 2 + 1 = 3 calls | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's own spawning RNG (`Level.random`) is not derived from the world seed at all - it is reseeded from wall-clock time on every server start via `RandomSupport.generateUniqueSeed()`, an `AtomicLong` uniquifier XORed with `System.nanoTime()`. | `Level.java:122` (`protected final RandomSource random = RandomSource.create()`); `RandomSource.java:14-16` (`create()` delegates to `create(RandomSupport.generateUniqueSeed())`, i.e. `new LegacyRandomSource(seed)`); `RandomSupport.java:14` (`private static final AtomicLong SEED_UNIQUIFIER = new AtomicLong(8682522807148012L)`) and `RandomSupport.java:43-45` (`SEED_UNIQUIFIER.updateAndGet(c -> c * 1181783497276652981L) ^ System.nanoTime()`) - no world-seed input anywhere on this path | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Because vanilla's spawning RNG is reseeded from wall-clock time on every server start rather than from the world seed, it is never bit-reproducible across two runs of the same world, even in vanilla itself. | Same locations as the preceding row (`Level.java:122`, `RandomSource.java:14-16`, `RandomSupport.java:43-45`): the seed is an `AtomicLong` state XOR `System.nanoTime()`, so two runs of the same world necessarily start `Level.random` from different seeds | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `Monster` category (vanilla `MobCategory` declaration index 0): max 70 instances per chunk, not friendly, not persistent, despawn distance 128 blocks. | `MobCategory.java:7` - `MONSTER("monster", "MO", 70, false, false, 128)` read against the constructor parameter order `name, debugAbbreviation, max, isFriendly, isPersistent, despawnDistance` (`MobCategory.java:25-34`); first-declared variant, ordinal 0 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `Creature` category (declaration index 1): max 10 instances per chunk, friendly, persistent, despawn distance 128 blocks. | `MobCategory.java:8` - `CREATURE("creature", "C", 10, true, true, 128)`; second-declared, ordinal 1 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `Ambient` category (declaration index 2): max 15 instances per chunk, friendly, not persistent, despawn distance 128 blocks. | `MobCategory.java:9` - `AMBIENT("ambient", "AM", 15, true, false, 128)`; third-declared, ordinal 2 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `Axolotls` category (declaration index 3): max 5 instances per chunk, friendly, not persistent, despawn distance 128 blocks. | `MobCategory.java:10` - `AXOLOTLS("axolotls", "AX", 5, true, false, 128)`; fourth-declared, ordinal 3 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `UndergroundWaterCreature` category (declaration index 4): max 5 instances per chunk, friendly, not persistent, despawn distance 128 blocks. | `MobCategory.java:11` - `UNDERGROUND_WATER_CREATURE("underground_water_creature", "UWC", 5, true, false, 128)`; fifth-declared, ordinal 4 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `WaterCreature` category (declaration index 5): max 5 instances per chunk, friendly, not persistent, despawn distance 128 blocks. | `MobCategory.java:12` - `WATER_CREATURE("water_creature", "WC", 5, true, false, 128)`; sixth-declared, ordinal 5 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `WaterAmbient` category (declaration index 6): max 20 instances per chunk, friendly, not persistent, despawn distance 64 blocks. | `MobCategory.java:13` - `WATER_AMBIENT("water_ambient", "WA", 20, true, false, 64)`. Against the constructor order at `MobCategory.java:25-34` the fifth argument is `isPersistent`, and it is **`false`**. Correction: `WaterAmbient` is max 20, friendly, **NOT persistent**, despawn distance 64. `CREATURE` (`MobCategory.java:8`) is the only `isPersistent == true` variant among the seven (`MISC`, `MobCategory.java:14`, is the only other one overall). Consequence for the blueprint: the Context category table's bolded **true** in the `WaterAmbient` Persistent column is wrong, and `MobCategory::is_persistent` must return `false` for `WaterAmbient` - otherwise `WaterAmbient` would wrongly be gated behind the `current_tick % 400 == 0` persistent-tick rule | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `no_despawn_distance` is a category-independent constant of 32 blocks - a hardcoded getter, not read from the per-instance field of the same name, which is dead and unused. | `MobCategory.java:22` declares `private final int noDespawnDistance = 32;` while `MobCategory.java:65-67` returns the literal `32` from `getNoDespawnDistance()` without reading that field; no other read of the field exists in the class and it has no other accessor | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `MobCategory` enum declares its seven variants in exactly this order: Monster, Creature, Ambient, Axolotls, UndergroundWaterCreature, WaterCreature, WaterAmbient. | `MobCategory.java:7-13` - declaration order `MONSTER, CREATURE, AMBIENT, AXOLOTLS, UNDERGROUND_WATER_CREATURE, WATER_CREATURE, WATER_AMBIENT` (followed by `MISC` at line 14, the eighth) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| `MISC` (uncapped, category value -1) is never naturally spawned and is excluded from vanilla's own `spawningCategories`. | `MobCategory.java:14` - `MISC("misc", "MI", -1, true, true, 128)`, max `-1`; `NaturalSpawner.java:62` - `SPAWNING_CATEGORIES = Stream.of(MobCategory.values()).filter(c -> c != MobCategory.MISC)`; also skipped at `NaturalSpawner.java:79` (census) and rejected at `NaturalSpawner.java:254-255` (`isValidSpawnPostitionForType`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's global mob-cap formula is `max_mob_count = category.max_instances_per_chunk() * spawnable_chunk_count / 289` using integer (floor) division, and a category may spawn globally only while `global_live_count[category] < max_mob_count`. | `NaturalSpawner.java:533-536` (`SpawnState.canSpawnForCategoryGlobal`): `int maxMobCount = mobCategory.getMaxInstancesPerChunk() * this.spawnableChunkCount / NaturalSpawner.MAGIC_NUMBER; return this.mobCategoryCounts.getInt(mobCategory) < maxMobCount;` - Java `int` division (truncation toward zero, and both operands are non-negative here, so a floor) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The global-cap magic number 289 equals 17 squared. | `NaturalSpawner.java:61` - `private static final int MAGIC_NUMBER = (int)Math.pow(17.0, 2.0);`, i.e. 289 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla checks the global mob cap once per category per tick, before any chunk is visited - a category whose global cap already denies is dropped from that tick's entire attempt list, the cap is never re-tightened mid-tick, and it is re-checked only at the top of the next tick. | `ServerChunkCache.java:378-382` calls `NaturalSpawner.getFilteredSpawningCategories(...)` exactly once per `tickChunks`, before `collectSpawningChunks` and the per-chunk loop at `ServerChunkCache.java:394-396`; `NaturalSpawner.java:104-116` evaluates `canSpawnForCategoryGlobal` once per category while building that list, and the resulting `List<MobCategory>` is passed unchanged to every chunk (`NaturalSpawner.java:118-131`) - no per-chunk global re-check exists | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's local mob cap is checked per chunk per category: for every player within 128.0 blocks of the target chunk's center, the chunk's attempt for that category proceeds if at least one such player's own local count for that category is below the category's max-instances-per-chunk constant (the same, undivided constant used for the global formula, never divided by 289). | `NaturalSpawner.java:125` (`state.canSpawnForCategoryLocal(mobCategory, chunk.getPos())`) leads to `NaturalSpawner.java:538-540` and `LocalMobCapCalculator.java:33-42`: iterate `getPlayersNear(pos)`, return true on the first player whose `MobCounts.canSpawn(category)` holds; `LocalMobCapCalculator.java:54-56` is `counts.getOrDefault(category, 0) < category.getMaxInstancesPerChunk()`, undivided. "Near" is `ChunkMap.getPlayersCloseForSpawning` (`ChunkMap.java:996-1011`) via `playerIsCloseEnoughForSpawning` (`ChunkMap.java:1013-1020`): `euclideanDistanceSquared(chunkPos, player.position()) < 16384.0`, i.e. 128 blocks from the chunk-centre column (`ChunkMap.java:1031-1037`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A mob with `persistence_required == true` is excluded from both the global and local mob-cap counters entirely in vanilla. | `NaturalSpawner.java:76-95` (`createState`): the entity loop skips any `Mob` for which `mob.isPersistenceRequired()` or `mob.requiresCustomPersistence()` holds, so such a mob reaches neither `mobCounts.addTo` (global) nor `localMobCapCalculator.addMob` (local) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Real vanilla `ON_GROUND` spawn-placement legality checks the support block's own per-block `isValidSpawn` predicate plus signal-source, collision-full-cube, fluid, and the block tag `PREVENT_MOB_SPAWNING_INSIDE` and per-species hazard tests at the target position and the position above; the per-species tag `ANIMALS_SPAWNABLE_ON` is checked separately, in the species spawn-rule predicate, not in `ON_GROUND` itself. | `SpawnPlacementTypes.java:24-42` (`ON_GROUND.isSpawnPositionOk`) checks the world border, `belowState.isValidSpawn(level, below, type)`, and `NaturalSpawner.isValidEmptySpawnBlock` at the position and the position above; `NaturalSpawner.java:348-360` is that helper - `isCollisionShapeFullBlock`, `isSignalSource`, non-empty `FluidState`, `BlockTags.PREVENT_MOB_SPAWNING_INSIDE`, `type.isBlockDangerous(blockState)`. Correction: `ANIMALS_SPAWNABLE_ON` is **not** part of `ON_GROUND` at all - it is checked in `Animal.checkAnimalSpawnRules` (`Animal.java:111-116`), a separate `SpawnPlacements.checkSpawnRules` predicate invoked one call site later at `NaturalSpawner.java:263`; and `PREVENT_MOB_SPAWNING_INSIDE` is a plain block tag, not per-species. Accurate restatement: ON_GROUND checks the support block's per-block `isValidSpawn` predicate plus signal-source, collision-full-cube, fluid, `PREVENT_MOB_SPAWNING_INSIDE` and per-species `isBlockDangerous` tests at the two target blocks; the per-species tag `ANIMALS_SPAWNABLE_ON` belongs to the species spawn-rule predicate | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In the overworld, vanilla's monster darkness gate uses `monster_spawn_block_light_limit = 0` and `monster_spawn_light_test = UniformInt(0, 7)`. | `data/minecraft/dimension_type/overworld.json`: `"monster_spawn_block_light_limit": 0` and `"monster_spawn_light_level": {"type": "minecraft:uniform", "min_inclusive": 0, "max_inclusive": 7}`, read back through `DimensionType.java:118-123` and `DimensionType.MonsterSettings` (`DimensionType.java:130-134`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's overworld monster darkness-gate algorithm: if sky light exceeds a `next_int_bounded(32)` roll the position fails immediately (1 RNG call, always paid); otherwise if block light is greater than 0 the position fails with no further roll (0 additional calls, since the overworld block-light limit is 0); otherwise the position passes only if `max(block_light, sky_light - sky_darken) <= next_int_bounded(8)` (1 more call), where `sky_darken` is the level's current sky-light darkening (a fixed 10 during a thunderstorm) - total RNG cost is 1 call when the sky-light term fails, 2 calls otherwise. | `Monster.java:85-98` (`isDarkEnoughToSpawn`). Terms 1 and 2 and the RNG budget are exact: `getBrightness(SKY, pos) > random.nextInt(32)` fails (1 call, always paid); then `blockLightLimit < 15 and getBrightness(BLOCK, pos) > blockLightLimit` fails with no roll (overworld limit 0). Correction to the third term: vanilla compares `level.getMaxLocalRawBrightness(pos)`, not `max(sky_light, block_light)`. `LevelReader.java:163-171` resolves that to `getRawBrightness(pos, this.getSkyDarken())`, and `LevelLightEngine.java:146-150` computes `max(blockLight, skyLight - skyDarken)` - the sky term is reduced by the level's current day/night sky-darkening before the max is taken. The comparison itself, `brightness <= monsterSpawnLightTest().sample(random)` with `UniformInt(0,7).sample` = `Mth.randomBetweenInclusive` = `nextInt(8) + 0` (`UniformInt.java:31-33`, `Mth.java:693-695`), is exactly 1 further call, so the stated 1-or-2-call total is correct | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's animal spawn light rule (`Animal.checkAnimalSpawnRules`) requires `max(sky_light, block_light) >= 9`, at zero RNG cost. | `Animal.java:111-116` (`checkAnimalSpawnRules`) leading to `Animal.java:118-120` (`isBrightEnoughToSpawn` = `level.getRawBrightness(pos, 0) > 8`); `LevelLightEngine.java:146-150` with `skyDampen == 0` is exactly `max(blockLight, skyLight)`, and `> 8` is `>= 9` on integers. No `RandomSource` use on that path. (The same method additionally requires the block below to be in `BlockTags.ANIMALS_SPAWNABLE_ON`, which the claim scopes out by naming only the light rule.) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla includes persistent-category mobs in the natural spawn cycle only on ticks where `current_tick % 400 == 0`. | `ServerChunkCache.java:380` - `boolean spawnPersistent = this.level.getGameTime() % 400L == 0L;`, feeding `NaturalSpawner.java:110`'s `(spawnPersistent or !mobCategory.isPersistent())` filter term | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla shuffles the candidate chunk list with a backward Fisher-Yates shuffle, consuming exactly `len - 1` calls to `next_int_bounded`, always fully consumed. | `ServerChunkCache.java:393` (`Util.shuffle(spawningChunks, this.level.getRandom())`) leading to `Util.java:1110-1117`: `for (int i = size; i > 1; i--) { int swapTo = random.nextInt(i); ... }` - a backward Fisher-Yates over indices `size` down to 2, i.e. exactly `size - 1` `nextInt` draws, none of them conditional | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A vanilla pack-spawn attempt's anchor position is drawn with three RNG calls: an X offset via `next_int_bounded(16)`, a Z offset via `next_int_bounded(16)`, and a Y position via one more `next_int_bounded` call over the column's open range above the world's minimum Y - all three calls are always paid. | `NaturalSpawner.java:339-346` (`getRandomPosWithin`): `pos.getMinBlockX() + level.random.nextInt(16)`, `pos.getMinBlockZ() + level.random.nextInt(16)`, then `Mth.randomBetweenInclusive(level.random, level.getMinY(), topEmptyY)` where `topEmptyY = chunk.getHeight(Heightmap.Types.WORLD_SURFACE, x, z) + 1`; `Mth.java:693-695` expands that to `nextInt(maxInclusive - min + 1) + min`, one draw. All three are unconditional | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| If a vanilla pack-spawn attempt's anchor block is a full opaque (redstone-conducting) cube, the whole attempt for that category and chunk is abandoned with no further RNG calls beyond the anchor roll. | `NaturalSpawner.java:162-163` - `BlockState state = chunk.getBlockState(start); if (!state.isRedstoneConductor(chunk, start)) { ... }`; the whole 3-group body sits inside that guard, so a conducting anchor returns from `spawnCategoryForPosition` having drawn nothing further. `BlockBehaviour.java:1013` shows the `isRedstoneConductor` predicate defaults to `BlockStateBase::isCollisionShapeFullBlock` - a full *collision-shape* cube rather than a light-opacity test, which is the precise reading of the claim's parenthetical | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A single vanilla pack-spawn attempt runs up to 3 group tries sharing one cluster-size counter across all three tries, never reset per group - up to 4 total mobs may result from one chunk's attempt for one category, never 4 per individual group try (MAX_SPAWN_CLUSTER_SIZE = 4). | `NaturalSpawner.java:165-167` - `int clusterSize = 0;` is declared **before** `for (int groupCount = 0; groupCount < 3; groupCount++)` and is never reset inside it; `NaturalSpawner.java:208` increments it per successful spawn and `NaturalSpawner.java:212-214` returns out of the whole method (all remaining group tries included) once `clusterSize >= mob.getMaxSpawnClusterSize()`, which is 4 for both tier-2 kinds | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Each vanilla group try's own maximum sub-spawn count is rolled as `(next_float() * 4.0).ceil()` (1 RNG call), yielding a value in the set {0, 1, 2, 3, 4}. | `NaturalSpawner.java:173` - `int max = Mth.ceil(level.random.nextFloat() * 4.0F);` - one `nextFloat()` in `[0.0, 1.0)`, product in `[0.0, 4.0)`, ceiling therefore in `{0, 1, 2, 3, 4}` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Within a vanilla group try, each candidate position offsets from the anchor by `next_int_bounded(6) - next_int_bounded(6)` on both the X and Z axes (2 RNG calls per axis, 4 calls per iteration). | `NaturalSpawner.java:177-178` - `x += level.random.nextInt(6) - level.random.nextInt(6);` and the identical line for `z`, both inside the `for (int ll = 0; ll < max; ll++)` body - 2 draws per axis, 4 per iteration, unconditional | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A candidate vanilla spawn position is rejected if the nearest player is within 24 blocks, checked as squared distance against 576 (24 squared). | `NaturalSpawner.java:228-233` (`isRightDistanceToPlayerAndSpawnPoint`): `if (nearestPlayerDistanceSqr <= 576.0) { return false; }`, fed by `nearestPlayer.distanceToSqr(xx, yStart, zz)` at `NaturalSpawner.java:184`; `MIN_SPAWN_DISTANCE = 24` at `NaturalSpawner.java:57`, and 24 squared is 576 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Once a species is picked for a vanilla group try, that group's own spawn count is re-rolled as `entry.min_count + next_int_bounded(1 + entry.max_count - entry.min_count)` - always exactly 1 RNG call once a species has been picked. | `NaturalSpawner.java:186-196` - inside `if (currentSpawnData == null)`, immediately after `getRandomSpawnMobAt` returns non-empty: `max = currentSpawnData.minCount() + level.random.nextInt(1 + currentSpawnData.maxCount() - currentSpawnData.minCount());`. Reached exactly once per group try, on the iteration that first picks a species | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Each successful vanilla spawn draws a yaw of `next_float() * 360.0` (1 RNG call). | `NaturalSpawner.java:205` - `mob.snapTo(xx, yStart, zz, level.random.nextFloat() * 360.0F, 0.0F);`, drawn once per candidate that reached the mob-construction step | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's individuality-bonus sample (`finalizeSpawn`) is a triangle distribution with mean 0.0 and spread 0.11485000000000001, computed as `mean + spread * (next_double() - next_double())` (2 calls), followed by a left-handed roll `next_float() < 0.05` (1 call) - 3 RNG calls total, always paid on every successful spawn. | `Mob.java:1083-1096`: `random.triangle(0.0, 0.11485000000000001)` (`RandomSource.java:59-61` = `mean + spread * (nextDouble() - nextDouble())`, 2 draws) then `setLeftHanded(random.nextFloat() < 0.05F)` (1 draw). The triangle draw sits under `if (!followRange.hasModifier(RANDOM_SPAWN_BONUS_ID))`, always true for a mob freshly created by `NaturalSpawner.getMobForSpawn` (`NaturalSpawner.java:269-281`), so all 3 are paid on every natural spawn | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's MAX_SPAWN_CLUSTER_SIZE is 4 - once the shared cluster-size counter reaches 4, all remaining group tries for that category-and-chunk attempt end immediately. | `Mob.java:825-827` - `getMaxSpawnClusterSize() { return 4; }`, consumed at `NaturalSpawner.java:212-214` where `clusterSize >= mob.getMaxSpawnClusterSize()` returns from `spawnCategoryForPosition`, abandoning the remaining group tries. (There is no constant literally named `MAX_SPAWN_CLUSTER_SIZE` in the reference - the 4 is this virtual method's default, overridden only by `AbstractHorse`, `AbstractFish`, `AbstractSchoolingFish`, `HappyGhast`, `Wolf`, `Ghast` and `Pillager`, none of them a tier-2 kind.) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `isMaxGroupSizeReached` is false by default for every tier-2 kind (Zombie, Cow), so it never causes an early break in the group-try loop for either. | `Mob.java:829-831` - `isMaxGroupSizeReached(int groupSize) { return false; }`; a repository-wide search finds exactly one override, `TropicalFish.java:124`. Neither `Zombie` (`monster/zombie/Zombie.java`) nor `Cow`/`AbstractCow` (`animal/cow/`) nor any class on their superclass chains overrides it, so the `break` at `NaturalSpawner.java:216-218` is unreachable for both | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `WeightedList::getRandom` performs a cumulative-weight linear scan consuming exactly one `next_int_bounded(total_weight)` draw, or zero RNG calls if the list is empty or every weight is zero. | `WeightedList.java:76-83` (`getRandom`): `if (this.selector == null) return Optional.empty();` then `int selection = random.nextInt(this.totalWeight);` - exactly one draw, and `selector` is null precisely when `totalWeight == 0` (`WeightedList.java:27-33`), which covers both the empty list and an all-zero-weight list. The cumulative-weight linear scan is `WeightedList.Compact.get` (used when `totalWeight >= 64`); for `totalWeight < 64` `WeightedList.Flat` indexes a precomputed expansion of the same cumulative mapping - identical result for an identical draw, identical RNG cost | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's default max health is 20.0 for Zombie and 10.0 for Cow. | Zombie: `Zombie.java:131-137` (`createAttributes`) adds no `MAX_HEALTH`, so it inherits the attribute default via `Mob.createMobAttributes` (`Mob.java:166-168`) and `LivingEntity.java:332-334` (`add(Attributes.MAX_HEALTH)` with no value) from `Attributes.java:68-70`, `new RangedAttribute("attribute.name.max_health", 20.0, 1.0, 1024.0)`. Cow: `AbstractCow.java:56-58` - `Animal.createAnimalAttributes().add(Attributes.MAX_HEALTH, 10.0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A naturally-spawned vanilla zombie's chance to be able to pick up loot is `0.55 * difficulty.getSpecialMultiplier()`, where the multiplier is 0 below effective difficulty 2.0 and reaches 1.0 (the full 55 percent) only at or above effective difficulty 4.0 - on Normal in a fresh, briefly-inhabited world the multiplier is 0 and the chance is 0 percent. | `Zombie.java:459-461` - `if (spawnReason != EntitySpawnReason.CONVERSION) { this.setCanPickUpLoot(random.nextFloat() < 0.55F * difficultyModifier); }` where `difficultyModifier = difficulty.getSpecialMultiplier()` (`Zombie.java:458`). Correction: 0.55 is only the ceiling of the probability, not the probability. `DifficultyInstance.java:35-41` returns `0.0F` whenever `effectiveDifficulty < 2.0F` and `(effectiveDifficulty - 2.0F) / 2.0F` (capped at `1.0F`) otherwise, and `DifficultyInstance.java:43-61` computes `effectiveDifficulty = difficulty.getId() * scale` with `scale` starting at 0.75 - so on Normal (id 2) in a fresh, briefly-inhabited world the effective difficulty is about 1.5, the multiplier is 0.0, and a naturally-spawned zombie has a **0 percent** chance of `canPickUpLoot`. The full 55 percent is reached only at special multiplier 1.0, i.e. effective difficulty above 4.0 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla, a live non-persistent mob despawns instantly, with no random roll, once the nearest player's squared distance exceeds the mob's category's own despawn distance squared (128 squared or 64 squared depending on category), but only while the mob's own per-species `removeWhenFarAway` predicate also holds - every Animal (including Cow) overrides it to always return false, so no Animal ever despawns this way. | `Mob.java:689-711` (`checkDespawn`). Correction: the instant branch is `if (distSqr > despawnDistanceSqr and this.removeWhenFarAway(distSqr)) { this.discard(); }` (`Mob.java:698-700`) - the distance test is conjoined with the per-species `removeWhenFarAway` predicate, so it is not unconditional. `Mob.java:679-681` defaults that predicate to `true`, but `Animal.java:127-130` overrides it to `return false`, so no `Animal` - Cow included, one of this blueprint's two shipped species - ever despawns by distance, persistence flag or not. Accurate restatement: a live mob that is neither `persistenceRequired` nor `requiresCustomPersistence` despawns instantly, with no roll, once the nearest player's squared distance exceeds its category's despawn distance squared **and** its own `removeWhenFarAway(distSqr)` returns true. Consequence for the blueprint: `check_despawn` needs a per-kind `remove_when_far_away` term that is `false` for `Creature`-category animals, or Cows will despawn in this engine and never in vanilla | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A vanilla mob becomes eligible for random despawn only once its own inactivity timer exceeds 600 ticks, and then despawns with probability 1/800 per tick (`next_int_bounded(800) == 0`) while its squared distance to the nearest player also exceeds the 32-block no-despawn-distance threshold (32 squared) and its own per-species `removeWhenFarAway` predicate holds - every Animal (including Cow) overrides it to false, so it is never eligible for random despawn either. | `Mob.java:704-706` - `if (this.noActionTime > 600 and this.random.nextInt(800) == 0 and distSqr > noDespawnDistanceSqr and this.removeWhenFarAway(distSqr)) { this.discard(); }`. The three conditions the claim names are exact and in the reference's own evaluation order (timer, then the 1-in-800 roll, then the 32-block test built at `Mob.java:702-703` from `getNoDespawnDistance()` squared). Correction: there is a fourth conjunct, `this.removeWhenFarAway(distSqr)`, defaulted `true` at `Mob.java:679-681` and overridden to `false` at `Animal.java:127-130` - so a Cow is never eligible for the random despawn either, exactly as in the preceding row | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A vanilla mob's inactivity timer resets to 0 whenever its squared distance to the nearest player drops below the 32-block no-despawn threshold (32 squared). | `Mob.java:706-708` - `else if (distSqr < noDespawnDistanceSqr) { this.noActionTime = 0; }`, with `noDespawnDistanceSqr` built at `Mob.java:702-703` from `getNoDespawnDistance()` (a hardcoded 32, `MobCategory.java:65-67`). Strict `<`, and skipped when the preceding random-despawn branch fired | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's own `noActionTime` counter increments unconditionally on every AI step, regardless of goal-selector throttling. | `Mob.java:716-731` (`serverAiStep`): `this.noActionTime++;` is the first statement of the method, ahead of the sensing tick and ahead of the `idBasedTickCount % 2 != 0` gate at `Mob.java:722` that throttles the target and goal selectors | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's real determination of which chunks are spawn-eligible uses an exact Euclidean squared distance from the chunk's own center to the nearest player (128 blocks, 16384 = 128 squared), not a chunk-graph BFS distance - the chunk-graph tracker (capped at 8 chunks) is only a coarse prefilter for `spawnableChunkCount` accounting, not the deciding predicate. | `ChunkMap.java:943-955` (`collectSpawningChunks`) admits a chunk only if `anyPlayerCloseEnoughForSpawningInternal(holder.getPos())` (`ChunkMap.java:986-994`) holds, which is `playerIsCloseEnoughForSpawning` (`ChunkMap.java:1013-1020`): `euclideanDistanceSquared(chunkPos, player.position()) < 16384.0` - a Euclidean squared distance from the chunk-centre column (`ChunkMap.java:1031-1037`) against 128 blocks squared. Correction: the chunk-graph distance is only a coarse prefilter - `DistanceManager.java:187-190` (`getSpawnCandidateChunks`) returns the `FixedPlayerDistanceChunkTracker(8)` set (`DistanceManager.java:42`), a chunk-level propagation capped at 8 chunks, and `DistanceManager.java:164-171` (`hasPlayersNearby`) explicitly falls through to `TriState.DEFAULT`, i.e. to the exact Euclidean test, for every chunk between `INSCRIBED_SQUARE_SPAWN_DISTANCE_CHUNK` and 8. The definitive spawn-eligibility predicate is the Euclidean 128-block radius, which is what this blueprint's own `spawn_candidate_chunks` already implements. (The chunk-graph set is used for one other thing: `spawnableChunkCount` in the global-cap formula is `naturalSpawnChunkCounter.chunks.size()`, `DistanceManager.java:159-162`.) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `NaturalSpawner` algorithm applies Nether Fortress and structure-specific spawn-list overrides as steps 1 and 2, before falling back to the biome spawn list. | `NaturalSpawner.java:315-326` (`mobsAt`) and `ChunkGenerator.java:442-467` (`getMobsAt`). Correction: the two overrides are in the opposite order. Step 1 is the Nether Fortress override, checked first and hardcoded in `NaturalSpawner.isInNetherFortressBounds` (`NaturalSpawner.java:323-324` and `328-337`) outside the generic table; step 2, reached only if step 1 did not match, is the generic `StructureSpawnOverride` scan in `ChunkGenerator.getMobsAt` (`ChunkGenerator.java:445-464`); step 3 is the biome fallback `biome.value().getMobSettings().getMobs(mobCategory)` (`ChunkGenerator.java:466`). The claim's substance - both overrides precede the biome list - holds; only the 1-and-2 assignment is reversed | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's `NaturalSpawner` algorithm includes a per-biome spawn charge (energy-budget) gate as a distinct algorithm step. | `NaturalSpawner.java:489-502` (`SpawnState.canSpawn`) is a distinct step, passed as the `extraTest` predicate at `NaturalSpawner.java:126` and evaluated at `NaturalSpawner.java:199` alongside the placement checks; it reads the biome's `MobSpawnSettings.MobSpawnCost` and compares `spawnPotential.getPotentialEnergyChange(testPos, charge)` against `mobSpawnCost.energyBudget()`, with `afterSpawn` (`NaturalSpawner.java:504-523`) feeding each new spawn's charge back in | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's own `plains.json` biome definition declares an empty `spawn_costs` map. | `data/minecraft/worldgen/biome/plains.json` - top-level key `"spawn_costs": {}`, an empty object (its `spawners` map is populated for ambient/creature/monster/underground_water_creature and empty for axolotls/misc/water_ambient/water_creature) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's chunk-generation-time mob spawning uses a structurally different algorithm from the tick-based `NaturalSpawner` cycle. | `NaturalSpawner.java:362-433` (`spawnMobsForChunkGeneration`) versus `NaturalSpawner.java:151-226` (`spawnCategoryForPosition`): the generation-time path loops `while (random.nextFloat() < mobSettings.getCreatureProbability())`, is hardcoded to `MobCategory.CREATURE` only, uses a 4-attempt retry per member with a re-clamping walk that keeps the position inside the chunk (`NaturalSpawner.java:420-427`), places at `getTopNonCollidingPos` rather than a sampled Y, and has neither the 3-group structure, the cluster-size cap, the 24-block player-distance rule nor any mob-cap involvement | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla runs zombie sieges, patrols, phantoms, and the cat and wandering-trader spawners as separate `CustomSpawner`s, executed after `NaturalSpawner`; slime-chunk spawning runs inside `NaturalSpawner` itself, as the slime's own species spawn-rule predicate. | `MinecraftServer.java:427` registers exactly five: `new PhantomSpawner(), new PatrolSpawner(), new CatSpawner(), new VillageSiege(), new WanderingTraderSpawner(this.savedDataStorage)`, run by `ServerLevel.java:484-488` (`tickCustomSpawners`) from `ServerChunkCache.java:403-407`, after the `tickSpawningChunks` loop at `ServerChunkCache.java:394-396` - so the ordering half of the claim is exact. Correction: slime-chunk spawning is **not** a `CustomSpawner`. It runs inside the ordinary `NaturalSpawner` path as a species spawn-rule predicate, `Slime.checkSlimeSpawnRules` (`monster/cubemob/Slime.java:73`, slime-chunk test at `Slime.java:93` via `WorldgenRandom.seedSlimeChunk`), registered at `SpawnPlacements.java:156`. The accurate list of separate `CustomSpawner`s is: phantoms, patrols, cats, zombie sieges (`VillageSiege`) and the wandering trader | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla, mob despawning (`Mob.checkDespawn`) is called once per mob during that mob's own tick, as a call site entirely independent from `NaturalSpawner`'s per-region spawn cycle. | `ServerLevel.java:426-431` - inside `this.entityTickList.forEach(...)`, `entity.checkDespawn()` is called once per non-removed, non-frozen entity ahead of that entity's own tick work; `Mob.java:689-711` is the `Mob` override. This is `ServerLevel.tick`'s entity pass, structurally separate from `ServerChunkCache.tickChunks` (`ServerChunkCache.java:369-410`), which owns the whole `NaturalSpawner` cycle | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In real vanilla, during thunderstorms the monster darkness gate's sky-light term uses a fixed sky-light darkening of 10 in place of the level's current `skyDarken`, sampled at the single block position - not a 10-block radius. | `Monster.java:96` - `int brightness = level.getLevel().isThundering() ? level.getMaxLocalRawBrightness(pos, 10) : level.getMaxLocalRawBrightness(pos);`. Correction: the `10` is not a radius. `LevelReader.java:167-171` declares that second parameter as `skyDarkening`, and `LevelLightEngine.java:146-150` applies it as `max(blockLight, skyLight - skyDampen)` at the single position `pos` - i.e. thunder substitutes a fixed sky-light darkening of 10 for the level's current `getSkyDarken()` (`LevelReader.java:163-165`), still sampling exactly one block. No radius or neighbourhood sampling exists anywhere on this path | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla's pack-spawn algorithm, if the sampled anchor Y position falls below one block above the world's minimum Y, the spawn attempt for that chunk and category is abandoned. | `NaturalSpawner.java:140-143` (`spawnCategoryForChunk`): `BlockPos start = getRandomPosWithin(level, chunk); if (start.getY() >= level.getMinY() + 1) { spawnCategoryForPosition(...); }` - the attempt is simply not made when the sampled Y falls below `minY + 1` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's per-tick mob-cap live and local counts update progressively as chunks are processed in shuffled order, even though each category's global-cap check is evaluated only once at the start of the tick. | `NaturalSpawner.java:504-523` (`afterSpawn`, wired as the `spawnCallback` at `NaturalSpawner.java:126` and invoked at `NaturalSpawner.java:211`) does `this.mobCategoryCounts.addTo(category, 1)` and `this.localMobCapCalculator.addMob(ChunkPos.containing(pos), category)` on every spawn; `canSpawnForCategoryLocal` (`NaturalSpawner.java:125` and `538-540`) re-reads those live counts for each chunk in the shuffled order established at `ServerChunkCache.java:393`, while the global filter list built at `ServerChunkCache.java:381` is never recomputed within the tick | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla's despawn check, a mob with `persistence_required` true is never despawned, and its inactivity timer is reset to zero on every tick regardless of its distance to the nearest player. | `Mob.java:690-711` - the `else if (!this.isPersistenceRequired() and !this.requiresCustomPersistence())` guard means a persistence-required mob falls through to the trailing `else { this.noActionTime = 0; }` at `Mob.java:709-711`, so no despawn branch is ever reached for it and the timer is zeroed every tick with no distance test performed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla's despawn check, when no player is loaded near a mob, despawn logic does not run for that mob that tick and it is kept unconditionally. | `Mob.java:692-693` - `Entity player = this.level().getNearestPlayer(this, -1.0); if (player != null) { ... }`; with no player the whole distance-and-roll block is skipped and the method returns having done nothing (the timer is not even reset), so the mob is kept | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's despawn random roll draws from the mob's own per-entity `RandomSource` stream, not the natural spawn cycle's `level.random` - but that per-entity stream is itself seeded from the same wall-clock-derived source, so it is likewise never bit-reproducible across runs. | `Mob.java:704` uses `this.random.nextInt(800)`, and `Entity.java:251` declares `protected final RandomSource random = RandomSource.create();` - a per-entity `RandomSource`, not `Level.random` (`Level.java:122`). Correction: the despawn roll draws from the mob's **own** stream, a different stream from the natural spawn cycle's `level.random`. The claim's conclusion still holds, for a different reason: `RandomSource.create()` seeds from `RandomSupport.generateUniqueSeed()` (`RandomSource.java:14-16`, `RandomSupport.java:43-45`), the same `AtomicLong` XOR `System.nanoTime()` source, so the per-entity stream is equally non-reproducible across runs | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| In vanilla, Zombie is classified as `MobCategory` `Monster` and Cow as `MobCategory` `Creature`; Villager is classified as `MobCategory` `Misc`. | `EntityTypes.java:1089-1091` - `ZOMBIE ... EntityType.Builder.<Zombie>of(Zombie::new, MobCategory.MONSTER)`; `EntityTypes.java:306-308` - `COW ... EntityType.Builder.of(Cow::new, MobCategory.CREATURE)`. Correction: `EntityTypes.java:1004-1006` declares `VILLAGER ... EntityType.Builder.of(Villager::new, MobCategory.MISC)` - Villager is **`MISC`**, not `Creature`. Consequence for the blueprint: the Context kind-to-category table's `Villager` -> `Creature` row is wrong; vanilla's villager is uncapped (`max == -1`) and excluded from `SPAWNING_CATEGORIES` outright (`NaturalSpawner.java:62`), a stronger form of the blueprint's own "never reached by this cycle" note. Per the planning role's decision 11 (2026-09-03), `MobCategory` gains an eighth variant, `Misc`, so `mob_category_for_kind(Villager)` returns `Some(Misc)` — `Misc` is simply never a member of `MobCategory::ALL`, so natural spawning still never reaches it; `Item` remains the only tier-2 kind mapping to `None` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
