# M4-B10 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| ButtonBlock extends FaceAttachedHorizontalDirectionalBlock and declares exactly three block-state properties -- FACING over the four horizontals, POWERED as a boolean, and FACE over floor/wall/ceiling -- giving 24 states per button block. | `ButtonBlock` (ButtonBlock.java:42) extends `FaceAttachedHorizontalDirectionalBlock`; `createBlockStateDefinition` (:189-192) adds FACING, POWERED, FACE. FACING is `HorizontalDirectionalBlock.FACING` = `BlockStateProperties.HORIZONTAL_FACING` (4 horizontals); FACE is `FaceAttachedHorizontalDirectionalBlock.FACE` = `BlockStateProperties.ATTACH_FACE` (floor/wall/ceiling, FaceAttachedHorizontalDirectionalBlock.java:18). reports/blocks.json `minecraft:stone_button` lists exactly those three properties and 24 states | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every button block's registered default state is FACING=north, POWERED=false, FACE=wall. | `ButtonBlock` constructor (ButtonBlock.java:64) `registerDefaultState(... FACING=NORTH, POWERED=false, FACE=WALL)`; reports/blocks.json marks `face=wall, facing=north, powered=false` as the default state of every `*_button` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| minecraft:stone_button and minecraft:polished_blackstone_button are both constructed with BlockSetType.STONE and a ticks_to_stay_pressed of 20. | Blocks.java:1930 `STONE_BUTTON = ... new ButtonBlock(BlockSetType.STONE, 20, p)`; Blocks.java:4937-4939 `POLISHED_BLACKSTONE_BUTTON = ... new ButtonBlock(BlockSetType.STONE, 20, p)` — the polished-blackstone button really does take `BlockSetType.STONE`, not `BlockSetType.POLISHED_BLACKSTONE` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| All twelve wooden button blocks -- oak, spruce, birch, jungle, acacia, cherry, dark_oak, pale_oak, mangrove, bamboo, crimson and warped -- are constructed with a ticks_to_stay_pressed of 30. | Blocks.java:2724-2733 (oak, spruce, birch, jungle, acacia, cherry, dark_oak, pale_oak, mangrove, bamboo) and Blocks.java:4752-4753 (crimson, warped) — all twelve are `new ButtonBlock(<set type>, 30, p)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Blocks.buttonProperties gives every button block noCollision. | `Blocks.buttonProperties` (Blocks.java:5802-5804) = `Properties.of().noCollision().strength(0.5F).pushReaction(PushReaction.DESTROY)`; all 14 button registrations pass `buttonProperties()` verbatim | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Blocks.buttonProperties gives every button block strength 0.5 for both hardness and blast resistance. | Blocks.java:5803 `.strength(0.5F)`; `Properties.strength(float)` (BlockBehaviour.java:1139-1141) delegates to `strength(destroyTime, explosionResistance)` (:1131-1133) with the same value for both | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Blocks.buttonProperties gives every button block PushReaction.DESTROY. | Blocks.java:5803 `.pushReaction(PushReaction.DESTROY)` inside `buttonProperties`, inherited by all 14 button registrations | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| No button block sets requiresCorrectToolForDrops. | `Blocks.buttonProperties` (Blocks.java:5802-5804) contains no `requiresCorrectToolForDrops()` call, and every button block registers with that helper unmodified (Blocks.java:1930, :2724-2733, :4752-4753, :4937-4939) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.useWithoutItem returns a consuming result without pressing when POWERED is already true. | `ButtonBlock.useWithoutItem` (ButtonBlock.java:85-95): `if (state.getValue(POWERED)) return InteractionResult.CONSUME;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.useWithoutItem calls press and returns a success result when POWERED is false. | `ButtonBlock.useWithoutItem` (ButtonBlock.java:93-94): `this.press(state, level, pos, player); return InteractionResult.SUCCESS;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.useWithoutItem carries no mayBuild guard of any kind. | `ButtonBlock.useWithoutItem` (ButtonBlock.java:85-95) — the whole body is the POWERED test, the `press` call and the result; there is no `mayBuild` reference anywhere in ButtonBlock.java | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.press performs, in this exact order: setBlock with POWERED=true at update flags 3, then updateNeighbours, then scheduleTick with a delay of ticks_to_stay_pressed, then playSound, then the BLOCK_ACTIVATE game event. | `ButtonBlock.press` (ButtonBlock.java:108-114): `setBlock(pos, state.setValue(POWERED,true), 3)`, `updateNeighbours(...)`, `scheduleTick(pos, this, ticksToStayPressed)`, `playSound(player, level, pos, true)`, `gameEvent(player, GameEvent.BLOCK_ACTIVATE, pos)` — exactly that order | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.updateNeighbours computes front as getConnectedDirection(state).getOpposite() and calls updateNeighborsAt both at the button's own position and at pos.relative(front), the mount cell. | `ButtonBlock.updateNeighbours` (ButtonBlock.java:180-187): `Direction front = getConnectedDirection(state).getOpposite();` then `updateNeighborsAt(pos, this, orientation)` and `updateNeighborsAt(pos.relative(front), this, orientation)`. Not asserted by the claim but present: both calls carry an `ExperimentalRedstoneUtils.initialOrientation` argument (:182-184) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.playSound passes the acting player as the excluded listener when pressing and null when releasing, so the presser does not hear their own press while every listener hears the release. | `ButtonBlock.playSound` (ButtonBlock.java:116-118): `level.playSound(pressed ? player : null, pos, getSound(pressed), SoundSource.BLOCKS)`; the first parameter of `LevelAccessor.playSound` / `Level.playSound` is named `except` and names the listener excluded from the broadcast (LevelAccessor.java:74-78, Level.java:436-451) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.getSound returns the block set type's buttonClickOn when pressed and buttonClickOff when released. | `ButtonBlock.getSound` (ButtonBlock.java:120-122): `return pressed ? this.type.buttonClickOn() : this.type.buttonClickOff();` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Button and pressure-plate sounds are emitted through LevelAccessor's four-argument playSound overload, whose defaults are volume 1.0 and pitch 1.0; neither block passes an explicit volume or pitch. | ButtonBlock.java:117 and BasePressurePlateBlock.java:107 and :110 all call the four-argument `playSound(except, pos, sound, source)`; that default (LevelAccessor.java:74-76) forwards to the six-argument form with `1.0F, 1.0F`. Neither class passes an explicit volume or pitch anywhere | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.ownSignal returns 15 while POWERED and 0 otherwise, with no direction exclusion of any kind. | `ButtonBlock.ownSignal` (ButtonBlock.java:141-144): `return state.getValue(POWERED) ? 15 : 0;` — the 26.2 signature `ownSignal(state, level, pos)` carries no Direction parameter at all, so no exclusion is even expressible | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.getDirectSignal returns 15 only when POWERED and the queried direction equals getConnectedDirection(state). | `ButtonBlock.getDirectSignal` (ButtonBlock.java:131-134): `return state.getValue(POWERED) && getConnectedDirection(state) == direction ? 15 : 0;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.isSignalSource returns true. | `ButtonBlock.isSignalSource` (ButtonBlock.java:136-139) returns `true` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| FaceAttachedHorizontalDirectionalBlock.getConnectedDirection returns UP for FACE=floor, DOWN for FACE=ceiling, and the FACING value itself for FACE=wall. | `FaceAttachedHorizontalDirectionalBlock.getConnectedDirection` (FaceAttachedHorizontalDirectionalBlock.java:73-79): CEILING -> DOWN, FLOOR -> UP, default (WALL) -> the FACING value | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| FaceAttachedHorizontalDirectionalBlock.canSurvive calls canAttach with getConnectedDirection(state).getOpposite(), and canAttach requires the mount block's own face toward the attached block to be sturdy for SupportType.FULL, for all three attach faces alike. | `FaceAttachedHorizontalDirectionalBlock.canSurvive` (:27-30) = `canAttach(level, pos, getConnectedDirection(state).getOpposite())`; `canAttach` (:32-35) tests `level.getBlockState(pos.relative(direction)).isFaceSturdy(level, relative, direction.getOpposite())`, and the three-argument `isFaceSturdy` supplies `SupportType.FULL` (BlockBehaviour.java:885-887). No branch on AttachFace anywhere in that chain | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| FaceAttachedHorizontalDirectionalBlock.updateShape replaces the block with air only when the shape update arrives from the mount direction and canSurvive is false. | `FaceAttachedHorizontalDirectionalBlock.updateShape` (:57-71): returns `Blocks.AIR.defaultBlockState()` only when `getConnectedDirection(state).getOpposite() == directionToNeighbour && !state.canSurvive(level, pos)`; otherwise `super.updateShape(...)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.tick calls checkPressed only when POWERED is currently true. | `ButtonBlock.tick` (ButtonBlock.java:146-151): `if (state.getValue(POWERED)) this.checkPressed(state, level, pos);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.checkPressed looks for the first AbstractArrow inside the button's own outline shape moved to its position, and only when the block set type's canButtonBeActivatedByArrows is true; otherwise no arrow is ever searched for or found. | `ButtonBlock.checkPressed` (ButtonBlock.java:162-166): `AbstractArrow firstArrow = this.type.canButtonBeActivatedByArrows() ? level.getEntitiesOfClass(AbstractArrow.class, ...).stream().findFirst().orElse(null) : null;` — the search is skipped entirely for a non-arrow set type. Precision note: the search volume is `state.getShape(level, pos).bounds().move(pos)`, the AABB bounds of the outline shape rather than the shape itself (identical here, since a button outline is a single box) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.checkPressed writes the new POWERED value at update flags 3, calls updateNeighbours and plays the click sound with no excluded listener, but only when the computed pressed state differs from the stored one. | `ButtonBlock.checkPressed` (ButtonBlock.java:168-173): inside `if (shouldBePressed != wasPressed)` it does `setBlock(pos, state.setValue(POWERED, shouldBePressed), 3)`, `updateNeighbours(...)`, `playSound(null, level, pos, shouldBePressed)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.checkPressed re-schedules a tick ticks_to_stay_pressed out whenever the computed pressed state is true, and schedules nothing when it is false. | `ButtonBlock.checkPressed` (ButtonBlock.java:175-177): `if (shouldBePressed) level.scheduleTick(new BlockPos(pos), this, this.ticksToStayPressed);` — the only scheduling in the method | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.checkPressed fires GameEvent.BLOCK_ACTIVATE when the computed pressed state becomes true and GameEvent.BLOCK_DEACTIVATE when it becomes false, in addition to writing the block state, calling updateNeighbours and playing the click sound. | `ButtonBlock.checkPressed` (ButtonBlock.java:172): `level.gameEvent(firstArrow, shouldBePressed ? GameEvent.BLOCK_ACTIVATE : GameEvent.BLOCK_DEACTIVATE, pos)`, the fourth statement of the `shouldBePressed != wasPressed` block after the setBlock, updateNeighbours and playSound calls (:169-171) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.entityInside triggers checkPressed only on the server, only when the block set type's canButtonBeActivatedByArrows is true, and only when POWERED is currently false. | `ButtonBlock.entityInside` (ButtonBlock.java:153-160): `if (!level.isClientSide() && this.type.canButtonBeActivatedByArrows() && !state.getValue(POWERED)) this.checkPressed(state, level, pos);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlockSetType.canButtonBeActivatedByArrows is true for every wooden block set type and false for both the stone and the polished_blackstone block set types. | `BlockSetType` record component 3 is `canButtonBeActivatedByArrows` (BlockSetType.java:15). STONE passes false (:88) and POLISHED_BLACKSTONE false (:106); CHERRY (:128), CRIMSON (:148), WARPED (:166) and BAMBOO (:185) pass true, and OAK/SPRUCE/BIRCH/ACACIA/JUNGLE/DARK_OAK/PALE_OAK/MANGROVE (:119-122, :141-143, :180) go through the one-argument constructor whose third argument is true (:200-217) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stone and polished_blackstone block set types use SoundEvents.STONE_BUTTON_CLICK_ON and STONE_BUTTON_CLICK_OFF. | `BlockSetType.STONE` (BlockSetType.java:97-98) and `BlockSetType.POLISHED_BLACKSTONE` (:115-116) both pass `SoundEvents.STONE_BUTTON_CLICK_OFF` / `STONE_BUTTON_CLICK_ON` as the buttonClickOff/buttonClickOn components | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The oak, spruce, birch, jungle, acacia, dark_oak, pale_oak and mangrove block set types all use SoundEvents.WOODEN_BUTTON_CLICK_ON and WOODEN_BUTTON_CLICK_OFF; none of them has a per-wood-species button sound. | OAK, SPRUCE, BIRCH, ACACIA (BlockSetType.java:119-122), JUNGLE, DARK_OAK, PALE_OAK (:141-143) and MANGROVE (:180) are all `new BlockSetType(name)`; that one-argument constructor (:200-217) supplies `SoundEvents.WOODEN_BUTTON_CLICK_OFF` / `WOODEN_BUTTON_CLICK_ON` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The cherry block set type uses CHERRY_WOOD_BUTTON_CLICK_ON and OFF, bamboo uses BAMBOO_WOOD_BUTTON_CLICK_ON and OFF, and crimson and warped both use NETHER_WOOD_BUTTON_CLICK_ON and OFF. | BlockSetType.java: CHERRY (:137-138) `CHERRY_WOOD_BUTTON_CLICK_OFF`/`_ON`; BAMBOO (:195-196) `BAMBOO_WOOD_BUTTON_CLICK_OFF`/`_ON`; CRIMSON (:158-159) and WARPED (:176-177) `NETHER_WOOD_BUTTON_CLICK_OFF`/`_ON`. All six ids resolve in reports/registries.json `minecraft:sound_event` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.affectNeighborsAfterRemoval calls updateNeighbours when the removed state was POWERED and the removal was not caused by a piston. | `ButtonBlock.affectNeighborsAfterRemoval` (ButtonBlock.java:124-129): `if (!movedByPiston && state.getValue(POWERED)) this.updateNeighbours(state, level, pos);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A button's outline shape is the ONLY_FIRST join of the attach-face-rotated base box Block.boxZ(6.0, 4.0, 8.0, 16.0) with a centred cube that is Block.cube(14.0) when POWERED and Block.cube(12.0) when not. | `ButtonBlock.makeShapes` (ButtonBlock.java:69-78): `pressedShaper = Block.cube(14.0)`, `unpressedShaper = Block.cube(12.0)`, `attachFace = Shapes.rotateAttachFace(Block.boxZ(6.0, 4.0, 8.0, 16.0))`, joined as `Shapes.join(attachFace.get(FACE).get(FACING), POWERED ? pressedShaper : unpressedShaper, BooleanOp.ONLY_FIRST)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Block.boxZ(6.0, 4.0, 8.0, 16.0) evaluates to the box spanning x 5 to 11, y 6 to 10 and z 8 to 16 in sixteenths. | `Block.boxZ(sizeX, sizeY, minZ, maxZ)` (Block.java:190-193) -> halfY 2.0 -> `boxZ(6.0, 6.0, 10.0, 8.0, 16.0)` (:195-198) -> halfX 3.0 -> `Block.box(5.0, 6.0, 8.0, 11.0, 10.0, 16.0)` (:159-161), i.e. x 5 to 11, y 6 to 10, z 8 to 16 in sixteenths | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Shapes.rotateAttachFace's identity entry is the FACE=wall, FACING=north orientation. | `Shapes.rotateAttachFace(north)` (Shapes.java:453-466) maps AttachFace.WALL to `rotateHorizontal(north, OctahedralGroup.IDENTITY)`, and `rotateHorizontal` (:411-422) maps Direction.NORTH to `rotate(north, initial)` = the unrotated shape | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Subtracting the centred cube leaves a wall-north button outline of x 5 to 11, y 6 to 10, z 14 to 16 when unpressed and x 5 to 11, y 6 to 10, z 15 to 16 when pressed, so a pressed button protrudes one pixel where an unpressed one protrudes two. | `Block.cube(12.0)` (Block.java:167-183) = `box(2,2,2,14,14,14)` and `Block.cube(14.0)` = `box(1,1,1,15,15,15)`; subtracting each from `box(5,6,8,11,10,16)` under `BooleanOp.ONLY_FIRST` (ButtonBlock.java:74-76) leaves z 14 to 16 unpressed and z 15 to 16 pressed, with x 5 to 11 and y 6 to 10 unchanged in both | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock's unpressed outline shape is Block.column(14.0, 0.0, 1.0) -- x and z from 1 to 15, y from 0 to 1 in sixteenths -- and its pressed shape is Block.column(14.0, 0.0, 0.5), the same footprint at half that height. | `BasePressurePlateBlock.SHAPE = Block.column(14.0, 0.0, 1.0)` and `SHAPE_PRESSED = Block.column(14.0, 0.0, 0.5)` (BasePressurePlateBlock.java:26-27), selected by `getShape` (:39-42) on `getSignalForState(state) > 0`; `Block.column(sizeXZ, minY, maxY)` (Block.java:176-183) expands to `box(1, minY, 1, 15, maxY, 15)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock's entity-detection box TOUCH_AABB is Block.column(14.0, 0.0, 4.0), spanning x and z from 1 to 15 and y from 0 to 4 in sixteenths, and it is the only detection box the class defines. | `BasePressurePlateBlock.TOUCH_AABB = Block.column(14.0, 0.0, 4.0).toAabbs().getFirst()` (BasePressurePlateBlock.java:28); `Block.column` (Block.java:176-183) gives `box(1,0,1,15,4,15)`. It is the only AABB the class declares — the other two shape constants (:26-27) are VoxelShapes | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.getPressedTime returns 20, and WeightedPressurePlateBlock overrides it to return 10. | `BasePressurePlateBlock.getPressedTime` (BasePressurePlateBlock.java:44-46) returns 20; `WeightedPressurePlateBlock.getPressedTime` (WeightedPressurePlateBlock.java:61-64) overrides it to return 10 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.canSurvive is true when the block below is sturdy on its own top face for SupportType.RIGID or for SupportType.CENTER -- an OR of two support kinds, not RIGID alone. | `BasePressurePlateBlock.canSurvive` (BasePressurePlateBlock.java:69-73): `canSupportRigidBlock(level, below)` OR-ed with `canSupportCenter(level, below, Direction.UP)`; `Block.canSupportRigidBlock` (Block.java:334-336) tests `isFaceSturdy(..., Direction.UP, SupportType.RIGID)` and `Block.canSupportCenter` (:338-342) `isFaceSturdy(..., direction, SupportType.CENTER)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.updateShape replaces the plate with air only when the shape update arrives from DOWN and canSurvive is false. | `BasePressurePlateBlock.updateShape` (BasePressurePlateBlock.java:53-67): air only when `directionToNeighbour == Direction.DOWN && !state.canSurvive(level, pos)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.tick calls checkPressed only when the stored state's own signal is greater than zero. | `BasePressurePlateBlock.tick` (BasePressurePlateBlock.java:75-81): `int signal = getSignalForState(state); if (signal > 0) checkPressed(null, level, pos, state, signal);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.entityInside calls checkPressed only on the server and only when the stored state's own signal is exactly zero. | `BasePressurePlateBlock.entityInside` (BasePressurePlateBlock.java:83-93): `if (!level.isClientSide()) { int signal = getSignalForState(state); if (signal == 0) checkPressed(entity, level, pos, state, signal); }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.checkPressed writes the recomputed signal into the block state at update flag 2 -- a client update with no updateNeighborsAt fan-out -- and then calls updateNeighbours explicitly, and does both only when the recomputed signal differs from the stored one. | `BasePressurePlateBlock.checkPressed` (BasePressurePlateBlock.java:99-104): inside `if (oldSignal != signal)` it does `level.setBlock(pos, newState, 2)` then `this.updateNeighbours(level, pos)`. Flag 2 is UPDATE_CLIENTS only (Block.java:91-92), and `Level.setBlock` runs `updateNeighborsAt` only when bit 1 is set (Level.java:250-255). Not asserted by the claim but present: a third statement `level.setBlocksDirty(pos, state, newState)` at :103 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.updateNeighbours calls updateNeighborsAt at the plate's own position and at the position directly below it. | `BasePressurePlateBlock.updateNeighbours` (BasePressurePlateBlock.java:126-129): `updateNeighborsAt(pos, this)` and `updateNeighborsAt(pos.below(), this)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.checkPressed plays the block set type's pressurePlateClickOff when the plate goes from pressed to unpressed and pressurePlateClickOn when it goes from unpressed to pressed, both with no excluded listener, and plays nothing when a nonzero signal merely changes to a different nonzero value. | `BasePressurePlateBlock.checkPressed` (BasePressurePlateBlock.java:106-112): `if (!isPressed && wasPressed) playSound(null, pos, type.pressurePlateClickOff(), BLOCKS); else if (isPressed && !wasPressed) playSound(null, pos, type.pressurePlateClickOn(), BLOCKS);` — a nonzero-to-nonzero change leaves both `isPressed` and `wasPressed` true (:97-98), so neither branch runs | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.checkPressed schedules a tick getPressedTime ticks out whenever the recomputed signal is greater than zero. | `BasePressurePlateBlock.checkPressed` (BasePressurePlateBlock.java:114-116): `if (isPressed) level.scheduleTick(new BlockPos(pos), this, this.getPressedTime());`, with `isPressed = signal > 0` (:98) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.checkPressed fires GameEvent.BLOCK_ACTIVATE when the recomputed signal becomes pressed and GameEvent.BLOCK_DEACTIVATE when it becomes unpressed, in addition to writing the block state and playing the click sound. | `BasePressurePlateBlock.checkPressed` (BasePressurePlateBlock.java:108, :111): `gameEvent(sourceEntity, GameEvent.BLOCK_DEACTIVATE, pos)` in the pressed-to-unpressed branch and `GameEvent.BLOCK_ACTIVATE` in the unpressed-to-pressed branch, each immediately after that branch's playSound | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.ownSignal returns the state's own signal toward every direction. | `BasePressurePlateBlock.ownSignal` (BasePressurePlateBlock.java:141-144): `return this.getSignalForState(state);` — the 26.2 signature carries no Direction parameter | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.getDirectSignal returns the state's own signal only for the queried direction UP. | `BasePressurePlateBlock.getDirectSignal` (BasePressurePlateBlock.java:131-134): `return direction == Direction.UP ? this.getSignalForState(state) : 0;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.isSignalSource returns true. | `BasePressurePlateBlock.isSignalSource` (BasePressurePlateBlock.java:136-139) returns `true` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.getEntityCount counts entities of the requested class inside the given box filtered by EntitySelector.NO_SPECTATORS and by the entity not ignoring block triggers. | `BasePressurePlateBlock.getEntityCount` (BasePressurePlateBlock.java:146-148): `level.getEntitiesOfClass(entityClass, entityDetectionBox, EntitySelector.NO_SPECTATORS.and(e -> !e.isIgnoringBlockTriggers())).size()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| PressurePlateBlock carries the single boolean property POWERED, whose signal is 15 when true and 0 when false. | `PressurePlateBlock.POWERED` (PressurePlateBlock.java:20) with `createBlockStateDefinition` adding only POWERED (:51-54); `getSignalForState` (:32-35) returns `state.getValue(POWERED) ? 15 : 0`. reports/blocks.json shows 2 states for every plain plate | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| PressurePlateBlock.getSignalStrength counts entities of class Entity when the block set type's pressure-plate sensitivity is EVERYTHING and of class LivingEntity when it is MOBS, returning 15 when the count is greater than zero and 0 otherwise. | `PressurePlateBlock.getSignalStrength` (PressurePlateBlock.java:42-49): `switch (type.pressurePlateSensitivity()) { case EVERYTHING -> Entity.class; case MOBS -> LivingEntity.class; }` then `getEntityCount(level, TOUCH_AABB.move(pos), entityClass) > 0 ? 15 : 0` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlockSetType.PressurePlateSensitivity has exactly two values, EVERYTHING and MOBS. | `BlockSetType.PressurePlateSensitivity` (BlockSetType.java:231-239) declares exactly EVERYTHING and MOBS | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stone and polished_blackstone block set types have pressure-plate sensitivity MOBS, while every wooden block set type plus iron, gold and copper have sensitivity EVERYTHING. | `BlockSetType` record component 4 is `pressurePlateSensitivity` (BlockSetType.java:16). STONE (:89) and POLISHED_BLACKSTONE (:107) pass MOBS; IRON (:35), COPPER (:53) and GOLD (:71) pass EVERYTHING; every wooden set type goes through the one-argument constructor, which passes EVERYTHING (:206) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| WeightedPressurePlateBlock carries the single integer property POWER over the range 0 to 15 and uses that value directly as its signal. | `WeightedPressurePlateBlock.POWER = BlockStateProperties.POWER` (WeightedPressurePlateBlock.java:26) with `createBlockStateDefinition` adding only POWER (:66-69); `getSignalForState` (:51-54) returns `state.getValue(POWER)`. reports/blocks.json lists `power` 0 through 15 and 16 states for both weighted plates | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| WeightedPressurePlateBlock.getSignalStrength always counts entities of class Entity regardless of its own block set type's sensitivity. | `WeightedPressurePlateBlock.getSignalStrength` (WeightedPressurePlateBlock.java:40-49) passes `Entity.class` as a literal to `getEntityCount`; `pressurePlateSensitivity()` is never read in this class | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| WeightedPressurePlateBlock.getSignalStrength clamps the entity count to max_weight, divides that by max_weight as a float, multiplies by 15.0 and takes the ceiling, returning 0 when the count is zero. | `WeightedPressurePlateBlock.getSignalStrength` (WeightedPressurePlateBlock.java:41-48): `int count = Math.min(getEntityCount(...), this.maxWeight); if (count > 0) { float percent = (float)Math.min(this.maxWeight, count) / this.maxWeight; return Mth.ceil(percent * 15.0F); } else { return 0; }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| minecraft:light_weighted_pressure_plate is a WeightedPressurePlateBlock with max_weight 15 and BlockSetType.GOLD, and minecraft:heavy_weighted_pressure_plate has max_weight 150 and BlockSetType.IRON. | Blocks.java:2835-2840 `LIGHT_WEIGHTED_PRESSURE_PLATE = ... new WeightedPressurePlateBlock(15, BlockSetType.GOLD, p)`; Blocks.java:2840-2845 `HEAVY_WEIGHTED_PRESSURE_PLATE = ... new WeightedPressurePlateBlock(150, BlockSetType.IRON, p)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The gold and iron block set types use METAL_PRESSURE_PLATE_CLICK_ON and OFF; stone and polished_blackstone use STONE_PRESSURE_PLATE_CLICK_ON and OFF; the eight plain wooden types use WOODEN_PRESSURE_PLATE_CLICK_ON and OFF; cherry, bamboo, crimson and warped use their own CHERRY_WOOD, BAMBOO_WOOD and NETHER_WOOD variants. | BlockSetType.java: GOLD (:77-78) and IRON (:41-42) `METAL_PRESSURE_PLATE_CLICK_OFF`/`_ON`; STONE (:95-96) and POLISHED_BLACKSTONE (:113-114) `STONE_PRESSURE_PLATE_CLICK_OFF`/`_ON`; the one-argument constructor used by the eight plain wooden types (:214-215) `WOODEN_PRESSURE_PLATE_CLICK_OFF`/`_ON`; CHERRY (:135-136) `CHERRY_WOOD_`, BAMBOO (:193-194) `BAMBOO_WOOD_`, CRIMSON (:156-157) and WARPED (:174-175) `NETHER_WOOD_` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every pressure plate block is registered with noCollision. | All 16 plate registrations in Blocks.java carry `.noCollision()`: the 14 plain plates whose `register(...)` calls begin at :1768 stone, :1784 oak, :1796 spruce, :1808 birch, :1820 jungle, :1832 acacia, :1844 cherry, :1856 dark_oak, :1868 pale_oak, :1880 mangrove, :1892 bamboo, :4680 crimson, :4691 warped and :4926 polished_blackstone, plus the two weighted plates at :2835 and :2840 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every pressure plate block has strength 0.5 for both hardness and blast resistance. | The same 16 registrations each carry `.strength(0.5F)`; `Properties.strength(float)` (BlockBehaviour.java:1139-1141) sets destroyTime and explosionResistance to the same value via `strength(t, t)` (:1131-1133) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every pressure plate block is registered with PushReaction.DESTROY. | The same 16 registrations each carry `.pushReaction(PushReaction.DESTROY)` (Blocks.java, the plate rows listed above, including :2839 and :2844) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| None of the pressure plate blocks sets requiresCorrectToolForDrops. | None of the 16 plate `Properties` chains in Blocks.java contains `requiresCorrectToolForDrops()` — the modifiers used are mapColor, forceSolidOn, instrument, noCollision, strength, ignitedByLava and pushReaction only | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BasePressurePlateBlock.affectNeighborsAfterRemoval calls updateNeighbours when the removed state's own signal was greater than zero and the removal was not caused by a piston. | `BasePressurePlateBlock.affectNeighborsAfterRemoval` (BasePressurePlateBlock.java:119-124): `if (!movedByPiston && this.getSignalForState(state) > 0) this.updateNeighbours(level, pos);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Neither pressure plate class carries any orientation property: PressurePlateBlock's only property is POWERED and WeightedPressurePlateBlock's only property is POWER. | `PressurePlateBlock.createBlockStateDefinition` (PressurePlateBlock.java:51-54) adds POWERED only; `WeightedPressurePlateBlock.createBlockStateDefinition` (WeightedPressurePlateBlock.java:66-69) adds POWER only. reports/blocks.json confirms `powered` as the sole property of every plain plate and `power` as the sole property of both weighted plates | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlockBehaviour.getBlockSupportShape defaults to the block's own collision shape, and BlockBehaviour.getCollisionShape returns an empty shape whenever the block was registered with noCollision, so a noCollision block is sturdy on no face for any SupportType. | `BlockBehaviour.getBlockSupportShape` (BlockBehaviour.java:291-292) returns `this.getCollisionShape(state, level, pos, CollisionContext.empty())`; `getCollisionShape` (:327-328) returns `this.hasCollision ? state.getShape(level, pos) : Shapes.empty()`; `Properties.noCollision()` sets `hasCollision = false` (:1091). An empty support shape fails FULL, CENTER and RIGID alike (SupportType.java:12-33) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| All three SupportType variants -- FULL, CENTER and RIGID -- evaluate against getBlockSupportShape, never against the block's outline or visual shape. | `SupportType` (SupportType.java:11-33): FULL calls `Block.isFaceFull(state.getBlockSupportShape(level, pos), direction)` (:15); CENTER (:23) and RIGID (:31) each call `state.getBlockSupportShape(level, pos).getFaceShape(direction)`. No variant reads the outline or visual shape | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Entity.checkInsideBlocks deflates the entity's own bounding box by 1.0E-5 before enumerating the block positions it intersects. | `Entity.checkInsideBlocks` private overload (Entity.java:1319-1334): `AABB deflatedBoundingBoxAtTarget = this.makeBoundingBox(to).deflate(1.0E-5F);` (:1326), passed to `BlockGetter.forEachBlockIntersectedBetween(from, to, deflatedBoundingBoxAtTarget, ...)` (:1331-1334). Precision note: the deflated box is built at the segment's target position `to` via `makeBoundingBox(to)`, and the literal is the float `1.0E-5F` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlockBehaviour.getEntityInsideCollisionShape returns the full block cube by default, and Entity.checkInsideBlocks short-circuits its intersection test to true whenever that shape is the full cube, so a button or plate counts an entity as inside whenever the entity's deflated box intersects the block's whole cell. | `BlockBehaviour.getEntityInsideCollisionShape` (BlockBehaviour.java:331-333) returns `Shapes.block()`; Entity.java:1354-1355 computes `insideBlock = intersectShape == Shapes.block()` OR-ed with `this.collidedWithShapeMovingFrom(...)`, a reference-identity short-circuit that skips the per-shape test. Neither ButtonBlock nor BasePressurePlateBlock overrides `getEntityInsideCollisionShape`, and the cells enumerated are those of the deflated box (Entity.java:1326, :1331) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Entity.checkInsideBlocks visits each intersected block position at most once per movement, tracked in a visited-position set. | `Entity.checkInsideBlocks` (Entity.java:1286-1317) holds a `LongSet visitedBlocks` and dispatches only when `(insideBlock OR insideFluid) && visitedBlocks.add(blockIntersection.asLong())` (:1357), so `entityInside` fires at most once per position. Two precision notes: the set is shared across every `Entity.Movement` segment of one call and cleared only after the whole loop (:1315), so the guarantee is at most once per call rather than per segment; and positions where neither insideBlock nor insideFluid holds are never recorded | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Entity.checkInsideBlocks sweeps the movement segment through forEachBlockIntersectedBetween, but the 16-iteration cap is a hard budget for the whole movement rather than a per-segment allowance: the sweep visits the cells along the path only up to that budget, after which the caller re-checks only the destination box, so a movement needing more than 16 traversal steps has its tail cells skipped; a teleport resets the old position and produces no Entity.Movement, so it produces no sweep at all. | The sweep and the cap are right; the stated consequence is not. `Entity.checkInsideBlocks` does call `BlockGetter.forEachBlockIntersectedBetween(from, to, deflatedBox, visitor)` (Entity.java:1331-1334, BlockGetter.java:173-207) with `int maxMovementIterations = 16` (Entity.java:1293), but 16 is a hard budget, not a per-step allowance: the visitor returns false at `iteration >= maxMovementIterations` (Entity.java:1340-1342), and when the budget is exhausted the caller falls back to `checkInsideBlocks(movement.to(), movement.to(), effectCollector, visitedBlocks, 1)` (:1310-1312), which enumerates only the destination box's own cells. A movement longer than 16 traversal steps therefore skips every cell past the budget, and a teleport does not enter this path at all — it sets the position directly rather than producing an `Entity.Movement`. Correct form: the sweep visits the cells along the path only up to a 16-iteration budget, after which just the destination box is re-checked | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| AABB.intersects tests strict inequality on both bounds -- the queried box's min less than the other box's max, and its max greater than the other box's min -- never inclusive equality at either bound. | `AABB.intersects` (AABB.java:245-247): `this.minX < maxX && this.maxX > minX && this.minY < maxY && this.maxY > minY && this.minZ < maxZ && this.maxZ > minZ` — strict `<` and `>` on all six comparisons | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Level.setBlock with update flag bit 2 set sends a client block update but performs no updateNeighborsAt call, while the neighbour shape updates still run because they are gated on flag bit 16 being clear. | `Level.setBlock` (Level.java:222-262): the client update is gated on `(updateFlags & 2) != 0` (:244-248); `updateNeighborsAt` is gated on `(updateFlags & 1) != 0` (:250-255), which flag 2 alone leaves clear; the neighbour and indirect shape updates run under `(updateFlags & 16) == 0 && updateLimit > 0` (:257-262). Flag bit 16 is `UPDATE_KNOWN_SHAPE` (Block.java:95) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Level.setBlock with update flags 3 performs both the client update and the six-neighbour updateNeighborsAt fan-out. | `Level.setBlock` (Level.java:222-262) with flags 3 satisfies both gates: `(updateFlags & 2) != 0` -> `sendBlockUpdated` (:244-248) and `(updateFlags & 1) != 0` -> `updateNeighborsAt(pos, oldState.getBlock())` (:250-251). `UPDATE_ALL = 3` (Block.java:102) is UPDATE_NEIGHBORS or UPDATE_CLIENTS (:91-92) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The three-argument level.scheduleTick(pos, block, delay) used by both ButtonBlock and BasePressurePlateBlock schedules at TickPriority.NORMAL. | `ScheduledTickAccess.scheduleTick(pos, Block, tickDelay)` (ScheduledTickAccess.java:21-23) -> `LevelAccessor.createTick(pos, type, tickDelay)` (LevelAccessor.java:34-37) -> the four-argument `ScheduledTick` constructor (ScheduledTick.java:36-38), which supplies `TickPriority.NORMAL`. Both ButtonBlock (:111, :176) and BasePressurePlateBlock (:115) use the three-argument form | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ButtonBlock.onExplosionHit presses an unpressed button whenever the explosion can trigger blocks. | `ButtonBlock.onExplosionHit` (ButtonBlock.java:97-106): `if (explosion.canTriggerBlocks() && !state.getValue(POWERED)) this.press(state, level, pos, null);` before `super.onExplosionHit(...)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
