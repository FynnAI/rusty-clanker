# M4-B02 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Item entity gravity is 0.04 blocks per tick, subtracted from vertical velocity before collision resolution each tick. | `ItemEntity.getDefaultGravity` (ItemEntity.java:112-115) returns 0.04; `Entity.applyGravity` (Entity.java:1536-1541) subtracts it from delta-movement Y; called at ItemEntity.java:136, before `move(MoverType.SELF, ...)` at :151. Two gates the blueprint should note: gravity is skipped entirely while the item is in water or lava with that fluid's height > 0.1 (:131-137), and the whole move/gravity/drag block is skipped on 3 of every 4 ticks for an at-rest item (:148-150) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Item entity air drag is 0.98: after collision resolution, velocity is multiplied on all three axes by 0.98, with X/Z further multiplied by ground_friction when the item is on the ground. | `Entity.getAirDrag` (Entity.java:1543-1545) returns 0.98F and ItemEntity does not override it; ItemEntity.java:153-159 sets `groundFriction = airDrag`, multiplies it by the below-block's `getFriction()` when `onGround()`, then applies `multiply(groundFriction, airDrag, groundFriction)` — X/Z get drag times block friction, Y gets drag alone | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| When an item entity is on the ground and its vertical velocity is still negative after the drag multiply, vertical velocity is halved and inverted (velocity.y *= -0.5). | ItemEntity.java:160-165 — when `onGround()` and the post-drag `movement.y < 0.0`, `multiply(1.0, -0.5, 1.0)` is applied, i.e. `velocity.y *= -0.5` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The item-entity tick order is: subtract gravity from vertical velocity, run collision resolution, apply the drag multiply, then apply the conditional halve-and-invert. | `ItemEntity.tick` (ItemEntity.java:117-192): `applyGravity()` :136, `move(MoverType.SELF, ...)` :151, drag multiply :153-159, conditional halve-and-invert :160-165 — exactly that order. The at-rest fast path at :148-150 skips steps 2-4 on 3 of every 4 ticks | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Living tier-2 mobs use a default gravity of 0.08 blocks per tick (Attributes.GRAVITY default). | `Attributes.GRAVITY` (Attributes.java:55-57) is a RangedAttribute with default 0.08 (range -1.0..1.0); `LivingEntity.getDefaultGravity` (LivingEntity.java:2411-2414) returns that attribute's value | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The living-entity tick order computes horizontal movement input first, runs collision resolution using the previous tick's post-drag velocity, then computes this tick's gravity/drag afterward for storage as next tick's velocity. | `LivingEntity.travelInAir` (LivingEntity.java:2460-2487) plus `handleRelativeFrictionAndCalculateMovement` (:2666-2675): `moveRelative(speed, input)` applies the horizontal input, `move(MoverType.SELF, delta)` resolves collision against that velocity, then `movementY -= getEffectiveGravity()` (:2472) and the friction multiply (:2481-2486) produce the value stored for the next tick | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Living-entity air drag is computed via a friction-modification function with a base value of 0.98. | `LivingEntity.getAirDrag` (LivingEntity.java:2489-2492) = `computeModifiedFriction(0.98F, AIR_DRAG_MODIFIER)`, with `computeModifiedFriction` (:515-517) = `clamp(1 - (1 - friction) * modifier, 0, 1)`. Note for §B's table: inside `travelInAir` only the VERTICAL axis uses the 0.98 base (:2484); the horizontal axis uses `blockFriction * computeModifiedFriction(0.91F, ...)` (:2482-2483) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Item entities do not step up onto low ledges the way a player does (effective step height 0.0). | `Entity.maxUpStep` (Entity.java:3927-3929) returns 0.0F and ItemEntity does not override it; only `LivingEntity.maxUpStep` (LivingEntity.java:3975-3977) substitutes the `Attributes.STEP_HEIGHT` value | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| An entity's no-gravity flag gates the gravity step exactly as vanilla's Entity.getGravity() checks its own isNoGravity() flag. | `Entity.getGravity` (Entity.java:1532-1534) returns 0.0 when `isNoGravity()` (:1520-1522, backed by DATA_NO_GRAVITY), and `applyGravity` (:1536-1541) does nothing when the gravity value is 0.0 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Item entities have a 0.25 x 0.25 collision box (width x height), i.e. a half-width of 0.125 blocks. | `EntityTypes.ITEM` (EntityTypes.java:558-566) declares `.sized(0.25F, 0.25F)`; `EntityDimensions.makeBoundingBox` (EntityDimensions.java:22-26) halves the width, giving a 0.125 half-width | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Zombies have a 0.6-wide, 1.95-tall collision box. | `EntityTypes.ZOMBIE` (EntityTypes.java:1089-1098) declares `.sized(0.6F, 1.95F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Villagers have a 0.6-wide, 1.95-tall collision box. | `EntityTypes.VILLAGER` (EntityTypes.java:1004-1006) declares `.sized(0.6F, 1.95F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Cows have a 0.9-wide, 1.4-tall collision box. | `EntityTypes.COW` (EntityTypes.java:306-308) declares `.sized(0.9F, 1.4F)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| An entity's default eye height is its own height multiplied by 0.85. | `EntityDimensions.defaultEyeHeight` (EntityDimensions.java:14-16) returns `height * 0.85F`, used by the `sized`/`scalable`/`fixed` constructors (:7-9, :44-50). Correction for §D's table, though not asserted by this claim: all four tier-2 kinds DO override the default via `EntityType.Builder.eyeHeight` — item 0.2125F (EntityTypes.java:563), zombie 1.74F (:1093), villager 1.62F (:1005), cow 1.3F (:308) — so `height * 0.85` is wrong for every one of them | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Living tier-2 mobs use the same step height as players, 0.6 blocks. | `Attributes.STEP_HEIGHT` (Attributes.java:98-100) has default 0.6; `LivingEntity.maxUpStep` (LivingEntity.java:3975-3977) reads it, and neither `Player.createAttributes` (Player.java:206-220) nor Zombie/Villager/Cow overrides the value | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The AABB used to scan for touched fluid cells is inset by 0.001 blocks on every axis from the entity's own collision box. | `Entity.getFluidInteractionBox` (Entity.java:4091-4099) returns `this.getBoundingBox().deflate(0.001)`; `EntityFluidInteraction.update` (EntityFluidInteraction.java:32-41) iterates the block cells of exactly that box | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla tracks fluid interaction with one independent tracker per relevant fluid tag (e.g. separate trackers for water and lava). | `EntityFluidInteraction` holds `Map<TagKey<Fluid>, Tracker> trackerByFluid`, one Tracker constructed per tag passed in (EntityFluidInteraction.java:24-30), resolved per cell by `getTrackerFor` (:121-130); water and lava are supplied as separate `FluidTags.WATER` / `FluidTags.LAVA` trackers (Entity.java:1654-1675) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| When a touched fluid cell's own height is below 0.4, its contribution to the entity's accumulated flow vector is scaled down proportionally (own_height / 0.4); at or above 0.4 it contributes at full scale. | EntityFluidInteraction.java:72-79 — the scale factor is `tracker.height` used RAW, with no division by 0.4: `tracker.height = max(fluidTop - entityBoxMinY, tracker.height); if (tracker.height < 0.4) flow = flow.scale(tracker.height);`. Two corrections: (a) the quantity tested and multiplied is the running submersion depth (fluid top minus the entity bounding box's own minY, accumulated as a max over the cells visited so far), NOT the fluid cell's own height; (b) there is no `/ 0.4` normalisation — 0.4 is only the cutoff below which the raw depth is used as the multiplier | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The water push-scale constant is 0.014. | `Entity.WATER_FLOW_SCALE = 0.014` (Entity.java:207), passed at :1668 as `fluidInteraction.applyCurrentTo(FluidTags.WATER, this, 0.014)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The fast-lava push-scale constant (used when the dimension's fast-lava flag is set) is 0.007. | `Entity.LAVA_FAST_FLOW_SCALE = 0.007` (Entity.java:208), selected at :1672 when `EnvironmentAttributes.FAST_LAVA` is set for the dimension | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The slow-lava push-scale constant (used when the dimension's fast-lava flag is not set) is 0.00233. | `Entity.LAVA_SLOW_FLOW_SCALE = 0.0023333333333333335` (Entity.java:209, literal repeated at :1672) — not 0.00233. The blueprint's truncated three-digit literal is a different double (relative error ~1.4e-4) and violates the project's own bit-identical-parity rule; use 0.0023333333333333335 | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Only a player entity receives vanilla's averaged, non-normalized fluid-flow push; every other entity receives the plain, normalized push vector. | `EntityFluidInteraction.Tracker.applyCurrentTo` (EntityFluidInteraction.java:174-192): `if (!(entity instanceof Player)) impulse = accumulatedCurrent.normalize(); else impulse = accumulatedCurrent.scale(1.0 / currentCount);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A stationary entity's scaled fluid-push impulse is renormalized up to a floor magnitude of 0.0045 so it always receives some perceptible push in a current. | EntityFluidInteraction.java:185-188 — when `abs(velocity.x) < 0.003 && abs(velocity.z) < 0.003 && impulse.length() < 0.0045000000000000005`, the impulse is renormalised to exactly 0.0045000000000000005. Note for §E: the stationary test is 0.003 per horizontal axis on the entity's EXISTING velocity, not the blueprint's `1e-3` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The fluid push impulse is applied as an additive, post-tick impulse layered on top of the entity's ordinary gravity/drag step, not a replacement for it. | The additive part is right — `entity.addDeltaMovement(impulse)` (EntityFluidInteraction.java:190) — but the ordering is per-kind, not uniformly post-tick. For ItemEntity the push runs at ItemEntity.java:180, after the move/drag block (:151-165), matching the claim. For a living entity `updateFluidInteraction()` (Entity.java:1654-1678) runs inside `Entity.baseTick` (:537), i.e. BEFORE that tick's `aiStep`/`travel` computes gravity and drag, and again mid-move from `LivingEntity.checkFallDamage` (LivingEntity.java:362-366) while not in water. Applying it after the living tick step is a deliberate deviation, not vanilla order | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's real water-slowdown shape is per-axis (slowDown, 0.8, slowDown) — the Y axis always uses a flat 0.8 multiplier, not the same value as X/Z. | `LivingEntity.travelInWater` (LivingEntity.java:2530): `movement = movement.multiply(slowDown, 0.8F, slowDown)` — the Y factor is the literal 0.8F independent of slowDown | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's water-slowdown slowDown value applied to the X/Z axes is 0.9 while sprinting and 0.8 otherwise. | LivingEntity.java:2507 `float slowDown = this.isSprinting() ? 0.9F : this.getWaterSlowDown();` with `getWaterSlowDown` (:2402-2405) returning 0.8F. slowDown is further modified by `Attributes.WATER_MOVEMENT_EFFICIENCY` (:2508-2517) and forced to 0.96F under Dolphin's Grace (:2519-2521) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla clamps fall speed in water to a floor of -gravity/16.0 before applying the water-slowdown multiply. | Both the placement and the operation differ. `getFluidFallingAdjustedMovement` (LivingEntity.java:2678-2690) is applied AFTER the `(slowDown, 0.8, slowDown)` multiply (:2530 then :2531), and it SUBTRACTS rather than clamps: `y = movement.y - baseGravity / 16.0`, except that while falling with `abs(movement.y - 0.005) >= 0.003` and `abs(movement.y - baseGravity / 16.0) < 0.003` the result is exactly -0.003. The whole adjustment is skipped when `baseGravity == 0.0` or the entity is sprinting | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| An entity fully submerged in lava has its velocity multiplied by (0.5, 0.5, 0.5) with an additional gravity/4.0 subtracted from vertical velocity. | The multiply and the sink are right, the condition is not. `LivingEntity.travelInLava` (LivingEntity.java:2539-2556): `getDeltaMovement().scale(0.5)` is the ELSE branch of `isInShallowFluid(FluidTags.LAVA)` (:2535-2537) = `getFluidHeight(LAVA) <= getFluidJumpThreshold()`, and `Entity.getFluidJumpThreshold` (Entity.java:3692-3694) = 0.0 when eye height < 0.4, else 0.4 — so the branch is taken when the lava height above the entity's feet exceeds 0.4, not when the entity is fully submerged. The `-baseGravity / 4.0` add (:2551-2553) is applied in BOTH branches, not only this one | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| An entity only shallowly in lava (not fully submerged) has its velocity multiplied by (0.5, 0.8, 0.5). | The multiply is right, the condition and the omitted steps are not. LivingEntity.java:2541-2546 applies `multiply(0.5, 0.8F, 0.5)` when `isInShallowFluid(FluidTags.LAVA)` = `getFluidHeight(LAVA) <= getFluidJumpThreshold()` (0.4 for any entity with eye height >= 0.4, Entity.java:3692-3694) — a fluid-depth test, not 'not fully submerged'. The shallow branch additionally runs `getFluidFallingAdjustedMovement` (:2544-2545) and then the same `-baseGravity / 4.0` add (:2551-2553) that the deep branch gets | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's total air-supply constant is 300 ticks. | `Entity.TOTAL_AIR_SUPPLY = 300` (Entity.java:194); `Entity.getMaxAirSupply` (:2805-2807) returns 300 and the synched air field is initialised to it (:319) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Each tick a living entity's eyes are submerged in water, its air supply decreases by 1 down to a floor of -20 ticks; otherwise it increases by 1 up to the 300-tick cap. | Both rates and the floor are wrong. `LivingEntity.decreaseAirSupply` (LivingEntity.java:588-598) returns `currentSupply - 1` with NO lower clamp (an OXYGEN_BONUS/Respiration roll can skip the decrement entirely). `LivingEntity.increaseAirSupply` (:600-602) returns `min(currentSupply + 4, getMaxAirSupply())` — plus FOUR per tick, not one. Nothing clamps at -20; the value only returns to 0 through the drowning branch (:443-448). The decrement further requires `isEyeInFluid(WATER)`, the eye block not being a bubble column, and the entity neither breathing underwater nor water-breathing (:436-442) | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| When a living entity's air supply newly reaches the -20 floor, it resets to 0 and triggers a drowning-damage event. | `LivingEntity.shouldTakeDrowningDamage` (LivingEntity.java:506-508) = `getAirSupply() <= -20`; LivingEntity.java:443-448 then does `setAirSupply(0)`, broadcasts entity event 67 and applies `damageSources().drown()` at 2.0F — which matches the blueprint's suggested_magnitude 2.0. Because air is reset to 0 the same tick, the branch can only fire on the tick the value first reaches -20 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Item entities never take fall damage in vanilla, because ItemEntity overrides causeFallDamage to a no-op. | ItemEntity has NO `causeFallDamage` override in 26.2 — the only overrides in the tree are AbstractHorse, Llama, FallingBlockEntity, LivingEntity, Breeze, Creeper, Player and MinecartTNT. The base `Entity.causeFallDamage` (Entity.java:1588-1595) already deals no damage: it returns false after an `EntityTypeTags.FALL_DAMAGE_IMMUNE` check and passenger propagation. Items take no fall damage because only `LivingEntity.causeFallDamage` (:1787-1805) turns fall distance into damage, not because of an ItemEntity no-op | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A dropped item entity's spawn position offset on each axis is drawn uniformly within [0.25, 0.75] of the broken block's own unit cell. | True for X and Z, wrong for Y. `Block.popResource` (Block.java:412-419): `x = pos.getX() + 0.5 + Mth.nextDouble(random, -0.25, 0.25)` and the same for z (uniform in [0.25, 0.75] of the cell), but `y = pos.getY() + 0.5 + Mth.nextDouble(random, -0.25, 0.25) - halfHeight` where `halfHeight = EntityTypes.ITEM.getHeight() / 2.0 = 0.125` — the Y offset is uniform in [0.125, 0.625]. Each axis costs one `nextDouble` from `level.getRandom()` (Mth.nextDouble, Mth.java:157-159) | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A dropped item entity's initial velocity is Vec3(triangle(0.0, 0.11485), triangle(0.2, 0.1116), triangle(0.0, 0.11485)), where triangle(mean, spread) = mean + spread * (next_double() - next_double()). | No triangular distribution exists on this path. The drop constructor `ItemEntity(Level, x, y, z, ItemStack)` (ItemEntity.java:61-66) sets `setDeltaMovement(random.nextDouble() * 0.2 - 0.1, 0.2, random.nextDouble() * 0.2 - 0.1)` — X and Z uniform in [-0.1, 0.1] (one `nextDouble` each, from the entity's level random), Y a flat constant 0.2. The 0.11485 / 0.1116 spreads appear nowhere in the 26.2 source. The face-directed variant `Block.popResourceFromFace` (Block.java:421-435) instead uses `nextDouble(-0.1, 0.1)` per free axis and `nextDouble(0.0, 0.1)` for Y | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A newly spawned dropped item entity's pickup delay defaults to 10 ticks. | `Block.popResource` (Block.java:437-443) calls `entity.setDefaultPickUpDelay()` before `addFreshEntity`, and `ItemEntity.setDefaultPickUpDelay` (ItemEntity.java:400-402) sets `pickupDelay = 10`. The field's own default is 0 (:48-50), so the 10 comes from the drop path | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The loot-roll algorithm processes a loot table's pools in declaration order. | `LootTable.getRandomItemsRaw` (LootTable.java:89-100) iterates `for (LootPool pool : this.pools)` over the list decoded in declaration order by the codec (:43) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Each pool's roll count is computed as rolls.resolve(rng) + floor(bonus_rolls.resolve(rng) * luck). | `LootPool.addRandomItems` (LootPool.java:97-105): `int count = this.rolls.getInt(context) + Mth.floor(this.bonusRolls.getFloat(context) * context.getLuck());`, then `addRandomItem` is called `count` times | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Within a roll, a loot pool's entries are evaluated in declaration order. | `LootPool.addRandomItem` (LootPool.java:69-77) iterates `for (LootPoolEntryContainer entry : this.entries)` in decoded declaration order (codec at :32) while collecting valid entries | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Each loot entry's effective weight is computed as max(0, base_weight + floor(quality * luck)). | The floor placement differs. `LootPoolSingletonContainer`'s entry (LootPoolSingletonContainer.java:129-131) computes `Math.max(Mth.floor(weight + quality * luck), 0)` — the floor covers the whole sum, not just the `quality * luck` term. The two agree for integer base weights, but the blueprint should restate vanilla's form | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| When exactly one loot-table entry in a pool has positive weight, it is chosen with no RNG draw; otherwise exactly one next_int_bounded(total_weight) draw selects the entry by weighted walk. | `LootPool.addRandomItem` (LootPool.java:79-93): with `entryCount == 1` the single entry's `createItemStack` is called with no draw; otherwise exactly one `random.nextInt(totalWeight)` is taken and each entry's weight is subtracted until the index goes below zero | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A Uniform count or roll provider resolves as: if min >= max return min with no draw, otherwise return min + next_int_bounded(max - min + 1). | `UniformGenerator.getInt` (UniformGenerator.java:28-31) delegates to `Mth.nextInt` (Mth.java:149-151): `minInclusive >= maxInclusive ? minInclusive : random.nextInt(maxInclusive - minInclusive + 1) + minInclusive` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking a Stone block drops 1 Cobblestone. | data/minecraft/loot_table/blocks/stone.json — one pool, `rolls: 1.0`, a single `minecraft:alternatives` entry: silk-touch tool yields `minecraft:stone`, otherwise `minecraft:cobblestone`. Note for §J: the entry is NOT unconditional — the cobblestone child carries `minecraft:survives_explosion` and the stone child a `minecraft:match_tool` silk-touch predicate | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking a Dirt block drops 1 Dirt. | data/minecraft/loot_table/blocks/dirt.json — one pool, `rolls: 1.0`, single item entry `minecraft:dirt`; the pool carries a `minecraft:survives_explosion` condition | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking a Grass Block drops 1 Dirt. | data/minecraft/loot_table/blocks/grass_block.json — one pool, `rolls: 1.0`, a `minecraft:alternatives` entry: silk touch yields `minecraft:grass_block`, otherwise `minecraft:dirt` gated on `minecraft:survives_explosion` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking Redstone Wire drops 1 Redstone (dust). | data/minecraft/loot_table/blocks/redstone_wire.json — one pool, `rolls: 1.0`, single item entry `minecraft:redstone`, condition `minecraft:survives_explosion` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking a Redstone Torch or Redstone Wall Torch drops 1 Redstone Torch. | data/minecraft/loot_table/blocks/redstone_torch.json — one pool, `rolls: 1.0`, single item entry `minecraft:redstone_torch`. There is no redstone_wall_torch.json: `Blocks.REDSTONE_WALL_TORCH` is declared `wallVariant(REDSTONE_TORCH, true)` (Blocks.java:1925-1929), so the wall form shares the standing torch's loot table and random_sequence id | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking a Repeater drops 1 Repeater (the block itself). | data/minecraft/loot_table/blocks/repeater.json — one pool, `rolls: 1.0`, single item entry `minecraft:repeater`, condition `minecraft:survives_explosion` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking a Comparator drops 1 Comparator (the block itself). | data/minecraft/loot_table/blocks/comparator.json — one pool, `rolls: 1.0`, single item entry `minecraft:comparator`, condition `minecraft:survives_explosion` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking a Piston or Sticky Piston drops the block itself. | data/minecraft/loot_table/blocks/piston.json and sticky_piston.json — each one pool, `rolls: 1.0`, single item entry naming the block itself, condition `minecraft:survives_explosion` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking a Chest drops 1 Chest (the block itself). | data/minecraft/loot_table/blocks/chest.json — one pool, `rolls: 1.0`, single item entry `minecraft:chest` with a `minecraft:copy_components` function carrying `minecraft:custom_name` from the block entity, condition `minecraft:survives_explosion` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking a Hopper drops 1 Hopper (the block itself). | data/minecraft/loot_table/blocks/hopper.json — one pool, `rolls: 1.0`, single item entry `minecraft:hopper` with the same `copy_components` custom-name function, condition `minecraft:survives_explosion` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Breaking a Furnace, Blast Furnace, or Smoker drops the block itself. | data/minecraft/loot_table/blocks/furnace.json, blast_furnace.json, smoker.json — each one pool, `rolls: 1.0`, single item entry naming the block itself with the `copy_components` custom-name function, condition `minecraft:survives_explosion` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Since Minecraft 1.20, a block's loot-table random_sequence id follows the convention minecraft:blocks/<block_id>. | Every generated block table carries `"random_sequence": "minecraft:blocks/<block_id>"` — verified across data/minecraft/loot_table/blocks/*.json (stone, dirt, grass_block, redstone_wire, redstone_torch, repeater, comparator, piston, sticky_piston, chest, hopper, furnace, blast_furnace, smoker all match). The string hashed for the seed is the Identifier's `toString()` (RandomSequence.seedForKey, RandomSequence.java:38-40). The `since 1.20` provenance is version history the pinned 26.2 reference cannot settle; the convention itself holds in 26.2 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Xoroshiro128++'s golden-ratio-64 constant is -7046029254386353131 (hex 0x9E3779B97F4A7C15). | `RandomSupport.GOLDEN_RATIO_64 = -7046029254386353131L` (RandomSupport.java:11); the same literal is inlined at :28 and as the zero-state fallback in Xoroshiro128PlusPlus.java:21 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Xoroshiro128++'s silver-ratio-64 constant is 7640891576956012809 (hex 0x6A09E667F3BCC909). | `RandomSupport.SILVER_RATIO_64 = 7640891576956012809L` (RandomSupport.java:12); inlined at :27 and Xoroshiro128PlusPlus.java:22 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Stafford mix13 finalizer multiplies by the two constants -4658895280553007687 and -7723592293110705685 (with intervening XOR-shifts of 30 and 27 bits) before a final XOR-shift of 31 bits. | `RandomSupport.mixStafford13` (RandomSupport.java:19-24): `z = (z ^ z >>> 30) * -4658895280553007687L; z = (z ^ z >>> 27) * -7723592293110705685L; return z ^ z >>> 31;` — shift widths 30, 27, 31 and both multipliers match exactly | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Upgrading a legacy 64-bit seed to the unmixed 128-bit Xoroshiro seed pair computes lo = legacy_seed XOR SILVER_RATIO_64 and hi = lo + GOLDEN_RATIO_64 (wrapping add). | `RandomSupport.upgradeSeedTo128bitUnmixed` (RandomSupport.java:26-30): `lowBits = legacySeed ^ SILVER_RATIO_64; highBits = lowBits + GOLDEN_RATIO_64;` (Java long addition is wrapping) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Xoroshiro128++'s next_long step computes the output as rotate_left(s0 + s1, 17) + s0, then updates state via s1 ^= s0, new_lo = rotate_left(s0, 49) XOR s1 XOR (s1 << 21), new_hi = rotate_left(s1, 28). | `Xoroshiro128PlusPlus.nextLong` (Xoroshiro128PlusPlus.java:26-34): `result = Long.rotateLeft(s0 + s1, 17) + s0; s1 ^= s0; seedLo = Long.rotateLeft(s0, 49) ^ s1 ^ s1 << 21; seedHi = Long.rotateLeft(s1, 28);`. Missing from the blueprint's §K pseudocode: the constructor (:17-24) replaces an all-zero (lo, hi) pair with (GOLDEN_RATIO_64, SILVER_RATIO_64) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Xoroshiro's next_int() truncates next_long() to its low 32 bits. | `XoroshiroRandomSource.nextInt()` (XoroshiroRandomSource.java:48-51) returns `(int) this.randomNumberGenerator.nextLong()` — a narrowing cast keeping the low 32 bits | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Xoroshiro's next_float() is next_bits(24) scaled by 2^-24. | `XoroshiroRandomSource.nextFloat` (XoroshiroRandomSource.java:86-89) = `(float) nextBits(24) * FLOAT_UNIT`, FLOAT_UNIT declared `5.9604645E-8F` (:9) — the nearest float to that literal is exactly 2^-24 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Xoroshiro's next_double() is next_bits(53) scaled by 2^-53. | `XoroshiroRandomSource.nextDouble` (XoroshiroRandomSource.java:91-94) = `nextBits(53) * DOUBLE_UNIT`, DOUBLE_UNIT declared as `double DOUBLE_UNIT = 1.110223E-16F` (:10) — a float literal widened to double whose nearest float value is exactly 2^-53, which is why §K's rule of deriving it as an exact power of two rather than transcribing the decimal is correct | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's bounded-integer draw (next_int_bounded) uses a Lemire-style multiply-and-reject algorithm on the unsigned product of a random 32-bit value and the bound, not the legacy rejection-loop algorithm. | `XoroshiroRandomSource.nextInt(int bound)` (XoroshiroRandomSource.java:53-74): `randomBits = Integer.toUnsignedLong(nextInt()); product = randomBits * bound; fractional = product & 0xFFFFFFFFL;` with the reject threshold `Integer.remainderUnsigned(~bound + 1, bound)` and the returned value `(int)(product >> 32)` — the Lemire form, no legacy rejection loop | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A random_sequence's MD5-based seed is the first 8 bytes and last 8 bytes of the MD5 hash of the sequence id's UTF-8 bytes, each read as a big-endian i64. | `RandomSupport.seedFromHashOf` (RandomSupport.java:36-41) hashes the string's UTF-8 bytes with MD5 and reads bytes 0-7 and 8-15 via Guava `Longs.fromBytes`, which is big-endian, into (seedLo, seedHi) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Creating a random_sequence XORs the (optionally included) world seed with a salt, upgrades that to the 128-bit unmixed seed pair, XORs in the MD5-seed halves of the (optionally included) sequence id, and mixes both resulting words with Stafford mix13. | `RandomSequences.createSequence` (RandomSequences.java:54-59): `long seed = (includeWorldSeed ? worldSeed : 0L) ^ salt;` then `RandomSequence.createSequence` (RandomSequence.java:29-36): `upgradeSeedTo128bitUnmixed(seed)`, `.xor(seedForKey(key))` when the id is included, then `.mixed()` = Stafford mix13 on both words (`Seed128bit.mixed`, RandomSupport.java:59-61) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's default random_sequence creation uses salt = 0 and includes both the world seed and the sequence id. | `RandomSequences` field defaults (RandomSequences.java:30-32): `salt` 0, `includeWorldSeed` true, `includeSequenceId` true; the codec repeats true as the optional-field default for both flags (:20-22) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Xoroshiro128++ seeded with 0 produces the five consecutive next_long() values 3038984756725240190, -3694039286755638414, 4633751808701151732, 2160572957309072155, 1839370574944072389. | Reproduced by executing the decompiled algorithm (RandomSupport.upgradeSeedTo128bit + Xoroshiro128PlusPlus.nextLong, RandomSupport.java:19-34 and Xoroshiro128PlusPlus.java:17-34) on legacy seed 0: 3038984756725240190, -3694039286755638414, 4633751808701151732, 2160572957309072155, 1839370574944072389 — all five match | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Xoroshiro128++ seeded with 42 produces the three consecutive next_long() values -4695948378737616609, 7341713790291473579, -7542733514721318211. | Same execution of RandomSupport.java:19-34 plus Xoroshiro128PlusPlus.java:17-34 on legacy seed 42: -4695948378737616609, 7341713790291473579, -7542733514721318211 — all three match | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Upgrading and mixing a legacy seed of 0 into the 128-bit Xoroshiro seed pair yields exactly (3847398142028685078, 7192185014346937746). | Same execution: `upgradeSeedTo128bitUnmixed(0).mixed()` (RandomSupport.java:26-30 and :59-61) yields (3847398142028685078, 7192185014346937746) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla item stack sizes vary per item rather than a single uniform cap; for example eggs stack to only 16. | `Items.EGG` (Items.java:1291-1293) is registered with `new Item.Properties().stacksTo(16)`; other items set `stacksTo(1)` (e.g. SADDLE, Items.java:870; MINECART :874; shulker boxes :636) against the 64 default, so the cap is genuinely per-item | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Two item entities merge in vanilla when they hold the same item id and components and are within 0.5 blocks of each other. | The distance test is not a centre distance. `ItemEntity.mergeWithNeighbours` (ItemEntity.java:212-224) selects candidates by AABB overlap against `this.getBoundingBox().inflate(0.5, 0.0, 0.5)` — 0.5 blocks on X and Z, ZERO on Y. `areMergable` (:243-247) then requires `ItemStack.isSameItemSameComponents` AND `otherCount + thisCount <= otherStack.getMaxStackSize()`. The scan is also not per-tick: it runs every 2nd tick while the item crossed a block boundary, otherwise every 40th (:168-174), and only for entities passing `isMergable` (:226-229: alive, pickupDelay != 32767, age != -32768, age < 6000, count < max stack size) | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's real item pickup detection is based on AABB collision-box overlap between the player and the item entity, not a fixed pickup-radius constant. | `Player.aiStep` (Player.java:457-478) collects `level().getEntities(this, pickupArea)` and calls `playerTouch` on each; `ItemEntity.playerTouch` (ItemEntity.java:341-357) does the pickup. Note for §M: the inflation is applied to the PLAYER's bounding box and is `inflate(1.0, 0.5, 1.0)` (Player.java:462), not 0.5 on every axis of the item's box | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A dropped item entity becomes eligible for player pickup only once its age reaches its pickup delay (10 ticks by default). | Age is never compared to the pickup delay. `ItemEntity.playerTouch` (ItemEntity.java:341-346) requires `this.pickupDelay == 0`, and `pickupDelay` is a countdown decremented once per tick while `> 0` and `!= 32767` (:123-125); `age` is an independent counter (:176-178). The elapsed wait after a block drop is the same 10 ticks, but the mechanism is a decrementing delay, not an age threshold — and it keeps 32767 (never pick up) and explicit `setPickUpDelay` values expressible | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's inventory insertion for a picked-up item tries the currently selected hotbar slot first, if it is empty or already holds a mergeable stack of the same item. | An empty selected slot is not preferred. `Inventory.getSlotWithRemainingSpace` (Inventory.java:224-243) tests the selected slot with `hasRemainingSpaceForItem` (:95-100), which requires the slot to be NON-empty, same item and components, stackable, and below its max count. Empty slots are only reached later, through `getFreeSlot` (:102-110) once no partial stack was found (`addResource`, :195-202) | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Failing the selected slot, vanilla scans occupied inventory slots hotbar-then-main-inventory in ascending index order for a mergeable partial stack to top up. | One candidate is missing between the two. `Inventory.getSlotWithRemainingSpace` (Inventory.java:224-243) checks the selected slot first, then slot 40 — the OFF-HAND slot (`SLOT_OFFHAND = 40`, :33) — and only then scans `items` ascending from index 0. Armor is excluded not by a rule but because `items` holds only 36 entries (hotbar 0-8, main 9-35; :56), so the blueprint's 'armor/off-hand excluded' is wrong for the off-hand | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Failing a mergeable partial stack, vanilla fills the first empty slot found in that same hotbar-then-main-inventory scan order. | `Inventory.addResource` (Inventory.java:195-202) falls back to `getFreeSlot()` when `getSlotWithRemainingSpace` returns -1, and `getFreeSlot` (:102-110) returns the first empty index of the 36-entry `items` list — hotbar 0-8 then main 9-35, ascending | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Any remainder of a picked-up stack that does not fit in any slot is left on the item entity rather than being picked up. | `Inventory.add` (Inventory.java:255-296) loops `addResource` only while the count keeps shrinking and leaves the remainder on the passed stack; `ItemEntity.playerTouch` (ItemEntity.java:341-355) discards the entity only when the stack ends up empty, otherwise the entity survives with the reduced count | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| An item entity's age increments by 1 every tick unconditionally, including ticks where it does not move. | `ItemEntity.tick` (ItemEntity.java:176-178): `if (this.age != -32768) { this.age++; }` — outside the moved/at-rest branch, so it runs on every tick of a live item. The only exception is the INFINITE_LIFETIME sentinel -32768 (:45); the tick body as a whole is skipped only when the stack is empty (:119-121) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A dropped item entity despawns once its age reaches 6000 ticks (5 minutes). | `ItemEntity.LIFETIME = 6000` (ItemEntity.java:43); the server-side discard is `if (!level().isClientSide() && this.age >= 6000) this.discard();` (:188-190) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla's per-entity network position/velocity resync interval defaults to every 3 ticks (updateInterval default). | `EntityType.Builder`'s field default is `private int updateInterval = 3;` (EntityType.java:476), consumed by ServerEntity's `tickCount % this.updateInterval == 0` gate (ServerEntity.java:125). Note for §O: ITEM overrides it to 20 (`updateInterval(20)`, EntityTypes.java:565), so applying 3 uniformly is wrong for the item kind; zombie, villager and cow do not override it | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The delta-encoded entity position update packet form only covers a per-axis range of plus or minus 8 blocks, beyond which an absolute teleport packet must be used instead. | `VecDeltaCodec` encodes each axis as `Math.round(value * 4096.0)` (VecDeltaCodec.java:7-21) and the packet carries shorts (ClientboundMoveEntityPacket.java:14-16), so the representable delta is 32768/4096 = 8 blocks per axis; `ServerEntity` (ServerEntity.java:149-161) switches to the absolute packet when any axis leaves the short range. Note: the absolute packet vanilla sends on that path is `ClientboundEntityPositionSyncPacket` (`minecraft:entity_position_sync`, protocol_id 35), not `minecraft:teleport_entity` (125) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The Take Item Entity clientbound play packet is assigned id 0x71 and carries, in order, collected_entity_id, collector_entity_id, and pickup_item_count (all varint-encoded). | The packet id is 124 (0x7C), not 0x71 (113): reports/packets.json, play / clientbound, `minecraft:take_item_entity` -> protocol_id 124. The payload IS as claimed — `ClientboundTakeItemEntityPacket` (ClientboundTakeItemEntityPacket.java:22-32) reads and writes three varints in the order itemId (the collected entity), playerId (the collector), amount | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A living entity's eyes are considered to be in a fluid of a given kind when that fluid's surface height at the block containing the eye position exceeds the eye position's own y coordinate. | `EntityFluidInteraction.update` (EntityFluidInteraction.java:68-70) sets `tracker.eyesInside` when `x == entity.getBlockX() && z == entity.getBlockZ() && eyeY >= fluidBottom && eyeY <= fluidTop`, where `fluidTop = blockY + fluidState.getHeight(level, pos)` (:58-59); `isEyeInFluid` (:148-151) reports that flag. Two nuances: the top comparison is inclusive (`eyeY <= fluidTop`), and only cells inside the deflated fluid-interaction box are visited | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Item entities carry an air-supply field that is never consumed, matching vanilla's own harmless-but-unused air field on non-LivingEntity kinds. | The air field is defined on `Entity` (DATA_AIR_SUPPLY_ID, Entity.java:268), initialised to `getMaxAirSupply()` (:319) and persisted as `Air` (:2170); the only consumer is `LivingEntity.aiStep` (LivingEntity.java:436-458). ItemEntity neither reads nor writes it — its own save data is Health/Age/PickupDelay/Thrower/Owner/Item (ItemEntity.java:315-338) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Fall-impact damage evaluation triggers on the tick an entity newly transitions onto the ground, having been airborne the previous tick, while carrying a positive accumulated fall distance. | There is no was-airborne-last-tick test. `Entity.checkFallDamage` (Entity.java:1562-1580) accumulates `fallDistance -= ya` for negative Y movement while not in water, then fires `onState.getBlock().fallOn(...)` whenever `onGround && fallDistance > 0.0`, and resets fallDistance to 0 on every grounded evaluation. An entity that leaves the ground and lands within the same movement therefore still triggers, and the trigger is evaluated per move call (`doCheckFallDamage`, :1553-1560), not once per tick against the previous tick's ground flag. `LivingEntity.checkFallDamage` (LivingEntity.java:362-386) layers the damage onto that same condition | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla maintains two independent RNG streams: a general per-world random source used for non-deterministic effects such as a dropped item's spawn position/velocity jitter, and a separate, deterministic random_sequence stream used for loot-table rolls. | `Block.popResource` (Block.java:412-419) draws its jitter from `level.getRandom()`; loot rolls draw from `LootContext.Builder.create` (LootContext.java:138-141), which resolves the random as the explicit override, else `server.getRandomSequence(randomSequenceKey)` for the table's `random_sequence` id, else `level.getRandom()` — the two streams are distinct, and the sequence one is the persisted, deterministic `RandomSequences` state (RandomSequences.java:45-59) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Vanilla exempts certain item entities from age-based despawn: those given a custom name, and those on a fixed persistent-category list. | Neither exemption exists for item entities. The despawn path is only `age >= 6000` (ItemEntity.java:188-190); the sole escape is the sentinel age -32768 (INFINITE_LIFETIME, :45) set by `setUnlimitedLifetime` (:420-422), which nothing in the 26.2 server calls, plus `setExtendedLifetime` (:424-426) starting age at -6000 (used once, WitherBoss.java:492 for the dropped nether star). There is no custom-name check and no persistent-category list on this path — a named item entity still despawns. (Custom-name/persistence exemptions belong to Mob despawning, not ItemEntity.) | WRONG | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Xoroshiro's next_bits(n) is computed as an unsigned right shift of next_long() by (64 - n) bits. | `XoroshiroRandomSource.nextBits` (XoroshiroRandomSource.java:108-110): `return this.randomNumberGenerator.nextLong() >>> 64 - bits;` — Java's `>>>` is the unsigned right shift | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Xoroshiro's next_bool() is true when the low bit of next_long() is set. | `XoroshiroRandomSource.nextBoolean` (XoroshiroRandomSource.java:81-84): `return (this.randomNumberGenerator.nextLong() & 1L) != 0L;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
