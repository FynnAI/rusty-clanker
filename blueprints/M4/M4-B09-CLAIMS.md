# M4-B09 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Registry attribute `ATTACK_SPEED` defaults to `4.0` within range `[0.0, 1024.0]`, and Zombie, Villager, and Cow each use `4.0` (Context Part B, attribute reconciliation table). | `net/minecraft/world/entity/ai/attributes/Attributes.java:19-21` — `register("attack_speed", new RangedAttribute("attribute.name.attack_speed", 4.0, 0.0, 1024.0).setSyncable(true))`, plus `Attributes.java:9` `DEFAULT_ATTACK_SPEED = 4.0`. Default and range are exact. Note on the per-kind rows: in vanilla only `Player.createAttributes` registers this attribute (`Player.java:210`); `LivingEntity.createLivingAttributes` (`LivingEntity.java:332-360`) and `Mob.createMobAttributes` (`Mob.java:166-168`) do not, so a vanilla Zombie, Villager or Cow carries no `attack_speed` instance at all — `4.0` for those kinds is the registry-wide default, not a vanilla per-mob value (consistent with the blueprint's own "mob melee path never reads it") | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Registry attribute `SAFE_FALL_DISTANCE` defaults to `3.0` within range `[-1024.0, 1024.0]`, used by Zombie, Villager, and Cow (Context Part B, attribute reconciliation table). | `Attributes.java:86-88` — `register("safe_fall_distance", new RangedAttribute("attribute.name.safe_fall_distance", 3.0, -1024.0, 1024.0).setSyncable(true))`; registered for every living entity at `LivingEntity.java:343` (`createLivingAttributes().add(Attributes.SAFE_FALL_DISTANCE)`), which Zombie (`Monster.createMonsterAttributes` to `Mob.createMobAttributes`), Villager and Cow all inherit, none overriding the base value | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Registry attribute `FALL_DAMAGE_MULTIPLIER` defaults to `1.0` within range `[0.0, 100.0]`, used by Zombie, Villager, and Cow (Context Part B, attribute reconciliation table). | `Attributes.java:44-47` — `register("fall_damage_multiplier", new RangedAttribute("attribute.name.fall_damage_multiplier", 1.0, 0.0, 100.0).setSyncable(true).setSentiment(NEGATIVE))`; registered for every living entity at `LivingEntity.java:344`, inherited unmodified by Zombie (`Zombie.java:131-138`), Villager (`Villager.java:227-229`) and Cow (`AbstractCow.java:56-58`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Registry attribute `SWEEPING_DAMAGE_RATIO` defaults to `0.0` within range `[0.0, 1.0]`, used by Zombie, Villager, and Cow (Context Part B, attribute reconciliation table). | `Attributes.java:104-106` — `register("sweeping_damage_ratio", new RangedAttribute("attribute.name.sweeping_damage_ratio", 0.0, 0.0, 1.0).setSyncable(true))`. Default and range are exact. Note on the per-kind rows: vanilla registers this attribute only on the player (`Player.java:217`) and reads it only in the player sweep path (`Player.java:1167`); Zombie, Villager and Cow carry no instance of it, so `0.0` for those kinds is the registry default, matching the blueprint's own "vestigial, retained for Update Attributes registry completeness" framing | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| The clientbound Update Attributes packet has protocol id `0x83` (Context Part B, "Retiring the duplicate UpdateAttributes wire packet"). | `datagen/generated/reports/packets.json`, `play` to `clientbound` to `minecraft:update_attributes`: `{"protocol_id": 131}`; 131 decimal is `0x83` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A Zombie's default `ATTACK_DAMAGE` attribute value is `3.0` (Deliverables, `ai_scenario_layout.rs` acceptance-test spec). | `net/minecraft/world/entity/monster/zombie/Zombie.java:131-138` — `createAttributes()` adds `Attributes.ATTACK_DAMAGE, 3.0` on top of `Monster.createMonsterAttributes()` (`Monster.java:119-121`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A Zombie's `FOLLOW_RANGE` attribute is `35.0` blocks; a target at `40.0` blocks is never acquired (Context Part G, scenario 3). | `Zombie.java:133` — `.add(Attributes.FOLLOW_RANGE, 35.0)` (overriding `Mob.createMobAttributes`'s own `16.0`, `Mob.java:167`, and the registry default `32.0`, `Attributes.java:51`). `NearestAttackableTargetGoal.java:45` and `:86` build the targeting conditions with `range(this.getFollowDistance())`, and `TargetingConditions.test` (`TargetingConditions.java:78-85`) rejects any candidate whose squared distance exceeds `max(range * visibilityModifier, 2.0)` squared — the visibility modifier is at most `1.0`, so the effective range never exceeds `35.0` and a target at `40.0` is always rejected | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A Zombie's `NearestAttackableTargetGoal` is gated by `has_line_of_sight`, so a target within range but behind an opaque wall is never acquired (Context Part G, scenario 4). | `NearestAttackableTargetGoal.java:45` constructs `TargetingConditions.forCombat()`, whose `checkLineOfSight` field defaults to `true` (`TargetingConditions.java:13`) and is not cleared anywhere on this path; `TargetingConditions.test` (`TargetingConditions.java:87-89`) returns false when `checkLineOfSight` and the targeter is a `Mob` and `mob.getSensing().hasLineOfSight(target)` is false. Contrast `HurtByTargetGoal.java:19`, which explicitly calls `ignoreLineOfSight()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A Cow's `target_selector` is constructed with zero goal entries, so a Cow never acquires an attack target (Context Part G, scenario 6). | `net/minecraft/world/entity/animal/cow/AbstractCow.java:40-49` — `registerGoals()` adds eight entries, all to `goalSelector` (priorities 0 through 7: Float, Panic, Breed, Tempt, FollowParent, WaterAvoidingRandomStroll, LookAtPlayer, RandomLookAround) and none to `targetSelector`; `Cow.java` does not override `registerGoals`, and neither `Animal.java` nor `Mob.java` adds a target goal | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A mob's `HurtByTargetGoal` sets its current target to the attacker within 2 ticks of taking damage, overriding ordinary range-based targeting (Context Part G, scenario 7). | The "overrides ordinary range-based targeting" half is exact: `HurtByTargetGoal.start` (`HurtByTargetGoal.java:60-63`) calls `mob.setTarget(mob.getLastHurtByMob())`, its `HURT_BY_TARGETING` conditions set no range and call `ignoreLineOfSight()`/`ignoreInvisibilityTesting()` (`HurtByTargetGoal.java:19`), and Zombie registers it at target-selector priority `1` against `NearestAttackableTargetGoal`'s `2` (`Zombie.java:124-125`), so `GoalSelector`'s FLAG_TARGET eviction (`GoalSelector.java:82-93`) prefers it. The timing half is WRONG: `canUse`/`start` are only evaluated on a *full* selector tick, and `Mob.serverAiStep` (`Mob.java:722-736`) runs the full `targetSelector.tick()` only when `(tickCount + getId()) % 2 == 0` (or `tickCount <= 1`), otherwise just `tickRunningGoals(false)` — so vanilla acquires the attacker 1 or 2 ticks after the damage depending on the entity-id phase, never guaranteed on the immediately-next tick. Correction: scenario 7's deadline must be 2 ticks, not next-tick, to stay consistent with this blueprint's own half-tick-throttle claim | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A Cow's flee-on-damage behavior (`PanicGoal`) moves away from the attacker's last-known position at a `2.0` navigation-speed modifier; the Villager has no `FleeFromHostile` goal, and its own brain-driven panic package computes a runaway speed of `1.5x` the villager's own speed modifier instead (Context Part C.4, citing M4-B03). | WRONG for the Cow. `AbstractCow.java:42` — `this.goalSelector.addGoal(1, new PanicGoal(this, 2.0))`: the cow's panic speed modifier is `2.0`, not `1.5`. The Villager half is right but the goal name is not: the villager has no `FleeFromHostile` goal — it is brain-driven, and `VillagerGoalPackages.getPanicPackage` (`VillagerGoalPackages.java:231-240`) computes `runawaySpeed = speedModifier * 1.5F` and hands it to `SetWalkTargetAwayFrom.entity(MemoryModuleType.NEAREST_HOSTILE, ...)` and `SetWalkTargetAwayFrom.entity(MemoryModuleType.HURT_BY_ENTITY, ...)`. Correction: Cow panic multiplier `2.0`; the `1.5x` multiplier is the villager panic package's, applied to the villager's own speed modifier | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A Villager's brain, on taking damage, sets the `HurtBy` memory module type to the damage source itself and the `HurtByEntity` memory module type to the attacking entity only when that source has a living attacker, both written by a sensor with no TTL argument (Context Part C.4). | `net/minecraft/world/entity/ai/sensing/HurtBySensor.java:22-31` — the sensor (not the damage path) reads `body.getLastDamageSource()` and stores `MemoryModuleType.HURT_BY` = the `DamageSource` itself, typed `MemoryModuleType<DamageSource>` (`MemoryModuleType.java:56`), and only if `damageSource.getEntity() instanceof LivingEntity` does it store `MemoryModuleType.HURT_BY_ENTITY` = that `LivingEntity` (`MemoryModuleType.java:57`). Correction: `HurtBy` holds the damage source, not the attacker id; `HurtByEntity` holds the attacking entity and is set only when the source has a living attacker. Neither `setMemory` call passes an expiry, so there is no TTL: `HURT_BY` is erased when `getLastDamageSource()` returns null, which happens once more than 40 ticks have passed since the hit (`LivingEntity.java:1419-1425`), and `HURT_BY_ENTITY` is erased when the attacker dies or changes level (`HurtBySensor.java:33-36`) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A mob's pathfinding never returns a path where two consecutive nodes differ by more than `3` in y, when the mob has no current AI target — the descent scan is bounded by the mob's own max-fall-distance, which is `3` only in the no-target case and far larger, health- and difficulty-dependent, once a target is set (Context Part G, scenario 2, citing M4-B03 section F). | `net/minecraft/world/level/pathfinder/WalkNodeEvaluator.java:353-370` (`tryFindFirstGroundNodeBelow`) scans downward from `y - 1` all the way to the world floor and only returns a blocked node once `y - currentY > this.mob.getMaxFallDistance()` — the bound is the mob's own max fall distance, not a fixed `3`, and the scan is not a fixed Y-1..=Y-3 window. `Mob.getMaxFallDistance` (`Mob.java:834-846`) returns `getComfortableFallDistance(0.0F)` = `floor(0 + 3)` = `3` only when the mob has NO target; with a target it returns `floor(3 + max(0, (int)(health - maxHealth * 0.33F) - (3 - difficultyId) * 4))` — for a full-health 20 HP zombie that is `3 + 13 - 4 = 12` on Normal and `3 + 13 = 16` on Hard. Correction: a targeting zombie will path down far more than 3 blocks; the `<= 3` bound holds only for a mob with no current target | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A player's default, unenchanted `AttackDamage` attribute value is `1.0`; `2.0` is only the attribute's registry-wide default, which the player's own attribute supplier overrides (Context Part G, scenario 10). | `net/minecraft/world/entity/player/Player.java:206-219` — `createAttributes()` adds `Attributes.ATTACK_DAMAGE, 1.0`. Correction: the player's base attack damage is `1.0` (bare-handed); `2.0` is only the attribute's registry-wide default (`Attributes.java:17`), which the player overrides. Every downstream number in scenario 10 that starts from `2.0` inherits this error | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Against a target with `10.0` armor and `0.0` armor toughness, a `2.0`-damage hit computes real_armor=clamp(10.0-2.0/2.0,2.0,20.0)=9.0, armor_fraction=9.0/25.0=0.36, and deals dealt=2.0*0.64=1.28 damage — the clamp applies to real_armor within [total_armor*0.2,20.0], not to the fraction within [0,1] (Context Part G, scenario 10). | `net/minecraft/world/damagesource/CombatRules.java:19-35` (`getDamageAfterAbsorb`, reached via `LivingEntity.getDamageAfterArmorAbsorb`, `LivingEntity.java:1902-1909`): `toughnessDivisor = 2.0 + armorToughness / 4.0`; `realArmor = clamp(totalArmor - damage / toughnessDivisor, totalArmor * 0.2, 20.0)`; `armorFraction = realArmor / 25.0`; the `clamp(..., 0.0, 1.0)` exists only around the enchantment-modified fraction, never around the raw one. Correction: the clamp belongs on realArmor with bounds `[totalArmor * 0.2, 20.0]`, not on the fraction with bounds `[0, 1]` — the two coincide at these inputs (realArmor = clamp(9.0, 2.0, 20.0) = 9.0, fraction 0.36, dealt 1.28, so both asserted numbers are right) but diverge elsewhere, e.g. armor 1.0 with damage 10.0 gives vanilla fraction 0.008 against the claim's 0.0 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At attack-cooldown ticker `3` of a 5-tick charge window, the charge fraction is charge=clamp(3.5/5,0,1)=0.7 (Context Part G, scenario 10). | `Player.java:1826-1828` — `getAttackStrengthScale(a) = clamp((attackStrengthTicker + a) / getCurrentItemAttackStrengthDelay(), 0.0, 1.0)`, called with `a = 0.5F` on every melee path (`Player.java:956`, `:1208`); `getCurrentItemAttackStrengthDelay` (`Player.java:1816-1818`) = `1.0 / attack_speed * 20.0` = `5.0` ticks at the bare-handed `ATTACK_SPEED` of `4.0`. Ticker 3 gives `(3 + 0.5) / 5 = 0.7` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| When a target's invulnerable_time exceeds `10` ticks, a second hit within that window computes raw damage as raw=(0.2+charge^2*0.8)*base_damage, which for charge=0.7 and base_damage=2.0 gives raw=(0.2+0.7^2*0.8)*2.0=1.184 (Context Part G, scenario 10). | `Player.baseDamageScaleFactor` (`Player.java:1207-1210`) = `0.2F + attackStrengthScale * attackStrengthScale * 0.8F`, applied as `baseDamage *= this.baseDamageScaleFactor()` in `Player.attack` (`Player.java:957`). At charge 0.7: `0.2 + 0.49 * 0.8 = 0.592`, times `2.0` = `1.184`. The formula and the arithmetic are exact; two framing notes that do not change this claim's own numbers — the charge scaling applies to every hit, not only to hits inside the invulnerability window, and a real player's `base_damage` is `1.0` rather than `2.0` (see the player-attack-damage row) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| If a top-up hit's raw, pre-armor incoming damage does not exceed the previous hit's own raw, pre-armor incoming damage (`lastHurt`), the damage pipeline is a no-op and the target's health is unchanged (Context Part G, scenario 10). | `LivingEntity.hurtServer` (`LivingEntity.java:1217-1224`): inside the `invulnerableTime > 10.0F` branch, `if (damage <= this.lastHurt) return false;` — both sides are the **raw, pre-armor** incoming amounts (`lastHurt` is assigned the raw `damage` of the previous hit at `:1223` and `:1226`), and when the new hit does exceed, only the raw difference `damage - lastHurt` is passed to `actuallyHurt`, which applies armor afterwards (`LivingEntity.java:1954-1957`). Correction: the comparison is raw-incoming against raw-previous, not post-armor gross against previously-dealt; the two orderings can disagree whenever the two hits see different armor-reduction fractions | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Across the three hits of scenario 10, a `20.0`-health target's health sequence is 20.0 -> 19.38 -> 19.38 (unchanged) -> 19.38 (unchanged) (Context Part G, scenario 10). | Recomputed against the reference for scenario 10's own setup (bare-handed player, zombie with `ARMOR` overridden to `10.0`, toughness `0.0`, max health `20.0` from `Attributes.java:68-70` since Zombie overrides no `MAX_HEALTH`): hit 1 raw = `1.0 * 1.0` = `1.0` (`Player.java:206-219`, `:952`, `:957`), dealt = `1.0 * (1 - clamp(10 - 1.0/2, 2, 20)/25)` = `1.0 * 0.62` = `0.62`, health `19.38`, `lastHurt = 1.0`, `invulnerableTime = 20` (`LivingEntity.java:1226-1228`); hit 2 at T0+3, `invulnerableTime = 17 > 10`, raw = `0.592 <= 1.0` so `return false`, health unchanged; hit 3 at T0+8, `invulnerableTime = 12` (decremented once per tick, `LivingEntity.java:471-472`) is still `> 10`, so it takes the top-up branch too and raw = `1.0 <= lastHurt 1.0` gives another no-op. Correction: the vanilla sequence is 20.0 -> 19.38 -> 19.38 -> 19.38; hit 3 at T0+8 does not deal fresh damage, because the top-up branch is still active at that point | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A target's post-hit invulnerability bookkeeping window is `20` ticks, but the damage top-up branch applies only while that counter exceeds `10`; a hit landing after it has decremented to `10` or below instead deals independent fresh damage and resets the counter (Context Part G, scenario 10 setup). | `LivingEntity.java:1227` sets `invulnerableTime = 20` on a full hit (also `:2046`), and `LivingEntity.java:471-472` decrements it once per tick for every non-`ServerPlayer` entity — so the `20`-tick figure is exact. The second half is WRONG: the top-up branch is gated on `invulnerableTime > 10.0F` (`LivingEntity.java:1217`), i.e. only the first 10 ticks after the hit; from tick 11 through 20 the else branch runs instead, dealing independent fresh damage and resetting `lastHurt` and `invulnerableTime` (`LivingEntity.java:1225-1231`). Correction: top-up window = 10 ticks, invulnerability bookkeeping window = 20 ticks | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A critical hit multiplies damage by `1.5x` (Context Part G, scenario 11, citing M4-B05's can_critical_attack). | `Player.attack` (`Player.java:970-973`) — `boolean criticalAttack = fullStrengthAttack && this.canCriticalAttack(entity); if (criticalAttack) { baseDamage *= 1.5F; }`. The multiplier applies to the charge-scaled base damage (plus the item's attack-damage bonus, `:969`) before the enchantment magic boost is added at `:974` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A critical hit is eligible only when fall distance is nonzero, the attacker is not on the ground, not on a climbable block, not in water, not mobility-restricted, not a passenger, the target is a LivingEntity, and the attacker is not sprinting — plus the caller's own full-strength gate, attack-cooldown charge scale > 0.9 (Context Part G, scenario 11, citing M4-B05's can_critical_attack). | `Player.canCriticalAttack` (`Player.java:1032-1041`) requires all eight of: `fallDistance > 0.0`, `!onGround()`, `!onClimbable()`, `!isInWater()`, `!isMobilityRestricted()`, `!isPassenger()`, `entity instanceof LivingEntity`, `!isSprinting()`; and the caller gates it further on `fullStrengthAttack`, i.e. `getAttackStrengthScale(0.5F) > 0.9F` (`Player.java:962` and `:971`). Correction: the four listed conditions are necessary but not the full set — add `!onClimbable`, `!isMobilityRestricted`, `!isPassenger`, target is a `LivingEntity`, and charge `> 0.9` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| At attack-cooldown ticker `0`, the charge scale is charge_scale=clamp(0.5/5,0,1)=0.1, giving a base-damage scale factor of base_damage_scale_factor=0.2+0.01*0.8=0.208 (Context Part G, scenario 11). | `Player.java:1826-1828` with `a = 0.5F` and `getCurrentItemAttackStrengthDelay()` = `20 / 4.0` = `5.0` (`Player.java:1816-1818`, bare-handed `ATTACK_SPEED` 4.0 from `Attributes.java:19-21`) gives `(0 + 0.5) / 5 = 0.1`; `Player.baseDamageScaleFactor` (`Player.java:1207-1210`) then gives `0.2 + 0.1 * 0.1 * 0.8 = 0.208` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A fully-charged critical hit's damage-scale factor is `1.0`; after the 1.5x critical multiplier, the ratio of a fully-charged critical hit's damage to an uncharged hit's damage is (1.0*1.5)/0.208 approximately equal to 7.212 (Context Part G, scenario 11). | `Player.baseDamageScaleFactor` (`Player.java:1207-1210`) at scale `1.0` gives `0.2 + 1.0 * 0.8 = 1.0`; the critical multiplier is `1.5F` (`Player.java:970-973`), and `1.5 / 0.208 = 7.2115...`. Consistent with the eligibility gate, since a full-charge hit satisfies `attackStrengthScale > 0.9F` while the ticker-0 hit at scale `0.1` cannot be critical (`Player.java:962`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A mob's AI goal/target-selector evaluation is throttled to run only every other tick (the half-tick throttle, `should_full_tick`, M4-B03 §D), bounding any AI reaction to a newly eligible target or attack to at most one extra tick of latency beyond the triggering event (Context Part G, scenarios 5 and 9). | `Mob.serverAiStep` (`Mob.java:716-736`) — `int idBasedTickCount = this.tickCount + this.getId(); if (idBasedTickCount % 2 != 0 && this.tickCount > 1) { targetSelector.tickRunningGoals(false); goalSelector.tickRunningGoals(false); } else { targetSelector.tick(); goalSelector.tick(); }`. The full re-evaluation (`GoalSelector.tick`'s cleanup plus `canUse` sweep, `GoalSelector.java:70-96`) therefore runs on every other tick, with the phase offset per entity by its entity id; on the skipped ticks only running goals that declare `requiresUpdateEveryTick()` are ticked (`GoalSelector.java:98-109`). One extra tick of latency is the exact bound | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A Zombie's random-stroll goal is registered in its goal selector at priority `7` (Context Part G, scenario 9 setup). | `Zombie.addBehaviourGoals` (`Zombie.java:119-129`) — `this.goalSelector.addGoal(7, new WaterAvoidingRandomStrollGoal(this, 1.0))`, alongside SpearUse at 2, ZombieAttackGoal at 3, MoveThroughVillage at 6, and (from `registerGoals`, `Zombie.java:112-117`) ZombieAttackTurtleEgg at 4 and the two look goals at 8 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| A mob's per-tick AI evaluation order is: sensing, then target-selector evaluation, then goal-selector evaluation, then navigation, then brain evaluation (not Villager-only — any mob whose own AI hook populates one), then movement-intent application via the move/look/jump controls last (Context Part D, citing M4-B03's `systems.rs` doc comment). | `Mob.serverAiStep` (`Mob.java:716-752`) runs, in order: `sensing.tick()` (`:720`); `targetSelector` then `goalSelector` (`:722-736`, target selector first); `navigation.tick()` (`:740`); `customServerAiStep(level)` (`:743`), which is where the Villager brain ticks (`Villager.java:236-240`); then the controls `moveControl.tick()`, `lookControl.tick()`, `jumpControl.tick()` (`:747-750`). Correction: navigation runs BEFORE the brain, not after, and the movement/look/jump controls run last of all — vanilla order is sensing, target selector, goal selector, navigation, brain, movement controls | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
| Each modifier entry in the Update Attributes packet's per-attribute modifier array carries an identifier, an amount, and an operation (Context Part B, "Retiring the duplicate UpdateAttributes wire packet"). | `net/minecraft/network/protocol/game/ClientboundUpdateAttributesPacket.java:62-70` — `AttributeSnapshot.MODIFIER_STREAM_CODEC` composes exactly `Identifier.STREAM_CODEC` (`AttributeModifier::id`), `ByteBufCodecs.DOUBLE` (`AttributeModifier::amount`), `AttributeModifier.Operation.STREAM_CODEC` (`AttributeModifier::operation`), in that order; each snapshot is the attribute holder, a double base value, and that modifier collection (`:71-80`), and the packet is entity id plus a list of snapshots capped at 128 (`:20-26`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-03 |
