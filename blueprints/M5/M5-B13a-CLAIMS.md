# M5-B13a — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Desert pyramid, jungle temple, and swamp hut are each generated via random_spread placement as a single procedurally-coded ScatteredFeaturePiece that writes its own geometry directly through generateBox/placeBlock/maybeGenerateBlock — no NBT template, StructureTemplate, or TemplateStructurePiece is involved, and the piece's horizontal orientation is a Direction drawn via getRandomHorizontalDirection, not a Rotation. | Placement is right, the piece shape is not. `desert_pyramids`/`jungle_temples`/`swamp_huts` structure_set json are all `minecraft:random_spread` (spacing 32, separation 8, salts 14357617/14357619/14357620), but none of the three uses an NBT template at all: `DesertPyramidPiece` (DesertPyramidPiece.java:23-31), `JungleTemplePiece` (JungleTemplePiece.java:30-41) and `SwampHutPiece` (SwampHutPiece.java:25-31) all extend `ScatteredFeaturePiece` and write every block procedurally through `generateBox`/`placeBlock` (e.g. SwampHutPiece.java:57-70). There is no `StructureTemplate`, no `TemplateStructurePiece`, and no rotation - orientation is a `Direction` from `getRandomHorizontalDirection` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Igloo is generated via random_spread placement as one igloo/top template on a failed basement roll, or as 5 to 12 stacked templates (one igloo/bottom, depth-1 igloo/middle pieces, and one igloo/top, where depth = next_int_bounded(8) + 4 gives 4..11) on a successful basement roll. | Placement and the coin-flip are right, the piece count is not. `igloos.json` is `random_spread` (spacing 32, separation 8, salt 14357618) and `IglooPieces.addPieces` (IglooPieces.java:47-64) rolls `random.nextDouble() < 0.5`, but on success it draws `int depth = random.nextInt(8) + 4` (4..11) and emits ONE `igloo/bottom` plus `depth - 1` `igloo/middle` pieces plus the `igloo/top` piece - 5 to 12 template pieces (1 + (depth-1) + 1 = depth+1), never "one or two" | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean ruin is generated via random_spread placement as a uniformly-picked NBT template (array[next_int_bounded(array.length)], never a weighted pick) plus a cluster of satellite ruins. | Placement and the cluster are right, "weighted" is not. `ocean_ruins.json` is `random_spread` (spacing 20, separation 8, salt 14357621) and `OceanRuinPieces.addClusterRuins` (OceanRuinPieces.java:170-199) adds the satellites, but the template pick is UNIFORM: warm uses `Util.getRandom(WARM_RUINS, random)` / `Util.getRandom(BIG_WARM_RUINS, random)` (:146-152) = `array[random.nextInt(array.length)]` (Util.java:826-828), and cold draws one shared index `random.nextInt(bricks.length)` and stamps THREE overlaid pieces from the brick, cracked and mossy arrays at integrities 0.9-or-0.8, 0.7 and 0.5 (:236-245). No weights exist anywhere on this path | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Shipwreck is generated via random_spread placement as a uniformly-picked NBT template (array[next_int_bounded(array.length)], never a weighted pick); only its vertical placement is terrain-driven, not its orientation, which is a plain uniform rotation draw. | Placement is right, "weighted" is not. `shipwrecks.json` is `random_spread` (spacing 24, separation 4, salt 165745295), but `ShipwreckPieces.addRandomPiece` (ShipwreckPieces.java:76-88) picks with `Util.getRandom(isBeached ? STRUCTURE_LOCATION_BEACHED : STRUCTURE_LOCATION_OCEAN, random)` - a uniform `nextInt(length)` over 11 beached or 20 ocean template ids (:34-68). Terrain affects only the vertical placement, not the rotation | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Buried treasure is generated via a degenerate random_spread placement (spacing=1, separation=0) as a single chest with no template. | `buried_treasures.json` is `minecraft:random_spread` with `spacing: 1`, `separation: 0`, `salt: 0`, `frequency: 0.01`, `frequency_reduction_method: legacy_type_2`, `locate_offset: [9,0,9]`; `BuriedTreasurePieces.BuriedTreasurePiece` (BuriedTreasurePieces.java:24-81) extends `StructurePiece` directly, loads no template, and ends in one `createChest(..., BuiltInLootTables.BURIED_TREASURE, null)` (:75) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal is generated via random_spread placement as a genuinely weighted Setup pick (a cumulative-weight walk) plus a uniformly-picked NBT template (array[next_int_bounded(array.length)], never weighted) plus procedural reskin rules. | Placement, the weighted Setup pick and the reskin rules are right, the weighted TEMPLATE pick is not. `ruined_portals.json` is `random_spread` (spacing 40, separation 15, salt 34222645); the Setup pick is a genuine cumulative-weight walk over `Setup.weight()` (RuinedPortalStructure.java:70-88); but the template is chosen UNIFORMLY - `STRUCTURE_LOCATION_GIANT_PORTALS[random.nextInt(3)]` or `STRUCTURE_LOCATION_PORTALS[random.nextInt(10)]` (:97-101) - and the reskin is a `RuleProcessor` list plus `BlockAgeProcessor`, `ProtectedBlockProcessor`, `LavaSubmergedBlockProcessor` and optionally `BlackstoneReplaceProcessor` (RuinedPortalPiece.java:126-149) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Pillager outposts are JigsawStructure-typed exactly like villages, per 06-structures.md section 3.9's own opening line. | `data/minecraft/worldgen/structure/pillager_outpost.json` declares `"type": "minecraft:jigsaw"` with `start_pool: minecraft:pillager_outpost/base_plates`, `size: 7`, `use_expansion_hack: true` - the same `StructureType.JIGSAW` (StructureType.java:29) the five `village_*.json` entries use | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| dungeon (the vanilla "monster room" content) is not a StructureType at all -> it is the monster_room Feature kind. | `Feature.MONSTER_ROOM = register("monster_room", new MonsterRoomFeature(...))` (Feature.java:101), registered as a configured feature at CaveFeatures.java:62/133. `StructureType` registers exactly sixteen ids (StructureType.java:24-39) and `dungeon` is not among them | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| desert_well is likewise a Feature kind, not a StructureType. | `Feature.DESERT_WELL = register("desert_well", new DesertWellFeature(...))` (Feature.java:73), implemented in DesertWellFeature.java; absent from the sixteen `StructureType` registrations (StructureType.java:24-39) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's StructurePiece base class provides generateBox, generateAirBox, generateMaybeBox, and fillColumnDown as shared primitives every hand-coded piece composes from (06-structures.md section 3.4). | `StructurePiece.generateAirBox` (StructurePiece.java:206-216), four `generateBox` overloads (:218-244, :246-255, :257-280, :282-291), `generateMaybeBox` (:293-324) and `fillColumnDown` (:380-389); `SwampHutPiece.postProcess` (SwampHutPiece.java:57-70) and `DesertPyramidPiece` are exactly such composing callers | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| generateMaybeBox draws exactly one next_float() per position — taken first in its condition chain, before any skip-air or interior check — and places the block when the draw is less than or equal to the given probability; maybeGenerateBlock is the separate primitive that uses the strict less-than comparison. | One draw per position is right, the comparison is not. `StructurePiece.generateMaybeBox` (StructurePiece.java:293-324) tests `!(random.nextFloat() > probability)` - i.e. it places when the draw is LESS THAN OR EQUAL TO the probability, and the draw is taken first in the `&&` chain so it happens for every position regardless of `skipAir`/`hasToBeInside`. Only `maybeGenerateBlock` (:326-337) uses the strict `random.nextFloat() < probability` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| fillColumnDown fills straight down from a given position with a state for as long as the block there is replaceable by structures (air, any liquid, glow lichen, seagrass, or tall seagrass — not simply non-air), stopping once the cursor reaches the level's own minimum-Y-plus-one floor; it takes no caller-supplied minimum-Y parameter, and does nothing at all when the starting position falls outside the chunk bounding box. | Both the stop condition and the floor differ. `StructurePiece.fillColumnDown` (StructurePiece.java:380-389) starts at the piece-local position mapped through `getWorldPos`, does nothing at all unless `chunkBB.isInside(pos)`, and loops while `isReplaceableByStructures(level.getBlockState(pos))` - which is air OR any liquid OR glow lichen OR seagrass OR tall seagrass (:391-393), not "non-air" - and while `pos.getY() > level.getMinY() + 1`. The floor is the level's own `getMinY() + 1`, not a caller-supplied `min_y` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla distinguishes the MOTION_BLOCKING_NO_LEAVES heightmap (used for ground-height averaging) from WORLD_SURFACE_WG/OCEAN_FLOOR_WG (used where a family specifically needs one or the other). | `ScatteredFeaturePiece.updateAverageGroundHeight` and `updateHeightPositionToLowestGroundHeight` both sample `Heightmap.Types.MOTION_BLOCKING_NO_LEAVES` (ScatteredFeaturePiece.java:63, :90); `Structure.onTopOfChunkCenter`/`getCornerHeights` take `WORLD_SURFACE_WG` (Structure.java:121-129, :154-165); `BuriedTreasureStructure` and `OceanRuinStructure` take `OCEAN_FLOOR_WG` (BuriedTreasureStructure.java:20, OceanRuinStructure.java:41), and `ShipwreckStructure` picks between the two on `isBeached` (ShipwreckStructure.java:28) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| updateAverageGroundHeight averages the heightmap over every column in a footprint using an integer-truncating average (matching vanilla's own int accumulator/divide). | `ScatteredFeaturePiece.updateAverageGroundHeight` (ScatteredFeaturePiece.java:50-76) accumulates `int total` and `int count` over the piece bounding box's Z-outer, X-inner column loop and assigns `this.heightPosition = total / count` - integer division. Two details for the blueprint: only columns satisfying `chunkBB.isInside(pos)` are counted, and the method returns false (deferring placement) when `count == 0` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| updateHeightPositionToLowestGroundHeight takes the minimum MOTION_BLOCKING_NO_LEAVES sample over a footprint, but woodland mansion and end city instead call a separate, deprecated helper, Structure::get_lowest_y_in_5x5_box_offset_7_blocks, which offsets to the chunk's own (7,7) column, flips its two 5-block spans by rotation, and takes the minimum of four WORLD_SURFACE_WG corner samples — the two share no code. | The first half is right, the second conflates two different methods. `ScatteredFeaturePiece.updateHeightPositionToLowestGroundHeight` (ScatteredFeaturePiece.java:78-102) does take the `Math.min` of `MOTION_BLOCKING_NO_LEAVES` over the bounding-box footprint, but woodland mansion and end city instead call `Structure.getLowestYIn5by5BoxOffset7Blocks` (Structure.java:183-200, used at WoodlandMansionStructure.java:31 and EndCityStructure.java:24) - a separate deprecated helper that offsets to `chunkPos.getBlockX(7)`/`getBlockZ(7)`, flips its 5-block spans by rotation, and takes the min of four `WORLD_SURFACE_WG` corner heights. The two share no code | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A container block entity stores a LootTable resource-location tag plus a LootTableSeed i64 tag; unpacking the loot table is triggered by any container access — isEmpty, getItem, removeItem, removeItemNoUpdate, setItem, or createMenu, not only a player opening it — and clears only the LootTable key; the LootTableSeed field keeps its last value and simply stops being persisted afterward. | The tags are right, the trigger and the clearing are not. `RandomizableContainer.LOOT_TABLE_TAG = "LootTable"` and `LOOT_TABLE_SEED_TAG = "LootTableSeed"` with a `long` seed (RandomizableContainer.java:22-23, RandomizableContainerBlockEntity.java:21-22, :44-46). `unpackLootTable` is invoked from `isEmpty`, `getItem`, `removeItem`, `removeItemNoUpdate`, `setItem` AND `createMenu` (RandomizableContainerBlockEntity.java:48-95) - so ANY container access, including a hopper's, triggers it, not only a player opening it. And it clears only the table key (`this.setLootTable(null)`, RandomizableContainer.java:82); the seed field keeps its value and simply stops being written because `trySaveLootTable` returns early on a null table (:57-70) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The loot-table seed for a generation-time-placed container is drawn as rng.next_long() from the piece's own already-seeded RNG stream at the point of placement. | `StructurePiece.createChest` (StructurePiece.java:450-472) calls `chestBlockEntity.setLootTable(lootTable, random.nextLong())` at :465-466, `createDispenser` (:474-490) the same at :488, and `RandomizableContainer.setBlockEntityLootTable` (RandomizableContainer.java:42-48) likewise; every call site passes the piece's own `postProcess` `RandomSource` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Of this blueprint's families, only the thirteen ruined_portal templates carry a baked LootTable tag in their block-entity compound (and no vanilla template anywhere carries a baked LootTableSeed); igloo, ocean_ruin, and shipwreck templates carry no baked loot tag at all and instead place their chests from structure-block data markers at postProcess time, setting both the table and a freshly-drawn next_long() seed. | No vanilla template carries a baked `LootTableSeed`, and none of this blueprint's families relies on a baked `LootTable`. Scanning all 1212 `data/minecraft/structure/**.nbt` entries of the pinned server jar: 132 contain the string `LootTable`, ZERO contain `LootTableSeed`. Of this blueprint's families only the thirteen `ruined_portal/*.nbt` files carry a baked `LootTable`; `igloo/*`, `underwater_ruin/*` and `shipwreck/*` carry none - they place their chests from structure-block data markers at postProcess time, setting both table and a fresh `random.nextLong()` seed (IglooPieces.java:113-123, OceanRuinPieces.java:321-329, ShipwreckPieces.java:132-140) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Jungle temple is a SinglePieceStructure consisting of one procedurally-coded ScatteredFeaturePiece-derived piece; swamp hut is not a SinglePieceStructure at all — it extends Structure directly and implements its own find_generation_point with no sea-level gate — though its piece is likewise ScatteredFeaturePiece-derived. | True for jungle temple, false for swamp hut. `JungleTempleStructure extends SinglePieceStructure` with `super(JungleTemplePiece::new, 12, 15, settings)` (JungleTempleStructure.java:8-13), but `SwampHutStructure extends Structure` directly and overrides `findGenerationPoint` itself (SwampHutStructure.java:10-24) - it is not a `SinglePieceStructure` and therefore has no `getLowestY` sea-level gate. Both pieces are indeed `ScatteredFeaturePiece`-derived (JungleTemplePiece.java:30, SwampHutPiece.java:25) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Neither jungle temple's nor swamp hut's piece is a TemplateStructurePiece — both extend ScatteredFeaturePiece, a sibling class under StructurePiece, not a subclass of TemplateStructurePiece; in this blueprint's scope only igloo, ocean_ruin, shipwreck, and ruined_portal pieces are TemplateStructurePieces. | Neither is. `JungleTemplePiece extends ScatteredFeaturePiece` (JungleTemplePiece.java:30) and `SwampHutPiece extends ScatteredFeaturePiece` (SwampHutPiece.java:25), and `ScatteredFeaturePiece extends StructurePiece` (ScatteredFeaturePiece.java:11) - a sibling of, not a subclass of, `TemplateStructurePiece`. In this blueprint's scope only `IglooPiece`, `OceanRuinPiece`, `ShipwreckPiece` and `RuinedPortalPiece` are `TemplateStructurePiece`s | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Neither jungle temple nor swamp hut has any documented afterPlace logic beyond the template stamp itself. | Grepping `public void afterPlace` across `world/level/levelgen/structure/` yields exactly three files: the empty base `Structure.afterPlace` (Structure.java:143-152), `DesertPyramidStructure` and `WoodlandMansionStructure`. Neither `JungleTempleStructure` nor `SwampHutStructure` overrides it. Framing correction for the blueprint: there is no template stamp either - both pieces write blocks procedurally in `postProcess` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For jungle temple, swamp hut, and desert pyramid, the only draw performed at piece construction is a single next_int_bounded(4) that selects one of the four horizontal Direction values (via get_random_horizontal_direction) feeding the piece's bounding-box orientation — no Rotation is drawn and no template is ever loaded, since none of the three uses a template. | There is one `nextInt(4)` draw, but it is not a rotation and no template is ever loaded. Each piece constructor calls `getRandomHorizontalDirection(random)` (JungleTemplePiece.java:40, SwampHutPiece.java:30, DesertPyramidPiece.java:31) = `Direction.Plane.HORIZONTAL.getRandomDirection` (StructurePiece.java:79-81, Direction.java:588-590) = `Util.getRandom(this.faces, random)` = `faces[random.nextInt(4)]` (Util.java:826-828). The result is a `Direction` fed to `StructurePiece.makeBoundingBox` and `setOrientation`, which swaps width/depth for X-axis orientations (StructurePiece.java:71-77) - the world-position mapping then happens in `getWorldX`/`getWorldY`/`getWorldZ` (:136-166), not through a `Rotation` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A jungle temple or desert pyramid generation point is rejected (NoValidPoint) when the minimum of four WORLD_SURFACE_WG corner samples over the unrotated, chunk-corner-anchored footprint is below sea level, mirroring vanilla's getLowestY(width, depth) < seaLevel gate; swamp hut has no such gate at all. | The gate exists but neither its quantity nor its scope match. `SinglePieceStructure.findGenerationPoint` (SinglePieceStructure.java:23-28) returns `Optional.empty()` when `getLowestY(context, width, depth) < seaLevel`, and `Structure.getLowestY` (Structure.java:171-181) is the MINIMUM of four `WORLD_SURFACE_WG` corner samples taken at `(chunkMinX, chunkMinZ)`, `(chunkMinX, chunkMinZ+sizeZ)`, `(chunkMinX+sizeX, chunkMinZ)`, `(chunkMinX+sizeX, chunkMinZ+sizeZ)` (:154-165) - a four-corner minimum on the unrotated, chunk-corner-anchored footprint, never an average and never `MOTION_BLOCKING_NO_LEAVES`. It applies to jungle temple (12x15) and desert pyramid (21x21) only; `SwampHutStructure` has no such gate at all (SwampHutStructure.java:17-20) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| StructurePoolElement.getGroundLevelDelta() defaults to 1. | `StructurePoolElement.getGroundLevelDelta` (pools/StructurePoolElement.java:92-94) returns the literal 1; consumed at pools/JigsawPlacement.java:105, :116, :428-433 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Jungle temple, swamp hut, and desert pyramid's generation-stub position (used for the biome check and for /locate) is the chunk center (chunk_x*16+8, chunk_z*16+8), but the piece's own bounding box — where blocks are actually placed — is anchored at the chunk's minimum block corner, (chunk_x*16, chunk_z*16). | The generation-stub POSITION is the chunk centre, the FOOTPRINT is not. `Structure.onTopOfChunkCenter` (Structure.java:121-129) builds the stub at `chunkPos.getMiddleBlockX()`/`getMiddleBlockZ()` = `chunk*16 + 8`, but the piece itself is constructed at `chunkPos.getMinBlockX()`/`getMinBlockZ()` = `chunk*16` (SinglePieceStructure.java:30-33, SwampHutStructure.java:22-24), and that corner becomes the bounding box's own min-X/min-Z through `StructurePiece.makeBoundingBox` (StructurePiece.java:71-77). The stub position is used only for the biome check and for `/locate`; block placement is anchored at the chunk's minimum block corner | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Desert pyramid's afterPlace hook scatters suspicious sand after the template is placed (06-structures.md sections 3.9/5). | `DesertPyramidStructure.afterPlace` (DesertPyramidStructure.java:33-67) collects `getPotentialSuspiciousSandWorldPositions()` from every `DesertPyramidPiece`, places one guaranteed suspicious sand at `getRandomCollapsedRoofPos()`, shuffles the rest and converts a drawn subset to `Blocks.SUSPICIOUS_SAND`, reverting every remaining candidate to plain `Blocks.SAND` (:63-65). `afterPlace` runs after the pieces' own `postProcess` block writes | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Desert pyramid's afterPlace RNG is built by forking a positional factory off a fresh world-seed-keyed stream — the fork consumes one next_long() from that stream, so the factory's own seed is a scrambled derivative of the world seed, not the world seed itself — and evaluated at the center of the whole pieces-container bounding box, not the structure start position. | Both the fork seed and the position differ. DesertPyramidStructure.java:53 is `RandomSource.createThreadLocalInstance(level.getSeed()).forkPositional().at(pieces.calculateBoundingBox().getCenter())`. `createThreadLocalInstance(seed)` builds a `SingleThreadedRandomSource` (RandomSource.java:31-33), whose `forkPositional()` first CONSUMES one `nextLong()` and seeds the `LegacyPositionalRandomFactory` with it (SingleThreadedRandomSource.java:23-26) - so the factory seed is not the world seed. `at(x,y,z)` then makes `new LegacyRandomSource(Mth.getSeed(x,y,z) ^ factorySeed)` (LegacyRandomSource.java:63-68), and the position passed is the CENTRE of the whole pieces container's bounding box, not the structure start position | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Desert pyramid's afterPlace shuffles the candidate suspicious-sand positions before drawing the kept count. | DesertPyramidStructure.java:52-55: the candidates are collected into a `SortedArraySet` ordered by `Vec3i::compareTo` (:43), copied into an `ObjectArrayList`, passed to `Util.shuffle(shuffledSandPlacements, positionalRandom)` (:54), and only then is the count drawn (:55). `Util.shuffle` (Util.java:1110-1117) is a downward Fisher-Yates consuming one `nextInt(i)` for i = size down to 2 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The number of suspicious-sand positions kept by desert pyramid's afterPlace is drawn as min(candidate_count, 5 + next_int_bounded(3)) — a uniform integer of 5, 6, or 7 (never 8, since the upper bound is exclusive) — and this draw is not the first draw of the stream: a Fisher-Yates shuffle of the candidate positions consumes one next_int_bounded(i) per element first. | The upper bound is EXCLUSIVE. DesertPyramidStructure.java:55 is `Math.min(uniqueSandPlacements.size(), positionalRandom.nextInt(5, 8))`, and `RandomSource.nextInt(origin, bound)` (RandomSource.java:73-79) returns `origin + this.nextInt(bound - origin)` = `5 + nextInt(3)`, i.e. 5, 6 or 7 - never 8. It is also clamped down to the candidate count, and it is a different function from `nextIntBetweenInclusive` (:45-47), which WOULD give 5..8 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A fresh RcLegacyRandom::new(0).next_int_bounded(4) == 2 and a fresh RcLegacyRandom::new(0)'s 5 + next_int_bounded(3) == 5 (next_int_bounded(3) == 0) are isolated arithmetic facts only — they are not desert pyramid's actual suspicious-sand count at world seed 0, since the real stream is the afterPlace positional fork, with the candidate shuffle's own draws consumed ahead of the count draw. | The two arithmetic facts hold, the application to desert pyramid does not. Reproducing `LegacyRandomSource`/`BitRandomSource` exactly (LegacyRandomSource.java:31-49, BitRandomSource.java:16-34): seed 0 gives `nextInt(4) == 2`, and `nextIntBetweenInclusive(5,8) = nextInt(4) + 5 = 7` (RandomSource.java:45-47). But vanilla's count draw is `nextInt(5, 8) = 5 + nextInt(3)` (DesertPyramidStructure.java:55, RandomSource.java:73-79), which at seed 0 yields `nextInt(3) == 0` -> 5, not 7; and the stream is the positional fork of DesertPyramidStructure.java:53, never a raw `LegacyRandomSource(world_seed)` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A fresh RcLegacyRandom::new(5000).next_int_bounded(4) == 0 and a fresh RcLegacyRandom::new(5000)'s 5 + next_int_bounded(3) == 6 (next_int_bounded(3) == 1) are isolated arithmetic facts only — they are not desert pyramid's actual suspicious-sand count at world seed 5000, for the same reason as the seed-0 vector above. | The arithmetic holds - seed 5000 gives `nextInt(4) == 0` and hence `nextIntBetweenInclusive(5,8) == 5` - but the same two corrections as the previous row apply: vanilla draws `nextInt(5,8) = 5 + nextInt(3)`, which at seed 5000 gives `nextInt(3) == 1` -> 6, and the stream is the positional fork of `pieces.calculateBoundingBox().getCenter()`, not a raw `LegacyRandomSource(5000)` (DesertPyramidStructure.java:53-55, RandomSource.java:73-79) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Desert pyramid's loot table is minecraft:chests/desert_pyramid. | `DesertPyramidPiece` line 299-300 calls `createChest(level, chunkBB, random, 10 + xo, -11, 10 + zo, BuiltInLootTables.DESERT_PYRAMID)`, and `BuiltInLootTables.DESERT_PYRAMID = register("chests/desert_pyramid")` (BuiltInLootTables.java:38). Correction for Context section F, which reuses this id for the suspicious sand: the sand uses a DIFFERENT table, `BuiltInLootTables.DESERT_PYRAMID_ARCHAEOLOGY = register("archaeology/desert_pyramid")` (BuiltInLootTables.java:128), applied via `setLootTable(..., blockPos.asLong())` - a position-derived seed, not an RNG draw (DesertPyramidStructure.java:69-75) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Igloo's basement variant occurs exactly 50% of the time. | `IglooPieces.addPieces` (IglooPieces.java:54) gates the basement on `random.nextDouble() < 0.5`. Note for the blueprint: the draw is a `nextDouble` (two `next()` calls, BitRandomSource.java:54-60), not a `nextFloat` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Igloo's basement variant is connected by a fixed igloo/middle tunnel segment made of 12 stone bricks and 3 ladders. | `data/minecraft/structure/igloo/middle.nbt` in the pinned server jar parses to `size = [3,3,3]` with a block list of exactly 12 `minecraft:stone_bricks` and 3 `minecraft:ladder`. Note for the blueprint: the segment is placed `depth - 1` times, with `depth = random.nextInt(8) + 4`, so the shaft is 3 to 10 copies of that same fixed segment (IglooPieces.java:55-60), not one | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When igloo's basement roll succeeds, 5 to 12 template pieces are placed — one igloo/bottom at depth*3 blocks below, depth-1 igloo/middle pieces at i*3 blocks below for i = 0..depth-2, and one igloo/top at the surface, where depth = next_int_bounded(8) + 4 (4..11) — each additionally shifted by its own fixed offset (top (0,0,0) pivot (3,5,5); middle (2,-3,4) pivot (1,3,1); bottom (0,-3,-2) pivot (3,6,7)) before the vertical drop, not one continuous stack of exactly three pieces. | There are not three pieces and the stack is not continuous. `IglooPieces.addPieces` (IglooPieces.java:54-63) draws `depth = random.nextInt(8) + 4`, adds `igloo/bottom` at `depth * 3` blocks down, then `depth - 1` copies of `igloo/middle` at `i * 3` down for i = 0..depth-2, then `igloo/top` at 0 - 5 to 12 pieces (1 + (depth-1) + 1 = depth+1). Each piece is further shifted by a fixed per-template offset before the drop: top `(0,0,0)`, middle `(2,-3,4)`, bottom `(0,-3,-2)` (:40-42, `makePosition` :103-105), with rotation pivots `(3,5,5)`, `(1,3,1)`, `(3,6,7)` (:37-39) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Igloo's generation draws the rotation first (Rotation::all()[next_int_bounded(4)]) and only then draws the basement coin-flip as rng.next_double() < 0.5 — a next_double, consuming two next() calls, not a next_float. | Both the draw kind and the order are inverted. `IglooStructure.generatePieces` (IglooStructure.java:26-32) draws the rotation FIRST via `Rotation.getRandom(random)` (Rotation.java:112-113 -> `Util.getRandom(values(), random)` = `nextInt(4)`), and only then calls `IglooPieces.addPieces`, whose first statement is `random.nextDouble() < 0.5` (IglooPieces.java:54) - a `nextDouble`, consuming two `next()` calls, not a `nextFloat` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A fresh RcLegacyRandom::new(4096).next_float() == 0.0978928804... reproduces exactly, but it is not the draw that decides igloo's basement at that seed — vanilla's basement test is a next_double taken after the rotation's next_int_bounded(4), so a fresh stream's first next_float never occurs on igloo's actual draw sequence. | The float value reproduces exactly (`LegacyRandomSource(4096)`, `next(24) * 5.9604645E-8F` = 0.0978928804, BitRandomSource.java:49-52), but the igloo conclusion does not follow: vanilla's basement test is `nextDouble() < 0.5` taken AFTER a `nextInt(4)` rotation draw (IglooStructure.java:30, IglooPieces.java:54), so the first `nextFloat` of a fresh stream is never the value tested | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A fresh RcLegacyRandom::new(0).next_float() == 0.7309677601... reproduces exactly, but for the same reason as the seed-4096 vector above it is not the draw that decides igloo's basement at that seed. | The float value reproduces exactly (`LegacyRandomSource(0).nextFloat() == 0.7309677601`), but the same ordering correction applies: the basement roll is a `nextDouble` taken after the rotation `nextInt(4)` (IglooStructure.java:30, IglooPieces.java:54), so a fresh stream's first `nextFloat` does not decide it | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Igloo's chest loot table is minecraft:chests/igloo_chest. | `IglooPiece.handleDataMarker` (IglooPieces.java:113-123) responds to the `"chest"` data marker with `chestBlockEntity.setLootTable(BuiltInLootTables.IGLOO_CHEST, random.nextLong())`, and `BuiltInLootTables.IGLOO_CHEST = register("chests/igloo_chest")` (BuiltInLootTables.java:41) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Igloo's three template locations are the literal resource locations minecraft:igloo/top, minecraft:igloo/middle, and minecraft:igloo/bottom. | IglooPieces.java:34-36 - `STRUCTURE_LOCATION_IGLOO = Identifier.withDefaultNamespace("igloo/top")`, `STRUCTURE_LOCATION_LADDER = "igloo/middle"`, `STRUCTURE_LOCATION_LABORATORY = "igloo/bottom"`; all three files exist in the jar at `data/minecraft/structure/igloo/{top,middle,bottom}.nbt` with sizes [7,5,8], [3,3,3] and [7,6,9] | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean ruin's structure.extra carries a biome_temp field with values "warm"/"cold", plus large_probability and cluster_probability fields, both f32 (06-structures.md section 7). | `OceanRuinStructure.CODEC` (OceanRuinStructure.java:17-25) declares `biome_temp` via `OceanRuinStructure.Type.CODEC` (enum WARM = "warm", COLD = "cold", :55-76) plus `Codec.floatRange(0.0F, 1.0F)` fields `large_probability` and `cluster_probability`; `ocean_ruin_cold.json` and `ocean_ruin_warm.json` carry exactly those three keys | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean ruin's large-ruin probability is 0.3. | `data/minecraft/worldgen/structure/ocean_ruin_cold.json` and `ocean_ruin_warm.json` both set `"large_probability": 0.3`. Note the comparison in `OceanRuinPieces.addPieces` (OceanRuinPieces.java:162) is `random.nextFloat() <= structure.largeProbability` - less-than-OR-EQUAL, not strict | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean ruin's cluster probability (given a large ruin) is 0.9. | Both ocean ruin structure json files set `"cluster_probability": 0.9`, tested at OceanRuinPieces.java:165 as `isLarge && random.nextFloat() <= structure.clusterProbability` - again less-than-or-equal, and only reachable when the large roll succeeded | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean ruin's cluster size, when clustering occurs, is 4 to 8 additional small ruins. | `int ruins = Mth.nextInt(random, 4, 8)` (OceanRuinPieces.java:185), and `Mth.nextInt(random, min, max)` is inclusive on both ends (Mth.java:149-151). Note for the blueprint: the loop runs that many times but adds a piece only when the candidate list is non-empty AND the satellite box does not intersect the parent box (:187-197), and in a COLD biome each added "ruin" is three overlaid pieces (:236-245), so the placed-piece count is not simply 4..8 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean ruin's generation draws, in order: a rotation draw, an is-large float draw (next_float() <= large_probability), the main template's uniform pick draw(s), then — only when large, via && short-circuit — a cluster float draw (next_float() <= cluster_probability); when clustering, the eight candidate satellite positions are built first (two next_int draws per candidate, sixteen total), then a cluster-count draw (4 to 8, inclusive both ends), then per satellite one index-removal draw, one rotation draw, and (only if its box does not intersect the parent's) its own template pick — never an angle/distance pair. | The real order is different at every step. `OceanRuinStructure.generatePieces` (OceanRuinStructure.java:44-48) draws the ROTATION first (`Rotation.getRandom` = one `nextInt(4)`), then `OceanRuinPieces.addPieces` draws `nextFloat() <= largeProbability` (:162), then the template pick - one `nextInt(8)` or `nextInt(4)` for warm, one shared `nextInt(bricks.length)` for cold (:146-152, :236-245) - then `nextFloat() <= clusterProbability` (:165). Inside `addClusterRuins` the eight CANDIDATE POSITIONS are built first, costing 16 `Mth.nextInt` draws (two per candidate, `allPositions` :201-212), and only then is `Mth.nextInt(random, 4, 8)` drawn (:185); each satellite iteration then costs one `random.nextInt(allPositions.size())`, one `Rotation.getRandom`, and its own template pick (:188-196) - there is no angle/distance pair anywhere | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean ruin's satellite ruins are drawn from a fixed list of eight candidate offsets from the main ruin's bottom-left corner (X shifts -16,-16,-16,0,0,+16,+16,+16; Z shifts +16,0,-16,+16,-16,+16,0,-16), each further jittered by a small per-candidate next_int range (1..8, 1..7, 4..8, 1..7, 4..6, 3..8, 1..7, 4..8) and skipped when its 6x7 footprint box intersects the main ruin's 16x16 footprint box — never a radius band. | There is no radius band at all. `OceanRuinPieces.allPositions` (OceanRuinPieces.java:201-212) builds a FIXED list of eight candidate offsets from the parent piece's bottom-left corner, each a hardcoded -16 / 0 / +16 block shift plus a small `Mth.nextInt` jitter, e.g. `origin.offset(-16 + Mth.nextInt(random, 1, 8), 0, 16 + Mth.nextInt(random, 1, 7))`; the per-axis jitter ranges are 1..8, 1..7, 3..8 and 4..8 depending on the entry. Satellites are then drawn by removing a random index from that list (:188-189), and any whose 5x6 box intersects the parent's 15x15 box is skipped (:191-196) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A fresh RcLegacyRandom::new(4096)'s next_float() sequence, [0.09789288, 0.87547785, 0.78668922, 0.32294023], reproduces exactly, but none of these four floats is ocean ruin's actual large/cluster decision at that seed as drawn: the stream's first draw is the rotation's next_int_bounded(4), the large test's next_float() sits after it, and the template-pick draws sit between the large test and the cluster test. | The four float values reproduce exactly (`LegacyRandomSource(4096)`, `next(24) * 5.9604645E-8F`: 0.0978928804, 0.8754778504, 0.7866892219, 0.3229402304). The semantic mapping is wrong: the first draw off the structure's stream is the rotation `nextInt(4)` (OceanRuinStructure.java:46), so the large test is not the first `nextFloat`; and between the large test and the cluster test sit the template-pick draws (OceanRuinPieces.java:162-166) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A fresh RcLegacyRandom::new(2048).next_float() == 0.91443032... reproduces exactly and does fail the <= 0.3 large test (selecting a small ruin with the cluster branch never entered), but the rotation's next_int_bounded(4) is consumed before this next_float, not after it. | The value reproduces exactly (0.9144303203), and 0.9144 fails the `<= 0.3` large test, but the ordering claim is inverted: the rotation `nextInt(4)` is consumed BEFORE the large-probability `nextFloat`, not after (OceanRuinStructure.java:46 then OceanRuinPieces.java:162) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean ruin's loot tables are minecraft:chests/underwater_ruin_small and minecraft:chests/underwater_ruin_big. | `OceanRuinPiece.handleDataMarker` (OceanRuinPieces.java:321-329) sets `this.isLarge ? BuiltInLootTables.UNDERWATER_RUIN_BIG : BuiltInLootTables.UNDERWATER_RUIN_SMALL`, registered as `chests/underwater_ruin_big` and `chests/underwater_ruin_small` (BuiltInLootTables.java:43-44). Separately the suspicious-block processors use `archaeology/ocean_ruin_warm` and `archaeology/ocean_ruin_cold` (OceanRuinPieces.java:55-60, BuiltInLootTables.java:131-132) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Two Structure entries, shipwreck and shipwreck_beached, share one StructureSet (06-structures.md sections 5/7). | `data/minecraft/worldgen/structure_set/shipwrecks.json` lists `minecraft:shipwreck` and `minecraft:shipwreck_beached`, each weight 1, under one `random_spread` placement; the two structure json files differ only in `is_beached` false/true and in their biome tag | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Only shipwreck's vertical placement is terrain-driven (sunk per the surrounding heightmap); its orientation is a plain uniform rotation draw with Mirror fixed to NONE and a constant pivot of (4,0,15), reading no terrain at all. | Only the sinking is terrain-driven. The rotation is a plain uniform draw, `Rotation.getRandom(context.random())` (ShipwreckStructure.java:33), with `Mirror.NONE` fixed and a constant pivot `(4,0,15)` (ShipwreckPieces.java:33, :124-130) - nothing about the orientation reads terrain. The VERTICAL placement is terrain-driven: `ShipwreckPiece.postProcess` (ShipwreckPieces.java:142-177) averages `OCEAN_FLOOR_WG` (or `WORLD_SURFACE_WG` when beached) over the whole template footprint and calls `adjustPositionHeight` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Shipwreck's generation draws a rotation before the weighted template pick. | The ordering is right: `ShipwreckStructure.generatePieces` (ShipwreckStructure.java:32-37) draws `Rotation.getRandom(context.random())` at :33 and only then calls `ShipwreckPieces.addRandomPiece`, whose `Util.getRandom(...)` draw is next (ShipwreckPieces.java:84). Correction for the blueprint: that pick is uniform (`array[random.nextInt(array.length)]`, Util.java:826-828) over 11 beached or 20 ocean ids, not weighted | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Shipwreck's vertical placement averages the heightmap (OCEAN_FLOOR_WG normally, WORLD_SURFACE_WG when beached) over every column of the template's footprint and tracks the minimum sampled height; a non-beached wreck uses that mean unchanged with no embed subtraction, while a beached wreck's Y is minY - template.size.y/2 - next_int_bounded(3) — the constant 3 appears only as that draw's exclusive upper bound, not as a fixed subtraction. | Neither the sample nor the offset matches. `ShipwreckPiece.postProcess` (ShipwreckPieces.java:142-177) walks EVERY column of the template footprint, accumulating `mean` and tracking `minY`, then divides by `size.x * size.z` - an integer mean over the whole footprint, not a centre sample - and for a non-beached wreck uses that mean UNCHANGED, with no embed subtraction. Only the beached branch offsets, via `calculateBeachedPosition` (:184-186) = `minY - template.getSize().getY() / 2 - random.nextInt(3)`, sampling `WORLD_SURFACE_WG`. The constant 3 appears only as that `nextInt(3)` bound | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Buried treasure is placed at the fixed chunk-local block position (9, ?, 9), matching the corpus's own locate_offset=[9,0,9] constant. | `BuriedTreasureStructure.generatePieces` (BuriedTreasureStructure.java:23-26) builds the piece at `new BlockPos(chunkPos.getBlockX(9), 90, chunkPos.getBlockZ(9))`, and `buried_treasures.json` carries `"locate_offset": [9, 0, 9]` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Buried treasure's Y position is resolved from the ocean-floor heightmap. | `BuriedTreasureStructure.findGenerationPoint` uses `onTopOfChunkCenter(context, Heightmap.Types.OCEAN_FLOOR_WG, ...)` (BuriedTreasureStructure.java:20), and the piece re-samples `level.getHeight(Heightmap.Types.OCEAN_FLOOR_WG, minX, minZ)` at postProcess time (BuriedTreasurePieces.java:47). Note the piece then walks DOWN from that Y until the block below is sandstone, stone, andesite, granite or diorite (:50-57) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Buried treasure's postProcess writes backfill blocks around the chosen position — for each of the six Direction neighbours that is air, water, or lava, either the solid below-block (when that neighbour's own below-block is also air/liquid and the direction is not UP) or a "soft" state (the current block, or sand when that is air/liquid) — before calling createChest; the structure's own generation step is underground_structures, not the FEATURES stage. | There is surrounding geometry. `BuriedTreasurePiece.postProcess` (BuriedTreasurePieces.java:60-72) iterates all six `Direction.values()` around the chosen position and, for every neighbour that is air or water or lava, WRITES a block there at flag 3 - the solid block found below when that neighbour's own below-block is also air or liquid and the direction is not UP, otherwise a "soft" state that is the current block or, when that is air or liquid, `Blocks.SAND`. Only after that backfill is `createChest` called (:75). The structure's `step` is `underground_structures`, not the FEATURES stage | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Buried treasure's generation point requires zero RNG draws, since its position is a fixed chunk-local offset. | `BuriedTreasureStructure.findGenerationPoint`/`generatePieces` (BuriedTreasureStructure.java:18-26) contain no `context.random()` reference at all; the only draw on the whole family's path is the chest's `random.nextLong()` loot seed inside `createChest` at postProcess time (StructurePiece.java:465-466) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Buried treasure's loot table is minecraft:chests/buried_treasure. | `BuriedTreasurePiece.postProcess` calls `createChest(level, chunkBB, random, pos, BuiltInLootTables.BURIED_TREASURE, null)` (BuriedTreasurePieces.java:75), and `BuiltInLootTables.BURIED_TREASURE = register("chests/buried_treasure")` (BuiltInLootTables.java:45) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's structure.extra["setups"] is a weighted list of Setup records, each independently configuring a VerticalPlacement mode, air_pocket_probability, mossiness, overgrown, vines, can_be_cold, replace_with_blackstone, and a float weight — eight fields in total. | Two keys are missing from the list. `RuinedPortalStructure.Setup.CODEC` (RuinedPortalStructure.java:255-267) has EIGHT fields: `placement`, `air_pocket_probability`, `mossiness`, `overgrown`, `vines`, `can_be_cold`, `replace_with_blackstone` (a bool driving `BlackstoneReplaceProcessor`, RuinedPortalPiece.java:144-146) and `weight` (`ExtraCodecs.POSITIVE_FLOAT`, so a float weight, not an integer). Every `ruined_portal*.json` setup object carries all eight. The list itself is `ExtraCodecs.nonEmptyList(Setup.CODEC.listOf())` under key `setups` (:52-55) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's Standard setup is two equally-weighted placements, underground and on_land_surface; its Mountain setup is two equally-weighted placements, in_mountain and on_land_surface (no underground entry). Both use a single mossiness value of 0.2 (never a range), with air_pocket_probability 1.0 on the underground/in_mountain placement and 0.5 on the on_land_surface placement. | Right for standard, wrong for mountain, and the mossiness range is wrong for both. `ruined_portal.json` has two weight-0.5 setups, `underground` and `on_land_surface`; `ruined_portal_mountain.json` has two weight-0.5 setups, `in_mountain` and `on_land_surface` - no `underground` entry at all. Mossiness is the single value 0.2 in all four setups, never a 0.2-0.5 range. Air pockets: `air_pocket_probability` 1.0 for the underground / in_mountain setup and 0.5 for the on_land_surface one | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's Desert biome family setup places the portal partly buried, with mossiness 0 and no air pockets. | `ruined_portal_desert.json` has one setup, weight 1.0: `"placement": "partly_buried"`, `"mossiness": 0.0`, `"air_pocket_probability": 0.0` (and `can_be_cold` false, `vines` false, `overgrown` false, `replace_with_blackstone` false). `RuinedPortalStructure.sample` short-circuits a 0.0 probability with no RNG draw (RuinedPortalStructure.java:161-167) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's Jungle biome family setup places the portal on-surface, with mossiness 0.8, overgrown, and vines. | `ruined_portal_jungle.json`, one setup, weight 1.0: `"placement": "on_land_surface"`, `"mossiness": 0.8`, `"overgrown": true`, `"vines": true`, `"air_pocket_probability": 0.5`, `can_be_cold` false | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's Swamp setup places the portal on_ocean_floor (not on-surface), with mossiness 0.5 and vines; the ocean-floor placement also switches its Y search to OCEAN_FLOOR_WG and forces an unconditional lava-to-magma reskin rule. | The placement is wrong. `ruined_portal_swamp.json`'s single setup is `"placement": "on_ocean_floor"`, not on-surface; `"mossiness": 0.5` and `"vines": true` are right, with `air_pocket_probability` 0.0, `can_be_cold` false, `overgrown` false. `on_ocean_floor` also switches the Y search and the lava rule: `OCEAN_FLOOR_WG` heightmap (RuinedPortalPiece.java:296-298) and an unconditional lava -> magma replacement (:151-153) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's Ocean biome family setup places the portal on the ocean floor, with mossiness 0.8 (high moss). | `ruined_portal_ocean.json`, one setup, weight 1.0: `"placement": "on_ocean_floor"`, `"mossiness": 0.8`, `"air_pocket_probability": 0.0`, `"can_be_cold": true` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's Nether biome family setup varies its vertical placement, has mossiness 0, and reskins to blackstone; the same RuinedPortalStructure Java class serves both the Overworld and Nether dimensions. | `ruined_portal_nether.json`, one setup: `"placement": "in_nether"`, `"mossiness": 0.0`, `"replace_with_blackstone": true` (adding `BlackstoneReplaceProcessor.INSTANCE`, RuinedPortalPiece.java:144-146); the same `RuinedPortalStructure`/`StructureType.RUINED_PORTAL` serves all seven entries (StructureType.java:35). "Varies" is accurate about the resulting Y rather than the mode: the `IN_NETHER` branch of `findSuitableY` picks `randomBetweenInclusive(32,100)` with an air pocket, else a 50/50 between `randomBetweenInclusive(27,29)` and `randomBetweenInclusive(29,100)` (RuinedPortalStructure.java:186-193) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's giant-portal chance is 5%, independent of which Setup is chosen (06-structures.md section 5's own PROBABILITY_OF_GIANT_PORTAL constant). | `RuinedPortalStructure.PROBABILITY_OF_GIANT_PORTAL = 0.05F` (RuinedPortalStructure.java:49), and the branch at :97 is the bare `random.nextFloat() < 0.05F` with no reference to the chosen setup | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal draws its template from 10 normal designs or, when the giant roll succeeds, 3 giant designs. | `STRUCTURE_LOCATION_PORTALS` holds ten ids, `ruined_portal/portal_1` .. `portal_10` (RuinedPortalStructure.java:34-45); `STRUCTURE_LOCATION_GIANT_PORTALS` holds three, `giant_portal_1` .. `giant_portal_3` (:46-48); the pick is `array[random.nextInt(array.length)]` on the matching array (:97-101). All thirteen `.nbt` files exist in the pinned jar | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's obsidian-to-crying-obsidian reskin chance is 15%. | `BlockAgeProcessor.maybeReplaceObsidian` (templatesystem/BlockAgeProcessor.java:96-98) returns crying obsidian when `random.nextFloat() < 0.15F`. Note it lives in `BlockAgeProcessor` - the mossiness processor added at RuinedPortalPiece.java:141 - not in the `RuleProcessor` list the generator assembles, so the 15% applies wherever a `BlockAgeProcessor` runs, independent of the setup's mossiness value | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal has no graded netherrack-replace rule. A cold setup adds an unconditional lava-to-netherrack rule; a non-cold setup instead adds a netherrack-to-magma rule at a flat 7% (PROBABILITY_OF_MAGMA_INSTEAD_OF_NETHERRACK). Separately, postProcess's own netherrack spread draws one next_int_bounded(max(1, 8 - average_width/2)) distance offset up front and then, per position, a next_double ramp test against {1,1,1,1,1,1,1,0.9,0.9,0.8,0.7,0.6,0.4,0.2}; its drip-column descent (capped at 8 extra blocks) is a separate 0.5 roll per block. | No such graded rule exists. The cold flag selects between two distinct rules: when cold, `getBlockReplaceRule(Blocks.LAVA, Blocks.NETHERRACK)` - an UNCONDITIONAL lava-to-netherrack replacement (RuinedPortalPiece.java:151-156); when not cold, a `getBlockReplaceRule(Blocks.NETHERRACK, 0.07F, Blocks.MAGMA_BLOCK)` rule is ADDED (:131-133), i.e. netherrack is replaced by magma 7% of the time. The related `PROBABILITY_OF_MAGMA_INSTEAD_OF_NETHERRACK = 0.07F` constant is at :53, and the postProcess netherrack spread uses its own 0.5 rolls (:211, :232) with a distance ramp `{1,1,1,1,1,1,1,0.9,0.9,0.8,0.7,0.6,0.4,0.2}` (:245) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's lava-to-magma reskin chance is 20% in a normal setup and 100% on the ocean floor. | `RuinedPortalPiece.getLavaProcessorRule` (RuinedPortalPiece.java:151-157): `ON_OCEAN_FLOOR` yields the unconditional `getBlockReplaceRule(Blocks.LAVA, Blocks.MAGMA_BLOCK)`, otherwise a non-cold setup yields `getBlockReplaceRule(Blocks.LAVA, 0.2F, Blocks.MAGMA_BLOCK)`; the constant `PROBABILITY_OF_MAGMA_INSTEAD_OF_LAVA = 0.2F` is at :54. Note the third branch: a COLD non-ocean setup replaces lava with netherrack unconditionally instead | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's generation draws, in order: the Setup's own weighted-pick float (only when more than one Setup is configured), an air-pocket sample (a next_float unless the Setup's air_pocket_probability is exactly 0.0 or 1.0, in which case no draw is taken), the is-giant float draw, a template-index draw, a rotation draw, a mirror draw (next_float() < 0.5 choosing Mirror::None or Mirror::FrontBack), and finally whatever find_suitable_y itself consumes; the cold-biome check is not part of this sequence at all — it is a zero-RNG biome.coldEnoughToSnow test evaluated later, inside the generation stub's own builder. | The order is different and two draws are missing. `RuinedPortalStructure.findGenerationPoint` draws, in order: (1) one `random.nextFloat()` for the weighted Setup pick, but ONLY when `setups.size() > 1` (:70-88); (2) the air-pocket `sample(random, setup.airPocketProbability())`, one `nextFloat` unless the probability is exactly 0.0F or 1.0F, in which case no draw is taken (:95, :161-167); (3) the giant `random.nextFloat() < 0.05F` (:97); (4) the template index `random.nextInt(3 or 10)` (:98-100); (5) the rotation `Util.getRandom(Rotation.values(), random)` = `nextInt(4)` (:104); (6) a MIRROR draw `random.nextFloat() < 0.5F` choosing NONE or FRONT_BACK (:105); (7) whatever `findSuitableY` consumes (:117). The cold test is also not a tag check and does not run here - it is evaluated later, inside the stub's builder lambda, as `biome.value().coldEnoughToSnow(pos, seaLevel)` (:126-135, :169-171) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's find_suitable_y for the Underground placement mode draws its own start via get_random_within_interval(min_y, surface_y_at_center - y_span) — min_y is height_accessor.min_y + 15 (for example -49 on a -64-floor overworld, not a literal 15), the upper bound is surface_y_at_center minus the piece's own Y span (never ground_y - ground_y/2), and the draw is unconditional Mth.randomBetweenInclusive only when min_y < max, otherwise the interval collapses to max with no draw at all. | Both bounds are wrong and the start is itself a draw. `RuinedPortalStructure.findSuitableY` (RuinedPortalStructure.java:197-200) computes the UNDERGROUND start as `getRandomWithinInterval(random, minY, surfaceYAtCenter - ySpan)` where `minY = heightAccessor.getMinY() + 15` (:184) - i.e. -49 in a standard overworld, not 15 - and `getRandomWithinInterval` (:236-238) is `Mth.randomBetweenInclusive(random, minPreferred, max)` when `minPreferred < max`, else `max`, so the start consumes RNG. The upper bound is `surfaceYAtCenter - ySpan` (the bounding box's own Y span), never `ground_y - ground_y/2`; `surfaceYAtCenter` itself is `getBaseHeight(center, heightmapFor(placement)) - 1` (:113-116) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's find_suitable_y for the Mountain placement mode draws via get_random_within_interval(70, surface_y_at_center - y_span) — 70 is a preferred lower bound of that draw, not a fixed search start, and the upper bound is surface_y_at_center minus the piece's own Y span, never ground_y - ground_y/2. | 70 is a preferred LOWER bound of a random draw, not a start, and the upper bound is different. RuinedPortalStructure.java:194-196: `IN_MOUNTAIN` uses `getRandomWithinInterval(random, 70, surfaceYAtCenter - ySpan)` = `Mth.randomBetweenInclusive(random, 70, max)` when `70 < max`, else exactly `max` (:236-238) - an RNG-consuming draw between 70 and `surfaceYAtCenter - ySpan`, never `ground_y - ground_y/2` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's find_suitable_y for the PartlyBuried placement mode is surface_y_at_center minus the piece's own Y span, plus a single next_int_between_inclusive(2, 8) draw — but this is far from the only RNG find_suitable_y performs: InNether draws next_int_between_inclusive(32, 100), or (on a next_float() < 0.5 branch) next_int_between_inclusive(27, 29) or (29, 100); InMountain and Underground each draw through get_random_within_interval; only OnLandSurface and OnOceanFloor consume no RNG at all. | The 2..8 draw is right, the base and the exclusivity are not. RuinedPortalStructure.java:200-201: `PARTLY_BURIED` gives `surfaceYAtCenter - ySpan + Mth.randomBetweenInclusive(random, 2, 8)` - the base is `surfaceYAtCenter - ySpan`, not `ground_y - ground_y/2`. And it is far from the only draw: `IN_NETHER` draws `randomBetweenInclusive(32,100)`, or a `nextFloat() < 0.5F` plus `randomBetweenInclusive(27,29)` or `randomBetweenInclusive(29,100)` (:186-193); `IN_MOUNTAIN` and `UNDERGROUND` each draw through `getRandomWithinInterval` (:194-199). Only `ON_LAND_SURFACE`/`ON_OCEAN_FLOOR` consume nothing (:202-204) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ruined portal's find_suitable_y walks downward one block at a time until at least 3 of the footprint's 4 horizontal corners rest on a non-air block, or its own lower search bound is reached. | RuinedPortalStructure.java:206-233: the four bottom corners of the rotated bounding box are turned into `NoiseColumn`s, then `for (projectedY = newY; projectedY > minY; projectedY--)` counts corners whose column block at that Y satisfies the placement's heightmap predicate, returning as soon as `++cornersOnSolidGround == 3`; falling out of the loop returns the bound itself, never a panic. Two precisions: the test is `heightmap.isOpaque().test(blockState)` with `OCEAN_FLOOR_WG` for `ON_OCEAN_FLOOR` and `WORLD_SURFACE_WG` otherwise (:215-217), and it inspects the block AT that Y rather than the block below it; the lower bound is `heightAccessor.getMinY() + 15` (:184) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| RcLegacyRandom::new(4640).next_float() == 0.049160659..., which is less than 0.05, selecting the giant-portal branch at that seed. | The value reproduces exactly (`LegacyRandomSource(4640)`, `next(24) * 5.9604645E-8F` = 0.0491606593), and 0.04916 < `PROBABILITY_OF_GIANT_PORTAL` 0.05F (RuinedPortalStructure.java:49, :97). The giant roll is genuinely the FIRST stream draw only for a single-setup entry whose `air_pocket_probability` is exactly 0.0 or 1.0 - `ruined_portal_desert`, `ruined_portal_ocean`, `ruined_portal_swamp`; for `ruined_portal` and `ruined_portal_mountain` (two setups, one at 0.5) it is the third draw, and for `ruined_portal_jungle`/`_nether` the second | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| RcLegacyRandom::new(0).next_float() == 0.7309677601..., which is at least 0.05, selecting the normal-portal branch at that seed. | The value reproduces exactly (`LegacyRandomSource(0).nextFloat() == 0.7309677601`), and 0.7310 fails `< 0.05F` (RuinedPortalStructure.java:97), so the ten-design normal array is used (:100). The same first-draw caveat as the preceding row applies: only the single-setup, 0.0-or-1.0-air-pocket entries put the giant roll first on the stream | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Fortress is a random_spread structure (the shared nether_complexes set) that is Nether-only. | `structure_set/nether_complexes.json` is `minecraft:random_spread` (spacing 27, separation 4, salt 30084232) holding `minecraft:fortress` at weight 2 and `minecraft:bastion_remnant` at weight 3; `structure/fortress.json` is typed `minecraft:fortress` with `biomes: #minecraft:has_structure/nether_fortress`, and that tag resolves to `#minecraft:is_nether` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| end_city is a random_spread structure that is End-only. | `structure_set/end_cities.json` is `minecraft:random_spread` with `spread_type: triangular` (spacing 20, separation 11, salt 10387313); `structure/end_city.json` uses `biomes: #minecraft:has_structure/end_city`, whose tag lists exactly `minecraft:end_highlands` and `minecraft:end_midlands` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| nether_fossil is a random_spread structure (near-continuous grid) that is Nether-only, and is unrelated to the separate fossil Feature kind (an overworld/underground decorative dinosaur-bone feature placed through the ordinary Feature/decoration pipeline) despite the shared name. | `structure_set/nether_fossils.json` is `random_spread` with `spacing: 2`, `separation: 1`, salt 14357921 - a near-continuous grid; `structure/nether_fossil.json` is typed `minecraft:nether_fossil` with `biomes: #minecraft:has_structure/nether_fossil` resolving to `minecraft:soul_sand_valley` only, `step: underground_decoration`. The unrelated feature is `Feature.FOSSIL = register("fossil", new FossilFeature(FossilFeatureConfiguration.CODEC))` (feature/Feature.java:74), a `Feature` and not a `StructureType` (StructureType.java:24-39) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Mineshaft is placed via a degenerate random_spread structure set and generated as an eager corridor/crossing/room random walk. | `structure_set/mineshafts.json` is `random_spread` with `spacing: 1`, `separation: 0`, `salt: 0`, `frequency: 0.004`, `frequency_reduction_method: legacy_type_3`, holding `minecraft:mineshaft` and `minecraft:mineshaft_mesa`. `MineshaftStructure.findGenerationPoint` (MineshaftStructure.java:35-43) builds the whole piece list eagerly inside the generation point and returns `Either.right(builder)`: `generatePiecesAndAdjust` (:45-65) seeds a `MineShaftRoom` and calls `addChildren`, which recursively emits corridors and crossings through `MineshaftPieces.generateAndAddPiece` (MineshaftPieces.java:82-108, corridor children at :174-360) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Stronghold is placed via concentric_rings placement and generated via a weighted piece-graph pick (a genuine cumulative-weight walk) with a portal-room-required retry; its expansion loop removes a randomly-indexed pending child each step (one next_int_bounded(pending.len()) draw per step) — a random-order traversal, not a breadth-first, FIFO sweep. | Placement, weighting and the retry are right; the traversal is not breadth-first. `structure_set/strongholds.json` is `minecraft:concentric_rings` (count 128, distance 32, spread 3, `preferred_biomes: #minecraft:stronghold_biased_to`); the piece choice is a genuine weighted walk, `random.nextInt(totalWeight)` over the `PieceWeight` table (StrongholdPieces.java:61-96, :168); and `StrongholdStructure.generatePieces` retries the entire layout with `setLargeFeatureSeed(seed + tries++, ...)` while the builder is empty or `startRoom.portalRoomPiece` is still null (StrongholdStructure.java:23-44). But the expansion loop removes a RANDOM pending child each step - `int pos = context.random().nextInt(pendingChildren.size()); pendingChildren.remove(pos)` (:36-40) - a random-order traversal that consumes one RNG draw per step, not a FIFO breadth-first sweep | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean monument is placed via random_spread placement and generated as a procedural, non-template room grid. | `structure_set/ocean_monuments.json` is `random_spread` with `spread_type: triangular` (spacing 32, separation 5, salt 10387313). `OceanMonumentPieces.java` contains ZERO references to `StructureTemplate`: `MonumentBuilding.generateRoomGraph` builds a `RoomDefinition` lattice which seven `MonumentRoomFitter` implementations then fill (OceanMonumentPieces.java:33-192, :193-210), and every block is written with `generateBox`/`placeBlock` (e.g. :398, :428-432). `OceanMonumentStructure.findGenerationPoint` additionally rejects the point unless every biome within 29 blocks is in `BiomeTags.REQUIRED_OCEAN_MONUMENT_SURROUNDING` (OceanMonumentStructure.java:29-42) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Woodland mansion is placed via random_spread placement and generated via a procedural grid/corridor layout with template-stamped rooms. | `structure_set/woodland_mansions.json` is `random_spread` with `spread_type: triangular` (spacing 80, separation 20, salt 10387319). `WoodlandMansionPieces.MansionGrid` lays out three 11x11 `SimpleGrid` floors plus a third-floor grid and carves corridors with `recursiveCorridor` (WoodlandMansionPieces.java:106-161, :187-216, :307), then `MansionPiecePlacer` stamps rooms from `StructureTemplateManager` (:412-441, :480-481). The start point comes from the deprecated `getLowestYIn5by5BoxOffset7Blocks` (WoodlandMansionStructure.java:31) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's piece placement clips every generated box to the piece's own chunk bounding box, skipping positions outside it (06-structures.md section 3.4). | `StructurePiece.placeBlock` (StructurePiece.java:167-190) maps the piece-local coordinate through `getWorldPos` and does nothing unless `chunkBB.isInside(pos)`; `getBlock` (:196-199) likewise returns air for a position outside it, and `createChest`/`createDispenser` (:450-472, :474-490) and `fillColumnDown` (:380-389) each carry the same guard. The box argument is the CHUNK bounding box handed to `postProcess`, not the piece's own box | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's generateBox has a walls-versus-interior overload that fills a box's six faces (walls, floor, ceiling) with one block state and everything strictly inside with a separate interior state (06-structures.md section 3.4). | `StructurePiece.generateBox(level, chunkBB, x0, y0, z0, x1, y1, z1, edgeBlock, fillBlock, skipAir)` (StructurePiece.java:218-244) places `fillBlock` when `y != y0 && y != y1 && x != x0 && x != x1 && z != z0 && z != z1` and `edgeBlock` otherwise - strictly-inside versus the six faces. A `BoundingBox`-taking convenience overload delegates to it (:246-255) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's generateMaybeBox iterates Y-outer, X-middle, Z-inner — a corpus-confirmed order, not a placeholder — and every sibling box primitive (generateAirBox, every generateBox overload, generateUpperHalfSphere) nests the same way; since one next_float is consumed per visited position, this blueprint's own fill_box_probabilistic must use the identical Y-outer/X-middle/Z-inner nesting, or its draw-to-position mapping breaks bit-identical parity for any non-cubic box. | The order is confirmable and it is not the blueprint's. `StructurePiece.generateMaybeBox` (StructurePiece.java:293-324) nests `for (int y = y0; y <= y1; y++)` then `for (int x = x0; x <= x1; x++)` then `for (int z = z0; z <= z1; z++)` - Y-outer, X-MIDDLE, Z-INNER. Every sibling primitive uses the same nesting: `generateAirBox` (:206-216), all four `generateBox` overloads (:218-291) and `generateUpperHalfSphere` (:339-378). Since one `nextFloat` is consumed per visited position, the blueprint's Z-middle/X-inner choice permutes the draw-to-position mapping and breaks bit-identical parity for any non-cubic box | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Igloo's vertical drop between each stacked template is a fixed 3-block step (igloo/bottom at depth*3 below, each igloo/middle at i*3 below), never the loaded template's own size.y — each piece is additionally shifted by its own fixed offset (top (0,0,0), middle (2,-3,4), bottom (0,-3,-2)) before that drop, and re-based at postProcess by (WORLD_SURFACE_WG height at the entrance column) − 90 − 1. | Vanilla uses fixed constants, not template heights. `IglooPieces.addPieces` (IglooPieces.java:54-63) drops `igloo/bottom` by `depth * 3` and the i-th `igloo/middle` by `i * 3` - a fixed 3-block step - and `makePosition` (:103-105) first applies the per-template `OFFSETS` `top (0,0,0)`, `middle (2,-3,4)`, `bottom (0,-3,-2)` (:40-42). The template sizes read from the pinned jar are top [7,5,8], middle [3,3,3], bottom [7,6,9], so `size.y` matches the 3-block step only by coincidence for the middle segment and is wrong for top and bottom. Each piece is additionally re-based at postProcess by `height - 90 - 1` against `WORLD_SURFACE_WG` at the entrance column (:138-142) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Buried treasure's generation writes nothing below the chest position — the downward walk already stops once the block below is solid (sandstone, stone, andesite, granite, or diorite) — but writes up to six neighbour blocks: for each of the six Direction neighbours that is air, water, or lava, either the solid below-block (when that neighbour's own below-block is also air/liquid and the direction is not UP) or a soft state that is sand only when the position's own current state is air or liquid. | Nothing is written below the chest - the block below is already solid, since the downward walk stops precisely when it is sandstone, stone, andesite, granite or diorite (BuriedTreasurePieces.java:50-57). What is written is up to six NEIGHBOUR blocks: for each of the six `Direction.values()` whose neighbour is air, water or lava, vanilla writes either that solid below-block (when the neighbour's own below-block is also air or liquid and the direction is not UP) or a "soft" state that is `Blocks.SAND` only when the position's current state is air or liquid (:58-72) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Swamp huts have a StructureSpawnOverride that naturally spawns a witch inside the generated hut (06-structures.md section 3.10). | `data/minecraft/worldgen/structure/swamp_hut.json` declares `spawn_overrides` with a `monster` entry, `bounding_box: piece`, spawning `minecraft:witch` (min 1, max 1, weight 1) - plus a `creature` entry spawning `minecraft:cat` on the same piece box. `SwampHutPiece` also tracks one-off `spawnedWitch`/`spawnedCat` flags in its own NBT (SwampHutPiece.java:26-27, :35-43) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
