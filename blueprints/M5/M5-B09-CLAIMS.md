# M5-B09 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| The pinned target's real ChunkStatus ladder has 12 rungs in this exact ascending order: empty, structure_starts, structure_references, biomes, noise, surface, carvers, features, initialize_light, light, spawn, full. | `ChunkStatus` registration chain (ChunkStatus.java:21-32) declares exactly twelve statuses, each with the preceding one as its parent, in the order empty, structure_starts, structure_references, biomes, noise, surface, carvers, features, initialize_light, light, spawn, full; `ChunkPyramid.GENERATION_PYRAMID` (ChunkPyramid.java:11-42) builds one step per status in that same order, and `ChunkStep.Builder(status, parent)` (ChunkStep.java:62-70) throws unless each step's index is exactly its predecessor's plus one | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID (addRequirement(status, radius)) assigns the structure_starts rung no neighbor-chunk requirement at all. | ChunkPyramid.java:13 — `.step(ChunkStatus.STRUCTURE_STARTS, s -> s.setTask(ChunkStatusTasks::generateStructureStarts))`, no `addRequirement` call. Nuance worth recording: `ChunkStep.Builder(status, parent)` (ChunkStep.java:62-70) seeds every non-first step with `directDependenciesByRadius = {parent.targetStatus}`, i.e. an implicit radius-0 (same-chunk, not neighbor) dependency on `empty`; the accumulated table (ChunkStep.java:111-132) additionally inherits the parent step's own radii | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the structure_references rung to have structure_starts completed at radius 8 (an up to 17x17 chunk neighborhood). | ChunkPyramid.java:14 — `.step(ChunkStatus.STRUCTURE_REFERENCES, s -> s.addRequirement(ChunkStatus.STRUCTURE_STARTS, 8).setTask(...))`; `addRequirement` (ChunkStep.java:72-89) fills the direct-dependency table to length `radius + 1` = 9, i.e. Chebyshev radius 8 = a 17x17 chunk square, and `ChunkStatus.MAX_STRUCTURE_DISTANCE = 8` (ChunkStatus.java:16) is the same constant | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the biomes rung to have structure_starts completed at radius 8. | ChunkPyramid.java:15 — `.step(ChunkStatus.BIOMES, s -> s.addRequirement(ChunkStatus.STRUCTURE_STARTS, 8).setTask(ChunkStatusTasks::generateBiomes))` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the noise rung to have structure_starts completed at radius 8. | ChunkPyramid.java:16-22 — the NOISE step's first builder call is `.addRequirement(ChunkStatus.STRUCTURE_STARTS, 8)` (:18) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the noise rung to have biomes completed at radius 1. | ChunkPyramid.java:19 — `.addRequirement(ChunkStatus.BIOMES, 1)` inside the NOISE step (:16-22), which also sets `blockStateWriteRadius(0)` (:20) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the surface rung to have structure_starts completed at radius 8. | ChunkPyramid.java:23-29 — the SURFACE step's `.addRequirement(ChunkStatus.STRUCTURE_STARTS, 8)` (:25) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the surface rung to have biomes completed at radius 1. | ChunkPyramid.java:26 — `.addRequirement(ChunkStatus.BIOMES, 1)` inside the SURFACE step (:23-29), which also sets `blockStateWriteRadius(0)` (:27) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the carvers rung to have structure_starts completed at radius 8. | ChunkPyramid.java:30 — `.step(ChunkStatus.CARVERS, s -> s.addRequirement(ChunkStatus.STRUCTURE_STARTS, 8).blockStateWriteRadius(0).setTask(ChunkStatusTasks::generateCarvers))` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the features rung to have structure_starts completed at radius 8. | ChunkPyramid.java:31-37 — the FEATURES step's `.addRequirement(ChunkStatus.STRUCTURE_STARTS, 8)` (:33) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the features rung to have carvers completed at radius 1. | ChunkPyramid.java:34 — `.addRequirement(ChunkStatus.CARVERS, 1)` inside the FEATURES step (:31-37); it is the only step in the generation pyramid whose `blockStateWriteRadius` is non-zero (`1`, :35) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the light rung to have initialize_light completed at radius 1. | ChunkPyramid.java:39 — `.step(ChunkStatus.LIGHT, s -> s.addRequirement(ChunkStatus.INITIALIZE_LIGHT, 1).setTask(ChunkStatusTasks::light))`; the loading pyramid repeats the same requirement (:53) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID requires the spawn rung to have biomes completed at radius 1. | ChunkPyramid.java:40 — `.step(ChunkStatus.SPAWN, s -> s.addRequirement(ChunkStatus.BIOMES, 1).setTask(ChunkStatusTasks::generateSpawn))` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID assigns the initialize_light rung no neighbor-chunk requirement at all. | ChunkPyramid.java:38 — `.step(ChunkStatus.INITIALIZE_LIGHT, s -> s.setTask(ChunkStatusTasks::initializeLight))`, no `addRequirement` call; only the implicit radius-0 dependency on its parent status `features` that `ChunkStep.Builder` seeds (ChunkStep.java:62-70) applies | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkPyramid.GENERATION_PYRAMID assigns the full rung no neighbor-chunk requirement at all. | ChunkPyramid.java:41 — `.step(ChunkStatus.FULL, s -> s.setTask(ChunkStatusTasks::full))`, no `addRequirement` call; only the implicit radius-0 dependency on its parent status `spawn` (ChunkStep.java:62-70) applies. Note that the FULL step's ACCUMULATED dependency list is still non-trivial (it is what `SAFETY_MARGIN_CHUNKS`, ChunkPyramid.java:57, and `ChunkLevel` :38 size themselves against), inherited from earlier rungs rather than assigned here | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's own FEATURES-status write margin is a 3x3-chunk window: a chunk's features rung reads and writes live block state across its own 3x3 neighborhood. | ChunkPyramid.java:35 sets `blockStateWriteRadius(1)` for the FEATURES step (the only non-zero write radius in the pyramid); `WorldGenRegion` copies it into `this.writeRadius` (WorldGenRegion.java:95) and `isWithinWriteZone` (:267-269) admits exactly `abs(dx) <= 1 && abs(dz) <= 1`, i.e. the 3x3 chunk square, with `ensureCanWrite` (:271-294) refusing `setBlock` (:296-303) outside it. Reads go through `getBlockState` (:157-162), which calls `warnIfReadOutsideWriteZone` (:237-259) and so flags any read beyond that same radius-1 window as unsafe | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In vanilla, one chunk's feature-placement outcome can depend on another overlapping chunk's already-placed blocks within their shared decoration window, since feature placement reads and writes live cross-chunk block state. | `ChunkStatusTasks.generateFeatures` (ChunkStatusTasks.java:135-150) builds one `WorldGenRegion` over the shared `StaticCache2D<GenerationChunkHolder>` and runs `applyBiomeDecoration` (ChunkGenerator.java:318-...) against it. `WorldGenRegion.setBlock` (WorldGenRegion.java:296-320) writes into `this.getChunk(pos)` — the real, shared neighbor `ChunkAccess` instance, anywhere in the radius-1 write zone — and `getBlockState` (:157-162) reads from that same shared instance, so a block another chunk's already-run features rung wrote is visible to this one. The radius-1 `CARVERS` requirement on the FEATURES step (ChunkPyramid.java:34) exists exactly to make those neighbors real | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's noise router exposes six climate axes as density-function fields named temperature, vegetation, continents, erosion, depth, and ridges. | `NoiseRouter` record components (NoiseRouter.java:12-17) are `temperature, vegetation, continents, erosion, depth, ridges`, all `DensityFunction`, in that order within the 15-component record; the codec (:30-35) serialises them under the JSON keys `temperature`, `vegetation`, `continents`, `erosion`, `depth`, `ridges`, and `RandomState` reads exactly those six to build the climate sampler (RandomState.java:105-110) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Climate.Sampler samples climate by wrapping the same FlatCache-marked density-function graph nodes the noise router itself marks for caching. | True for the sampler used at the biomes rung: `NoiseBasedChunkGenerator.createBiomes` (NoiseBasedChunkGenerator.java:95) calls `noiseChunk.cachedClimateSampler(randomState.router(), settings.spawnTarget())`, and `NoiseChunk.cachedClimateSampler` (NoiseChunk.java:169-179) maps each of the six router functions through `this::wrap`, whose `wrapNew` (NoiseChunk.java:378-407) turns a `Marker.FlatCache` node into a real memoizing `NoiseChunk.FlatCache` (and Interpolated/Cache2D/CacheOnce/CacheAllInCell into their own cache nodes). Nuance the blueprint should record: a SECOND `Climate.Sampler` exists on `RandomState` (RandomState.java:88-112) built through a `noiseFlattener` visitor that does the opposite — it STRIPS every `DensityFunctions.Marker` back to `marker.wrapped()` and unwraps `HolderHolder` — so the RandomState sampler is uncached and evaluates per point via `SinglePointContext` (Climate.java:497-510); only the NoiseChunk-built one caches | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The fixed axis order for one climate sample is temperature, humidity, continentalness, erosion, depth, weirdness. | `Climate.Sampler.sample` (Climate.java:497-510) computes and passes the six values in exactly that order to `Climate.target` (:35-46), whose parameter list is the same order, producing `Climate.TargetPoint(temperature, humidity, continentalness, erosion, depth, weirdness)` (:574-581). The sampler's own record components (:485-493) are in that order too, and it converts quart to block coordinates with `QuartPos.toBlock` (:498-500) before evaluating. Naming nuance: these six sampler slots are fed from the router fields named temperature, vegetation, continents, erosion, depth, ridges (RandomState.java:105-110) — vegetation maps to humidity, continents to continentalness, ridges to weirdness | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's spawnOriginalMobs (initial mob population performed at chunk-generation time) seeds via setDecorationSeed, which fully overwrites the fresh unique-seed RNG's state with a value derived from the world seed and the chunk's coordinates, so the mob-placement stream is reproducible from the world seed alone; only each entity's UUID and private RNG are non-reproducible. | The seeding shape is right, the determinism conclusion is wrong. `NoiseBasedChunkGenerator.spawnOriginalMobs` (NoiseBasedChunkGenerator.java:493-501) does build `new WorldgenRandom(new LegacyRandomSource(RandomSupport.generateUniqueSeed()))` and then call `random.setDecorationSeed(worldGenRegion.getSeed(), center.getMinBlockX(), center.getMinBlockZ())` — but `WorldgenRandom.setDecorationSeed` (WorldgenRandom.java:44-51) BEGINS with `this.setSeed(seed)`, which forwards to `LegacyRandomSource.setSeed` (LegacyRandomSource.java:32-38) and replaces the entire 48-bit state and resets the gaussian cache, discarding the unique seed completely. That stream is therefore a pure function of (world seed, chunk min block X, chunk min block Z). `worldGenRegion.getSeed()` is the world seed (WorldGenRegion.java:88, 405-407). The second stream, `level.getRandom()` used by `SpawnPlacements.checkSpawnRules` (NaturalSpawner.java:392-394) and by every reachable `finalizeSpawn` override (Mob.java:1083-1095, AgeableMob.java:46-62, Sheep/Horse/Panda/Rabbit/Fox/AbstractHorse), is likewise world-seed derived: `WorldGenRegion.getRandom()` returns a positional random taken at the center chunk's world position (WorldGenRegion.java:89, 426-427; RandomState.java:36, 119-121). So the chunk-generation mob population — species, counts, positions, group/baby data, variant rolls — is reproducible from the world seed alone; the only non-reproducible parts are per-entity, the UUID (Entity.java:284, over `Entity.random`) and the entity's private RNG (Entity.java:251 -> RandomSource.java:14-16, `generateUniqueSeed`). The call is also gated on `settings.disableMobGeneration()` (NoiseBasedChunkGenerator.java:494) and the SPAWN_MOBS gamerule (NaturalSpawner.java:367-433) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's initial mob population performed at chunk-generation time (spawnOriginalMobs) uses a structurally different algorithm from vanilla's tick-time natural mob spawning system. | `NaturalSpawner.spawnMobsForChunkGeneration` (NaturalSpawner.java:362-...) handles only `MobCategory.CREATURE` (:366), loops `while (random.nextFloat() < mobSettings.getCreatureProbability())` (:371), picks one `SpawnerData` by weight, draws a pack size `minCount + nextInt(1 + maxCount - minCount)` (:375), an in-chunk origin `xo + nextInt(16)` / `zo + nextInt(16)` (:377-378) and then random-walks it by `nextInt(5) - nextInt(5)` per placement attempt (up to 4 attempts per pack member, :385-422), all from the decoration-seeded `WorldgenRandom`. The tick-time path is `spawnForChunk` (:118-131) into `spawnCategoryForChunk` (:133-145) and `spawnCategoryForPosition` (:151-...): it iterates every eligible `MobCategory` under per-category mob-cap accounting (`state::canSpawn` / `state::afterSpawn`, :126), starts from `getRandomPosWithin(level, chunk)` (:140, :339), and gates each placement on `isRightDistanceToPlayerAndSpawnPoint` (:185, :228) — no mob cap and no player-distance gate exist on the chunk-generation path | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
