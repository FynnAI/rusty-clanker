# M5-B06 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Vanilla's chunk-generation stage order is Empty -> StructureStarts -> StructureReferences -> Biomes -> Noise -> Surface -> Carvers -> Features -> ..., i.e. the Carvers stage runs strictly after Surface and strictly before Features. | `ChunkStatus` static registration chain (ChunkStatus.java:21-32): `EMPTY` -> `STRUCTURE_STARTS` -> `STRUCTURE_REFERENCES` -> `BIOMES` -> `NOISE` -> `SURFACE` -> `CARVERS` -> `FEATURES` -> `INITIALIZE_LIGHT` -> `LIGHT` -> `SPAWN` -> `FULL`; each `register` call names its predecessor, so `CARVERS`'s parent is `SURFACE` and `FEATURES`'s parent is `CARVERS` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla 26.2 has no `carving_mask` placement modifier: `PlacementModifierType` registers 15 modifier types, none a carving-mask filter, and `PlacementContext.getCarvingMask(ChunkPos)` exists but has no caller anywhere in the source, so the Features stage does not consult the Carvers stage's output through any placement modifier. | No `carving_mask` placement modifier exists in 26.2. `PlacementModifierType` (PlacementModifierType.java:8-24) registers exactly 15 types and none is a carving-mask filter; `PlacementContext.getCarvingMask(ChunkPos)` (PlacementContext.java:30-32) exists but a whole-source grep for `getCarvingMask` finds no caller anywhere. The only surviving `carving_mask` occurrences are the chunk NBT key (SerializableChunkData.java:128, :473) and the datafixer CarvingStepRemoveFix.java:27. Correction: the Features stage does not consult the carving mask through any placement modifier in 26.2 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `applyCarvers`'s neighborhood is a fixed, hardcoded 17x17 chunk block (dx, dz each ranging -8..=8) around the target chunk, i.e. 289 candidate source chunks; the reach radius is a fixed engine constant, never a per-carver JSON value. | `NoiseBasedChunkGenerator.applyCarvers` (NoiseBasedChunkGenerator.java:317, :326-327): `int range = 8;` and the literal nested loops `for (int dx = -8; dx <= 8; dx++)` / `for (int dz = -8; dz <= 8; dz++)`, i.e. 17x17 = 289 source chunks, hardcoded in the generator and read from no configuration. (`WorldCarver.getRange()` at WorldCarver.java:58-60 returns 4, but it feeds the per-tunnel walk length at CaveWorldCarver.java:35 and CanyonWorldCarver.java:34, never the neighborhood bound) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The 289-source-chunk scan iterates dx outer / dz inner, matching vanilla's own nested-loop order. | NoiseBasedChunkGenerator.java:326-327 - the loop nesting is `for (int dx = -8; dx <= 8; dx++)` OUTER and `for (int dz = -8; dz <= 8; dz++)` INNER. Correction: dx is the outer axis and dz the inner axis, the reverse of the claim | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The carver list consulted for each source chunk is that source (neighbor) chunk's own biome's own carver list, in JSON/registry declaration order - never globally sorted the way vanilla's FeatureSorter sorts the Features stage's list. | NoiseBasedChunkGenerator.java:330-339 - `carverCenterChunk.carverBiome(...)` yields the SOURCE chunk's `BiomeGenerationSettings`, and `sourceBiomeGenerationSettings.getCarvers()` (BiomeGenerationSettings.java:55-57) returns the `HolderSet<ConfiguredWorldCarver<?>>` field unchanged, iterated with a plain for-each; no sort, comparator or index-unification step exists on this path | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The per-source-chunk carver index resets to 0 for every one of the 289 source chunks - never a running total across the whole neighborhood scan. | NoiseBasedChunkGenerator.java:337 - `int index = 0;` is declared inside the per-source-chunk body (inside both dx and dz loops), immediately before the per-carver loop, and is incremented only at :346 within that loop, so it restarts at 0 for every source chunk | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The seed passed to a carver's seed derivation is world_seed.wrapping_add(carver_index), and this sum is passed AS the `seed` parameter of the large-feature-seed derivation itself, not added to the result after derivation. | NoiseBasedChunkGenerator.java:341 - `random.setLargeFeatureSeed(seed + index, sourcePos.x(), sourcePos.z())`; `WorldgenRandom.setLargeFeatureSeed` (WorldgenRandom.java:58-64) does `setSeed(seed); a = nextLong(); b = nextLong(); setSeed(chunkX*a ^ chunkZ*b ^ seed)`, so the `seed + index` sum is the `seed` parameter itself and reappears in the final XOR | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A carver's start-chunk acceptance roll (`is_start_chunk`) is `random.next_float() <= config.probability`, exactly one RNG draw. | `CaveWorldCarver.isStartChunk` (CaveWorldCarver.java:21-23) and `CanyonWorldCarver.isStartChunk` (CanyonWorldCarver.java:20-22) both return `random.nextFloat() <= configuration.probability`. Correction: the comparison is less-than-OR-EQUAL, not strictly less-than; the single-draw part is correct | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Three `WorldCarver` types are registered in vanilla 26.2's `BuiltInRegistries.CARVER`: `CaveWorldCarver` (registry id `cave`), `CanyonWorldCarver` (registry id `canyon`), and `NetherWorldCarver` (registry id `nether_cave`, dispatched as its own carver type "minecraft:nether_cave") — `NetherWorldCarver` overrides `getCaveBound` (10), `getThickness` ((next_float()*2+next_float())*2, no widen roll), `getYScale` (5.0), and `carveBlock` entirely (writes lava at or below min_gen_y+31 and cave_air above, never consulting the aquifer), so it is not a config-only variant of `CaveWorldCarver`. | WorldCarver.java:32-34 registers THREE carvers in `BuiltInRegistries.CARVER`: `cave` (`CaveWorldCarver`), `nether_cave` (`NetherWorldCarver`) and `canyon` (`CanyonWorldCarver`), so `nether_cave` is its own registry id and dispatches as type `minecraft:nether_cave`, not `minecraft:cave` (confirmed by datagen configured_carver/nether_cave.json `"type": "minecraft:nether_cave"`). `NetherWorldCarver` is also not algorithm-identical: it overrides `getCaveBound()` to 10 (NetherWorldCarver.java:24-26), `getThickness` to `(nextFloat()*2+nextFloat())*2` (:28-31), `getYScale()` to 5.0 (:33-35) and `carveBlock` entirely, writing LAVA at or below `minGenY+31` and CAVE_AIR above without consulting the aquifer (:37-58) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla 26.2 has exactly four configured carver instances: cave and cave_extra_underground (carver_type "minecraft:cave"), nether_cave (carver_type "minecraft:nether_cave"), and canyon (carver_type "minecraft:canyon"). | datagen/generated/data/minecraft/worldgen/configured_carver/ contains exactly four files - cave.json, cave_extra_underground.json, nether_cave.json, canyon.json - so the count of four is right. Correction: their `type` values are `minecraft:cave`, `minecraft:cave`, `minecraft:nether_cave` and `minecraft:canyon`; only two of the four, not three, carry type `minecraft:cave` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's `canReach`-style early-exit prune (bailing a tunnel walk out early once it can no longer possibly reach the target chunk) terminates the tunnel's entire remaining walk via a bare `return`, and is not output-identical to omitting it: `canReach`'s circular bound (radius sqrt(remaining^2 + (thickness+18)^2)) and `carveEllipsoid`'s own square reach guard (half-width 16 + 2*horizontal_radius) are not nested in one direction for every parameter — for large thickness `canReach` is the more permissive of the two — but the decisive point stands regardless: since a walk advances at most one block per step, a pruned path could still have turned back toward the chunk, so later positions `carveEllipsoid` would have accepted are lost, plus, for the cave carver, any later splitPoint fork and both of its child tunnels. | `WorldCarver.canReach` (WorldCarver.java:208-218) is consumed at CaveWorldCarver.java:211-213 and CanyonWorldCarver.java:106-108 as `if (!canReach(...)) return;` - it terminates the whole remaining walk, not just one write. Correction: it is not a pure clip of out-of-range writes. `carveEllipsoid`'s own guard (WorldCarver.java:79-80) rejects a stamp only when `abs(x-chunkMiddleX) > 16.0 + horizontalRadius*2.0`, a strictly looser bound than canReach's `xd*xd + zd*zd - remaining*remaining <= (thickness + 2 + 16)^2`, so a walk canReach prunes can contain later positions carveEllipsoid would still accept; for the cave carver the prune also suppresses the `currentStep == splitPoint` fork at CaveWorldCarver.java:164 and every child tunnel it would have spawned. Omitting canReach is therefore not established as output-identical | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `applyCarvers` constructs one `WorldgenRandom` wrapping a fresh `LegacyRandomSource(RandomSupport.generateUniqueSeed())`, reused across the whole 289-source-chunk by per-carver-list scan for one target chunk, reseeded per (source_chunk, carver) pair via the large-feature-seed derivation. | NoiseBasedChunkGenerator.java:316 - `WorldgenRandom random = new WorldgenRandom(new LegacyRandomSource(RandomSupport.generateUniqueSeed()));` declared once before the dx/dz loops, and reseeded at :341 inside the innermost per-carver loop via `setLargeFeatureSeed(seed + index, sourcePos.x(), sourcePos.z())`; the same instance is then passed to `carver.carve(..., random, ...)` at :343 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The carver RNG carrier is always the Legacy random family, for every dimension including the overworld, even though the overworld's own terrain noise is Xoroshiro-backed. | NoiseBasedChunkGenerator.java:316 - the construction is unconditional in `applyCarvers` with no dimension or noise-settings branch anywhere in the method (:304-351); the generator's Xoroshiro-backed `RandomState` is used only for the biome sampler at :314 and :333, never for the carver carrier | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Both `CaveWorldCarver` and `CanyonWorldCarver` use `Mth.sin`/`Mth.cos` (a 65536-entry lookup table) for their heading trigonometry, never `Math.sin`/`Math.cos`. | `CaveWorldCarver.createTunnel` uses `Mth.sin`/`Mth.cos` at CaveWorldCarver.java:151, :153-156; `CanyonWorldCarver.doCarve` at CanyonWorldCarver.java:89, :93-97; `CanyonWorldCarver.updateVerticalRadius` uses `Mth.abs` at :147 and `CaveWorldCarver.createRoom` `Mth.sin` at :118. A grep for `Math.sin` or `Math.cos` across the whole carver package returns nothing (only `Math.PI` constants appear) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `Mth.sin`/`Mth.cos`'s lookup table has exactly 65536 entries (SIN_TABLE_SIZE = 65536, SIN_MASK = 65535). | `Mth.java:31-38` - `SIN_QUANTIZATION = 65536`, `SIN_MASK = 65535`, and `SIN = Util.make(new float[65536], ...)`. Numbers and shape match; vanilla's own field name for the size is `SIN_QUANTIZATION`, not `SIN_TABLE_SIZE` (the blueprint's constant name is its own) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `Mth.cos` reads the same table as `Mth.sin` at a quarter-turn offset of 16384 table units (COS_OFFSET = 16384). | Mth.java:33 declares `COS_OFFSET = 16384`, and `Mth.cos` (Mth.java:57-59) reads the same `SIN` array with `+ 16384.0` added to the scaled angle before the cast and mask | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The table-index scale constant is SIN_SCALE = 10430.378350470453, equal to 65536 / (2*PI). | Mth.java:34 - `private static final double SIN_SCALE = 10430.378350470453;` and 65536 / (2*PI) = 65536 / 6.283185307179586 = 10430.378350470453 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The table is built once, lazily, as table[i] = (Math.sin(i as f64 / SIN_SCALE)) as f32 for i in 0..65536 - the only place vanilla's carver code calls a real f64 sin, at table-construction time only. | Mth.java:35-38 - `SIN = Util.make(new float[65536], sin -> { for (int i = 0; i < sin.length; i++) sin[i] = (float)Math.sin(i / 10430.378350470453); })`, a static-initializer (class-init-time) build that is the single real `Math.sin` call reachable from carver code; the carver package itself contains no `Math.sin`/`Math.cos` call at all | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `Mth.sin(angle)` computes table[ ((angle * SIN_SCALE) as i64 & SIN_MASK) ], with the f64-to-i64 cast truncating toward zero before the mask is applied. | `Mth.sin(double)` (Mth.java:53-55): `return SIN[(int)((long)(i * 10430.378350470453) & 65535L)];` - the `(long)` cast truncates toward zero, the mask is applied after the cast, and an `(int)` narrowing follows | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `Mth.cos(angle)` computes table[ (((angle * SIN_SCALE + 16384.0) as i64) & SIN_MASK) ]. | `Mth.cos(double)` (Mth.java:57-59): `return SIN[(int)((long)(i * 10430.378350470453 + 16384.0) & 65535L)];` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `VerticalAnchor` resolves against a (min_y, height) context with exactly 3 variants: Absolute(y) resolves to y; AboveBottom(n) resolves to min_y + n; BelowTop(n) resolves to min_y + height - 1 - n. | `VerticalAnchor` (VerticalAnchor.java) declares exactly the three records `Absolute`, `AboveBottom`, `BelowTop`. `Absolute.resolveY` returns `this.y` (:76-79); `AboveBottom.resolveY` returns `heightAccessor.getMinGenY() + this.offset` (:56-59); `BelowTop.resolveY` returns `heightAccessor.getGenDepth() - 1 + heightAccessor.getMinGenY() - this.offset` (:96-99), algebraically the claim's `min_y + height - 1 - n`. The context is `WorldGenerationContext` with exactly `getMinGenY()`/`getGenDepth()` (WorldGenerationContext.java:6-20) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's `HeightProvider` has exactly 6 variants: Constant, Uniform, BiasedToBottom, VeryBiasedToBottom, Trapezoid, and WeightedList. | `HeightProviderType` (HeightProviderType.java:8-13) registers exactly six: `constant`, `uniform`, `biased_to_bottom`, `very_biased_to_bottom`, `trapezoid`, `weighted_list`, matching the six classes in the heightproviders package | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A Constant `HeightProvider` resolves its inner `VerticalAnchor` with zero RNG draws. | `ConstantHeight.sample` (ConstantHeight.java:24-27) returns `this.value.resolveY(context)` and never touches the `random` parameter | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A Uniform `HeightProvider` (min, max) resolves both endpoints via `VerticalAnchor`, then draws exactly one inclusive-both-ends random integer in [lo, hi] via a single next_int_range(lo, hi + 1) call. | `UniformHeight.sample` (UniformHeight.java:36-49) resolves both anchors then returns `Mth.randomBetweenInclusive(random, min, max)`, which is `random.nextInt(maxInclusive - min + 1) + min` (Mth.java:693-695) - one draw, both ends inclusive. Gate worth carrying into the blueprint: when `min > max` it logs and returns `min` with ZERO draws | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A WeightedList `HeightProvider` draws exactly one next_int_bounded(total_weight) roll to select among its weighted entries (plus whatever draws the chosen entry's own sampling needs), walking entries in order and selecting the first whose weight exceeds the running roll. | `WeightedListHeight.sample` (WeightedListHeight.java:20-23) is `distribution.getRandomOrThrow(random).sample(random, heightAccessor)`; `WeightedList.getRandomOrThrow` (WeightedList.java:85-92) draws exactly one `random.nextInt(this.totalWeight)` and hands it to the selector, whose `Compact.get` (WeightedList.java:172-183) walks entries in declaration order subtracting each weight and returns the first entry for which the running value goes negative | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's own call for a carver's per-biome carver list is `sourceBiomeGenerationSettings.getCarvers()`, evaluated at a representative point of the source (neighbor) chunk, resolved as a pure function of coordinates rather than a read of the neighbor's own already-generated block data. | NoiseBasedChunkGenerator.java:330-336 - `carverCenterChunk.carverBiome(() -> this.getBiomeGenerationSettings(this.biomeSource.getNoiseBiome(QuartPos.fromBlock(sourcePos.getMinBlockX()), 0, QuartPos.fromBlock(sourcePos.getMinBlockZ()), randomState.sampler())))` then `sourceBiomeGenerationSettings.getCarvers()` at :336. This also settles the blueprint's open [C-LOW]: the representative point is the source chunk's MINIMUM block corner converted to quart coordinates, at quart Y = 0. Note `ChunkAccess.carverBiome` (ChunkAccess.java:424-430) memoizes the result on the neighbor chunk object, so vanilla reads/writes a cached field there even though the value itself is a pure function of coordinates | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| There is no `GenerationStep.Carving` enum in vanilla 26.2 at all — `GenerationStep` declares only `Decoration`, and `BiomeGenerationSettings` stores carvers as a single flat `HolderSet<ConfiguredWorldCarver<?>>` under the JSON key `carvers`, with no per-step (air/liquid) dimension; the datafixer `CarvingStepRemoveFix` migrates the old chunk NBT `CarvingMasks.AIR` compound to a flat `carving_mask` key. | There is no `GenerationStep.Carving` enum in 26.2 at all: `GenerationStep` (GenerationStep.java) declares only `Decoration` with its 11 constants, and `BiomeGenerationSettings` (BiomeGenerationSettings.java:31, :36, :55-57) stores carvers as a single flat `HolderSet<ConfiguredWorldCarver<?>>` under the JSON key `carvers` with no per-step dimension. `CarvingStepRemoveFix` (CarvingStepRemoveFix.java:21-32) migrates the old chunk NBT `CarvingMasks.AIR` to a flat `carving_mask`. Correction: the air/liquid step split is removed from the engine, not merely unused by the shipped carvers, so "all four are air-step" is not a statement 26.2 can make | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A block position is carvable (replaceable by a carve) iff it is a member of the configured carver's own `replaceable` tag-or-list field. | `WorldCarver.canReplaceBlock` (WorldCarver.java:204-206): `return state.is(configuration.replaceable);` where `replaceable` is the `HolderSet<Block>` from `CarverConfiguration` (CarverConfiguration.java:24, :40). Called at WorldCarver.java:138 and NetherWorldCarver.java:48. One documented bypass: at WorldCarver.java:138 the check is skipped when `isDebugEnabled(configuration)` is true | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's carve-state resolution (`getCarveState`) returns Lava whenever the world Y coordinate is at or below the carver's own resolved lava_level Y, without needing to consult the aquifer for that case. | `WorldCarver.getCarveState` (WorldCarver.java:167-170): `if (blockPos.getY() <= configuration.lavaLevel.resolveY(context)) return LAVA.createLegacyBlock();` - the aquifer branch is the `else`, so it is not reached for that case | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When a carved position's world Y is above the resolved lava_level, vanilla's carve-state resolution defers to `Aquifer.computeSubstance` for the resulting fluid/air state; if the aquifer declines to override (returns no result), the position falls back to a debug-barrier state only if the carver's debug settings enable it, and is otherwise left untouched (not carved at all). | WorldCarver.java:171-176 - `BlockState state = aquifer.computeSubstance(new DensityFunction.SinglePointContext(x, y, z), 0.0); if (state == null) return isDebugEnabled(configuration) ? configuration.debugSettings.getBarrierState() : null;` and a null return from `getCarveState` makes `carveBlock` return false at :143-145 without writing. Extra gate the blueprint should note: on the non-null branch (:175) a debug-enabled carver rewrites the state through `getDebugState` (:180-189) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's `Aquifer.createDisabled(fluidPicker)` fallback performs no barrier/noise simulation at all, but is not unconditional: its `computeSubstance` returns null (no carve) whenever the density argument is greater than 0.0, and otherwise returns a fluid state supplied per position by a caller-injected `Aquifer.FluidPicker`, whose `FluidStatus.at(blockY)` returns the fluid type when `blockY < fluidLevel` and air otherwise — the Y level and fluid come from the picker, never from a constant baked into the disabled aquifer. | `Aquifer.createDisabled` (Aquifer.java:30-42): `computeSubstance` returns `density > 0.0 ? null : fluidRule.computeFluid(x, y, z).at(y)`. Correction on two points - it is not unconditional: it returns null (no carve) whenever the density argument is greater than 0.0; and the fluid is supplied per position by a caller-injected `Aquifer.FluidPicker` (Aquifer.java:48-50), whose `FluidStatus.at(blockY)` (Aquifer.java:52-59) returns `fluidType` when `blockY < fluidLevel` and `Blocks.AIR.defaultBlockState()` otherwise - so the Y level and fluid come from the picker, not from a constant baked into the disabled aquifer. The "no barrier/noise simulation" half of the claim is correct | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla specially re-runs its surface-material rule one block below any carved position whose pre-carve block state was grass_block or mycelium, firing only when that block below is dirt, so the newly-exposed dirt gets re-topped. | `WorldCarver.carveBlock` (WorldCarver.java:133-136, :152-162): the grass/mycelium observation sets `hasGrass`, and the re-top block is `helperPos.setWithOffset(blockPos, Direction.DOWN)` - one block BELOW the carved position - additionally gated on that block being `Blocks.DIRT`, after which `context.topMaterial(...)` replaces it. Correction: vanilla re-tops the block one below the carved position, not one above | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Only `NetherWorldCarver.carveBlock` ever writes cave_air (unconditionally above min_gen_y+31); the shared `WorldCarver` carve path used by the cave and canyon carvers always produces plain air for a freshly-carved solid-to-air conversion, since both aquifer implementations resolve the non-fluid case to `Blocks.AIR`. | `WorldCarver` declares both `AIR` and `CAVE_AIR` (WorldCarver.java:35-36), but the shared `carveBlock`/`getCarveState` path never writes `CAVE_AIR`: the written state is whatever `Aquifer.computeSubstance` returns (WorldCarver.java:142, :147), and both aquifer implementations return `Blocks.AIR.defaultBlockState()` for non-fluid positions (`FluidStatus.at`, Aquifer.java:56-58; `NoiseBasedAquifer.computeSubstance`, Aquifer.java:104-109). Correction: only `NetherWorldCarver.carveBlock` (NetherWorldCarver.java:52-53) writes `CAVE_AIR`, and it does so unconditionally above `minGenY+31`; the cave and canyon carvers produce plain `air` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's shared `WorldCarver.carveEllipsoid` primitive contains no radius threshold of any kind; its only early-out is the horizontal reach guard `max_delta = 16.0 + horizontal_radius * 2.0`, returning early unless both the x and z block-center offsets from the ellipsoid center are within `max_delta` — a degenerate small radius is handled implicitly by the index bounds collapsing to an empty range, not by a 0.5 test. | `WorldCarver.carveEllipsoid` (WorldCarver.java:76-119) contains no radius threshold of any kind. Correction: its only early-out is the horizontal reach guard at :79-80 - `maxDelta = 16.0 + horizontalRadius * 2.0;` and the method returns false unless both `abs(x - chunkMiddleX) <= maxDelta` and `abs(z - chunkMiddleZ) <= maxDelta`. Degenerate small radii are handled implicitly by the index bounds at :83-89 collapsing to an empty range, not by a 0.5 test | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `carveEllipsoid`'s local-chunk horizontal bounding box is computed as min_lx = clamp(floor(center_x - horizontal_radius) - target_chunk_x*16 - 1, 0, 16), max_lx = clamp(floor(center_x + horizontal_radius) - target_chunk_x*16 + 1, 0, 16), with min_lz/max_lz computed identically on the Z axis. | WorldCarver.java:81-89 - `chunkMinX = chunkPos.getMinBlockX()` (the claim's `target_chunk_x*16`), `minXIndex = Math.max(Mth.floor(x - horizontalRadius) - chunkMinX - 1, 0)`, `maxXIndex = Math.min(Mth.floor(x + horizontalRadius) - chunkMinX, 15)`, with the Z axis identical at :88-89, and the loops are inclusive (`xIndex <= maxXIndex`, :94). Vanilla's literal upper form is min-with-15 plus an inclusive loop rather than the claim's plus-1 clamped-to-16 exclusive form, but the two iterate exactly the same index set for every input, so the bounding box the claim describes is the reference's | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `carveEllipsoid`'s vertical bounding box is computed as min_y = max(floor(center_y - vertical_radius) - 1, ctx.min_y + 1), max_y = min(floor(center_y + vertical_radius) + 1, ctx.min_y + ctx.height - 1 - protected_blocks_on_top), where protected_blocks_on_top is 7 in the ordinary (non-upgrading) case and 0 while the chunk is upgrading. | WorldCarver.java:85-87 - `minY = Math.max(Mth.floor(y - verticalRadius) - 1, context.getMinGenY() + 1)` and `maxY = Math.min(Mth.floor(y + verticalRadius) + 1, context.getMinGenY() + context.getGenDepth() - 1 - protectedBlocksOnTop)` where `protectedBlocksOnTop = chunk.isUpgrading() ? 0 : 7` (:86). Correction: the lower clamp is `getMinGenY() + 1`, not `getMinGenY()`, and the upper clamp subtracts a further 7 protected blocks below the top in the ordinary (non-upgrading) case | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `carveEllipsoid`'s per-column horizontal test is x_dist*x_dist + z_dist*z_dist >= 1.0, where x_dist and z_dist are the block-center offset from the ellipsoid center divided by the horizontal radius; a column failing this test is skipped entirely. | WorldCarver.java:96, :100-101 - `xd = (worldX + 0.5 - x) / horizontalRadius`, `zd = (worldZ + 0.5 - z) / horizontalRadius`, and the body runs only under `if (!(xd * xd + zd * zd >= 1.0))`, so a column whose normalized squared horizontal distance reaches 1.0 is skipped without any Y scan | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `carveEllipsoid` scans each accepted column's Y range from the computed max_y down to, but EXCLUDING, min_y (the lowest Y actually visited is min_y + 1), and the per-position vertical normalization is y_dist = (world_y - 0.5 - center_y) / vertical_radius — MINUS 0.5, unlike the PLUS 0.5 used on the x and z terms. | WorldCarver.java:104 - `for (int worldY = maxY; worldY > minY; worldY--)`. The top-to-bottom direction is right, but the loop is EXCLUSIVE of `minY`, so the lowest Y actually visited is `minY + 1`, not `minY`. Correction while at this line: the vertical normalization is `yd = (worldY - 0.5 - y) / verticalRadius` (:105) - the block-center offset uses MINUS 0.5 on Y, unlike the PLUS 0.5 used on X and Z at :96/:100 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Within `carveEllipsoid`, the carving-mask dedup check (has this position already been carved by an earlier overlapping call) is applied before the replaceable-block check, which is applied before the carve-state fill resolution. | WorldCarver.java:106-109 - the order per position is: `skipChecker.shouldSkip(...)`, then the mask dedup `!mask.get(xIndex, worldY, zIndex)` (OR-ed with the debug bypass), then `mask.set(...)`, then `carveBlock`, which reads the block state and the grass/mycelium flag (:133-136), then `canReplaceBlock` (:138), then `getCarveState` (:142). Mask before replaceable before fill resolution, as claimed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The cave carver's per-target-chunk cave count is drawn as count = next_int_bounded(next_int_bounded(next_int_bounded(15) + 1) + 1): the outermost bound argument is evaluated first (an inner next_int_bounded(15) draw), then a middle next_int_bounded(<result>+1) draw, then the outer next_int_bounded(<result>+1) draw, giving a strongly zero-biased distribution. | `CaveWorldCarver.carve` (CaveWorldCarver.java:36): `int caveCount = random.nextInt(random.nextInt(random.nextInt(this.getCaveBound()) + 1) + 1);` with `getCaveBound()` returning 15 (:87-89); Java evaluates the innermost argument first, so the draw order is exactly as claimed. Two scoping notes for the blueprint: this is drawn once per accepted (source chunk, carver) `carve` call, not once per target chunk; and `NetherWorldCarver` overrides `getCaveBound()` to 10 (NetherWorldCarver.java:24-26), so 15 is the cave/cave_extra_underground value only | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For each cave, the cave carver draws local_x via next_int_bounded(16), then the origin Y via the height provider's own sample, then local_z via next_int_bounded(16), in that order. | CaveWorldCarver.java:39-41 - `double x = sourceChunkPos.getBlockX(random.nextInt(16)); double y = configuration.y.sample(random, context); double z = sourceChunkPos.getBlockZ(random.nextInt(16));` in that order. Gap the blueprint must close: three further per-cave draws follow immediately at :42-44 - `horizontalRadiusMultiplier.sample(random)`, `verticalRadiusMultiplier.sample(random)` and `floorLevel.sample(random)` - and the sampled `floorLevel` becomes the cave's skip predicate `yd <= floorLevel ? true : xd*xd + yd*yd + zd*zd >= 1.0` (:45, :233-235), which is not the plain sphere test the blueprint assumes | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The cave carver rolls a 1-in-4 chance (next_int_bounded(4) == 0) to carve a "room" at the cave's origin in addition to its tunnels. | CaveWorldCarver.java:47 - `if (random.nextInt(4) == 0) { ... }` guards the room branch (:48-52) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A cave carver's room first draws yScale = config.y_scale.sample(random) (an extra draw), then thickness = 1.0 + next_float() * 6.0 (a hardcoded 6.0, not a config field); the room's horizontal_radius is 1.5 + thickness (Mth.sin at a quarter turn is exactly 1.0), its vertical_radius is horizontal_radius * yScale (the sampled yScale, not a fixed 0.5), and the room's ellipsoid is centered at x + 1.0, not x. | CaveWorldCarver.java:48-50 and `createRoom` (:104-121). Correction: the room first draws `double yScale = configuration.yScale.sample(random)` (:48) - an extra draw the blueprint omits - then `float thickness = 1.0F + random.nextFloat() * 6.0F` (:49, a hardcoded 6.0, not a config field); `createRoom` computes `horizontalRadius = 1.5 + Mth.sin((float)(Math.PI/2)) * thickness` (:118, i.e. 1.5 + thickness since the table value at a quarter turn is 1) and `verticalRadius = horizontalRadius * yScale` (:119, the sampled yScale, not a fixed 0.5); the ellipsoid is centered at `x + 1.0`, not `x` (:120) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When a cave carver rolls a room, it adds next_int_bounded(4) extra tunnel branches on top of the base 1. | CaveWorldCarver.java:46, :51 - `int tunnels = 1;` and, inside the room branch, `tunnels += random.nextInt(4);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each cave tunnel's initial yaw is drawn as next_float() * 2 * PI. | CaveWorldCarver.java:55 - `float horizontalRotation = random.nextFloat() * (float)(Math.PI * 2);` (note the whole expression is float-precision, and the constant is the float cast of 2*PI) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each cave tunnel's initial pitch is drawn as (next_float() - 0.5) / 4.0. | CaveWorldCarver.java:56 - `float verticalRotation = (random.nextFloat() - 0.5F) / 4.0F;` (float arithmetic throughout) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each cave tunnel independently rolls a 1-in-6 chance (next_int_bounded(6) == 0) that marks it "steep", decided once per tunnel rather than re-rolled per walk step. | CaveWorldCarver.java:146 - `boolean steep = random.nextInt(6) == 0;` sits in `createTunnel` before the walk loop, so it is decided once per tunnel and never re-rolled. Structural gap the blueprint must close: this draw does NOT come from the carrier RNG - `createTunnel` opens a fresh `RandomSource.createThreadLocalInstance(tunnelSeed)` (:144) seeded by a `random.nextLong()` taken in `carve` (:65), and the steep roll is that tunnel-local source's SECOND draw, after `int splitPoint = random.nextInt(dist / 2) + dist / 4` (:145) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each cave tunnel's base thickness is itself two draws, next_float() * 2.0 + next_float() (not a config constant), and it may roll a "widen" chance gated by a hardcoded next_int_bounded(10) == 0 (not a configurable bound); when it hits, thickness is multiplied by next_float() * next_float() * 3.0 + 1.0. | `CaveWorldCarver.getThickness` (CaveWorldCarver.java:91-98). The widen multiplier `random.nextFloat() * random.nextFloat() * 3.0F + 1.0F` is exact (:94). Correction: the gate is `random.nextInt(10) == 0` (:93) - a hardcoded 10 in `getThickness`, not a configurable bound - and the base thickness is itself two draws, `random.nextFloat() * 2.0F + random.nextFloat()` (:92), not a config constant; `NetherWorldCarver` overrides the whole method to `(nextFloat()*2 + nextFloat()) * 2` with no widen roll at all (NetherWorldCarver.java:28-31) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Inside the cave tunnel's per-step worm walk, heading integrates turn-rate as horizontal_rotation += y_rota * 0.1 and vertical_rotation += x_rota * 0.1. | CaveWorldCarver.java:158-159 - `verticalRotation += xRota * 0.1F; horizontalRotation += yRota * 0.1F;` (both in float precision). Placement note: in vanilla these run AFTER the position update at :153-156 and after `verticalRotation *= steep ? 0.92F : 0.7F` at :157, whereas the blueprint's pseudocode applies the whole rotation block before moving | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Inside the cave tunnel's per-step worm walk, x_rota is damped each step by multiplying by 0.9, and y_rota is damped each step by multiplying by 0.75. | CaveWorldCarver.java:160-161 - `xRota *= 0.9F; yRota *= 0.75F;` once per step | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Inside the cave tunnel's per-step worm walk, x_rota is perturbed each step by (next_float() - next_float()) * next_float() * 2.0, and y_rota is perturbed each step by (next_float() - next_float()) * next_float() * 4.0 (three RNG draws each). | CaveWorldCarver.java:162-163 - `xRota += (random.nextFloat() - random.nextFloat()) * random.nextFloat() * 2.0F; yRota += (random.nextFloat() - random.nextFloat()) * random.nextFloat() * 4.0F;` - three draws each, six per step, taken from the tunnel-local RNG created at :144 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Inside the cave tunnel's per-step worm walk, vertical_rotation is additionally multiplied each step by 0.92 if the tunnel is steep, or 0.7 otherwise. | CaveWorldCarver.java:157 - `verticalRotation *= steep ? 0.92F : 0.7F;` once per step. Site note: it is applied immediately after the position update and before the `+= xRota * 0.1F` integration, not after it | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The cave tunnel's per-step radius envelope is h_radius = 1.5 + mth_sin(PI * step / step_count) * thickness (a constant 1.5 base added to the sine taper, so a tunnel never narrows below 1.5), and the value handed to carve_ellipsoid is further scaled by the per-cave sampled horizontal_radius_multiplier, with vertical_radius = h_radius * y_scale * vertical_radius_multiplier. | CaveWorldCarver.java:151 - `double horizontalRadius = 1.5 + Mth.sin((float)Math.PI * currentStep / dist) * thickness;`. Correction: there is a constant 1.5 base radius added to the sine envelope, so a tunnel never tapers below 1.5; and the value handed to `carveEllipsoid` is further multiplied by the per-cave sampled `horizontalRadiusMultiplier` (:224), with the vertical radius being `horizontalRadius * yScale * verticalRadiusMultiplier` (:152, :225) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A cave tunnel forks once, at a step index drawn once per tunnel as split_point = next_int_bounded(dist/2) + dist/4, gated on thickness > 1.0 at that step (not a per-step 1-in-4 roll); on a hit it spawns two children at the same position, each with its own freshly-drawn thickness = next_float() * 0.5 + 0.5 (not half the parent's), both headings turned by PI/2 in opposite directions (neither continuing straight), vertical_rotation / 3.0, a forced y_scale of 1.0, each re-seeded from its own next_long() draw, and the parent walk terminates at that step. | CaveWorldCarver.java:164-208. Correction on every numeric point: the fork gate is `currentStep == splitPoint && thickness > 1.0F`, where `splitPoint = random.nextInt(dist / 2) + dist / 4` is drawn once per tunnel at :145 - there is no per-step 1-in-4 fork roll (the `random.nextInt(4) != 0` at :210 is the per-step carve gate, a different thing). Each child's thickness is a fresh `random.nextFloat() * 0.5F + 0.5F` (:177, :198), not half the parent's; the headings are `horizontalRotation - (float)(Math.PI/2)` and `horizontalRotation + (float)(Math.PI/2)` (:178, :199) - both turned, neither continuing straight - with `verticalRotation / 3.0F` each (:179, :200), a forced `yScale` of 1.0 (:182, :203), each child re-seeded from its own `random.nextLong()` (:170, :191), and the children resume at `currentStep`, not at a midpoint. The parent's `return` at :207 is correct | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The canyon carver draws its origin as x = source_chunk_x*16 + next_int_bounded(16), then the origin Y via the height provider's own sample, then z = source_chunk_z*16 + next_int_bounded(16), in that order. | CanyonWorldCarver.java:35-37 - `double x = sourceChunkPos.getBlockX(random.nextInt(16)); int y = configuration.y.sample(random, context); double z = sourceChunkPos.getBlockZ(random.nextInt(16));` in that order | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The canyon carver's initial yaw is drawn as next_float() * 2 * PI, and its initial pitch is drawn from the configured `vertical_rotation` FloatProvider (a config-driven sample, not a hardcoded formula), which in the shipped canyon configuration spans twice the range a (next_float()-0.5)/4.0 formula would - the same divisor the cave carver itself uses, not a shallower one. | CanyonWorldCarver.java:38-39. The yaw half is right: `float horizontalRotation = random.nextFloat() * (float)(Math.PI * 2);`. Correction: the pitch is `float verticalRotation = configuration.verticalRotation.sample(random);` - a config `FloatProvider` (CanyonCarverConfiguration.java:20, :26), not a hardcoded formula. In datagen configured_carver/canyon.json it is `{"type": "minecraft:uniform", "min_inclusive": -0.125, "max_exclusive": 0.125}`, which happens to span the same range the claim's `(nextFloat()-0.5)/8.0` would, but it is a JSON-driven sample and must be read from the config | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The canyon carver's thickness is drawn from the configured `shape.thickness` FloatProvider (a trapezoid distribution in the shipped configuration), with no `3.0 + next_float() * range` expression anywhere; the only constant offset in the per-step radius math is the 1.5 base added to the sine envelope. | CanyonWorldCarver.java:41 - `float thickness = configuration.shape.thickness.sample(random);`. Correction: there is no `3.0 + nextFloat() * range` expression; thickness is a config `FloatProvider` under `shape.thickness` (CanyonCarverConfiguration.java:53), which in configured_carver/canyon.json is `{"type": "minecraft:trapezoid", "min": 0.0, "max": 6.0, "plateau": 2.0}`. The constant 1.5 base radius the claim attributes to a 3.0 offset actually appears per step at CanyonWorldCarver.java:89 as `1.5 + Mth.sin(...) * thickness` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Inside the canyon's per-step walk, heading integrates turn-rate as pitch += x_rota * 0.05 and yaw += y_rota * 0.05 - an integration constant of 0.05, half the cave carver's own 0.1, not shared with it. | CanyonWorldCarver.java:99-100 - `verticalRotation += xRota * 0.05F; horizontalRotation += yRota * 0.05F;`. Correction: the canyon's integration constant is 0.05, half the cave carver's 0.1 (CaveWorldCarver.java:158-159); the two walks do NOT share it | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Inside the canyon's per-step walk, x_rota is damped each step by multiplying by 0.8 and y_rota by 0.5 (the per-step 0.7 multiplier belongs to vertical_rotation, a different variable), versus the cave carver's own 0.9/0.75 damping constants. | CanyonWorldCarver.java:101-102 - `xRota *= 0.8F; yRota *= 0.5F;`. Correction: the canyon damps xRota by 0.8 and yRota by 0.5, not both by 0.7; the 0.7 in this walk is `verticalRotation *= 0.7F` at :98, a different variable | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Inside the canyon's per-step walk, x_rota and y_rota are perturbed by the same three-draw formulas as the cave walk: (next_float()-next_float())*next_float()*2.0 for x_rota, and (next_float()-next_float())*next_float()*4.0 for y_rota. | CanyonWorldCarver.java:103-104 - `xRota += (random.nextFloat() - random.nextFloat()) * random.nextFloat() * 2.0F; yRota += (random.nextFloat() - random.nextFloat()) * random.nextFloat() * 4.0F;`, identical in form to CaveWorldCarver.java:162-163 and likewise drawn from the tunnel-local RNG created at CanyonWorldCarver.java:83 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The canyon's per-step walk applies its x_rota *= 0.8 / y_rota *= 0.5 damping only once, and it runs BEFORE the perturbation step, not after it - there is no second damping pass. | CanyonWorldCarver.java:98-104. Correction: there is no second damping pass. The single `xRota *= 0.8F; yRota *= 0.5F;` at :101-102 runs BEFORE the perturbation at :103-104, and nothing multiplies xRota or yRota again in the step; the blueprint's placeholder applies 0.8/0.5 a second time after the impulse refresh, which vanilla never does | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The canyon's per-step vertical radius is horizontal_radius * y_scale (y_scale sampled once per carve from the configured `y_scale` FloatProvider), then rewritten per step by an update formula combining `shape.vertical_radius_default_factor` and `shape.vertical_radius_center_factor` with a distance-from-center weight and one further per-step random draw in [0.75, 1.0) - the only division by 6 in the canyon carver lives inside its skip predicate, not in the vertical-radius computation. | CanyonWorldCarver.java:90-92 and :144-150. Correction: the per-step vertical radius is `horizontalRadius * yScale` (:90, with `yScale` sampled once per carve from `configuration.yScale` at :40 - 3.0 in configured_carver/canyon.json), then rewritten by `updateVerticalRadius` as `(shape.verticalRadiusDefaultFactor + shape.verticalRadiusCenterFactor * (1 - abs(0.5 - step/distance) * 2)) * verticalRadius * Mth.randomBetween(random, 0.75F, 1.0F)` (:147-149) - a per-step draw the blueprint omits entirely. The only division by 6 in the canyon carver is inside its skip predicate, `(xd*xd + zd*zd) * widthFactorPerHeight[yIndex-1] + yd*yd/6.0 >= 1.0` (:152-155) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The canyon carver precomputes one width-scale factor per Y-level across the entire dimension height (a "piecewise-constant random spikiness profile down the entire world height"), not one per walk step. | `CanyonWorldCarver.initWidthFactors` (CanyonWorldCarver.java:128-142) allocates `new float[context.getGenDepth()]` and fills one entry per Y index across the whole dimension height, independent of the walk's step count; the array is looked up per position by `y - context.getMinGenY()` in `shouldSkip` (:152-155). Two mechanics the blueprint's placeholder gets wrong even though this claim holds: the refresh condition is yIndex equal to 0 OR `random.nextInt(configuration.shape.widthSmoothness) == 0` with `widthFactor = 1.0F + random.nextFloat() * random.nextFloat()` (two draws, :134-136), and the stored value is `widthFactor * widthFactor` (:138) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's carver JSON's discriminator key is `type` (a plain registry-dispatch key, not `carver_type`), and it selects "minecraft:cave" for cave/cave_extra_underground, "minecraft:nether_cave" for nether_cave, and "minecraft:canyon" for canyon. | The JSON key is `type`, not `carver_type` (ConfiguredWorldCarver.DIRECT_CODEC dispatches on the `BuiltInRegistries.CARVER` name, ConfiguredWorldCarver.java:20-22). Correction on the values: datagen configured_carver/cave.json and cave_extra_underground.json are `"type": "minecraft:cave"`, canyon.json is `"type": "minecraft:canyon"`, but nether_cave.json is `"type": "minecraft:nether_cave"` - its own registry entry (WorldCarver.java:33), not `minecraft:cave` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's carve algorithms never cache or memoize a source chunk's tunnel geometry across the different target chunks that same source chunk's carve walks can affect - every target chunk's own carving pass independently re-derives and re-walks the full neighborhood from scratch. | `applyCarvers` (NoiseBasedChunkGenerator.java:304-351) rebuilds every source chunk's carve geometry inside the dx/dz loops on each invocation; no geometry, tunnel or mask cache keyed by source chunk exists anywhere in the carver package or the generator. The only memo on this path is `ChunkAccess.carverBiome` (ChunkAccess.java:424-430), which caches the neighbor's `BiomeGenerationSettings`, not its carve geometry | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's carving stage touches only the target chunk's own block data; it never reads a neighbor source chunk's already-materialized block state, only re-derives that neighbor's own carve geometry from the world seed and coordinates. | Every write in the carve path targets the `chunk` argument - `chunk.setBlockState` at WorldCarver.java:147, :156 and NetherWorldCarver.java:54 - and `chunk` is the target chunk passed into `applyCarvers` (NoiseBasedChunkGenerator.java:310, :343). The neighbor is fetched at :329 only to obtain its `BiomeGenerationSettings`, whose value comes from `biomeSource.getNoiseBiome(...)` - a pure function of coordinates and seed - with no `getBlockState` call on the neighbor anywhere in the method. Caveat for GEN-D18's purity framing: `carverBiome` (ChunkAccess.java:424-430) does mutate a cached field on the neighbor chunk object | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A BiasedToBottom `HeightProvider` (min, max, inner) resolves both endpoints via `VerticalAnchor`, defaults its inner parameter to 1 (codec-constrained to at least 1, never merely non-negative), and - unless the span hi - lo - inner + 1 is non-positive (in which case it returns lo with zero draws) - returns lo + next_int_bounded(limit + inner) where limit = next_int_bounded(hi - lo - inner + 1), two nested next_int_bounded draws with these exact bounds. | `BiasedToBottomHeight.sample` (BiasedToBottomHeight.java:35-47). Correction on both the formula and the parameter: vanilla computes `limit = random.nextInt(max - min - inner + 1)` then returns `random.nextInt(limit + inner) + min` - the inner bound is `hi - lo - inner + 1` (not `(hi - lo - inner).max(1)`) and the outer bound is `limit + inner` (not `limit + inner + 1`). The empty-range case `max - min - inner + 1 <= 0` logs a warning and returns `min` with zero draws (:39-41). `inner` is codec-constrained to `Codec.intRange(1, Integer.MAX_VALUE)` with default 1 (:18), so it is at least 1, never merely non-negative | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's VeryBiasedToBottom `HeightProvider` is structurally distinct from BiasedToBottom, not the same formula with a steeper exponent: it makes three nested Mth.nextInt(random, lo, hi) calls, and each such call itself performs zero RNG draws (returning lo) whenever lo >= hi, so a reimplementation must reproduce that short-circuit to stay draw-synchronized on narrow ranges. | `VeryBiasedToBottomHeight.sample` (VeryBiasedToBottomHeight.java:38-51). Correction: it is structurally distinct, not the same formula with a different exponent - it makes THREE nested draws through `Mth.nextInt` (Mth.java:149-151), `upperInclusive = Mth.nextInt(random, min + inner, max)`, `biasedUpperInclusive = Mth.nextInt(random, min, upperInclusive - 1)`, `return Mth.nextInt(random, min, biasedUpperInclusive - 1 + inner)`, against BiasedToBottom's two `random.nextInt` draws. Its empty-range guard is the same `max - min - inner + 1 <= 0` -> return `min` (:41-43) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A Trapezoid `HeightProvider` (min, max, plateau) resolves both endpoints via `VerticalAnchor`, defaults its plateau parameter to 0 with no range restriction, and - unless min > max (in which case it returns min with zero draws) - computes range = hi - lo; when plateau >= range it draws a single inclusive next_int_range(lo, hi + 1), and otherwise draws two DIFFERENTLY-bounded inclusive values (next_int_range(0, plateau_end) and next_int_range(0, plateau_start), where plateau_start = (range - plateau) / 2 and plateau_end = range - plateau_start) and SUMS them onto lo - never subtracting or clamping either draw. | `TrapezoidHeight.sample` (TrapezoidHeight.java:45-60). Correction: nothing is subtracted and nothing is clamped. Vanilla computes `range = max - min`; if `plateau >= range` it returns a single `Mth.randomBetweenInclusive(random, min, max)`; otherwise `plateauStart = (range - plateau) / 2`, `plateauEnd = range - plateauStart`, and it returns `min + Mth.randomBetweenInclusive(random, 0, plateauEnd) + Mth.randomBetweenInclusive(random, 0, plateauStart)` - two draws with DIFFERENT bounds, SUMMED. The degenerate branch is `min > max` -> return `min` with zero draws (:48-51), not a span-below-1 test, and the `plateau` codec is a plain `Codec.INT` defaulting to 0 (:19) with no non-negative clamp | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The carving pass's reached_surface flag, once set true for a column after observing a pre-carve grass_block or mycelium state, stays true for the remainder of that column's Y scan, so every subsequently carved block in that column triggers a surface re-topping call, not only the block immediately below the original grass or mycelium position. | `WorldCarver.carveEllipsoid` creates `MutableBoolean hasGrass = new MutableBoolean(false)` once per (xIndex, zIndex) column at WorldCarver.java:102, inside the column loop and outside the Y loop at :104, and passes it by reference into every `carveBlock` call for that column (:109); `carveBlock` only ever calls `hasGrass.setTrue()` (:135) and never resets it, so it stays true for the rest of that column's downward scan. Two gates the blueprint should carry: the re-top attempt targets the block one DOWN from the carved position and fires only when that block is `Blocks.DIRT` (:152-162) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's carve call site passes a fixed sentinel density value of 0.0 to Aquifer.computeSubstance, rather than a locally sampled density, for a position already known by the ellipsoid geometry to be carved into non-solid space. | WorldCarver.java:171 - `aquifer.computeSubstance(new DensityFunction.SinglePointContext(blockPos.getX(), blockPos.getY(), blockPos.getZ()), 0.0)` - the literal `0.0` is the density argument at the only carve call site. Both aquifer implementations treat it as the "not solid" case: they return null only when `density > 0.0` (Aquifer.java:34 and :93-96) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The cave tunnel's per-tunnel step count (walk length) is a fixed engine constant, max_distance = (get_range()*2-1)*16, minus one RNG draw (next_int_bounded(max_distance/4)) - it does not depend on the tunnel origin's Y or on the dimension's generation depth at all; the canyon carver's own step count is likewise Y-independent, a config-sampled fraction of the same max_distance constant. | CaveWorldCarver.java:35, :58 - `int maxDistance = SectionPos.sectionToBlockCoord(this.getRange() * 2 - 1);` = (4*2-1)*16 = 112, then per tunnel `int distance = maxDistance - random.nextInt(maxDistance / 4);`. Correction: the walk length is a constant derived from `WorldCarver.getRange()` (WorldCarver.java:58-60) minus one RNG draw; it does not depend on the tunnel origin's Y or on `context.getGenDepth()` at all. The canyon's equivalent is `(int)(maxDistance * configuration.shape.distanceFactor.sample(random))` (CanyonWorldCarver.java:34, :42), also Y-independent | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The cave tunnel's per-step position update is x += mth_cos(vertical_rotation) * mth_cos(horizontal_rotation), z += mth_cos(vertical_rotation) * mth_sin(horizontal_rotation), y += mth_sin(vertical_rotation), using the table-based trig functions for every term. | CaveWorldCarver.java:153-156 - `float cosX = Mth.cos(verticalRotation); x += Mth.cos(horizontalRotation) * cosX; y += Mth.sin(verticalRotation); z += Mth.sin(horizontalRotation) * cosX;` - table-based `Mth` trig on every term, with the shared `cosX` factored out | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The cave tunnel's per-step "skip roughly 1 in 4 steps" organic-gap behavior IS a probabilistic roll, next_int_bounded(4) != 0, and it consumes one RNG draw on every step from the tunnel-local RNG - a deterministic step % 4 substitute would desynchronize the RNG stream from step 1 onward; the canyon walk uses the identical gate. | CaveWorldCarver.java:210 - `if (random.nextInt(4) != 0) { ... carveEllipsoid ... }`. Correction: the skip IS a probabilistic roll and it DOES consume one draw from the tunnel-local RNG on every step, so a deterministic `step % 4` substitute desynchronizes the stream from step 1 onward. The canyon walk uses the identical gate at CanyonWorldCarver.java:105 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The canyon's per-step position update uses the same table-based trig shape as the cave tunnel's own update: x += mth_cos(pitch) * mth_cos(yaw), z += mth_cos(pitch) * mth_sin(yaw), y += mth_sin(pitch). | CanyonWorldCarver.java:93-97 - `float xc = Mth.cos(verticalRotation); float xs = Mth.sin(verticalRotation); x += Mth.cos(horizontalRotation) * xc; y += xs; z += Mth.sin(horizontalRotation) * xc;` - the same table-based shape as CaveWorldCarver.java:153-156 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The canyon's per-step horizontal radius is (1.5 + mth_sin(step * PI / distance) * thickness) * config.shape.horizontal_radius_factor.sample(random) - a sine envelope with a 1.5 base times a freshly per-step-sampled config factor; the precomputed per-Y width-factor array never scales the radius at all, it is applied only inside the skip predicate to warp the ellipsoid test per Y level. | CanyonWorldCarver.java:89-91 and :152-155. Correction: the per-step horizontal radius is `(1.5 + Mth.sin(currentStep * (float)Math.PI / distance) * thickness) * configuration.shape.horizontalRadiusFactor.sample(random)` - a sine envelope with a 1.5 base times a freshly sampled per-step config factor. The per-Y width-factor array never scales the radius; it is applied inside the skip predicate as `(xd*xd + zd*zd) * widthFactorPerHeight[yIndex - 1] + yd*yd/6.0 >= 1.0`, i.e. it warps the ellipsoid test per Y level rather than resizing the stamp | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's cave carver configuration (not only the canyon carver configuration) is understood to also carry its own y_scale field, correcting an earlier assumption that y_scale was canyon-only. | `CarverConfiguration` (CarverConfiguration.java:17-30, :44) declares `FloatProvider yScale` on the SHARED base class, so both cave and canyon configurations carry it - the JSON key is the camelCase `yScale`, not `y_scale` (:22). Cave usage is confirmed at CaveWorldCarver.java:48, where the room branch samples it as the room's vertical-radius multiplier (:119); datagen configured_carver/cave.json carries `"yScale": {"type": "minecraft:uniform", "min_inclusive": 0.1, "max_exclusive": 0.9}` and nether_cave.json `"yScale": 0.5`. Note the cave TUNNEL uses `getYScale()` (CaveWorldCarver.java:77, :100-102 - a hardcoded 1.0, overridden to 5.0 by NetherWorldCarver.java:33-35) rather than the config field | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
