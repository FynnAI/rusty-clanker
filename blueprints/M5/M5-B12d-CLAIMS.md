# M5-B12d — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| The vanilla fossil feature's configuration data shape has five fields: fossil_structures (a list of structure resource locations), overlay_structures (a list of structure resource locations paired by index with fossil_structures), fossil_processors (a processor list id), overlay_processors (a processor list id), and max_empty_corners_allowed (an integer threshold). | `FossilFeatureConfiguration.CODEC` (FossilFeatureConfiguration.java:13-22) declares exactly those five field names in that order, with fields at :23-27. Two refinements for §M.4: `max_empty_corners_allowed` is `Codec.intRange(0, 7)` — an 8-corner budget, see the corner row below — and the two processor fields are `Holder<StructureProcessorList>` (a registry reference or an inline list), matching the blueprint's `ProcessorListId` only for the by-id case. The constructor (:36-42) also rejects an empty `fossil_structures` and any length mismatch between the two lists. Confirmed against data/minecraft/worldgen/configured_feature/fossil_coal.json and fossil_diamonds.json, whose configs carry exactly these five keys | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Fossil placement draws exactly one random index via next_int_bounded(fossil_structures.len()) to select which fossil/overlay structure pair, matched by index, to place. | `FossilFeature.place` (FossilFeature.java:32) `int fossilIndex = random.nextInt(config.fossilStructures.size());` and :34-35 index BOTH `fossilStructures` and `overlayStructures` with that same value — one draw, index-paired. Note for §M.4's ordering: this is the SECOND draw of the function, not the first — `Rotation.getRandom(random)` at :30 already consumed one | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| If either the selected fossil structure or its index-paired overlay structure fails to load from disk, fossil placement does not abort: the heightmap scan is entered but contributes nothing over the empty template's zero-size footprint, the depth draw and the empty-corners gate still run, and if the gate passes both place_in_world calls still run and each returns immediately with zero draws of its own, so fossil placement still reports success. | No such abort path exists. `FossilFeature.place` (FossilFeature.java:33-35) uses `StructureTemplateManager.getOrCreate`, which (StructureTemplateManager.java:83-92) returns a freshly constructed EMPTY `StructureTemplate` and caches it when the file is absent — it never signals failure, so the feature continues. Placement then proceeds through the heightmap scan (:50-54), a third draw `random.nextInt(10)` (:56) and the corner gate (:58); only the individual `StructureTemplate.placeInWorld` call bails, at its own `this.palettes.isEmpty()` guard (StructureTemplate.java:263-264), returning false with zero draws of its own. Correction: a missing template neither aborts the feature nor prevents the rotation, index and depth draws | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Fossil placement draws a random value via next_int_bounded(4), as the first draw of the function, to select one of the four Rotation values (None, Cw90, Cw180, Ccw90) to apply to both the fossil and overlay structure placements. | The draw exists and the shared rotation is right, the position in the stream is not. `FossilFeature.place` (FossilFeature.java:30) calls `Rotation.getRandom(random)` FIRST, before the index draw at :32; `Rotation.getRandom` (Rotation.java:112-114) is `Util.getRandom(values(), random)` = `values()[random.nextInt(4)]` (Util.java:826-828) over NONE, CLOCKWISE_90, CLOCKWISE_180, COUNTERCLOCKWISE_90 (Rotation.java:18-21). That rotation is put into the one `StructurePlaceSettings` (:45) reused for both placements (:64, :67). Correction: the rotation draw is the FIRST draw, the structure-pair index draw the second | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For a successful fossil placement, the shared decoration-feature RNG stream consumes three pre-placement draws in order (the rotation draw, then the structure-pair index draw, then a depth draw), plus one palette-pick draw per place_in_world call (two total) plus one nextFloat draw per processed base-template block and per processed overlay-template block, so the total shared-stream draw count scales with the loaded templates' size rather than being a fixed constant. | Both the count and the order are wrong, and the size-independence is wrong. `FossilFeature.place` takes THREE draws before any placement, in this order: rotation `nextInt(4)` (FossilFeature.java:30), pair index `nextInt(size)` (:32), depth `nextInt(10)` inside `targetY = max(lowestSurfaceY - 15 - random.nextInt(10), level.getMinY() + 10)` (:56). It then passes that same shared random into the settings via `setRandom(random)` (:45), so each `placeInWorld` (:64, :67) draws further from it: one `getRandom(pos).nextInt(paletteSize)` per call for the palette pick (StructureTemplate.java:267 via StructurePlaceSettings.java:141-147) and one `nextFloat()` PER BLOCK from `BlockRotProcessor` (BlockRotProcessor.java:49-52, which reads `settings.getRandom(...)`, i.e. the shared stream) — and every vanilla fossil processor list carries a `block_rot` (data/minecraft/worldgen/processor_list/fossil_rot.json integrity 0.9, fossil_coal.json and fossil_diamonds.json integrity 0.1). The shared-stream draw count therefore scales with template size | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The empty-corners validity gate computes the fossil template's own full 3D rotated bounding box at a target position derived from a heightmap scan and a depth draw (swapping size_x and size_z under Cw90/Ccw90 rotation), then counts how many of that box's 8 corners are currently air, lava, or water blocks in the world. | Three things differ. (a) It is the full 3D bounding box of the rotated template at the DERIVED target position, not a horizontal footprint at the origin's Y: `fossilBase.getBoundingBox(settings, targetPos)` (FossilFeature.java:58) where `targetPos` comes from `getZeroPositionWithTransform(lowCorner.atY(targetY), Mirror.NONE, rotation)` (:57) and `targetY = max(lowestSurfaceY - 15 - random.nextInt(10), minY + 10)` (:56), with `lowestSurfaceY` the minimum OCEAN_FLOOR_WG heightmap value over the rotated footprint (:48-54) and `lowCorner = origin.offset(-sizeX/2, 0, -sizeZ/2)` (:47). (b) `BoundingBox.forAllCorners` (BoundingBox.java:241-251) visits EIGHT corners (both Y extremes), not four — matching the config's own `intRange(0, 7)` budget. (c) The predicate is air OR lava OR water: `state.isAir() or state.is(Blocks.LAVA) or state.is(Blocks.WATER)` (FossilFeature.java:75). The size_x/size_z swap under Cw90/Ccw90 is right, via `fossilBase.getSize(rotation)` (:46) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Fossil placement aborts, after all three RNG draws (the rotation draw, the structure-pair index draw, and the depth draw) and before any block writes, if the empty-corners count from the validity gate exceeds max_empty_corners_allowed. | The abort condition and its position before any block write are right, the draw account is wrong. FossilFeature.java:58-60 `if (countEmptyCorners(level, fossilBase.getBoundingBox(settings, targetPos)) > config.maxEmptyCornersAllowed) return false;` sits before both `placeInWorld` calls (:64, :67). But three draws precede it, not two, and in a different order: rotation (:30), pair index (:32), depth `nextInt(10)` (:56). Correction: "after all three RNG draws (rotation, then structure-pair index, then the nextInt(10) depth draw)" | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The empty-corners footprint check performs zero RNG draws - it is pure world-read logic (low confidence: this blueprint's own reconstruction of whether this is vanilla's real gate shape at all). | `FossilFeature.countEmptyCorners` (FossilFeature.java:71-80) takes no `RandomSource`, and its body only calls `level.getBlockState(pos)` inside `BoundingBox.forAllCorners` (BoundingBox.java:241-251) — zero RNG. The gate shape itself is real vanilla (see the corner row above for its actual 8-corner air/lava/water form). One caveat for §M.4: deriving the bounding box handed to it is not draw-free — `targetY` costs one `random.nextInt(10)` (FossilFeature.java:56) — but the check itself is pure | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Both the fossil structure and its overlay structure are placed at the same origin position with mirror = None and offset = [0,0,0], using known_shape = false and keep_jigsaws = false, differing only in which processor list (fossil_processors vs overlay_processors) and which template each uses. | Both calls pass the identical `targetPos` for position and reference (FossilFeature.java:64, :67) and share ONE `StructurePlaceSettings` object (:45) whose only mutation between them is `clearProcessors()` plus the new list (:62-63, :65-66). That settings object leaves `mirror` at `Mirror.NONE`, `rotationPivot` at `BlockPos.ZERO` (the blueprint's offset [0,0,0]) and `knownShape` at false — all defaults, never set (StructurePlaceSettings.java:15-25); vanilla has no `keepJigsaws` settings field at all (jigsaw retention is a pool-element processor concern, JigsawPlacement.java:224-259) and no jigsaw processor is added here, so false is right. Two settings the blueprint omits are load-bearing: vanilla also sets `setBoundingBox` to the chunk inflated by 16 on X/Z over the world's full Y range (:37-45) and `setRandom(random)` with the SHARED feature stream (:45), and passes update flags 260 to `placeInWorld` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The fossil structure is placed into the world first, and the overlay structure is placed second at the same origin, so the overlay placement's blocks can overwrite whatever the fossil placement already wrote wherever the two templates overlap. | FossilFeature.java:62-67 — `fossilProcessors` are installed and `fossilBase.placeInWorld(level, targetPos, targetPos, settings, random, 260)` runs first, then the processors are swapped for `overlayProcessors` and `fossilOverlay.placeInWorld(...)` runs at the same `targetPos`. Each placed block goes through a plain `level.setBlock(blockPos, state, updateMode)` (StructureTemplate.java:294) with no already-occupied check, so the later overlay write wins on overlap | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For every place_in_world call this blueprint makes for the fossil placement, the overlay placement, and the template feature placement, the reference_pos argument passed equals the origin argument, i.e. the placement position itself rather than some separately derived reference point. | All three vanilla calls pass the same value twice: `fossilBase.placeInWorld(level, targetPos, targetPos, settings, random, 260)` and `fossilOverlay.placeInWorld(level, targetPos, targetPos, settings, random, 260)` (FossilFeature.java:64, :67), and `template.placeInWorld(level, pos, pos, settings, random, 3)` (TemplateFeature.java:34). Note for §M.4/§M.5: that shared value is NOT the feature's own origin — fossil uses `targetPos` derived at :47-57, and template uses `context.origin()` shifted by the rotated half-size offsets (TemplateFeature.java:30-32) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Only the Rule/RandomBlockMatch-style probability checks run by RuleProcessor are seeded per-block via Mth::get_seed(world_pos); the BlockRotProcessor check that every vanilla fossil processor list runs first instead reads the same shared decoration-feature RNG stream handed to place_in_world, drawing one nextFloat per processed block whenever its rottable_blocks filter is absent, as it is in every vanilla fossil processor list. | True only for `RuleProcessor`, false for the processor every vanilla fossil list actually uses. `RuleProcessor.processBlock` (RuleProcessor.java:33) does build its own `RandomSource.create(Mth.getSeed(processedBlockInfo.pos()))` per block (Mth.getSeed, Mth.java:335-339), so `RandomBlockMatchTest.test` (RandomBlockMatchTest.java:27-29) never touches the shared stream. But `BlockRotProcessor.processBlock` (BlockRotProcessor.java:49-52) reads `settings.getRandom(processedBlockInfo.pos())`, and `StructurePlaceSettings.getRandom` (StructurePlaceSettings.java:113-119) returns the injected random whenever one was set — FossilFeature sets exactly the shared feature random (FossilFeature.java:45). Every vanilla fossil processor list starts with a `block_rot` (fossil_rot.json, fossil_coal.json, fossil_diamonds.json), so one shared-stream `nextFloat()` per placed block IS consumed. The palette pick (StructurePlaceSettings.java:141-147) and any container `LootTableSeed` (StructureTemplate.java:306) draw from that same shared stream too. Correction: only per-block Mth.getSeed applies when no random is injected, or inside RuleProcessor | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vanilla template feature's configuration data shape has one field, templates, a weighted list of entries each carrying a structure resource location (key id) and an optional list of allowed Rotation values defaulting to all four; there is no processors field. | The shape is one field, not two, and there is no processor field at all. `TemplateFeatureConfiguration` (TemplateFeatureConfiguration.java:10-14) is a record with a single `templates` field, codec key "templates", holding a `WeightedList<TemplateEntry>`. `TemplateEntry` (:19-29) is itself a record of `id` (an Identifier) plus an optional `rotations` list of Rotation, defaulting to all four values. Confirmed against data/minecraft/worldgen/configured_feature/sulfur_spring.json, whose four `minecraft:template` configs each carry only `templates`, a weighted list of `{ "data": { "id": ... }, "weight": 1 }` entries with no processor key | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Template feature placement first draws a weighted entry from config.templates and a rotation from that entry's rotations list, then loads the entry's template; if it fails to load, placement performs zero further draws and zero writes but the two draws already taken are not undone. | Both halves are wrong. `TemplateFeature.place` (TemplateFeature.java:26) picks its entry with `config.templates().getRandomOrThrow(random)`, i.e. `random.nextInt(totalWeight)` over the weighted list (WeightedList.java:85-92) — one shared-stream draw before anything is loaded — and only then loads that entry's id at :29. Loading cannot fail: `StructureTemplateManager.getOrCreate` (StructureTemplateManager.java:83-92) returns an empty template for a missing file. With an empty template, `placeInWorld` returns false at its own `palettes.isEmpty()` guard (StructureTemplate.java:263-264), but the entry draw and the rotation draw (:27) have already been taken | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Template feature placement draws its rotation at random from the selected entry's rotations list (all four values by default) and uses that rotation to both orient and horizontally centre the placement on the origin, with Mirror::None, offset = [0,0,0], known_shape = false, and no keep_jigsaws field existing at all. | The rotation IS randomized. `TemplateFeature.place` (TemplateFeature.java:27) takes `Util.getRandom(templateEntry.rotations(), random)` = `rotations.get(random.nextInt(rotations.size()))` (Util.java:834-836) and installs it with `setRotation(rotation)` (:33); `TemplateEntry.rotations` defaults to all four Rotation values when the json omits the key (TemplateFeatureConfiguration.java:24-26), and every vanilla `minecraft:template` config in sulfur_spring.json does omit it, so all four are live. The chosen rotation also shifts the placement position: `pos = origin + rotatedOffset(X) + rotatedOffset(Z)` centres the template by half its size on each axis (:30-32, :37-39). Mirror NONE, rotationPivot ZERO and knownShape false are right — all untouched defaults (StructurePlaceSettings.java:15-24) — and vanilla has no keepJigsaws settings field. Vanilla also calls `setRandom(random)` (:33) and passes update flags 3 (:34) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Template feature placement consumes at least two draws from the shared decoration-feature RNG stream (the weighted entry pick, the rotation pick) even when the resolved template is missing, plus one further unconditional palette-pick draw when the template loads, so the WorldgenRandom's state is never bit-identical before and after a place_template call. | It consumes at least two draws before placement and more during it. `TemplateFeature.place`: `getRandomOrThrow(random)` = `nextInt(totalWeight)` (TemplateFeature.java:26 via WeightedList.java:85-92) and `Util.getRandom(templateEntry.rotations(), random)` = `nextInt(rotations.size())` (:27 via Util.java:834-836). It then injects that same random into the settings with `setRandom(random)` (:33), so `placeInWorld` adds one `nextInt(paletteSize)` palette pick (StructureTemplate.java:267 via StructurePlaceSettings.java:141-147) plus one `nextLong()` per placed randomizable container (StructureTemplate.java:306) — content-dependent, not bit-identical | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The barrier block state place_in_world writes immediately before the real state, for both fossil and template placement, is minecraft:barrier with no block-state properties, written only for template blocks that carry NBT and under its own distinct update flags. | The state vanilla writes in that role is minecraft:barrier, not minecraft:air. `StructureTemplate.placeInWorld` (StructureTemplate.java:290-292) clears any NBT-carrying template block with `level.setBlock(blockPos, Blocks.BARRIER.defaultBlockState(), 820)` immediately before writing the real state at :294 — the only pre-placement placeholder on this path, shared by fossil (FossilFeature.java:64, :67) and template (TemplateFeature.java:34). `Blocks.AIR.defaultBlockState()` appears in the template system only as `SimplePalette.DEFAULT_BLOCK_STATE` (StructureTemplate.java:855), the palette's own unset-id filler, which is not a placement barrier. Correction: use `minecraft:barrier` with no properties for M5-B08's `barrier_state` argument | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
