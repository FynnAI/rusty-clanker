# M5-B08 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Vanilla Java 26.2's StructureType registry has 16 entries: jigsaw, plus 15 hand-coded families each with dedicated piece-generation code -> buried_treasure, desert_pyramid, end_city, fortress (nether fortress), igloo, jungle_temple, mineshaft, nether_fossil, ocean_monument, ocean_ruin, ruined_portal, shipwreck, stronghold, swamp_hut, woodland_mansion. | `StructureType` (net/minecraft/world/level/levelgen/structure/StructureType.java:24-39) registers exactly 16 types: `jigsaw` plus buried_treasure, desert_pyramid, end_city, fortress, igloo, jungle_temple, mineshaft, nether_fossil, ocean_monument, ocean_ruin, ruined_portal, shipwreck, stronghold, swamp_hut, woodland_mansion, each dispatching to its own hand-coded `*Structure` class under structure/structures/ | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every village variant, pillager outposts, bastion remnants, ancient city, trail ruins, and trial chambers are jigsaw-typed structures needing no structure-family-specific piece-generation code beyond the generic jigsaw engine. | datagen `data/minecraft/worldgen/structure/{village_plains,village_desert,village_savanna,village_snowy,village_taiga,pillager_outpost,bastion_remnant,ancient_city,trail_ruins,trial_chambers}.json` all carry `"type": "minecraft:jigsaw"`, i.e. they are all `JigsawStructure` instances driven by `JigsawPlacement.addPieces` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| random_spread's potential_structure_chunk computes grid_x = floor_div(source_x, spacing) and grid_z = floor_div(source_z, spacing), then seeds via set_large_feature_with_salt(world_seed, grid_x, grid_z, salt), then draws spread_x and spread_z from SpreadType::evaluate with limit = spacing - separation, and returns (grid_x*spacing+spread_x, grid_z*spacing+spread_z). | `RandomSpreadStructurePlacement.getPotentialStructureChunk` (RandomSpreadStructurePlacement.java:67-76): `Math.floorDiv(sourceX, spacing)` and `Math.floorDiv(sourceZ, spacing)`, then `random.setLargeFeatureWithSalt(seed, spacedGridX, spacedGridZ, salt())`, `limit = spacing - separation`, two `spreadType.evaluate(random, limit)` draws in X-then-Z order, returning `new ChunkPos(gridX*spacing + spreadX, gridZ*spacing + spreadZ)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| SpreadType::Linear evaluates to a single rng.next_int_bounded(limit) draw. | `RandomSpreadType.evaluate` (RandomSpreadType.java:23-28), LINEAR arm at :25 -> `random.nextInt(limit)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| SpreadType::Triangular evaluates to (rng.next_int_bounded(limit) + rng.next_int_bounded(limit)) / 2 using i32 division that truncates toward zero. | `RandomSpreadType.evaluate` (RandomSpreadType.java:26) -> `(random.nextInt(limit) + random.nextInt(limit)) / 2`; Java `int` division truncates toward zero, and since both draws are non-negative the truncation is a floor here | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| is_random_spread_chunk(world_seed, x, z, placement) is true iff potential_structure_chunk with that placement's parameters equals (x, z), i.e. every chunk in a grid cell computes the identical candidate. | `RandomSpreadStructurePlacement.isPlacementChunk` (RandomSpreadStructurePlacement.java:79-82) computes `getPotentialStructureChunk(state.getLevelSeed(), sourceX, sourceZ)` and returns true only when it equals `(sourceX, sourceZ)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For the villages structure set (spacing=34, separation=8, salt=10387312, spread_type=Linear) with world_seed=0, potential_structure_chunk for source chunk (0,0) resolves to grid cell (0,0), reseed value 10387312, spread (15,2), candidate chunk (15,2). | recomputed from `getPotentialStructureChunk` (:67-76) with `WorldgenRandom.setLargeFeatureWithSalt` (WorldgenRandom.java:66-69) over `LegacyRandomSource` (LegacyRandomSource.java:32-49) and `BitRandomSource.nextInt` (BitRandomSource.java:17-34), against villages.json (spacing 34, separation 8, salt 10387312, no spread_type override): grid (0,0), reseed 10387312, spread (15,2), candidate chunk (15,2) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For the villages structure set (spacing=34, separation=8, salt=10387312, spread_type=Linear) with world_seed=0, potential_structure_chunk for source chunk (-5,-5) resolves to grid cell (-1,-1), reseed value -474760728941, spread (25,0), candidate chunk (-9,-34). | same recomputation, source chunk (-5,-5): `Math.floorDiv(-5,34) = -1` on both axes, reseed `-1*341873128712 + -1*132897987541 + 0 + 10387312 = -474760728941`, spread (25,0), candidate chunk (-9,-34) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For the villages structure set (spacing=34, separation=8, salt=10387312, spread_type=Linear) with world_seed=0, potential_structure_chunk for source chunk (100,-37) resolves to grid cell (2,-2), reseed value 417960669654, spread (8,8), candidate chunk (76,-60). | same recomputation, source chunk (100,-37): grid (2,-2), reseed `2*341873128712 + -2*132897987541 + 0 + 10387312 = 417960669654`, spread (8,8), candidate chunk (76,-60) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For the ocean monuments structure set (spacing=32, separation=5, salt=10387313, spread_type=Triangular) with world_seed=12345, potential_structure_chunk for source chunks (0,0) and (1,1) (the same grid cell (0,0)) both resolve via reseed value 10399658 to spread (3,19) and candidate chunk (3,19). | same recomputation against ocean_monuments.json (spacing 32, separation 5, salt 10387313, spread_type triangular) with seed 12345: `Math.floorDiv(0,32) = Math.floorDiv(1,32) = 0`, reseed 10399658, limit 27, triangular spread (3,19), candidate chunk (3,19) for both source chunks | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The villages structure set's spread_type is Linear, the enum's own default, since vanilla's data only names an explicit spread_type override for ocean monuments, woodland mansions, and end cities. | `RandomSpreadStructurePlacement.CODEC` (:21) declares `optionalFieldOf("spread_type", RandomSpreadType.LINEAR)`; across all 20 files in datagen `data/minecraft/worldgen/structure_set/` only ocean_monuments.json, woodland_mansions.json and end_cities.json carry an explicit `"spread_type": "triangular"`, villages.json carries none | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| HIGHLY_ARBITRARY_RANDOM_SALT, the salt used by the frequency-reduction reseed (the "legacy_type_2 reducer"), equals 10387320. | `StructurePlacement.HIGHLY_ARBITRARY_RANDOM_SALT` (StructurePlacement.java:27) `= 10387320`, consumed at :118 by `legacyArbitrarySaltProbabilityReducer`, which backs `FrequencyReductionMethod.LEGACY_TYPE_2` (:159) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| FrequencyReductionMethod::LegacyType1 builds a WorldgenRandom over LegacyRandomSource(0), computes cx = x >> 4 and cz = z >> 4, seeds via rng.set_seed(cx ^ (cz << 4) ^ world_seed), discards one rng.next_int() draw, and passes when rng.next_int_bounded((1.0/frequency) as i32) == 0. | `StructurePlacement.legacyPillagerOutpostReducer` (StructurePlacement.java:122-129) is NOT a bare integer hash. Reference: `cx = sourceX >> 4; cz = sourceZ >> 4;` then a `WorldgenRandom(new LegacyRandomSource(0L))` with `random.setSeed(cx ^ cz << 4 ^ seed)` (Java precedence: `cx ^ (cz << 4) ^ seed`, int-promoted to long), then one discarded `random.nextInt()`, and the gate is `random.nextInt((int)(1.0F / probability)) == 0`. There is no rem_euclid-100 comparison and no `frequency*100` term | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| FrequencyReductionMethod::Default seeds via set_large_feature_with_salt(world_seed, salt, x, z) -- the placement's own salt in the x slot, x in the z slot, z as the blend -- while LegacyType2 seeds via set_large_feature_with_salt(world_seed, x, z, HIGHLY_ARBITRARY_RANDOM_SALT); both then pass when a simulated nextFloat() -> rng.next_bits(24) as f64 * 2^-24 -> is less than frequency. | the two methods do not share a seeding call. `FrequencyReductionMethod.DEFAULT` -> `probabilityReducer` (StructurePlacement.java:104-108) calls `random.setLargeFeatureWithSalt(seed, salt, sourceX, sourceZ)` -- the placement's own `salt` is passed as the x argument, `sourceX` as z and `sourceZ` as the blend, and HIGHLY_ARBITRARY_RANDOM_SALT is not involved. Only `LEGACY_TYPE_2` -> `legacyArbitrarySaltProbabilityReducer` (:116-120) calls `setLargeFeatureWithSalt(seed, sourceX, sourceZ, 10387320)`. Both then compare `random.nextFloat() < probability`, and `BitRandomSource.nextFloat` (BitRandomSource.java:50-52) is `next(24) * 5.9604645E-8F`, so that half of the claim holds | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| FrequencyReductionMethod::LegacyType3 seeds via the unsalted set_large_feature_seed(world_seed, x, z) -- structurally different from Default/LegacyType2's set_large_feature_with_salt -- and passes when rng.next_double() is less than frequency. | `legacyProbabilityReducerWithDouble` (StructurePlacement.java:110-114) seeds with `random.setLargeFeatureSeed(seed, sourceX, sourceZ)` -- the unsalted large-feature seed of `WorldgenRandom.setLargeFeatureSeed` (WorldgenRandom.java:58-64: setSeed(seed), two nextLong draws, `chunkX*xScale ^ chunkZ*zScale ^ seed`) -- not `setLargeFeatureWithSalt`. The `random.nextDouble() < probability` gate is correct | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For LegacyType2 (buried treasure, frequency=0.01) with world_seed=0, the simulated nextFloat() values at chunk coordinates (0,0), (1,0), (2,0), (3,0), (4,0) are 0.719376, 0.961357, 0.416874, 0.751377, and 0.057676 respectively, none of which pass the < 0.01 threshold. | recomputed through `legacyArbitrarySaltProbabilityReducer` (StructurePlacement.java:116-120) with `setLargeFeatureWithSalt(0, x, 0, 10387320)` and `BitRandomSource.nextFloat` (:50-52): 0.719376, 0.961357, 0.416874, 0.751377, 0.057676 for x = 0..4, all >= 0.01 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The buried treasure structure set's placement parameters are spacing=1, separation=0, frequency=0.01, frequency_reduction_method=legacy_type_2. | datagen `data/minecraft/worldgen/structure_set/buried_treasures.json`: `spacing: 1`, `separation: 0`, `frequency: 0.01`, `frequency_reduction_method: "legacy_type_2"` (it also sets `locate_offset: [9,0,9]` and `salt: 0`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Exclusion zones perform a brute-force (2*range+1)^2 chunk-square scan around the candidate chunk, testing the excluded structure set's own full is_structure_chunk gate (is_random_spread_chunk AND passes_frequency_reduction AND, recursively, that same set's own exclusion-zone check) for every cell in that square, rejecting the candidate if any cell's full gate holds; the two coincide only for vanilla's single consumer (pillager outposts excluding villages, frequency 1.0, no nested exclusion zone). | `ChunkGeneratorStructureState.hasStructureChunkInRange` (ChunkGeneratorStructureState.java:191-203) does scan the full `(2*range+1)^2` square and returns on the first hit, but the per-cell predicate is `placement.isStructureChunk(this, testX, testZ)` -- the excluded set's FULL gate (`StructurePlacement.isStructureChunk`, :82-86 = isPlacementChunk AND applyAdditionalChunkRestrictions AND applyInteractionsWithOtherStructures) -- not the bare `is_random_spread_chunk`/`isPlacementChunk` test. For vanilla's only consumer (pillager_outposts excluding villages, whose placement has frequency 1.0 and no exclusion zone of its own) the two coincide, but the general shape is the full gate | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The pillager-outposts-vs-villages exclusion zone's range (its chunk_count field) is 10. | datagen `data/minecraft/worldgen/structure_set/pillager_outposts.json`: `exclusion_zone: { chunk_count: 10, other_set: "minecraft:villages" }`; the field is `chunkCount` in `StructurePlacement.ExclusionZone` (StructurePlacement.java:132-141, codec range 1..16) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| concentric_rings ring positions are computed once per world, seeded from the world's own concentric-rings seed field, which in a normal world equals the same levelSeed value the per-structure-chunk placement seed derives from -> not a distinct legacy value; only a superflat world's generator seeds the ring computation with 0 instead. | the ring positions are indeed computed once per world (`ChunkGeneratorStructureState.generatePositions` / `ensureStructuresGenerated`, ChunkGeneratorStructureState.java:87-105, :169-174) and seeded once from `this.concentricRingsSeed` (:119), but that value is NOT distinct from the placement seed: `createForNormal` (:52-59) passes the same `levelSeed` for both the `levelSeed` and `concentricRingsSeed` fields, and `ChunkMap` (server/level/ChunkMap.java:190) calls `generator.createState(..., levelSeed)`. Only `createForFlat` (:45-50) differs, passing `0L` as the concentric-rings seed | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| generate_ring_positions seeds an RcLegacyRandom from the concentric-rings seed, draws an initial angle = next_double() * 2*PI, and for each of placement.count positions computes dist = 4*distance + distance*circle*6 + (next_double()-0.5)*distance*2.5, then initial_x = (angle.cos()*dist + 0.5).floor() as i32 and initial_z = (angle.sin()*dist + 0.5).floor() as i32 -> round-half-up, not a truncating cast, and already in chunk units with no multiplication by 32. | `ChunkGeneratorStructureState.generateRingPositions` (ChunkGeneratorStructureState.java:118-127): `RandomSource.create()` then `random.setSeed(this.concentricRingsSeed)` (`RandomSource.create(long)` is `new LegacyRandomSource(seed)`, util/RandomSource.java:23-25), `angle = random.nextDouble() * Math.PI * 2.0`, and `dist = 4*distance + distance*circle*6 + (random.nextDouble() - 0.5) * (distance * 2.5)` -- those match. The position formula does not: it is `initialX = (int)Math.round(Math.cos(angle) * dist)` and `initialZ = (int)Math.round(Math.sin(angle) * dist)` (:126-127) -- round-half-up via Math.round, not a truncating cast, and with NO multiplication by 32. `initialX`/`initialZ` are already chunk coordinates (the fallback at :146 returns `new ChunkPos(initialX, initialZ)` directly) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ring-position biome search samples at block coordinates (initial_x*16+8, 0, initial_z*16+8) -> the section centre of the candidate chunk -> with a search radius of 112 blocks (28 quart cells) and a step of one quart cell (4 blocks), forking a per-position RNG for its own tie-breaking. | `ChunkGeneratorStructureState.generateRingPositions` (:132-141) calls `biomeSource.findBiomeHorizontal(SectionPos.sectionToBlockCoord(initialX, 8), 0, SectionPos.sectionToBlockCoord(initialZ, 8), 112, preferredBiomes::contains, biomeSearchGenerator, sampler)`. `SectionPos.sectionToBlockCoord(c, 8)` (core/SectionPos.java:128-134) is `(c << 4) + 8`, so the sample centre is `(raw_x*16 + 8, 0, raw_z*16 + 8)`, not `(raw_x*16, 0, raw_z*16)`. The search radius argument is 112 BLOCKS, converted to `QuartPos.fromBlock(112) = 28` quart cells by `BiomeSource.findBiomeHorizontal` (world/level/biome/BiomeSource.java:117-160), and the step is `skipSteps = 1` quart cell = 4 blocks (the 5-arg overload at :70-80 passes `1`). Not 112*8*16 blocks and not a 8*16-block step. Note also the per-position `random.fork()` at :128 that supplies the search's tie-breaking RNG | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| After placing each ring position, the angle advances by 2*PI / positions_in_ring, and when the current ring is exhausted, the ring index (circle) increments, positions_in_ring grows by (2*positions_in_ring)/(circle+1) capped at (placement.count - i), and an extra angle jitter of next_double()*2*PI is added. | `ChunkGeneratorStructureState.generateRingPositions` (:152-159): `angle += (Math.PI * 2) / spread;` then `if (++positionInCircle == spread) { circle++; positionInCircle = 0; spread += 2 * spread / (circle + 1); spread = Math.min(spread, count - i); angle += random.nextDouble() * Math.PI * 2.0; }`. Everything matches except the cap, which is `Math.min(spread, count - i)` -- `count - i`, not `count - i - 1` (`i` is still the current loop index at that point, incremented only by the for-header afterwards) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's stronghold placement uses concentric_rings parameters distance=32, spread=3, count=128, salt=0, biased toward the #minecraft:stronghold_biased_to biome tag. | datagen `data/minecraft/worldgen/structure_set/strongholds.json`: `type: minecraft:concentric_rings`, `distance: 32`, `spread: 3`, `count: 128`, `salt: 0`, `preferred_biomes: "#minecraft:stronghold_biased_to"`; field names per `ConcentricRingsStructurePlacement.codec` (ConcentricRingsStructurePlacement.java:36-41) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Strongholds are 26.2's only built-in concentric_rings consumer. | of the 20 files in datagen `data/minecraft/worldgen/structure_set/`, strongholds.json is the only one whose `placement.type` is `minecraft:concentric_rings`; every other set is `minecraft:random_spread` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| isPlacementChunk for a concentric_rings set is true iff the once-computed ring_positions list contains (chunk_x, chunk_z). | `ConcentricRingsStructurePlacement.isPlacementChunk` (ConcentricRingsStructurePlacement.java:83-87): `getRingPositionsFor(this)` then `positions.contains(new ChunkPos(sourceX, sourceZ))`, returning false when the list is null | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The village_plains structure's extra jigsaw fields are: start_pool = "village/plains/town_centers", size=6, max_distance_from_center=80, project_start_to_heightmap="WORLD_SURFACE_WG" (an optional Heightmap.Types name, not a boolean), use_expansion_hack=true, terrain_adaptation=beard_thin. | datagen `data/minecraft/worldgen/structure/village_plains.json` confirms `start_pool: "minecraft:village/plains/town_centers"`, `size: 6`, `max_distance_from_center: 80`, `use_expansion_hack: true`, `terrain_adaptation: "beard_thin"` -- but `project_start_to_heightmap` is NOT a boolean `true`: it is `"WORLD_SURFACE_WG"`, a `Heightmap.Types` string in an optional field (`JigsawStructure.CODEC`, JigsawStructure.java:44: `Heightmap.Types.CODEC.optionalFieldOf("project_start_to_heightmap")` -> `Optional<Heightmap.Types>`; absent means no projection). The file also carries `start_height: { absolute: 0 }` and `step: surface_structures` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ancient_city structure's extra jigsaw fields are: start_pool = "ancient_city/city_center", start_jigsaw_name = "city_anchor", start_height.absolute = -27, size=7, max_distance_from_center=116, step=underground_decoration, terrain_adaptation=beard_box. | datagen `data/minecraft/worldgen/structure/ancient_city.json`: `start_pool: "minecraft:ancient_city/city_center"`, `start_jigsaw_name: "minecraft:city_anchor"`, `start_height: { absolute: -27 }`, `size: 7`, `max_distance_from_center: 116`, `step: "underground_decoration"`, `terrain_adaptation: "beard_box"` (and `use_expansion_hack: false`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| STRUCTURE_STARTS generation skips a structure set entirely when placement.is_structure_chunk(world_seed, x, z) is false. | `ChunkGenerator.createStructures` (world/level/chunk/ChunkGenerator.java:494) gates the whole per-set body on `featurePlacement.isStructureChunk(state, sourceChunkPos.x(), sourceChunkPos.z())`; the seed reaches it through `state.getLevelSeed()` rather than as an explicit argument | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For a multi-structure StructureSet, weighted_draw_order computes a draw-without-replacement candidate order and each candidate is tried in turn until one succeeds or the list is exhausted. | `ChunkGenerator.createStructures` (ChunkGenerator.java:508-550): a copy of the entries, a running `total` weight, then a loop that draws, tries `tryGenerateStructure`, returns on success, and otherwise removes the entry and decrements `total`. The draws are interleaved with the attempts rather than precomputed, but `tryGenerateStructure` never touches this `random`, so the resulting candidate order is identical to a draw-without-replacement order computed up front | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's multi-structure StructureSets are villages, nether_complexes, ocean_ruins, ruined_portals, shipwrecks, and mineshafts. | of the 20 files in datagen `data/minecraft/worldgen/structure_set/`, exactly these six have more than one entry in `structures`: villages (5), nether_complexes (2), ocean_ruins (2), ruined_portals (7), shipwrecks (2), mineshafts (2) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| find_valid_generation_point's biome check tests the structure generator's returned stub position against the structure's allowed biomes at quart resolution (biome_at_quart); a failing candidate in a multi-structure set causes the next weighted candidate to be tried rather than aborting the whole set. | `Structure.findValidGenerationPoint` (world/level/levelgen/structure/Structure.java:204-206) filters the stub through `isValidBiome` (:131-141), which samples `getNoiseBiome(QuartPos.fromBlock(x), QuartPos.fromBlock(y), QuartPos.fromBlock(z), sampler)` at the stub's own position; a false result makes `Structure.generate` (:101-118) return `INVALID_START`, so `tryGenerateStructure` (ChunkGenerator.java:588-593) returns false and `createStructures` (:548-549) removes that entry and draws the next candidate | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| weighted_draw_order seeds its RNG via set_large_feature_seed(world_seed, chunk_x, chunk_z) -> using world_seed directly, unsalted -> then repeatedly draws r = rng.next_int_bounded(total_remaining_weight) and picks the first remaining entry whose cumulative weight exceeds r, removing it from the remaining pool each iteration. | `ChunkGenerator.createStructures` (ChunkGenerator.java:511-512) builds `new WorldgenRandom(new LegacyRandomSource(0L))` and calls `random.setLargeFeatureSeed(state.getLevelSeed(), sourceChunkPos.x(), sourceChunkPos.z())` -- the unsalted variant; then (:519-530, :548-549) `choice = random.nextInt(total)`, subtract each remaining entry's weight until `choice < 0`, take that index, remove it and subtract its weight from `total` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| STRUCTURE_REFERENCES scans the full 17x17 neighborhood (+/-MAX_STRUCTURE_DISTANCE = 8 chunks in both axes) and records a reference for every neighbor's already-generated StructureStart whose bounding box intersects the chunk's own 16x16 footprint. | `ChunkGenerator.createReferences` (ChunkGenerator.java:603-632): `int range = 8`, loops `sourceX` and `sourceZ` over `target +/- 8` (17x17 chunks), and for each already-generated start calls `addReferenceForStructure` when `start.isValid() && start.getBoundingBox().intersects(targetBlockX, targetBlockZ, targetBlockX + 15, targetBlockZ + 15)`. The named constant `MAX_STRUCTURE_DISTANCE = 8` lives in world/level/chunk/status/ChunkStatus.java:16; `createReferences` uses the literal 8 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| StructureStart::references is capped at MAX_REFERENCES = 1, vanilla's own base-class default never overridden by any structure family. | `StructureStart.getMaxReferences` (world/level/levelgen/structure/StructureStart.java:138-140) returns 1 and `canBeReferenced` (:126-128) is `references < getMaxReferences()`. `StructureStart` is declared `final` in 26.2 (:22), so no family can override it. There is no constant literally named MAX_REFERENCES in the reference -- the value lives in that method | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Template-pool construction flattens weights into literal list repetition (copying each element `weight` times) rather than a cumulative-weight scan, so get_random_template is a plain next_int_bounded(flattened.len()) draw. | `StructureTemplatePool` constructor (StructureTemplatePool.java:48-61) appends each element `weight` times into `this.templates`; `getRandomTemplate` (:105-107) is `templates.get(random.nextInt(templates.size()))`, returning `EmptyPoolElement.INSTANCE` for an empty list | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| get_shuffled_templates performs a Java Collections.shuffle-style Fisher-Yates shuffle of the flattened pool list: for i from len-1 down to 1, swap list[i] with list[rng.next_int_bounded(i+1)]. | `StructureTemplatePool.getShuffledTemplates` (StructureTemplatePool.java:109-111) -> `Util.shuffledCopy` -> `Util.shuffle` (util/Util.java:1110-1117): `for (int i = size; i > 1; i--) { int swapTo = random.nextInt(i); swap(list[i-1], list[swapTo]); }`, i.e. exactly j from len-1 down to 1 swapping with `nextInt(j+1)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla has five PoolElement kinds: SinglePoolElement, LegacySinglePoolElement, ListPoolElement, FeaturePoolElement, and EmptyPoolElement. | `StructurePoolElementType` (StructurePoolElementType.java:8-12) registers exactly five: `single_pool_element` (SinglePoolElement), `list_pool_element`, `feature_pool_element`, `empty_pool_element`, `legacy_single_pool_element` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| LegacySinglePoolElement auto-appends BlockIgnoreProcessor::STRUCTURE_AND_AIR instead of ::STRUCTURE_BLOCK at placement, unlike SinglePoolElement. | `LegacySinglePoolElement.getSettings` (LegacySinglePoolElement.java:31-39) takes the super's settings, then `settings.popProcessor(BlockIgnoreProcessor.STRUCTURE_BLOCK)` and `settings.addProcessor(BlockIgnoreProcessor.STRUCTURE_AND_AIR)`; `SinglePoolElement.getSettings` (SinglePoolElement.java:172) adds `STRUCTURE_BLOCK`. Note the swap also moves that processor to the END of the settings list | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ListPoolElement places every sub-element at the same anchor position, and its jigsaw connectors come only from its first sub-element (elements[0]). | `ListPoolElement.place` (ListPoolElement.java:84-92) passes the same `position` to every sub-element, and `getShuffledJigsawBlocks` (:53-58) delegates to `this.elements.get(0)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Picking an EmptyPoolElement as the very first center-pool draw aborts structure generation entirely. | `JigsawPlacement.addPieces` (pools/JigsawPlacement.java:76-79): `centerElement = centerPool.getRandomTemplate(random); if (centerElement == EmptyPoolElement.INSTANCE) return Optional.empty();` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Projection::TerrainMatching implicitly appends a GravityProcessor{heightmap: "WORLD_SURFACE_WG", offset: -1} to an element's processor chain at placement time; Projection::Rigid appends nothing. | `StructureTemplatePool.Projection` (StructureTemplatePool.java:117-119): TERRAIN_MATCHING carries `ImmutableList.of(new GravityProcessor(Heightmap.Types.WORLD_SURFACE_WG, -1))`, RIGID carries `ImmutableList.of()`; `SinglePoolElement.getSettings` (SinglePoolElement.java:180) appends `getProjection().getProcessors()` last of all | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Pool aliases are resolved once per structure-start attempt, before any pool lookup, using a random source created via RandomSource::create(seed) (equivalently LegacyRandomSource::new(seed)) positioned at the structure-start position. | `JigsawStructure.findGenerationPoint` (structures/JigsawStructure.java:159) evaluates `PoolAliasLookup.create(this.poolAliases, startPos, context.seed())` once, as an argument to `addPieces`, so before any pool lookup; `PoolAliasLookup.create` (pools/alias/PoolAliasLookup.java:19-31) builds `RandomSource.create(seed).forkPositional().at(pos)` and `RandomSource.create(long)` is `new LegacyRandomSource(seed)` (util/RandomSource.java:23-25). Worth noting for a reimplementation: `forkPositional()` is not free -- it consumes one `nextLong()` from the base source (LegacyRandomSource.java:26-29) and the final source is `new LegacyRandomSource(Mth.getSeed(x,y,z) ^ forkSeed)` (:63-68) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A PoolAliasBinding::Random draws its chosen target once from a weighted list using the same flatten-then-pick discipline as template pools; a PoolAliasBinding::RandomGroup draws its chosen group the same way and then resolves every binding inside that one chosen group using the same random stream, which is what correlates paired aliases such as trial chambers' matched spawner mob variants. | `RandomPoolAlias.forEachResolved` (pools/alias/RandomPoolAlias.java:26-31) -> `this.targets.getRandomOrThrow(random)`; `RandomGroupPoolAlias.forEachResolved` (RandomGroupPoolAlias.java:23-28) -> `this.groups.getRandomOrThrow(random).forEach(binding -> binding.forEachResolved(random, ...))`, threading the SAME random through every binding of the chosen group. `WeightedList.getRandomOrThrow` (util/random/WeightedList.java:85-92) is a single `random.nextInt(totalWeight)` mapped through a Flat selector below weight 64 and a cumulative Compact selector at or above it -- observationally the same single draw as the template pools' flatten-then-pick. datagen `data/minecraft/worldgen/structure/trial_chambers.json` carries one `minecraft:random_group` alias whose groups pair `spawner/contents/ranged` and `spawner/contents/slow_ranged` targets, plus two `minecraft:random` aliases | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| An unresolved pool-alias lookup passes the original name through unchanged. | `PoolAliasLookup.create` (pools/alias/PoolAliasLookup.java:28-30) returns `resourceKey -> aliasMappings.getOrDefault(resourceKey, resourceKey)`, and `PoolAliasLookup.EMPTY` (:15) is `key -> key` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| add_pieces returns None immediately if the center pool's random draw selects an EmptyPoolElement. | `JigsawPlacement.addPieces` (pools/JigsawPlacement.java:76-79) returns `Optional.empty()` on `centerElement == EmptyPoolElement.INSTANCE`, before any piece is constructed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When ctx.start_jigsaw_name is set, add_pieces re-anchors the origin so that the named jigsaw block in the center element lands exactly at ctx.origin. | `JigsawPlacement.addPieces` (pools/JigsawPlacement.java:82-100): `getRandomNamedJigsaw` (:177-192) finds the named jigsaw's position with the element notionally placed at `position`, then `localAnchorPosition = anchoredPosition.subtract(position)` and `adjustedPosition = position.subtract(localAnchorPosition)`, so the named block lands back on `position` in X and Z. Note that the piece is then moved vertically at :113-117 so its ground level sits at `bottomY`, and the reported stub Y is `bottomY + localAnchorPosition.getY()` (:125) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When ctx.project_start_to_heightmap names a heightmap, the center piece's bounding box is translated vertically so its ground level (bounding_box.min.y + ground_level_delta) sits on that named heightmap sampled at the piece's own bounding-box centre column, offset by the origin's own Y. | `JigsawPlacement.addPieces` (pools/JigsawPlacement.java:110-117) computes `centerX = (box.maxX() + box.minX()) / 2`, `centerZ = (box.maxZ() + box.minZ()) / 2`, then `bottomY = position.getY() + chunkGenerator.getFirstFreeHeight(centerX, centerZ, projectStartToHeightmap.get(), ...)`, `oldAbsoluteGroundY = box.minY() + centerPiece.getGroundLevelDelta()`, `centerPiece.move(0, bottomY - oldAbsoluteGroundY, 0)`. Four divergences from the claim: the heightmap is whatever `project_start_to_heightmap` names (not hard-coded WORLD_SURFACE_WG); the sampled column is the centre of the piece's bounding box, not the origin column; what is placed on that height is the piece's GROUND LEVEL (`minY + groundLevelDelta`), not its minimum Y; and the height is offset by `position.getY()` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| If ctx.max_depth is 0, add_pieces returns just the center piece with no further expansion attempted. | `JigsawPlacement.addPieces` (pools/JigsawPlacement.java:132) wraps the whole child-expansion block in `if (maxDepth > 0)`, so the stub's builder receives only `centerPiece` (added at :131) when maxDepth is 0 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| try_placing_children shuffles a piece's own jigsaw blocks via collections_shuffle and then stable-sorts them by descending selection_priority, so ties preserve the shuffle order. | `JigsawPlacement.Placer.tryPlacingChildren` (pools/JigsawPlacement.java:319-321) iterates `sourceElement.getShuffledJigsawBlocks(...)`, which for a single element is `SinglePoolElement.getShuffledJigsawBlocks` (SinglePoolElement.java:114-122): `Util.shuffle(jigsaws, random)` then `sortBySelectionPriority` (:124-127) with `HIGHEST_SELECTION_PRIORITY_FIRST = Comparator.comparingInt(JigsawBlockInfo::selectionPriority).reversed()` (:39-42). `List.sort` is a stable merge sort, so equal priorities keep the shuffle order | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Both the source piece's and each candidate's jigsaw-block list are shuffled during child placement, via the same shuffle-then-priority-sort method, the candidate's shuffle run once per (candidate, rotation) pair from the shared RNG stream. | `JigsawPlacement.Placer.tryPlacingChildren` (pools/JigsawPlacement.java:367-369) obtains the candidate's jigsaw blocks via `targetElement.getShuffledJigsawBlocks(this.structureTemplateManager, BlockPos.ZERO, targetRotation, this.random)` -- the same shuffle-then-priority-sort method used for the source piece (SinglePoolElement.java:114-122). The candidate's own jigsaw blocks ARE shuffled, once per (candidate, rotation) pair, and that shuffle consumes the shared RNG stream | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For each candidate pool element, a fresh shuffle of the four Rotation values is drawn before trying rotations. | `JigsawPlacement.Placer.tryPlacingChildren` (pools/JigsawPlacement.java:366) calls `Rotation.getShuffled(this.random)` inside the `for (StructurePoolElement targetElement : targetPieces)` loop; `Rotation.getShuffled` (world/level/block/Rotation.java:116-118) is `Util.shuffledCopy(values(), random)` over all four constants | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| can_attach requires source.front == target.front.opposite() AND (source.joint == Rollable OR source.top == target.top) AND source.target == target.name; only the source's own target field is checked against the candidate's name -> the candidate's own target field is never consulted. | `JigsawBlock.canAttach` (world/level/block/JigsawBlock.java:80-88): `sourceFront == targetFront.getOpposite() && (rollable  or  or  sourceTop == targetTop) && source.target().equals(target.name())`, where `rollable` is `source.jointType() == JointType.ROLLABLE`. The candidate's own `target()` is never read | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vertical placement reuses the source piece's own free-space shape (rather than the ambient shared free space) whenever the target jigsaw's connector position falls inside the source piece's own bounding box. | `JigsawPlacement.Placer.tryPlacingChildren` (pools/JigsawPlacement.java:342-351): `attachInsideSource = sourceBB.isInside(targetJigsawPos)`; when true `childrenFree = sourceFree`, lazily initialised to `Shapes.create(AABB.of(sourceBB))`, otherwise `childrenFree = contextFree` (the shared ambient shape) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Any placement involving a TerrainMatching-projected piece re-samples the heightmap at the source jigsaw block's own column and anchors the piece relative to that surface height. | `JigsawPlacement.Placer.tryPlacingChildren` (pools/JigsawPlacement.java:401-412): only when `sourceRigid && targetRigid` is `targetBoxY = sourceBoxY + deltaY`; otherwise `sourceJigsawBaseHeight = chunkGenerator.getFirstFreeHeight(sourceJigsawPos.getX(), sourceJigsawPos.getZ(), Heightmap.Types.WORLD_SURFACE_WG, ...)` and `targetBoxY = sourceJigsawBaseHeight - targetJigsawLocalY`. The sample is cached per source jigsaw via the `Integer.MIN_VALUE` sentinel (:327, :404) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The expansion hack -- set on every village variant and on pillager_outpost, not village-only -- inflates any candidate whose own box is 16 blocks tall or less, using each pool's cached maximum non-empty element Y-span, to reserve headroom before the collision test. | the mechanics match exactly -- `JigsawPlacement.Placer.tryPlacingChildren` (pools/JigsawPlacement.java:372) gates on `doExpansionHack && hackBox.getYSpan() <= 16`, `expandTo` is the max over the candidate's jigsaws of `max(childPool.getMaxSize(), childFallbackPool.getMaxSize())` (:373-385) where `StructureTemplatePool.getMaxSize` (StructureTemplatePool.java:83-94) caches the largest Y-span over non-`EmptyPoolElement` entries, and the inflation at :417-420 precedes the collision test at :422. But it is not village-only: `grep -l '"use_expansion_hack": true'` over datagen `data/minecraft/worldgen/structure/` matches village_plains, village_desert, village_savanna, village_snowy, village_taiga AND pillager_outpost.json | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's minimum jigsaw recursion depth (MIN_DEPTH) is 0. | `JigsawStructure.MIN_DEPTH` (structures/JigsawStructure.java:34) `= 0`, matching the `Codec.intRange(0, 20)` lower bound of the `size` field (:41) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's maximum jigsaw recursion depth cap (MAX_DEPTH_CAP) is 20, the datapack's own size field codec range. | the value is 20 and the codec range is `Codec.intRange(0, 20).fieldOf("size")` (structures/JigsawStructure.java:41); vanilla names the constant `MAX_DEPTH` (:35), not MAX_DEPTH_CAP | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's maximum total structure range (MAX_TOTAL_STRUCTURE_RANGE) is 128 blocks. | `JigsawStructure.MAX_TOTAL_STRUCTURE_RANGE` (structures/JigsawStructure.java:33) `= 128`, enforced by `verifyRange` (:64-72) and mirrored by `MaxDistance.HORIZONTAL_VALUE_CODEC = Codec.intRange(1, 128)` (:181) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A non-None TerrainAdaptation applies a terrain_adaptation_edge_padding of 12 blocks to a structure's reported bounding box for range-check purposes (0 for TerrainAdaptation::None); this inflation is reporting-only and never changes a piece's actually-placed geometry. | `Structure.adjustBoundingBox` (world/level/levelgen/structure/Structure.java:79-81) returns `boundingBox.inflatedBy(12)` when `terrainAdaptation() != TerrainAdjustment.NONE` and the box unchanged otherwise; it is called only from `StructureStart.getBoundingBox` (StructureStart.java:69-77), i.e. the reported box, never the placed geometry. `JigsawStructure.verifyRange` (:64-68) uses the same 12 (0 for NONE) in its range check | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's TerrainAdaptation enum has five values: None, Bury, BeardThin, BeardBox, Encapsulate. | `TerrainAdjustment` (world/level/levelgen/structure/TerrainAdjustment.java:6-11): NONE("none"), BURY("bury"), BEARD_THIN("beard_thin"), BEARD_BOX("beard_box"), ENCAPSULATE("encapsulate"). Vanilla names the enum `TerrainAdjustment`; `terrain_adaptation` is the datapack field name | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The beardifier's kernel radius is 12 blocks, backed by a precomputed 24^3 (13824-entry) kernel. | `Beardifier.BEARD_KERNEL_RADIUS = 12`, `BEARD_KERNEL_SIZE = 24`, `BEARD_KERNEL = new float[13824]` filled over a 24x24x24 zi/xi/yi triple loop indexed `zi*24*24 + xi*24 + yi` (world/level/levelgen/Beardifier.java:22-32) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The beardifier converts nearby rigid structure pieces and jigsaw junctions into a density perturbation so terrain does not clip through them, and vanilla defines four TerrainAdjustment behaviors for it. | `Beardifier.forStructuresInChunk` (Beardifier.java:38-88) collects RIGID-projected pool pieces and non-pool pieces as `Rigid` records plus the nearby `JigsawJunction`s, and `Beardifier.compute` (:110-156) turns them into a density term. Note the enum itself has five values (TerrainAdjustment.java:6-11): NONE is filtered out at :39 and contributes 0.0 at :141, leaving the four behaviours BURY, BEARD_THIN, BEARD_BOX and ENCAPSULATE that actually produce a contribution | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Only a structure piece adapted with TerrainAdaptation::None contributes nothing to the beardifier density perturbation (and never even reaches it, filtered out upstream); ::Encapsulate contributes a real, non-zero getBuryContribution(dx/2, dy/2, dz/2)*0.8 perturbation. | `Beardifier.compute` (Beardifier.java:140-145): only the `NONE` arm returns 0.0 (:141), and NONE-adapted starts never even reach the beardifier (`forStructuresInChunk` filters on `terrainAdaptation() != TerrainAdjustment.NONE`, :39). `ENCAPSULATE` contributes `getBuryContribution(dx / 2.0, dy / 2.0, dz / 2.0) * 0.8` (:144) with `dy = Math.max(0, Math.max(box.minY() - blockY, blockY - box.maxY()))` (:137) -- a real, non-zero perturbation | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A structure NBT template's root compound contains size (3 ints), palette or palettes (a list of block-state palette entries, plural when multiple randomly-selectable palette variants are shipped, one resolved per placement via get_random_palette), blocks (each {pos: [x,y,z], state: paletteIndex, nbt?: block-entity compound}), entities (each {pos: [3 doubles], blockPos: [3 ints], nbt: entity compound}), and DataVersion (int). | `StructureTemplate.save` (templatesystem/StructureTemplate.java:630-705) and the key constants at :69-79: `size` (a 3-element int list, :704), `palette` or `palettes` (:672, :686), `blocks` entries `{pos: [x,y,z], state: <palette index>, nbt?}` (:649-664), `entities` entries `{pos: [3 doubles], blockPos: [3 ints], nbt}` (:694-703), and `DataVersion` via `NbtUtils.addCurrentDataVersion` (:705). The plural `palettes` form is resolved per placement by `StructurePlaceSettings.getRandomPalette` (StructurePlaceSettings.java:141-148), used at StructureTemplate.java:207, :233, :267 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A structure template's on-disk location inside the vanilla server jar is data/<namespace>/structure/<path>.nbt. | `StructureTemplateManager` (templatesystem/StructureTemplateManager.java:41-47): `STRUCTURE_DIRECTORY_NAME = "structure"`, `STRUCTURE_FILE_EXTENSION = ".nbt"`, and `RESOURCE_STRUCTURE_LISTER = new FileToIdConverter("structure", ".nbt")`, i.e. `data/<namespace>/structure/<path>.nbt` (util/filefix/fixes/GeneratedStructuresRenameFileFix.java:16 is the rename from the older `structures/` directory) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| StructureTemplate.placeInWorld resolves the palette, runs every block through the processor pipeline, places blocks (setting a temporary marker state first for any block carrying block-entity NBT to avoid a transient invalid state), runs a flood-fill liquid-reconciliation pass over any fluid displaced by the template, recalculates shape at the placed region's edge (skipped when settings.known_shape is true), and finally places entities with position and yaw transformed by rotation and mirror and a freshly assigned entity id. | `StructureTemplate.placeInWorld` (templatesystem/StructureTemplate.java:255-413): palette resolved at :267, `processBlockInfos` at :282, the barrier marker for NBT-carrying blocks at :290-292 (`setBlock(pos, Blocks.BARRIER.defaultBlockState(), 820)`), the fluid flood-fill loop at :329-357, `updateShapeAtEdge` plus the neighbour-shape pass guarded by `!settings.getKnownShape()` at :359-384, and `placeEntities` at :395-406. `placeEntities` (:487-523) transforms `blockPos` and `pos` by mirror/rotation/pivot, computes `yRot` through `entity.rotate(rotation)` and `entity.mirror(mirror)` (:510-514), and removes the stored `UUID` (:508) so a fresh one is assigned | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's StructureTemplateManager caches loaded templates unbounded, but not invalidated only on restart: a datapack/resource reload clears the whole cache, and a single-entry remove is also available. | the cache is indeed an unbounded `ConcurrentHashMap` with no eviction policy (`structureRepository`, templatesystem/StructureTemplateManager.java:48, filled via `computeIfAbsent` at :95), but it is not invalidated only on restart: `onResourceManagerReload` (:116-119) calls `this.structureRepository.clear()` -- so any datapack/resource reload (`/reload`) drops the whole cache -- and `remove(Identifier)` (:172-174) evicts a single entry | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A StructureProcessor's process_block may replace, keep, or cancel (return None for) a block as it is stamped; a processor that also implements finalize_processing and reports evaluates_entire_piece_state() == true switches the whole pipeline from a chunk-bounds-clipped fast path to a whole-piece slow path -> only the Capped processor needs this. | `StructureProcessor` (templatesystem/StructureProcessor.java:11-37): `processBlock` returns a `@Nullable StructureBlockInfo` (null cancels), plus `finalizeProcessing` and `evaluatesEntirePieceState`. `StructureTemplate.processBlockInfos` (StructureTemplate.java:450-457) sets `processOnlyInCurrentChunk = false` as soon as any processor reports true, which drops the `chunkBb.isInside(blockPos)` clip at :463. `CappedProcessor` (CappedProcessor.java:37-40) is the only override in the whole reference tree | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlackstoneReplace performs an unconditional 1:1 substitution table of roughly 20 mappings (cobblestone to blackstone, stone to polished blackstone, stone bricks to polished blackstone bricks plus their stairs/slabs/walls, chiseled and cracked variants, iron bars to iron chain), preserving stair/slab shape properties. | `BlackstoneReplaceProcessor` (templatesystem/BlackstoneReplaceProcessor.java:19-43) is an unconditional 23-entry block-to-block map covering cobblestone/mossy cobblestone -> blackstone, stone -> polished blackstone, stone bricks and mossy stone bricks -> polished blackstone bricks, the matching stairs/slabs/walls, CHISELED_STONE_BRICKS -> CHISELED_POLISHED_BLACKSTONE, CRACKED_STONE_BRICKS -> CRACKED_POLISHED_BLACKSTONE_BRICKS and IRON_BARS -> IRON_CHAIN. `processBlock` (:48-77) copies StairBlock.FACING, StairBlock.HALF and SlabBlock.TYPE onto the new default state -- note the stair SHAPE property is NOT carried over | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlockAge{mossiness} applies, per block type, a 50% chance to swap full stone-brick blocks for cracked or stairs variants (further split mossy vs non-mossy by mossiness), a 50% chance to age stairs, a mossiness-scaled chance to moss slabs/walls, and a flat 15% chance to turn obsidian into crying obsidian. | `BlockAgeProcessor` (templatesystem/BlockAgeProcessor.java:23-98): PROBABILITY_OF_REPLACING_FULL_BLOCK = 0.5F and PROBABILITY_OF_REPLACING_STAIRS = 0.5F guard `maybeReplaceFullStoneBlock` (:63-75, for STONE_BRICKS/STONE/CHISELED_STONE_BRICKS, picking cracked stone bricks or random-facing stone-brick stairs, or the mossy pair, via `getRandomBlock` splitting on `random.nextFloat() < mossiness`, :106-108) and `maybeReplaceStairs` (:77-86); `maybeReplaceSlab` (:88-90) and `maybeReplaceWall` (:92-94) are plain `random.nextFloat() < this.mossiness` gates; PROBABILITY_OF_REPLACING_OBSIDIAN = 0.15F in `maybeReplaceObsidian` (:96-98) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlockIgnore{blocks} drops any block matching its list. | `BlockIgnoreProcessor.processBlock` (templatesystem/BlockIgnoreProcessor.java:30-40) returns null when `toIgnore.contains(processedBlockInfo.state().getBlock())`; the codec field is `blocks` (:19) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlockRot{rottable_blocks, integrity} performs a per-block coin flip seeded from Mth::get_seed(world_pos), gated to rottable_blocks when set, dropping the block; integrity is the keep probability. | `BlockRotProcessor` (templatesystem/BlockRotProcessor.java:40-53): `RandomSource random = settings.getRandom(processedBlockInfo.pos())` and `StructurePlaceSettings.getRandom` (StructurePlaceSettings.java:113-119) is `RandomSource.create(Mth.getSeed(pos))` unless the settings carry an explicit random; the block is dropped when `(rottableBlocks absent  or  or  state.is(rottableBlocks)) && !(random.nextFloat() <= integrity)`, so `integrity` is the keep probability. Codec fields `rottable_blocks` (optional) and `integrity` (:19-20) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Capped{delegate, limit} runs delegate against only a random limit-sized subset of the whole piece's blocks and reports evaluates_entire_piece_state() == true. | `CappedProcessor` (templatesystem/CappedProcessor.java:17-88): codec fields `delegate` and `limit` (an IntProvider), `evaluatesEntirePieceState()` returns true (:37-40), and `finalizeProcessing` (:42-88) samples `maxToReplace = min(limit.sample(random), size)` from a positional random at the piece position (:62-63), walks a shuffled index list (:68) and applies the delegate until `replaced` reaches that cap | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Gravity{heightmap, offset} re-projects a block onto the live-world heightmap at its column plus offset, preserving the block's relative template height. | `GravityProcessor.processBlock` (templatesystem/GravityProcessor.java:28-56): `height = level.getHeight(heightmap, pos.getX(), pos.getZ()) + this.offset`, `delta = templateRelativePos.getY()`, new position `(pos.getX(), height + delta, pos.getZ())`. Worth noting: on a `ServerLevel` the WG heightmaps are swapped for their live counterparts (WORLD_SURFACE_WG -> WORLD_SURFACE, OCEAN_FLOOR_WG -> OCEAN_FLOOR, :38-45). Codec fields `heightmap` (default WORLD_SURFACE_WG) and `offset` (default 0) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| JigsawReplacement replaces every placed JIGSAW block with the state parsed from its final_state tag (or air), unless keep_jigsaws is set; a final_state of structure_void drops the block entirely, and this processor is auto-appended by SinglePoolElement and LegacySinglePoolElement unless keep_jigsaws. | `JigsawReplacementProcessor.processBlock` (templatesystem/JigsawReplacementProcessor.java:24-55): non-JIGSAW blocks pass through, otherwise the state is parsed from `nbt.getStringOr("final_state", "minecraft:air")` and a resulting STRUCTURE_VOID returns null (block dropped). `SinglePoolElement.getSettings` (SinglePoolElement.java:175-177) adds `JigsawReplacementProcessor.INSTANCE` only when `!keepJigsaws`, and `LegacySinglePoolElement` inherits that (LegacySinglePoolElement.java:35) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| LavaSubmergedBlock forces a placed block to remain lava if the target world position is already lava and the incoming block is not a full cube. | `LavaSubmergedBlockProcessor.processBlock` (templatesystem/LavaSubmergedBlockProcessor.java:17-31): when the existing world block at the target position `is(Blocks.LAVA)` and `!Block.isShapeFullBlock(processedBlockInfo.state().getShape(level, pos))`, the block is replaced by `Blocks.LAVA.defaultBlockState()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ProtectedBlocks{value} cancels placement if the existing world block at the target position matches its protected set. | `ProtectedBlockProcessor.processBlock` (templatesystem/ProtectedBlockProcessor.java:22-32) returns null when `level.getBlockState(processedBlockInfo.pos()).is(this.cannotReplace)`; the codec field is literally `value` (:15) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| RuleProcessor seeds its per-block RNG from Mth::get_seed(world_pos) -> deterministic per world position, independent of processing order. | `RuleProcessor.processBlock` (templatesystem/RuleProcessor.java:33) builds `RandomSource.create(Mth.getSeed(processedBlockInfo.pos()))` fresh per block, so the stream depends only on the world position | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's RuleTest has six kinds: AlwaysTrue, BlockMatch (exact block match ignoring state properties), BlockstateMatch (exact state match), TagMatch, RandomBlockMatch, and RandomBlockstateMatch (the latter two ANDed with rng.next_float() < probability). | `RuleTestType` (templatesystem/RuleTestType.java:8-13) registers exactly six: always_true, block_match, blockstate_match, tag_match, random_block_match, random_blockstate_match. `BlockMatchTest` matches by block (ignoring properties), `BlockStateMatchTest.test` (:16-18) is `blockState == this.blockState`, and `RandomBlockMatchTest.test` (:26-29) is `blockState.is(this.block) && random.nextFloat() < this.probability` (RandomBlockStateMatchTest likewise) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's PosRuleTest has three kinds: AlwaysTrue, LinearPos (probability linearly interpolated over Manhattan distance from a reference position between min_chance/max_chance and min_dist/max_dist), and AxisAlignedLinearPos (the same interpolation measured along one axis only). | `PosRuleTestType` (templatesystem/PosRuleTestType.java:28-30) registers exactly three: always_true, linear_pos, axis_aligned_linear_pos. `LinearPosTest.test` (:37-41) uses `worldPos.distManhattan(worldReference)` and `random.nextFloat() <= Mth.clampedLerp(Mth.inverseLerp(dist, minDist, maxDist), minChance, maxChance)`; `AxisAlignedLinearPosTest.test` (:41-49) measures the same distance along the single configured axis | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| run_processor_list runs the two auto-appended processors first (BlockIgnore, then JigsawReplacement when not keep_jigsaws), then the datapack's own declared order, and finally the projection's own processors (the TERRAIN_MATCHING GravityProcessor) last. | `SinglePoolElement.getSettings` (SinglePoolElement.java:164-182) builds the list in this order: `BlockIgnoreProcessor.STRUCTURE_BLOCK` (:172), then `JigsawReplacementProcessor.INSTANCE` when `!keepJigsaws` (:175-177), THEN the datapack's own `processors.value().list()` (:179), and finally the projection's processors (the TERRAIN_MATCHING GravityProcessor, :180). The two auto-appended processors therefore run FIRST, not last, and the projection's GravityProcessor is the one that runs last. `StructureTemplate.processBlockInfos` (:467-471) then runs them in that stored order | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold structure family uses concentric_rings placement plus a retry-until-portal-room weighted-piece expansion with MAX_DEPTH=50, a random-index (neither FIFO nor LIFO) pending-piece drain, and a retry loop that adds its counter into the seed argument of set_large_feature_seed rather than using it as a salt. | `StrongholdStructure.generatePieces` (structures/StrongholdStructure.java:23-44) does use concentric_rings placement (strongholds.json), the weighted piece table (`StrongholdPieces.PieceWeight[]`, StrongholdPieces.java:44-68, selected by `random.nextInt(totalWeight)` at :166-168), `MAX_DEPTH = 50` (StrongholdPieces.java:41) and a `do { ... } while (builder.isEmpty()  or  or  startRoom.portalRoomPiece == null)` retry. Two divergences: the expansion is NOT breadth-first -- the pending list is drained by `int pos = context.random().nextInt(pendingChildren.size()); pendingChildren.remove(pos)` (:37-38), a random-index drain; and the retry counter is added to the SEED, `setLargeFeatureSeed(context.seed() + tries++, chunkX, chunkZ)` (:29), not used as a salt argument | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The nether fortress (fortress) structure family uses a dual weighted-piece grammar (separate bridge and castle piece tables) with MAX_DEPTH=30, fixed Y-band placement, and random_spread placement via the nether_complexes structure set shared with bastion_remnant. | `NetherFortressPieces` (structures/NetherFortressPieces.java:30-48) declares `MAX_DEPTH = 30`, `BRIDGE_PIECE_WEIGHTS` (:33) and `CASTLE_PIECE_WEIGHTS` (:41) as two separate weighted tables; `NetherFortressStructure.findGenerationPoint` (:30-35) fixes the start at y=64 (`MAGIC_START_Y`, NetherFortressPieces.java:32) and `generatePieces` ends with `builder.moveInsideHeights(context.random(), 48, 70)` (:51). datagen structure_set/nether_complexes.json is a `random_spread` set holding `minecraft:fortress` (weight 2) and `minecraft:bastion_remnant` (weight 3) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The woodland_mansion structure family uses a fixed 11x11 room grid with recursive corridor carving and three per-floor room collections, under random_spread placement. | `WoodlandMansionPieces` (structures/WoodlandMansionPieces.java:132-148): `houseSize = 11`, `baseGrid = new SimpleGrid(11, 11, 5)` and four `recursiveCorridor(...)` calls; the three per-floor grids at :154-156 and the three collections `FirstFloorRoomCollection`, `SecondFloorRoomCollection`, `ThirdFloorRoomCollection` at :472-476. datagen structure_set/woodland_mansions.json is `random_spread` (spacing 80, separation 20, triangular) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ocean_monument structure family is a single procedural room-grid piece gated by a full-surrounding-biome check over a three-dimensional quart-resolution box of radius 29 blocks (roughly 59 blocks across on each axis, not a flat 29x29 square), under random_spread placement. | `OceanMonumentStructure.findGenerationPoint` (structures/OceanMonumentStructure.java:29-42) calls `context.biomeSource().getBiomesWithin(chunkPos.getBlockX(9), chunkGenerator.getSeaLevel(), chunkPos.getBlockZ(9), 29, sampler)` and rejects unless EVERY returned biome `is(BiomeTags.REQUIRED_OCEAN_MONUMENT_SURROUNDING)`. The 29 is a RADIUS in blocks, and `BiomeSource.getBiomesWithin` (world/level/biome/BiomeSource.java:44-...) sweeps a three-dimensional quart-resolution box from `QuartPos.fromBlock(c - 29)` to `QuartPos.fromBlock(c + 29)` on all three axes -- roughly 59 blocks across in X, Y and Z, not a 29x29 square. The rest holds: one `OceanMonumentPieces.MonumentBuilding` piece is added (:44-53) under the `random_spread` ocean_monuments set | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The mineshaft structure family eagerly generates its full piece tree inside find_generation_point (rather than lazily), has normal and mesa skins, and uses a degenerate random_spread placement of spacing=1, separation=0, frequency=0.004. | `MineshaftStructure.findGenerationPoint` (structures/MineshaftStructure.java:35-43) builds its own `StructurePiecesBuilder`, runs `generatePiecesAndAdjust` (:45-65) immediately and returns `Either.right(builder)` -- an eagerly-built piece tree rather than the lazy `Consumer<StructurePiecesBuilder>` form; `MineshaftStructure.Type` is NORMAL/MESA (:72-77); datagen structure_set/mineshafts.json is `spacing: 1`, `separation: 0`, `frequency: 0.004` (with `frequency_reduction_method: legacy_type_3`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The end_city structure family is a recursive tower with chance-based side/ship extensions (no gateway extension), template-driven with no jigsaw involvement, under random_spread placement. | `EndCityPieces` (structures/EndCityPieces.java) is template-driven with no jigsaw involvement and recurses through `HOUSE_TOWER_GENERATOR`/`TOWER_GENERATOR`/`TOWER_BRIDGE_GENERATOR`/fat-tower section generators, with chance-based bridge branches (`random.nextInt(3) == 0` at :115) and a ship (`if (!this.shipCreated && random.nextInt(10 - genDepth) == 0)` at :211-215), under the `random_spread` end_cities set. But there is no gateway extension: the complete template set used by the file is base_floor, base_roof, bridge_end, bridge_gentle_stairs, bridge_piece, bridge_steep_stairs, fat_tower_base, fat_tower_middle, fat_tower_top, second_floor_1/2, second_roof, ship, third_floor_1/2, third_roof, tower_base, tower_piece, tower_top -- no `gateway`, and the string does not appear anywhere in EndCityPieces.java | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| desert_pyramid, jungle_temple, and swamp_hut are each a single procedural ScatteredFeaturePiece under random_spread placement. | `DesertPyramidPiece`, `JungleTemplePiece` and `SwampHutPiece` all `extends ScatteredFeaturePiece` (structures/DesertPyramidPiece.java:23, JungleTemplePiece.java:30, SwampHutPiece.java:25). `DesertPyramidStructure` (:26-31) and `JungleTempleStructure` (:8-13) go through `SinglePieceStructure` (world/level/levelgen/structure/SinglePieceStructure.java:23-33, a single `builder.addPiece`), and `SwampHutStructure.generatePieces` (:23) adds one piece. All three sets (desert_pyramids, jungle_temples, swamp_huts) are `random_spread` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| igloo is template-based with an occasional basement variant of 5..12 pieces (one igloo/bottom laboratory piece plus depth-1 igloo/middle ladder pieces, depth = 4..11, plus the unconditional igloo/top piece), under random_spread placement. | `IglooPieces.addPieces` (structures/IglooPieces.java:50-64) is template-based (igloo/top, igloo/middle, igloo/bottom, :34-36) under the `random_spread` igloos set, and the basement is indeed occasional -- `if (random.nextDouble() < 0.5)`. But it is not a two-piece variant: that branch adds one `igloo/bottom` laboratory piece at `depth * 3` plus `depth - 1` `igloo/middle` ladder pieces, where `depth = random.nextInt(8) + 4` (4..11), and the `igloo/top` piece is added afterwards regardless -- so the basement variant is 5..12 pieces | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ocean_ruin has warm and cold biome-temperature template variants with a cluster probability, under random_spread placement. | `OceanRuinStructure` (structures/OceanRuinStructure.java:17-37, :55-76): codec fields `biome_temp` (the WARM/COLD `Type` enum), `large_probability` and `cluster_probability`; `OceanRuinPieces` uses them at :162 (`isLarge = random.nextFloat() <= structure.largeProbability`) and :165 (`isLarge && random.nextFloat() <= structure.clusterProbability`). Templates are placed via `structureTemplateManager`; the ocean_ruins set is `random_spread` and holds ocean_ruin_cold and ocean_ruin_warm | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| shipwreck is template-based and terrain-oriented, under random_spread placement. | `ShipwreckStructure.findGenerationPoint` (structures/ShipwreckStructure.java:26-30) picks WORLD_SURFACE_WG for beached and OCEAN_FLOOR_WG otherwise, and `generatePieces` (:32-50) adds a random template piece via `ShipwreckPieces.addRandomPiece` then re-anchors it with `calculateBeachedPosition` or `getMeanFirstOccupiedHeight` when it is too big for the worldgen region. The shipwrecks set is `random_spread` and holds shipwreck and shipwreck_beached | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| buried_treasure is a single fixed procedural piece placed at the chunk center on the ocean floor, under a degenerate random_spread placement. | `BuriedTreasureStructure` (structures/BuriedTreasureStructure.java:18-26): `onTopOfChunkCenter(context, Heightmap.Types.OCEAN_FLOOR_WG, ...)` and a single `BuriedTreasurePieces.BuriedTreasurePiece` at `(chunkPos.getBlockX(9), 90, chunkPos.getBlockZ(9))`; the buried_treasures set is the degenerate `random_spread` spacing 1 / separation 0. Note the piece origin is chunk block offset 9, while `onTopOfChunkCenter` (Structure.java:121-129) reports the stub at the true chunk middle (block 8) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| ruined_portal is 7 sibling structures (base, desert, jungle, swamp, mountain, ocean, nether) under one random_spread placement, each carrying a setups list of only 1 or 2 entries, with six vertical-placement modes. | the 7 are sibling STRUCTURES, not Setups on one structure. datagen structure_set/ruined_portals.json holds seven `minecraft:ruined_portal*` structures (base, desert, jungle, swamp, mountain, ocean, nether) under one `random_spread` placement, and each of those seven `RuinedPortalStructure` files carries a `setups` list of only 1 or 2 entries (ruined_portal.json and ruined_portal_mountain.json have 2, the other five have 1; `RuinedPortalStructure.CODEC`, structures/RuinedPortalStructure.java:51-56, is a `nonEmptyList` of `Setup`). `RuinedPortalPiece.VerticalPlacement` (:325-331) does have six modes: on_land_surface, partly_buried, on_ocean_floor, in_mountain, underground, in_nether | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| nether_fossil adds exactly one randomly chosen template out of fourteen fossil variants at one random rotation (no vertical stack of segments), under a near-continuous-grid random_spread placement. | `NetherFossilPieces.addPieces` (structures/NetherFossilPieces.java:46-56) adds exactly ONE piece: a single randomly chosen template out of the fourteen `nether_fossils/fossil_1` .. `fossil_14` identifiers (:26-41), at one random rotation. There is no vertical stack of segments -- `NetherFossilStructure.findGenerationPoint` (:29-55) only walks the noise column down to find the anchor Y. The `near-continuous-grid random_spread` half is right: nether_fossils.json is spacing 2, separation 1 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The structures chunk-NBT compound's root shape is structures: { starts: Compound, References: Compound }. | `SerializableChunkData.packStructureData` (world/level/chunk/storage/SerializableChunkData.java:524-551) builds `outTag` with `starts` (a CompoundTag keyed by structure id, :539) and `References` (a CompoundTag keyed by structure id, :549), and the whole thing is stored under `structures` at :482 (read back at :160, :556, :577) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each References entry is <structureId>: LongArray, where each i64 packs a referencing chunk's (x, z) with x in the low 32 bits and z in the high 32 bits: pack_chunk_pos(x, z) = (z as i64) << 32 or (x as u32 as i64). | `SerializableChunkData.packStructureData` (:542-547) writes `referencesTag.putLongArray(structureId, longSet.toLongArray())` (skipping empty sets); the packing is `ChunkPos.pack` (world/level/ChunkPos.java:76-78) `= x & 4294967295L  or  (z & 4294967295L) << 32`, i.e. x in the low 32 bits and z in the high 32 bits | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| MAX_STRUCTURE_DISTANCE = 8 is the maximum Chebyshev chunk distance for a valid structure-reference entry. | `SerializableChunkData.unpackStructureReferences` (:586-594) drops any entry whose `ChunkPos.unpack(chunkLongPos).getChessboardDistance(pos) > 8` (Chebyshev chunk distance); the named constant `MAX_STRUCTURE_DISTANCE = 8` is world/level/chunk/status/ChunkStatus.java:16 (the unpack site uses the literal 8) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each starts entry is <structureId>: StructureStart-compound with fields id: String (the structure's namespaced id, or the literal string "INVALID" when the chunk has no valid start, in which case ChunkX/ChunkZ/Children are absent entirely rather than merely empty), ChunkX: Int, ChunkZ: Int, references: Int, and Children: List<Compound>; there is no Processed field in 26.2 -- the monument's saved pieces are rebuilt at load time instead. | `StructureStart.createTag` (world/level/levelgen/structure/StructureStart.java:103-116) writes, for a valid start, `id` (the namespaced structure id), `ChunkX`, `ChunkZ`, `references` (an Int -- omitted from the claim entirely, and read back at :53) and `Children`; for an invalid start it writes only `id = "INVALID"` (:113), which is the part the claim gets right. There is no `Processed` field anywhere in 26.2 -- the string does not occur in the reference source; the monument's saved pieces are instead rebuilt by `OceanMonumentStructure.regeneratePiecesAfterLoad` (:58-59) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each piece compound has confirmed fields BB: IntArray[6] (minX,minY,minZ,maxX,maxY,maxZ), id: String (the piece-type identifier), O: Int (orientation), and GD: Int (gen depth). | `StructurePiece.createTag` (world/level/levelgen/structure/StructurePiece.java:83-92): `id` (String, the STRUCTURE_PIECE registry key), `BB` via `BoundingBox.CODEC`, `O` (Int) and `GD` (Int). `BoundingBox.CODEC` (BoundingBox.java:24-29) is an INT_STREAM of exactly 6 values encoded as `minX, minY, minZ, maxX, maxY, maxZ` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| PosX/PosY/PosZ per-piece fields are written only by the jigsaw piece class; non-jigsaw template pieces record TPX/TPY/TPZ plus Template instead, and purely procedural pieces add only their own family-specific fields. | in 26.2 `PosX`/`PosY`/`PosZ` are written by the JIGSAW piece class, not the legacy ones: `PoolElementStructurePiece.addAdditionalSaveData` (world/level/levelgen/structure/PoolElementStructurePiece.java:71-74) is the only writer of those three keys in the whole reference tree (read back at :57). Non-jigsaw template pieces write `TPX`/`TPY`/`TPZ` plus `Template` instead (`TemplateStructurePiece.addAdditionalSaveData`, :74-79), and purely procedural pieces add only their own family-specific fields | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's PoolElementStructurePiece (used by jigsaw pieces) carries its own Rotation field in addition to, not instead of, the base O field every piece writes; O itself is not a six-way orientation but a four-value horizontal Direction index 0..3, or -1 when null. | two errors. `PoolElementStructurePiece` does carry its own `rotation` field (`tag.store("rotation", Rotation.LEGACY_CODEC, ...)`, PoolElementStructurePiece.java:78, alongside PosX/PosY/PosZ, ground_level_delta, pool_element and junctions), but not INSTEAD of `O`: `StructurePiece.createTag` is `final` (StructurePiece.java:83-92) and writes `O` for every piece, jigsaw pieces included. And `O` is not a six-way orientation: it is `orientation.get2DDataValue()` (:88), a four-value horizontal `Direction` index 0..3, or -1 when the orientation is null (read back at :67-68 via `Direction.from2DDataValue`) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla feeds one world's structures from a single seed value in the normal case: the levelSeed and concentricRingsSeed fields both hold the same value in a normal world; only a superflat world's generator diverges, seeding the concentric-rings computation with 0 instead. | there is only one seed value in a normal world. `ChunkGeneratorStructureState` does keep two fields, `levelSeed` and `concentricRingsSeed` (ChunkGeneratorStructureState.java:38-39), but `createForNormal` (:52-59) passes the same `levelSeed` into both, and `ChunkMap` (server/level/ChunkMap.java:190) supplies the world seed for it. Only `createForFlat` (:45-50) makes them differ, by passing `0L` as the concentric-rings seed. The per-chunk placement values are of course derived (`setLargeFeatureWithSalt`), but they derive from that same world seed | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's depth cutoff for jigsaw assembly still consults a target pool's fallback pool even at max_depth; a hard stop without trying fallback would produce dangling connectors vanilla never has. | `JigsawPlacement.Placer.tryPlacingChildren` (pools/JigsawPlacement.java:353-358): `if (depth != this.maxDepth) { targetPieces.addAll(targetPool.value().getShuffledTemplates(this.random)); }` followed by an UNCONDITIONAL `targetPieces.addAll(fallback.value().getShuffledTemplates(this.random));`, so the fallback pool is consulted at every depth including maxDepth | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In generate_ring_positions, the first (innermost) ring's position count (positions_in_ring) starts at the concentric_rings placement's own spread field, before any per-ring growth is applied. | `ChunkGeneratorStructureState.generateRingPositions` (ChunkGeneratorStructureState.java:116) initialises `int spread = placement.spread();` before the loop, and only mutates it inside the ring-exhausted branch at :156-157 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When the ring-position biome search finds no matching biome within its search radius, the ring position falls back to the raw, unadjusted angle/distance position rather than being dropped. | `ChunkGeneratorStructureState.generateRingPositions` (ChunkGeneratorStructureState.java:142-147): when `findBiomeHorizontal` returns null the task yields `new ChunkPos(initialX, initialZ)` -- the raw rounded angle/distance position -- rather than dropping the entry | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| STRUCTURE_STARTS skips a structure set entirely for a chunk if any of its member structures already has a recorded start there. | `ChunkGenerator.createStructures` (world/level/chunk/ChunkGenerator.java:487-492) loops the set's entries and `return`s out of the whole per-set lambda on the first `existingStart != null && existingStart.isValid()`. Worth noting the `isValid()` qualifier: a recorded but INVALID start (piece container empty, StructureStart.java:118-120) does not trigger the skip | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every pool lookup performed while walking add_pieces -> both the center pool and every jigsaw's target pool encountered during child placement -> is resolved through the pool alias lookup, not only the initial center-pool lookup. | `JigsawPlacement.addPieces` (pools/JigsawPlacement.java:73-75) resolves the centre pool through `poolAliasLookup.lookup(key)`, and `Placer.tryPlacingChildren` (:328) resolves every source jigsaw's target pool through `poolAliasLookup.lookup(sourceJigsaw.pool())`; the expansion hack resolves child pools the same way at :379 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The pending-piece queue that drives jigsaw child placement is a priority-bucketed FIFO (SequencedPriorityIterator): every piece queued at the current highest priority is placed before any piece queued at a lower priority, with FIFO order within one priority bucket. | `JigsawPlacement.Placer.placing` is a `SequencedPriorityIterator` (pools/JigsawPlacement.java:281), fed at :482 with `this.placing.add(state, placementPriority)` and drained at :212-215. `SequencedPriorityIterator` (util/SequencedPriorityIterator.java:22-72) keeps one `ArrayDeque` per priority, appends with `addLast` (:24, :27), pops with `removeFirst` from the highest-priority deque (:40) and only switches to the next-highest priority once that deque empties (:45-46, :52-72) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A piece (the center piece or any candidate child piece) whose bounding box fails the dimension-padding check is rejected -> for the center piece add_pieces returns None outright, and for a candidate child that specific rotation/candidate attempt is abandoned and the next one is tried. | the centre piece is checked by `isStartTooCloseToWorldHeightLimits` (pools/JigsawPlacement.java:118-124, :165-175) and `addPieces` returns `Optional.empty()` on failure. For candidate children there is no separate padding check: the padding is folded into the initial free-space AABB (`Math.max(centerY - vertical, minY + dimensionPadding.bottom())` / `Math.min(centerY + vertical + 1, maxY + 1 - dimensionPadding.top())`, :133-141), so an out-of-range candidate fails the free-space test at :422-424 and the loop falls through to the next target jigsaw, then the next rotation, then the next candidate | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Collision testing against tracked free space deflates each candidate's bounding box by 0.25 blocks before checking for overlap. | `JigsawPlacement.Placer.tryPlacingChildren` (pools/JigsawPlacement.java:422-424): `Shapes.joinIsNotEmpty(childrenFree.get(), Shapes.create(AABB.of(targetBB).deflate(0.25)), BooleanOp.ONLY_SECOND)` -- the candidate box is deflated by 0.25 before the overlap test | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A JigsawJunction's field names (source_x/source_z/source_ground_y/delta_y/projection) are shared by both junctions of an attachment, but the values are not a simple reversal: the junction handed to the SOURCE piece carries the connector position one step in front of the source jigsaw, not the source jigsaw's own x/z (only the reversed junction handed to the new piece uses the source jigsaw's position), and source_ground_y/delta_y are derived through rigid/non-rigid junctionY branches rather than a plain terrain-height sample. | the record's field NAMES match (`sourceX`, `sourceGroundY`, `sourceZ`, `deltaY`, `destProjection`, serialised as source_x/source_ground_y/source_z/delta_y/dest_proj, pools/JigsawJunction.java:9-51), but not the values. At pools/JigsawPlacement.java:461-478 the junction handed to the SOURCE piece carries `targetJigsawPos.getX()`/`.getZ()` -- the connector block one step in front of the source jigsaw, not the source jigsaw's own x/z (only the reversed junction handed to the new piece uses `sourceJigsawPos`). `sourceGroundY` is `junctionY - sourceJigsawLocalY + sourceGroundLevelDelta`, and `junctionY` (:445-459) is the terrain heightmap value only when neither piece is rigid; when the source is rigid it is `sourceBoxY + sourceJigsawLocalY`, when only the target is rigid `targetBoxY + targetJigsawLocalY`, and in the both-non-rigid case it is `sourceJigsawBaseHeight + deltaY / 2`. `deltaY` is `sourceJigsawLocalY - targetJigsawLocalY + frontFacing.getStepY()` (:399), negated for the reversed junction, not the new piece's Y minus the source jigsaw's Y | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Once a rotation/candidate/target-jigsaw combination successfully attaches to a source jigsaw, no further rotation or candidate is tried for that same source jigsaw -> the first valid attachment found wins. | `JigsawPlacement.Placer.tryPlacingChildren` (pools/JigsawPlacement.java:484) ends a successful attachment with `continue label129`, jumping straight to the next source jigsaw of the outer `for` at :319 and abandoning the remaining target jigsaws, rotations and candidates | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A structure's max_distance_from_center plus its terrain_adaptation_edge_padding must not exceed MAX_TOTAL_STRUCTURE_RANGE (128). | `JigsawStructure.verifyRange` (structures/JigsawStructure.java:64-72), attached to the codec via `.validate(JigsawStructure::verifyRange)` (:52): `edgeNeeded` is 0 for NONE and 12 for BURY/BEARD_THIN/BEARD_BOX/ENCAPSULATE, and `maxDistanceFromCenter.horizontal() + edgeNeeded > 128` is a codec error | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BeardThin and BeardBox share the identical getBeardContribution arm, differing only in how dy is measured; whether the term pushes toward solid or air depends on the sign of the yToGround+0.5 offset, not on which adaptation is used. Bury is a separate, always-non-negative getBuryContribution term, and Encapsulate is a scaled bury term. The falloff is a Gaussian kernel (exp(-distanceSqr/16)), not inverse-distance-squared, zero outside the 24-cell kernel window. | `Beardifier.compute` (world/level/levelgen/Beardifier.java:133-152) does not split BEARD_THIN from BEARD_BOX by sign: both take the identical `getBeardContribution(dx, dy, dz, dyToGround) * 0.8` arm (:143), differing only in how `dy` is measured (:135-137). Whether that term pushes toward solid or air depends on the sign of `dyWithOffset = yToGround + 0.5` (:178-180), not on which of the two adaptations is used. BURY is `getBuryContribution(dx, dy/2, dz)` (:142) = `Mth.clampedMap(distance, 0, 6, 1, 0)` (:168-171), always non-negative, and ENCAPSULATE is a scaled bury term (:144) the claim omits. The falloff is also not inverse-distance-squared: `getBeardContribution` (:173-185) is `-dyWithOffset * Mth.fastInvSqrt(distanceSqr / 2.0) / 2.0` multiplied by the precomputed Gaussian kernel `exp(-distanceSqr / 16.0)` (:191-198), zero outside the 24-cell kernel window (:177, :187-189) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The pinned Minecraft Java Edition 26.2 structure-template NBT DataVersion is 4903. | `StructureTemplate.save` (templatesystem/StructureTemplate.java:705) ends with `NbtUtils.addCurrentDataVersion(tag)`, which writes `DataVersion = SharedConstants.getCurrentVersion().dataVersion().version()` (nbt/NbtUtils.java:522-528); the pinned jar's own version.json reports `world_version: 4903` (with `id: 26.2`, `protocol_version: 776`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A Rule processor's ProcessorRule replaces the template block with its output_state only when the template block passes the rule's input_predicate, the existing world block at the target position passes the rule's location_predicate, and its position_predicate (AlwaysTrue by default) passes given the template-relative position, world position, and reference position. | `ProcessorRule.test` (templatesystem/ProcessorRule.java:54-65): `inputPredicate.test(inputState, random) && locPredicate.testAgainstWorldState(level, worldPos, random) && posPredicate.test(inTemplatePos, worldPos, reference, random)`, where `RuleTest.testAgainstWorldState` (RuleTest.java:16-18) reads `level.getBlockState(pos)`. The codec (:16-25) defaults `position_predicate` to `PosAlwaysTrueTest.INSTANCE`, and `RuleProcessor.processBlock` (RuleProcessor.java:36-45) applies the first matching rule's `getOutputState()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
