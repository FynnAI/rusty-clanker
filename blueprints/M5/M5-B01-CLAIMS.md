# M5-B01 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Vanilla's legacy RNG is a 48-bit LCG, bit-compatible with java.util.Random. | `LegacyRandomSource` (LegacyRandomSource.java:10-13, 32-49) uses MULTIPLIER 25214903917, INCREMENT 11, MODULUS_MASK 281474976710655 and `(int)(newSeed >> 48 - bits)`; JDK 25 `java.util.Random` (src.zip java/util/Random.java:298-303) declares the identical multiplier 0x5DEECE66D, addend 0xB, mask (1L<<48)-1, DOUBLE_UNIT 0x1.0p-53, FLOAT_UNIT 0x1.0p-24, and its `nextInt(bound)` (Random.java:549-562) is the same power-of-two path plus the same rejection loop as `BitRandomSource.nextInt` (BitRandomSource.java:17-34). Only note: `java.util.Random.nextGaussian` (Random.java:745-762) uses `StrictMath.sqrt`/`StrictMath.log` where Minecraft's `MarsagliaPolarGaussian` (MarsagliaPolarGaussian.java:34) uses `Math.sqrt`/`Math.log` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's legacy RNG holds one 48-bit-masked i64 of state; vanilla's Xoroshiro RNG holds two i64 state words. | `LegacyRandomSource.seed` is one `AtomicLong` masked to 48 bits (LegacyRandomSource.java:14, 33, 43); `Xoroshiro128PlusPlus` holds `seedLo`/`seedHi`, two `long` fields (Xoroshiro128PlusPlus.java:8-9) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy's nextInt(bound) uses a power-of-two fast path plus a next(31)-modulo rejection loop; Xoroshiro's nextInt(bound) uses Lemire multiply-high rejection with no power-of-two fast path. | `BitRandomSource.nextInt(int)` (BitRandomSource.java:17-34): `(bound & bound - 1) == 0` fast path at :22-24, rejection loop at :26-33; `XoroshiroRandomSource.nextInt(int)` (XoroshiroRandomSource.java:53-74) has no power-of-two branch, only the multiply/threshold/reject sequence | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's nether, end, caves, and floating_islands noise-settings presets set legacy_random_source: true (use the legacy LCG); overworld, large_biomes, and amplified presets set legacy_random_source: false (use Xoroshiro). | datagen `data/minecraft/worldgen/noise_settings/*.json`: nether.json, end.json, caves.json, floating_islands.json each `"legacy_random_source": true`; overworld.json, large_biomes.json, amplified.json each `"legacy_random_source": false` (all seven presets checked, no others exist) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla slime chunks always use the legacy RNG regardless of dimension. | `WorldgenRandom.seedSlimeChunk` (WorldgenRandom.java:71-73) calls `RandomSource.createThreadLocalInstance(...)`, which is `new SingleThreadedRandomSource(seed)` (RandomSource.java:31-33) — a `BitRandomSource` with the identical 48-bit LCG (SingleThreadedRandomSource.java:6-41); no dimension or noise-settings input reaches this path, and the only call site (Slime.java:93) passes it unconditionally | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's structure-spread jitter (RandomSpreadStructurePlacement) always uses the legacy RNG regardless of dimension. | `RandomSpreadStructurePlacement.getPotentialStructureChunk` (RandomSpreadStructurePlacement.java:67-76) constructs `new WorldgenRandom(new LegacyRandomSource(0L))` unconditionally at :70 before `setLargeFeatureWithSalt` at :71 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's random_sequence (used for loot) always uses the Xoroshiro RNG regardless of dimension. | `RandomSequence` holds a `XoroshiroRandomSource` field and its factory returns one unconditionally (RandomSequence.java:15, 29-36); `RandomSequences.get`/`createSequence` (RandomSequences.java:45-59) offers no algorithm choice | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's RandomSource.nextIntBetweenInclusive(min, maxInclusive) is defined as nextInt(maxInclusive - min + 1) + min. | `RandomSource.nextIntBetweenInclusive` (RandomSource.java:45-47) — `this.nextInt(maxInclusive - min + 1) + min`, a default method no implementor overrides | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's RandomSource.nextIntRange(origin, boundExclusive) is defined as origin + nextInt(boundExclusive - origin). | `RandomSource.nextInt(int origin, int bound)` (RandomSource.java:73-79) — `origin + this.nextInt(bound - origin)`; note it first throws `IllegalArgumentException("bound - origin is non positive")` when `origin >= bound` (:74-75), a guard the blueprint's provided method omits | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's RandomSource triangle(mean, spread) helper is defined as mean + spread * (nextDouble() - nextDouble()), with the second draw subtracted. | `RandomSource.triangle(double, double)` (RandomSource.java:59-61); a `float` overload with the same shape over `nextFloat()` exists at :63-65 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's RandomSource consume-count helper draws exactly one nextInt() call per round, discarding the result, for the given number of rounds. | `RandomSource.consumeCount` (RandomSource.java:67-71) — `for (int i = 0; i < rounds; i++) this.nextInt();`. Note for the blueprint's "never overridden" trait comment: `XoroshiroRandomSource.consumeCount` (XoroshiroRandomSource.java:101-106) DOES override it, drawing `randomNumberGenerator.nextLong()` per round — state-advance-equivalent, since Xoroshiro's `nextInt()` is itself exactly one `nextLong()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Calling nextInt(bound) with bound <= 0 throws Java's IllegalArgumentException in vanilla. | `BitRandomSource.nextInt` (BitRandomSource.java:18-20) and `XoroshiroRandomSource.nextInt` (XoroshiroRandomSource.java:55-57) both throw `new IllegalArgumentException("Bound must be positive")` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy LCG multiplier constant is 25214903917 (0x5DEECE66D). | `LegacyRandomSource.MULTIPLIER` (LegacyRandomSource.java:12), used inline at :33 and :43; same value in `SingleThreadedRandomSource` (:9, 30, 38) and in JDK `java.util.Random.multiplier = 0x5DEECE66DL` (Random.java:298) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy LCG addend constant is 11 (0xB). | `LegacyRandomSource.INCREMENT = 11L` (LegacyRandomSource.java:13), used inline at :43; JDK `java.util.Random.addend = 0xBL` (Random.java:299) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy LCG modulus mask is 281474976710655 (0xFFFFFFFFFFFF, i.e. 2^48 minus 1). | `LegacyRandomSource.MODULUS_BITS = 48` / `MODULUS_MASK = 281474976710655L` (LegacyRandomSource.java:10-11), applied at :33 and :43 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy setSeed computes state = (seed XOR 25214903917) AND 281474976710655, and resets the Gaussian cache. | `LegacyRandomSource.setSeed` (LegacyRandomSource.java:32-38): `(seed ^ 25214903917L) & 281474976710655L` at :33, `this.gaussianSource.reset()` at :37 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy next(bits) computes state = (state * 25214903917 + 11) AND 281474976710655, then returns the top `bits` bits of the new state (an arithmetic right shift by 48-bits, harmless since the top 16 bits of state are always zero). | `LegacyRandomSource.next` (LegacyRandomSource.java:41-49): `oldSeed * 25214903917L + 11L & 281474976710655L` at :43, `(int)(newSeed >> 48 - bits)` at :47 (Java `>>` on long is the arithmetic shift, JVM `lshr`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy nextInt() returns next(32), the top 32 bits of one LCG step. | `BitRandomSource.nextInt()` (BitRandomSource.java:12-14) — `this.next(32)`; javap of the shipped class shows `bipush 32; invokeinterface next:(I)I; ireturn` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro nextInt() returns the low 32 bits (truncating cast) of a freshly drawn nextLong(). | `XoroshiroRandomSource.nextInt()` (XoroshiroRandomSource.java:48-51) — `(int)this.randomNumberGenerator.nextLong()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy's power-of-two fast path for nextInt(bound) computes ((bound as i64) * (next(31) as i64)) >> 31 using a single draw. | `BitRandomSource.nextInt` (BitRandomSource.java:22-24) — `(int)((long)bound * this.next(31) >> 31)`; javap confirms `i2l; next(31); i2l; lmul; bipush 31; lshr; l2i` (one `next` call, signed long shift) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro's nextInt(bound) never uses a power-of-two fast path; every bound, including powers of two, goes through the same multiply/threshold/reject Lemire logic. | `XoroshiroRandomSource.nextInt(int)` (XoroshiroRandomSource.java:53-74) — after the bound-positive check the only path is :59-73; no `bound & bound - 1` test exists anywhere in the class | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy nextLong() is ((next(32) as i64) << 32) + (next(32) as i64) - two calls, high word drawn first, combined with addition (not bitwise or) because the low word is sign-extended and can be negative. | `BitRandomSource.nextLong` (BitRandomSource.java:37-42): `upper = next(32)` first, `lower = next(32)` second, `((long)upper << 32) + lower`; javap shows `i2l` on the low word then `ladd` (sign-extending addition, not `lor`) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro nextLong() is a single raw core step (next_u64 as i64), with no combination of two draws. | `XoroshiroRandomSource.nextLong` (XoroshiroRandomSource.java:76-79) returns `this.randomNumberGenerator.nextLong()` — one `Xoroshiro128PlusPlus.nextLong` step (Xoroshiro128PlusPlus.java:26-34) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy nextBoolean() is next(1) != 0. | `BitRandomSource.nextBoolean` (BitRandomSource.java:45-47) — `this.next(1) != 0` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro nextBoolean() is (nextLong() & 1) != 0. | `XoroshiroRandomSource.nextBoolean` (XoroshiroRandomSource.java:81-84) — `(this.randomNumberGenerator.nextLong() & 1L) != 0L` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy nextFloat() is (next(24) as f32) * FLOAT_UNIT. | `BitRandomSource.nextFloat` (BitRandomSource.java:50-52) — `this.next(24) * 5.9604645E-8F`; javap: `bipush 24; next; i2f; ldc float 5.9604645E-8f; fmul; freturn` (the multiply is f32 arithmetic) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro nextFloat() takes the top 24 of 64 bits of one nextLong() (logical shift right by 64-24), then multiplies by FLOAT_UNIT. | `XoroshiroRandomSource.nextFloat` (XoroshiroRandomSource.java:86-89) — `(float)this.nextBits(24) * 5.9604645E-8F`, with `nextBits` = `nextLong() >>> 64 - bits` (:108-110); javap: `nextBits:(I)J; l2f; ldc float; fmul` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy nextDouble() is (((next(26) as i64) << 27) + (next(27) as i64)) as f64 * DOUBLE_UNIT, from two next(bits) calls split 26-then-27. | `BitRandomSource.nextDouble` (BitRandomSource.java:55-60) — `next(26)` first, `next(27)` second, `((long)upper << 27) + lower`, then the double multiply; javap: `i2l; bipush 27; lshl; i2l; ladd; l2d; ldc2_w double 1.1102230246251565E-16d; dmul; dreturn` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro nextDouble() takes the top 53 of 64 bits of one nextLong() (logical shift right by 64-53), then multiplies by DOUBLE_UNIT. | `XoroshiroRandomSource.nextDouble` (XoroshiroRandomSource.java:91-94) — `this.nextBits(53) * 1.110223E-16F` over `nextBits` = `nextLong() >>> 64 - bits` (:108-110); javap: `bipush 53; nextBits:(I)J; l2d; ldc2_w double 1.1102230246251565E-16d; dmul` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy's nextInt(bound) rejection loop draws bits = next(31), computes val = bits % bound (Java's % on a non-negative i32 operand is a plain remainder), and accepts val when the 32-bit wrapping computation (bits - val + bound - 1) is >= 0, otherwise redraws. | `BitRandomSource.nextInt` (BitRandomSource.java:26-33) — `do { sample = next(31); modulo = sample % bound; } while (sample - modulo + (bound - 1) < 0);` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Legacy's rejection-loop acceptance test must be computed in wrapping 32-bit arithmetic; computing it in a wider integer type changes the rejection rate (and therefore call count) for a fraction of (bits, bound) pairs near i32::MAX. | javap of `BitRandomSource.nextInt` shows the test as `iload_2; iload_3; isub; iload_1; iconst_1; isub; iadd; iflt` — pure `int` (32-bit wrapping) opcodes, so the sum only ever goes negative by overflow; the same test in i64 never goes negative and would accept every sample | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro's Lemire nextInt(bound) algorithm draws random_bits as the zero-extended low 32 bits of a fresh nextLong(), computes product = random_bits * bound as u64 (exact, both operands under 2^32), takes frac = product AND 0xFFFFFFFF, and if frac is less than bound computes threshold as Java's Integer.remainderUnsigned(-bound, bound) (an unsigned 32-bit remainder) and redraws while frac stays below threshold; the result is (product >> 32) as i32. | `XoroshiroRandomSource.nextInt(int)` (XoroshiroRandomSource.java:59-73): `Integer.toUnsignedLong(this.nextInt())` :59, `randomBits * bound` :60, `& 4294967295L` :61, `if (fractionalPart < bound)` :62, `Integer.remainderUnsigned(~bound + 1, bound)` :63 (`~bound + 1` is the two's-complement `-bound`), redraw loop :63-69, `(int)(multipliedRandomBits >> 32)` :72-73 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Integer.remainderUnsigned(-bound, bound) threshold in the Lemire algorithm must be computed as a genuinely unsigned 32-bit remainder; a signed remainder gives the wrong threshold for roughly half of all bound values, because -bound as a signed i32 is negative while the unsigned reinterpretation is 2^32 - bound. | `XoroshiroRandomSource.nextInt` (XoroshiroRandomSource.java:63) calls `Integer.remainderUnsigned`, whose JDK contract is the unsigned remainder of the 32-bit operands; `~bound + 1` is negative as a signed int for every bound > 0, so a signed `%` would yield a negative (or zero) threshold instead of `2^32 - bound mod bound` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's FLOAT_UNIT and DOUBLE_UNIT constants are used unmodified by both RNG families' nextFloat/nextDouble implementations (Legacy's BitRandomSource.FLOAT_MULTIPLIER/DOUBLE_MULTIPLIER and Xoroshiro's own next_float/next_double reuse the identical two constants). | `BitRandomSource.FLOAT_MULTIPLIER`/`DOUBLE_MULTIPLIER` (BitRandomSource.java:6-7) and `XoroshiroRandomSource.FLOAT_UNIT`/`DOUBLE_UNIT` (XoroshiroRandomSource.java:9-10) are separate field declarations carrying the identical literals; javap of both classes shows the identical constant-pool entries used at the multiply sites — `float 5.9604645E-8f` and `double 1.1102230246251565E-16d` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's nextGaussian() uses the Marsaglia polar method with a per-instance single-value cache that setSeed resets. | `MarsagliaPolarGaussian` (MarsagliaPolarGaussian.java:6-38) with `nextNextGaussian`/`haveNextNextGaussian` fields and `reset()` :15-17; one instance per source (LegacyRandomSource.java:15, XoroshiroRandomSource.java:14), reset from `setSeed` (LegacyRandomSource.java:37, XoroshiroRandomSource.java:45, SingleThreadedRandomSource.java:31-33) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's nextGaussian(): if a cached value exists, consume and return it; otherwise loop drawing v1 = 2*nextDouble()-1 and v2 = 2*nextDouble()-1 until s = v1*v1+v2*v2 is less than 1.0 and not equal to 0.0, then set mul = sqrt(-2*ln(s)/s), cache v2*mul, and return v1*mul. | `MarsagliaPolarGaussian.nextGaussian` (MarsagliaPolarGaussian.java:19-38): cache check :20-23, draws :29-30, `Mth.square(x) + Mth.square(y)` :31, `while (radiusSquared >= 1.0 or radiusSquared == 0.0)` :32, `Math.sqrt(-2.0 * Math.log(radiusSquared) / radiusSquared)` :34, caches `y * multiplier` :35, returns `x * multiplier` :37 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's nextGaussian() rejection loop draws two nextDouble() calls per iteration, with an expected approximately 1.273 iterations per fresh pair (acceptance probability pi/4, approximately 78.5%). | `MarsagliaPolarGaussian.nextGaussian` (MarsagliaPolarGaussian.java:28-32) draws exactly two `nextDouble()` per iteration; the accepted region is the open unit disc inside the square [-1,1]^2, so acceptance is pi/4 = 0.7853981... and the expected iteration count is 4/pi = 1.27324 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every nextGaussian() call other than the one that triggers a fresh pair draw is "free" - it consumes zero RNG draws and returns the previously cached value. | `MarsagliaPolarGaussian.nextGaussian` (MarsagliaPolarGaussian.java:20-23) returns `nextNextGaussian` and clears the flag before reaching any `randomSource` call | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A fresh Gaussian pair costs four raw LCG steps under the legacy RNG (two next(bits) calls inside nextDouble(), called twice) but only two raw state transitions under Xoroshiro (one nextLong() inside nextDouble(), called twice). | `BitRandomSource.nextDouble` is two `next(bits)` calls (BitRandomSource.java:55-60), so two `nextDouble()` per accepted iteration (MarsagliaPolarGaussian.java:29-30) is four LCG steps; `XoroshiroRandomSource.nextDouble` is one `nextBits(53)` = one `nextLong()` (XoroshiroRandomSource.java:91-94, 108-110), so two state transitions | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Math.log used inside nextGaussian() is HotSpot-JIT-intrinsified and carries only a within-1-ulp, semi-monotonic contract, so it is not guaranteed bit-identical across different JVM implementations or hardware; Math.sqrt, also intrinsified, is specified as the correctly rounded IEEE-754 squareRoot operation and is bit-identical on every conforming JVM, equal to Rust's f64::sqrt, though the Gaussian multiplier sqrt(-2*ln(s)/s) still inherits ln's cross-platform uncertainty through that otherwise-exact sqrt. | WRONG for `sqrt`. Both are `@IntrinsicCandidate` (JDK 25 src.zip java/lang/Math.java:412-413 and :358-359), but `Math.sqrt` is specified as "the correctly rounded positive square root ... the double value closest to the true mathematical square root", i.e. the IEEE 754 squareRoot operation (Math.java:391-411) — bit-identical on every conforming JVM and equal to Rust's `f64::sqrt`. Only `Math.log` carries the weaker contract "must be within 1 ulp of the exact result. Results must be semi-monotonic." (Math.java:351-352) and is therefore not guaranteed bit-identical. Correction: restrict the blueprint's cross-platform caveat (Context E, Done-definition tolerance) to `ln` alone; `sqrt` may be asserted exactly. Call site: MarsagliaPolarGaussian.java:34 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro's golden-ratio-64 mixing constant is -7046029254386353131 (0x9E3779B97F4A7C15). | `RandomSupport.GOLDEN_RATIO_64 = -7046029254386353131L` (RandomSupport.java:11); 0x9E3779B97F4A7C15 = 11400714819323198485 unsigned, which is -7046029254386353131 as a signed i64 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro's silver-ratio-64 mixing constant is 7640891576956012809 (0x6A09E667F3BCC909). | `RandomSupport.SILVER_RATIO_64 = 7640891576956012809L` (RandomSupport.java:12); 0x6A09E667F3BCC909 = 7640891576956012809 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro's stafford-13 mix first multiplier constant is -4658895280553007687 (0xBF58476D1CE4E5B9). | `RandomSupport.mixStafford13` (RandomSupport.java:21) multiplies by `-4658895280553007687L`; 0xBF58476D1CE4E5B9 = 13787848793156543929 unsigned = -4658895280553007687 signed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Xoroshiro's stafford-13 mix second multiplier constant is -7723592293110705685 (0x94D049BB133111EB). | `RandomSupport.mixStafford13` (RandomSupport.java:22) multiplies by `-7723592293110705685L`; 0x94D049BB133111EB = 10723151780598845931 unsigned = -7723592293110705685 signed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's stafford-13 mix function computes z = (z XOR (z logical-shift-right 30)) * stafford_mul_1, then z = (z XOR (z logical-shift-right 27)) * stafford_mul_2, then returns z XOR (z logical-shift-right 31), with every multiply/add performed as 64-bit wrapping arithmetic and all three shifts unsigned (logical). | `RandomSupport.mixStafford13` (RandomSupport.java:20-24) — all three shifts written `>>>` (JVM `lushr`), both multiplies plain Java `long` multiplies (wrapping by definition) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's 128-bit seed upgrade (unmixed) computes lo = seed XOR silver_ratio_64 and hi = lo + golden_ratio_64 (wrapping add). | `RandomSupport.upgradeSeedTo128bitUnmixed` (RandomSupport.java:26-30) — `lowBits = legacySeed ^ 7640891576956012809L`, `highBits = lowBits + -7046029254386353131L` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's full 128-bit seed upgrade applies the stafford-13 mix to both the lo and hi halves produced by the unmixed upgrade. | `RandomSupport.upgradeSeedTo128bit` (RandomSupport.java:32-34) returns `upgradeSeedTo128bitUnmixed(legacySeed).mixed()`, and `Seed128bit.mixed()` (RandomSupport.java:59-61) mixes both halves | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A genuinely fresh top-level Xoroshiro construction from one i64 seed applies the full mixed seed upgrade (stafford-13 mix on both halves). | `XoroshiroRandomSource(long)` (XoroshiroRandomSource.java:16-18) uses `RandomSupport.upgradeSeedTo128bit(seed)`; `setSeed(long)` (:42-46) does the same | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's create_random_sequence applies its own separate, explicit stafford-13 mixing step at the very end of its own derivation, after any optional XORs. | `RandomSequence.createSequence` (RandomSequence.java:29-36): unmixed upgrade :30, optional `xor(seedForKey(...))` :31-33, then `new XoroshiroRandomSource(seed128bit.mixed())` :35 — the mix runs exactly once, last, regardless of the optional branch | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Xoroshiro positional-factory output and its fork()/fork_positional() output are used directly as a raw state pair, with no stafford-13 mixing applied (aside from the all-zero-state guard). | `XoroshiroPositionalRandomFactory.at`/`fromHashOf`/`fromSeed` (XoroshiroRandomSource.java:121-137) all reach the `(long, long)` or `Seed128bit` constructors (:20-26), which hand the pair straight to `new Xoroshiro128PlusPlus(...)`; `XoroshiroRandomSource.fork` (:32-35) likewise. The only transformation is the zero guard in Xoroshiro128PlusPlus.java:20-23 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's xoroshiro128++ core step uses output rotation 17, s0 rotation 49, an XOR-shift of 21, and s1 rotation 28. | `Xoroshiro128PlusPlus.nextLong` (Xoroshiro128PlusPlus.java:29-32): `Long.rotateLeft(s0 + s1, 17)`, `Long.rotateLeft(s0, 49)`, `s1 << 21`, `Long.rotateLeft(s1, 28)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's xoroshiro128++ nextLong() computes result = rotate_left(s0+s1, 17) + s0 (wrapping), then updates s1 ^= s0 and sets state to (rotate_left(s0,49) XOR s1 XOR (s1 << 21), rotate_left(s1,28)), returning the original result. | `Xoroshiro128PlusPlus.nextLong` (Xoroshiro128PlusPlus.java:26-34) in exactly that order: result :29, `s1 ^= s0` :30, `seedLo` :31, `seedHi` :32, `return result` :33 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's own all-zero-seed substitution for a 128-bit Xoroshiro state pair is (golden_ratio_64, silver_ratio_64), not the SplitMix64-derived fallback that the rand_xoshiro crate's own from_seed applies to an all-zero 16-byte seed. | Vanilla half CONFIRMED: `Xoroshiro128PlusPlus(long, long)` (Xoroshiro128PlusPlus.java:20-23) — `if ((seedLo or seedHi) == 0) { seedLo = -7046029254386353131L; seedHi = 7640891576956012809L; }`, i.e. (GOLDEN_RATIO_64, SILVER_RATIO_64). The contrasting clause about `rand_xoshiro` 0.8.1's own `from_seed` is outside the ASSET-D18(f) reference and was NOT verified here — the crate is not vendored and not present in this machine's cargo registry cache | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's fork() draws two nextLong() calls (not re-mixed) to produce a new raw state pair; fork_positional() likewise draws two nextLong() calls (not re-mixed) to produce a new positional-factory seed pair. | `XoroshiroRandomSource.fork` (XoroshiroRandomSource.java:32-35) and `forkPositional` (:37-40) each call `randomNumberGenerator.nextLong()` twice and pass the pair unmixed. Scope note: this holds for the Xoroshiro family only — `LegacyRandomSource.fork`/`forkPositional` (LegacyRandomSource.java:22-29) each draw ONE `nextLong()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's positional seed hash mth_get_seed(x,y,z) computes x_term as x*3129871 performed in 32-bit space and only then sign-extended to i64. | `Mth.getSeed(int, int, int)` (Mth.java:335-339) — `x * 3129871` has no `L` on either operand, so it is `int * int` (32-bit wrapping) and only widens when XORed with the `long` term at :336 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's positional seed hash computes z_term as z (sign-extended to i64) multiplied by 116129781 genuinely in 64-bit space (the Java source literal carries an L suffix). | `Mth.getSeed` (Mth.java:336) — `z * 116129781L`, the `L` suffix forcing `int * long` binary numeric promotion to 64-bit | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's positional seed hash combines seed = x_term XOR z_term XOR (y sign-extended to i64), then computes seed = seed*seed*42317861 + seed*11 (wrapping), and returns seed arithmetic-shifted right by 16 bits (a signed, sign-preserving shift). | `Mth.getSeed` (Mth.java:336-338) — `long seed = x * 3129871 ^ z * 116129781L ^ y;`, `seed = seed * seed * 42317861L + seed * 11L;`, `return seed >> 16;` (Java `>>` on long is arithmetic) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Computing x*3129871 directly in i64 space instead of 32-bit-then-sign-extend diverges from vanilla for any x whose product with 3129871 overflows i32, which occurs routinely for absolute values of x past roughly 686. | Follows from Mth.java:336 (int multiply); 2^31 / 3129871 = 686.1, so the 32-bit product first overflows at abs(x) = 687 and the two computations disagree from there on | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Java String.hashCode(), used by java_string_hash_code, iterates the string's UTF-16 code units (including surrogate-pair halves for non-BMP characters), computing h = h*31 + unit (wrapping) starting from h=0. | JDK 25 src.zip java/lang/String.java:2408-2421 — the specified value is `s[0]*31^(n-1) + ... + s[n-1]` "using int arithmetic, where s[i] is the ith character of the string" (a Java `char`, i.e. one UTF-16 code unit; the empty string hashes to zero), computed by `StringLatin1.hashCode`/`StringUTF16.hashCode` (String.java:2432-2433). Call site: `LegacyPositionalRandomFactory.fromHashOf` (LegacyRandomSource.java:72) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's java_string_hash_code("hello") equals 99162322, and java_string_hash_code("") equals 0. | Recomputed from the String.java:2409-2416 formula: 104, 3325, 103183, 3198781, 99162322 for "hello"; the empty product is 0, and String.java:2417 states the empty string's hash is zero | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's md5_seed(name) computes the MD5 digest of name's UTF-8 bytes and splits it into two big-endian i64 halves: lo from the first 8 digest bytes, hi from the last 8 digest bytes. | `RandomSupport.seedFromHashOf` (RandomSupport.java:36-41) — `MD5_128.hashString(input, StandardCharsets.UTF_8).asBytes()` with `MD5_128 = Hashing.md5()` (:13), then `Longs.fromBytes(hashCode[0]..[7])` for lo and `hashCode[8]..[15]` for hi; Guava's `Longs.fromBytes(b1..b8)` takes b1 as the most significant byte, i.e. big-endian | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Xoroshiro positional factory's from_hash_of uses the raw (unmixed) md5_seed halves directly, with no stafford-13 mixing applied at that call site. | `XoroshiroPositionalRandomFactory.fromHashOf` (XoroshiroRandomSource.java:128-132) — `RandomSupport.seedFromHashOf(name)` then `.xor(seedLo, seedHi)` straight into the `Seed128bit` constructor (:20-22); no `mixed()` call on this path | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's legacy positional factory computes .at(x,y,z) as a legacy RNG seeded with mth_get_seed(x,y,z) XOR the factory's own seed. | `LegacyPositionalRandomFactory.at` (LegacyRandomSource.java:63-68) — `Mth.getSeed(x, y, z) ^ this.seed` into `new LegacyRandomSource(...)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Xoroshiro positional factory computes .at(x,y,z) by perturbing only the low seed half with mth_get_seed(x,y,z) XOR seed_lo, while the high seed half (seed_hi) passes through unchanged. | `XoroshiroPositionalRandomFactory.at` (XoroshiroRandomSource.java:121-126) — `new XoroshiroRandomSource(Mth.getSeed(x,y,z) ^ this.seedLo, this.seedHi)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's legacy positional factory computes .from_hash_of(name) as a legacy RNG seeded with (java_string_hash_code(name) as i64) XOR the factory's own seed. | `LegacyPositionalRandomFactory.fromHashOf` (LegacyRandomSource.java:70-74) — `int positionalSeed = name.hashCode();` then `new LegacyRandomSource(positionalSeed ^ this.seed)`, the int widening to long by sign extension | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Xoroshiro positional factory computes .from_hash_of(name) by XORing each md5_seed half of name with the corresponding seed_lo/seed_hi half. | `XoroshiroPositionalRandomFactory.fromHashOf` (XoroshiroRandomSource.java:128-132) with `Seed128bit.xor(lo, hi)` (RandomSupport.java:51-53) — `(seedLo ^ lo, seedHi ^ hi)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's legacy positional factory's .from_seed(s) constructs a legacy RNG directly from s, ignoring the factory's own stored seed field entirely. | `LegacyPositionalRandomFactory.fromSeed` (LegacyRandomSource.java:76-79) — `return new LegacyRandomSource(seed);`, with `this.seed` unreferenced | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Xoroshiro positional factory's .from_seed(s) constructs a raw state pair (s XOR seed_lo, s XOR seed_hi). | `XoroshiroPositionalRandomFactory.fromSeed` (XoroshiroRandomSource.java:134-137) — `new XoroshiroRandomSource(seed ^ this.seedLo, seed ^ this.seedHi)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's WorldgenRandom wrapper's own nextInt/nextInt(bound)/nextLong/nextFloat/nextDouble are always computed via the legacy-style formulas built exclusively on the wrapped source's next(bits), even when the wrapped source is the Xoroshiro RNG. | `WorldgenRandom extends LegacyRandomSource` (WorldgenRandom.java:6) and overrides none of those five methods, so all five come from `BitRandomSource`'s defaults (BitRandomSource.java:12-60), which call only `this.next(bits)`. Precision note: the primitive is `WorldgenRandom.next(int)` (WorldgenRandom.java:29-35), which either delegates to `LegacyRandomSource.next` or performs `(int)(randomSource.nextLong() >>> 64 - bits)` itself — the wrapped Xoroshiro source has no `next(bits)` of its own to call | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's WorldgenRandom class extends LegacyRandomSource and overrides only its next(bits) primitive to forward to whichever wrapped RNG source it carries; every other derived-value method is inherited unmodified from the legacy family's own implementation, itself built exclusively on next(bits). | `WorldgenRandom` (WorldgenRandom.java:6, 29-35) — the derived-value half is exact: nextInt, nextInt(bound), nextLong, nextBoolean, nextFloat, nextDouble and nextGaussian are all inherited. Note it also overrides three NON-derived-value members: `fork` (:19-22), `forkPositional` (:24-27) and `setSeed` (:37-42), and adds the four seed-derivation methods plus `getCount` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Calling next_int_bounded through a WorldgenRandom wrapping the Xoroshiro RNG runs the legacy rejection-loop algorithm applied to Xoroshiro-sourced bits, never Xoroshiro's own native Lemire algorithm; the Lemire algorithm only runs when next_int_bounded is called directly on a bare Xoroshiro RNG instance (e.g. via create_random_sequence). | `WorldgenRandom` does not override `nextInt(int)`, so `BitRandomSource.nextInt` (BitRandomSource.java:17-34) runs over `WorldgenRandom.next` (WorldgenRandom.java:29-35); `XoroshiroRandomSource.nextInt(int)` (XoroshiroRandomSource.java:53-74) is only reachable on a bare instance, e.g. the one `RandomSequence.random()` hands out (RandomSequence.java:42-44) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For seed 0 and bound 10, a WorldgenRandom wrapping the Xoroshiro RNG yields the four next_int_bounded(10) values [8, 5, 8, 6], while the bare Xoroshiro RNG's own native Lemire next_int_bounded(10) for the same seed yields [9, 1, 1, 3]. | Recomputed from the reference algorithms (RandomSupport.java:20-34 seed upgrade, Xoroshiro128PlusPlus.java:26-34 core step, WorldgenRandom.java:29-35 plus BitRandomSource.java:17-34 for the wrapper, XoroshiroRandomSource.java:53-74 for the bare source): wrapper gives [8, 5, 8, 6], bare Lemire gives [9, 1, 1, 3] | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For seed 0 and bound 10, a WorldgenRandom wrapping the legacy RNG is bit-identical to a bare legacy RNG, both yielding the values [0, 8, 9, 7, 5]. | Recomputed from LegacyRandomSource.java:32-49 plus BitRandomSource.java:17-34, and from WorldgenRandom.java:29-35 wrapping the same source: both produce [0, 8, 9, 7, 5] | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's WorldgenRandom.setSeed override forwards only to the wrapped inner RNG's own setSeed (resetting that inner source's private Gaussian cache); it does not reset WorldgenRandom's own separate Gaussian cache, inherited unmodified from LegacyRandomSource. | `WorldgenRandom.setSeed` (WorldgenRandom.java:37-42) is `if (this.randomSource != null) this.randomSource.setSeed(seed);` with no `super.setSeed(...)` call, so the inherited `LegacyRandomSource.gaussianSource` (LegacyRandomSource.java:15) is never reset — while `LegacyRandomSource.setSeed` (:32-38) and `XoroshiroRandomSource.setSeed` (:42-46) each do reset their own | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's set_decoration_seed(world_seed, chunk_min_x, chunk_min_z) sets the seed to world_seed, draws x_scale = nextLong() bitwise-or 1 and z_scale = nextLong() bitwise-or 1 (both forced odd), computes result = (chunk_min_x*x_scale + chunk_min_z*z_scale) XOR world_seed (wrapping), reseeds with result, and returns result. | `WorldgenRandom.setDecorationSeed` (WorldgenRandom.java:44-51) exactly: `setSeed(seed)` :45, `nextLong() or 1L` twice :46-47, `chunkX * xScale + chunkZ * zScale ^ seed` :48 (Java `+` binds tighter than `^`), `setSeed(result)` :49, `return result` :50. Call sites pass block-min coordinates (ChunkGenerator.java:327, NoiseBasedChunkGenerator.java:498) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's decoration/population seed method was historically nicknamed setPopulationSeed before version 1.18; vanilla's current source names it setDecorationSeed. | Second half CONFIRMED: `WorldgenRandom.setDecorationSeed` (WorldgenRandom.java:44). First half NOT verifiable from the ASSET-D18(f) reference — a full-tree grep of the 26.2 source for "setPopulationSeed" returns no hits, and a single pinned version cannot attest to a pre-1.18 name (the name is in fact a third-party mapping set's, not a former Mojang name, which the reference also cannot settle) | UNVERIFIABLE | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's set_feature_seed(decoration_seed, index, step) reseeds with decoration_seed + index + step*10000 (wrapping), a pure arithmetic operation that consumes zero RNG draws. | `WorldgenRandom.setFeatureSeed` (WorldgenRandom.java:53-56) — `long result = seed + index + 10000 * step; this.setSeed(result);`, no draw. Minor width note: `10000 * step` is an `int` multiply (harmless over the 0-10 step range), and `index` widens by sign extension | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In vanilla, set_feature_seed's index parameter is a global index assigned once across every biome by FeatureSorter's DFS-topological sort over the whole biome registry's iteration order, not a per-biome-list-local counter; its step parameter is the decoration-step ordinal, ranging 0 to 10 across vanilla's 11 fixed decoration steps. | `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:385-389) passes `globalIndexOfFeature`, an index into `StepFeatureData.features()` produced by `FeatureSorter.buildFeaturesPerStep` (FeatureSorter.java:31-115) — features numbered across all biome sources at :54, ordered by `Graph.depthFirstSearch` at :75 and reversed at :105, then bucketed per step at :108-112; `GenerationStep.Decoration` (GenerationStep.java:10-21) has exactly 11 constants, and the loop bound is `max(Decoration.values().length, featureStepCount)` (ChunkGenerator.java:341-343). Refinement: the index is the position within that step's own DFS-sorted list (so it restarts per step), and the structure branch at :344-347 uses a plain 0-based counter instead | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's set_large_feature_seed(seed, chunk_x, chunk_z) reseeds with seed, draws x_scale = nextLong() and z_scale = nextLong() used unmodified (not forced odd, unlike decoration seed), computes result = (chunk_x*x_scale) XOR (chunk_z*z_scale) XOR seed, and reseeds with result. | `WorldgenRandom.setLargeFeatureSeed` (WorldgenRandom.java:58-64) — `setSeed(seed)` :59, two bare `nextLong()` :60-61, `chunkX * xScale ^ chunkZ * zScale ^ seed` :62, `setSeed(result)` :63; no return value, unlike setDecorationSeed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla calls set_large_feature_seed once per (source_chunk, carver) pair as set_large_feature_seed(world_seed + carver_index, source_chunk_x, source_chunk_z), folding the carver's own additive offset into the seed parameter itself before the call rather than applying it separately afterward. | `NoiseBasedChunkGenerator.applyCarvers` (NoiseBasedChunkGenerator.java:339-347) — `random.setLargeFeatureSeed(seed + index, sourcePos.x(), sourcePos.z())` at :341, inside the per-carver loop, where `seed` is the method's world-seed parameter (:304-306) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's carver_index is scoped per source chunk, reset to 0 for every one of the 289 neighbor chunks visited by the 17x17 source-chunk neighborhood scan, and is never a global index shared with set_feature_seed's own FeatureSorter indexing. | `NoiseBasedChunkGenerator.applyCarvers` (NoiseBasedChunkGenerator.java:326-347) — `for (dx = -8; dx <= 8; dx++) for (dz = -8; dz <= 8; dz++)` is 17*17 = 289 source chunks, and `int index = 0;` is declared INSIDE that pair of loops at :337, incremented at :346; it is a local of this method and never touches `FeatureSorter` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's set_large_feature_with_salt(seed, x, z, salt) reseeds with (x*341873128712 + z*132897987541) + (seed + salt), a pure arithmetic operation consuming zero RNG draws. | `WorldgenRandom.setLargeFeatureWithSalt` (WorldgenRandom.java:66-69) — `long result = x * 341873128712L + z * 132897987541L + seed + blend;` then `setSeed(result)`, no draw. Both literals carry `L`, so each `int * long` product is 64-bit; the four addends associate differently in source order but wrapping addition is associative, so the value matches. Naming note: 26.2 calls the fourth parameter `blend`, not `salt` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's create_random_sequence always uses the Xoroshiro RNG regardless of dimension: it computes base = (world_seed if include_world_seed else 0) XOR salt, applies the unmixed 128-bit seed upgrade to base, optionally XORs in the raw (also unmixed) md5_seed halves of the sequence id when include_sequence_id is set, and finally applies the stafford-13 mix to both resulting halves exactly once, at the end, regardless of which optional steps ran. | `RandomSequences.createSequence` (RandomSequences.java:54-59) — `long seed = (includeWorldSeed ? worldSeed : 0L) ^ salt;` :57 (int salt sign-extends), then `new RandomSequence(seed, includeSequenceId ? Optional.of(key) : Optional.empty())` :58; `RandomSequence.createSequence` (RandomSequence.java:29-36) — `upgradeSeedTo128bitUnmixed` :30, optional `xor(seedForKey(key))` :31-33 over the raw MD5 halves (:38-40, RandomSupport.java:36-41), `seed128bit.mixed()` :35 last and once. The RNG is always `XoroshiroRandomSource` (:35) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every unmodified vanilla 26.2 world uses salt = 0, include_world_seed = true, and include_sequence_id = true as its create_random_sequence per-world defaults. | `RandomSequences` field initializers (RandomSequences.java:30-32) — `private int salt;` (Java default 0), `includeWorldSeed = true`, `includeSequenceId = true`; the codec's own defaults for absent fields are likewise `true`/`true` (:21-22). Only the `/random reset` command path changes them (RandomCommand.java:188-206) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's slime-chunk seeding (seed_slime_chunk) always constructs a fresh, one-shot legacy RNG, deliberately kept on the classic LCG for backward compatibility with pre-1.18 "slime seed" community tooling, independent of whichever RNG algorithm the hosting dimension's own terrain uses. | Mechanism CONFIRMED: `WorldgenRandom.seedSlimeChunk` (WorldgenRandom.java:71-73) returns `RandomSource.createThreadLocalInstance(...)`, i.e. a brand-new `SingleThreadedRandomSource` (RandomSource.java:31-33) — the same 48-bit LCG (SingleThreadedRandomSource.java:29-41) — on every call, with no dimension or noise-settings input. The stated MOTIVE (deliberate backward compatibility with community tooling) is nowhere in the reference and was not verified | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's seed_slime_chunk(x, z, world_seed, salt) computes inner = world_seed + (x*x*4987142, wrapped in 32-bit int space then sign-extended) + (x*5947611, wrapped in 32-bit int space then sign-extended) + ((z*z, wrapped in 32-bit int space then sign-extended) * 4392871, genuinely 64-bit since only that literal carries an L) + (z*389711, wrapped in 32-bit int space then sign-extended), all summed and only then XORed once with salt over the entire expression (matching Java operator precedence, where ^ binds looser than + and *). | Precedence half CONFIRMED, arithmetic WIDTHS WRONG. `WorldgenRandom.seedSlimeChunk` (WorldgenRandom.java:71-73) is `seed + x * x * 4987142 + x * 5947611 + z * z * 4392871L + z * 389711 ^ salt`. Only ONE literal carries `L`: `4392871L`. So `x * x * 4987142`, `x * 5947611`, the inner `z * z`, and `z * 389711` are all evaluated in 32-bit wrapping `int` arithmetic and only then sign-extended into the `long` sum; only `(z*z) * 4392871L` is a 64-bit multiply. The blueprint's Context I pseudocode computes every term in i64 and therefore diverges wherever an int product overflows — `x*x*4987142` already overflows at abs(x) >= 21, `x*5947611` at abs(x) >= 361, `z*z` at abs(z) >= 46341 — i.e. for nearly every real chunk. Correction: mirror the exact widths above | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's own slime-chunk call site uses salt = 987234911 (e.g. in Slime.java), then draws exactly one next_int_bounded(10) and treats a result of 0 as "this is a slime chunk." | `Slime` spawn predicate (world/entity/monster/cubemob/Slime.java:93) — `WorldgenRandom.seedSlimeChunk(chunkPos.x(), chunkPos.z(), worldGenLevel.getSeed(), 987234911L).nextInt(10) == 0` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's seed-string parsing (parse_seed_string) trims the input first, yields no seed at all (letting its caller substitute a freshly generated random seed) when the trimmed result is empty or whitespace-only, and only then tries to parse the trimmed string as an i64, falling back on failure to treating java_string_hash_code of the TRIMMED string (cast to i64) as the seed. | Incomplete, and wrong on two steps. `WorldOptions.parseSeed` (WorldOptions.java:75-86) first does `seedString = seedString.trim();` (:76), then returns `OptionalLong.empty()` for an empty or whitespace-only string (:77-79) — which is NOT hash 0: the caller substitutes a fresh random seed (`withSeed`/`randomSeed`, WorldOptions.java:71-72, 88-90; DedicatedServerProperties.java:137). Only after that does it try `Long.parseLong(trimmed)` (:82) and fall back to `trimmed.hashCode()` (:84) — the hash is of the TRIMMED string, not the raw input. Correction: add the trim and the empty-input random-seed branch to Context J | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Java's Long.parseLong grammar accepts an optional leading + or -, any Basic-Multilingual-Plane Unicode decimal digit for which Character.digit(char, 10) is non-negative (not ASCII decimal digits only — supplementary-plane decimal digits are rejected, since parsing iterates UTF-16 char values one at a time), no embedded whitespace, no underscore digit separators, and range-checks to i64 with overflow rejected rather than wrapped. | "ASCII decimal digits only" is WRONG. JDK 25 src.zip java/lang/Long.java:494-497 specifies that the characters "must all be digits of the specified radix (as determined by whether Character.digit(char, int) returns a nonnegative value)", and `Character.digit` returns a value for every character with `isDigit` true, i.e. all Unicode decimal digits (Character.java:11437-11440) — so `Long.parseLong` accepts e.g. Arabic-Indic U+0660-U+0669 or Devanagari U+0966-U+096F, which Rust's `i64::from_str` rejects (ASCII 0-9 only). Every other part of the claim is confirmed by Long.java:553-595: optional leading `+`/`-` (a sign alone fails, the string must be longer than 1), no whitespace or underscores accepted, and out-of-range values throw rather than wrap. Correction: note the non-ASCII-digit divergence in Context J | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla seed-string parsing test cases confirmed to parse successfully as integers: "5", "+5", "-5", "007" (leading zeros allowed, parses to 7), and both the i64::MIN and i64::MAX boundary strings. | `Long.parseLong` (JDK 25 src.zip java/lang/Long.java:553-595 plus the javadoc examples at :532-544, which include `parseLong("+42") returns 42L`): the sign branch at :576-579 accepts `+`/`-` when the string is longer than 1, leading zeros are ordinary digits, and the negative accumulation at :580-591 admits exactly -9223372036854775808 through 9223372036854775807. Reached through `WorldOptions.parseSeed` (WorldOptions.java:82) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla seed-string parsing test cases confirmed to fail integer parsing and fall through to the string-hash branch (hashing the TRIMMED string): a lone "+" or "-", one past i64::MAX ("9223372036854775808"), non-numeric text, and an underscore-separated numeral ("1_000"); strings with surrounding whitespace (" 5", "5 ") are trimmed first and parse successfully as 5, never reaching the hash branch, and the empty string (or any whitespace-only string) yields no seed at all, with the caller substituting a freshly generated random seed rather than falling through to a hash. | Three of the listed cases are WRONG against vanilla. `WorldOptions.parseSeed` trims first (WorldOptions.java:76), so " 5" and "5 " become "5" and parse successfully as 5 — they never reach the hash branch; and the empty string returns `OptionalLong.empty()` (:77-79), which yields a fresh RANDOM seed at the call site, not hash 0. The remaining cases do fall through to `seedString.hashCode()` (:84): a lone "+" or "-" (Long.java:576-579 requires length > 1 after a sign), "9223372036854775808" (range check, Long.java:586-590), non-numeric text, and "1_000" (`Character.digit('_', 10) < 0`). Correction: retest those three against the trim-then-empty-check shape | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's FLOAT_UNIT constant equals 2^-24 exactly, with bit pattern 0x33800000. | `BitRandomSource.FLOAT_MULTIPLIER = 5.9604645E-8F` (BitRandomSource.java:6) and `XoroshiroRandomSource.FLOAT_UNIT` (XoroshiroRandomSource.java:9); javap of the shipped classes prints the constant-pool value as `float 5.9604645E-8f`, and the nearest f32 to that decimal is exactly 2^-24 (checked by round-tripping the literal through f32: it compares equal to 2^-24, whose IEEE-754 binary32 encoding is exponent field 103 = 0x67, i.e. 0x33800000). JDK `java.util.Random.FLOAT_UNIT = 0x1.0p-24f` (Random.java:303) corroborates | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's DOUBLE_UNIT constant equals 2^-53 exactly, with bit pattern 0x3CA0000000000000. | javap -constants on the shipped jar prints `public static final double DOUBLE_MULTIPLIER = 1.1102230246251565E-16d` for `BitRandomSource` (source line BitRandomSource.java:7), the shortest decimal for 2^-53, whose binary64 encoding is exponent field 970 = 0x3CA, i.e. 0x3CA0000000000000 (verified by bit-pattern round-trip). JDK `java.util.Random.DOUBLE_UNIT = 0x1.0p-53` (Random.java:302) corroborates | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The true Java source constant embedded in vanilla's nextDouble() bytecode is 1.1102230246251565E-16d, the exact double value of 2^-53 (confirmed via javap -c -constants bytecode disassembly against the compiled .class file, with nextDouble() performing a genuine dmul against it), not the truncated decimal 1.110223E-16 that appears in decompiled source (a Vineflower decompiler rendering artifact). | Re-disassembled here: `javap -c -constants -cp server-26.2.jar net.minecraft.world.level.levelgen.BitRandomSource` shows `nextDouble` ending `l2d; ldc2_w // double 1.1102230246251565E-16d; dmul; dreturn`, and the same for `XoroshiroRandomSource.nextDouble` (`bipush 53; nextBits:(I)J; l2d; ldc2_w double 1.1102230246251565E-16d; dmul`). Two refinements worth recording: the decompiled literal keeps an `F` suffix (BitRandomSource.java:7, :59; XoroshiroRandomSource.java:10, :93), and the nearest f32 to 1.110223E-16 is itself exactly 2^-53, so the constant's VALUE survives the rendering; what the rendering loses is the `(double)` cast — as written, `long * float` would be an f32 `fmul`, where the bytecode is an f64 `dmul` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Typing the truncated decimal literal 1.110223E-16 directly as a Rust f64 produces bit pattern 0x3C9FFFFFF4178DA3, a different, off-by-roughly-2^-24-relative-error value from the correct 0x3CA0000000000000. | Recomputed by IEEE-754 round-trip of the decimal 1.110223e-16 into binary64: bits 0x3c9ffffff4178da3, versus 0x3ca0000000000000 for 2^-53; the relative difference is about 2.2e-8, i.e. of order 2^-25.4 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Computing RcXoroshiroRandom::new(0)'s first next_double() using 2f64.powi(-53) as DOUBLE_UNIT reproduces the independently-hand-derived reference value 0.16474369376959186 exactly. | Recomputed from the reference algorithms only (RandomSupport.java:20-34 mixed 128-bit upgrade of seed 0, Xoroshiro128PlusPlus.java:26-34 one core step, XoroshiroRandomSource.java:91-94 and :108-110 for `nextBits(53) * DOUBLE_UNIT` with DOUBLE_UNIT = 2^-53): the first value is 0.16474369376959186 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's positional random factories, both the legacy and Xoroshiro flavors, are stateless: each of .at(x,y,z), .from_hash_of(name), and .from_seed(s) is a pure function of the factory's own captured seed material plus its own argument, consuming no draw from the factory itself. | `LegacyPositionalRandomFactory` (LegacyRandomSource.java:56-86) holds one `final long seed` and its three methods only read it; `XoroshiroPositionalRandomFactory` (XoroshiroRandomSource.java:112-144) holds `final long seedLo`/`seedHi` likewise. Neither class holds or advances a generator, so no method consumes a draw | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's create_random_sequence is a pure, stateless seed-derivation step that computes only the correct starting state for one (sequence_id, world_seed, salt, include_world_seed, include_sequence_id) combination; persisting and continuing the returned generator's stream across many subsequent calls is the caller's own responsibility, not something create_random_sequence itself does. | `RandomSequence.createSequence` (RandomSequence.java:29-36) is `private static`, reads no mutable state and returns a fresh source; the persistence lives in the caller — `RandomSequences.sequences`, a `Map` populated via `computeIfAbsent` in `get` (RandomSequences.java:33, 45-48) and serialized by the saved-data codec (:18-29) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's WorldgenRandom.next(bits) — not any next(bits) on XoroshiroRandomSource itself, which declares none — is the primitive WorldgenRandom draws on when wrapping the Xoroshiro RNG: the top `bits` bits of a freshly drawn nextLong() via an unsigned logical right shift by 64-bits, consuming one full nextLong() state transition per call; XoroshiroRandomSource's own private nextBits(bits) exists only to serve its own nextFloat/nextDouble and is unreachable from WorldgenRandom - a different bit-extraction from Xoroshiro's own native nextInt(), which takes the LOW 32 bits of a fresh nextLong(). | The arithmetic is right but the mechanism is WRONG. `XoroshiroRandomSource implements RandomSource` (XoroshiroRandomSource.java:8), not `BitRandomSource`, and declares no `next(int)` at all; its `private long nextBits(final int bits)` (:108-110) is used ONLY by its own `nextFloat`/`nextDouble` (:86-94). `WorldgenRandom` never calls it: `WorldgenRandom.next(int)` (WorldgenRandom.java:29-35) performs the extraction itself, `randomSource instanceof LegacyRandomSource ? legacy.next(bits) : (int)(this.randomSource.nextLong() >>> 64 - bits)`. Both halves of the arithmetic claim hold — one full `nextLong()` per call, top `bits` bits by `>>>`, versus the low 32 bits in native `nextInt()` (:48-51). Correction: attribute the primitive to `WorldgenRandom.next`, not to a `next(bits)` on XoroshiroRandomSource | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
