# M5-B13b — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Vanilla's StructurePiecesBuilder.moveBelowSeaLevel function (not a BoundingBox method) has a 4-argument signature (seaLevel, minY, random, offset=10) and shifts a whole piece tree's bounding box vertically by targeting its own MAX Y (target = tree Y span + minY + 1, optionally raised toward the ceiling seaLevel - offset), with no clamp of any kind — the resulting tree bottom lands at minY + 2. | No such method exists on `BoundingBox`. The function is `StructurePiecesBuilder.moveBelowSeaLevel` (StructurePiecesBuilder.java:34-46), a `@Deprecated` method on the piece-list builder; its four parameters are `(int seaLevel, int minY, RandomSource random, int offset)` and both call sites (MineshaftStructure.java:63, StrongholdStructure.java:42) pass 10 as `offset`, so the arity and the literal 10 are right. Semantics differ from the claim: it targets the tree bounding box's MAX Y, not a floor clamp — it starts from `box.getYSpan() + minY + 1`, optionally raises that toward `seaLevel - offset`, then shifts every piece by `target - box.maxY()`. A tree taller than `seaLevel - offset - minY - 1` therefore ends up ABOVE sea level, uncapped. Correction for Context §A: rename the cited function to `StructurePiecesBuilder.moveBelowSeaLevel` and restate the formula as target-max-Y rather than floor-clamped-at-minY | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's StructurePiecesBuilder.moveBelowSeaLevel jitters its vertical shift by at most one conditional next_int_bounded draw over the remaining gap to the ceiling seaLevel - offset, not a final next_int_between_inclusive call, and takes zero draws for a tall tree or a low sea level. | StructurePiecesBuilder.java:36-45 — the draw is `random.nextInt(maxY - y1Pos)` (a plain bounded next_int_bounded, NOT next_int_between_inclusive), it is taken CONDITIONALLY (only when `y1Pos < maxY`, i.e. zero draws for a tall tree or a low sea level), and it is not the final step: the draw raises the target max-Y, after which `dy` and the piece shift are computed. Correction for Context §A: `move_below_sea_level` draws at most one `next_int_bounded(maxY - y1Pos)`, guarded by that comparison. The mesa mineshaft path is the one that uses an inclusive-range draw, `Mth.randomBetweenInclusive(random, seaLevel, surfaceHeight)`, and only when `surfaceHeight > seaLevel` (MineshaftStructure.java:58) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A mineshaft structure's skin ("normal" or "mesa") is selected by reading a structure.extra["mineshaft_type"] field, a confirmed field name per the research corpus's structures document section 7. | `MineshaftStructure.CODEC` (MineshaftStructure.java:25-27) reads `MineshaftStructure.Type.CODEC.fieldOf("mineshaft_type")`; the datagen structures carry it as a plain string field — data/minecraft/worldgen/structure/mineshaft.json `"mineshaft_type": "normal"` and mineshaft_mesa.json `"mineshaft_type": "mesa"`. It is a pure data read with no RNG and no biome lookup, exactly as Context §B assumes. The corpus reference is right too (06-structures.md:326 lists `mineshaft: mineshaft_type` in its §7 data-generator cross-reference) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The mineshaft "normal" skin uses oak planks and fences for its blocks, while the "mesa" skin uses dark oak, per the research corpus's structures document section 3.9. | `MineshaftStructure.Type` (MineshaftStructure.java:72-88): NORMAL is constructed with oak log, oak planks and oak fence, MESA with dark oak log, dark oak planks and dark oak fence, each stored as a default block state and exposed via getWoodState/getPlanksState/getFenceState (:98-108). Corpus 06-structures.md:195 says the same. One addition the blueprint omits: each skin carries a third state, the LOG, alongside planks and fence | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Mineshafts are placed via a legacy_type_3 structure-set placement with spacing=1, separation=0, and frequency=0.004. | data/minecraft/worldgen/structure_set/mineshafts.json — placement `"type": "minecraft:random_spread"` with `"frequency": 0.004`, `"frequency_reduction_method": "legacy_type_3"`, `"salt": 0`, `"separation": 0`, `"spacing": 1`. Every number and key matches | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Both mineshaft skins share one StructureSet that is weighted-picked per successful placement roll. | data/minecraft/worldgen/structure_set/mineshafts.json holds exactly two entries in its `structures` list, `minecraft:mineshaft` and `minecraft:mineshaft_mesa`, each `"weight": 1`; the per-roll weighted pick over a shrinking candidate list is `ChunkGenerator`'s structure-creation path (ChunkGenerator.java:485-552, accumulating `option.weight()` and subtracting the drawn value, removing a failed candidate and re-picking) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Mineshaft is the one vanilla structure family whose entire piece tree is built eagerly inside its own point-finding function, purely so the final vertical offset can be computed and folded into the returned stub position, per the research corpus's structures document section 3.9. | `MineshaftStructure.findGenerationPoint` (MineshaftStructure.java:36-43) constructs a `StructurePiecesBuilder`, calls `generatePiecesAndAdjust` (:45-65) which builds the root `MineShaftRoom` and recurses through `addChildren` immediately, and returns `Either.right(builder)` with the start position offset by the computed `yOffset` — the only vanilla structure taking the eager arm of `GenerationStub`'s Either. Corpus 06-structures.md:34, :195 and :347 state the same, including that the Either exists precisely for this case | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A mineshaft's root structure piece is a randomly sized parlor room, not a fixed 10x10 footprint: three draws size its X/Z spans independently 8 to 13 blocks and its Y span 5 to 10 blocks, floored at MAGIC_START_Y = 50, with no corpus confirmation of any fixed footprint. | The root room is not 10x10 and not fixed. `MineshaftPieces.MineShaftRoom`'s generating constructor (MineshaftPieces.java:1071-1079) builds its box as `(west, 50, north)` to `(west + 7 + random.nextInt(6), 54 + random.nextInt(6), north + 7 + random.nextInt(6))` — three draws, giving an X and Z span anywhere from 8 to 13 blocks (independently drawn, so the room is usually not square) and a top Y of 54..59 over a floor at MAGIC_START_Y = 50 (:46). The corpus's §3.4 (06-structures.md:81-90) describes the StructurePiece class hierarchy and states no mineshaft-room footprint at all, so the cited confirmation does not exist. Correction for Context §B: `build_room` must draw three `next_int_bounded(6)` values in that order and size the box as above | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla builds a mineshaft's exits via four fixed-order per-wall scans (north, south, west, east), each a data-dependent number of exits rather than one bool roll per direction, with no 1-to-4 cap and no empty-set fallback draw. | Contradicted by the reference, which settles the behavior so no wiki fetch was needed. `MineShaftRoom.addChildren` (MineshaftPieces.java:1087-1210) runs four independent wall scans in the fixed order north, south, west, east. Each is a `while` loop that advances along the wall by `pos += random.nextInt(span)`, breaks when `pos + 3 > span`, otherwise attempts one child at `minY + random.nextInt(heightSpace) + 1` and then advances a further 4 blocks — so one wall can spawn several exits, the draw count is data-dependent rather than exactly four, there is no per-direction boolean roll, and there is no 1-to-4 cap or empty-set fallback draw. Correction for Context §B: replace the four `next_bool()` draws and the `next_int_bounded(4)` fallback with the four per-wall scan loops | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A mineshaft crossing piece has a fixed 5x5 horizontal footprint but a random dual floor: one next_int_bounded(4) draw, taken before the collision test, makes it two-floored on one draw in four and single-floored otherwise. | Half right. `MineShaftCrossing.findCrossing` (MineshaftPieces.java:628-651) always builds a 5-by-5 horizontal box (spans -1..3 and -4..0 for NORTH, rotated per direction), so the 5x5 footprint holds — but the second floor is random, not fixed: the box's Y extent is 0..6 when `random.nextInt(4) == 0` and 0..2 otherwise, and the constructor sets `isTwoFloored = boundingBox.getYSpan() > 3` (:622-626). A crossing is therefore dual-floored on one draw in four and single-floored otherwise, and the draw happens before the collision test. Corpus §3.4 (06-structures.md:81-90) states nothing about crossings. Correction for Context §B: `build_piece(Crossing, ...)` consumes one `next_int_bounded(4)` and selects a 5x5x7 or 5x5x3 box accordingly, not zero extra draws | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| minecraft.wiki describes mineshaft corridors as 3x3 tunnels. | Settled by the reference. `MineShaftCorridor.findCorridorSize` (MineshaftPieces.java:147-171) builds boxes spanning 0..2 on the two axes perpendicular to travel — 3 wide and 3 tall — and `MineshaftPieces.DEFAULT_SHAFT_WIDTH`/`DEFAULT_SHAFT_HEIGHT` are both 3 (:40-41). The length is `(random.nextInt(3) + 2) * 5` blocks, i.e. 2 to 4 sections of `DEFAULT_SHAFT_LENGTH` = 5 (:42), retried one section shorter on each collision — so Context §B's `next_int_between_inclusive(2, 6)` range is wrong (vanilla is `next_int_bounded(3) + 2`) even though SECTION_LENGTH = 5 is right | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla mineshaft piece generation is fallible per attempt: a piece placement that would collide with an already-placed piece is simply declined rather than generated, matching vanilla's own generateAndAddPiece behavior per the research corpus's structures document section 3.4's framing of piece generation. | `MineshaftPieces.createRandomShaftPiece` (MineshaftPieces.java:51-80) returns null whenever the candidate box fails `findCollisionPiece` — corridors first retry at successively shorter lengths inside their own loop (:155-168), crossings and stairs decline immediately (:645-650) — and `generateAndAddPiece` (:82-108) then adds nothing and recurses no further. Two extra decline conditions the blueprint should note: the same function refuses outright when `depth > 8` (:92-94, matching `MAX_DEPTH` = 8 at :45, so CHAIN_LENGTH_LIMIT = 8 is correct) and when the candidate foot is more than 80 blocks in X or Z from the start piece's box minimum (:96) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A mineshaft's point-finding function seeds its random source via set_large_feature_seed(world_seed, chunk_x, chunk_z) before any piece generation begins. | `Structure.GenerationContext.makeRandom` (Structure.java:250-253) calls `random.setLargeFeatureSeed(seed, chunkPos.x(), chunkPos.z())` when the context is constructed (:222-247), before `findGenerationPoint` is invoked, and `MineshaftStructure` never re-seeds. One RNG-order detail the blueprint's pseudocode omits: `MineshaftStructure.findGenerationPoint` burns exactly one `context.random().nextDouble()` (MineshaftStructure.java:37) before the root room is built, so every subsequent mineshaft draw is offset by that discarded double | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Stronghold ring placement uses a ConcentricRingsPlacement with distance=32, spread=3, count=128, and salt=0, biased toward the #minecraft:stronghold_biased_to tag. | data/minecraft/worldgen/structure_set/strongholds.json — placement `"type": "minecraft:concentric_rings"` with `"count": 128`, `"distance": 32`, `"spread": 3`, `"salt": 0` and `"preferred_biomes": "#minecraft:stronghold_biased_to"`; the single structure entry is `minecraft:stronghold` at weight 1. Corpus 06-structures.md:179 and :256 agree | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold Straight piece has weight 40, an unlimited maximum placement count, and no depth gate. | `StrongholdPieces.STRONGHOLD_PIECE_WEIGHTS` (StrongholdPieces.java:45-67), entry at :46 — `PieceWeight(Straight.class, 40, 0)`; a maxPlaceCount of 0 means unlimited per `PieceWeight.doPlace`/`isValid` (:747-753), and the entry has no `doPlace` override, hence no depth gate | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold PrisonHall piece has weight 5, a maximum placement count of 5, and no depth gate. | StrongholdPieces.java:47 — `PieceWeight(PrisonHall.class, 5, 5)`, no `doPlace` override | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold LeftTurn piece has weight 20, an unlimited maximum placement count, and no depth gate. | StrongholdPieces.java:48 — `PieceWeight(LeftTurn.class, 20, 0)`, 0 meaning unlimited (:747-753), no `doPlace` override | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold RightTurn piece has weight 20, an unlimited maximum placement count, and no depth gate. | StrongholdPieces.java:49 — `PieceWeight(RightTurn.class, 20, 0)`, 0 meaning unlimited (:747-753), no `doPlace` override | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold RoomCrossing piece has weight 10, a maximum placement count of 6, and no depth gate. | StrongholdPieces.java:50 — `PieceWeight(RoomCrossing.class, 10, 6)`, no `doPlace` override | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold StraightStairsDown piece has weight 5, a maximum placement count of 5, and no depth gate. | StrongholdPieces.java:51 — `PieceWeight(StraightStairsDown.class, 5, 5)`, no `doPlace` override | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold StairsDown piece has weight 5, a maximum placement count of 5, and no depth gate. | StrongholdPieces.java:52 — `PieceWeight(StairsDown.class, 5, 5)`, no `doPlace` override | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold FiveCrossing piece has weight 5, a maximum placement count of 4, and no depth gate. | StrongholdPieces.java:53 — `PieceWeight(FiveCrossing.class, 5, 4)`, no `doPlace` override. Note for Context §C: FiveCrossing is nonetheless reachable outside the weighted draw, because the start piece forces it as `imposedPiece` for its own first child (:1232-1234), and that forced placement bypasses the weight table without incrementing any count | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold ChestCorridor piece has weight 5, a maximum placement count of 4, and no depth gate. | StrongholdPieces.java:54 — `PieceWeight(ChestCorridor.class, 5, 4)`, no `doPlace` override | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold Library piece has weight 10, a maximum placement count of 2, and is gated to depth > 4 per the research corpus, matching minecraft.wiki's "not within 4 rooms of the start". | StrongholdPieces.java:55-60 — `PieceWeight(Library.class, 10, 2)` with an anonymous subclass whose `doPlace(depth)` returns `super.doPlace(depth) && depth > 4`. The gated value is the CHILD depth: `generateAndAddPiece` passes `depth + 1` into `generatePieceFromSmallDoor` (:212), which forwards it to `doPlace` (:173) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold PortalRoom piece has weight 20, a maximum placement count of 1, and is gated to depth > 5 per the research corpus, matching minecraft.wiki's "never within 5 rooms of the start". | StrongholdPieces.java:61-66 — `PieceWeight(PortalRoom.class, 20, 1)` with `doPlace(depth)` returning `super.doPlace(depth) && depth > 5`; same child-depth convention as the Library gate (:173, :212) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| On each stronghold generation retry, vanilla re-seeds its random source with world_seed + tries, an incrementing-salt-by-tries scheme the research corpus's structures document section 3.9 describes as "re-seeds context.random() with context.seed() + tries". | `StrongholdStructure.generatePieces` (StrongholdStructure.java:23-44), line 29: `context.random().setLargeFeatureSeed(context.seed() + tries++, context.chunkPos().x(), context.chunkPos().z())` inside the do-while. `tries` starts at 0 (:24) and is post-incremented, so the first attempt uses seed + 0 exactly as Context §C's test 4 expects. Corpus 06-structures.md:179 states the same | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each stronghold generation retry attempt resets its own placed-piece-count tracking, vanilla's "resetPieces" behavior per the research corpus's structures document section 3.9. | StrongholdStructure.java:30 calls `StrongholdPieces.resetPieces()` every attempt; `resetPieces` (StrongholdPieces.java:76-85) rebuilds `currentPieces` from the static weight table, zeroes every `placeCount`, and clears `imposedPiece`. The attempt also discards the pieces themselves via `builder.clear()` (StrongholdStructure.java:28) — the blueprint's `pieces = vec![start]` covers that implicitly, but the `imposedPiece` reset is a third piece of per-attempt state Context §C does not model | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold start piece is placed at y=64, HIGH confidence per both the research corpus and minecraft.wiki. | `StrongholdPieces.MAGIC_START_Y = 64` (StrongholdPieces.java:44); the start piece's constructor chain `StartPiece(random, west, north)` (:1292-1294) calls the source `StairsDown` constructor (:1201-1206) which builds `makeBoundingBox(west, 64, north, direction, 5, 11, 5)` — the literal 64 is the box's Y origin. The start direction costs one draw, `getRandomHorizontalDirection(random)` (:1293) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| minecraft.wiki describes the stronghold start piece as a spiral staircase flowing into a five-way crossing. | Settled by the reference. `StartPiece` extends `StairsDown` (StrongholdPieces.java:1287-1300), a 5-wide, 11-tall, 5-deep shaft whose `postProcess` lays a descending ring of stone-brick and smooth-stone-slab steps around the shaft walls (:1266-1285) — a spiral staircase; and because the source variant sets `isSource`, its `addChildren` (:1231-1237) assigns `StrongholdPieces.imposedPiece = FiveCrossing.class` so the next piece generated is forced to be the five-way crossing. Correction the blueprint needs anyway: that `addChildren` emits exactly ONE forward child via `generateSmallDoorChildForward(..., 1, 1)`, not the "up to 5 initial exit candidates" Context §C reconstructs — the branching comes from the FiveCrossing itself | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla drains the stronghold's pending-piece queue in random removal order rather than FIFO order, per the research corpus's structures document section 3.9. | StrongholdStructure.java:36-40 — `while (!pendingChildren.isEmpty())` draws `context.random().nextInt(pendingChildren.size())` and calls `List.remove(index)` on that position, then expands that piece. One draw per dequeue, exactly as Context §C models. Note the list is `startRoom.pendingChildren` (StrongholdPieces.java:1290), appended by `generateAndAddPiece` (:215) as pieces are created, so it grows while being drained | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Stronghold piece expansion stops only once a pending piece's own depth strictly exceeds 50 (MAX_DEPTH), per the research corpus's structures document section 5 — a piece at depth exactly 50 still expands, producing children at depth 51. | The comparison is strictly greater, not "reaches or exceeds". `StrongholdPieces.generateAndAddPiece` (StrongholdPieces.java:207-209) returns null only when `depth > 50`, where `depth` is the PARENT piece's genDepth and the child is built at `depth + 1` (:212). A parent at depth exactly 50 therefore still expands, producing children at genDepth 51. `MAX_DEPTH = 50` (:41) is the named constant but the literal 50 is inlined at the comparison. Correction for Context §C: the cutoff is `child.depth > MAX_DEPTH`, not `>=` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Stronghold piece expansion stops once a pending piece's Chebyshev distance from the start piece exceeds 112 blocks (the piece search radius), per the research corpus's structures document section 5. | `StrongholdPieces.generateAndAddPiece` (StrongholdPieces.java:211) proceeds only while `Math.abs(footX - startPiece.getBoundingBox().minX()) <= 112 && Math.abs(footZ - startPiece.getBoundingBox().minZ()) <= 112` — exactly `max(abs(dx), abs(dz)) <= 112`, a horizontal Chebyshev test, and the piece is declined once it exceeds 112. Two details worth carrying into Context §C: the reference point is the START PIECE's bounding-box minimum corner (not its centre or locator position) and the measured point is the candidate's foot position; Y is not considered. Corpus 06-structures.md:259 agrees | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| At each stronghold pending-piece expansion, vanilla makes up to 5 weighted draws attempting to find a non-colliding piece, a confirmed shape per the research corpus's structures document section 3.9. | `StrongholdPieces.generatePieceFromSmallDoor` (StrongholdPieces.java:164-191) loops `while (numAttempts < 5)`, taking exactly one `random.nextInt(totalWeight)` per attempt (:168) and walking the weight list by subtraction until the running value goes negative (:170-172). An attempt can also be consumed without any collision test, because the selected entry is abandoned when `doPlace(depth)` fails or when it is the same PieceWeight as the previous placement (:173-175) — a rule Context §C does not model. `totalWeight` is recomputed by `updatePieceWeight` (:87-100) over ALL still-listed entries, including gated ones, so the divisor is not the eligible-set sum the blueprint's hand-derived vectors assume | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| If all 5 of a stronghold pending-piece expansion's weighted-draw attempts collide, vanilla's filler-piece fallback is conditional, not unconditional: it places a FillerCorridor only when a box can be found plugging the gap to an already-placed piece with min_y() > 1, otherwise the branch dead-ends with no piece placed at all. | The fallback is conditional, and frequently places nothing. StrongholdPieces.java:193-194 asks `FillerCorridor.findPieceBox` for a box and returns a FillerCorridor only when `box != null && box.minY() > 1`, otherwise null — and `findPieceBox` (:317-345) itself returns null whenever the candidate 5x5x4 box hits NO existing piece, since a filler exists only to plug the gap up to a piece already there, shrinking to depth 2 or 1 to fit. Correction for Context §C: the `unwrap_or_else` fallback must be able to yield "no piece at all", dead-ending the branch, rather than always producing a FillerCorridor | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla retries stronghold piece-graph generation from scratch, unboundedly, until an attempt successfully places a PortalRoom piece, confirmed independently by both the research corpus and minecraft.wiki. | `StrongholdStructure.generatePieces` (StrongholdStructure.java:23-44) is a do-while loop whose continue condition is "builder is empty OR startRoom.portalRoomPiece is null", with no attempt cap — each pass clears the builder, re-seeds, resets the piece bookkeeping and regrows the whole tree. The loop condition carries a second reject the blueprint omits: an EMPTY builder also retries, independently of the portal room. Corpus 06-structures.md:179 states the same | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The stronghold's own final vertical-placement call uses the dimension's own minimum build height as min_y (not LOWEST_Y_POSITION = 10) and a jitter value of 10; LOWEST_Y_POSITION = 10 is instead the per-piece isOkBox rejection floor applied when each candidate piece box is created. | The jitter/offset argument is 10, the min_y argument is not. StrongholdStructure.java:42 calls `builder.moveBelowSeaLevel(context.chunkGenerator().getSeaLevel(), context.chunkGenerator().getMinY(), context.random(), 10)` — the second argument is the DIMENSION's minimum build height from the chunk generator (-64 in the overworld), not 10. `LOWEST_Y_POSITION = 10` does exist (StrongholdPieces.java:42) but is unused at that call site; the only 10-block floor in this family is the per-piece validity test `isOkBox` = `box.minY() > 10` (:1742-1744), applied when each candidate piece box is created. Correction for Context §C: pass the generator's min Y, and model `LOWEST_Y_POSITION` as the `isOkBox` piece-rejection floor instead | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ChestCorridor structure's chest(s) use the loot table minecraft:chests/stronghold_corridor. | `StrongholdPieces.ChestCorridor.postProcess` (StrongholdPieces.java:289-293) calls `createChest(..., 3, 2, 3, BuiltInLootTables.STRONGHOLD_CORRIDOR)`, and `BuiltInLootTables.STRONGHOLD_CORRIDOR` is registered as `chests/stronghold_corridor` in the minecraft namespace (BuiltInLootTables.java:37). Exactly one chest per piece: the call is guarded by a `hasPlacedChest` latch and by the chest position lying inside the chunk box being written | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Library structures deterministically include a chest on their upper floor whenever the tall two-storey variant (isTall = boundingBox.getYSpan() > 6) fits — there is no rarity roll of any kind. | There is no rarity roll. `StrongholdPieces.Library.postProcess` (StrongholdPieces.java:727-732) always places a ground-floor chest at (3, 3, 5) and, whenever `isTall`, UNCONDITIONALLY places a second chest at (12, 8, 1) after clearing the block above it. `isTall` is not random either: it is `boundingBox.getYSpan() > 6` (:581), and `Library.createPiece` (:595-611) first tries the two-storey 14x11x15 box, falling back to the single-storey 14x6x15 box only when the tall box fails `isOkBox` or collides. Correction for Context §C: the upper-floor chest is a deterministic consequence of the tall variant fitting, not a rare roll | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Library structure's upper-floor chest uses the loot table minecraft:chests/stronghold_library. | StrongholdPieces.java:730 — `createChest(..., 12, 8, 1, BuiltInLootTables.STRONGHOLD_LIBRARY)`, with `BuiltInLootTables.STRONGHOLD_LIBRARY` registered as `chests/stronghold_library` (BuiltInLootTables.java:35). The same table is used by the ground-floor chest at (3, 3, 5) (:727), so both Library chests share it | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
