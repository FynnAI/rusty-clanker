# M5-B10 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| The Anvil region file format's header is 8 KiB, made up of 1024 4-byte location entries followed by 1024 4-byte Unix-timestamp entries, in that order. | `RegionFile` (RegionFile.java:42, 61-64): `header = ByteBuffer.allocateDirect(8192)`; `offsets = header.asIntBuffer()` with `limit(1024)` covers bytes 0-4095, then `header.position(4096)` and `timestamps = header.asIntBuffer()` covers bytes 4096-8191. The constructor reads exactly 8192 header bytes at offset 0 (:73-77) and warns on a shorter read; `SECTOR_BYTES = 4096`, `SECTOR_INTS = 1024` (:27-29). Timestamps are seconds since epoch (`getTimestamp`, :155-157) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each region-file header location entry is encoded as a big-endian (sector_offset: u24, sector_count: u8) pair. | `RegionFile.packSectorOffset` (RegionFile.java:201-203) writes `index << 8 or size`; `getSectorNumber` (:209-211) reads `offset >> 8 & 16777215` (24 bits) and `getNumSectors` (:205-207) reads `offset & 0xFF` (8 bits). The ints come from `header.asIntBuffer()` on a `ByteBuffer` left at its default BIG_ENDIAN order (:42, :61-64), so the on-disk byte order is big-endian. An all-zero entry means "chunk not present" (`CHUNK_NOT_PRESENT = 0`, :36) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A chunk's record within a region file is located via (local_x, local_z) = (chunk_x.rem_euclid(32), chunk_z.rem_euclid(32)), with the record beginning at sector_offset * 4096 bytes into the file. | `RegionFile.getOffsetIndex` (RegionFile.java:349-351) = `pos.getRegionLocalX() + pos.getRegionLocalZ() * 32`, and `ChunkPos.getRegionLocalX/Z` (ChunkPos.java:147-153) = `x & 31` / `z & 31`, which is exactly `rem_euclid(32)` on a two's-complement i32. The record read is `this.file.read(buffer, sectorNumber * 4096)` (RegionFile.java:122, and :228 for the existence probe). Note the index is row-major in Z (`local_x + local_z * 32`), and the region file itself is `r.<chunk_x>>5>.<chunk_z>>5>.mca` (RegionFileStorage.java:45) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A chunk's on-disk record begins with a 4-byte big-endian length field giving the payload byte count including the 1-byte compression tag, followed by length-1 bytes of the (possibly external) compressed payload. | `RegionFile.getChunkDataInputStream` (RegionFile.java:129-151): `int length = buffer.getInt(); byte versionId = buffer.get(); int streamLength = length - 1;` on a default-BIG_ENDIAN `ByteBuffer`, with `CHUNK_HEADER_SIZE = 5` (:30) confirming the 4+1 framing. Two nuances for the reader: the tag byte sits physically between the length field and the payload (order is length, tag, payload), and in the external case the in-file payload is zero bytes long (`length == 1`, a non-zero `streamLength` there is only logged as "Chunk has both internal and external streams", :137-142) — the bytes come from the external file instead | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The record's leading compression tag byte has its 0x80 bit set when the payload is stored in an external file, and its low 7 bits select the compression scheme: 1=GZip, 2=Zlib, 3=uncompressed, 4=LZ4. | `RegionFile.EXTERNAL_STREAM_FLAG = 128` (RegionFile.java:34); `isExternalStreamChunk` (:159-161) = `(version & 128) != 0` and `getExternalChunkVersion` (:163-165) = `version & -129` (clears bit 7). Scheme ids in `RegionFileVersion` (RegionFileVersion.java:26-44): 1 = GZip, 2 = "deflate" (zlib Inflater/Deflater streams), 3 = "none" (uncompressed), 4 = "lz4", plus 5 = VERSION_CUSTOM id 127 which a reader must reject rather than treat as unknown-garbage. Implementation note: id 4 is jpountz `LZ4BlockInputStream` framing, not raw LZ4 blocks, so a raw-LZ4 decoder will not read it. `DEFAULT = VERSION_DEFLATE` (:45) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When a chunk record's payload is stored externally, it lives in a paired c.<chunk_x>.<chunk_z>.mcc file, named from the chunk coordinates, alongside the region file. | The prefix and the coordinates are both wrong. `RegionFile.getExternalChunkPath` (RegionFile.java:107-110) builds `"c." + pos.x() + "." + pos.z() + ".mcc"` — prefix `c.`, not `r.`, and CHUNK coordinates, not region coordinates. It resolves against `externalFileDir`, which `RegionFileStorage` (RegionFileStorage.java:44-46) passes as the same region folder that holds `r.<regionX>.<regionZ>.mca`, so "alongside the region file" is right. `EXTERNAL_FILE_EXTENSION = ".mcc"`, `EXTERNAL_CHUNK_THRESHOLD = 256` sectors (RegionFile.java:33-35). Correction for §D.3: the paired file is `c.<chunk_x>.<chunk_z>.mcc` in the region directory | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In the chunk NBT format, the root compound's "sections" field is a list of compounds: every section index -4..=19 is always present (including all-air ones), plus a light-only entry may additionally appear at Y=20 and/or Y=-5 carrying light data but no block_states/biomes; each entry carries a signed byte "Y" field holding the section index, ranging -5..=20 for a world with WORLD_MIN_Y=-64 and WORLD_HEIGHT=384, and discarded when outside -4..=19 before mapping to local_section_index = Y + 4. | The field name, list-of-compounds shape and signed-byte "Y" are right; the range and the "populated" qualifier are wrong. `SerializableChunkData.copyOf` (SerializableChunkData.java:351-362) walks `sectionY` from `lightEngine.getMinLightSection()` to `getMaxLightSection()` exclusive, and `LevelLightEngine` (LevelLightEngine.java:157-166) makes that `minSectionY - 1` up to `minSectionY - 1 + sectionCount + 2`. For minY=-64/height=384 (minSectionY=-4, maxSectionY=19, LevelHeightAccessor.java:15-25) that is sectionY -5..20, and a light-only section (typically Y=20, carrying full sky light above the build limit) is emitted with `SkyLight`/`BlockLight` but no `block_states`/`biomes` (:356-361, write at :438-457). Conversely every section -4..19 is ALWAYS written, including all-air ones, because `ChunkAccess` back-fills null sections with default containers (ChunkAccess.java:108-116) so `hasSection` holds for the whole range. Correction for §D.4: "Y" ranges -5..=20; map it as `local_section_index = Y + 4` only after discarding Y values outside 0..24, and expect all 24 in-range sections present rather than only populated ones | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each section compound's "block_states" field is a compound containing a "palette" field, a list of compounds, and a "data" field, an optional LongArray. | `SerializableChunkData` reads/writes the section subfield `block_states` through the block-state container codec (SerializableChunkData.java:173-179 read, :442 write); that codec is `PalettedContainer.codec` (PalettedContainer.java:49-62) = `elementCodec.listOf().fieldOf("palette")` plus `Codec.LONG_STREAM.lenientOptionalFieldOf("data")`. The element codec is `BlockState.CODEC` (PalettedContainerFactory.java:34), which serialises each entry as a compound (StateHolder.java:148-162), and `NbtOps` renders the long stream as TAG_Long_Array. `data` is genuinely optional (lenient) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each "block_states" palette entry compound has a "Name" string field and an optional "Properties" compound of string-to-string key-value pairs. | `StateHolder.codec` (StateHolder.java:148-162) dispatches on the field literal `"Name"` (`NAME_TAG`, :18) and, for any non-singleton state definition, attaches `definition.propertiesCodec().codec().lenientOptionalFieldOf("Properties")` (`PROPERTIES_TAG`, :19). Every property value is serialised through `Property.valueCodec` built on `Codec.STRING` (Property.java:17-24, 47-48), so the Properties compound is string-to-string. A block with no properties emits no Properties field at all (`isSingletonState` branch, StateHolder.java:157-158) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each section compound's "biomes" field has the same palette/data shape as "block_states", except palette entries are plain namespaced-id strings, at 4x4x4-block-per-cell granularity. | Same codec (`PalettedContainer.codec`, PalettedContainer.java:49-62) is used for the `biomes` subfield (SerializableChunkData.java:180-186 read, :443 write), so `palette` plus optional `data` LongArray is identical; the element codec is `biomes.holderByNameCodec()` (PalettedContainerFactory.java:37), i.e. a plain namespaced-id string. Granularity: `Strategy.createForBiomes` uses `bitsPerAxis = 2` (Strategy.java:50-51) giving `entryCount = 1 << 6` = 64 cells of 4x4x4 blocks per section, versus 4096 for block states. Caveat, see the bit-packing row below: the shape is the same but the bits-per-entry rule is NOT | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A section index absent from a chunk's "sections" list means that whole section is all-air (for blocks) or default-biome (for biomes). | `SerializableChunkData.read` (SerializableChunkData.java:226-239) leaves the `LevelChunkSection[]` slot null for any section index the list did not supply, and `ChunkAccess.replaceMissingSections` (ChunkAccess.java:108-116) fills every null slot with `new LevelChunkSection(containerFactory)` (LevelChunkSection.java:36-39), which builds `createForBlockStates`/`createForBiomes` single-value containers. The same defaults apply when a listed section omits the subfield (`orElseGet(containerFactory::createForBlockStates/ForBiomes)`, SerializableChunkData.java:179, :186). Concrete defaults for a from-scratch reader: `Blocks.AIR.defaultBlockState()` and biome `minecraft:plains` (PalettedContainerFactory.java:27-30) — the biome default is specifically plains, not registry index 0 or an "unknown" sentinel | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's post-1.18 paletted-container bit-packing never lets an entry cross a 64-bit long boundary: values_per_long = 64 / bits_per_entry using integer division (unused high bits per long are padding, never packed into), entry i's raw palette index is (data[i / values_per_long] >> ((i % values_per_long) * bits_per_entry)) & ((1u64 << bits_per_entry) - 1), and bits_per_entry is derived per container: 0 for a single-entry palette, else max(4, ceil(log2(palette_len))) for block states or plain ceil(log2(palette_len)) for biomes (no 4-bit floor). | The packing/extraction half is exact; the bits_per_entry formula is block-states-only and must not be applied to biomes. `SimpleBitStorage` (SimpleBitStorage.java:244-254, 295-301): `valuesPerLong = (char)(64 / bits)`, array length `(size + valuesPerLong - 1) / valuesPerLong`, and `get` = `(data[cellIndex] >> ((index - cellIndex * valuesPerLong) * bits)) & ((1L << bits) - 1)` with `cellIndex = index / valuesPerLong` (:266-269) — matches the claim's formula exactly, and leftover high bits per long are never used. But the disk bit width comes from `Strategy.getConfigurationForPaletteSize` (Strategy.java:83-90) = `getConfigurationForBitCount(Mth.ceillog2(paletteSize))`, and that mapping differs per container: block states (Strategy.java:33-48) map bit counts 1-4 all up to 4 bits, 5-8 to themselves, above 8 to `Configuration.Global` whose `bitsInStorage` is the raw `ceillog2` (Configuration.java:14-27) — i.e. `max(4, ceil(log2(palette_len)))`; BIOMES (Strategy.java:50-63) map 1 to 1 bit, 2 to 2, 3 to 3, above 3 to the raw `ceillog2` — i.e. plain `ceil(log2(palette_len))` with NO 4-bit floor. Correction for §D.4's shared `unpack_paletted_indices`: it must take bits_per_entry from the caller (block-state rule vs biome rule), never derive `max(4, ...)` for both, or every multi-biome section decodes as garbage | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When a paletted container's palette has exactly one entry, its "data" long array is entirely absent and every position resolves directly to palette[0], never requiring a zero-bit read. | `PalettedContainer.pack` (PalettedContainer.java:225-248): `bitsOnDisc` for a size-1 palette is 0 (`Mth.ceillog2(1) == 0` -> `ZERO_BITS`, Strategy.java:10, 83-90), and the `bitsOnDisc != 0` guard leaves `values = Optional.empty()`, so no `data` field is emitted. `PalettedContainer.unpack` (:182-221) mirrors it: `bitsInMemory() == 0` builds a `SingleValuePalette` plus `ZeroBitStorage` and never touches `discData.storage()`; only the non-zero branch errors with "Missing values for non-zero storage" (:198-200). Holds identically for the biome container | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's "forceload" console command keeps a rectangular chunk region loaded and, if a chunk is not yet generated, triggers its generation, with no player needing to be present. | `ForceLoadCommand.changeForceLoad` (ForceLoadCommand.java:154-184) takes two block-column corners, converts them to chunk coords, and iterates the full inclusive `minChunkX..maxChunkX` x `minChunkZ..maxChunkZ` rectangle calling `level.setChunkForced(x, z, true)`. `ServerLevel.setChunkForced` (ServerLevel.java:1506-1513) registers the forced ticket and, when newly added, calls `this.getChunk(chunkX, chunkZ)` — a load-or-generate to `ChunkStatus.FULL`. The ticket is `TicketType.FORCED` (TicketType.java:22) with timeout 0 (never expires) and flags 15 (persist plus load plus simulate plus keep-dimension-active), so it holds the chunks without any player. Hard limit relevant to §D.1: the rectangle may not exceed `MAX_CHUNK_LIMIT = 256` chunks per command (:24-27, :164-167), which the 21x21 = 441-chunk near-field block EXCEEDS — that single command will fail with commands.forceload.toobig and must be split | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In vanilla, chunk generation triggered by "forceload" happens asynchronously on background worker threads, and the generated chunk data is not guaranteed to be reflected on disk until an explicit save such as "save-all flush" is performed. | Both halves hold. Generation runs on a `ConsecutiveExecutor` named "worldgen" wrapping the server's shared background executor, distinct from the main-thread executor (ChunkMap.java:162-163, 191-210; chunk parse/encode likewise on `Util.backgroundExecutor()`, :566, :773, :924). Persistence is separate: `save-all flush` maps to `MinecraftServer.saveEverything(true, flush, true)` (SaveAllCommand.java:20-33), and chunks otherwise only reach disk on autosave/unload (ChunkMap.java:430, :773). Nuance worth recording for §D.2: `ServerLevel.setChunkForced` (ServerLevel.java:1509) calls the BLOCKING `getChunk`, which does `mainThreadProcessor.managedBlock(future::isDone)` (ServerChunkCache.java:165-167), so by the time the forceload command returns the chunks are already FULL in memory — only the disk write still requires the save, which is exactly what D.2's settle-poll must wait on | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In vanilla, a structure-placed container stores only an unrolled loot-table reference plus a seed at chunk generation time, with the actual loot contents rolled lazily on first access to the container (not only a player interaction); a structure-placed spawner stores no loot-table reference at all, only spawn data (entity type plus a seed). | True for containers, false for spawners. Containers: `RandomizableContainer.setBlockEntityLootTable` (RandomizableContainer.java:42-48) stores only `LootTable` plus `LootTableSeed` (`random.nextLong()`), persisted as those two tags (:22-23, :57-70), and `unpackLootTable` (:72-90) rolls `lootTable.fill(...)` with that seed on first container access. Spawners store no loot table at all: a structure-placed spawner gets an entity id via `SpawnerBlockEntity.setEntityId` (e.g. MineshaftPieces.java:434-435 for the cave-spider spawner), which is spawn data, not a loot reference. Second nuance: `unpackLootTable` fires on ANY container access, not only a player's — `RandomizableContainerBlockEntity` calls it from `getContainerSize`/`getItem`/`removeItem`/`setItem`/`isEmpty` (RandomizableContainerBlockEntity.java:50-86), so a hopper or comparator reading the block also unrolls it. The §C conclusion (block-entity NBT carries a reference plus seed at generation, not rolled items) still stands for containers | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In vanilla's protocol, each chunk that enters a client's render distance is sent to that client as a "Level Chunk with Light" class packet. | `PlayerChunkSender.sendChunk` (PlayerChunkSender.java:76-84) sends exactly one `ClientboundLevelChunkWithLightPacket` per chunk; chunks enter the queue through `markChunkPendingToSend` (:40-42), driven by `ChunkMap.applyChunkTrackingView`'s enter-callback when the tracking view changes (ChunkMap.java:1122-1131), and leave with `ClientboundForgetLevelChunkPacket` (:44-48). The packet is `minecraft:level_chunk_with_light` in reports/packets.json (play / clientbound). Nuance for the bot-side chunk counter in §I.2: the per-chunk packets are wrapped in `ClientboundChunkBatchStartPacket` / `ClientboundChunkBatchFinishedPacket` and rate-limited by a per-tick batch quota (PlayerChunkSender.java:50-74), so loaded-chunk count ramps up over several ticks and the bot must acknowledge batches to keep them flowing | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's client chunk-loading area for a given render distance of r chunks is a rounded square, not a full (2r+1) by (2r+1) grid and not a circular approximation either: a chunk at relative offset (dx, dz) is tracked/sent iff max(0, |dx|-2)^2 + max(0, |dz|-2)^2 < r*r (637 chunks at r=12, versus 625 for (2r+1)^2 and 533 for the narrower in-view-only variant that drops the buffer to 1). | It is a rounded square, not a full square. `ChunkTrackingView.isWithinDistance` (ChunkTrackingView.java:71-80) computes `dx = max(0, abs(chunkX - centerX) - buffer)`, `dz` likewise, and requires `dx*dx + dz*dz < r*r`, with buffer 1 for `isInViewDistance` (:63-69) and buffer 2 for the default `contains` (:55-57) that `Positioned.forEach` (:113-121) and `difference` (:31-43, feeding markChunkPendingToSend at ChunkMap.java:1129) actually use. `r` is `clamp(player.requestedViewDistance(), 2, serverViewDistance)` (ChunkMap.java:825-826, 1110-1119). Corner chunks are therefore cut: at r=12 the in-view set is 533 chunks and the tracked/sent set is 637, neither of which is 625 (counts computed directly from that formula). Correction for §I.1: `EXPECTED_LOADED_CHUNK_COUNT = (2r+1)^2 = 625` is not a value any bot can reach, and asserting `loaded_count >= 625` would fail permanently — derive the expected count from the same buffer-2 predicate (637 at r=12) or assert against the buffer-1 in-view count (533) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
