# M5-B12b — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Vanilla's delta_feature configuration (DeltaFeatureConfiguration) has JSON fields "contents" (a plain BlockStateSpec, not a BlockStateProvider), "rim" (a plain BlockStateSpec, not a BlockStateProvider), "size" (an IntProvider clamped 0..=16), and "rim_size" (an IntProvider clamped 0..=16). | The four key names and their codec order are right, the types of the first two are not. `DeltaFeatureConfiguration.CODEC` (DeltaFeatureConfiguration.java:10-18) declares `contents` and `rim` as plain `BlockState.CODEC` fields (single block states), NOT block-state providers; only `size` and `rim_size` are IntProviders, each range-clamped to 0..16 by `IntProviders.codec(0, 16)`. data/minecraft/worldgen/configured_feature/delta.json confirms: "contents" is the literal state minecraft:lava level=0 and "rim" the literal minecraft:magma_block, while "size" and "rim_size" are uniform int providers. Using a provider for contents/rim would add RNG draws vanilla never takes | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| delta_feature first draws a spawn_rim boolean (random.next_double() < 0.9); when true it samples config.rim_size twice for independent rim_x/rim_z offsets, otherwise both are 0 with no draw; only then does it sample config.size twice for independent radius_x/radius_z values, with radius_limit = max(radius_x, radius_z) and no rim_r = r + rim_size sum anywhere. | The order is reversed and the draw count is wrong. `DeltaFeature.place` (DeltaFeature.java:27-40) draws, in order: (1) one `random.nextDouble()` against the 0.9 RIM_SPAWN_CHANCE constant (DeltaFeature.java:20, :33); (2) if that passes, TWO `config.rimSize().sample(random)` draws producing independent rimX and rimZ offsets (:34-35), otherwise both are 0 with no draw; (3) TWO `config.size().sample(random)` draws producing independent radiusX and radiusZ (:37-38). rim_size is therefore sampled BEFORE size, twice each, and no rim_r = r + rim_size sum exists anywhere; rimX/rimZ are used as a translation offset (:52), not as an outer radius. radiusLimit = max(radiusX, radiusZ) (:39) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| delta_feature iterates a Manhattan-radius walk (radius_x, vertical reach 0, radius_z) centered on origin, breaking once a cell's Manhattan distance exceeds radius_limit, writing config.rim at each accepted cell that has_rim and config.contents at that cell's own (rim_x, 0, rim_z) offset when that offset cell also passes the is_clear gate — never a 3D-distance ellipsoid test. | Neither the iteration shape nor the write rule matches. `DeltaFeature.place` (DeltaFeature.java:41-58) iterates `BlockPos.withinManhattan(origin, radiusX, 0, radiusZ)` — a Manhattan-distance (diamond) walk with vertical reach ZERO, so the feature is strictly one block tall — and BREAKS out of the loop (not continues) at the first position whose `distManhattan(origin)` exceeds radiusLimit (:42-44). There is no 3D-distance test at all: for each position passing the `isClear` gate (:63-81 — not the contents block, not one of the seven CANNOT_REPLACE blocks at :16-18, and every non-UP neighbour non-air while UP is air), `config.rim()` is written at that position when hasRim (:47-50), and `config.contents()` is written at that position translated by (rimX, 0, rimZ) when that translated position also passes `isClear` (:52-56). hasRim additionally requires rimX != 0 AND rimZ != 0 (:36) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's basalt_columns configuration (BasaltColumnsConfiguration) has JSON fields "reach" (an IntProvider) and "height" (an IntProvider). | `ColumnFeatureConfiguration.CODEC` (ColumnFeatureConfiguration.java:9-12) declares exactly two fields in that order: "reach" (`IntProviders.codec(0, 3)`) and "height" (`IntProviders.codec(1, 10)`), both IntProviders. Confirmed by data/minecraft/worldgen/configured_feature/small_basalt_columns.json ("reach": 1, "height": uniform 1..4) and large_basalt_columns.json. Note the class is named `ColumnFeatureConfiguration`, not BasaltColumnsConfiguration | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| basalt_columns first runs a zero-draw placement gate, then samples config.height for columnHeight and draws one next_float() against 0.9 to pick the clustered variant, deriving scatter_reach = min(columnHeight, 5 or 8) and a constant column count of 50 when clustered or 15 otherwise — config.reach is not sampled at this point at all. | Every element is wrong. `BasaltColumnsFeature.place` (BasaltColumnsFeature.java:39-65) first runs a zero-draw `canPlaceAt` gate (:45-47) and returns false on failure, then draws `config.height().sample(random)` for columnHeight (:49) and one `random.nextFloat()` against 0.9 to pick the clustered variant (:50). config.reach() is NOT sampled here at all. The scatter reach is `Math.min(columnHeight, 5 or 8)` (:51, constants CLUSTERED_REACH 5 and UNCLUSTERED_REACH 8 at :29-31) and the column count is the CONSTANT 50 when clustered, else 15 (:52, CLUSTERED_SIZE 50 and UNCLUSTERED_SIZE 15 at :30-32) — no 1 + nextInt(4) draw exists | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For each column, basalt_columns draws its X offset, then a degenerate Y offset (next_int_bounded(1), always 0 but still consumed), then its Z offset — three draws per column via next_int_bounded(2*scatter_reach+1) - scatter_reach on X and Z — and only when columnHeight minus that position's Manhattan distance from origin is >= 0 does it then draw config.reach as that column's own horizontal reach before growing the column via its own zero-RNG grow_basalt_column routine, structurally unrelated to basalt_pillar. | The per-column scatter draw is X then Y then Z, and the fourth draw is reach, not height. `BasaltColumnsFeature.place` (BasaltColumnsFeature.java:55-62) iterates `BlockPos.randomBetweenClosed(random, count, x-reach, y, z-reach, x+reach, y, z+reach)`, whose iterator (BlockPos.java:265-285) draws `nextInt(width)`, then `nextInt(height)`, then `nextInt(depth)` per element — three draws per column, the middle one on a degenerate 1-wide Y range but still consumed. Only then, and only when `columnHeight - pos.distManhattan(origin) >= 0`, does it draw `config.reach().sample(random)` (:60) as that column's own horizontal reach; the column's height is the pre-drawn columnHeight minus the Manhattan distance, not a fresh config.height sample. The growth routine `placeColumn` (:67-100) is itself zero-RNG, but it is BasaltColumnsFeature's own private method, not BasaltPillarFeature | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's basalt_pillar feature carries no configuration at all (NoneFeatureConfiguration, a zero-field unit codec) and no BasaltPillarConfiguration type or "state" field exists; the placed block is the hard-coded constant minecraft:basalt. | There is no BasaltPillarConfiguration and no "state" field. `Feature.BASALT_PILLAR` (Feature.java:135) is `register("basalt_pillar", new BasaltPillarFeature(NoneFeatureConfiguration.CODEC))` — the feature is config-free, and `NoneFeatureConfiguration.CODEC` (NoneFeatureConfiguration.java:8) is a unit codec with zero fields. data/minecraft/worldgen/configured_feature/basalt_pillar.json is {"type": "minecraft:basalt_pillar", "config": {}}. The placed block is hard-coded `Blocks.BASALT.defaultBlockState()` (BasaltPillarFeature.java:36, :67, :81, :87) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| basalt_pillar requires the origin to be empty and the block above it to be non-empty, then grows a solid column of hard-coded basalt strictly DOWNWARD only (there is no upward branch), stopping at the first non-empty block or on leaving build height with no step counter, while drawing one random.next_int_bounded(10) per active side per step for a decorative hang-off pass, four random.next_bool() draws at the base for base hang-offs, and 49 further random.next_int_bounded(10) draws sweeping a -3..=3 base footprint — the routine is RNG-heavy, not zero-draw. | The direction, the stop rule and the zero-RNG claim are all wrong. `BasaltPillarFeature.place` (BasaltPillarFeature.java:19-77) requires the origin to be empty AND the block above it to be non-empty (:23), then grows strictly DOWNWARD only: while `level.isEmptyBlock(pos)` it writes BASALT and moves DOWN (:31-42), with no step counter and no max_height — it stops at the first non-empty block or when it leaves build height (:32-34). It is RNG-heavy, not zero-draw: on every downward step it calls `placeHangOff` up to four times (N, S, W, E; :37-40), each consuming one `random.nextInt(10)` (:86); at the bottom it calls `placeBaseHangOff` four times, each one `random.nextBoolean()` (:45-48, :80); and it then sweeps a 7x7 base footprint drawing one `random.nextInt(10)` per cell (:52-55), 49 further draws | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| basalt_pillar's own downward growth loop has no max_height cap of any kind — it terminates only at the first non-empty block or on leaving build height; its vertical extent in the real game instead comes from its own placed_feature JSON (count, in_square, a full-range height_range, biome). | The premise is right, the cap is invented. `BasaltPillarFeature` does carry no height field (NoneFeatureConfiguration, Feature.java:135), but no 64 — or any other — height cap exists in the code: the downward loop (BasaltPillarFeature.java:31-42) terminates only on a non-empty block or on `level.isOutsideBuildHeight(pos)`, so the column can run the full dimension height. The only numeric bounds in the routine are the 3-block base drop limit (:57-64) and the -3..3 base footprint (:52-53). A basalt_pillar's vertical placement is instead controlled from data/minecraft/worldgen/placed_feature/basalt_pillar.json (count 10, in_square, a full-range height_range, biome) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's netherrack_replace_blobs configuration (ReplaceSphereConfiguration) has JSON fields, in order, "target" (a plain BlockStateSpec, not a RuleTest), "state" (a BlockStateSpec, the replacement written into matching cells), and "radius" (an IntProvider clamped 0..=12). | The three key names are right, the target's type and the codec order are not. `ReplaceSphereConfiguration.CODEC` (ReplaceSphereConfiguration.java:10-17) declares, in order, "target" as `BlockState.CODEC` — a plain block state, NOT a RuleTest — then "state" as `BlockState.CODEC`, then "radius" as `IntProviders.codec(0, 12)`. data/minecraft/worldgen/configured_feature/basalt_blobs.json confirms "target" is {"Name": "minecraft:netherrack"}, a bare block state with no RuleTest predicate_type key. The class name is `ReplaceSphereConfiguration` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| netherrack_replace_blobs first performs a zero-draw downward scan from origin (Y-clamped to the build range) to find the first cell matching config.target, returning early when none is found; it then draws config.radius three times for independent radius_x/radius_y/radius_z values and iterates a Manhattan-radius walk centered on the FOUND cell (not on origin), breaking past max(radius_x, radius_y, radius_z), writing config.state into every cell whose block-state identity equals config.target. | The draw count, the centre, the iteration shape and the match test all differ. `ReplaceBlobsFeature.place` (ReplaceBlobsFeature.java:20-49) first performs a zero-draw downward scan from the origin (Y-clamped to the build range) for the first cell holding the target block, `findTarget` (:51-62), returning false when none is found — the blob is centred on that found cell, not on the origin. It then draws `config.radius().sample(random)` THREE times, for radiusX, radiusY and radiusZ independently (:30-32), and iterates `BlockPos.withinManhattan(centerPos, radiusX, radiusY, radiusZ)` — a Manhattan diamond, not an ellipsoid — BREAKING at the first position past `max(radiusX, max(radiusY, radiusZ))` (:36-39). The per-cell test is a plain `blockState.is(targetBlock)` block identity check (:42), and the write is `config.replaceState` (:43) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Neither netherrack_replace_blobs's config.target nor blue_ice's own comparison evaluates a RuleTest of any kind — both are plain block-state identity checks consuming zero draws per cell, and a RandomBlockMatch-style variant is not reachable on either path. | No RuleTest is involved on either side. netherrack_replace_blobs's "target" is a plain BlockState (ReplaceSphereConfiguration.java:12) tested with `blockState.is(targetBlock)` (ReplaceBlobsFeature.java:42) — zero draws per cell, and a RandomBlockMatch variant is not expressible there at all. Vanilla's RuleTest hierarchy (net/minecraft/world/level/levelgen/structure/templatesystem/RuleTest.java and its RandomBlockMatchTest sibling) is used by ore/replace_single_block features and structure processors, not by ReplaceBlobsFeature. blue_ice likewise runs no RuleTest: `BlueIceFeature.place` (BlueIceFeature.java:26-63) uses direct `state.is(Blocks.WATER)` / `is(Blocks.PACKED_ICE)` / `is(Blocks.BLUE_ICE)` identity checks | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's glowstone_blob feature uses NoneFeatureConfiguration, i.e. it is config-free in the real game. | `Feature.GLOWSTONE_BLOB` (Feature.java:82) is `register("glowstone_blob", new GlowstoneFeature(NoneFeatureConfiguration.CODEC))`, and `GlowstoneFeature extends Feature<NoneFeatureConfiguration>` (GlowstoneFeature.java:12). `NoneFeatureConfiguration.CODEC` (NoneFeatureConfiguration.java:8) is a unit codec with zero fields; data/minecraft/worldgen/configured_feature/glowstone_extra.json is {"type": "minecraft:glowstone_blob", "config": {}} | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| glowstone_blob's first gate, with zero random draws, is that the origin cell itself must be empty (air); only then does it check, also with zero draws, that the block above the origin is one of an explicit three-block whitelist (netherrack, basalt or blackstone) rather than any sturdy-face test — failing either gate places nothing. | The gate order and the test itself differ. `GlowstoneFeature.place` (GlowstoneFeature.java:18-29) first requires the ORIGIN itself to be an empty block (`if (!level.isEmptyBlock(origin)) return false;`, :22-24) and only then inspects the block above — and that inspection is not a sturdy-face query but an explicit three-block identity whitelist: the state above must be `Blocks.NETHERRACK`, `Blocks.BASALT` or `Blocks.BLACKSTONE`, otherwise the feature returns false (:26-29). Both checks are indeed zero-draw | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| glowstone_blob has no config.state at all (it is config-free); once both placement gates pass, it writes the hard-coded block minecraft:glowstone at the origin unconditionally, with no separate air-or-replaceable test on that write. | There is no config.state (the feature is config-free, Feature.java:82) and the emptiness test is a precondition, not a per-write condition. `GlowstoneFeature.place` (GlowstoneFeature.java:22-31) uses `level.isEmptyBlock(origin)` as the FIRST gate — before the above-block whitelist — and then unconditionally writes the hard-coded `Blocks.GLOWSTONE.defaultBlockState()` at the origin with flag 2 (:31). "Air or replaceable" is also broader than vanilla's test, which is a plain air check | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| glowstone_blob draws no extra-attempt count at all — it runs a fixed, unconditional loop of exactly 1500 scatter attempts, a compile-time constant consuming zero draws to establish it. | No count is drawn at all. `GlowstoneFeature.place` (GlowstoneFeature.java:33) runs a fixed, unconditional `for (int i = 0; i < 1500; i++)` loop — 1500 scatter attempts, a compile-time constant, consuming zero draws to establish it | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For each of its 1500 scatter attempts, glowstone_blob draws five values in the fixed order next_int_bounded(8), next_int_bounded(8), next_int_bounded(12), next_int_bounded(8), next_int_bounded(8), giving dx and dz as a difference of two uniform 0..=7 draws (triangular over -7..=7) and dy = -next_int_bounded(12) (0..=-11) — not a single next_int_bounded(9) - 4 per axis. | Five draws per attempt, with different bounds. `GlowstoneFeature.place` (GlowstoneFeature.java:34) builds the offset as `origin.offset(random.nextInt(8) - random.nextInt(8), -random.nextInt(12), random.nextInt(8) - random.nextInt(8))`; Java evaluates the arguments left to right, so the draw order per attempt is nextInt(8), nextInt(8), nextInt(12), nextInt(8), nextInt(8). The horizontal offset is therefore a difference of two uniform 0..7 draws (a triangular distribution over -7..7), not a single nextInt(9) - 4, and the downward drift is -nextInt(12) (0..-11), not -nextInt(4) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| glowstone_blob places its hard-coded glowstone at each scatter-attempt offset only when that cell is air (not the broader air-or-replaceable) and EXACTLY one of its 6 DIRECTION_ORDER neighbours is already glowstone — a cell touching two or more existing glowstone blocks is rejected, and sturdiness is never queried. | The neighbour test is an exact-count glowstone test, not a sturdy-face test, and "at least one" is wrong. `GlowstoneFeature.place` (GlowstoneFeature.java:35-51) requires the target cell to be air (`isAir()`, :35), then counts, over `Direction.values()`, how many of the 6 neighbours ARE `Blocks.GLOWSTONE` (:38-46), breaking early once the count exceeds 1, and writes `Blocks.GLOWSTONE.defaultBlockState()` only when the count is EXACTLY 1 (:48-50) — a cell touching two or more existing glowstone blocks is rejected. Sturdiness is never queried | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's iceberg feature configuration (IcebergConfiguration) has one JSON field, "state" (a BlockStateSpec). | `Feature.ICEBERG` (Feature.java:103) is `register("iceberg", new IcebergFeature(BlockStateConfiguration.CODEC))`, and `BlockStateConfiguration.CODEC` (BlockStateConfiguration.java:7) is exactly `BlockState.CODEC.fieldOf("state")` — a single "state" field holding one block state. Confirmed by data/minecraft/worldgen/configured_feature/iceberg_packed.json and iceberg_blue.json, each carrying only "state". The class is `BlockStateConfiguration`, shared across several features, not a dedicated IcebergConfiguration | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Before any fill, iceberg re-centres its working origin onto the chunk generator's sea level and then draws, in order, next_double() for snow_on_top against 0.7, next_double() for a shape angle, next_int_bounded(5) for shape_ellipse_a = 11 minus that, next_int_bounded(3) for shape_ellipse_c = 3 plus that, and next_double() for is_ellipse against 0.7 — five draws precede any height or radius value, and there is no independent height = 8 + next_int_bounded(8) or radius = 4 + next_int_bounded(6) draw anywhere. | Neither draw exists in that form, and five other draws precede them. `IcebergFeature.place` (IcebergFeature.java:20-40) re-centres the origin on the generator's sea level (:23) and then draws, in order: `nextDouble()` for snowOnTop against 0.7 (:25); `nextDouble()` for the shape angle (:27); `nextInt(5)` for shapeEllipseA = 11 minus that (:28); `nextInt(3)` for shapeEllipseC = 3 plus that (:29); `nextDouble()` for isEllipse against 0.7 (:30). The height is then `nextInt(6) + 6` (6..11) for an ellipse or `nextInt(15) + 3` (3..17) otherwise (:32), with a conditional `nextDouble()` plus `nextInt(19) + 7` extension in the non-ellipse case (:33-35); underWaterHeight is `min(overWaterHeight + nextInt(11), 18)` (:37) and width is `min(overWaterHeight + nextInt(7) - nextInt(5), 11)` (:38) — a width, not a radius, and derived from the height rather than drawn independently | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| iceberg's own over_water_height is drawn as 6 + next_int_bounded(6) for the ellipse variant or 3 + next_int_bounded(15) otherwise (with a further conditional next_double()-gated extension in the non-ellipse case), under_water_height is min(over_water_height + next_int_bounded(11), 18), and width is min(over_water_height + next_int_bounded(7) - next_int_bounded(5), 11); real vanilla then runs a two-pass rectangular sweep with per-layer height-dependent radius functions, a smoothing pass and a cut-out pass, not a single ellipsoid fill — this blueprint's own place_iceberg substitutes one ellipsoid_cells fill (horizontal radius = width, vertical radius = over_water_height/2) over cells that are air/replaceable or still water as a deliberate, documented simplification of that machinery. | The iteration is a nested rectangular sweep with a per-layer signed-distance test, run twice, plus a smoothing and a cut-out pass. `IcebergFeature.place` (IcebergFeature.java:41-75) sweeps xo and zo over -a..a-1 (a = shapeEllipseA for the ellipse variant, else the constant 11, :39) and yOff over 0..overWaterHeight-1, computing a per-layer radius via `heightDependentRadiusEllipse` (:243-247) or the RNG-consuming `heightDependentRadiusRound` (:232-241); each candidate cell is accepted by a signed ellipse or circle distance (`signedDistanceEllipse` :226-230, `signedDistanceCircle` :221-224, the latter drawing one `nextFloat()` per cell) with a further `nextDouble() > 0.9` rejection near the rim (:182). A second sweep builds the underwater body downward with `heightDependentRadiusSteep` (:58-70, :249-253). Writes go through `setIcebergBlock` (:190-210), which accepts only air, SNOW_BLOCK, ICE or WATER and may write `Blocks.SNOW_BLOCK` instead of config.state when snowOnTop holds (:201-208). `smooth` (:263-295) and `generateCutOut` (:80-119) then delete blocks again | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's blue_ice feature carries no configuration at all (NoneFeatureConfiguration, a zero-field unit codec) and no BlueIceConfiguration type, "state" field or "packed_ice" field exists; the written state (minecraft:blue_ice) and the compared block (minecraft:packed_ice) are both hard-coded. | There is no BlueIceConfiguration and no fields at all. `Feature.BLUE_ICE` (Feature.java:102) is `register("blue_ice", new BlueIceFeature(NoneFeatureConfiguration.CODEC))` and `BlueIceFeature extends Feature<NoneFeatureConfiguration>` (BlueIceFeature.java:12) — a unit codec with zero fields (NoneFeatureConfiguration.java:8). data/minecraft/worldgen/configured_feature/blue_ice.json is {"type": "minecraft:blue_ice", "config": {}}. Both the written state (`Blocks.BLUE_ICE`, BlueIceFeature.java:43, :59) and the packed-ice reference (`Blocks.PACKED_ICE`, :33, :55) are hard-coded | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| blue_ice draws no radius at all: it first requires origin.y <= sea_level - 1, then that the origin or the block below it is still water, then that some non-Down neighbour is packed ice, writes hard-coded blue ice at the origin, and then runs a fixed 200-iteration scatter loop, each iteration drawing six values (two for a y_off in -5..=4, four for x/z offsets against a derived xz_diff), writing blue ice at an offset cell only when that cell is air/water/packed-ice/ice AND at least one of its 6 neighbours is already blue ice — no ellipsoid, and no RuleTest of any kind on either side. | No radius is drawn and there is no ellipsoid or RuleTest. `BlueIceFeature.place` (BlueIceFeature.java:18-67) runs three zero-draw gates first: origin Y must be below sea level minus 1 (:22-24), the origin or the block below it must be water (:26-28), and at least one non-DOWN neighbour must be PACKED_ICE (:32-41). It writes `Blocks.BLUE_ICE` at the origin (:43) and then runs a fixed 200-iteration scatter loop (:45-65). Each iteration draws `nextInt(5)` and `nextInt(6)` for yOff (:46), derives an xzDiff of 3 (reduced by yOff/2 when yOff < 2, :47-50), and draws four further offset values as `nextInt(xzDiff) - nextInt(xzDiff)` twice (:53) — six draws per iteration. A cell is written only when it is air, WATER, PACKED_ICE or ICE (:55) AND at least one of its 6 neighbours is already BLUE_ICE (:56-62) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's freeze_top_layer feature configuration (FreezeTopLayerConfiguration) has no JSON fields. | `Feature.FREEZE_TOP_LAYER` (Feature.java:83-85) is `register("freeze_top_layer", new SnowAndFreezeFeature(NoneFeatureConfiguration.CODEC))`, and `SnowAndFreezeFeature extends Feature<NoneFeatureConfiguration>` (SnowAndFreezeFeature.java:14) — the unit codec with zero fields (NoneFeatureConfiguration.java:8). data/minecraft/worldgen/configured_feature/freeze_top_layer.json is {"type": "minecraft:freeze_top_layer", "config": {}}. The implementing class is named `SnowAndFreezeFeature`, and vanilla has no FreezeTopLayerConfiguration type | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| freeze_top_layer's own placed_feature JSON carries exactly one placement modifier in vanilla, minecraft:biome, not zero — and the comparison to fill_layer has no referent at all, since the vanilla data pack contains no fill_layer placed_feature or configured_feature anywhere (fill_layer is used only by flat-level-generator presets); the feature still runs once per chunk at that chunk's raw minimum corner, but because no count/position modifier accompanies the biome filter, not because the modifier list is empty. | Both halves are wrong. data/minecraft/worldgen/placed_feature/freeze_top_layer.json carries exactly ONE placement modifier, {"type": "minecraft:biome"} — a biome filter, not an empty list. And the comparison has no referent: the vanilla data pack contains no fill_layer placed_feature and no fill_layer configured_feature at all (a recursive search of data/minecraft/worldgen for fill_layer returns nothing) — fill_layer is used by flat-level-generator presets, not by the placed-feature pipeline. The "once per chunk at the raw minimum corner" conclusion does hold in effect, but it follows from the absence of a count/position modifier alongside the biome filter, not from an empty modifier list | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| freeze_top_layer iterates all 256 chunk-local columns (x from 0 to 15, z from 0 to 15) of its chunk, and for each column looks up the MOTION_BLOCKING heightmap Y value (not WorldSurfaceWg), with zero random draws anywhere in the whole pass — though the pass is not light-independent, since its own per-column should_freeze/should_snow tests read block light. | The 256-column sweep and the zero-draw property are right; the heightmap is the wrong one. `SnowAndFreezeFeature.place` (SnowAndFreezeFeature.java:26-46) does iterate dx 0..15 and dz 0..15 over `origin.getX() + dx` / `origin.getZ() + dz`, and `context.random()` is never touched anywhere in the method — but the lookup is `level.getHeight(Heightmap.Types.MOTION_BLOCKING, x, z)` (:30), the MOTION_BLOCKING heightmap, not WORLD_SURFACE_WG. The pass is also not light-independent: the per-column `Biome.shouldFreeze` / `shouldSnow` tests read `level.getBrightness(LightLayer.BLOCK, pos)` (Biome.java:150, :188) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| freeze_top_layer runs two independent predicates per column, should_freeze and should_snow, on two different positions (one below the heightmap-surface position and the heightmap-surface position itself) rather than a single can_freeze gate — either, both, or neither may fire for the same column, and a false should_freeze result never skips the should_snow check. | There is no single can-freeze gate. `SnowAndFreezeFeature.place` (SnowAndFreezeFeature.java:34-44) runs TWO independent predicates on two different positions: `biome.shouldFreeze(level, belowPos, false)` decides the ice write at belowPos (one below the heightmap Y), and `biome.shouldSnow(level, topPos)` separately decides the snow write at topPos. Either, both or neither may fire for the same column — a false freeze result does not skip the snow branch. The biome is resolved per column via `level.getBiome(topPos).value()` (:33); `Biome.shouldFreeze` with checkNeighbors=false (Biome.java:145-169) requires cold-enough temperature, block light below 10, and a water LiquidBlock at that position, while `Biome.shouldSnow` (:183-196) requires SNOW precipitation, block light below 10, an air-or-snow state and snow survivability | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For each column freeze_top_layer visits, if should_freeze returns true for the position one below the heightmap-surface Y, the literal block state minecraft:ice with zero block-state properties is written there (also setting the SNOWY property true on that block when it has one), independently of and not exclusive with: if should_snow returns true for the heightmap-surface position itself, a minecraft:snow block state with layers=1 is placed there. | The positions are off by one and the two branches are not exclusive. `SnowAndFreezeFeature.place` (SnowAndFreezeFeature.java:30-44) sets topPos to the MOTION_BLOCKING heightmap Y — already the first free cell — and belowPos one block DOWN from it. Ice is written at belowPos (:35), i.e. at heightmap Y minus 1, gated by `biome.shouldFreeze`, not by a bare water test. Snow is written at topPos (:39), i.e. AT the heightmap Y itself, not one above the ice position, gated by `biome.shouldSnow`, and it runs whether or not the ice branch fired. The literal states are right in shape (`Blocks.ICE.defaultBlockState()` carries no properties; `Blocks.SNOW.defaultBlockState()` has LAYERS = 1, SnowLayerBlock.java:38), but vanilla additionally sets the SNOWY property to true on the block below when that block has it (:40-43) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's spike feature configuration (SpikeConfiguration) has THREE JSON fields: "state" (a BlockStateSpec), "can_place_on" (a BlockPredicate) and "can_replace" (a BlockPredicate). | The class name is right, the field count is not. `SpikeConfiguration` (SpikeConfiguration.java:8-16) is a record with THREE codec fields: "state" (`BlockState.CODEC`), "can_place_on" (`BlockPredicate.CODEC`) and "can_replace" (`BlockPredicate.CODEC`). data/minecraft/worldgen/configured_feature/ice_spike.json carries all three: state minecraft:packed_ice, can_place_on matching_blocks minecraft:snow_block, can_replace matching_block_tag minecraft:ice_spike_replaceable. Omitting the two predicates would drop both the placement gate (SpikeFeature.java:27-29) and the replace gate (:51, :58, :84) the algorithm depends on | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's spike feature (the ice-spike-tower feature) is distinct from the end_spike feature. | Two separately registered features with distinct classes and configs. `Feature.SPIKE` (Feature.java:81) = `register("spike", new SpikeFeature(SpikeConfiguration.CODEC))`; `Feature.END_SPIKE` (Feature.java:109) = `register("end_spike", new EndSpikeFeature(EndSpikeConfiguration.CODEC))` — different implementation classes (SpikeFeature.java vs EndSpikeFeature.java) and different configuration types. data/minecraft/worldgen/configured_feature/ice_spike.json has "type": "minecraft:spike" while end_spike.json has "type": "minecraft:end_spike", so the blueprint's reading that "spike" is the ice-spike feature is correct | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| spike first walks its origin down while empty and above min_y+2 (zero draws), applies the zero-draw config.can_place_on gate, then draws random.next_int_bounded(4) to raise the origin BEFORE drawing height = 7 + random.next_int_bounded(4) (7..=10); its own width is height/4 + random.next_int_bounded(2) — an integer-division term plus one draw, giving an overall range of 1..=3, not a max_radius = 1 + random.next_int_bounded(2) draw with range 1..=2. | The height range and the height-before-width order are right; the preceding draw and the width formula are not. `SpikeFeature.place` (SpikeFeature.java:22-36) first walks the origin down while the cell is empty (zero draws, :22-24), applies the zero-draw `config.canPlaceOn()` gate (:27-29), and then draws `random.nextInt(4)` to raise the origin (`origin.above(...)`, :31) BEFORE the height draw. Height is `random.nextInt(4) + 7` (7..10, matching), but the width is `height / 4 + random.nextInt(2)` (:33) — an integer-division term plus one draw, so the resulting value range coincides for heights 7..10 while the formula does not. A further `random.nextInt(60) == 0` draw and a conditional `10 + random.nextInt(30)` lift follow when width > 1 (:34-36) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For each Y layer from 0 to height-1, spike computes scale = (1.0 - y_off/height) * width with NO max(_, 0.4) floor anywhere (the raw float scale is retained and reused as the squared-radius bound in the per-cell test), and new_width = ceil(scale) — a profile that is entirely its own, sharing no code or shape with the large_dripstone feature. | The linear taper is right, the floor and the provenance are not. `SpikeFeature.place` (SpikeFeature.java:38-40) computes `float scale = (1.0F - (float)yOff / height) * width;` and `int newWidth = Mth.ceil(scale);` — the taper factor 1 minus yOff/height multiplied by width, with NO max(..., 0.4) floor anywhere; when scale reaches 0 the layer simply collapses. The raw float scale is also retained and reused as the squared-radius bound in the per-cell test (:47), so vanilla uses both the ceiled integer and the unrounded float. The shape is SpikeFeature's own; `LargeDripstoneFeature` uses an unrelated bluntness/scale profile and shares no code with it | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| spike writes config.state into a cell of each layer's xo/zo sweep (bounded by -new_width..=new_width) only when that cell passes an inset circular test (abs(xo)-0.25, abs(zo)-0.25 against scale*scale, or the exact center) AND, for cells on the bounding square's edge, a further random.next_float() > 0.75 rejection draw consumed only by cells already passing the shape test and lying on that edge — and the write condition is state.isAir() or config.can_replace, not a generic air-or-replaceable check; each accepted layer is additionally mirrored downward at -y_off whenever y_off != 0 and new_width > 1, and a further RNG-consuming pillar pass drops columns afterward. | The per-cell acceptance test, the RNG and the mirrored layers are missing. `SpikeFeature.place` (SpikeFeature.java:42-64) sweeps xo and zo over -newWidth..newWidth and accepts a cell when xo == 0 and zo == 0, OR when `dx*dx + dz*dz <= scale*scale`, where dx and dz are `Mth.abs(offset) - 0.25F` (:43, :46-47) — an inset circular test against the unrounded float scale, not plain ellipsoid membership. Every cell on the bounding square's edge is additionally rejected by a `random.nextFloat() > 0.75F` draw (:48), so the layer sweep is NOT zero-RNG. The write condition is `state.isAir()` OR `config.canReplace().test(level, pos)` (:51) — the JSON predicate, not a generic replaceable check. Each layer is also mirrored downward at -yOff whenever yOff != 0 and newWidth > 1 (:55-61), and a further RNG-consuming pillar pass drops columns down to Y 50 afterwards (:67-96) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
