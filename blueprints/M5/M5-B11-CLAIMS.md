# M5-B11 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| WorldgenRandom overrides next(bits), fork, forkPositional and setSeed, but never next_int_bounded itself; next_int_bounded always resolves to the one backend-independent implementation, whose power-of-two fast path runs first: when bound is a power of two it returns the high bits of a 31-bit draw with no loop, and only for a non-power-of-two bound does it fall back to the classic rejection loop (bits = next_bits(31); val = bits % bound; loop until (bits - val + bound - 1) >= 0 in wrapping 32-bit arithmetic). | `WorldgenRandom` (WorldgenRandom.java:6, 30-35) overrides only `next(bits)`, so `nextInt(bound)` is always `BitRandomSource.nextInt` (BitRandomSource.java:17-34) whatever backend is wrapped - that half is right. But that method is NOT purely the rejection loop: lines 22-24 are a power-of-two fast path, `if ((bound & bound - 1) == 0) return (int)((long)bound * this.next(31) >> 31)`, which returns the HIGH bits of the 31-bit draw, not `bits % bound`. Every `nextInt(2)` and `nextInt(4)` in this blueprint (corner-skip rolls, direction indices) takes that path and yields a different value from `bits % bound`. The loop at :26-33 is `do { sample = next(31); modulo = sample % bound; } while (sample - modulo + (bound - 1) < 0)`, matching the claim's condition. Separately, `XoroshiroRandomSource.nextInt` (XoroshiroRandomSource.java:53-74) is Lemire multiply-shift with an unbiased-bucket rejection - it is simply never reached through WorldgenRandom | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| next_int_between_inclusive(min, max_inclusive) equals next_int_bounded(max_inclusive - min + 1) + min, consumed as one draw. | `RandomSource.nextIntBetweenInclusive` (RandomSource.java:45-47) = `this.nextInt(maxInclusive - min + 1) + min`, a single delegated `nextInt` call | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| next_float() is uniform on [0.0, 1.0), computed as next_bits(24) as f32 * 2^-24, one draw. | `BitRandomSource.nextFloat` (BitRandomSource.java:49-52) = `this.next(24) * 5.9604645E-8F`; `FLOAT_MULTIPLIER` (:6) is 5.9604645E-8F, which is exactly 2^-24 as an f32. One `next()` call, hence one WorldgenRandom draw | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| next_bool() is next_bits(1) != 0, one draw. | `BitRandomSource.nextBoolean` (BitRandomSource.java:44-47) = `this.next(1) != 0`, one `next()` call. (`XoroshiroRandomSource.nextBoolean` (:81-84) instead tests the LOW bit of a `nextLong`, but WorldgenRandom never reaches it - see row 1) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| next_double() is uniform on [0.0, 1.0), implemented as two next_bits calls internally but counted as one logical draw. | `BitRandomSource.nextDouble` (BitRandomSource.java:54-60) = `((long)next(26) << 27) + next(27)` times `DOUBLE_MULTIPLIER` (:7, the f32 literal 1.110223E-16F, not the exact 2^-53) - two `next()` calls, upper 26 bits drawn first. Note for the blueprint's own draw accounting: vanilla's `WorldgenRandom.getCount()` (WorldgenRandom.java:30-35) increments once per `next()` call, so a `nextDouble` advances that counter by 2 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| IntProvider::Constant(n) samples to n with zero RNG draws; IntProvider::Uniform{min,max} samples via next_int_between_inclusive(min,max), one draw. | `ConstantInt.sample` (ConstantInt.java:21-24) returns `this.value` with no `random` use; `UniformInt.sample` (UniformInt.java:30-33) is `Mth.randomBetweenInclusive(random, min, maxInclusive)` = `random.nextInt(max - min + 1) + min` (Mth.java:693-695), i.e. exactly `nextIntBetweenInclusive`, one draw | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| HeightProvider has six kinds: Constant, Uniform, BiasedToBottom, VeryBiasedToBottom, Trapezoid, and WeightedList. | `HeightProviderType` (HeightProviderType.java:8-13) registers exactly six: `constant`, `uniform`, `biased_to_bottom`, `very_biased_to_bottom`, `trapezoid`, `weighted_list` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlockStateProvider::SimpleStateProvider consumes zero RNG draws; WeightedStateProvider consumes one draw to make a cumulative-weight selection among its entries. | `SimpleStateProvider.getState` (SimpleStateProvider.java:22-25) returns the stored state, no draw. `WeightedStateProvider.getState` (WeightedStateProvider.java:34-37) is `weightedList.getRandomOrThrow(random)` = one `random.nextInt(totalWeight)` (WeightedList.java:85-92); the index is resolved by cumulative weight - literally so in the `Compact` selector (WeightedList.java:174-192, subtracting each entry's weight until negative) and by an equivalent precomputed flat table in `Flat` for totalWeight < 64 (:194-212), so the mapping is the same | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The shared tree-height formula is height = base_height + random.next_int_bounded(height_rand_a + 1) + random.next_int_bounded(height_rand_b + 1), two draws, with the height_rand_a draw strictly before the height_rand_b draw. | `TrunkPlacer.getTreeHeight` (TrunkPlacer.java:58-60) = `this.baseHeight + random.nextInt(this.heightRandA + 1) + random.nextInt(this.heightRandB + 1)`; Java evaluates the operands left to right, so the heightRandA draw precedes the heightRandB draw | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlobFoliagePlacer samples radius outside the foliage placer entirely (drawn once by the tree driver before the root and trunk placers run) and offset once per FoliageAttachment inside the foliage placer (two draws overall, at different call sites, not two draws local to the placer), then per layer fills a SQUARE footprint (not a disk) with a probabilistic corner-skip that consumes one next_int_bounded(2) draw per exact-corner candidate block. | Draw count is right but the locus, the ordering and the shape are not. The radius draw does NOT happen inside the foliage placer: `TreeFeature.doPlace` (TreeFeature.java:65-68) calls `foliagePlacer.foliageRadius(random, trunkHeight)` (= `this.radius.sample(random)`, FoliagePlacer.java:65-67) BEFORE the root placer and BEFORE `placeTrunk`, while the offset draw happens inside `FoliagePlacer.createFoliage` (FoliagePlacer.java:48, 69-71) once per FoliageAttachment - so any trunk-placer draws fall between the two, and a multi-attachment tree re-draws offset per attachment. `BlobFoliagePlacer.foliageHeight` (BlobFoliagePlacer.java:50-53) returns the constant `height`, zero draws. The per-layer footprint is a full SQUARE, not a disk: `placeLeavesRow` (FoliagePlacer.java:106-113) iterates dx, dz over -currentRadius..currentRadius (+1 on each axis when doubleTrunk) and `BlobFoliagePlacer.shouldSkipLocation` (:55-58) only ever skips the exact corner, `dx == currentRadius and dz == currentRadius and (random.nextInt(2) == 0 or y == 0)` - the nextInt(2) draw is taken at every exact corner (short-circuit puts it before the `y == 0` test), which is the one part of the claim that holds | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Real vanilla's placement context carries an Optional<PlacedFeature> for the currently-placing top-level feature, not a feature id, populated only at the true top-level entry point; a nested re-entry passes an empty Optional, so a Biome{} placement modifier encountered inside a nested chain throws in real vanilla rather than checking any feature's presence in the current biome's step list, and vanilla data packs simply never nest a Biome{} modifier this deep. | Vanilla does the opposite. `PlacementContext` (PlacementContext.java:17-24, 46-48) carries an `Optional<PlacedFeature> topFeature`, not a feature ResourceLocation. Only the top-level entry point sets it: `PlacedFeature.placeWithBiomeCheck` (PlacedFeature.java:42-44) passes `Optional.of(this)`, while a nested re-entry goes through `PlacedFeature.place` (:38-40), which builds a context with `Optional.empty()`. `BiomeFilter.shouldPlace` (BiomeFilter.java:21-26) then does `context.topFeature().orElseThrow(...)` - so a `minecraft:biome` modifier inside a nested chain THROWS IllegalStateException in vanilla rather than checking the nested feature's own biome membership; vanilla data packs simply never put a biome filter in a nested chain | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Chorus Plant (the chorus_plant feature) grows only in the End dimension in real vanilla. | Data pack: the only worldgen file referencing the `minecraft:chorus_plant` placed feature is `data/minecraft/worldgen/biome/end_highlands.json` (via `placed_feature/chorus_plant.json` -> `configured_feature/chorus_plant.json`), an End biome. (The `chorus_plant` hits in `configured_feature/crimson_fungus.json` and `warped_fungus.json` are the chorus_plant BLOCK inside a replaceable-blocks predicate, not the feature.) `ChorusPlantFeature` itself carries no dimension test | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The complete named vanilla Feature registry this project recognizes comprises 63 kinds: 6 already implemented (ore, disk, spring_feature, lake, tree, simple_block), 17 owned by this blueprint (fallen_tree, huge_red_mushroom, huge_brown_mushroom, vines, vegetation_patch, waterlogged_vegetation_patch, seagrass, kelp, coral_tree, coral_mushroom, coral_claw, sea_pickle, bamboo, huge_fungus, nether_forest_vegetation, weeping_vines, twisting_vines), 35 owned elsewhere, and 5 End-exclusive kinds out of scope (chorus_plant, end_platform, end_spike, end_island, end_gateway). | The `minecraft:feature` registry has 63 entries, not 64: `Feature.java:61-160` registers exactly 63 ids, and `random_patch` is not one of them (there is no `RandomPatchConfiguration` in `feature/configurations/` and no `random_patch` anywhere under `datagen/generated/data/minecraft/worldgen/`). The blueprint's own 7-item implemented list is therefore 6 real kinds plus one that does not exist in 26.2. The 17 kinds claimed by this blueprint and the 5 End kinds are all genuine registry ids (`Feature.java:63, 66, 75-80, 86, 88-93, 108-125`); with random_patch removed the partition is 6 + 17 + 35 + 5 = 63 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| random_patch is not a real vanilla feature kind: it registers no Feature id, has no configuration type, and appears nowhere in the vanilla data pack; the research corpus's own 63-name enumeration is the correct and complete universe of confirmed vanilla feature kinds. | `random_patch` is not a 26.2 vanilla feature kind at all. It appears nowhere in `Feature.java`'s registration block (:61-160), there is no `RandomPatchConfiguration.java` under `world/level/levelgen/feature/configurations/`, and no file under `datagen/generated/data/minecraft/worldgen/` (226 configured features included) mentions it. The research corpus's 63-name enumeration is the correct one; the true universe is 63, not 64 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Only mangrove-style trees carry a root system (RootPlacer) in real vanilla; straight and bending trunk placers never carry one. | Data pack: `root_placer` occurs in exactly two configured features, `worldgen/configured_feature/mangrove.json` and `tall_mangrove.json`, both `minecraft:mangrove_root_placer`. Neither uses a straight or bending trunk placer - both use `minecraft:upwards_branching_trunk_placer` (the kind row 35 shows the blueprint's registry list is missing) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Mangrove root placement has no soil check on the block directly below the tree's origin; the mud/muddy-mangrove-roots requirement is a placement-modifier gate on the placed feature (a would_survive-style predicate keyed on the tree's real support-block tag, broader than mud alone), applied before the root placer ever runs. The root placer itself walks the column from origin up to the shifted trunk origin, requiring every position to be air/replaceable-by-trees or can_grow_through-tagged; if any position fails, the entire tree placement is a no-op. | There is no soil check on the block below the origin anywhere in the root placer. `MangroveRootPlacer.placeRoots` (MangroveRootPlacer.java:39-77) first walks the column from `origin` up to `trunkOrigin` requiring every position to satisfy `canPlaceRoot` (:51-57), i.e. air / `replaceable_by_trees` (RootPlacer.java:55-57 via `TreeFeature.validTreePos`) or a block in the config's `can_grow_through` set (:122-125); then it runs `simulateRoots` for each of the four horizontal directions and bails on the first failure. The mud requirement is a placement-modifier gate in the placed feature, not a root-placer gate. The consequence the claim states is right for the wrong reason: a false from `placeRoots` makes `TreeFeature.doPlace` (TreeFeature.java:76-78) return false, so the whole tree is a no-op | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Mangrove root placement samples a trunk_offset_y value and shifts the real trunk placer's origin upward by that amount from the root placer's own origin, since roots grow down from the tree's nominal base into the mud below while the visible trunk starts above it. | `RootPlacer.getTrunkOrigin` (RootPlacer.java:87-89) = `origin.above(this.trunkOffsetY.sample(random))`, and `TreeFeature.doPlace` (TreeFeature.java:69) uses that as `trunkOrigin` for `placeTrunk`. The draw sits between the foliage-radius draw (:68) and `placeRoots` (:76) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A mangrove tree always starts exactly four root strands, one per fixed horizontal direction in North, East, South, West order, each rooted at the shifted trunk origin's own neighbor in that direction with zero positional-jitter draws; max_root_width is not a strand count but a Manhattan-distance bound from the root origin, tested against each candidate as the recursive walk proceeds. | No such per-strand jitter exists. `MangroveRootPlacer.placeRoots` (MangroveRootPlacer.java:61-70) starts exactly four strands, one per `Direction.Plane.HORIZONTAL` entry in fixed N, E, S, W order, each rooted at `trunkOrigin.relative(dir)` with zero positional draws; `max_root_width` is not a strand count but a Manhattan-distance bound from the root origin, tested inside `potentialRootPositions` (:105-120). Growth is a depth-first recursion (`simulateRoots`, :79-103), not a straight downward walk | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| During a mangrove root strand's recursive walk, a candidate whose distance from the root origin exceeds max_root_width advances straight down with zero draws; otherwise one skew_roll = random.next_float() is drawn against random_skew_chance, and depending on the distance band this either yields both a below and a sideways-then-below candidate, or gates a second random.next_bool() draw choosing between a sideways and a below candidate — the sideways direction is always the strand's own already-established direction, never a freshly drawn random.next_int_bounded(4). | `MangroveRootPlacer.potentialRootPositions` (MangroveRootPlacer.java:105-120): when `width > maxRootWidth` no draw happens at all (:113-114). Otherwise one `random.nextFloat()` is compared to `random_skew_chance`; in the outer band (`width > maxRootWidth - 3 and width <= maxRootWidth`) success yields the pair `[below, nextTo.below()]` (:111-112), and in the inner band success yields `[below]` while FAILURE takes a second draw, `random.nextBoolean()`, choosing `[nextTo]` or `[below]` (:115-118). There is no `nextInt(4)`, and the horizontal step is always the strand's own inherited direction `prevDir`, never a freshly drawn one | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A mangrove root strand's recursive walk does not stop early on a non-placeable candidate; such a candidate is simply skipped and the recursion continues to the next one. The only real early exit is a layer/length guard (reaching max_root_length), which returns failure and aborts the entire tree, not just that strand. | `simulateRoots` (MangroveRootPlacer.java:79-103) does not stop at the first non-placeable position: a candidate failing `canPlaceRoot` is simply skipped and the loop continues (:90-97), and the method returns true when the candidate list is exhausted. The only early exit is the length guard at :89 - `layer == maxRootLength or rootPositions.size() > maxRootLength` returns FALSE, which propagates up and aborts the ENTIRE tree (:64-66, then TreeFeature.java:76-78), the opposite of a graceful stop. Placeability is air / `replaceable_by_trees` or a `can_grow_through` block (:122-125); water is not named | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Mangrove root placement never contributes foliage attachment points; a tree's returned FoliageAttachments come only from its trunk placer. | `RootPlacer.placeRoots` returns `boolean` (RootPlacer.java:46-53) and contributes nothing else; `TreeFeature.doPlace` (TreeFeature.java:80-81) takes its attachment list solely from `config.trunkPlacer.placeTrunk(...)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| This blueprint's own RootPlacer (used only by mangrove-style trees, Context §E) and M5-B12c's own root_system Feature kind are two different vanilla mechanisms that happen to share a similar-sounding name; RootPlacer is a tree-configuration component while root_system is a standalone Feature (used for e.g. flowering azalea trees), and the two do not interact. | `RootPlacer` is a component of the tree config - `TreeConfiguration.rootPlacer` is an `Optional<RootPlacer>` field decoded from `root_placer` (TreeConfiguration.java:29, 41). `root_system` is a separate top-level Feature, `Feature.ROOT_SYSTEM = register("root_system", new RootSystemFeature(RootSystemConfiguration.CODEC))` (Feature.java:94). The two never interact. Its vanilla users are `configured_feature/rooted_azalea_tree.json` (which nests `minecraft:azalea_tree`, whose foliage provider is a weighted mix of azalea and flowering-azalea leaves) and `rooted_sulfur_spring.json` - the exact vanilla name is `rooted_azalea_tree`, not "flowering azalea tree" | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vanilla RootPlacer registry comprises exactly one kind, mangrove_root_placer, unimplemented anywhere else in the project before this blueprint. | `RootPlacerType` (RootPlacerType.java:8) registers exactly one kind, `mangrove_root_placer`. In this repository `RootPlacer` appears only as a generated registry name in `crates/registries/generated/v776/registries.rs`, with no implementation | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| TreeDecorators run, in their declared list order, strictly after the trunk, foliage, and (if present) root placer have all finished writing blocks. | `TreeFeature.place` (TreeFeature.java:153-158) runs `doPlace` first - which performs root placement (:76), trunk placement (:80-81) and every foliage attachment (:82-85) - and only then builds `TreeDecorator.Context` and calls `config.decorators.forEach(decorator -> decorator.place(decoratorContext))`, i.e. in declared list order | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vanilla TreeDecorator registry comprises 10 kinds, of which this blueprint implements 6 (beehive, trunk_vine, leave_vine, cocoa, attached_to_leaves, attached_to_logs) and leaves 4 named-deferred (pale_moss, creaking_heart, alter_ground, place_on_ground). | `TreeDecoratorType` (TreeDecoratorType.java:8-17) registers exactly 10: `trunk_vine`, `leave_vine`, `pale_moss`, `creaking_heart`, `cocoa`, `beehive`, `alter_ground`, `attached_to_leaves`, `place_on_ground`, `attached_to_logs`. The blueprint's 6-implemented and 4-deferred split partitions that list exactly | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The fixed horizontal-direction table maps direction indices 0, 1, 2, 3 to (dx, dz) offsets (0,-1), (1,0), (0,1), (-1,0), corresponding to North, East, South, and West respectively. | `Direction.Plane.HORIZONTAL` (Direction.java:577) is the fixed array `{NORTH, EAST, SOUTH, WEST}`, with offsets `NORTH = (0,0,-1)`, `EAST = (1,0,0)`, `SOUTH = (0,0,1)`, `WEST = (-1,0,0)` (Direction.java:35-38). `getRandomDirection` (Direction.java:588-590) is `Util.getRandom(this.faces, random)` = `faces[random.nextInt(4)]` (Util.java:826-828), so index 0..3 maps exactly as claimed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For the beehive tree decorator, a probability roll (random.next_float(), one draw) gates the entire rest of the decorator; on success, nothing resembling a single direction-index draw follows — instead a hive Y is derived from the lowest log/leaf positions (drawing an extra random.next_int_bounded(3) only when there are no leaves), a candidate list is built from every log at that Y offset one step in each of three fixed directions (East, South, West), and that list is Fisher-Yates shuffled before a survivor is chosen. | The probability gate is real - `BeehiveDecorator.place` (BeehiveDecorator.java:42) is `if (!(random.nextFloat() >= this.probability))` - but nothing resembling a single direction-index draw follows. On success vanilla may draw `random.nextInt(3)` for the hive Y when the leaf list is empty (:43-45), then shuffles the whole candidate list with `Util.shuffle(hivePlacements, random)` (:51), a Fisher-Yates loop consuming one `nextInt(i)` per element from n down to 2 (Util.java:1110-1117), and on a successful placement draws `2 + random.nextInt(2)` bees plus one `random.nextInt(599)` per bee (:57-63) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| On a successful beehive probability roll, the candidate bee-nest position is the first shuffled candidate — a log at the derived hive Y offset exactly one step (not two) in one of three fixed directions (East, South, West, never North) — that survives the placement test; no direction index is ever drawn. | Every particular is different. `BeehiveDecorator.place` (BeehiveDecorator.java:43-45) picks `hiveY` from the LOWEST log/leaf - `TreeDecorator.Context` sorts logs, leaves and roots ascending by Y (TreeDecorator.java:51-53), so `getFirst()` is the bottom, and hiveY is `max(leaves.getFirst().getY() - 1, logs.getFirst().getY() + 1)`, or `min(logs.getFirst().getY() + 1 + random.nextInt(3), logs.getLast().getY())` when there are no leaves. Candidates are every log at that Y offset by ONE step in each of `SPAWN_DIRECTIONS` (:21-24: the horizontal directions minus NORTH, i.e. EAST, SOUTH, WEST - three of them, not four), :46-49; the winner is chosen by shuffling that list and taking the first survivor (:51-54), not by a direction index, and it is one block out, not two | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A bee nest is placed at the beehive candidate position only if that position is strictly air (not air-or-replaceable) and the position one step South of it (the hive's fixed facing direction, not a leaf test) is also strictly air. | `BeehiveDecorator.place` (BeehiveDecorator.java:52-54) requires `context.isAir(pos) and context.isAir(pos.relative(WORLDGEN_FACING))`. `Context.isAir` (TreeDecorator.java:64-66) is strict air, not air-or-replaceable, and the second test is the neighbour one step SOUTH (`WORLDGEN_FACING = Direction.SOUTH`, :20) - the block the hive faces - not a leaf above it. No test on `pos.above()` exists | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In real vanilla, the bee_nest block carries block-entity NBT tracking its occupant bees and honey level beyond the placed block's own facing state, a payload this blueprint's beehive decorator does not populate since it places the bare block state only. | The block entity is real and vanilla does populate it: `BeehiveDecorator.place` (BeehiveDecorator.java:57-63) fetches `BlockEntityTypes.BEEHIVE` at the placed position and stores `2 + random.nextInt(2)` occupants, each `BeehiveBlockEntity.Occupant.create(random.nextInt(599))`. Two corrections for the blueprint text: honey level is a BLOCK STATE property of bee_nest, not block-entity NBT (the decorator places `Blocks.BEE_NEST.defaultBlockState().setValue(BeehiveBlock.FACING, WORLDGEN_FACING)` at :56 and never touches honey level); and skipping the occupant payload also skips 3 to 4 RNG draws, so a bare-blockstate implementation desyncs every later feature in the stream | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For the trunk_vine decorator, for each log position and each of the four cardinal directions in fixed West, East, North, South order, a vine-placement roll (random.next_int_bounded(3), one draw) is drawn unconditionally, before the neighbor's air state is tested; a vine is placed on that face iff the roll is greater than 0 (probability 2/3) and the neighbor is air, and its facing property is the opposite of the offset direction. | `TrunkVineDecorator.place` (TrunkVineDecorator.java:20-52) differs on all four points: the direction order is WEST, EAST, NORTH, SOUTH; the roll is `random.nextInt(3)` (bound 3, not 2), taken UNCONDITIONALLY before the air test, once per direction per log; the vine is placed when the roll is `> 0` (probability 2/3), not when it equals 0; and the vine's boolean property is the opposite face of the offset (the west neighbour gets `VineBlock.EAST`, :25-28) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The leave_vine decorator shares only its four unrolled direction blocks with trunk_vine: each direction's roll is a random.next_float() against a codec-configured probability (not random.next_int_bounded(3)), and on success the vine is additionally extended downward through up to 4 further air positions, a behavior trunk_vine has no counterpart for. | `LeaveVineDecorator.place` (LeaveVineDecorator.java:25-57) shares only the four unrolled direction blocks. The roll is `random.nextFloat() < this.probability` against a codec-configured probability, not `nextInt(3)`; and on success `addHangingVine` (:59-67) places the vine and then extends it downward through up to 4 further air positions, which trunk_vine never does | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For the cocoa decorator, a single probability roll (random.next_float(), one draw) is drawn once for the whole decorator, before any log is visited, not once per log; on success, for each log within 2 of the lowest log's Y and each of the four cardinal directions, an unconditional random.next_float() draw is compared against a fixed 0.25 (not the configured probability), and on success a cocoa pod is placed on the opposite face at an age drawn via random.next_int_bounded(3), not a fixed age of 0. | `CocoaDecorator.place` (CocoaDecorator.java:26-51): the `random.nextFloat() >= this.probability` roll is taken ONCE for the whole decorator (:28), before any log is visited, not once per log. Only logs within 2 of the lowest log's Y are then iterated (:31-33), and for each, all four `Direction.Plane.HORIZONTAL` entries are visited with a `random.nextFloat() <= 0.25F` roll each (:36-37) - drawn unconditionally per direction, and it is a fixed 0.25, not the configured probability. On success the pod goes at `pos` offset in the OPPOSITE direction (:38-39) and its AGE is `random.nextInt(3)` (:42), an extra draw, not a fixed 0 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For attached_to_leaves, the leaf list is shuffled first (one draw per element), then per leaf a direction index is drawn unconditionally from a required, non-empty configured directions list (no all-four-cardinals default) before a probability roll is drawn (short-circuited away for a position already inside an exclusion box grown around each already-placed propagule, not around the tree's origin); for attached_to_logs, which has no exclusion-radius or required-empty-blocks fields at all, the log list is shuffled first, then per log a direction index and a probability roll are drawn and gate a single air test at the offset position, with no multi-step walk. | `AttachedToLeavesDecorator.place` (AttachedToLeavesDecorator.java:51-70) draws in the reverse order and the exclusion radius means something else: the leaf list is first shuffled with `Util.shuffledCopy(context.leaves(), random)` (:56, one `nextInt(i)` per element), then per leaf a direction is drawn FIRST and unconditionally via `Util.getRandom(this.directions, random)` (:57), and only afterwards does `random.nextFloat() < this.probability` run (:59). `exclusion_radius_xz` / `exclusion_radius_y` define a blacklist box grown around each ALREADY PLACED propagule (:60-65), not a skip zone around the tree origin. `directions` is `ExtraCodecs.nonEmptyList` (:24) - there is no all-four-cardinals default. `AttachedToLogsDecorator` (AttachedToLogsDecorator.java:15-44) has neither exclusion radius nor `required_empty_blocks` fields at all and only tests `context.isAir(placementPos)` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vanilla TrunkPlacer registry comprises exactly 9 kinds: straight_trunk_placer and bending_trunk_placer (already implemented) plus forking_trunk_placer, giant_trunk_placer, mega_jungle_trunk_placer, dark_oak_trunk_placer, fancy_trunk_placer, cherry_trunk_placer, and upwards_branching_trunk_placer, the last of which is named-deferred (it is exactly the kind both mangrove configured features use). | `TrunkPlacerType` (TrunkPlacerType.java:8-18) registers NINE kinds, not eight. The list is `straight_trunk_placer`, `forking_trunk_placer`, `giant_trunk_placer`, `mega_jungle_trunk_placer`, `dark_oak_trunk_placer`, `fancy_trunk_placer`, `bending_trunk_placer`, `upwards_branching_trunk_placer` and `cherry_trunk_placer`. The missing one, `upwards_branching_trunk_placer` (:15-17, UpwardsBranchingTrunkPlacer.java), is exactly the kind both mangrove configured features use, so omitting it leaves mangrove trees unbuildable | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| forking_trunk_placer is a leaning main trunk plus one conditional fork, not a straight column plus two symmetric branches: it draws a lean direction, a lean height, and a lean count (three draws) and drifts the main column by that lean before emitting one FoliageAttachment at its own top; it then draws a second direction index unconditionally, and only forks (drawing a branch position and a branch length of 1 + random.next_int_bounded(3), i.e. 1 to 3 logs, and emitting a second FoliageAttachment) if that second direction differs from the lean direction. | `ForkingTrunkPlacer.placeTrunk` (ForkingTrunkPlacer.java:29-92) is a leaning main trunk plus one conditional fork, not a straight column plus two symmetric branches. Draws in order: `leanDirection = Direction.Plane.HORIZONTAL.getRandomDirection(random)` (:40), `leanHeight = treeHeight - random.nextInt(4) - 1` (:41), `leanSteps = 3 - random.nextInt(3)` (:42); the column then drifts one block per layer in leanDirection for leanSteps layers from leanHeight up (:48-59) and DOES emit a FoliageAttachment at its top with radiusOffset 1 (:61-63). A second `getRandomDirection` (:67) is drawn unconditionally; only if it differs from leanDirection are `branchPos = leanHeight - random.nextInt(2) - 1` (:69) and `branchSteps = 1 + random.nextInt(3)` (:70) drawn - branch length 1 to 3, not 1 or 2 - and the branch steps diagonally outward and upward, adding a second attachment with radiusOffset 0 (:86-88) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| giant_trunk_placer places four below-trunk blocks, then stamps a 2x2 log footprint for every Y layer of the drawn height EXCEPT the top layer, which keeps only a single column; it consumes zero draws beyond the two height draws, and returns exactly one FoliageAttachment one block above the top trunk layer with double_trunk true. | The attachment half is right, the footprint half is not. `GiantTrunkPlacer.placeTrunk` (GiantTrunkPlacer.java:27-53) returns exactly one `FoliageAttachment(origin.above(treeHeight), 0, true)` (:52). But the 2x2 stamp is not applied at every layer: `(0, hh, 0)` is placed on every layer while the other three columns `(1,hh,0)`, `(1,hh,1)`, `(0,hh,1)` are placed only when `hh < treeHeight - 1` (:43-50), so the TOP layer is a single column. It also writes four below-trunk blocks at y-1 (:36-40). RNG use beyond the two height draws is zero only while `trunkProvider` and `belowTrunkProvider` are non-drawing providers | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| mega_jungle_trunk_placer calls giant_trunk_placer's own routine first, then adds a whole additional randomized branch loop giant_trunk_placer does not have, drawing a starting branch height, a per-iteration decrement, and a per-iteration angle, and emitting one further FoliageAttachment with double_trunk false per branch. | `MegaJungleTrunkPlacer.placeTrunk` (MegaJungleTrunkPlacer.java:28-56) calls `super.placeTrunk` (:38, the Giant routine) and then adds a whole branch loop the claim does not mention: `for (int branchHeight = treeHeight - 2 - random.nextInt(4); branchHeight > treeHeight / 2; branchHeight -= 2 + random.nextInt(4))` (:40) - one draw to start plus one per iteration - and inside each iteration `float angle = random.nextFloat() * 2 pi` (:41), a 5-block diagonal branch stamped from cos/sin of that angle (:45-50), and an extra `FoliageAttachment(..., -2, false)` per branch (:52). So it differs from Giant by far more than the three height constants | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| dark_oak_trunk_placer draws a lean direction, a lean height, and a lean count (three draws, the same lean shape as forking_trunk_placer) and stamps the 2x2 footprint only where the base position is air-or-leaves, returning exactly one FoliageAttachment with double_trunk true at the top layer itself (not y+1); it then scans a 4x4 ring of 12 cells around the inner 2x2, each drawing a next_int_bounded(3), and only when that roll is at most 0 draws a branch length and stamps a downward branch with its own FoliageAttachment (double_trunk false) — 1 to 13 attachments total, never a fixed two. | `DarkOakTrunkPlacer.placeTrunk` (DarkOakTrunkPlacer.java:29-88) matches none of this. It draws `leanDirection` (:44), `leanHeight = treeHeight - random.nextInt(4)` (:45) and `leanSteps = 2 - random.nextInt(3)` (:46), then stamps the 2x2 only on layers whose base position is air-or-leaves (:63-68). It adds ONE attachment with doubleTrunk true at `(tx, y + treeHeight - 1, tz)` (:71) - at the top layer itself, not y+1. Then it scans the 4x4 ring `ox, oz in -1..2` excluding the inner 2x2 (:73-75): each of those 12 cells draws `random.nextInt(3)` and, when `<= 0`, a further `random.nextInt(3) + 2` branch length (:75-76), stamps a downward log column, and adds an attachment with doubleTrunk FALSE (:82). There is no single jut-direction draw and no pair of diagonally-opposite double-trunk attachments | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| fancy_trunk_placer draws no branch count, direction index, or branch length at all: trunk_height is the golden-ratio-scaled floor of height+2 (not the raw drawn height), and branches instead come from a descending scan of candidate Y layers, each of up to a computed number of cluster attempts, with each attempt drawing a radius float and an angle float (two draws) to place a branch's own start offset; branch length falls out of the branch's own walk toward its computed endpoint rather than being sampled. | `FancyTrunkPlacer.placeTrunk` (FancyTrunkPlacer.java:35-91) works entirely differently. The golden-ratio split is `trunkHeight = Mth.floor(height * 0.618)` where `height = treeHeight + 2` (:45-46), not the drawn height itself. There is no `3 + random.nextInt(3)` branch count and no direction-index draw: branch seeds come from a descending scan of `relativeY` from `height - 5` down to 0 (:55), with `clustersPerY = Math.min(1, Mth.floor(1.382 + (height / 13.0)^2))` attempts per eligible layer (:49), and each attempt draws TWO floats - `radius = treeShape * (random.nextFloat() + 0.328)` and `angle = random.nextFloat() * 2 pi` (:60-61) - which are turned into an x/z offset by sin/cos. Branch length is not drawn at all; it falls out of the limb walk in `makeLimb` (:93-124). The straight column is only laid down at :80, after the whole scan | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| fancy_trunk_placer's branches do not start at a deterministic evenly-spread Y; each branch's base Y is computed from its own randomized start offset and is then trimmed against a height-proportional retention threshold, so the vertical spread is driven by the randomized descending-layer scan, not a zero-draw formula. | No such formula exists in `FancyTrunkPlacer`. Each branch's base Y is `checkStart.getY() - Math.sqrt(dx*dx + dz*dz) * 0.381`, clamped to `trunkTop` (FancyTrunkPlacer.java:67-71), where `checkStart` came from the randomized radius/angle offsets at the current scan layer; branches are then trimmed by `trimBranches`, `localY >= height * 0.2` (:149-151, applied at :85 and :165). The vertical spread is therefore driven by the descending `relativeY` scan (:55) and consumes two `nextFloat` draws per cluster per eligible layer, not zero | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| cherry_trunk_placer always places exactly two branches, not branch_count-many: both branch start offsets are sampled first, before anything else; branch_count is sampled next but only feeds trunk_height's own derivation; exactly one direction index is drawn for the whole placer, with the second branch using its opposite; and each branch draws its own end-offset and horizontal-length providers, then walks toward its endpoint drawing a float at every step to choose a vertical or horizontal step, a drooping staircase rather than a flat-Y horizontal run. | `CherryTrunkPlacer.placeTrunk` (CherryTrunkPlacer.java:71-145) differs in order, in count and in the walk. Both branch start offsets are sampled FIRST, before anything else - `branchStartOffsetFromTop.sample` then `secondBranchStartOffsetFromTop.sample` (:81-82) - then `branchCount.sample` (:87), and the trunk column height depends on that count (:91-97), so it is not simply the drawn tree height. Exactly ONE direction is drawn for the whole placer, `Direction.Plane.HORIZONTAL.getRandomDirection(random)` (:109), the second branch using its opposite (:136). Inside `generateBranch` (:147-184) the order is `branchEndOffsetFromTop.sample` (:161) then `branchHorizontalLength.sample` (:163), and the branch is NOT flat in Y: after 1 or 2 fixed horizontal steps (:165-169) it walks toward the end position drawing `random.nextFloat() < chanceToGrowVertically` at EVERY step to choose vertical or horizontal (:173-183), so branch_end_offset_from_top steers the whole staircase, not just the final attachment | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The multi-attachment foliage driving loop calls the foliage placer once per FoliageAttachment the trunk placer returned, in the same order the trunk placer produced them, with every attachment's own draws completing fully before the next attachment's call begins. | `TreeFeature.doPlace` (TreeFeature.java:82-85) is `foliageAttachments.forEach(a -> config.foliagePlacer.createFoliage(level, foliageSetter, random, config, clippedTreeHeight, a, foliageHeight, leafRadius))` over the list the trunk placer returned - a sequential forEach in list order on one shared RandomSource, so each attachment's draws complete before the next call starts | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vanilla FoliagePlacer registry comprises exactly 11 kinds: blob_foliage_placer and spruce_foliage_placer (already implemented) plus pine_foliage_placer, acacia_foliage_placer, bush_foliage_placer, fancy_foliage_placer, jungle_foliage_placer, mega_pine_foliage_placer, dark_oak_foliage_placer, cherry_foliage_placer, and random_spread_foliage_placer, the last of which is named-deferred (it is what the azalea tree's configured feature uses). | `FoliagePlacerType` (FoliagePlacerType.java:8-20) registers ELEVEN kinds, not ten: `blob_foliage_placer`, `spruce_foliage_placer`, `pine_foliage_placer`, `acacia_foliage_placer`, `bush_foliage_placer`, `fancy_foliage_placer`, `jungle_foliage_placer` (class MegaJungleFoliagePlacer, :14), `mega_pine_foliage_placer`, `dark_oak_foliage_placer`, `random_spread_foliage_placer` (:17-19) and `cherry_foliage_placer`. The missing one is `random_spread_foliage_placer`, which is what `configured_feature/azalea_tree.json` uses | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| pine_foliage_placer draws four values, not two: a base radius provider sample plus a second next_int_bounded draw added to it, an offset, and its own foliage-height provider sample; its per-layer radius is a ramp-up-then-ramp-down profile driven by those draws, not a fixed even/odd alternation, filling a square footprint (that part holds) with a deterministic, zero-draw exact-corner skip whenever the current radius is above zero. | `PineFoliagePlacer` draws four values, not two, and its radius progression is not an even/odd alternation. `foliageRadius` (PineFoliagePlacer.java:51-54) is `super.foliageRadius(...) + random.nextInt(Math.max(trunkHeight + 1, 1))` - the base radius provider sample PLUS a second draw - and `foliageHeight` (:56-59) is `this.height.sample(random)`, a third; the offset provider is the fourth (FoliagePlacer.java:48, 69-71). `createFoliage` (:27-49) starts `currentRadius` at 0 and, per descending layer, increments it while below `leafRadius + radiusOffset`, decrementing once at `yo == offset - foliageHeight + 1` (:43-47). The square footprint is right, but that is `placeLeavesRow`'s behaviour for every placer; `shouldSkipLocation` (:61-64) skips the exact corner whenever `currentRadius > 0`, deterministically | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| acacia_foliage_placer draws radius0 and offset0 (two draws total, its own height field being a fixed zero) but places THREE placeLeavesRow-style layers, not one; only the topmost (y == 0) layer carves a plus/cross shape via a deterministic edge test, while the other two layers use Blob's own deterministic exact-corner skip. | Two draws is right (`foliageHeight` returns a literal 0, AcaciaFoliagePlacer.java:42-45, so only the radius and offset provider samples occur), but it is not one layer and not a four-corner skip. `createFoliage` (:23-40) issues THREE `placeLeavesRow` calls, at `y = -1 - foliageHeight`, `y = -foliageHeight` and `y = 0` with radii `leafRadius + radiusOffset`, `leafRadius - 1` and `leafRadius + radiusOffset - 1`. `shouldSkipLocation` (:47-50) is `y == 0 ? (dx > 1 or dz > 1) and dx != 0 and dz != 0 : dx == currentRadius and dz == currentRadius and currentRadius > 0` - on the y == 0 layer that carves a plus/cross, not four corners. Deterministic (no random) is correct | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| bush_foliage_placer shares Blob's draw shape exactly (two provider draws plus one next_int_bounded(2) per exact-corner candidate) but not its geometry: its per-layer radius formula omits Blob's own halving of the layer offset and its own zero-clamp, and its skip test omits Blob's additional y == 0 clause, so it keeps that layer's corners half the time where Blob always removes them. | The draw shape matches - `BushFoliagePlacer` extends `BlobFoliagePlacer` (BushFoliagePlacer.java:10-15) and inherits its constant `foliageHeight`, so two provider draws plus one `random.nextInt(2)` per exact corner (:40-43) - but it is not a verbatim reuse of the Blob geometry. Blob's per-layer radius is `Math.max(leafRadius + radiusOffset - 1 - yo / 2, 0)` (BlobFoliagePlacer.java:45); Bush's is `leafRadius + radiusOffset - 1 - yo` (BushFoliagePlacer.java:35) - no halving of yo and no clamp at 0. Bush's skip test also drops Blob's `or y == 0` clause (compare BlobFoliagePlacer.java:57 with BushFoliagePlacer.java:42), so the bottom layer keeps its corners half the time instead of always losing them | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| jungle_foliage_placer draws an extra value beyond radius and offset when its attachment is not double-trunked, uses a per-layer radius formula that grows rather than shrinks toward the top, and uses a true circular skip test with zero draws, never Blob's next_int_bounded(2) corner roll. | `MegaJungleFoliagePlacer` (registered as `jungle_foliage_placer`, FoliagePlacerType.java:14) is not Blob with one overridden layer. `createFoliage` (MegaJungleFoliagePlacer.java:27-45) draws an EXTRA value when the attachment is not double-trunked, `leafHeight = doubleTrunk ? foliageHeight : 1 + random.nextInt(2)` (:39), and its per-layer radius is `leafRadius + radiusOffset + 1 - yo` (:42) against Blob's `max(leafRadius + radiusOffset - 1 - yo / 2, 0)`. `shouldSkipLocation` (:52-55) is a true circular test, `dx + dz >= 7 ? true : dx*dx + dz*dz > currentRadius*currentRadius`, with no `nextInt(2)` corner roll at all | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| fancy_foliage_placer draws radius0 and offset0 (two draws total, its own height field being a fixed constant), then for each layer sets the radius to radius0 on the first and last layers and radius0+1 in between, and keeps only positions where (dx+0.5)^2 + (dz+0.5)^2 is at most the radius squared, not dx*dx + dz*dz <= layer_radius*layer_radius + 1. | Two draws is right (`FancyFoliagePlacer` extends `BlobFoliagePlacer` and inherits its constant `foliageHeight`, FancyFoliagePlacer.java:15), but both the layer radius and the fill test are different. `createFoliage` (:23-39) iterates yo DOWNWARD from `offset` to `offset - foliageHeight` and uses `currentRadius = leafRadius + (yo != offset and yo != offset - foliageHeight ? 1 : 0)` - leafRadius on the top and bottom layers, leafRadius + 1 in between - not `radius0 - abs(layer - height/2)/2`. `shouldSkipLocation` (:41-44) skips when `Mth.square(dx + 0.5F) + Mth.square(dz + 0.5F) > currentRadius * currentRadius`, i.e. it keeps `(dx+0.5)^2 + (dz+0.5)^2 <= r^2`, not `dx^2 + dz^2 <= r^2 + 1` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| mega_pine_foliage_placer draws radius0, offset0, and crown_height (three draws total), but has no constant-radius crown band and no even/odd alternation: its radius grows toward the bottom of the crown via a floor-scaled term with a jagged +1 bump, using the same circular skip test as jungle_foliage_placer. | Three draws is right - `MegaPineFoliagePlacer.foliageHeight` is `this.crownHeight.sample(random)` (MegaPineFoliagePlacer.java:61-64) alongside the radius and offset provider samples - but the radius rule is not Pine's. `MegaPineFoliagePlacer` extends `FoliagePlacer` directly (:19-22), and `createFoliage` (:29-59) computes `smoothRadius = leafRadius + radiusOffset + Mth.floor((float)yo / foliageHeight * 3.5F)` for `yo = foliagePos.getY() - yy`, i.e. the radius GROWS toward the bottom, with a jagged +1 when `yo > 0 and smoothRadius == prevRadius and (yy & 1) == 0` (:46-52). There is no crown_height band of constant radius0 and no even/odd alternation. `shouldSkipLocation` (:66-69) is the circular `dx*dx + dz*dz > r*r` with a `dx + dz >= 7` cutoff, not Pine's corner skip | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| dark_oak_foliage_placer's radius provider is sampled exactly once per tree by the tree driver itself, not per attachment; only offset is sampled per call (one draw, not two), and its own height field is a fixed constant. It is called once per each of the trunk placer's 1 to 13 attachments (never a fixed two): a double-trunk attachment places three rows plus a coin-flip-gated fourth row, while every other attachment places two rows. | Wrong on the attachment count, on the per-attachment radius draw and on the shape. The radius is drawn ONCE PER TREE by `TreeFeature.doPlace` (TreeFeature.java:68), not per createFoliage call - only the offset is per attachment (FoliagePlacer.java:48) - and `DarkOakFoliagePlacer.foliageHeight` returns the literal 4 (DarkOakFoliagePlacer.java:50-53), zero draws. `DarkOakTrunkPlacer` returns 1 to 13 attachments, not two (DarkOakTrunkPlacer.java:71, 82). `createFoliage` (:23-48) places THREE rows at y = -1, 0, 1 with radii leafRadius+2, leafRadius+3, leafRadius+2 for a double-trunk attachment plus a `random.nextBoolean()` draw that may add a fourth row at y = 2 (:37-43), and only two rows (leafRadius+2 at y = -1, leafRadius+1 at y = 0) otherwise (:44-47). Corner skipping being deterministic is correct (:55-71, no random) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| cherry_foliage_placer draws radius0, offset0, and its own foliage-height provider sample (three draws, not two) and uses no integer rolls at all: its layer stack is two tapered rows, a run of full-radius rows, and two hanging-leaves-below rows, and its skip test draws only floats — a wide layer drops an exact corner unconditionally with no draw and rolls a near-corner band instead, while only a narrow layer rolls the exact corner itself. | `CherryFoliagePlacer` uses no `nextInt(3)` and no `nextInt(2)`. It extends `FoliagePlacer` directly and `foliageHeight` is `this.height.sample(random)` (CherryFoliagePlacer.java:85-88), a third draw beyond radius and offset. `shouldSkipLocation` (:90-101) draws `random.nextFloat()` against `wideBottomLayerHoleChance` on the y == -1 edge and against `cornerHoleChance` for corners (and, when `currentRadius > 2`, for the `dx + dz > currentRadius*2 - 2` near-corner band); non-corner interior positions are kept with no draw at all. The layer stack (:55-83) is its own - two tapered rows above, a run of full-radius rows, then two `placeLeavesRowWithHangingLeavesBelow` rows at y = -1 and -2 whose extension attempts draw further `nextFloat`s (FoliagePlacer.java:154-168) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A FoliageAttachment's double_trunk flag is true only for attachments produced above a 2x2 trunk footprint (giant_trunk_placer, mega_jungle_trunk_placer, dark_oak_trunk_placer), meaning the foliage placer must center its canopy across all 4 trunk columns at that attachment rather than a single column. | Only two call sites construct an attachment with doubleTrunk true: `GiantTrunkPlacer.java:52` and `DarkOakTrunkPlacer.java:71`; MegaJungle inherits the Giant one through `super.placeTrunk` (MegaJungleTrunkPlacer.java:38) while its own branch attachments are false (:52), and DarkOak's ring attachments are false (:82). The consequence is exactly as described: `FoliagePlacer.placeLeavesRow` (FoliagePlacer.java:103-107) extends the dx/dz sweep by one on the +X/+Z sides when doubleTrunk, and `shouldSkipLocationSigned` (:77-91) measures distance as `min(abs(dx), abs(dx - 1))` and `min(abs(dz), abs(dz - 1))`, i.e. from the 2x2 footprint rather than a single column | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| chorus_plant, end_platform, end_spike, end_island, and end_gateway are End-dimension-exclusive vanilla features. | All five are registered Feature kinds (Feature.java:66, 108-111) and each appears in the data pack only inside End biomes: `chorus_plant` and `end_gateway_return` in `worldgen/biome/end_highlands.json`, `end_platform` and `end_spike` in `worldgen/biome/the_end.json`, `end_island_decorated` in `worldgen/biome/small_end_islands.json` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| void_start_platform, despite its End-adjacent name, is usable outside the End dimension in real vanilla. | `Feature.VOID_START_PLATFORM` (Feature.java:70-72) is referenced by `worldgen/placed_feature/void_start_platform.json` from exactly one biome, `worldgen/biome/the_void.json`, and that biome is used by `worldgen/flat_level_generator_preset/the_void.json` - a superflat OVERWORLD preset (single air layer, `features: true`), not an End dimension | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| fallen_tree places an unconditional stump log at origin first, then draws a direction index (one draw) and a log length via its configured IntProvider minus 2 (one or more draws), then draws a further start-offset roll (one draw) placing the log's own start 2 or 3 blocks out from origin; placement is accepted as long as at most 2 consecutive non-solid support columns occur along the log's length, not a single solid-block-below-origin gate. | `FallenTreeFeature.placeFallenTree` (FallenTreeFeature.java:36-45) differs on the gate, the length and the draw count. It first places a STUMP log at the origin unconditionally and runs `stumpDecorators` (:37, :59-62). Then `direction = Direction.Plane.HORIZONTAL.getRandomDirection(random)` (:38, one draw), `logLength = config.logLength.sample(random) - 2` (:39, note the minus 2), and an extra draw for the start offset, `origin.relative(direction, 2 + random.nextInt(2))` (:40) - so the log begins 2 or 3 blocks from the origin, not at it. Ground is then found by scanning up 1 and down up to 6 (:47-57), and placement is gated by `canPlaceEntireFallenLog` (:64-87), which tolerates up to 2 consecutive non-solid columns rather than requiring a solid block below the origin. The horizontal axis property is right (:138-140) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| huge_fungus always requires the block below origin to match the configured valid_base_state, regardless of the planted flag (planted only skips a separate generation-depth ceiling check); it draws total_height = 4 + random.next_int_bounded(9) (one draw, 4 to 12), doubles it when random.next_int_bounded(12) == 0 (a second draw), and forms a hat whose height and per-layer radius are themselves drawn (further draws) rather than a fixed-radius-3 disk, with each hat-candidate block drawing two separate next_float() rolls (a decor roll then a hat-fill roll), never a single random.next_int_bounded(5) == 0 roll. | Wrong on the gate, the height and the hat. `HugeFungusFeature.place` (HugeFungusFeature.java:32-39) requires `level.getBlockState(origin.below()).is(config.validBaseState.getBlock())` ALWAYS - `planted` does not skip it; `planted` only skips the gen-depth ceiling check at :46-51. Height is `totalHeight = Mth.nextInt(random, 4, 13)` (:41, one draw, 4 to 13) followed by `if (random.nextInt(12) == 0) totalHeight *= 2` (:42-44), a second draw; a third, `isHuge = !planted and random.nextFloat() < 0.06F` (:53), widens the stem to 3x3 with a `nextFloat() < 0.1F` roll per corner column (:95-98). The hat is not a fixed-radius-3 disk: `hatHeight = Math.min(random.nextInt(1 + totalHeight / 3) + 5, totalHeight)` (:118) and the per-layer radius is `dy < totalHeight - random.nextInt(3) ? 2 : 1` (:122, one draw per layer), forced to 3 when `hatHeight > 8 and dy < hatStartY + 4`, plus 1 when isHuge. Per candidate, `placeHatBlock` (:161-178) draws `nextFloat` against a position-dependent decor chance (0.1F inside, 0.01F at corners, 5.0E-4F on edges) and then a second `nextFloat` against a hat chance - never a `nextInt(5) == 0` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| huge_red_mushroom and huge_brown_mushroom draw height = 4 + random.next_int_bounded(3) (one draw, 4 to 6), then double it when random.next_int_bounded(12) == 0 (a second draw, giving 4-6 or 8-12), place the cap before the stem, and place a straight stem column for the full height layers, not height-1. | `AbstractHugeMushroomFeature.getTreeHeight` (AbstractHugeMushroomFeature.java:41-48) draws TWICE: `treeHeight = random.nextInt(3) + 4` and then `if (random.nextInt(12) == 0) treeHeight *= 2`, so the range is 4 to 6 or 8 to 12, not a flat 4 to 6. `placeTrunk` (:20-32) runs `for (int dy = 0; dy < treeHeight; dy++)`, i.e. treeHeight stem layers, not treeHeight - 1. `place` (:82-97) also runs `makeCap` BEFORE `placeTrunk` (:94-95) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The huge mushroom cap is not a uniform 3-layer shape for both kinds: huge_red_mushroom's cap spans 4 Y-layers (height-3 through height inclusive, no clamp at 0) while huge_brown_mushroom's cap is a single layer at height; both are fully deterministic given height for a non-drawing cap_provider, but a weighted cap_provider would draw once per candidate position. | Neither kind uses three layers. `HugeRedMushroomFeature.makeCap` (HugeRedMushroomFeature.java:25) loops `for (int dy = treeHeight - 3; dy <= treeHeight; dy++)` - FOUR layers. `HugeBrownMushroomFeature.makeCap` (HugeBrownMushroomFeature.java:24-56) places a SINGLE layer at `dy = treeHeight`. Neither clamps at 0. "Zero further draws" holds only because vanilla's huge-mushroom configs use non-drawing cap providers - the code calls `config.capProvider().getState(level, random, origin)` per candidate (HugeRedMushroomFeature.java:39, HugeBrownMushroomFeature.java:41), which would draw for a weighted provider | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| huge_red_mushroom's cap keeps only an edge ring (exactly one of the X/Z edge flags set) with corners already excluded on its three lower layers, and is solid on its narrower top layer (radius foliage_radius - 1); huge_brown_mushroom's cap is a single flat disk at the SAME radius as red's lower layers (not one larger), with its four exact corners notched (both edge flags set), the geometry essentially inverted relative to the original description. | Both silhouettes are described wrongly. Red (HugeRedMushroomFeature.java:25-56): the radius is `config.foliageRadius()` on the three lower layers and `foliageRadius - 1` on the TOP layer only (:26) - no per-layer taper; the fill test `dy >= treeHeight or xEdge != zEdge` (:37) means the lower layers keep only the edge ring MINUS its corners while the top layer is filled solid, the opposite of "corners notched on the top layer only". Brown (HugeBrownMushroomFeature.java:25-35): one layer at radius `config.foliageRadius()` - the same radius, not one larger - and it DOES notch, skipping every position where `xEdge and zEdge` (:35) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| vegetation_patch and waterlogged_vegetation_patch draw x_radius and z_radius as two independent samples of xz_radius (each plus 1), X before Z, then scan candidate columns within that rectangle in ascending dx then dz order. | `VegetationPatchFeature.place` (VegetationPatchFeature.java:28-29) samples `xzRadius` TWICE and adds one each time: `int xRadius = config.xzRadius().sample(random) + 1` then `int zRadius = config.xzRadius().sample(random) + 1`, X before Z. The scan order is right - `placeGroundPatch` (:50-53) iterates dx from -xRadius to xRadius outermost and dz from -zRadius to zRadius innermost - but the two radii are independent draws and each is one larger than the provider value | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For vegetation_patch and waterlogged_vegetation_patch, boundary columns use a rectangular (not circular) edge test; the four corners are skipped unconditionally with zero draws, and only an edge-but-not-corner column draws an edge_roll (one draw), short-circuited away entirely when extra_edge_column_chance is 0; interior columns draw nothing. | `VegetationPatchFeature.placeGroundPatch` (VegetationPatchFeature.java:50-58) uses a rectangular edge test, not a circular one: `isXEdge = dx == -xRadius or dx == xRadius`, `isZEdge = dz == -zRadius or dz == zRadius`. The four CORNERS (both edges) are skipped unconditionally with zero draws; only edge-but-not-corner columns roll, and the roll is `config.extraEdgeColumnChance() != 0.0F and !(random.nextFloat() > config.extraEdgeColumnChance())` - short-circuited away entirely when the configured chance is 0, so it is not always one draw per boundary column. Interior columns drawing nothing is correct | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For vegetation_patch and waterlogged_vegetation_patch, each surviving column moves inward while air then outward while non-air (zero draws) and is accepted when empty with a sturdy inward-facing neighbor, never scanning from origin.y + vertical_range for the replaceable_tag directly; the waterlogged variant does not require pre-existing water, it keeps positions not exposed on four sides and down, then sets water into them. | `VegetationPatchFeature.placeGroundPatch` (VegetationPatchFeature.java:59-71) does not scan from `origin.y + vertical_range` and never tests `replaceable` while searching. From `(dx, 0, dz)` it first moves INWARD (`config.surface().getDirection()`) while the position is air, up to verticalRange steps (:61-63), then moves OUTWARD while the position is non-air, up to verticalRange steps (:65-67); the column is accepted when `level.isEmptyBlock(pos)` and the block one step inward `isFaceSturdy` (:69-71). The `replaceable` tag is consulted only later, inside `placeGround` (:113-135). The waterlogged variant does not require water either: `WaterloggedVegetationPatchFeature.placeGroundPatch` (WaterloggedVegetationPatchFeature.java:32-46) keeps the surface positions that are NOT exposed on any of N/E/S/W/DOWN and then SETS water at them | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For vegetation_patch and waterlogged_vegetation_patch, a depth IntProvider is sampled (one or more draws) plus possibly one more via a next_float() roll gated by extra_bottom_block_chance (short-circuited away at chance 0), and ground_state blocks are then written starting one step further inward of the found surface position, never at the surface position itself. | `VegetationPatchFeature.placeGroundPatch` (VegetationPatchFeature.java:72-75) computes `depth = config.depth().sample(random) + (config.extraBottomBlockChance() > 0.0F and random.nextFloat() < config.extraBottomBlockChance() ? 1 : 0)` - the claim omits the `depth` IntProvider sample entirely, which is one or more draws taken BEFORE the extra-bottom roll, and the extra roll is short-circuited away when the configured chance is 0. `placeGround` (:113-135) then writes `depth` ground blocks starting at `belowPos` (one step INWARD of the air surface position) and stepping further inward, so nothing is written at the surface position itself | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| vegetation_patch and waterlogged_vegetation_patch re-enter the configured vegetation_feature as a full nested placement-modifier chain at the computed surface position. | `VegetationPatchConfiguration.vegetationFeature` is a `Holder<PlacedFeature>` (VegetationPatchConfiguration.java:19, 32) and `VegetationPatchFeature.placeVegetation` (VegetationPatchFeature.java:103-111) calls `.value().place(level, generator, random, pos)`, which runs the complete modifier list (PlacedFeature.java:38-40, 46-52). Two details for the blueprint to carry: each surface position is first gated by `random.nextFloat() < config.vegetationChance()` (:97), and the nested feature is entered at `vegetationPos.relative(config.surface().getDirection().getOpposite())` (:110) - one block OUT of the ground block, with the waterlogged variant applying a further `.below()` (WaterloggedVegetationPatchFeature.java:70) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ordinary overworld climbing vines feature consumes zero RNG draws and touches exactly one position: it checks the six directions in fixed Down, Up, North, South, West, East order, skipping Down, and places a single vine face on the FIRST direction whose neighbor is acceptable, never a 9x9 footprint and never a next_bool() roll. | `VinesFeature.place` (VinesFeature.java:17-33) consumes ZERO RNG draws and touches ONE position. It returns false unless `level.isEmptyBlock(origin)`, then iterates `Direction.values()` - DOWN, UP, NORTH, SOUTH, WEST, EAST - skipping DOWN, and on the FIRST direction whose neighbour satisfies `VineBlock.isAcceptableNeighbour` it sets a single vine face and returns true. There is no 9x9 footprint, no `nextBool` roll, and it is explicitly first-match-wins, not all-four-faces | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| weeping_vines is genuinely config-free and hardcodes its own native block ids, but twisting_vines is NOT non-datapack-configurable: it carries its own spread_width/spread_height/max_height config, read at placement time. | The block ids are indeed hardcoded (`WeepingVinesFeature` uses `Blocks.NETHER_WART_BLOCK`, `WEEPING_VINES`, `WEEPING_VINES_PLANT`; `TwistingVinesFeature` uses `TWISTING_VINES`, `TWISTING_VINES_PLANT`), but "non-datapack-configurable" is false for twisting_vines: `Feature.TWISTING_VINES = register("twisting_vines", new TwistingVinesFeature(TwistingVinesConfig.CODEC))` (Feature.java:125), and `TwistingVinesFeature.place` reads `config.spreadWidth()`, `config.spreadHeight()` and `config.maxHeight()` from that JSON config (TwistingVinesFeature.java:29-32, 35-41). Only weeping_vines is config-free (`NoneFeatureConfiguration`, Feature.java:124) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| weeping_vines and twisting_vines are both multi-hundred-attempt scatter features, not a single column at origin: each runs many scatter attempts across a footprint bounded by its own spread parameters, and each successful hit draws its own vine height and places body segments plus a distinct head block whose age is drawn separately, rather than a shared height = 8 + random.next_int_bounded(4) formula applied to one column. | Neither feature works this way and neither draws `8 + nextInt(4)`. `WeepingVinesFeature.place` (WeepingVinesFeature.java:22-39) requires the origin to be empty with netherrack or nether wart block ABOVE, sets a nether wart block at the origin, then runs 200 scatter attempts each drawing six ints (`nextInt(6) - nextInt(6)`, `nextInt(2) - nextInt(5)`, `nextInt(6) - nextInt(6)`, :47) to spread wart, then 100 more attempts with six ints each (:73) where each valid hit draws `vineHeight = Mth.nextInt(random, 1, 8)`, `if (random.nextInt(6) == 0) vineHeight *= 2`, `if (random.nextInt(5) == 0) vineHeight = 1` (:77-84). `TwistingVinesFeature.place` (TwistingVinesFeature.java:35-53) runs `spreadWidth * spreadWidth` attempts with three `Mth.nextInt` draws each and the same three height rolls against the configured `maxHeight`. Both column writers place PLANT segments and one head block whose AGE is a further `Mth.nextInt(random, 17, 25)` draw (WeepingVinesFeature.java:94-109, TwistingVinesFeature.java:71-86), and stop when the next position is not empty | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| bamboo draws stalk_height = 5 + random.next_int_bounded(12) (one draw, 5 to 16) first and unconditionally; the probability roll (random.next_float(), one draw) comes after and gates only a separate podzol disk, not the bamboo growth. Leaves are not thirds: only when the column reaches at least 3 blocks does it get a leafed top of exactly 3 blocks (one new block placed above the last trunk block plus the two trunk blocks just below it rewritten in place), with everything below staying bare. | `BambooFeature.place` (BambooFeature.java:30-75) reverses the order and the leaf layout. `int height = random.nextInt(12) + 5` is drawn FIRST and unconditionally (:41), before `random.nextFloat() < config.probability` (:42); the probability roll gates only a podzol disk whose radius is a third draw, `r = random.nextInt(4) + 1` (:43-56). The column places up to `height` plain `BAMBOO_TRUNK` blocks while the position is empty (:59-62); leaves are not thirds - if the column reached at least 3 blocks, exactly the top three blocks are rewritten as `BAMBOO_FINAL_LARGE`, `BAMBOO_TOP_LARGE` and `BAMBOO_TOP_SMALL` (:64-68), everything below staying leafless | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| nether_forest_vegetation's nylium gate is a single up-front test on the block below origin, not a per-candidate floor-tag test; each attempt draws six values, not three, two per axis in the order X then Y then Z, and samples the configured block state before testing placement, for every attempt, not merely successful candidates. | The attempt count `config.spreadWidth * config.spreadWidth` is right (NetherForestVegetationFeature.java:31), the offsets are not. Each attempt draws SIX values, not three: `origin.offset(random.nextInt(spreadWidth) - random.nextInt(spreadWidth), random.nextInt(spreadHeight) - random.nextInt(spreadHeight), random.nextInt(spreadWidth) - random.nextInt(spreadWidth))` (:32-36), in the argument order X then Y then Z with two draws per axis, and Y uses a separate `spreadHeight` bound - there is no `nextInt(spread*2 + 1) - spread`. The nylium check is a single up-front test on `origin.below()` against `BlockTags.NYLIUM` (:20-25), not a per-candidate floor-tag test; per candidate the tests are `level.isEmptyBlock(finalPos)`, `finalPos.getY() > level.getMinY()` and `state.canSurvive(level, finalPos)` (:38), and `config.stateProvider.getState(level, random, finalPos)` is evaluated BEFORE those tests (:37) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| seagrass draws four scatter values (x and z, two draws each) before any placement test, taking Y from the OCEAN_FLOOR heightmap at that offset column rather than acting at origin itself; is_tall is gated by the config's own codec-supplied probability field, never a hardcoded 0.3. | `SeagrassFeature.place` (SeagrassFeature.java:19-50) starts with FOUR scatter draws, `int x = random.nextInt(8) - random.nextInt(8)` and `int z = random.nextInt(8) - random.nextInt(8)` (:26-27), and takes Y from `Heightmap.Types.OCEAN_FLOOR` at that offset position (:28-29) - the feature never acts at the origin itself. The water test is `level.getBlockState(grassPos).is(Blocks.WATER)` at that scattered position (:30). The tall roll is `random.nextDouble() < config.probability` (:31) - a codec-supplied probability, not a hardcoded 0.3. The tall/short branch behaviour (:34-43) does match, including placing nothing when the position above is not water | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| kelp uses a single random.next_int_bounded(10) draw, never a nested random.next_int_bounded(random.next_int_bounded(10) + 1); Y comes from the OCEAN_FLOOR heightmap and that position must be water, with no separate solid-floor test. | There is no nested `nextInt`. `KelpFeature.place` (KelpFeature.java:24-29) takes Y from `Heightmap.Types.OCEAN_FLOOR`, requires that position to be `Blocks.WATER`, and draws `int height = 1 + random.nextInt(10)` - a single `nextInt(10)`. The column loop runs `h = 0..height` inclusive (:31-49) requiring both the position and the one above to be water; at `h == height` it places KELP with `AGE = random.nextInt(4) + 20`, an extra draw (:34), otherwise KELP_PLANT; when the water check fails at `h > 0` it instead places a KELP head one block BELOW with another `nextInt(4) + 20` draw and breaks (:39-46). No solid-floor test is made | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| sea_pickle's configured IntProvider is the number of placement ATTEMPTS, not a pickles value; each attempt independently draws its own scatter offset and its own pickles = 1 + random.next_int_bounded(4) (a fresh draw per attempt), and the gate is that the position is WATER (not air-or-water) with a survivable pickle state. | `CountConfiguration.count()` is the number of ATTEMPTS, not the pickles property: `SeaPickleFeature.place` (SeaPickleFeature.java:24-26) samples it once and loops that many times. Each attempt draws four scatter values, `x = random.nextInt(8) - random.nextInt(8)` and `z = random.nextInt(8) - random.nextInt(8)` (:27-28), takes Y from `Heightmap.Types.OCEAN_FLOOR` (:29), and sets `SeaPickleBlock.PICKLES` to `random.nextInt(4) + 1` (:31) - a fresh draw per attempt, not `(1 + raw).clamp(1, 4)`. The gate is `level.getBlockState(picklePos).is(Blocks.WATER) and pickleState.canSurvive(level, picklePos)` (:32) - water, not air-or-water | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| coral_tree, coral_mushroom, and coral_claw all first draw a random coral block from the coral-blocks tag (one draw) before any placement test, and their shared gate is that the target is water or an already-coral-tagged block with water above it — there is no solid-floor test at all. | Not zero draws and not that gate. `CoralFeature.place` (CoralFeature.java:25-32) first draws a coral block from the `CORAL_BLOCKS` tag via `BuiltInRegistries.BLOCK.getRandomElementOf(BlockTags.CORAL_BLOCKS, random)` (:30) before any placement test. `placeCoralBlock` (:36-70) then requires the target to be `Blocks.WATER` or a `CORALS`-tagged block AND the block ABOVE to be water (:39) - there is no solid-floor test - and on success draws `nextFloat() < 0.25F` for a coral above, possibly `nextFloat() < 0.05F` for a sea pickle (whose PICKLES value is another `nextInt(4) + 1`), and one `nextFloat() < 0.2F` per horizontal direction for wall fans, each success drawing again from `WALL_CORALS` (:41-64) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| coral_tree's branch count is 2 + random.next_int_bounded(3) (2 to 4, never a fixed two), its branch directions come from a shuffle of the four horizontal directions (three further draws), and each branch draws its own height and a per-step outward-growth roll, consuming far more draws than the trunk-length roll alone. | The trunk draw matches - `int trunckHeight = random.nextInt(3) + 1` (CoralTreeFeature.java:20) - but nothing after it does. `nBranches = random.nextInt(3) + 2` (:31, 2 to 4 branches, not a fixed 2), the directions come from `Direction.Plane.HORIZONTAL.shuffledCopy(random)` (:32, a Fisher-Yates shuffle drawing `nextInt(i)` for i = 4, 3, 2 - Util.java:1098-1117), and each branch draws `branchHeight = random.nextInt(5) + 2` (:37) then walks upward with a `random.nextFloat() < 0.25F` step-out roll after the first segment and once a segment reaches length 2 (:40-47). Every `placeCoralBlock` inside the walk draws further (see row 74) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| coral_mushroom draws four values up front (height, width, length each 3 + random.next_int_bounded(3), and a sink value), walks a box 4 to 6 positions per axis (inclusive loop bounds over 3-to-5 drawn dimensions) keeping only its outer shell minus edges and corners, and rolls an additional hole chance per surviving candidate — never a fixed radius-2, 2-layer, zero-draw shape. | `CoralMushroomFeature.placeFeature` (CoralMushroomFeature.java:17-41) opens with FOUR draws: `height = random.nextInt(3) + 3`, `width = random.nextInt(3) + 3`, `length = random.nextInt(3) + 3`, `sinkValue = random.nextInt(3) + 1` (:18-21). It then walks a box of 3 to 5 per axis, sunk by sinkValue, keeping only the shell minus its edges and corners (:29-32), and each surviving candidate draws `random.nextFloat() < 0.1F` as a hole roll before `placeCoralBlock` (:33-34), which draws again. It is neither radius 2, nor 2 layers, nor deterministic | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| coral_claw draws a claw direction before the arm count, shuffles its own arm directions from the claw direction and its two neighbors, and draws a sideways length plus an inward length and a per-step rise roll for every arm — consuming far more draws than the arm-count roll alone. | The arm count formula matches - `nBranches = random.nextInt(2) + 2` (CoralClawFeature.java:26) - and nothing else does. A coral block is placed at the origin first, with its own draws (:21-23, CoralFeature.java:36-70); `clawDirection = Direction.Plane.HORIZONTAL.getRandomDirection(random)` is drawn before the count (:25); the arm directions come from `Util.toShuffledList` over {clawDirection, its clockwise, its counter-clockwise} (:27-29), another shuffle. Per arm: `sidewayLength = random.nextInt(2) + 1` (:33), then either `inwayLenth = random.nextInt(3) + 2` for the claw direction or a `Util.getRandom({branchDirection, UP}, random)` segment-direction draw plus `inwayLenth = random.nextInt(3) + 3` otherwise (:37-45), a sideways walk, and an inward walk of up to inwayLenth steps each drawing `random.nextFloat() < 0.25F` to rise one block (:54-63) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
