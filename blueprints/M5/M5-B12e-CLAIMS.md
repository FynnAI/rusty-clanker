# M5-B12e — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| no_op is a genuine registered Feature kind, distinct from an unrecognized name, that performs zero draws and zero writes. | `Feature.NO_OP` (Feature.java:61) registers the id `no_op` with `NoneFeatureConfiguration.CODEC`; `NoOpFeature.place` (NoOpFeature.java:11-14) has an empty body that only returns true — no RandomSource use, no setBlock. It is genuinely distinct from an unknown name: an unregistered `type` fails codec decoding of the configured feature (Feature registry lookup), it is not silently no-op'd, so the blueprint's own "registered no-op vs unrecognized name" split is right. Config shape is the unit codec `NoneFeatureConfiguration` (NoneFeatureConfiguration.java:6-9), matching the blueprint's empty `NoOpConfiguration` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| random_selector evaluates its listed entries in order, drawing one next_float per entry checked, and selects the first entry whose draw is less than that entry's chance value, consuming zero further draws once a success is found. | `RandomSelectorFeature.place` (RandomSelectorFeature.java:23-27): `for (WeightedPlacedFeature feature : config.features()) { if (random.nextFloat() < feature.chance()) return feature.place(...); }` — one `nextFloat` per entry visited, strict `<` against `chance`, immediate return on the first success so later entries are never visited and draw nothing. Entry shape is `WeightedPlacedFeature(feature, chance)` with `chance` a `Codec.floatRange(0.0F, 1.0F)` (WeightedPlacedFeature.java:13-17), matching the blueprint's `WeightedPlacedFeatureRef`. Note for the blueprint: `RandomSelectorFeature`, `RandomFeatureConfiguration` and `WeightedPlacedFeature` are all marked `@Deprecated` in 26.2 (Feature.java:137-140, RandomFeatureConfiguration.java:12, WeightedPlacedFeature.java:12) — still fully live, but flagged | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| random_selector falls through to its default feature, with zero further draws, if none of its listed entries succeed. | `RandomSelectorFeature.place` (RandomSelectorFeature.java:29): after the loop falls through, `config.defaultFeature().value().place(level, chunkGenerator, random, origin)` runs with no intervening draw. The JSON key is `default` (RandomFeatureConfiguration.java:18, `PlacedFeature.CODEC.fieldOf("default")`), confirmed in datagen at data/minecraft/worldgen/configured_feature/bamboo_vegetation.json (`"config": { "default": { "feature": ..., "placement": [...] }, ... }`) — matching the blueprint's `RandomSelectorConfiguration.default` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| weighted_random_selector sums the weight field of every listed entry into a total; this kind has no default feature at all, so when that total is 0 it selects nothing and consumes zero draws. | Correction: there is NO default feature on this kind. `WeightedRandomFeatureConfiguration` (WeightedRandomFeatureConfiguration.java:11-15) has exactly one field, `features`, a `WeightedList<Holder<PlacedFeature>>` — no `default` key exists in the codec, and datagen confirms it (data/minecraft/worldgen/configured_feature/sulfur_spring.json carries only `config.features`). The total IS summed: `WeightedList` constructor (WeightedList.java:25-35) sets `totalWeight = WeightedRandom.getTotalWeight(items, Weighted::weight)` (WeightedRandom.java:13-25, a long accumulator that throws above 2147483647). When that total is 0 the internal `selector` is left null (WeightedList.java:28-29), `getRandom` returns `Optional.empty()` with zero draws (:76-79), and `WeightedRandomSelectorFeature.place` (WeightedRandomSelectorFeature.java:25-26) then returns false having placed nothing — it does not fall back to any feature. Also correct the entry shape: an entry is `{ "data": <placed feature>, "weight": <non-negative int> }` (Weighted.java:29-38, `elementCodec.fieldOf("data")` plus `NON_NEGATIVE_INT.fieldOf("weight")`), not the blueprint's `{ feature, weight }` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| weighted_random_selector, when the total weight is nonzero, draws exactly one next_int_bounded(total) value, then walks its entries in list order subtracting each entry's weight from the running roll until the entry where roll is less than that entry's weight is selected. | `WeightedList.getRandom` (WeightedList.java:76-83): exactly one `random.nextInt(this.totalWeight)`, then `selector.get(selection)`. For totalWeight >= 64 the selector is `Compact.get` (WeightedList.java:181-191) which is literally that walk in list order — `selection -= entry.weight(); if (selection < 0) return entry` — the same entry the blueprint's `roll < weight` test-then-subtract form picks. For totalWeight < 64 vanilla precomputes a flat lookup array instead (`Flat`, WeightedList.java:194-212) filled in list order, so the index-to-entry mapping and therefore the selected entry are identical; the draw count is one either way. The blueprint's trailing "delegate to default" safety net after the walk has no counterpart in vanilla (see the previous row) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| simple_random_selector draws exactly one next_int_bounded(N) value, where N is the number of listed features, and selects the feature at that index. | `SimpleRandomSelectorFeature.place` (SimpleRandomSelectorFeature.java:23-25): `int index = random.nextInt(config.features().size()); PlacedFeature feature = config.features().get(index).value();` — one draw, direct index. Config is `CompositeFeatureConfiguration(HolderSet<PlacedFeature> features)` under the key `features`, wrapped in `ExtraCodecs.nonEmptyHolderSet` so N is never 0 (CompositeFeatureConfiguration.java:12-15) — matching the blueprint's `SimpleRandomSelectorConfiguration` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| random_boolean_selector draws exactly one next_bool() value, selecting feature_true on true and feature_false on false. | `RandomBooleanSelectorFeature.place` (RandomBooleanSelectorFeature.java:22-23): `boolean result = random.nextBoolean(); return (result ? config.featureTrue : config.featureFalse).value().place(...)` — exactly one draw. The JSON keys are `feature_true` and `feature_false` (RandomBooleanFeatureConfiguration.java:13-14), matching the blueprint's `RandomBooleanSelectorConfiguration` field names | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| sequence runs its listed features in list order at the same origin, each to full completion before the next begins, but stops the moment one delegated feature fails: the first failure aborts the sequence and every later entry is skipped, consuming zero RNG draws of its own beyond whatever each delegated feature's own chain consumes. | Correction: vanilla SHORT-CIRCUITS. `SequenceFeature.place` (SequenceFeature.java:14-21) is `for (Holder<PlacedFeature> feature : context.config().features()) { if (!feature.value().place(context.level(), context.chunkGenerator(), context.random(), context.origin())) return false; } return true;` — the first entry whose `place` returns false aborts the sequence and every later entry is skipped entirely, so "runs every one of its listed features" holds only while each entry succeeds. Everything else in the claim is right: iteration is in the decoded list order of `CompositeFeatureConfiguration.features` (CompositeFeatureConfiguration.java:12-15), each entry gets the unchanged `context.origin()` and the unchanged shared `context.random()`, each runs to completion before the next starts, and SequenceFeature itself takes zero draws | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In sequence, later entries in the list can overwrite earlier entries' own blocks, which is correct and matches vanilla's own layered-sequence behavior. | `SequenceFeature.place` (SequenceFeature.java:14-21) keeps no per-position bookkeeping of any kind — no visited set, no already-written guard — so each entry's own feature writes through the ordinary `WorldGenLevel.setBlock` path and a later entry writing a position an earlier entry already wrote simply replaces it. Vanilla relies on exactly that layering in its own data: data/minecraft/worldgen/configured_feature/sulfur_spring.json's `minecraft:sequence` first scatters `minecraft:tuff` via `simple_block` over a trapezoid-spread cloud of positions and then places a `minecraft:template` structure over the same neighbourhood at `y_spread: -7`, which necessarily rewrites part of that tuff | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| scattered_ore draws one next_int_bounded(size + 1) value before any attempt, where size is OreConfiguration's own field, and repeats its placement attempt that many times (zero attempts is a normal outcome). | Correction: the repetition count is neither 3 nor fixed. `ScatteredOreFeature.place` (ScatteredOreFeature.java:23) computes `int numberOfTries = random.nextInt(config.size + 1);` — one `next_int_bounded(size + 1)` draw taken BEFORE any attempt, where `size` is `OreConfiguration.size` from the configured feature (e.g. data/minecraft/worldgen/configured_feature/ore_ancient_debris_large.json has `"size": 3`, so 0..3 tries, and ore_ancient_debris_small.json its own size). The loop then runs `for (int i = 0; i < numberOfTries; i++)` (:26). The whole "fixed 3" placeholder must be replaced by the drawn count | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each of scattered_ore's attempts computes dx as round((next_float() - next_float()) * min(attempt_index, 7)) — two next_float draws, not a bounded-int draw. | Correction: no bounded-int draw and no minus-4 offset exists on this path. `ScatteredOreFeature.offsetTargetPos` (ScatteredOreFeature.java:41-48) computes each axis via `getRandomPlacementInOneAxisRelativeToOrigin` (:50-52) = `Math.round((random.nextFloat() - random.nextFloat()) * maxDistanceFromOrigin)` — TWO `nextFloat` draws per axis, differenced, scaled and rounded. The scale is `Math.min(i, 7)` for attempt index i (:27, with `MAX_DIST_FROM_ORIGIN = 7` at :11), so the very first attempt (i = 0) always lands exactly on the origin and later attempts widen up to plus or minus 7. Draw order per attempt is xd, then yd, then zd (:44-46) — six `nextFloat` draws per attempt, not two bounded-int draws | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each of scattered_ore's attempts computes dz the same way as dx — round((next_float() - next_float()) * min(attempt_index, 7)) — and also draws dy by the identical formula between the x and z draws, applying all three offsets; the Y coordinate is not left unchanged. | Correction, twice over. The Z draw has the same wrong shape as the X draw above — `Math.round((random.nextFloat() - random.nextFloat()) * Math.min(i, 7))`, two `nextFloat` draws (ScatteredOreFeature.java:46, :50-52) — and Y is NOT left unchanged: `offsetTargetPos` draws a `yd` by the identical formula between the X and Z draws and applies it, `targetPos.setWithOffset(origin, xd, yd, zd)` (ScatteredOreFeature.java:44-47). scattered_ore scatters in all three axes | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| scattered_ore does not reuse the ore feature kind's own placement algorithm; each attempt reads the block state at the jittered position offset by (dx, dy, dz) and, for the first target-state entry passing the shared admissibility check the ore kind also uses, writes a single block there. | Correction: `scattered_ore` does not reuse the `ore` kind's placement algorithm at all. `ScatteredOreFeature.place` (ScatteredOreFeature.java:26-36) reads the block state at the jittered position and, for the first `config.targetStates` entry passing the shared static predicate `OreFeature.canPlaceOre(...)`, does a single `level.setBlock(targetPos, targetState.state, 2)` — one block per attempt. `OreFeature.place`/`doPlace` (OreFeature.java:22-52 and :54 onward) is an entirely different algorithm: one `nextFloat` for an angle, two `nextInt(3)` Y draws, a lerped double-ended ellipsoid blob over `config.size` samples with a BitSet-guarded probe volume. Only the static helper `canPlaceOre` is shared. Reusing M5-B07's `ore::place` here is therefore a deviation, not a verbatim vanilla reuse, and the offset is `(dx, yd, zd)` rather than `(dx, 0, dz)` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The RNG stream used by a feature's placement continues across every one of that feature's own multiple placement attempts within one chunk, rather than being reseeded between attempts. | `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:385-393) calls `random.setFeatureSeed(decorationSeed, globalIndexOfFeature, stepIndex)` exactly ONCE per placed feature, immediately before `feature.placeWithBiomeCheck(level, this, random, origin)`; nothing reseeds inside. `PlacedFeature.placeWithContext` (PlacedFeature.java:46-63) then threads that same `RandomSource` through every placement modifier (`placementModifier.getPositions(context, random, p)`, :49-51) and through `feature.place(..., random, pos)` for each of the possibly many positions the chain yields (:55-62), so a `count`-multiplied feature's attempts all consume one continuous stream | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla never resolves a Biome placement modifier inside a nested selector/sequence delegation chain at all: the nested-call path leaves the top feature unset, and vanilla's own data never places a Biome modifier inside a nested chain; only the top-level call binds a checkable feature, and it is the outer placed feature's own identity that gets checked there. | Correction: vanilla checks NEITHER — it throws. A nested delegation goes through `PlacedFeature.place` (PlacedFeature.java:38-40), which builds `new PlacementContext(level, generator, Optional.empty())` — `topFeature` is empty (PlacementContext.java:17-24, :46-48). `BiomeFilter.shouldPlace` (BiomeFilter.java:21-26) does `context.topFeature().orElseThrow(...)`, so a `minecraft:biome` modifier inside a nested chain raises IllegalStateException ("Tried to biome check an unregistered feature..."), and vanilla's own data never puts one there (grep for `"minecraft:biome"` in data/minecraft/worldgen/configured_feature/bamboo_vegetation.json and sulfur_spring.json returns 0 hits). Only the TOP-LEVEL call uses `placeWithBiomeCheck` (PlacedFeature.java:42-44), binding `topFeature` to that outer PlacedFeature, which is then the name tested by `BiomeGenerationSettings.hasFeature` (BiomeGenerationSettings.java:67). So vanilla's rule is the opposite of the claim: the outer feature's own identity, and only at the outer level. Rebinding `feature_name` to the nested feature in the re-entrant `PlacementCtx` is a deviation that must be recorded as such | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| random_selector, weighted_random_selector, simple_random_selector, and random_boolean_selector each place their selected feature at the same block position as the outer call, continuing the same already-seeded RNG stream rather than starting a new one. | All four pass the outer `context.origin()` and the outer `context.random()` straight into the selected `PlacedFeature.place(level, chunkGenerator, random, origin)`: RandomSelectorFeature.java:25 and :29 (via `WeightedPlacedFeature.place`, WeightedPlacedFeature.java:22-24), WeightedRandomSelectorFeature.java:22-26, SimpleRandomSelectorFeature.java:21-25, RandomBooleanSelectorFeature.java:21-23. No new RandomSource is constructed and no reseed occurs anywhere on these paths; the same holds for `sequence` (SequenceFeature.java:16) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| end_platform, end_spike, end_island, and end_gateway are Feature kinds that vanilla only ever places in the End dimension. | All four are registered kinds (`Feature.END_PLATFORM`, `END_SPIKE`, `END_ISLAND`, `END_GATEWAY`, Feature.java:108-111). Their configured features (data/minecraft/worldgen/configured_feature/end_platform.json, end_spike.json, end_island.json, end_gateway_return.json, end_gateway_delayed.json) reach biome decoration only through placed features referenced by End biomes: `minecraft:end_platform` and `minecraft:end_spike` by data/minecraft/worldgen/biome/the_end.json, `minecraft:end_gateway_return` by end_highlands.json, `minecraft:end_island_decorated` by small_end_islands.json — no Overworld or Nether biome references any of them. The remaining uses are runtime End-only paths (the dragon fight's obsidian platform, spikes and delayed gateway), still End dimension only | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
