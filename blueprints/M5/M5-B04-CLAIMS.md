# M5-B04 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| In vanilla's chunk generation pipeline, carvers (caves and canyons) run in a separate, later ChunkStatus stage, strictly after the Surface stage. | `ChunkPyramid.GENERATION_PYRAMID` (ChunkPyramid.java:17-34) declares the steps in order NOISE, SURFACE, CARVERS, FEATURES; the CARVERS step (:30) runs `ChunkStatusTasks::generateCarvers` and the FEATURES step (:32-34) adds a requirement on CARVERS | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The classic minecraft:ore feature places every ore type including copper and iron via its own ore_iron/ore_iron_small/ore_copper_small/ore_copper_large configured features, and the noise-router-driven vein system places copper and iron ore veins as an additional, separate mechanism rather than a replacement for that feature. | The pinned data pack still ships `minecraft:ore`-typed configured features for both ores: `data/minecraft/worldgen/configured_feature/ore_iron.json`, `ore_iron_small.json`, `ore_copper_small.json`, `ore_copper_large.json` (each `"type": "minecraft:ore"`; `ore_iron.json` targets `minecraft:iron_ore` or `minecraft:deepslate_iron_ore`). Correction: the OreVeinifier is an ADDITIONAL, separate mechanism that paints large copper/iron veins during the Noise stage — it does not replace the classic ore feature for copper and iron, which are placed by BOTH mechanisms | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| QuartPos.toBlock converts a quart-position size to blocks by left-shifting by 2 (multiplying by 4): cell_width = dims.size_horizontal << 2, cell_height = dims.size_vertical << 2. | `QuartPos.toBlock` (QuartPos.java:20-22) returns `quart << 2`; `NoiseSettings.getCellHeight`/`getCellWidth` (NoiseSettings.java:50-56) return `QuartPos.toBlock(noiseSizeVertical())` and `QuartPos.toBlock(noiseSizeHorizontal())` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A chunk is 16 blocks wide on X/Z, so the number of noise cells per chunk axis is 16 / cell_width (2, 4, or rarely more, depending on the dimension's noise settings). | `NoiseChunk.forChunk` (NoiseChunk.java:92) computes `cellCountXZ = 16 / noiseSettings.getCellWidth()`; `doFill` (NoiseBasedChunkGenerator.java:401-402) recomputes `cellCountX = cellCountZ = 16 / cellWidth`. Note on the parenthetical: `size_horizontal` is codec-bounded to 1..4 (NoiseSettings.java:17), so cell_width is 4, 8, 12 or 16 and the per-axis cell count is only ever 4, 2 or 1 — never more than 4. Pinned data: overworld/amplified/large_biomes/nether/caves use size_horizontal 1 (count 4), end/floating_islands use 2 (count 2) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The number of noise cells spanning a chunk column's full height is cell_count_y = dims.height / cell_height. | `NoiseChunk` constructor (NoiseChunk.java:110) sets `cellCountY = Mth.floorDiv(noiseSettings.height(), cellHeight)`; the same expression appears at NoiseBasedChunkGenerator.java:360 and :202. `height` is codec-guarded to a multiple of 16 (NoiseSettings.java:34-36), so the floor division is exact | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's own doFill walk order visits noise cells from top to bottom (descending Y), and within each cell also visits local Y from top to bottom, before local X ascending then local Z ascending. | `NoiseBasedChunkGenerator.doFill` (NoiseBasedChunkGenerator.java:404-452): `for cellXIndex ascending` then `for cellZIndex ascending` then `for cellYIndex = cellCountY-1 down to 0` then `for yInCell = cellHeight-1 down to 0` then `for xInCell ascending` then `for zInCell ascending`. Note for the blueprint's own driver pseudocode (Context §D): vanilla calls `initializeForFirstCellX()` ONCE before the cell-X loop (:397) and then `advanceCellX(cellXIndex)` for EVERY cell X including index 0 (:405) — not "initialize for cell 0, advance otherwise" | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| selectCellYZ marks the start of a new cell, and update_for_y/update_for_x/update_for_z are each called once per block-offset change along their axis, updating every interpolator node sharing that cell's coarse grid at once. | `NoiseChunk.selectCellYZ` (NoiseChunk.java:295-311) calls `selectCellYZ` on every tracked interpolator, sets the cell's start block Y/Z and eagerly fills every `CacheAllInCell`; `updateForY`/`updateForX`/`updateForZ` (:313-336) each set the in-cell offset and loop over `this.interpolators` applying the shared fraction. `doFill` calls `updateForY` once per yInCell (:424), `updateForX` once per xInCell (:430), `updateForZ` once per zInCell (:436) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The terrain material rule fills a block as default_block whenever the sampled density at that position is greater than 0.0. | Mechanism: `Aquifer.NoiseBasedAquifer.computeSubstance` (Aquifer.java:152-156) returns `null` immediately when `density > 0.0`; `MaterialRuleList.calculate` (MaterialRuleList.java:13-21) then falls through, and `doFill` (NoiseBasedChunkGenerator.java:437-440) substitutes `settings.defaultBlock()` for a null result. Note: at a solid position the OreVeinifier filler runs first and may substitute a vein block instead of default_block | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ore-vein material rule (OreVeinifier) is evaluated at every position the aquifer filler left unresolved -- both ordinary solid positions (density > 0) and aquifer-barrier stays-solid positions (density <= 0) -- and only ever replaces default_block -> it never turns a position into air or fluid. | The "only replaces default_block" half is right (OreVeinifier.create returns only `veinType.ore`, `rawOreBlock`, `filler` or null — OreVeinifier.java:30-60). The gating half is WRONG: `MaterialRuleList` (MaterialRuleList.java:13-21) runs the aquifer filler FIRST and the OreVeinifier only when the aquifer returned null (NoiseChunk.java:155-166). The aquifer returns null both for `density > 0.0` (Aquifer.java:153-156) AND for the barrier "stays solid" branch at `density <= 0.0` (Aquifer.java:258-261, :267-270, :276-279) — so the OreVeinifier also runs at aquifer-barrier positions whose density is not positive | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For positions where density <= 0.0, the aquifer algorithm alone decides whether the block becomes air, water, or lava. | `NoiseChunk` constructor (NoiseChunk.java:161) makes `aquifer.computeSubstance(context, fullNoiseValue.compute(context))` the first and only fluid-deciding rule; no other filler places air or fluid. Note: the aquifer has a fourth outcome — returning null ("stays solid", Aquifer.java:258-279), after which the OreVeinifier and then `settings.defaultBlock()` decide the block | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The final_density noise-router field already incorporates any BlendDensity{}, BlendAlpha{}, and BlendOffset{} contributions the compiled density-function graph specifies, but not Beardifier{} -- the fill driver adds its own Beardifier term (currently 0.0) to the sampled final_density value and wraps the sum in a cache_all_in_cell node. | WRONG for the Beardifier. `NoiseChunk` constructor (NoiseChunk.java:156-160) builds the fill density as `cacheAllInCell(add(wrappedRouter.finalDensity(), BeardifierMarker.INSTANCE)).mapAll(this::wrap)` — the Beardifier term is added OUTSIDE `final_density`, by the fill driver, as an extra summation. Confirming that from the data side: the pinned `data/minecraft/worldgen/noise_settings/overworld.json` `noise_router.final_density` contains one `minecraft:blend_density` node and zero `minecraft:beardifier` and zero `minecraft:cache_all_in_cell` nodes; `beardifier` appears nowhere in NoiseRouterData.java. Blend alpha/offset do live inside the router graph (NoiseRouterData.java:166, :316, :482). Correction: this blueprint's driver must add its own Beardifier term (currently 0.0) to the sampled `final_density` and wrap the sum in cache-all-in-cell, exactly as NoiseChunk does | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's fillFromNoise wraps the entire final-density sum in a cache_all_in_cell cache node. | `NoiseChunk` constructor (NoiseChunk.java:156-160): `DensityFunctions.cacheAllInCell(DensityFunctions.add(wrappedRouter.finalDensity(), BeardifierMarker.INSTANCE))`, stored as `fullNoiseDensity` and consumed by `getInterpolatedDensity` (:185-187) and the aquifer filler (:161). Naming note: 26.2 has no `fillFromNoise` method — the driver is `NoiseBasedChunkGenerator.doFill` (:382-452) and the cache wrapping happens in the NoiseChunk constructor | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The aquifer grid's cells are 16x12x16 blocks: X_SPACING=16, Y_SPACING=12, Z_SPACING=16. | `Aquifer.NoiseBasedAquifer` constants (Aquifer.java:68-70): `X_SPACING = 16`, `Y_SPACING = 12`, `Z_SPACING = 16`; the grid helpers use those spacings directly (`gridX`/`gridZ` shift by 4, `gridY` floor-divides by 12 — :370-392) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A block position's aquifer grid cell is computed by floor division (not truncation) of its coordinates by the grid spacings: grid_x = floor_div(block_x,16), grid_y = floor_div(block_y,12), grid_z = floor_div(block_z,16). | `Aquifer.NoiseBasedAquifer.gridX`/`gridY`/`gridZ` (Aquifer.java:370-388): `blockCoord >> 4` (arithmetic shift = floor division by 16), `Math.floorDiv(blockCoord, 12)`, `blockCoord >> 4`. Important note the blueprint must fold in: the query is NOT anchored on the block's own cell — `computeSubstance` (:172-174) anchors at `gridX(posX + SAMPLE_OFFSET_X)`, `gridY(posY + SAMPLE_OFFSET_Y)`, `gridZ(posZ + SAMPLE_OFFSET_Z)` with SAMPLE_OFFSET_X = -5, SAMPLE_OFFSET_Y = 1, SAMPLE_OFFSET_Z = -5 (:75-77) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each aquifer grid cell's jittered location is derived from exactly 3 RNG draws, always next_int_bounded, always in this order: X jitter bounded by 10 (range 0..=9), then Y jitter bounded by 9 (range 0..=8), then Z jitter bounded by 10 (range 0..=9). | Aquifer.java:196-199 — `RandomSource random = this.positionalRandomFactory.at(spacedGridX, spacedGridY, spacedGridZ); BlockPos.asLong(fromGridX(spacedGridX, random.nextInt(10)), fromGridY(spacedGridY, random.nextInt(9)), fromGridZ(spacedGridZ, random.nextInt(10)))` — Java evaluates the arguments left to right, so the draw order is bound 10, bound 9, bound 10. The bounds match the named constants X_RANGE = 10, Y_RANGE = 9, Z_RANGE = 10 (:62-64) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The jittered aquifer location's coordinates are loc_x = grid_x*16 + jx, loc_y = grid_y*12 + jy, loc_z = grid_z*16 + jz. | `fromGridX` and `fromGridZ` (Aquifer.java:374-376, :390-392) return `(gridCoord << 4) + blockOffset`; `fromGridY` (:382-384) returns `gridCoord * 12 + blockOffset` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Computing an aquifer location's FluidStatus after the 3 jitter draws consumes no further RNG draws. | `computeFluid` (Aquifer.java:406-442), `computeSurfaceLevel` (:448-483), `computeRandomizedFluidSurfaceLevel` (:485-499) and `computeFluidType` (:501-516) use only density-function samples, `Mth` arithmetic and the global fluid picker — no RandomSource is touched. Note: the status is computed LAZILY and separately from the location, via `getAquiferStatus` (:394-404), only for a rank that is actually consulted | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Aquifer grid cells straddling a chunk boundary are resolved correctly because both neighboring chunks independently recompute the identical grid-cell sample. | The location is a pure function of the grid coordinates: `positionalRandomFactory.at(spacedGridX, spacedGridY, spacedGridZ)` (Aquifer.java:196) off the `(seed, dimension)`-scoped `RandomState.aquiferRandom` (RandomState.java:38); `aquiferLocationCache`/`aquiferCache` (:134-136) are per-NoiseBasedAquifer-instance, i.e. per chunk. The status likewise depends only on position — `noiseChunk.preliminarySurfaceLevel` (NoiseChunk.java:219-229) is a quart-quantized single-point sample cached per NoiseChunk | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's own nearest-neighbor aquifer search scans the 2x3x2 = 12 neighboring grid cells, a narrower scan than a full 3x3x3 = 27 neighborhood, made possible by the 10/9/10-block jitter bounds. | `computeSubstance` (Aquifer.java:184-186): `for (x1 = 0; x1 <= 1)`, `for (y1 = -1; y1 <= 1)`, `for (z1 = 0; z1 <= 1)` — 2 x 3 x 2 = 12 cells, matching the named bounds MIN_CELL_SAMPLE_X/Y/Z = 0, -1, 0 and MAX_CELL_SAMPLE_X/Y/Z = 1, 1, 1 (:78-83). The asymmetric X/Z window works because the anchor is pre-offset by SAMPLE_OFFSET_X/Z = -5 (:75-77, :172-174) against the X_RANGE/Z_RANGE = 10 jitter, and MAX_REASONABLE_DISTANCE_TO_AQUIFER_CENTER = 11 (:73) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ties among aquifer grid locations at equal squared distance in the nearest-neighbor search are broken by ascending (dx,dy,dz) scan order over the neighboring-cell scan, so the LAST-encountered candidate at a tied distance displaces the incumbent and takes the earlier rank. | The scan order is right (x1, then y1, then z1 ascending — Aquifer.java:184-186), but the tie rule is inverted. The insertion tests are `if (distanceSqr1 >= newDistance) ... else if (distanceSqr2 >= newDistance) ... else if (distanceSqr3 >= newDistance) ... else if (distanceSqr4 >= newDistance)` (:207-231) — all `>=`, so a LATER candidate at an exactly equal squared distance DISPLACES the earlier one and takes the better rank, pushing the earlier candidate down. Correction: the LAST-encountered candidate at a tied distance keeps the earlier rank | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The aquifer lava cutoff Y is min(-54, sea_level). | `NoiseBasedChunkGenerator.createFluidPicker` (NoiseBasedChunkGenerator.java:68-79): `return y < Math.min(-54, seaLevel) ? lavaStatus : seaStatus` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The aquifer similarity formula is similarity(dist_sqr_a, dist_sqr_b) = 1.0 - (dist_sqr_b - dist_sqr_a) / 25.0, where 25.0 is a named constant divisor. | `Aquifer.NoiseBasedAquifer.similarity` (Aquifer.java:302-305): `return 1.0 - (distanceSqr2 - distanceSqr1) / 25.0;`. The distances are ints, so the subtraction is exact integer arithmetic and the division promotes to double. Naming note: `double threshold = 25.0` on :303 is a documentation-only local — the divisor is the inline literal | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The barrier/pressure gate checks exactly the three location pairs (1st/2nd), (1st/3rd), (2nd/3rd) among the 4 nearest aquifer locations; the 4th-nearest location is used only in the search itself, never in these pairwise checks. | Exactly three barrier checks, in that pair order: (1,2) at Aquifer.java:256-261, (1,3) at :263-271, (2,3) at :273-280 — the 4th-nearest appears in none of them. Note: `distanceSqr4`/`closestIndex4` are not purely search bookkeeping — they feed the `shouldScheduleFluidUpdate` decision at :286-288 (out of this blueprint's scope, since it only marks positions for post-processing fluid ticks) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The whole barrier check is gated on similarity12 (the 1st/2nd pair) being positive -- when it is not, the closest location's own FluidStatus resolves the block immediately with no pressure evaluated; otherwise each of the three pairs (1st/2nd always, 1st/3rd and 2nd/3rd only when their own similarity is also positive) computes a similarity-weighted barrier (barrier12 = sim12 * pressure, barrier13 = sim12 * sim13 * pressure, barrier23 = sim12 * sim23 * pressure), and if density plus any checked barrier is greater than 0.0 the position stays solid (left unresolved for the ore-vein filler), otherwise the closest location's own FluidStatus wins. | The stays-solid outcome and the fallback are right in shape (return null at Aquifer.java:260, :269, :278; otherwise `closestStatus1.at(posY)` at :238, :248, :293), but the gating and the barrier value are not. Corrections: (a) the whole barrier machinery is gated on `similarity12 > 0.0` — when `similarity(distanceSqr1, distanceSqr2) <= 0.0` the closest status's fluid is returned immediately with no pressure evaluated (:240-248); (b) the pairs (1,3) and (2,3) are additionally gated on their own `similarity13 > 0.0` / `similarity23 > 0.0` and are reached only inside the sim12-positive branch; (c) the checked barrier is a similarity-weighted pressure, not the raw pressure: `barrier12 = similarity12 * calculatePressure(...)` (:257), `barrier13 = similarity12 * similarity13 * calculatePressure(...)` (:266), `barrier23 = similarity12 * similarity23 * calculatePressure(...)` (:275) — note the similarity12 factor appears in all three | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The aquifer pressure formula's four named constants (2.5, 1.5, 10.0, 3.0) are DIVISORS chosen by a second, inner sign test on centerPoint, not multiplicative biases: strictly above the average fluid level, centerPoint = distanceFromBarrierEdgeTowardsMiddle and gradient = centerPoint / 1.5 when centerPoint > 0.0 else centerPoint / 2.5; at or below it, centerPoint = 3.0 + distanceFromBarrierEdgeTowardsMiddle and gradient = centerPoint / 3.0 when centerPoint > 0.0 else centerPoint / 10.0. | The four numbers exist and group as claimed, but they are DIVISORS selected by a second, inner sign test, not multiplicative biases, and the outer branch is strict. `calculatePressure` (Aquifer.java:325-347): the above-average branch is taken when `howFarAboveAverageFluidPoint > 0.0` (strictly greater, not "at or above") and uses `centerPoint = 0.0 + distanceFromBarrierEdgeTowardsMiddle`, then `gradient = centerPoint > 0.0 ? centerPoint / 1.5 : centerPoint / 2.5`; the below branch uses `centerPoint = 3.0 + distanceFromBarrierEdgeTowardsMiddle`, then `gradient = centerPoint > 0.0 ? centerPoint / 3.0 : centerPoint / 10.0`. The declared names are furthestRocksFromTopBias = 2.5, furthestHolesFromTopBias = 1.5, bottomBias = 3.0, furthestRocksFromBottomBias = 10.0, furthestHolesFromBottomBias = 3.0, topBias = 0.0 (:325-330) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The aquifer pressure formula computes avg_level = (a.surface_level + b.surface_level)/2, but delta = pos.y + 0.5 - avg_level (a +0.5 block-centre offset) and the selecting test is strictly delta > 0.0; two guard clauses precede this arithmetic entirely -- a lava/water pair at pos.y returns the constant 2.0, and equal fluid levels return 0.0. | `calculatePressure` (Aquifer.java:322-323): `averageFluidY = 0.5 * (statusClosest1.fluidLevel + statusClosest2.fluidLevel)` matches, but `howFarAboveAverageFluidPoint = posY + 0.5 - averageFluidY` — there is a +0.5 block-centre offset the claim omits, and the selecting test is the strict `howFarAboveAverageFluidPoint > 0.0` (:333). Two guard clauses precede all of it: if one status resolves to lava and the other to water at posY the function returns the constant 2.0 (:316, :366), and if the two fluid levels are equal it returns 0.0 (:317-320) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The aquifer pressure formula computes level_gap = abs(a.surface_level - b.surface_level), but there is no clamped_map: base_value = level_gap / 2.0, distance_from_barrier_edge = base_value - abs(delta), center_point = bias + distance_from_barrier_edge (bias 0.0 above the average fluid level, 3.0 below it), and gradient = center_point / divisor, the divisor chosen by the sign of center_point (1.5 or 2.5 above, 3.0 or 10.0 below). | `fluidYDiff = Math.abs(statusClosest1.fluidLevel - statusClosest2.fluidLevel)` (Aquifer.java:317) is correct, but there is no clamped_map anywhere in `calculatePressure`. The actual combination (:324, :331-347) is: `baseValue = fluidYDiff / 2.0`; `distanceFromBarrierEdgeTowardsMiddle = baseValue - Math.abs(howFarAboveAverageFluidPoint)`; `centerPoint = bias + distanceFromBarrierEdgeTowardsMiddle` with bias 0.0 above the average level and 3.0 below it; `gradient = centerPoint / divisor` with the divisor chosen by the sign of centerPoint (1.5 or 2.5 above, 3.0 or 10.0 below) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The final aquifer pressure value is 2.0 * (barrier_sample + gradient), not sim * falloff * barrier_sample -- the barrier noise is added to the gradient and the sum doubled, sampled only when gradient lies inside [-2.0, 2.0] (otherwise the sample is 0.0) and memoized across the three pair checks; the similarity weighting (sim12, sim12*sim13, sim12*sim23) is applied by the caller, not inside the pressure function. | `calculatePressure` returns `2.0 * (noiseValue + gradient)` (Aquifer.java:364) — the barrier noise is ADDED to the gradient and the sum is doubled, never multiplied by it; and the noise is only sampled when the gradient is inside [-2.0, 2.0], otherwise `noiseValue = 0.0` (:351-362). The sample is `this.barrierNoise.compute(context)` at the query position (:354), memoized across the three pair checks of one position through the shared `MutableDouble` (:255, :352-359). The similarity weighting is applied by the CALLER, not inside the pressure function (:257, :266, :275) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In the aquifer pressure formula, a location's surface_level(pos.y) is its own quantized/global fluid level for a Water or Lava FluidStatus, and a sentinel value far below pos.y for a None (dry) FluidStatus, so delta and level_gap still compute without special-casing. | `calculatePressure` reads `statusClosest1.fluidLevel` / `statusClosest2.fluidLevel` as plain ints with no dry special-case (Aquifer.java:317, :322), and a dry location is represented by `fluidSurfaceLevel = DimensionType.WAY_BELOW_MIN_Y` (Aquifer.java:479; `WAY_BELOW_MIN_Y = MIN_Y << 4` — DimensionType.java:53). Two shape notes: vanilla's `FluidStatus` has no None kind — dryness IS the sentinel level, and `fluidType` stays whatever the global picker gave (Aquifer.java:52-59, :441); and `calculatePressure` does special-case a lava/water pair (return 2.0) and an equal-level pair (return 0.0) before reaching this arithmetic (:316-320, :366) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's own near-surface aquifer test samples preliminarySurfaceLevel at the location's own column plus 12 nearby chunk-corner offsets (13 total, each sample adjusted by +8), and its unconditional early return fires only at the centre sample when loc_y - 12 is greater than that adjusted level -- not an at-or-above-the-minimum-of-all-samples test. | The sampling shape is right — `SURFACE_SAMPLING_OFFSETS_IN_CHUNKS` (Aquifer.java:102-104) is `{0,0}` plus 12 further offsets, each converted with `SectionPos.sectionToBlockCoord` (i.e. multiplied by 16) and fed to `noiseChunk.preliminarySurfaceLevel` (:413-416). The decision rule is not. `computeFluid` (:406-438): every sampled level is first adjusted by `adjustSurfaceLevel` = `preliminarySurfaceLevel + 8` (:444-446); at the centre offset it returns the global fluid immediately when `y - 12 > adjustedSurfaceLevel` (:409-421); for any offset with `y + 12 > adjustedSurfaceLevel` it returns the global fluid AT that adjusted surface level if that is not air (:423-434). The minimum of the samples (`lowestPreliminarySurface`, :437) is NOT the at-or-above test — it is passed on to `computeSurfaceLevel` (:440) for the floodedness factor and to cap the randomized level | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| FluidStatus determination first tests, at the centre sample only, whether loc_y - 12 exceeds the adjusted preliminary surface level; if so, it inherits the global fluid picker's own level unconditionally. A second, offset-driven return path fires whenever loc_y + 12 exceeds any of the 13 samples' own adjusted level and the global fluid at that adjusted level is non-air, returning THAT status rather than the location's own global fluid. | `computeFluid` (Aquifer.java:406-421) does run the surface test first, but the threshold is not "at or above": the unconditional early return is `bottomOfAquiferCell > adjustedSurfaceLevel` at the centre offset, i.e. `y - 12 > preliminarySurfaceLevel(x,z) + 8` (:410, :418-421, :444-446). The second, offset-driven return path is conditional on the global fluid at the adjusted surface level being non-air (:423-434), and returns THAT status rather than the location's own global fluid | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When not near the surface, floodedness is sampled from the noise router's fluid_level_floodedness_noise field; a fully-flooded result inherits the global fluid picker, otherwise the block is partially-flooded or dry. | `computeSurfaceLevel` (Aquifer.java:448-483): `floodednessNoiseValue = Mth.clamp(this.fluidLevelFloodednessNoise.compute(context), -1.0, 1.0)` (:466), then `fullyFloodidness > 0.0 -> globalFluid.fluidLevel`, `partiallyFloodedness > 0.0 -> computeRandomizedFluidSurfaceLevel(...)`, else `DimensionType.WAY_BELOW_MIN_Y` (dry) (:473-482). For the blueprint's flagged moderate-confidence thresholds, the real ones are: `floodednessFactor = surfaceAtCenterIsUnderGlobalFluidLevel ? Mth.clampedMap(lowestPreliminarySurface + 8 - y, 0.0, 64.0, 1.0, 0.0) : 0.0`; `fullyFloodedThreshold = Mth.map(floodednessFactor, 1.0, 0.0, -0.3, 0.8)`; `partiallyFloodedThreshold = Mth.map(floodednessFactor, 1.0, 0.0, -0.8, 0.4)`; the tested quantities are the noise value MINUS each threshold (:463-470). Deep-dark regions force both to -1.0, i.e. always dry (:459-461) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In the partially-flooded/dry branch, the fluid-level cell indices are cell_x=floor_div(loc_x,16), cell_y=floor_div(loc_y,40), cell_z=floor_div(loc_z,16), but the spread noise is sampled at those RAW cell indices themselves, never at a cell_x*16+8/cell_z*16+8 centre position; only a cell_middle_y = cell_y*40+20 base level exists. | The cell indices are right (`computeRandomizedFluidSurfaceLevel`, Aquifer.java:488-490: `Math.floorDiv(x,16)`, `Math.floorDiv(y,40)`, `Math.floorDiv(z,16)`), but the cell centre is not a sampling point at all. The spread noise is sampled at the RAW CELL INDICES: `fluidLevelSpreadNoise.compute(new DensityFunction.SinglePointContext(fluidLevelCellX, fluidLevelCellY, fluidLevelCellZ))` (:493-495). Only `fluidCellMiddleY = fluidLevelCellY * 40 + 20` exists (:491), and it is the base LEVEL the quantized spread is added to (:497) — the X/Z halves `cell_x*16+8` and `cell_z*16+8` appear nowhere. The lava-type cell is a different, 64x40x64 quantization, also sampled at raw indices: `floorDiv(x,64), floorDiv(y,40), floorDiv(z,64)` (`computeFluidType`, :506-509) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The fluid spread level is computed as raw_level = spread*10.0, then quantized to steps of 3 via level = floor(raw_level/3.0)*3.0 (FLOOR, not round), with a maximum spread of +/-10; the quantized value is an offset added to cell_middle_y, and the result is capped by the minimum of the near-surface scan's own lowest_preliminary_surface. | `computeRandomizedFluidSurfaceLevel` (Aquifer.java:493-498): `fluidLevelSpread = noise * 10.0` matches (maxSpread = 10 is a named local, :492), but the quantization uses FLOOR, not round — `Mth.quantize(value, resolution)` is `floor(value / resolution) * resolution` (Mth.java:729-731). Two further omissions: the quantized value is an OFFSET added to the cell's middle Y (`targetFluidSurfaceLevel = fluidCellMiddleY + fluidLevelSpreadQuantized`, :497), and the returned level is capped by `Math.min(lowestPreliminarySurface, targetFluidSurfaceLevel)` (:498) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The fluid kind gate has three conjuncts, not one -- level <= -10 AND level is not the dry sentinel AND the fallback global-fluid kind is not already lava -- and only then does abs(lava-noise sample) > 0.3 select Lava; the gate applies to every computed FluidStatus, not only the partially-flooded branch, and the non-lava fallback is the global fluid picker's own kind, not unconditionally Water. | `computeFluidType` (Aquifer.java:501-516) has three conjuncts, not one, and is not restricted to the partially-flooded branch — it is applied to every computed status (:441). The real gate is `fluidSurfaceLevel <= -10 && fluidSurfaceLevel != DimensionType.WAY_BELOW_MIN_Y && globalFluid.fluidType != Blocks.LAVA.defaultBlockState()`, and only then is `Math.abs(lavaNoiseValue) > 0.3` consulted (:503-512). The fallback is not Water but `globalFluid.fluidType` — the picker's own type, which is the settings' `default_fluid` above the lava cutoff and lava below it (NoiseBasedChunkGenerator.java:69-77) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The global fluid picker resolves as exactly two outcomes: if y < min(-54, sea_level) the fluid is Lava at the FIXED level -54 (not at min(-54,sea_level)); otherwise it is the dimension's own default fluid at sea_level -- there is no third "no fluid" outcome, since air above sea level comes later from the level-vs-query-Y test, not from the picker itself. | `NoiseBasedChunkGenerator.createFluidPicker` (NoiseBasedChunkGenerator.java:68-79) has only TWO outcomes: `y < Math.min(-54, seaLevel) ? lavaStatus : seaStatus`, with `lavaStatus = new FluidStatus(-54, LAVA)` and `seaStatus = new FluidStatus(seaLevel, settings.defaultFluid())`. Corrections: (a) the lava status's level is the fixed constant -54, not `min(-54, sea_level)` — the two differ whenever sea_level is below -54, as in the pinned floating_islands preset (sea_level -64); (b) there is no third "no fluid" outcome — above sea level the picker still returns the sea status, and the air result comes later from `FluidStatus.at(y)`. This matters because the aquifer consumes the picker's LEVEL directly (`globalFluid.fluidLevel`, Aquifer.java:475) as well as its `at(y)` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A FluidStatus only yields fluid at a queried Y if that Y is strictly below the status's own level; otherwise (or if the status's kind is None) the block is air. | `Aquifer.FluidStatus.at` (Aquifer.java:56-58): `return blockY < this.fluidLevel ? this.fluidType : Blocks.AIR.defaultBlockState();`. The "kind is None" case is expressed in vanilla as the WAY_BELOW_MIN_Y sentinel level (:479), which makes the same test yield air for every real Y | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When aquifers are disabled for a dimension (every dimension except the overworld family), the stays-solid barrier branch never fires -> the block is resolved purely via the global fluid picker, never default_block. | `Aquifer.createDisabled` (Aquifer.java:30-42): `density > 0.0 ? null : fluidRule.computeFluid(blockX, blockY, blockZ).at(blockY)` — no grid, no pressure, no stays-solid path for non-positive density; selected by `NoiseChunk` when `!settings.isAquifersEnabled()` (NoiseChunk.java:145-146). Pinned data confirm the dimension split: `aquifers_enabled` is true only in `noise_settings/overworld.json`, `amplified.json` and `large_biomes.json`, and false in `caves.json`, `nether.json`, `end.json`, `floating_islands.json` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The copper ore vein occupies Y range [0, 50] and consists of minecraft:copper_ore ore, minecraft:raw_copper_block raw-ore block, with minecraft:granite filler. | `OreVeinifier.VeinType.COPPER` (OreVeinifier.java:64): `COPPER(Blocks.COPPER_ORE.defaultBlockState(), Blocks.RAW_COPPER_BLOCK.defaultBlockState(), Blocks.GRANITE.defaultBlockState(), 0, 50)` with the constructor parameter order ore, rawOreBlock, filler, minY, maxY (:73-79) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The iron ore vein occupies Y range [-60, -8] and consists of minecraft:deepslate_iron_ore ore, minecraft:raw_iron_block raw-ore block, with minecraft:tuff filler. | `OreVeinifier.VeinType.IRON` (OreVeinifier.java:65): `IRON(Blocks.DEEPSLATE_IRON_ORE.defaultBlockState(), Blocks.RAW_IRON_BLOCK.defaultBlockState(), Blocks.TUFF.defaultBlockState(), -60, -8)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ore-vein material rule is evaluated at every position the aquifer filler left unresolved, both solid-by-density positions (density > 0.0) and aquifer-barrier stays-solid positions (density <= 0.0), not solely at density > 0.0 positions. | Same defect as the earlier OreVeinifier claim: `MaterialRuleList.calculate` (MaterialRuleList.java:13-21) runs the ore-vein filler whenever the aquifer filler returned null, and the aquifer returns null both for `density > 0.0` (Aquifer.java:153-156) and for its barrier stays-solid branch at `density <= 0.0` (Aquifer.java:258-261, :267-270, :276-279). Correction: the ore-vein rule is evaluated at every position the aquifer left unresolved — solid-by-density positions AND aquifer-barrier positions | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The noise router's vein_toggle field is already Y-limited to [-60, 50] by the compiled graph itself. | `NoiseRouterData.overworld` (NoiseRouterData.java:354-356): `veinMinY` and `veinMaxY` are the min/max over `OreVeinifier.VeinType.values()` (-60 and 50), and `veinToggle = yLimitedInterpolatable(y, noise(ORE_VEININESS, 1.5, 1.5), veinMinY, veinMaxY, 0)`; `yLimitedInterpolatable` (:515-521) is `interpolated(rangeChoice(y, minYInclusive, maxYInclusive + 1, whenInRange, constant(whenOutOfRange)))`, i.e. the in-range window is y in [-60, 51) and the out-of-range value is 0.0. Note the out-of-range 0.0 is not positive, so a position outside the window selects the IRON vein type before the per-type Y-band test rejects it | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vein type is copper when the sampled vein_toggle value is greater than 0.0, and iron otherwise. | OreVeinifier.java:33 — `VeinType veinType = oreVeininessNoiseValue > 0.0 ? VeinType.COPPER : VeinType.IRON;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In the ore-vein rule, dist_from_top = vein_type.max_y - pos.y and dist_from_bottom = pos.y - vein_type.min_y are the distances from the current Y to the vein type's own upper and lower Y bounds. | OreVeinifier.java:35-36 — `int distanceFromTop = veinType.maxY - posY; int distanceFromBottom = posY - veinType.minY;` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A position outside its own vein type's Y band (dist_from_bottom < 0 or dist_from_top < 0) yields no vein, with zero RNG draws. | OreVeinifier.java:37 gates the whole body on `distanceFromBottom >= 0 && distanceFromTop >= 0`; the else branch (:57-59) returns `defaultState` (null in non-debug builds, :29) without ever constructing a RandomSource | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The edge round-off term is clamped_map(dist_from_edge, 0.0, 20.0, -0.2, 0.0), where dist_from_edge is the minimum of distance-from-top and distance-from-bottom of the vein's own Y band. | OreVeinifier.java:38-39 — `int distanceFromEdge = Math.min(distanceFromTop, distanceFromBottom); double edgeRoundoff = Mth.clampedMap(distanceFromEdge, 0.0, 20.0, -0.2, 0.0);`, matching the named constants EDGE_ROUNDOFF_BEGIN = 20 and MAX_EDGE_ROUNDOFF = 0.2 (:11-12). All five arguments are exact doubles here | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A position where veininess_ridged (the absolute value of vein_toggle) plus edge_roundoff is less than 0.4 yields no vein, consuming zero RNG draws. | OreVeinifier.java:34, :40-41 — `veininessRidged = Math.abs(oreVeininessNoiseValue)` and `if (veininessRidged + edgeRoundoff < 0.4F) return defaultState;`, before the RandomSource is created on :43. Bit-parity note for the implementer: the threshold is the FLOAT literal VEININESS_THRESHOLD = 0.4F (:10) widened to double for this comparison, i.e. 0.4000000059604645, not the double 0.4 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ore-vein rule's first RNG draw is next_float() > 0.7, which rejects (yields no vein) when true. | OreVeinifier.java:43-45 — `RandomSource positionalRandom = oreVeinsPositionalRandomFactory.at(context.blockX(), posY, context.blockZ()); if (positionalRandom.nextFloat() > 0.7F) return defaultState;`, matching VEIN_SOLIDNESS = 0.7F (:13). Float-vs-float comparison, no widening | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| After the first ore-vein draw passes, the rule checks router.vein_ridged >= 0.0 (no RNG draw), rejecting (yields no vein) when true. | OreVeinifier.java:46-47 — `else if (veinRidged.compute(context) >= 0.0) return defaultState;`, a plain double comparison with no RandomSource use | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ore-vein richness value is richness = clamped_map(veininess_ridged, 0.4, 0.6, 0.1, 0.3). | OreVeinifier.java:49 — `double richness = Mth.clampedMap(veininessRidged, 0.4F, 0.6F, 0.1F, 0.3F);`, matching VEININESS_THRESHOLD 0.4F, MAX_RICHNESS_THRESHOLD 0.6F, MIN_RICHNESS 0.1F, MAX_RICHNESS 0.3F (:10, :14-16). Bit-parity note: the first argument is a double, so Java selects `Mth.clampedMap(double,double,double,double,double)` (Mth.java:657-659) and each float literal is widened — the effective constants are 0.4000000059604645, 0.6000000238418579, 0.10000000149011612 and 0.30000001192092896, not the decimal doubles the claim writes | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ore-vein rule's second RNG draw is next_float() < richness combined with router.vein_gap > -0.3; when both hold the position yields ore or raw ore, otherwise it yields the vein's filler block. | OreVeinifier.java:50-54 — `if (positionalRandom.nextFloat() < richness && veinGap.compute(context) > -0.3F) { ... } else { return veinType.filler; }`, with SKIP_ORE_IF_GAP_NOISE_IS_BELOW = -0.3F (:18). Java's `&&` evaluates the nextFloat draw first and unconditionally, so the draw happens even when the gap test would fail. Bit-parity note: -0.3F widened to double is -0.30000001192092896 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When the second ore-vein draw's combined condition holds, the rule's third RNG draw is next_float() < 0.02, yielding the vein's raw-ore block when true and its ordinary ore block otherwise. | OreVeinifier.java:51 — `return positionalRandom.nextFloat() < 0.02F ? veinType.rawOreBlock : veinType.ore;`, matching CHANCE_OF_RAW_ORE_BLOCK = 0.02F (:17). Float-vs-float comparison | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ore-vein evaluation consumes exactly 0, 1, 2, or 3 next_float() calls depending on which branch is taken, never a fixed count. | Walking OreVeinifier.java:30-60: 0 draws on the Y-band reject (:37, :57-59) and the veininess-threshold reject (:40-41); 1 draw on the solidness reject (:44) and on the vein-ridged reject (:46, since the RandomSource is already advanced once); 2 draws on the filler branch (:50, :53); 3 draws on the ore or raw-ore branch (:50, :51) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The real Minecraft 26.2 surface-rule condition kinds are exactly 11: Biome, NoiseThreshold, VerticalGradient, YAbove, Water, Temperature, Steep, Hole, AbovePreliminarySurface, Not, and StoneDepth -> there is no and/or condition kind, and above_preliminary_surface does exist. | `SurfaceRules.ConditionSource.bootstrap` (SurfaceRules.java:279-291) registers exactly biome, noise_threshold, vertical_gradient, y_above, water, temperature, steep, not, hole, above_preliminary_surface, stone_depth — 11 entries, no and/or. Cross-checked against the datagen registry dump `reports/registries.json`, key `minecraft:worldgen/material_condition`, which lists those same 11 ids; the sibling `minecraft:worldgen/material_rule` lists exactly bandlands, block, condition, sequence (matching SurfaceRules.java:682-687) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Surface rules only ever run against, and only ever overwrite, positions whose current block still equals default_block; already-placed fluid, ore-vein blocks, and air are never touched by surface rules. | `SurfaceSystem.buildSurface` (SurfaceSystem.java:129-155): the rule is evaluated only inside the non-air, non-fluid else-branch and only under `if (old == this.defaultBlock)` (:150-155) — an identity comparison against the settings' default block, so a granite/tuff/ore vein block fails it too. Note: two column extensions outside the rule engine DO write over air and water — `erodedBadlandsExtension` for eroded badlands columns (:116-118, :205-233) and `frozenOceanExtension` for frozen-ocean columns (:158-160, :235-284) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The per-column surface-rule driver scans blocks from one block above the WorldSurfaceWg heightmap's highest non-air Y (height = getHeight(...) + 1) down to the world's minimum Y, inclusive. | The bottom bound is right (`endY = protoChunk.getMinY()`, SurfaceSystem.java:127, loop `for (int y = height; y >= endY; y--)` :129), the top bound is off by one. `height = protoChunk.getHeight(Heightmap.Types.WORLD_SURFACE_WG, x, z) + 1` (SurfaceSystem.java:120), and `ChunkAccess.getHeight` (ChunkAccess.java:182-193) returns `heightmap.getFirstAvailable(x & 15, z & 15) - 1`, i.e. the highest non-air Y. Correction: the scan starts one block ABOVE the highest non-air block — heightmap value + 1 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Whenever the per-column surface-rule scan encounters an air block, it resets stone_depth_above to 0 and water_height to its sentinel. | SurfaceSystem.java:131-133 — `if (old.isAir()) { stoneAboveDepth = 0; waterHeight = Integer.MIN_VALUE; }` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Whenever the per-column surface-rule scan encounters any block with a non-empty fluid state (also matching a waterlogged block, not an identity comparison against the water/lava ids), it does NOT reset stone_depth_above -- only the air branch does that -- and, the first time this happens in the column, records water_height = y+1. | SurfaceSystem.java:134-138 — the fluid branch is `else if (!old.getFluidState().isEmpty()) { if (waterHeight == Integer.MIN_VALUE) { waterHeight = y + 1; } }`. Two corrections: (a) it does NOT reset stoneAboveDepth — only the air branch does; (b) the test is "this block has a non-empty fluid state", which also matches waterlogged blocks, not an identity comparison against the water and lava block ids | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| For any other block, the per-column surface-rule scan increments stone_depth_above by 1. | SurfaceSystem.java:139-149 — the final else branch runs `stoneAboveDepth++` (:148) before computing `stoneBelowDepth` and calling `context.updateY(stoneAboveDepth, stoneBelowDepth, waterHeight, y)` (:150) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| get_surface_depth(x,z) is computed as (surface_noise * 2.75 + 3.0 + jitter) as i32, where surface_noise is the "surface" named noise sampled at (x, 0, z) and jitter is random.at(x,0,z).next_double() * 0.25, truncated toward zero exactly like Java's (int) cast. | `SurfaceSystem.getSurfaceDepth` (SurfaceSystem.java:169-172): `double noiseValue = this.surfaceNoise.getValue(blockX, 0.0, blockZ); return (int)(noiseValue * 2.75 + 3.0 + this.noiseRandom.at(blockX, 0, blockZ).nextDouble() * 0.25);`, with `surfaceNoise = randomState.getOrCreateNoise(Noises.SURFACE)` (:54) and `Noises.SURFACE = createKey("surface")` (Noises.java:52). This also settles the blueprint's flagged moderate-confidence guess about which factory is used: `noiseRandom` is the SurfaceSystem constructor argument, and RandomState passes `this.random` — the dimension's ROOT positional factory, unforked (RandomState.java:42, :36) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| surface_secondary is sampled once per column via the "surface_secondary" named noise at (x, 0, z), with no jitter term. | `SurfaceSystem.getSurfaceSecondary` (SurfaceSystem.java:174-176): `return this.surfaceSecondaryNoise.getValue(blockX, 0.0, blockZ);`, with `surfaceSecondaryNoise = randomState.getOrCreateNoise(Noises.SURFACE_SECONDARY)` (:55) and `Noises.SURFACE_SECONDARY = createKey("surface_secondary")` (Noises.java:53). The once-per-column memoization is `SurfaceRules.Context.getSurfaceSecondary` (SurfaceRules.java:367-374), keyed on the XZ update counter | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The NoiseThreshold condition is true when min_threshold <= v <= max_threshold (inclusive on both ends); v is sampled at (x, 0, z) and cached per XZ by default (the condition's own is_3d field defaults to false), or at (x, y, z) and cached per Y only when is_3d is true. | The inclusive test is right (`SurfaceRules.NoiseThresholdConditionSource` inner condition, SurfaceRules.java:641-644: `value >= minThreshold && value <= maxThreshold`), the sampling position is not. The condition carries a fourth compiled field `is_3d`, codec-defaulted to FALSE (:620), and the sampler is chosen by it (:634, :430-434): with is_3d false the noise is sampled at `(blockX, 0.0, blockZ)` and cached per XZ (:436-456); only with is_3d true is it sampled at `(blockX, blockY, blockZ)` and cached per Y (:458-478). Correction for the blueprint's condition table: the default, and the common case in the pinned data, is a 2D sample at Y = 0, not the current position | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The VerticalGradient condition resolves to true unconditionally at or below true_at_and_below, false unconditionally at or above false_at_and_above, and otherwise draws exactly one next_float() compared against a linear-map probability between those two anchors -> zero RNG draws in either deterministic zone, exactly one in the probabilistic band. | `SurfaceRules.VerticalGradientConditionSource` inner condition (SurfaceRules.java:892-906): `if (blockY <= trueAtAndBelow) return true; if (blockY >= falseAtAndAbove) return false; double probability = Mth.map(blockY, trueAtAndBelow, falseAtAndAbove, 1.0, 0.0); RandomSource random = randomFactory.at(blockX, blockY, blockZ); return random.nextFloat() < probability;` — the RandomSource is only constructed inside the band. Two notes for the blueprint's own pseudocode: vanilla uses `Mth.map`, not clampedMap (harmless inside the band), and the comparison is a FLOAT nextFloat promoted to double against a DOUBLE probability — do not cast the probability down to f32 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A surface rule's named random factory (used by VerticalGradient's random_name) is derived as root.from_hash_of(random_name).fork_positional(), memoized per distinct name so every reference to the same name shares one stream. | `RandomState.getOrCreateRandomFactory` (RandomState.java:119-121): `return this.positionalRandoms.computeIfAbsent(name, key -> this.random.fromHashOf(name).forkPositional());` over a ConcurrentHashMap keyed by Identifier (:25, :41); called once per condition instance at SurfaceRules.java:885 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The YAbove condition is true when block_y + (stone_depth_above if add_stone_depth else 0) is at or above resolve_anchor(anchor) + surface_depth_multiplier * surface_depth -- the stone-depth term is added to the current Y on the left side, not folded into the threshold on the right. | `SurfaceRules.YConditionSource` inner condition (SurfaceRules.java:975-979): `return this.context.blockY + (addStoneDepth ? this.context.stoneDepthAbove : 0) >= anchor.resolveY(this.context.context) + this.context.surfaceDepth * surfaceDepthMultiplier;`. Correction: the stone-depth term is added to the LEFT side (the current Y), not to the threshold — `y + s >= anchor + m*d` is not the same predicate as `y >= anchor + m*d + s`; the sign of the stone-depth contribution is inverted in the blueprint's table. `surface_depth_multiplier` is codec-bounded to -20..20 (:955) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Water condition does not simply mirror YAbove against water_height: it is true when water_height still equals its sentinel (no fluid seen yet in the column) OR block_y + (stone_depth_above if add_stone_depth else 0) is at or above water_height + offset + surface_depth_multiplier * surface_depth, and it carries its own integer offset field rather than reusing YAbove's VerticalAnchor. | `SurfaceRules.WaterConditionSource` inner condition (SurfaceRules.java:937-944): `return this.context.waterHeight == Integer.MIN_VALUE` OR `this.context.blockY + (addStoneDepth ? this.context.stoneDepthAbove : 0) >= this.context.waterHeight + offset + this.context.surfaceDepth * surfaceDepthMultiplier;` (a Java short-circuit or-else between the two). Two corrections: (a) it does NOT mirror YAbove exactly — there is a leading short-circuit that returns TRUE whenever no fluid has been seen yet in the column (the Integer.MIN_VALUE sentinel), which YAbove has no analogue for; (b) as with YAbove, the stone-depth term sits on the left side, added to the current Y, not in the threshold | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The per-column biome used by surface rules is re-resolved at the CURRENT scan Y on every Y step (via the already-filled 3D BiomeColumn at biome-cell coordinates x>>2, y>>2, z>>2), never fixed once per column at top_y. | `SurfaceRules.Context.getBiome` (SurfaceRules.java:376-382) resolves `this.biomeGetter.apply(this.pos.set(this.blockX, this.blockY, this.blockZ))` — at the CURRENT scan Y, not at top_y — and `updateY` invalidates the cached value on every Y step (:358-365), so the biome is re-resolved per Y, not once per column. The getter is `biomeManager::getBiome` (SurfaceSystem.java:100-102), i.e. the BiomeManager's own quart lookup with its noise-based smearing, not a raw BiomeColumn read. The only biome sampled once per column at the top is `surfaceBiome` (SurfaceSystem.java:114), used exclusively to select the eroded-badlands and frozen-ocean column extensions | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Biome condition is true when the current column's biome resource location is contained in the condition's own biome_is list. | `SurfaceRules.BiomeConditionSource` inner condition (SurfaceRules.java:203-206): `return BiomeConditionSource.this.biomes.contains(this.context.getBiome());`. Two shape notes: `biome_is` is a `HolderSet<Biome>` decoded by `RegistryCodecs.homogeneousList` (:185), so it may legitimately be a TAG reference as well as an inline list; and the biome tested is the one at the current (x, y, z), not a per-column value. There is also a build-time short-circuit: when the set of possible biomes for the chunk is known, the condition collapses to a constant false or true (:209-217) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Temperature condition is true when the current position/biome is cold enough for snow to form, a per-biome climate test rather than a noise-router field sample. | `SurfaceRules.Context.TemperatureHelperCondition` (SurfaceRules.java:527-539): `return this.context.getBiome().value().coldEnoughToSnow(this.context.pos.set(blockX, blockY, blockZ), this.context.getSeaLevel());`; `Biome.coldEnoughToSnow` (Biome.java:171-173) is `!warmEnoughToRain(pos, seaLevel)` and `warmEnoughToRain` (:175-177) is `getTemperature(pos, seaLevel) >= 0.15F` — the biome's height-adjusted climate temperature, no density function involved. Note the seaLevel argument comes from the SurfaceSystem's own settings sea level (:384-386) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Steep condition never reads the current column's own height: it compares the two OPPOSITE neighbors on each axis against each other with a signed, directional test -- Z axis first (south >= north + 4 returns true), otherwise X axis (west >= east + 4) -- never an absolute-difference test against the current column's own value. | `SurfaceRules.Context.SteepMaterialCondition.compute` (SurfaceRules.java:506-524) never reads the current column's height. It compares the two OPPOSITE NEIGHBOURS on each axis against each other, with a directional, signed test on each axis: Z axis first, `heightSouth >= heightNorth + 4` (heights at chunk-local (x, z-1 clamped) and (x, z+1 clamped)) returns true; otherwise X axis, `heightWest >= heightEast + 4` (heights at (x-1 clamped, z) and (x+1 clamped, z)). Corrections: neighbour-versus-neighbour, not neighbour-versus-self; signed, not absolute — a steep drop in the opposite direction on either axis does not trigger; and the two axes use opposite orientations (south minus north, west minus east) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Steep's own chunk-edge behavior clamps a neighbor position that would fall outside the current chunk to the current column's own value (contributing a difference of 0 for that neighbor) rather than reading another chunk's data. | `SteepMaterialCondition.compute` (SurfaceRules.java:508-522) works entirely in chunk-local coordinates and clamps them: `zNorth = Math.max(chunkBlockZ - 1, 0)`, `zSouth = Math.min(chunkBlockZ + 1, 15)`, `xWest = Math.max(chunkBlockX - 1, 0)`, `xEast = Math.min(chunkBlockX + 1, 15)`, all read via `chunk.getHeight(...)` on the current chunk — no neighbouring chunk is ever consulted. Since the clamp only bites when the current column IS on that edge, the clamped neighbour resolves to the current column itself, as the claim says. The parenthetical "difference of 0" does not follow, though, because the comparison is between the two opposite neighbours (see the previous row) — at an edge one side of the pair simply becomes the current column | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Hole condition is true when the column's own surface_depth is less than or equal to 0. | `SurfaceRules.Context.HoleCondition.compute` (SurfaceRules.java:490-499): `return this.context.surfaceDepth <= 0;`, evaluated as a LazyXZCondition (recomputed once per column) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The AbovePreliminarySurface condition is true when the current Y is at or above min_surface_level(x,z) + surface_depth - 8, where min_surface_level is the noise router's preliminary_surface_level field sampled once per column. | `SurfaceRules.Context.AbovePreliminarySurfaceCondition.test` (SurfaceRules.java:484-487) is `blockY >= getMinSurfaceLevel()`, and `getMinSurfaceLevel` (:396-428) ends with `this.minSurfaceLevel = preliminarySurfaceLevel + this.surfaceDepth - 8`, with HOW_FAR_BELOW_PRELIMINARY_SURFACE_LEVEL_TO_BUILD_SURFACE = 8 (:297). The whole quantity is memoized per XZ update (:397-398). Naming note: vanilla's `getMinSurfaceLevel()` already includes the `+ surfaceDepth - 8` term, and its `preliminarySurfaceLevel` is a bilinear blend of four samples, not one (see the next row) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla caches the preliminary_surface_level noise-router field per-XZ via a 4-corner bilinear lookup, since the field is Y-independent and is queried at the same (x,z) for every Y within a column. | `SurfaceRules.Context.getMinSurfaceLevel` (SurfaceRules.java:396-428): the four corners of the enclosing 16x16 surface cell (SURFACE_CELL_BITS 4, SURFACE_CELL_SIZE 16, :298-300) are sampled into `preliminarySurfaceCache[0..3]` and re-used until the cell origin changes (:402-412), then combined with `Mth.floor(Mth.lerp2((blockX & 15) / 16.0F, (blockZ & 15) / 16.0F, cache[0], cache[1], cache[2], cache[3]))` (:414-423). The underlying samples are `NoiseChunk.preliminarySurfaceLevel` (NoiseChunk.java:219-229), themselves quart-quantized, memoized per column, and computed from a `SinglePointContext(blockX, 0, blockZ)` — Y-independent as the claim states | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Not condition evaluates to the boolean negation of its own wrapped condition. | `SurfaceRules.NotCondition.test` (SurfaceRules.java:651-659): `return !this.target.test();`, built by `NotConditionSource.apply` (:674-676) from the compiled `invert` field (:662-664) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Bedrock's two surface rules (bedrock_floor and bedrock_roof) are ordinary instances of the general VerticalGradient condition and its shared named-random mechanism, with no special-casing. | `SurfaceRuleData` (data/worldgen/SurfaceRuleData.java:288, :293, :320-321) builds both as plain `SurfaceRules.verticalGradient("bedrock_floor", ...)` / `("bedrock_roof", ...)` wrapped in `SurfaceRules.ifTrue(..., BEDROCK)`; the factory lookup is the generic `RandomState.getOrCreateRandomFactory` (SurfaceRules.java:885, RandomState.java:119-121). Nothing in SurfaceRules.java or SurfaceSystem.java branches on the name bedrock | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ON_FLOOR stone_depth preset is offset=0, add_surface_depth=false, surface_type=Floor, secondary_depth_range=0. | `SurfaceRules.ON_FLOOR` (SurfaceRules.java:36) is `stoneDepthCheck(0, false, CaveSurface.FLOOR)`, which the two-arg helper (:46-48) expands to `new StoneDepthCheck(0, false, 0, FLOOR)`. Correction: `add_surface_depth` is FALSE, not true. Cross-checked against the pinned data — every `minecraft:stone_depth` instance across all seven `worldgen/noise_settings/*.json` files uses `offset: 0`, and the floor variants split into `add_surface_depth: false` (27 instances) and `true` (13 + 5 + 5) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The UNDER_FLOOR stone_depth preset is offset=0, add_surface_depth=true, surface_type=Floor, secondary_depth_range=0 -- the "under" behavior comes from the constant +1 baked into the StoneDepth condition's own comparison, not from a nonzero offset. | `SurfaceRules.UNDER_FLOOR` (SurfaceRules.java:37) is `stoneDepthCheck(0, true, CaveSurface.FLOOR)` = `new StoneDepthCheck(0, true, 0, FLOOR)` (:46-48). Correction: `offset` is 0, not 1 — the "under" behaviour comes from the constant +1 baked into the condition's own comparison (:801), not from the offset field. Confirmed by the pinned data, which contains no stone_depth instance with a nonzero offset | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The DEEP_UNDER_FLOOR stone_depth preset is offset=0, add_surface_depth=true, surface_type=Floor, secondary_depth_range=6. | `SurfaceRules.DEEP_UNDER_FLOOR` (SurfaceRules.java:38) is `stoneDepthCheck(0, true, 6, CaveSurface.FLOOR)` = `new StoneDepthCheck(0, true, 6, FLOOR)` (:50-54). Correction: the 6 is `secondary_depth_range`, not `offset`; offset is 0 and secondary_depth_range is 6, so this preset does consult the surface_secondary noise. The pinned data contain 5 instances of `{offset 0, add_surface_depth true, secondary_depth_range 6, surface_type floor}` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The VERY_DEEP_UNDER_FLOOR stone_depth preset is offset=0, add_surface_depth=true, surface_type=Floor, secondary_depth_range=30. | `SurfaceRules.VERY_DEEP_UNDER_FLOOR` (SurfaceRules.java:39) is `stoneDepthCheck(0, true, 30, CaveSurface.FLOOR)` = `new StoneDepthCheck(0, true, 30, FLOOR)` (:50-54). Correction: the 30 is `secondary_depth_range`, not `offset`; offset is 0 and secondary_depth_range is 30. The pinned data contain 5 instances of `{offset 0, add_surface_depth true, secondary_depth_range 30, surface_type floor}` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The ON_CEILING stone_depth preset is offset=0, add_surface_depth=false, surface_type=Ceiling, secondary_depth_range=0. | `SurfaceRules.ON_CEILING` (SurfaceRules.java:40) is `stoneDepthCheck(0, false, CaveSurface.CEILING)` = `new StoneDepthCheck(0, false, 0, CEILING)` (:46-48). Correction: `add_surface_depth` is FALSE, not true. The pinned data contain 70 instances of `{offset 0, add_surface_depth false, secondary_depth_range 0, surface_type ceiling}` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The UNDER_CEILING stone_depth preset is offset=0, add_surface_depth=true, surface_type=Ceiling, secondary_depth_range=0. | `SurfaceRules.UNDER_CEILING` (SurfaceRules.java:41) is `stoneDepthCheck(0, true, CaveSurface.CEILING)` = `new StoneDepthCheck(0, true, 0, CEILING)` (:46-48). Correction: `offset` is 0, not 1. The pinned data contain 2 instances of `{offset 0, add_surface_depth true, secondary_depth_range 0, surface_type ceiling}` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla computes stoneDepthBelow lazily via a cached nextCeilingStoneY, computed only when the current position could plausibly be the top of a solid run. | `SurfaceSystem.buildSurface` (SurfaceSystem.java:126, :139-149): `nextCeilingStoneY` is initialised to Integer.MAX_VALUE per column and recomputed only under `if (nextCeilingStoneY >= y)` — i.e. when the cached run bottom is no longer strictly below the current Y, which happens on entering a new solid run — then `stoneBelowDepth = y - nextCeilingStoneY + 1` (:149). Direction note for the blueprint's own reconstruction (Context §H): the look-ahead scans DOWNWARD from `y - 1` to `endY - 1` for the first non-stone block and sets `nextCeilingStoneY = lookaheadY + 1` (:141-147) — it locates the BOTTOM of the current solid run, so an upward scan toward top_y computes a different quantity; `isStone` here means "not air and no fluid state" (:177-179) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The StoneDepth condition is true when stone_depth_above (Floor) or stone_depth_below (Ceiling) is less than or equal to 1 + offset + surface_depth (if add_surface_depth, else 0) + a secondary-noise term that is a truncating linear remap of surface_secondary from [-1,1] onto [0, secondary_depth_range] (if secondary_depth_range != 0, else 0) -- the constant +1 in the base value is what makes offset=0 mean "the top block of the run". | `SurfaceRules.StoneDepthCheck` inner condition (SurfaceRules.java:794-802): `int stoneDepth = ceiling ? stoneDepthBelow : stoneDepthAbove; int surfaceDepth = addSurfaceDepth ? context.surfaceDepth : 0; int secondarySurfaceDepth = secondaryDepthRange == 0 ? 0 : (int)Mth.map(context.getSurfaceSecondary(), -1.0, 1.0, 0.0, secondaryDepthRange); return stoneDepth <= 1 + offset + surfaceDepth + secondarySurfaceDepth;`. Two corrections: (a) there is a constant +1 in the base value that the blueprint's reconstruction omits entirely — this is what makes offset 0 mean "the top block of the run"; (b) the secondary term is not `round(secondary * range)` but a truncating linear remap of the secondary noise from [-1, 1] onto [0, secondary_depth_range] (`(int)Mth.map(...)`, Mth.java:665-667, truncating toward zero) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Bandlands clay-band palette is a fixed 192-entry palette generated once at SurfaceSystem construction, offset by round(clayBandsOffsetNoise(x,z) * 4), and indexed by (y + offset) mod 192. | `SurfaceSystem.generateBands` (SurfaceSystem.java:286-317) allocates `new BlockState[192]`, and the constructor calls it once with `noiseRandom.fromHashOf(Identifier.withDefaultNamespace("clay_bands"))` (:53). `getBand` (:332-335): `int offset = (int)Math.round(this.clayBandsOffsetNoise.getValue(worldX, 0.0, worldZ) * 4.0); return this.clayBands[(y + offset + this.clayBands.length) % this.clayBands.length];` — note the `+ length` before the remainder, needed because Java's % is a remainder, not a floored modulo; use a floored modulo or reproduce the added length | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The pinned bedrock_floor surface rule uses true_at_and_below = bottom(0) and false_at_and_above = aboveBottom(5). | `SurfaceRuleData` (data/worldgen/SurfaceRuleData.java:293, :320): `SurfaceRules.verticalGradient("bedrock_floor", VerticalAnchor.bottom(), VerticalAnchor.aboveBottom(5))`, and `VerticalAnchor.BOTTOM = aboveBottom(0)` (VerticalAnchor.java:10, :26-28). Confirmed in the pinned data: the overworld `noise_settings/overworld.json` surface_rule contains `{"type": "minecraft:vertical_gradient", "random_name": "minecraft:bedrock_floor", "true_at_and_below": {"above_bottom": 0}, "false_at_and_above": {"above_bottom": 5}}` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The pinned bedrock_roof surface rule uses true_at_and_below = belowTop(5) and false_at_and_above = top(0), always wrapped in a Not condition in the real surface-rule tree. | `SurfaceRuleData` (data/worldgen/SurfaceRuleData.java:288, :321): `SurfaceRules.ifTrue(SurfaceRules.not(SurfaceRules.verticalGradient("bedrock_roof", VerticalAnchor.belowTop(5), VerticalAnchor.top())), BEDROCK)` at both sites, and `VerticalAnchor.TOP = belowTop(0)` (VerticalAnchor.java:11, :30-32). Confirmed in the pinned data: both bedrock_roof instances across `worldgen/noise_settings/*.json` are `{"true_at_and_below": {"below_top": 5}, "false_at_and_above": {"below_top": 0}}` and both have a `minecraft:not` as their immediate parent | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
