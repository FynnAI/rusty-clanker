# M5-B12c — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| In `root_system`, the only early abort is when the block at `origin` itself is not air, checked before any RNG draw; there is no `root_requires_solid_ground` field, and the config's 15 keys are `feature`, `required_vertical_space_for_tree`, `level_test_distance`, `max_level_deviation`, `root_radius`, `root_replaceable`, `root_state_provider`, `root_placement_attempts`, `root_column_max_height`, `hanging_root_radius`, `hanging_roots_vertical_span`, `hanging_root_state_provider`, `hanging_root_placement_attempts`, `allowed_vertical_water_for_tree`, `allowed_tree_position`. | `RootSystemFeature.place` (RootSystemFeature.java:21-37) and `RootSystemConfiguration` (RootSystemConfiguration.java:14-50) -- No `root_requires_solid_ground` field exists. The codec's 15 keys are `feature`, `required_vertical_space_for_tree`, `level_test_distance`, `max_level_deviation`, `root_radius`, `root_replaceable`, `root_state_provider`, `root_placement_attempts`, `root_column_max_height`, `hanging_root_radius`, `hanging_roots_vertical_span`, `hanging_root_state_provider`, `hanging_root_placement_attempts`, `allowed_vertical_water_for_tree`, `allowed_tree_position`. The only early abort is `!level.getBlockState(origin).isAir()` (:24-26), zero draws; the solidity test on the block below (:93) applies to the candidate tree position inside `placeDirtAndTree`, not to `origin.y-1` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `root_system`, `root_column_max_height` is not an RNG bound at all; it is the exclusive bound of an upward tree-site search loop, and no `next_int_bounded` draw is taken for it. | `RootSystemFeature.placeDirtAndTree` (RootSystemFeature.java:85) -- `rootColumnMaxHeight` is the exclusive bound of a search loop `for (int y = 0; y < config.rootColumnMaxHeight(); y++)`, not an RNG bound; no draw is taken for it | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `root_system`, the upward walk searches for a tree site rather than placing a straight air-gated column: at each step it checks the world-surface heightmap, `allowed_tree_position`, available vertical space, and a non-lava solid block below, and only once the embedded tree feature successfully places at the discovered site does a dirt fill run from `origin.y` up to that site's own height, one scattered-attempt pass per level rather than a single air-gated column write. | `RootSystemFeature.placeDirtAndTree` / `placeDirt` (RootSystemFeature.java:77-117) -- The upward walk searches for a tree site: per step it moves up one, aborts if the WORLD_SURFACE heightmap is below the position (:87), then requires `allowedTreePosition` plus `spaceForTree` (:91) and a non-lava solid block below (:93), and finally attempts the configured tree placed feature (:97). Only when that tree succeeds does `placeDirt` (:107-117) fill the y-range `origin.y .. targetHeight-1`, and it does so by calling `placeRootedDirt` (scattered attempts) per level, never by writing a straight air-gated column | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `root_system`, the scattered-roots pass (`place_rooted_dirt`) runs `root_placement_attempts` times per column level; each attempt draws `dx = next_int_bounded(root_radius) - next_int_bounded(root_radius)`, then `dz` the same way -- four draws per attempt, evaluated x, x, z, z, with the y offset the literal 0, and resets to the column axis after each attempt. | `RootSystemFeature.placeRootedDirt` (RootSystemFeature.java:119-141) -- Four draws per attempt, not three: `nextInt(rootRadius) - nextInt(rootRadius)` for the x offset and the same pair for the z offset, evaluated x, x, z, z; the y offset is the literal 0 and no `col_height`-bounded dy draw exists. The bound is `rootRadius` itself, not `rootRadius*2+1`. The attempt loop runs `root_placement_attempts` times per column level and is re-run for every y from `origin.y` to `targetHeight-1` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `root_system`, a scattered root is written only if its target position is a member of the `root_replaceable` block holder set, and each such write consumes a fresh `sample_block_state_provider` draw from `root_state_provider`. | `RootSystemFeature.placeRootedDirt` (RootSystemFeature.java:128, :134-136) -- The gate is `s.is(config.rootReplaceable())` -- the existing block must be a member of the config's `root_replaceable` block holder set -- not an air-or-replaceable test. The provider call `config.rootStateProvider().getState(level, random, workingPos)` (:135) is indeed inside that branch, one fresh sample per write | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `root_system`, the hanging-roots phase runs `hanging_root_placement_attempts` times (a config field distinct from `root_placement_attempts`); each attempt draws `dx = next_int_bounded(hanging_root_radius) - next_int_bounded(hanging_root_radius)`, then `dy` the same way against `hanging_roots_vertical_span`, then `dz` the same way against `hanging_root_radius` -- six draws per attempt, in x, x, y, y, z, z order, against the raw config values rather than `radius*2+1`/`span+1`. | `RootSystemFeature.placeRoots` (RootSystemFeature.java:143-163) -- The loop count is `config.hangingRootPlacementAttempts()` (:149), a separate config field from `root_placement_attempts`. Six draws per attempt, in x, x, y, y, z, z order: `nextInt(hangingRootRadius) - nextInt(hangingRootRadius)`, `nextInt(verticalSpan) - nextInt(verticalSpan)`, `nextInt(hangingRootRadius) - nextInt(hangingRootRadius)` (:150-155) -- bounds are the raw radius and span, not `radius*2+1` / `span+1` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `root_system`, a hanging root candidate is gated first on strict air at the target position, then a `hanging_root_state_provider` draw is taken for every such strictly-empty candidate whether or not it is written, and only then is the write made, gated on `canSurvive` plus a sturdy face on `Direction::Down` from the block directly above. | `RootSystemFeature.placeRoots` (RootSystemFeature.java:156-161) -- Order is `level.isEmptyBlock(workingPos)` (strictly air, not air-or-replaceable), THEN the provider draw `config.hangingRootStateProvider().getState(...)` (:157), THEN `targetState.canSurvive(level, workingPos)` AND `level.getBlockState(workingPos.above()).isFaceSturdy(level, workingPos, Direction.DOWN)` (:158). The provider draw is therefore consumed for every empty candidate position, not only for written ones, and the claim omits the `canSurvive` condition | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's `root_system` feature is a distinct mechanism from the `mangrove_root_placer` (a `TreeConfiguration`-embedded `RootPlacer` variant) despite the similar name -- the two share no mechanism. | `RootSystemFeature` (RootSystemFeature.java:15) vs `MangroveRootPlacer` (rootplacers/MangroveRootPlacer.java:19-26) -- `RootSystemFeature extends Feature<RootSystemConfiguration>` and is dispatched as its own feature type; `MangroveRootPlacer extends RootPlacer` and is reached only through `TreeConfiguration`'s root-placer slot, driven by `TreeFeature`. No shared code path or config type | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `multiface_growth` (glow lichen), when `chance_of_spreading` is not configured, the spreading probability defaults to 0.5. | `MultifaceGrowthConfiguration.CODEC` (MultifaceGrowthConfiguration.java:26) -- `Codec.floatRange(0.0F, 1.0F).optionalFieldOf("chance_of_spreading", 0.5F)` -- the default is exactly 0.5 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `multiface_growth`, the set of face directions checked is built from the `can_place_on_floor`/`can_place_on_ceiling`/`can_place_on_wall` config flags (never a fixed six -- glow lichen's own config yields 5, sculk vein's yields 6) and is handed out shuffled per call, consuming RNG, rather than iterated in a fixed declared order. | `MultifaceGrowthConfiguration` (:62-82) and `MultifaceGrowthFeature.place` (:30, :39) -- The direction list is neither fixed nor necessarily six long. `validDirections` is built from the config flags -- UP when `can_place_on_ceiling`, DOWN when `can_place_on_floor`, then the four horizontals when `can_place_on_wall` (:63-73) -- and is handed out SHUFFLED per call by `getShuffledDirections` / `getShuffledDirectionsExcept` (:76-82), consuming RNG. Vanilla's `glow_lichen` config omits `can_place_on_floor`, so its list holds 5 directions. The `Direction` enum's own declaration order is DOWN, UP, NORTH, SOUTH, WEST, EAST (Direction.java:33-38), but this feature never iterates it | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `multiface_growth`, a face direction whose neighbor is not a member of the `can_be_placed_on` block holder set is skipped with zero RNG draws consumed for that gate itself, though RNG is already consumed before any direction is examined by the direction-list shuffle. | `MultifaceGrowthFeature.placeGrowthIfPossible` (MultifaceGrowthFeature.java:69-71) -- The per-direction gate is `neighbourState.is(config.canBePlacedOn)` -- membership in the config's `can_be_placed_on` block holder set -- not a sturdy-face test. And draws are consumed before any direction is examined, by the direction-list shuffle (:30, :39) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `multiface_growth`, there is no per-direction float gating placement; exactly one `next_float()` is drawn after a successful write, and it gates only whether the multiface spreader spreads onward from the placed face, not whether that face gets placed. | `MultifaceGrowthFeature.placeGrowthIfPossible` (MultifaceGrowthFeature.java:72-83) -- There is no per-direction float. Exactly one `random.nextFloat()` is drawn AFTER a successful `level.setBlock` (:77-79), and it gates only whether the multiface spreader spreads onward from the placed face (:80). A direction that fails the `can_be_placed_on` test consumes no float at all | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `multiface_growth`, the origin gate is `is_air_or_water` (air or the water block, not air-or-replaceable), and placement is not confined to `origin`: on failure at `origin`, the feature retries at the neighbouring position one step along each shuffled search direction, re-deriving that same one-step-away position from `origin` on every retry up to `search_range` times rather than walking outward, drawing a fresh excluded-direction shuffle per search direction. | `MultifaceGrowthFeature.place` (MultifaceGrowthFeature.java:25, :35-51, :90-92) -- The gate is `isAirOrWater(state)` -- air or `Blocks.WATER` (:90-92) -- not air-or-replaceable. And placement is not confined to `origin`: when the attempt at `origin` fails, the feature walks outward up to `config.searchRange` (default 10) along each shuffled search direction and places the growth at the reached position instead (:37-51) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `underwater_magma`, placement aborts entirely with zero draws unless `origin` itself is water and a floor is found by scanning the water column within `floor_search_range`; there is no alternative solid-block-below gate. | `UnderwaterMagmaFeature.place` / `getFloorY` (UnderwaterMagmaFeature.java:33-36, :52-57) via `Column.scan` (Column.java:67-83) -- The abort is `getFloorY(...).isEmpty()`. `Column.scan` requires the block AT `origin` to be `Blocks.WATER` (Column.java:75-77) and then scans up and down within `floor_search_range` for the water column's floor. There is no "or the block at origin.y-1 is solid" alternative; the solid-floor condition is not an OR branch but the scanned column's own floor | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `underwater_magma`, candidate cells form an axis-equal cube (never an ellipsoid) of radius `placement_radius_around_floor` on every axis, centred on the scanned floor position, not on `origin`. | `UnderwaterMagmaFeature.place` (UnderwaterMagmaFeature.java:38-40) -- The candidate volume is a cube, not an ellipsoid: `BoundingBox.fromCorners(floorPos.subtract(radius), floorPos.offset(radius))` with `radius = new Vec3i(placementRadiusAroundFloor, placementRadiusAroundFloor, placementRadiusAroundFloor)` -- the same radius on all three axes, with no vertical halving -- and it is centred on the scanned floor position `origin.atY(floorY)` (:38), not on `origin` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `underwater_magma`, every candidate cell of the cube consumes one `next_float()` draw against `placement_probability_per_valid_position` before validity is checked at all, and magma is placed only where that draw succeeds and the cell additionally passes the (non-`percent`, non-`radius`) validity predicate. | `UnderwaterMagmaFeature.place` / `isValidPlacement` (UnderwaterMagmaFeature.java:41-49, :59-71) and `UnderwaterMagmaConfiguration` (:9-11) -- The `random.nextFloat()` filter runs on EVERY position of the box and runs BEFORE the validity filter (:42 precedes :43), so the draw is consumed for invalid cells too. The threshold field is `placement_probability_per_valid_position`; there is no `percent` or `radius` key (the three keys are `floor_search_range`, `placement_radius_around_floor`, `placement_probability_per_valid_position`). Validity is `!isWaterOrAir(state)` plus face-occlusion checks on the block below and the four horizontal neighbours (:59-71), not an `is_solid` test | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `monster_room`, two draws (`xr = next_int_bounded(2) + 2`, `zr = next_int_bounded(2) + 2`) are taken before any scan, and placement aborts with zero further draws unless every cell of the box's own floor (`dy == -1`) and ceiling (`dy == 4`) layers is solid, and the perimeter's hole count at `dy == 0` falls between 1 and 5 inclusive; there is no 20-solid-cell ellipsoid threshold. | `MonsterRoomFeature.place` (MonsterRoomFeature.java:33-69) -- The feature takes `NoneFeatureConfiguration` and draws two ints BEFORE any scan: `xr = random.nextInt(2) + 2` (:39) and `zr = random.nextInt(2) + 2` (:44). The scanned volume is a box -- dx in [-xr-1, xr+1], dy in [-1, 4], dz in [-zr-1, zr+1] (:49-51) -- not an ellipsoid. The aborts are: any dy == -1 or dy == 4 cell not solid returns false (:54-59), and a hole count outside 1..5 (:69), where a hole is a wall-ring cell at dy == 0 that is empty with an empty block above (:62-64). No 20-solid-cell threshold exists | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `monster_room`, the two room-size draws (`xr`, `zr`) are taken before the solid floor/ceiling scan, so even an abort path is not zero-draw; the scan itself consumes zero further RNG. | `MonsterRoomFeature.place` (MonsterRoomFeature.java:39, :44, :49-67) -- The triple scan loop itself does consume zero draws, but no 20-cell threshold exists, and the two room-size draws at :39 and :44 are taken before the scan begins -- so the abort path is not zero-draw | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `monster_room`, a shell cell becomes mossy cobblestone with probability 3/4 (`dy == -1 && next_int_bounded(4) != 0`) and cobblestone otherwise, and only at `dy == -1`; the draw is consumed only for solid, non-chest, dy == -1 shell cells, never for shell cells on other layers. | `MonsterRoomFeature.place` (MonsterRoomFeature.java:75-83) -- The roll is inverted and floor-only: a shell cell becomes `Blocks.MOSSY_COBBLESTONE` when `dy == -1 && random.nextInt(4) != 0`, i.e. mossy with probability 3/4 and cobblestone with probability 1/4, and only on the dy == -1 floor layer (:79-83). The draw is consumed only for dy == -1 shell cells that are solid, not a CHEST, and sit on a solid block (:76-78); shell cells on other layers consume no draw | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `monster_room`, every non-shell (interior) cell of the rectangular box is cleared to cave air, skipping cells already holding a chest or spawner, consuming zero RNG draws. | `MonsterRoomFeature.place` (MonsterRoomFeature.java:26, :70-90) -- Zero draws in the interior branch is correct, but the volume is the rectangular box, not a radius-(3,2) ellipsoid; the fill state is `Blocks.CAVE_AIR` (:26), written through `safeSetBlock` under the `BlockTags.FEATURES_CANNOT_REPLACE` predicate; and cells already holding CHEST or SPAWNER are skipped (:85-87). Shell cells whose block below is not solid are also written to CAVE_AIR (:76-77) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `monster_room`, a spawner block is placed at `origin`, but its entity type is drawn via one `next_int_bounded(4)` pick among `{Skeleton, Zombie, Zombie, Spider}` -- the spawner write is not zero-draw. | `MonsterRoomFeature.place` (MonsterRoomFeature.java:92-121) and `randomEntityId` (:129-131) -- The spawner write at :116 is followed by `spawner.setEntityId(this.randomEntityId(random), random)` (:117-118); `randomEntityId` is `Util.getRandom(MOBS, random)` over the 4-entry MOBS array (:25), so at least one draw is consumed, plus whatever `BaseSpawner.setEntityId` (BaseSpawner.java:64-66) consumes. Two chest passes of up to three tries each, two int draws per try, also precede it (:92-114) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The shell-vs-interior split used for `monster_room` is a rectangular box-face test against the box's own min/max planes (`dx`/`dz` at the edges, or `dy == -1`/`dy == 4`), never an ellipsoid boundary evaluated at equality versus less-than-or-equal. | `MonsterRoomFeature.place` (MonsterRoomFeature.java:75) -- There is no ellipsoid and no equality-vs-`<=` split. Shell membership is a rectangular test against the box's own min/max planes: a cell is shell when `dx == minX` or `dy == -1` or `dz == minZ` or `dx == maxX` or `dy == 4` or `dz == maxZ`; everything else is interior. (The dy loop runs 3 down to -1, so the `dy == 4` disjunct never fires there.) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_pile`, the candidate extents are drawn (`xr = 2 + next_int_bounded(2)`, `zr = 2 + next_int_bounded(2)`), spanning `dx` in `-xr..=xr` and `dz` in `-zr..=zr` over two y layers -- 50 to 98 cells, never a fixed 5x5 of 25. | `BlockPileFeature.place` (BlockPileFeature.java:24-31) -- The extents are drawn, not fixed: `xr = 2 + random.nextInt(2)` and `zr = 2 + random.nextInt(2)` (:28-29), and the iteration is `BlockPos.betweenClosed(origin.offset(-xr, 0, -zr), origin.offset(xr, 1, zr))` (:31) -- dx in -xr..xr, dz in -zr..zr with xr and zr each 2 or 3, over TWO y layers (origin.y and origin.y+1). That is 50 to 98 cells, not a fixed 25. A zero-draw abort precedes all of it when `origin.getY() < level.getMinY() + 5` (:24-26) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_pile`, each candidate cell consumes two `next_float()` draws tested as `xd*xd + zd*zd <= next_float()*10.0 - next_float()*6.0` (where `xd`/`zd` are `origin` minus the cell's own coordinate), plus a third `next_float()` against 0.031 only when that first test fails; the formula `1.0 - dist_sq * 0.1` does not occur. | `BlockPileFeature.place` (BlockPileFeature.java:34-38) -- The test is `xd * xd + zd * zd <= random.nextFloat() * 10.0F - random.nextFloat() * 6.0F` -- two `nextFloat()` draws -- and when it fails a THIRD `nextFloat()` is drawn and compared with 0.031. The formula `1.0 - dist_sq * 0.1` appears nowhere, and the cell count is not 25 (see the preceding row) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_pile`, each cell's probability roll happens before any of that cell's placement gates are checked, so the draw is consumed even for cells that will later fail the solid-floor or air-replaceable gates. | `BlockPileFeature.place` / `tryPlaceBlock` (BlockPileFeature.java:34-38, :50-53) -- The distance/probability test at :34-38 is evaluated before `tryPlaceBlock`, whose `isEmptyBlock` and `mayPlaceOn` gates therefore run only afterwards; the roll's draws are consumed for every cell of the box regardless. (Count note for the blueprint: the roll is 2 draws, or 3 when the first test fails, not 1.) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_pile`, the per-cell skip test makes placement denser near the pile's center and sparser toward its edges, since `dist_sq` grows with distance from `(0,0)`. | `BlockPileFeature.place` (BlockPileFeature.java:32-38) -- `xd * xd + zd * zd <= nextFloat() * 10.0F - nextFloat() * 6.0F` becomes monotonically harder to satisfy as the squared horizontal distance from origin grows, with only a flat 0.031 fallback (:36) for cells that fail it -- so placement is densest at the centre and thins toward the edges. (The vanilla formula is not `1.0 - dist_sq * 0.1`; only the monotonicity property matches.) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_pile`, a candidate position that passes its probability roll is only used if it is strictly air and either the block below is face-sturdy on `Direction::Up`, or (when that block below is a dirt path) a `next_bool()` coin flip succeeds. | `BlockPileFeature.tryPlaceBlock` / `mayPlaceOn` (BlockPileFeature.java:44-53) -- The gates are `level.isEmptyBlock(blockPos)` -- strictly air, not air-or-replaceable -- and `mayPlaceOn`, which is `belowState.isFaceSturdy(level, below, Direction.UP)` (an upward-sturdy face, not general solidity), or a `random.nextBoolean()` coin flip when the block below is `Blocks.DIRT_PATH` (:47) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_pile`, checking whether a candidate position is strictly air consumes zero RNG draws, but the floor check consumes one `next_bool()` draw whenever the block below the candidate is a dirt path. | `BlockPileFeature.mayPlaceOn` (BlockPileFeature.java:44-48) -- `mayPlaceOn` consumes one `random.nextBoolean()` draw whenever the block below the candidate is `Blocks.DIRT_PATH` (:47), so the floor check is not always zero-draw | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_pile`, there is no per-cell column or height draw; the pile's vertical extent is the fixed two-layer y-range of the iteration box, and each of those two layers is an independent candidate with its own probability roll and its own single-block write. | `BlockPileFeature.place` (BlockPileFeature.java:31, :50-53) -- There is no per-cell column and no height draw at all. The pile's vertical extent is the fixed two-layer y-range of the iteration box (origin.y and origin.y+1, :31); each of those cells is an independent candidate carrying its own probability roll and its own single-block write | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_pile`, each block of the column consumes its own fresh `sample_block_state_provider` draw from `state_provider`. | `BlockPileFeature.tryPlaceBlock` (BlockPileFeature.java:52) -- Every write calls `config.stateProvider.getState(level, random, blockPos)` once -- a fresh provider sample per placed block. (Shape note for the blueprint: those blocks are independent cells of the two-layer box, not a column produced by a height draw.) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_column`, the configured `direction` string resolves to a vertical step of +1 for "up" and -1 for "down". | `BlockColumnConfiguration` (:12, :17) and `BlockColumnFeature.place` (:33, :41, :51); `data/minecraft/worldgen/configured_feature/{cactus,cave_vine,cave_vine_in_moss,dripleaf,sugar_cane}.json` -- `direction` is serialised by `Direction.CODEC` and the feature advances with `placePos.move(config.direction())`, so "up" steps +1 and "down" steps -1 in Y; all five vanilla `block_column` configured features use only "up" or "down". Shape note: the codec accepts all six directions, so the field is not restricted to a vertical step | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_column`, layers are processed strictly in their configured order. | `BlockColumnFeature.place` (BlockColumnFeature.java:23-26, :44-54) -- Both loops iterate `config.layers()` by ascending index -- the height-sampling loop (:23-26) and the placement loop (:44-54) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_column`, each layer's height is drawn via `sample_int_provider`, consuming one or more RNG draws depending on the configured int provider. | `BlockColumnFeature.place` (BlockColumnFeature.java:23-26) -- `layerHeights[i] = config.layers().get(i).height().sample(random)`; draw count depends on the configured `IntProvider`. Ordering note for the blueprint: every layer's height is sampled up front, before any predicate test or block write, not interleaved layer by layer | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_column`, evaluating the placement predicate at a position consumes zero RNG draws. | `BlockColumnFeature.place` (BlockColumnFeature.java:36) and `BlockPredicate` -- `config.allowedPlacement().test(level, nextPos)` takes no `RandomSource`; `BlockPredicate` is a `BiPredicate<WorldGenLevel, BlockPos>`, so evaluation is deterministic and draw-free | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_column`, the placement predicate runs as a pre-pass over the full pre-sampled height before any writes; on the first failure at height `y`, `truncate` removes the excess from layer index 0 upward when `prioritize_tip` is true or from the last layer index downward when it is false -- which layers get abandoned depends on that flag, not simply on stopping in place. | `BlockColumnFeature.place` / `truncate` (BlockColumnFeature.java:32-42, :59-71) -- The predicate runs as a separate pre-pass over `totalHeight` positions starting one step beyond origin (:32-42). The first failure at height y calls `truncate(layerHeights, totalHeight, y, config.prioritizeTip())` (:37) and breaks; `truncate` (:59-71) removes the excess from index 0 upward when `prioritize_tip` is true and from the last index downward when it is false. All writes happen afterwards from the truncated heights (:44-54), so which layers are dropped is decided by the `prioritize_tip` flag, not by "stop where the failure occurred and abandon the rest" | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_column`, each block placed within a layer's height loop consumes its own fresh `sample_block_state_provider` draw from that layer's `provider`. | `BlockColumnFeature.place` (BlockColumnFeature.java:50) -- `level.setBlock(placePos, layer.state().getState(level, random, placePos), 2)` -- one fresh provider sample per placed block, from that layer's own provider | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `replace_single_block`, `config.targets` are evaluated in list order and the first target whose rule test matches at `origin` causes that target's state to be written and the function to return immediately, with no further targets evaluated and no further RNG draws consumed once a match is found. | `ReplaceBlockFeature.place` (ReplaceBlockFeature.java:20-25) and `ReplaceBlockConfiguration.CODEC` (:12) -- The loop iterates `config.targetStates` in list order; the first `targetState.target.test(level.getBlockState(origin), context.random())` returning true writes `targetState.state` at origin and `break`s, so no later target is tested and no further draw is taken. The JSON key is `targets`, matching the blueprint's field name | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_blob`, there is no radius `IntProvider` and no ellipsoid target; each of exactly three blob iterations draws its own `xr`, `yr`, `zr` (each `next_int_bounded(2)`) and computes the sphere radius as `(xr + yr + zr) * 0.333 + 0.5`, then re-centres by three more draws before the next iteration. | `BlockBlobConfiguration` (BlockBlobConfiguration.java:8-15) and `BlockBlobFeature.place` (:21-42) -- The config is `(state, can_place_on)` -- a `BlockState` plus a `BlockPredicate` -- with no radius `IntProvider` and no `RuleTest`. The feature first walks origin down while `canPlaceOn` fails at `origin.below()` (:21-27, aborting at `minY + 3`), then runs THREE blob iterations; each draws `xr`, `yr`, `zr` = `random.nextInt(2)` separately (:30-32) and uses `tr = (xr + yr + zr) * 0.333F + 0.5F` as the sphere radius (:33), then re-offsets origin with three more draws `-1 + nextInt(2)`, `-nextInt(2)`, `-1 + nextInt(2)` (:41) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `block_blob`, every cell within `distSqr(cell, pos) <= tr*tr` is written unconditionally with `config.state`; there is no per-cell rule test and no `target` field. | `BlockBlobFeature.place` (BlockBlobFeature.java:35-39) -- Cells inside `blockPos.distSqr(origin) <= tr * tr` are written unconditionally with `config.state()`; there is no per-cell rule test, no target config, and no per-cell draw. All RNG is the three radius draws and three re-offset draws per blob iteration | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `desert_well`, the well is built only if the block reached by walking up one from `origin` and then down while air (down to `min_y + 2`) is sand; otherwise, or if any of the 25 (ox,oz) columns beneath has both `y-1` and `y-2` empty, zero blocks are placed. | `DesertWellFeature.place` (DesertWellFeature.java:29-48) -- The feature takes `NoneFeatureConfiguration` and first moves origin UP one, then walks it DOWN while the block is empty until `level.getMinY() + 2` (:32-36). The gate is then `IS_SAND.test(level.getBlockState(origin))` -- the landing block must be `Blocks.SAND` (:38-40) -- plus a second abort when any of the 25 (ox, oz) columns has both (ox, -1, oz) and (ox, -2, oz) empty (:42-48). There is no air-or-replaceable test at `origin.y+1` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `desert_well`, sandstone at the well's own `y+1` layer is not confined to the outer ring: the full 5x5x3 body below is solid sandstone, the `y+1` ring cells outside `ox.abs() == 2` or `oz.abs() == 2` are also sandstone at other layers, and four ring cells at `(2,0)`, `(-2,0)`, `(0,2)`, `(0,-2)` are overwritten with sandstone slabs. | `DesertWellFeature.place` (DesertWellFeature.java:50-56, :71-82, :94-99) -- At the +1 layer, sandstone is written both on the outer ring where `ox` is -2 or 2 or `oz` is -2 or 2 (:71-77) AND at the four inner pillar positions (+/-1, 1, +/-1) (:94-99), so "only on the outer ring" is false. Four ring cells are then overwritten with sandstone slabs at (2,1,0), (-2,1,0), (0,1,2), (0,1,-2) (:79-82). A solid 5x5x3 sandstone body is also written at oy = -2..0 (:50-56) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `desert_well`, five water source blocks are placed -- the well's own position plus its four horizontal neighbours -- not a single block at `(origin.x, origin.y+1, origin.z)`. | `DesertWellFeature.place` (DesertWellFeature.java:58-62, :101-105) -- Water is written at the adjusted origin ITSELF (:58), not at y+1, and at all four of its horizontal neighbours (:60-62) -- five water blocks, not one. Two suspicious-sand blocks are then written one and two blocks below randomly chosen members of that same five-position set (:101-105) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `desert_well`, the four corner posts sit at `(ox,oz)` of `(-1,-1)`, `(-1,1)`, `(1,-1)`, `(1,1)`, each three blocks of plain sandstone tall (`oy` 1 through 3), not slabs at `(±2,±2)`. | `DesertWellFeature.place` (DesertWellFeature.java:79-99) -- The corner pillars are at (+/-1, oy, +/-1) for oy = 1..3 and are SANDSTONE, three blocks tall (:94-99) -- not slabs at (+/-2, +/-2). The sandstone slabs sit at (2,1,0), (-2,1,0), (0,1,2), (0,1,-2) (:79-82) plus the +4 roof layer, where the 3x3 (ox, oz) in -1..1 is slabs except the sandstone centre (:84-92) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `desert_well` placement consumes exactly two `next_int_bounded(5)` draws, one per suspicious-sand block, each picking a random member of the five water positions -- never zero. | `DesertWellFeature.place` (DesertWellFeature.java:101-105) -- Two draws are consumed: `Util.getRandom(waterPositions, random)` is called once per suspicious-sand placement (:104, :105), picking from the five-entry water-position list | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `void_start_platform` clips a Chebyshev-distance plate (stone, with a single cobblestone marker cell) to the current chunk at `y = chunk_origin.y + 3`, only for chunks within one chunk of a fixed platform-origin chunk -- not a small fixed 2x1x2 platform at `y = 64` -- while still consuming zero RNG draws. | `VoidStartPlatformFeature` (VoidStartPlatformFeature.java:11-14, :25-48) -- The zero-RNG part holds, but nothing else does. The feature returns immediately for chunks whose checkerboard distance from the platform origin chunk exceeds 1 (:28-30); otherwise it fills every column of the current chunk whose checkerboard distance from the platform origin (x = 8, z = 8) is <= 16 (:35-44), at the single y = `context.origin().getY() + 3` (:32) -- not a fixed y = 64 -- writing `Blocks.STONE` everywhere except `Blocks.COBBLESTONE` at the platform origin itself (:39-43). The result is a 33x33-clipped plate spanning up to nine chunks, not a 2x1x2 platform | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `fill_layer`, all 256 (x,z) positions of the chunk's own 16x16 footprint are visited at `min_y + config.height`, but a write happens only where the existing block is already air, so the write count is data-dependent and at most 256, never guaranteed to be exactly 256. | `FillLayerFeature.place` (FillLayerFeature.java:20-29) -- All 256 (dx, dz) positions are visited, but a write happens only where `level.getBlockState(pos).isAir()` (:26-27), so the number of writes is data-dependent and not exactly 256. The y is `level.getMinY() + config.height` (:24) -- the configured `height` is an offset from the world's minimum Y, not an absolute Y | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `fill_layer`, `origin` is the chunk's own raw minimum corner, the same convention `freeze_top_layer` uses. | `FillLayerFeature.place` (FillLayerFeature.java:20-23), `SnowAndFreezeFeature.place` (SnowAndFreezeFeature.java:26-29), `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:321-322) -- Both features compute `x = origin.getX() + dx`, `z = origin.getZ() + dz` for dx, dz in 0..15, i.e. both treat `origin` as the chunk's raw minimum corner; the origin handed to feature placement is `SectionPos.of(chunkPos, level.getMinSectionY()).origin()`. Note: `fill_layer` ignores `origin.getY()` entirely and derives its y from `level.getMinY()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In `fill_layer`, `state` is a plain `BlockState`, not a provider, so the feature consumes zero RNG draws in total -- there is no `Simple`/`Weighted` provider distinction to draw from. | `LayerConfiguration` (LayerConfiguration.java:11, :16) and `FillLayerFeature.place` (:27) -- `LayerConfiguration.state` is a plain `BlockState` (`BlockState.CODEC.fieldOf("state")`), not a `BlockStateProvider`; `level.setBlock(pos, config.state, 2)` writes that one fixed state. The feature consumes zero RNG draws in total | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| `bonus_chest` shuffles the chunk's own 16 x- and 16 z-coordinates (two shuffles, consuming RNG), writes the configured chest state at the first heightmap-derived column found empty or with an empty collision shape (not unconditionally at `origin`), draws one `next_long()` as that chest's loot-table seed, and places torches on qualifying horizontal neighbours -- never zero RNG draws. | `BonusChestFeature.place` (BonusChestFeature.java:26-60) -- The feature shuffles the chunk's 16 x-coordinates and 16 z-coordinates via `Util.toShuffledList(..., random)` (:30-31), consuming draws; walks that shuffled grid for the first `MOTION_BLOCKING_NO_LEAVES` heightmap position that is empty or has an empty collision shape (:42-43); writes a CHEST there -- not at `origin` -- sets the `SPAWN_BONUS_CHEST` loot table with the same random (:45); and writes up to four TORCH blocks on the horizontal neighbours that can support one (:48-53) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vanilla sandstone block (backing `SANDSTONE_STATE`) is claimed to have zero block-state properties. | `reports/blocks.json` -> `minecraft:sandstone`; `DesertWellFeature` field `sandstone` (DesertWellFeature.java:21) -- The block entry carries no `properties` key at all, so it has exactly one state, the default | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vanilla sandstone slab block (backing `SANDSTONE_SLAB_STATE`) carries two properties, `type` (`top`/`bottom`/`double`) and `waterlogged` (`true`/`false`); the constant pins the default state, `type = bottom, waterlogged = false`. | `reports/blocks.json` -> `minecraft:sandstone_slab`; `DesertWellFeature` field `sandSlab` (DesertWellFeature.java:20) -- `minecraft:sandstone_slab` has two properties: `type` with values top, bottom, double, and `waterlogged` with values true, false. Vanilla writes `Blocks.SANDSTONE_SLAB.defaultBlockState()`, i.e. type=bottom, waterlogged=false, so the constant must pin those two property values rather than be propertyless | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vanilla still-water source block (backing `WATER_SOURCE_STATE`) carries one property, `level` (0-15); the constant pins the default state, `level = 0`. | `reports/blocks.json` -> `minecraft:water`; `DesertWellFeature` field `water` (DesertWellFeature.java:22) -- `minecraft:water` has the property `level` with values 0 through 15. The source state is level=0, which is the block's default state, as written by `Blocks.WATER.defaultBlockState()` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vanilla magma block (backing `MAGMA_BLOCK_STATE`) is claimed to have zero block-state properties. | `reports/blocks.json` -> `minecraft:magma_block`; `UnderwaterMagmaFeature.place` (UnderwaterMagmaFeature.java:45) -- The block entry carries no `properties` key at all; vanilla writes `Blocks.MAGMA_BLOCK.defaultBlockState()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
