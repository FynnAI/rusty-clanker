# M5-B07 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| In vanilla, the nether dimension's worldgen uses the legacy random source (legacy_random_source: true) while the overworld uses the modern xoroshiro-based source (legacy_random_source: false). | data/minecraft/worldgen/noise_settings/nether.json `"legacy_random_source": true`; data/minecraft/worldgen/noise_settings/overworld.json `"legacy_random_source": false` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's real applyBiomeDecoration places any structures registered for a decoration step before that step's features, using the same decoration_seed/setFeatureSeed mechanism with the structures' own structure-scoped index space. | `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:343-400): inside each `stepIndex` iteration the structures loop runs first (:345-365) with its own counter `int index = 0` reset per step and incremented per structure, calling `random.setFeatureSeed(decorationSeed, index, stepIndex)` (:347); only then does the feature block run (:367-400) with `random.setFeatureSeed(decorationSeed, globalIndexOfFeature, stepIndex)` (:389). Same `decorationSeed`, same formula, separate index counters (note: the two counters overlap numerically — a structure and a feature in the same step can share a seed) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ore-vein generation (GEN-D16) is a density-function-integrated mechanism entirely distinct from, and unrelated to, the ore feature that placed features use. | `OreVeinifier.create` (OreVeinifier.java:24-60) returns a `NoiseChunk.BlockStateFiller` driven by the density functions `veinToggle`/`veinRidged`/`veinGap` from `NoiseRouter` (NoiseRouter.java:20, NoiseRouterData.java:354-375) plus a positional random factory; it is installed in `NoiseChunk` (NoiseChunk.java:163) during the `noise` ChunkStatus. `OreFeature` (OreFeature.java) is an unrelated `Feature` run in the `features` step. No shared code or data | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each biome's data/minecraft/worldgen/biome/*.json file carries a BiomeGenerationSettings.features field that is a plain variable-length List<HolderSet<PlacedFeature>> (vanilla guards every access with stepIndex < featuresInBiome.size()), with trailing empty steps trimmed in the datagen output (of 66 files, 57 have 11 entries, 5 have 10, 1 has 8, 1 has 1, 2 have 0) - so a [Vec<..>; 11] representation must pad any missing trailing steps with empty lists. | `BiomeGenerationSettings` stores `List<HolderSet<PlacedFeature>> features` decoded by `PlacedFeature.LIST_OF_LISTS_CODEC` = `...listOf()` (PlacedFeature.java:33-34, BiomeGenerationSettings.java:37 and :63) — a plain variable-length list, NOT a fixed 11-element array. `ChunkGenerator.applyBiomeDecoration` guards every access with `if (stepIndex < featuresInBiome.size())` (ChunkGenerator.java:372-373). In the datagen output trailing empty steps are trimmed: of the 66 biome files, 57 carry 11 entries, 5 carry 10, 1 carries 8, 1 carries 1 and 2 carry 0. An implementation typed as `[Vec<..>; 11]` must pad the missing trailing steps with empty lists rather than expect 11 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| There are 66 vanilla biome definition files under data/minecraft/worldgen/biome/*.json. | `ls data/minecraft/worldgen/biome/*.json` in the 26.2 datagen output returns exactly 66 files | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| FeatureSorter's global-index scan visits every biome in the biome source's own possible-biome order (for the overworld, the multi-noise parameter list's declaration order) - not the biome registry's registration order - and the Object2IntOpenHashMap inside FeatureSorter itself is never iterated; it is a pure computeIfAbsent memo over a separate counter. | `ChunkGenerator` builds the sorter from `List.copyOf(biomeSource.possibleBiomes())` (ChunkGenerator.java:96-98), and `BiomeSource.possibleBiomes` (BiomeSource.java:29-31) is `collectPossibleBiomes().distinct().collect(ImmutableSet.toImmutableSet())`; for the overworld `MultiNoiseBiomeSource.collectPossibleBiomes` (MultiNoiseBiomeSource.java:45-47) streams `parameters().values()` in the multi-noise parameter list's DECLARED order. The scan order is therefore the biome source's first-occurrence parameter-list order, not the biome registry's registration order. The second half is wrong too: `Object2IntOpenHashMap featureIndex` (FeatureSorter.java:34) is never iterated — it is only a `computeIfAbsent` memo over a separate `MutableInt nextFeatureIndex` counter (:35, :54); the ordering comes from the `TreeMap`/`TreeSet` comparator `(step, featureIndex)` (:42-43, :59, :66-68) and the reversed DFS post-order (:105) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Registry-report dumps assign protocol ids to biomes in registration order, so registry order is the same as the ascending protocol-id sequence. | `MappedRegistry.register` (MappedRegistry.java:114-117) assigns `int newId = this.byId.size()` on each registration, so ids ascend strictly in registration order; `RegistryDumpReport` (RegistryDumpReport.java:42-44) writes `registry.getId(value)` as each entry's `protocol_id`. Note: this makes the report's ascending protocol-id order equal to registration order, but that order is NOT what `FeatureSorter` scans (see the row above) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's decoration pass has exactly 11 fixed GenerationStep.Decoration steps, in this exact order: RawGeneration, Lakes, LocalModifications, UndergroundStructures, SurfaceStructures, Strongholds, UndergroundOres, UndergroundDecoration, FluidSprings, VegetalDecoration, TopLayerModification. | `GenerationStep.Decoration` (GenerationStep.java:11-22): RAW_GENERATION, LAKES, LOCAL_MODIFICATIONS, UNDERGROUND_STRUCTURES, SURFACE_STRUCTURES, STRONGHOLDS, UNDERGROUND_ORES, UNDERGROUND_DECORATION, FLUID_SPRINGS, VEGETAL_DECORATION, TOP_LAYER_MODIFICATION — 11 constants in exactly that declaration order | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Decoration processes these 11 steps in ascending ordinal order, for every chunk, unconditionally - never reordered and never skipped - with a step that has zero reachable features for a given chunk being a no-op pass rather than an absent one. | `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:341-343): `int generationSteps = Math.max(GenerationStep.Decoration.values().length, featureStepCount)` then `for (int stepIndex = 0; stepIndex < generationSteps; stepIndex++)` — ascending, unconditional, nothing skipped. A step whose `possibleFeaturesThisStep` set is empty simply runs a zero-iteration inner loop (:380-400) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Within one decoration step, placed features execute in ascending global-index order - never in JSON-declaration order directly, and never in per-biome-list order directly. | `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:380-390): the reachable features' global indices are collected into an `IntArraySet`, dumped to `int[] indexArray`, sorted with `Arrays.sort(indexArray)` (:382) and iterated ascending, each feature fetched by `stepFeatureData.features().get(globalIndexOfFeature)` (:388) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| FeatureSorter builds ONE shared feature-index counter, ONE cross-step edge graph over (featureIndex, step) nodes and ONE topological sort spanning all 11 steps at once; the sorted list is only filtered per step afterwards, and the per-step global index is the position within that filtered sublist - biome order is the biome source's own possible-biome order, not registry order. | `FeatureSorter.buildFeaturesPerStep` (FeatureSorter.java:31-115) is NOT per-step-independent: ONE `Object2IntOpenHashMap featureIndex` plus ONE `MutableInt nextFeatureIndex` (:34-35) number features across all steps and all biomes, ONE `edges` graph over `(featureIndex, step)` nodes is built (:43, :58-63), and ONE topological sort runs over that whole graph (:70-105). Only afterwards is the single sorted list FILTERED per step (`sortedFeatures.stream().filter(p -> p.step() == finalStep)`, :108-112), the per-step global index being the position within that filtered sublist. The biome order is the biome source's possible-biome order, not registry order (see row 6) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every distinct PlacedFeature value encountered anywhere across all 11 steps is assigned a scan index (0, 1, 2, ...) from a separate counter the first time it is seen; re-encountering an already-indexed value (shared by a later biome, listed again in a later step, or repeated within one biome's own list) reuses its existing scan index and adds no new one - the numbering is global across steps, and the Object2IntOpenHashMap involved is a pure computeIfAbsent memo, never itself iterated. | `FeatureSorter.buildFeaturesPerStep` (FeatureSorter.java:54): `featureIndex.computeIfAbsent(feature, f -> nextFeatureIndex.getAndIncrement())` — first-encounter numbering reused on re-encounter is right in substance, but three details differ: (a) the counter is a separate `MutableInt` and the `Object2IntOpenHashMap`'s own iteration order is never used anywhere (the map is a pure memo), so the attribution to insertion-order iteration semantics is wrong; (b) identity is the decoded `PlacedFeature` VALUE (record equality over feature holder plus placement list), not the `ResourceLocation` name; (c) the counter is shared across ALL steps, so the numbers are global-across-steps, never per-step | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Per biome, all 11 steps are flattened into one step-ordered feature list, and a directed edge is recorded between every pair of adjacent entries of that flattened list ("i must run before i+1"), so an edge can cross a step boundary from the last feature of one step to the first feature of the next; nodes are (featureIndex, step) pairs, so one feature listed in two steps is two distinct nodes, and duplicate edges recorded by more than one biome collapse into one via a TreeSet. | `FeatureSorter.buildFeaturesPerStep` (FeatureSorter.java:47-63): per biome, ALL steps are flattened into ONE `List<FeatureData> featureList` (the `for (int i = 0; i < featuresForStep.size(); i++)` loop appends every step's entries in step order, :51-56), and edges are added between consecutive entries of that flattened list (:58-63) — so an edge can cross a step boundary, linking the last feature of step i to the first feature of step i+1. Nodes are `(featureIndex, step)` pairs, so one feature listed in two steps is two distinct nodes. Duplicate edges do collapse: the successor set is a `TreeSet` (:59) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The final per-step global index assignment is produced by one DFS-based topological sort spanning all 11 steps at once: visiting every (featureIndex, step) node in a TreeMap's own step-major (step, featureIndex) order, recursively visiting each unvisited node's successors depth-first, appending each node to a "finished" list on post-order completion, then reversing the finished list once for the whole run; the per-step global index is the position of a node within that single reversed order's own step-filtered sublist. | `FeatureSorter.buildFeaturesPerStep` (FeatureSorter.java:70-105) iterates `edges.keySet()`, and `edges` is a `TreeMap` ordered by `Comparator.comparingInt(FeatureData::step).thenComparingInt(FeatureData::featureIndex)` (:42-43) — the root scan order is STEP-MAJOR then feature index, not plain ascending scan index. `Graph.depthFirstSearch` (Graph.java:12-35) appends each node on post-order completion with successors visited in the `TreeSet`'s own `(step, featureIndex)` order (FeatureSorter.java:59), and `Collections.reverse(sortedFeatures)` (:105) yields the reversed post-order; the per-step index is then the position within the step-filtered sublist (:108-112). Post-order-plus-reverse is right; the root iteration order and the per-step independence are not | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A cycle between two biomes' required orderings (e.g. biome X wants A before B while biome Y wants B before A) is a hard, immediate bootstrap error in vanilla, never resolved silently. | `FeatureSorter.buildFeaturesPerStep` (FeatureSorter.java:75-101): when `Graph.depthFirstSearch` reports a back edge it throws `IllegalStateException("Feature order cycle found")` immediately when `tryReducingError` is false, and with `tryReducingError` true (the production call, ChunkGenerator.java:97) it re-runs the sort over progressively reduced source lists purely to name the offending biomes and then still throws `IllegalStateException("Feature order cycle found, involved sources: ...")` (:101). Never silently resolved | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla constructs its decoration RNG carrier from a non-deterministic "unique seed" whose value is discarded the instant setDecorationSeed overwrites all state, i.e. the carrier is constructed with a dummy seed of 0L purely to satisfy the supertype. | `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:326): `new WorldgenRandom(new XoroshiroRandomSource(RandomSupport.generateUniqueSeed()))`, and `WorldgenRandom`'s constructor is `super(0L)` (WorldgenRandom.java:10-13) — the dummy 0L only satisfies the `LegacyRandomSource` supertype. `setDecorationSeed` (:44-51) calls `setSeed`, which delegates to the wrapped source (:38-42), discarding the unique seed. Note for §E.1: this carrier is ALWAYS Xoroshiro-backed, in every dimension — `legacy_random_source` selects the noise/surface random source, never the decoration carrier | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's setDecorationSeed call site is setDecorationSeed(level.getSeed(), sectionOrigin.x, sectionOrigin.z), where sectionOrigin.x/sectionOrigin.z are block-space coordinates of the chunk's minimum corner (i.e. chunk-grid coordinates multiplied by 16), not chunk-grid coordinates directly. | `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:321-327): `SectionPos sectionPos = SectionPos.of(centerPos, level.getMinSectionY()); BlockPos origin = sectionPos.origin(); ... random.setDecorationSeed(level.getSeed(), origin.getX(), origin.getZ())`. `SectionPos.origin` (SectionPos.java:200-202) is `sectionToBlockCoord(x/y/z)`, i.e. the section coordinate shifted left by 4 — block space, so X/Z are chunkX*16 and chunkZ*16 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's biome data is stored at "quart" granularity (PalettedContainer.getAll enumerates the distinct storage ids actually present via data.storage.getAll, mapped through palette.valueFor, not the raw palette list), and the possible-biome scan enumerates the distinct biome values actually present in every section of the 3x3-chunk neighbourhood (ChunkPos.rangeClosed(sectionPos.chunk(), 1)), not a per-quart-cell sample of a single chunk, then intersects the result with biomeSource.possibleBiomes(). | First half confirmed: `QuartPos.BITS = 2`/`SIZE = 4` (QuartPos.java:4-5) and `LevelChunkSection.BIOME_CONTAINER_BITS = 2` (LevelChunkSection.java:13) — biomes are a 4x4x4-cell paletted container per section. Second half wrong: `ChunkGenerator.applyBiomeDecoration` never samples per quart cell. It enumerates the PALETTE of every section of every chunk in `ChunkPos.rangeClosed(sectionPos.chunk(), 1)` — a 3x3-CHUNK neighbourhood — via `section.getBiomes().getAll(possibleBiomes::add)` (ChunkGenerator.java:328-335) and then intersects with `this.biomeSource.possibleBiomes()` (:336) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| GEN-D19 requires that a chunk spanning multiple biomes reach the union of every present biome's own decoration step lists, with each feature counted once regardless of how many present biomes list it. | `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:367-379): for every biome in `possibleBiomes` the step's `HolderSet` is mapped through `stepFeatureData.indexMapping()` into one `IntArraySet possibleFeaturesThisStep` — a set, so a feature listed by several present biomes is counted once. Two scope notes: the biome set spans the 3x3-chunk neighbourhood rather than only the centre chunk (:328-335), and it is intersected with `biomeSource.possibleBiomes()` (:336) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The starting origin for a placed feature's placement-modifier chain is BlockPos(chunk_x*16, Y, chunk_z*16), where X and Z are the chunk's own minimum corner but Y is the dimension's minimum build height (sectionToBlockCoord of the minimum section Y) - -64 in the overworld and 0 in the nether - never a literal Y=0, since using Y=0 in the overworld shifts every chain without a height_range/heightmap stage by 64 blocks. | `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:321-322 and :393): `origin = SectionPos.of(centerPos, level.getMinSectionY()).origin()`, passed to `feature.placeWithBiomeCheck(level, this, random, origin)`. X and Z are chunkX*16/chunkZ*16 as claimed, but Y is `sectionToBlockCoord(level.getMinSectionY())` (SectionPos.java:200-202) — the dimension's MINIMUM BUILD HEIGHT, i.e. -64 in the overworld and 0 in the nether, never a literal 0. Using Y=0 in the overworld shifts every chain without a `height_range`/`heightmap` stage by 64 blocks | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla calls setFeatureSeed(decoration_seed, global_index, step) exactly once per placed feature actually reached in a decoration step - never once per placement-modifier fan-out copy and never once per terminal Feature.place invocation - so every surviving position from that feature's own chain shares the one resulting RNG stream, which continues across all of that feature's placement attempts within one chunk. | `ChunkGenerator.applyBiomeDecoration` (ChunkGenerator.java:386-393): `setFeatureSeed` is called once per iteration of the per-feature loop, immediately before the single `feature.placeWithBiomeCheck(...)` call; `PlacedFeature.placeWithContext` (PlacedFeature.java:46-64) then threads that ONE `random` through every modifier stage and every surviving position's `feature.place(...)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's placement pipeline is a Java Stream<BlockPos> that starts as a single origin and is flatMap'd through each placement modifier in declared order, with the underlying Feature.place invoked once per position that survives the whole chain. | `PlacedFeature.placeWithContext` (PlacedFeature.java:46-64): `Stream<BlockPos> placements = Stream.of(origin); for (PlacementModifier m : this.placement) placements = placements.flatMap(p -> m.getPositions(context, random, p)); ... placements.forEach(pos -> feature.place(...))` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Java's Stream.flatMap is lazy and depth-first per element: a downstream stage pulls exactly one upstream element at a time and drives it all the way through every remaining stage (including the terminal Feature.place call) before the upstream is asked for its next element - so for a chain count(3) -> in_square -> height_range, the first of the 3 count-repeated positions runs through in_square, height_range, and Feature.place to full completion before the second count-repeated position's own in_square call ever begins. | Java SE `Stream.flatMap` semantics applied to PlacedFeature.java:47-59: with a `forEach` terminal the pipeline is push-based, so each source element is driven through every downstream stage (including `ConfiguredFeature.place`) before the next upstream element is produced. Each modifier's RNG draws happen inside `getPositions`, invoked at push time (e.g. `RepeatingPlacement.getPositions` samples its count eagerly, RepeatingPlacement.java:14-17), so the draw interleaving is exactly the depth-first, one-lineage-at-a-time order described | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Count{count} placement modifier samples n from its IntProvider with exactly one call and returns n identical, unmodified copies of the input position - it never itself jitters the position. | `CountPlacement.count` (CountPlacement.java:26-29) = `this.count.sample(random)`, one call; `RepeatingPlacement.getPositions` (RepeatingPlacement.java:14-17) = `IntStream.range(0, count(random, origin)).mapToObj(i -> origin)` — n identical, unmodified copies | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The RarityFilter{chance} placement modifier draws one random float and keeps the position iff that float is less than the rounded float reciprocal 1.0/chance (a single next_float draw against a float threshold, never a bounded-int draw compared to 0), consuming zero further draws either way. | `RarityFilter.shouldPlace` (RarityFilter.java:20-23) is `return random.nextFloat() < 1.0F / this.chance;` — ONE `nextFloat` draw compared against the float reciprocal, never `nextInt(chance) == 0`. The two consume different RNG (a float draw versus a bounded-int draw) and are not distributionally identical either, since `1.0F / chance` is a rounded float. `chance` is `ExtraCodecs.POSITIVE_INT` (:9) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The InSquare{} placement modifier draws a bounded random int in [0,16) for dx, then another for dz (two draws, X strictly before Z), and returns the position offset by (dx, 0, dz) - Y is never changed and it never filters (always 1-to-1). | `InSquarePlacement.getPositions` (InSquarePlacement.java:19-24): `int x = random.nextInt(16) + origin.getX(); int z = random.nextInt(16) + origin.getZ(); return Stream.of(new BlockPos(x, origin.getY(), z));` — X draw first, then Z, Y unchanged, always exactly one output | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The HeightRange{height} placement modifier samples Y from its HeightProvider and returns the position with X/Z unchanged - always 1-to-1, never filtering. | `HeightRangePlacement.getPositions` (HeightRangePlacement.java:35-38): `return Stream.of(origin.atY(this.height.sample(random, context)));` — X/Z preserved, always exactly one output | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Heightmap{heightmap} placement modifier consumes zero RNG and reads whatever Heightmap.Types the JSON names (the pinned data uses the non-_Wg variants most often: MOTION_BLOCKING 46x, OCEAN_FLOOR 25x, WORLD_SURFACE_WG 18x, OCEAN_FLOOR_WG 16x, MOTION_BLOCKING_NO_LEAVES 3x), and it also FILTERS - returning an empty stream when the resolved height is at or below the world's minimum Y - so it is not always a 1-to-1 pass-through. | `HeightmapPlacement.getPositions` (HeightmapPlacement.java:24-30) consumes zero RNG (confirmed) but reads whatever `Heightmap.Types` the JSON names, and the pinned data uses the non-`_Wg` variants more than the `_Wg` ones: across data/minecraft/worldgen/placed_feature/**, the `minecraft:heightmap` modifier appears with MOTION_BLOCKING 46x, OCEAN_FLOOR 25x, WORLD_SURFACE_WG 18x, OCEAN_FLOOR_WG 16x, MOTION_BLOCKING_NO_LEAVES 3x. `Heightmap.Types` (Heightmap.java:146-157) marks only WORLD_SURFACE_WG and OCEAN_FLOOR_WG as `Usage.WORLDGEN`. Also missing from §G.5: the modifier FILTERS, returning an empty stream when `height <= context.getMinY()` (:29), so it is not always 1-to-1 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Biome{} placement modifier consumes zero RNG and keeps the position only if the biome actually present at the current candidate position lists the outermost placed feature that started the current placement chain (not necessarily the feature currently being evaluated) somewhere in that biome's own feature set flattened across all 11 decoration steps (never only the current step's own list) - this is vanilla's mechanism for sharing one PlacedFeature across biomes with different feature lists correctly. | `BiomeFilter.shouldPlace` (BiomeFilter.java:20-26) is zero-RNG and does re-sample `context.getLevel().getBiome(origin)` at the current candidate, but the membership test is `context.generator().getBiomeGenerationSettings(biome).hasFeature(feature)`, and `BiomeGenerationSettings.hasFeature` (BiomeGenerationSettings.java:52 and :67-69) tests a `featureSet` that FLATTENS every step's `HolderSet` into one `Set<PlacedFeature>` — membership in the biome's whole feature set across all 11 steps, not in that biome's list for the current step. The feature tested is `context.topFeature()` (:22-23), the outermost placed feature, which for a nested placement is not the feature currently being placed | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The BlockPredicateFilter{predicate} placement modifier consumes zero RNG and evaluates its predicate against the world's CURRENT block state (i.e. it can observe blocks a different, already-processed feature in the same decoration pass has just placed). | `BlockPredicateFilter.shouldPlace` (BlockPredicateFilter.java:23-26) = `this.predicate.test(context.getLevel(), origin)`; `BlockPredicate extends BiPredicate<WorldGenLevel, BlockPos>` (BlockPredicate.java:21) — no `RandomSource` parameter anywhere, and the read goes to the live `WorldGenLevel`, so earlier features' writes in the same pass are visible | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The SurfaceWaterDepthFilter{max_water_depth} placement modifier consumes zero RNG and does no block read or downward walk at all: it computes the difference of two heightmaps (WorldSurface minus OceanFloor, both non-_Wg) at the candidate's own column, ignoring the candidate's Y entirely, and keeps the position iff that difference is less than or equal to max_water_depth. | `SurfaceWaterDepthFilter.shouldPlace` (SurfaceWaterDepthFilter.java:24-29) does no downward walk and reads no block state: `int yOceanFloor = context.getHeight(Heightmap.Types.OCEAN_FLOOR, x, z); int ySurfaceFloor = context.getHeight(Heightmap.Types.WORLD_SURFACE, x, z); return ySurfaceFloor - yOceanFloor <= this.maxWaterDepth;` — the difference of two heightmaps (both non-`_Wg`) at the candidate's own column, ignoring the candidate's Y entirely. Zero RNG is correct | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The SurfaceRelativeThresholdFilter{heightmap, min_inclusive, max_inclusive} placement modifier consumes zero RNG and keeps the position iff (position.y - heightmap_y_at_that_column) falls within [min_inclusive, max_inclusive] (defaulting to i32::MIN/i32::MAX when either bound is absent). | `SurfaceRelativeThresholdFilter.shouldPlace` (SurfaceRelativeThresholdFilter.java:33-39): `long surfaceY = context.getHeight(this.heightmap, x, z); long minY = surfaceY + minInclusive; long maxY = surfaceY + maxInclusive; return minY <= origin.getY() && origin.getY() <= maxY;` with codec defaults `Integer.MIN_VALUE`/`Integer.MAX_VALUE` (:14-15). Zero RNG. Note: vanilla widens to `long` before adding, so an i32 implementation must do the same to avoid overflow at the default bounds | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The EnvironmentScan{direction_of_search, target_condition, allowed_search_condition, max_steps} placement modifier consumes zero RNG and walks up to max_steps positions from the candidate along the up or down direction. | `EnvironmentScanPlacement.getPositions` (EnvironmentScanPlacement.java:47-71) takes no RNG draw and steps `pos.move(this.directionOfSearch)` at most `maxSteps` times; `direction_of_search` is `Direction.VERTICAL_CODEC` (:20), so up or down only, and `max_steps` is `Codec.intRange(1, 32)` (:23) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| EnvironmentScan stops and outputs the first position, within its scan loop, where target_condition evaluates true; target_condition is a required field that can never be absent (it is allowed_search_condition, not target_condition, that is the optional field, defaulting to always-true). | `target_condition` is a REQUIRED field — `BlockPredicate.CODEC.fieldOf("target_condition")` (EnvironmentScanPlacement.java:21) — so it can never be absent; it is `allowed_search_condition` that is optional, defaulting to `BlockPredicate.alwaysTrue()` (:22). The rest holds: the loop returns `Stream.of(pos)` at the first position where `targetCondition.test` succeeds (:55-58) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| EnvironmentScan aborts with no output only if allowed_search_condition evaluates false at the unmoved origin before the scan loop even starts; a failure at a stepped-to position during the loop merely stops the loop rather than aborting, after which target_condition is still tested once more at that same position and returned if it succeeds; stepping to a position outside the world's build height is a separate, distinct no-output path. | `EnvironmentScanPlacement.getPositions` (EnvironmentScanPlacement.java:47-71): a failing `allowedSearchCondition` at the ORIGIN, before the loop, returns `Stream.of()` (:51-53); but a failure at a stepped-to position only `break`s the loop (:65-67), after which line 70 still evaluates `this.targetCondition.test(level, pos)` at that same position and returns it when it succeeds. A separate empty-return path the claim omits: stepping outside build height returns `Stream.of()` (:61-63) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| EnvironmentScan yields no output if max_steps is exhausted without target_condition ever succeeding. | `EnvironmentScanPlacement.getPositions` (EnvironmentScanPlacement.java:55-70): when the `maxSteps` loop completes without a target hit, line 70 returns `Stream.of()`. Note for §G.10: vanilla performs `maxSteps + 1` target tests in the exhaustion case — one per loop iteration plus the trailing test at :70 on the position after the last move — so an implementation that stops after exactly `maxSteps` tests can drop a position vanilla would return | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The RandomOffset{xz_spread, y_spread} placement modifier draws dx from xz_spread, then dy from y_spread, then dz from the same xz_spread provider (two independent draws of xz_spread) - three draws total, in the order X then Y then Z, never X then Z then Y. | `RandomOffsetPlacement.getPositions` (RandomOffsetPlacement.java:45-51): `int scatterX = origin.getX() + this.xzSpread.sample(random); int scatterY = origin.getY() + this.ySpread.sample(random); int scatterZ = origin.getZ() + this.xzSpread.sample(random);` — the draw order is X (xz_spread), then Y (y_spread), then Z (xz_spread). Three draws total, but X then Y then Z, not X then Z then Y | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The NoiseBasedCount{noise_to_count_ratio, noise_factor, noise_offset} placement modifier consumes zero draws from the WorldgenRandom stream itself - it samples a fixed, process-shared PerlinSimplexNoise (vanilla's own Biome.BIOME_INFO_NOISE, built from a legacy-seeded RandomSource with a single octave, never a NormalNoise seeded from a "decoration" hash) at the candidate's (x,z) DIVIDED by noise_factor (never multiplied), with no max(0) clamp of its own, and the sampled count fans out that many identical copies of the position exactly as Count does. | `NoiseBasedCountPlacement.count` (NoiseBasedCountPlacement.java:33-37): `double flowerNoise = Biome.BIOME_INFO_NOISE.getValue(origin.getX() / this.noiseFactor, origin.getZ() / this.noiseFactor, false); return (int)Math.ceil((flowerNoise + this.noiseOffset) * this.noiseToCountRatio);`. Zero `WorldgenRandom` draws is right and it does fan out through `RepeatingPlacement` exactly as `Count` does, but: the coordinates are DIVIDED by `noise_factor`, not multiplied; the noise is `Biome.BIOME_INFO_NOISE`, a `PerlinSimplexNoise` built from `new WorldgenRandom(new LegacyRandomSource(2345L))` with octaves `[0]` (Biome.java:66), not a `NormalNoise` seeded from a "decoration" hash; `noise_to_count_ratio` is an INT and `noise_offset` an optional double defaulting to 0.0 (:13-15); and there is no `max(0)` clamp — a negative count simply yields an empty `IntStream.range` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The NoiseThresholdCount{noise_level, below_noise, above_noise} placement modifier uses the same shared noise sample as NoiseBasedCount, but scaled by a hardcoded 200.0 divisor rather than noise_factor (so the two modifiers sample the same noise field at different scales and must never share a cached sample), and selects between the literal below_noise/above_noise counts depending on whether the sample is below noise_level, also consuming zero WorldgenRandom-stream draws. | `NoiseThresholdCountPlacement.count` (NoiseThresholdCountPlacement.java:33-37): `Biome.BIOME_INFO_NOISE.getValue(origin.getX() / 200.0, origin.getZ() / 200.0, false)`. The same shared noise, the zero `WorldgenRandom` draws and the below/above selection are right, but the coordinates are divided by a hardcoded 200.0 — there is no unscaled sampling. Caching one shared sample across the two modifiers would also be wrong: they sample the same noise at different scales | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The CountOnEveryLayer{count} placement modifier is a do/while loop over an ascending layer whose inner loop re-samples count's own IntProvider on every evaluation of the loop condition and, per iteration, draws two bounded random ints (one for x, one for z) that scatter each candidate across the full 16x16 column rather than fixing it at the candidate's own (x,z); the placement Y for each candidate is then found by walking down from the column's MOTION_BLOCKING-heightmap height counting empty-over-non-empty transitions (air/water/lava, excluding bedrock) until the target layer is reached - so, unlike NoiseBasedCount/NoiseThresholdCount, this modifier does consume RNG. | `CountOnEveryLayerPlacement.getPositions` (CountOnEveryLayerPlacement.java:34-58) is a different algorithm and it DOES consume RNG: a `do { ... } while (foundAny)` loop over ascending `layer`, whose inner `for (int i = 0; i < this.count.sample(random); i++)` re-samples the `IntProvider` on EVERY evaluation of the loop condition, and each iteration draws `random.nextInt(16)` for x and again for z (:44-45) — so positions are scattered across the 16x16 column, not fixed at the candidate's (x,z). `findOnGroundYPosition` (:65-85) then walks DOWN from the MOTION_BLOCKING height counting `isEmpty`-over-non-`isEmpty` transitions (`isEmpty` = air or water or lava, :87-89; bedrock excluded) until it reaches `layerToPlaceOn`. The class is `@Deprecated` (:15) and is used 8 times in the pinned placed_feature data | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The FixedPlacement{positions} placement modifier consumes zero RNG and uses absolute world coordinates, but does not ignore the input position: it returns no output unless at least one entry of positions lies in the same chunk as the input position, in which case it returns only the entries that lie in that chunk - vanilla's real-world use of this kind is narrow, e.g. bonus_chest. | `FixedPlacement.getPositions` (FixedPlacement.java:25-39): zero RNG is right, but the input position is NOT ignored — the origin's chunk coordinates are computed (:27-28), the method returns `Stream.empty()` unless at least one configured position lies in that same chunk (:29-38), and it then returns only the positions in that chunk (`filter(pos -> isSameChunk(...))`, :38). The positions are absolute `BlockPos.CODEC` values (:13), as claimed | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The 15th placement-modifier kind in vanilla's confirmed schema is fixed_placement, not carving_mask - no carving_mask kind exists among the 15 confirmed placement-modifier kinds. | reports/registries.json, `minecraft:worldgen/placement_modifier_type`: exactly 15 entries — block_predicate_filter, rarity_filter, surface_relative_threshold_filter, surface_water_depth_filter, biome, count, noise_based_count, noise_threshold_count, count_on_every_layer, environment_scan, heightmap, height_range, in_square, random_offset, fixed_placement. No `carving_mask` entry, and net/minecraft/world/level/levelgen/placement/ contains no such class | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| UniformInt.sample in vanilla is random.nextInt(max-min+1)+min. | `UniformInt.sample` (UniformInt.java:31-33) delegates to `Mth.randomBetweenInclusive` (Mth.java:693-695) = `random.nextInt(maxInclusive - min + 1) + min` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's IntProvider Constant(n) kind returns its fixed value n with zero random draws. | `ConstantInt.sample` (ConstantInt.java:22-24) returns `this.value` and never touches the `random` argument | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's HeightProvider has exactly six confirmed kinds: Constant, Uniform, BiasedToBottom, VeryBiasedToBottom, Trapezoid, and WeightedList. | reports/registries.json, `minecraft:height_provider_type`: exactly 6 entries in protocol-id order — constant, uniform, biased_to_bottom, very_biased_to_bottom, trapezoid, weighted_list; matching `HeightProviderType` (HeightProviderType.java:8-13) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| HeightProvider::Constant{value} resolves via resolve_vertical_anchor with zero RNG draws; Uniform{min,max} samples via one next_int_between_inclusive draw. | `ConstantHeight.sample` (ConstantHeight.java:26-28) = `this.value.resolveY(context)`, zero draws, JSON key `value` (:10). `UniformHeight.sample` (UniformHeight.java:36-49) = `Mth.randomBetweenInclusive(random, min, max)` — one draw — after resolving both anchors; the JSON keys are `min_inclusive`/`max_inclusive` (:17-18), and an inverted range returns `min` with NO draw (:40-45) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| HeightProvider::BiasedToBottom{min,max,inner} skews its sampled value toward min. | `BiasedToBottomHeight.sample` (BiasedToBottomHeight.java:37-46): `int limit = random.nextInt(max - min - this.inner + 1); return random.nextInt(limit + this.inner) + min;` — two draws, the second bounded by the first, biasing the result toward `min`. Note for §H.2: the degenerate guard is `if (max - min - inner + 1 <= 0) return min;` with ZERO draws (:40-43), not a `.max(1)` clamp that still draws; `inner` is optional with default 1 (:17) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| HeightProvider::VeryBiasedToBottom{min,max,inner} skews its sampled value toward min more strongly than BiasedToBottom does. | `VeryBiasedToBottomHeight.sample` (VeryBiasedToBottomHeight.java:37-50): `int upperInclusive = Mth.nextInt(random, min + inner, max); int biasedUpperInclusive = Mth.nextInt(random, min, upperInclusive - 1); return Mth.nextInt(random, min, biasedUpperInclusive - 1 + inner);` — three nested draws, each re-bounding the next toward `min`, so the bias is strictly stronger than `BiasedToBottom`'s two-draw form. Note for §H.2: the exact formula differs from the blueprint's reconstruction (three `Mth.nextInt` range draws, and the `-1` before `+ inner` in the final bound), and the empty-range guard returns `min` with zero draws (:41-44) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| HeightProvider::Trapezoid{min,max,plateau} produces a triangular/trapezoidal distribution skewed toward the middle of its range. | `TrapezoidHeight.sample` (TrapezoidHeight.java:41-58): `int range = max - min; if (plateau >= range) return Mth.randomBetweenInclusive(random, min, max); int plateauStart = (range - plateau) / 2; int plateauEnd = range - plateauStart; return min + Mth.randomBetweenInclusive(random, 0, plateauEnd) + Mth.randomBetweenInclusive(random, 0, plateauStart);` — a sum of two uniforms, i.e. a triangular/trapezoidal peak in the middle. Note for §H.2: the two draws carry DIFFERENT bounds (`plateauEnd` and `plateauStart`), not the blueprint's identical `half+1` pair; there is no `+ plateau/2` term; and `plateau >= range` collapses to a single uniform draw | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| HeightProvider::WeightedList{distribution} selects one nested HeightProvider via cumulative-weight selection and then delegates sampling to that chosen provider. | `WeightedListHeight.sample` (WeightedListHeight.java:20-22) = `this.distribution.getRandomOrThrow(random).sample(random, heightAccessor)`; `WeightedList.getRandomOrThrow` (WeightedList.java:85-92) takes one `random.nextInt(this.totalWeight)` and resolves it through the selector, whose `Compact` form (WeightedList.java:174-190) subtracts each entry's weight in list order until the running value goes negative — cumulative-weight selection. The chosen provider's own `sample` then runs, consuming its own draws | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| resolve_vertical_anchor resolves Absolute{absolute} to absolute itself, AboveBottom{above_bottom} to WORLD_MIN_Y + above_bottom, and BelowTop{below_top} to WORLD_MIN_Y + WORLD_HEIGHT - 1 - below_top. | `VerticalAnchor` (VerticalAnchor.java:46-104): `Absolute.resolveY` returns `this.y` (:75-78, JSON key `absolute`); `AboveBottom.resolveY` returns `heightAccessor.getMinGenY() + this.offset` (:55-58, key `above_bottom`); `BelowTop.resolveY` returns `heightAccessor.getGenDepth() - 1 + heightAccessor.getMinGenY() - this.offset` (:95-98, key `below_top`). Note: the min and depth come from the `WorldGenerationContext` (per-dimension generator values), not from fixed constants | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's BlockStateProvider includes at least a SimpleStateProvider kind (zero RNG draws) and a WeightedStateProvider kind (one draw, cumulative-weight selection), plus further kinds named noise_provider, dual_noise_provider, and rotated_block_provider. | reports/registries.json, `minecraft:worldgen/block_state_provider_type`: 8 entries — simple_state_provider, weighted_state_provider, noise_threshold_provider, noise_provider, dual_noise_provider, rotated_block_provider, randomized_int_state_provider, rule_based_state_provider. `SimpleStateProvider.getState` (SimpleStateProvider.java:23-25) returns the fixed state with no draw; `WeightedStateProvider.getState` (WeightedStateProvider.java:35-37) = `this.weightedList.getRandomOrThrow(random)` — one draw, cumulative-weight selection (WeightedList.java:85-92 and :174-190). Three further kinds the blueprint does not name: noise_threshold_provider, randomized_int_state_provider, rule_based_state_provider | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's IntProvider registry includes further kinds beyond Constant/Uniform: clamped, clamped_normal, weighted_list, and a biased_to_bottom int-provider variant distinct from the height-provider kind of the same name. | reports/registries.json, `minecraft:int_provider_type`: 7 entries — constant, uniform, biased_to_bottom, clamped, weighted_list, clamped_normal, trapezoid. All four named kinds are present, and `biased_to_bottom` is a genuinely distinct int-provider registration (BiasedToBottomInt.java) from the height-provider kind of the same name (BiasedToBottomHeight.java). One further kind beyond the list: `trapezoid` (TrapezoidInt.java), used by `random_offset` (RandomOffsetPlacement.java:28-29) and 137 times in the pinned placed_feature data | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's minecraft:block_predicate_type registry includes at least these kinds: all_of, any_of, not, true, has_sturdy_face, inside_world_bounds, matching_blocks, matching_block_tag, matching_fluids, would_survive, replaceable, and solid. | reports/registries.json, `minecraft:block_predicate_type`: 14 entries — matching_blocks, matching_block_tag, matching_fluids, matching_biomes, has_sturdy_face, solid, replaceable, would_survive, inside_world_bounds, any_of, all_of, not, true, unobstructed. All 12 named kinds are present; the two the blueprint's `BlockPredicate` enum does not name are `matching_biomes` and `unobstructed`, which would fall into a `#[serde(other)]` arm | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every minecraft:block_predicate_type kind is a pure function of current world state, consuming zero RNG draws. | `BlockPredicate extends BiPredicate<WorldGenLevel, BlockPos>` (BlockPredicate.java:21) — the test signature carries no `RandomSource`, so no kind can draw; every implementation in net/minecraft/world/level/levelgen/blockpredicates/ tests only level state at `origin.offset(offset)` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The would_survive block predicate evaluates whether the given block state could survive being placed at the position, given the current world state around it (e.g. requiring a solid block below). | `WouldSurvivePredicate.test` (WouldSurvivePredicate.java:25-27) = `this.state.canSurvive(level, origin.offset(this.offset))`, with codec fields `offset` (optional, `Vec3i.ZERO`) and `state` (:11-16) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's decoration pass for one chunk writes into a small, fixed radius around itself that overlaps its neighboring chunks' own decoration windows, and because some placement-modifier filters (e.g. BlockPredicateFilter) and some terminal features read currently-placed block state as a placement precondition, which of two overlapping chunks' decoration passes runs first can affect the exact outcome at the seam - this is the one documented, bounded parity exception GEN-D1 permits. | Both halves of the mechanism confirmed: `ChunkPyramid.GENERATION_PYRAMID` gives `ChunkStatus.FEATURES` `.blockStateWriteRadius(1)` (ChunkPyramid.java:31-37), so a chunk's feature pass may write into its 8 neighbours, and `BlockPredicateFilter` (BlockPredicateFilter.java:23-26) plus terminal features such as `SimpleBlockFeature` (`stateToPlace.canSurvive`, SimpleBlockFeature.java:26) and `SpringFeature` (SpringFeature.java:19-30) read the live `WorldGenLevel` as a placement precondition, so a neighbour's already-written blocks change the outcome at the seam. The clause naming this the one exception GEN-D1 permits is a project decision (04-worldgen-parity.md), not a fact the reference can settle | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A chunk being decorated as the "center" may write blocks into any of the 3x3 chunks centered on it (up to 1 chunk of overflow in any horizontal direction), matching vanilla's own FEATURES-status write margin. | `ChunkPyramid.GENERATION_PYRAMID` (ChunkPyramid.java:31-37): the FEATURES step declares `.blockStateWriteRadius(1)` — one chunk of horizontal overflow in every direction, i.e. the 3x3 window centred on the chunk being decorated. Every other generation step declares `blockStateWriteRadius(0)` (:20, :27, :30) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ChunkStatus ladder runs carvers -> features -> initialize_light -> light, i.e. InitializeLight/Light are the two GenStage steps immediately after features. | `ChunkStatus` (ChunkStatus.java:27-30): CARVERS (parent SURFACE), FEATURES (parent CARVERS), INITIALIZE_LIGHT (parent FEATURES), LIGHT (parent INITIALIZE_LIGHT) — exactly that order, with SPAWN and FULL after (:31-32) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Feature registry contains exactly 63 distinct feature kinds in total. | reports/registries.json, `minecraft:worldgen/feature`: exactly 63 entries | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's ore feature algorithm (OreConfiguration: targets, size, discard_chance_on_air_exposure) is a randomized-angle line-blob: it first aborts unless some probe column in the line's own footprint reaches the OCEAN_FLOOR_WG heightmap at or above the origin's Y; it draws one random angle in [0,PI), computes half-size line endpoints via sin/cos of that angle scaled by size/8.0, draws two separate Y jitter values each in {-2,-1,0} via next_int_bounded(3)-2, then for each of size interpolation steps along that line draws an extra random double to build a sphere whose radius never tapers to zero (always >= 0.5); spheres fully contained inside another sphere are culled (radius set to -1 and skipped); the surviving spheres' candidate blocks are then filled via a plain (non-Y-compressed) sphere volume test, each candidate block visited at most once across all spheres. | `OreFeature.place`/`doPlace` (OreFeature.java:22-166). Confirmed: `float dir = random.nextFloat() * (float)Math.PI` (:28), `spreadXY = config.size / 8.0F` (:29), the four `origin +/- sin(dir)*spreadXY` and `+/- cos(dir)*spreadXY` endpoints (:31-34), the `Mth.lerp(step, ...)` interpolation over `size` steps (:77-81), and the config field names `targets`/`size`/`discard_chance_on_air_exposure` (OreConfiguration.java:11-18). Wrong on four points: (a) the Y jitter is `origin.getY() + random.nextInt(3) - 2` (:36-37), i.e. offsets in {-2,-1,0}, not {-1,0,1}; (b) each interpolation step takes an EXTRA draw, `double ss = random.nextDouble() * size / 16.0`, and the radius is `r = ((Mth.sin((float)Math.PI * step) + 1.0F) * ss + 1.0) / 2.0` (:82-83) — never `(size/8.0) * abs(sin(PI*t))`, and it does not taper to zero (the `+ 1.0) / 2.0` keeps r >= 0.5); (c) a bubble-culling pass then sets the radius of any sphere contained inside another to -1, skipping it entirely (:90-108); (d) the per-step volume test is a plain SPHERE, `xd*xd + yd*yd + zd*zd < 1.0` with `xd = (x + 0.5 - xx) / r` and so on (:124-132) — no 2x Y compression — and each candidate is visited at most once through a `BitSet` (:72, :133-135). There is also a pre-pass that aborts the whole feature unless some probe column satisfies `yStart <= level.getHeight(OCEAN_FLOOR_WG, ...)` (:44-50), and `targetState.target.test(state, random)` (:176) can itself draw for RuleTest kinds such as `random_block_match` | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In vanilla's ore feature, the discard-chance-on-air-exposure roll (next_float() >= discard_chance_on_air_exposure) is drawn FIRST, for every target-matching candidate block, whenever discard_chance_on_air_exposure is strictly between 0.0 and 1.0; whether the block is actually exposed to air on any of its 6 face-adjacent neighbors is checked only when that roll did not already decide to skip the check, and there is no draw at all when discard_chance_on_air_exposure is <= 0.0 (always place) or >= 1.0 (place only when not air-adjacent). | `OreFeature.canPlaceOre`/`shouldSkipAirCheck` (OreFeature.java:168-189) do the opposite: for every candidate that passes the target test, `shouldSkipAirCheck` draws `random.nextFloat() >= discardChanceOnAirExposure` FIRST (:187) and only runs `isAdjacentToAir` when that draw fails to skip (:179). The draw is therefore unconditional per target-matching candidate whenever `0 < discard_chance_on_air_exposure < 1`, and is skipped entirely (no draw) when the chance is `<= 0.0F` (:184-185) or `>= 1.0F` (:187). Gating the draw on actual air exposure desynchronises the stream | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's disk feature fills a circular horizontal cross-section (positions where dx*dx+dz*dz <= radius*radius) spanning a vertical range from -half_height to +half_height around the origin's Y level. | `DiskFeature.place` (DiskFeature.java:15-37): `int top = originY + config.halfHeight(); int bottom = originY - config.halfHeight() - 1;` with `placeColumn` looping `for (int y = top; y > bottom; y--)` (:45) — inclusive over [originY - half_height, originY + half_height]; the horizontal test is `xd * xd + zd * zd <= r * r` (:31) across `BlockPos.betweenClosed(origin.offset(-r,0,-r), origin.offset(r,0,r))` (:28). Note: columns are walked TOP-DOWN, which matters because `markAboveForPostProcessing` is driven by a run-tracking flag (:43, :51-59) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's disk feature (DiskConfiguration: state_provider, target, radius, half_height) samples a fresh block-state from state_provider once per matched position within the disk, not once for the whole disk. | `DiskFeature.placeColumn` (DiskFeature.java:45-61): `config.stateProvider().getOptionalState(level, random, pos)` is called inside the per-position loop, once per position whose `config.target().test(level, pos)` passes, while `int r = config.radius().sample(random)` is sampled once for the whole disk (:25). Note for §N.2: `target` is a `BlockPredicate`, not a `RuleTest` (DiskConfiguration.java:14), `radius` is bounded 0..8 and `half_height` 0..4 (:15-16), and `getOptionalState` may return null, in which case nothing is placed at that position | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's spring_feature (SpringConfiguration: state, requires_block_below [optional, default true], rock_count, hole_count, valid_blocks) is a zero-RNG deterministic validity check: the block above the origin must be in valid_blocks; if requires_block_below, the block below the origin must ALSO be in valid_blocks (not merely solid); the origin block itself must be air or in valid_blocks; among the west/east/north/south/below face-adjacent neighbors (never above, five neighbours), the count matching valid_blocks must equal rock_count exactly and the count that is air-or-replaceable must equal hole_count exactly (an equality test, not "at least"); state is a fluid state, and successful placement also schedules a fluid tick at the origin. | `SpringFeature.place` (SpringFeature.java:14-82). Zero RNG is right and the config field names match (SpringConfiguration.java:12-21), but five things differ: (a) the first check is that the block ABOVE the origin is in `valid_blocks` (:19-21), which the blueprint has no counterpart for; (b) `requires_block_below` requires the block below to be in `valid_blocks`, not merely "solid" (:23-25), and its codec default is `true` (SpringConfiguration.java:15); (c) the origin block itself must be air or in `valid_blocks` (:27-30); (d) the rock and hole scans cover west/east/north/south plus BELOW — five neighbours, never the position above (:34-73) — and holes are counted with `level.isEmptyBlock`; (e) the final test is EQUALITY, `rockCount == config.rockCount && holeCount == config.holeCount` (:75), not "at least". Placement is `setBlock(origin, config.state.createLegacyBlock(), 2)` followed by `level.scheduleTick(origin, config.state.getType(), 0)` (:76-77), and `state` is a `FluidState` (SpringConfiguration.java:14) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's real lake feature algorithm uses a per-Y-layer precomputed bubble-grid shape, distinct from a simple fixed-radius ellipsoid. | `LakeFeature.place` (LakeFeature.java:23-152): the shape is precomputed into `boolean[] grid = new boolean[2048]` by `random.nextInt(4) + 4` randomly sized and positioned ellipsoids (:34-58), then reused for the fluid fill (:93-109), the barrier shell (:111-137) and an ice cap (:139-149) — decisively not a fixed-radius ellipsoid. One precision correction: the grid is a single 16x16x8 array indexed `(xx * 16 + zz) * 8 + yy` (:53), not a per-Y-layer structure. Further omissions from §N.4: the origin is first offset by (-8,-4,-8) (:33), a full validity pre-pass can abort the whole feature (:62-91), Y < 4 receives fluid while Y >= 4 receives CAVE_AIR (:99-100), and the barrier pass takes one `random.nextInt(2)` per shell cell above Y=4 (:125) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla has exactly 9 trunk-placer kinds: straight_trunk_placer, bending_trunk_placer, forking_trunk_placer, giant_trunk_placer, mega_jungle_trunk_placer, dark_oak_trunk_placer, fancy_trunk_placer, upwards_branching_trunk_placer, cherry_trunk_placer. | reports/registries.json, `minecraft:worldgen/trunk_placer_type`: 9 entries, not 8 — straight_trunk_placer, forking_trunk_placer, giant_trunk_placer, mega_jungle_trunk_placer, dark_oak_trunk_placer, fancy_trunk_placer, bending_trunk_placer, upwards_branching_trunk_placer, cherry_trunk_placer. The blueprint's list omits `upwards_branching_trunk_placer` (UpwardsBranchingTrunkPlacer.java), so the deferred count is 7, not 6 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla has exactly 11 foliage-placer kinds: blob_foliage_placer, spruce_foliage_placer, pine_foliage_placer, acacia_foliage_placer, bush_foliage_placer, fancy_foliage_placer, jungle_foliage_placer, mega_pine_foliage_placer, dark_oak_foliage_placer, random_spread_foliage_placer, cherry_foliage_placer. | reports/registries.json, `minecraft:worldgen/foliage_placer_type`: 11 entries, not 10 — blob_foliage_placer, spruce_foliage_placer, pine_foliage_placer, acacia_foliage_placer, bush_foliage_placer, fancy_foliage_placer, jungle_foliage_placer, mega_pine_foliage_placer, dark_oak_foliage_placer, random_spread_foliage_placer, cherry_foliage_placer. The blueprint's list omits `random_spread_foliage_placer`, so the deferred count is 9, not 8 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Both the straight_trunk_placer and bending_trunk_placer share vanilla's own canonical tree-height draw formula: height = base_height + random.next_int_bounded(height_rand_a + 1) + random.next_int_bounded(height_rand_b + 1), drawing height_rand_a's bound before height_rand_b's (two draws total). | `TrunkPlacer.getTreeHeight` (TrunkPlacer.java:58-60) = `this.baseHeight + random.nextInt(this.heightRandA + 1) + random.nextInt(this.heightRandB + 1)` — two draws, A's bound first. It is defined on the base class, so all NINE trunk placers share it, not only straight and bending; `TreeFeature` calls it first of all (TreeFeature.java:65), before `foliageHeight` (:66) and `foliageRadius` (:68). Codec keys are `base_height` (0..32) and `height_rand_a`/`height_rand_b` (0..24) (TrunkPlacer.java:29-35) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| StraightTrunkPlacer places height consecutive log blocks straight up from the origin, sampling a fresh trunk-provider block state per log block. | `StraightTrunkPlacer.placeTrunk` (StraightTrunkPlacer.java:27-43): `for (int y = 0; y < treeHeight; y++) this.placeLog(level, trunkSetter, random, origin.above(y), config);` — exactly `treeHeight` logs straight up, and `placeLog` (TrunkPlacer.java:85-99) calls `config.trunkProvider.getState(level, random, pos)` per log. Two additions §N.5 omits: `placeBelowTrunkBlock(level, trunkSetter, random, origin.below(), config)` runs first (:36, TrunkPlacer.java:62-73) and samples `config.belowTrunkProvider`, and each log is gated on `TreeFeature.validTreePos` (TrunkPlacer.java:93), so a blocked position places nothing and takes no provider draw | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BendingTrunkPlacer draws one horizontal cardinal direction once, up front; then in a vertical loop running from 0 up to height-1, draws a fresh bounded int on every iteration to decide whether to also drift sideways this iteration (a test that only starts engaging near the top of the trunk and can repeat), moving straight up by one log every iteration regardless; after that loop, a second loop places bend_length.sample(random)+1 further logs stepping only horizontally in the drawn direction, producing a horizontal arm at the top of the trunk rather than a resumed vertical trunk; bend_length is an IntProvider bounded 1..64, not a plain integer. | `BendingTrunkPlacer.placeTrunk` (BendingTrunkPlacer.java:45-89) is a different algorithm. The horizontal direction is drawn ONCE up front via `Direction.Plane.HORIZONTAL.getRandomDirection(random)` (:54, Direction.java:588-590). The vertical loop runs `i = 0..treeHeight-1` and draws `random.nextInt(2)` on EVERY iteration for the bend-start test `if (i + 1 >= logHeight + random.nextInt(2)) pos.move(direction);` (:61-64), so the sideways drift starts near the top and repeats. After that loop a SECOND loop places `this.bendLength.sample(random) + 1` further logs stepping horizontally in `direction` (:77-86), producing a horizontal arm rather than a resumed vertical trunk. The config fields are `min_height_for_leaves` (optional, default 1) and `bend_length` (an IntProvider bounded 1..64), not a plain `bend_length: i32` (:21-30) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| BlobFoliagePlacer samples radius and offset from its own IntProviders, then for each layer from offset downward to offset - foliage_height (foliage_height being the constant height field, no draw), computes a per-layer radius that shrinks as the layer descends (not a constant radius), and skips a corner cell iff it is an exact corner AND (a next_int_bounded(2) == 0 draw OR the layer is the attachment-level row) before placing a fresh foliage-provider block state at positions gated by a valid-tree-position check (not a plain air-or-replaceable test). | `BlobFoliagePlacer.createFoliage` (BlobFoliagePlacer.java:32-48) loops `for (int yo = offset; yo >= offset - foliageHeight; yo--)` — DOWNWARD from `+offset`, not upward from `height - offset` — with a per-layer `currentRadius = Math.max(leafRadius + foliageAttachment.radiusOffset() - 1 - yo / 2, 0)` (:45), so the radius is not constant across layers either. `radius` is sampled by `FoliagePlacer.foliageRadius` (FoliagePlacer.java:65-67) at TreeFeature.java:68 and `offset` by `FoliagePlacer.offset` (FoliagePlacer.java:69-70) at FoliagePlacer.java:48; `foliageHeight` is the constant `height` field with no draw (BlobFoliagePlacer.java:50-53). The corner rule is `shouldSkipLocation` = `dx == currentRadius && dz == currentRadius && (random.nextInt(2) == 0 OR y == 0)` (:56-58), reached through `shouldSkipLocationSigned`, which passes abs(dx) and abs(dz) (FoliagePlacer.java:77-91): the extra `y == 0` disjunct the blueprint omits makes the topmost row's corners ALWAYS skipped, and the draw is taken only at corner candidates because of `&&` short-circuiting. Placement goes through `tryPlaceLeaf` (FoliagePlacer.java:110), gated on `TreeFeature.validTreePos`, not a plain air-or-replaceable test | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| SpruceFoliagePlacer uses a radius pattern that GROWS as a sawtooth as Y descends (starting from one random draw of 0 or 1, then cycling upward with an increasing cap each time it resets), neither an alternation between two fixed values nor a shrinking radius - and BlobFoliagePlacer's own radius is not constant across layers either. | `SpruceFoliagePlacer.createFoliage` (SpruceFoliagePlacer.java:28-55): `int currentRadius = random.nextInt(2)` (one draw, :41), then a sawtooth that GROWS as `yo` descends — `if (currentRadius >= maxRadius) { currentRadius = minRadius; minRadius = 1; maxRadius = Math.min(maxRadius + 1, leafRadius + radiusOffset); } else currentRadius++;` (:47-53) — producing 0,1,0,1,2,1,2,3,... capped at `leafRadius`, neither an alternation between two values nor a shrinking radius. Its `foliageHeight` also draws: `Math.max(4, treeHeight - this.trunkHeight.sample(random))` (:57-60). The comparison is wrong at the other end too: `BlobFoliagePlacer`'s radius is not constant (BlobFoliagePlacer.java:45) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| No random_patch feature kind exists in vanilla's 63-entry Feature registry at all; grass/flower patches are expressed purely as ordinary placement-modifier chains (in_square, heightmap, biome, count, random_offset, block_predicate_filter, and for a handful of features noise_threshold_count) over a plain simple_block-configured feature - e.g. patch_grass_badlands.json is minecraft:grass behind such a chain, with no nested feature reference and no recursive placement-chain call of any kind. | No `random_patch` feature exists in 26.2. reports/registries.json `minecraft:worldgen/feature` carries no such entry among its 63 kinds, there is no RandomPatchFeature or RandomPatchConfiguration class under net/minecraft/world/level/levelgen/feature/, no configured_feature in the pinned data uses that type, and the only occurrence of the string in the source tree is a datafixer (SavedDataFeaturePoolElementFix.java). The mechanism it described is now expressed purely with placement modifiers over a plain feature: data/minecraft/worldgen/placed_feature/patch_grass_badlands.json is `minecraft:grass` with `noise_threshold_count` then `in_square` then `heightmap` then `biome` then `count: 32` then `random_offset {xz_spread: trapezoid -7..7, y_spread: trapezoid -3..3}` then `block_predicate_filter {matching_block_tag minecraft:air}`. `random_patch` must therefore be dropped from the implemented-feature tier and its RNG shape not reproduced | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's simple_block feature samples its to_place BlockStateProvider exactly once at the origin, but placement is neither unconditional nor always one block: a null sample places nothing, the sampled state must pass a would-survive check against the current world state or nothing is placed, two named block kinds are special-cased (one placing two blocks, one using its own placement helper), and SimpleBlockConfiguration carries a second field, schedule_tick (optional, default false), that schedules a block tick on successful placement. | `SimpleBlockFeature.place` (SimpleBlockFeature.java:16-47) does sample exactly once — `config.toPlace().getOptionalState(level, context.random(), origin)` (:21) — but the placement is neither unconditional nor always one block: a null sample returns false (:22-24), the state must pass `stateToPlace.canSurvive(level, origin)` or nothing is placed (:26, :44-46), a `DoublePlantBlock` requires air above and places TWO blocks via `DoublePlantBlock.placeAt` (:27-32), a `MossyCarpetBlock` goes through `MossyCarpetBlock.placeAt` using `level.getRandom()` (:33-34), and `SimpleBlockConfiguration` carries a second field `schedule_tick` (optional, default false) adding `level.scheduleTick(origin, ..., 1)` (:39-41, SimpleBlockConfiguration.java:7-14) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
