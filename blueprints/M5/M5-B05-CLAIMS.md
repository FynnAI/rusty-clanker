# M5-B05 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Vanilla's overworld and nether dimensions use the identical multi-noise biome-placement algorithm, differing only in which compiled biome-parameter-list data is supplied to it. | `MultiNoiseBiomeSource.getNoiseBiome` (MultiNoiseBiomeSource.java:59-67) is the only lookup for either dimension; data/minecraft/worldgen/world_preset/normal.json gives minecraft:overworld the biome source type minecraft:multi_noise with preset minecraft:overworld and minecraft:the_nether the same type with preset minecraft:nether; `MultiNoiseBiomeSourceParameterLists.bootstrap` (MultiNoiseBiomeSourceParameterLists.java:16-20) registers exactly those two presets, each wrapping a `Climate.ParameterList` consumed by the identical `findValue` path | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Nether's biome-parameter list contains 5 entries. | `MultiNoiseBiomeSourceParameterList.Preset.NETHER` (MultiNoiseBiomeSourceParameterList.java:51-67) lists exactly five pairs: nether_wastes, soul_sand_valley, crimson_forest, warped_forest, basalt_deltas. reports/biome_parameters/minecraft/nether.json has the same five entries (the overworld list has 7594) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Nether's biome-parameter-list entries pin the depth and weirdness axes as quantized point spans with min 0 and max 0 on all five entries, rather than omitting them; the full-range -10000..10000 wildcard span exists only on the Overworld side. | MultiNoiseBiomeSourceParameterList.java:58-62 builds every nether entry through `Climate.parameters(float, ...)`, which routes each of the six axes through `Climate.Parameter.point` (Climate.java:48-66, 117-119) = a span with min equal to max, and reports/biome_parameters/minecraft/nether.json shows depth 0.0 and weirdness 0.0 as scalars on all five entries. Correction: the nether's depth and weirdness are quantized POINT spans, min 0 and max 0, not wildcards with min -10000 and max 10000; the -10000..10000 wildcard that does exist is the overworld builder's FULL_RANGE = span(-1.0F, 1.0F) (OverworldBiomeBuilder.java:38). The blueprint's zero-special-casing conclusion still holds for a different reason: all five entries pin depth and weirdness identically, so those axes add the same constant to every nether fitness and never move the argmin | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A wildcard span whose range fully covers the target's value on that axis always contributes a distance, and therefore a squared distance, of 0. | `Climate.Parameter.distance(long)` (Climate.java:142-146): when the target lies inside the span, the overshoot above max is not positive and the shortfall below min is not positive, so the method returns the shortfall clamped up to 0; `Mth.square(0L)` (Mth.java:653-655) is then 0 in `ParameterPoint.fitness` (Climate.java:240-248) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The End dimension's biome placement uses a structurally different, hardcoded (non-JSON-driven, non-R-tree) algorithm (TheEndBiomeSource): a central 64-chunk (1024-block)-radius island special case plus threshold-bucketed reuse of the erosion axis slot. | `TheEndBiomeSource.getNoiseBiome` (TheEndBiomeSource.java:60-81). The shape is right: hardcoded, non-JSON, non-RTree, sampling `sampler.erosion()` at the containing chunk's centre block and bucketing it by fixed thresholds (above 0.25 end_highlands, at or above -0.0625 end_midlands, below -0.21875 small_end_islands, else end_barrens). The radius is wrong: the central-island test is chunkX squared plus chunkZ squared at most 4096 in CHUNK coordinates (:67), i.e. a 64-chunk (1024-block) radius. The 8 appearing in that method is the chunk-centre offset in `(chunkCoord * 2 + 1) * 8` (:70-71), not a radius. Correction: central island radius 64 chunks, not 8 | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The End dimension has no multi-noise biome-parameter list at all. | data/minecraft/worldgen/multi_noise_biome_source_parameter_list/ contains only nether.json and overworld.json; `MultiNoiseBiomeSourceParameterLists` (MultiNoiseBiomeSourceParameterLists.java:10-11) declares only the NETHER and OVERWORLD keys; data/minecraft/worldgen/world_preset/normal.json gives minecraft:the_end the biome source type minecraft:the_end, which takes five biome holders and no parameter list at all (TheEndBiomeSource.java:14-23) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's biome-selection functions (Climate.Sampler.sample, Climate.ParameterList.findValue) and its spawn-point search (Climate.findSpawnPosition) are pure functions of density-function output and quantized-integer arithmetic, with no Random/RandomSource reference anywhere in either algorithm. | Climate.java carries no Random or RandomSource import or reference anywhere: `Sampler.sample` (:497-510) is density-function evaluation plus `quantizeCoord`, `ParameterList.findValue`/`findValueIndex`/`findValueBruteForce` (:183-212) and `RTree.search`/`SubTree.search` (:398-403, :461-478) are pure long arithmetic, and `SpawnFinder` (:517-572) uses only `Math.sin`/`Math.cos` and long arithmetic | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's biome selection at a given coordinate depends only on that coordinate's own seed-derived density-function output, with no cross-chunk read and no cached mutable state of its own beyond the noise router's own memoization, except for three exclusions: the R-tree-equivalent search's own thread-local last-result cache can decide an exact fitness tie by that thread's query history, Blender's biome resolver can override the result with a neighbouring old chunk's stored biome while blending, and BelowZeroRetrogen's resolver can replace it with the proto-chunk's own stored biome while a pre-1.18 chunk is upgrading; the claim holds for tie-free targets in a freshly generated, non-blended, non-upgrading world. | The distance math itself is coordinate-pure, but two of the claim's negatives fail. (a) `Climate.RTree` holds its own mutable per-thread cache, `ThreadLocal<Leaf<T>> lastResult` (Climate.java:260), read as the initial branch-and-bound candidate and written back after every query (:398-403); it seeds `minDistance` in `SubTree.search` (:465-466), so on an exact fitness tie the previously returned leaf wins and the answer depends on that thread's query history. (b) On the real fill path the source is wrapped: `NoiseBasedChunkGenerator.doCreateBiomes` (NoiseBasedChunkGenerator.java:92-95) resolves through `BelowZeroRetrogen.getBiomeResolver(blender.getBiomeResolver(this.biomeSource), protoChunk)`, which consults neighbouring old chunks while blending. Correction: the claim holds for tie-free targets in a non-blended world; state those two exclusions instead of a blanket no-cached-state assertion | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A biome-parameter list's entries are stored in the codec-decoded list order (or a built-in preset's own emission order), a Vec never re-sorted by that storage step itself, but vanilla's production search never scans that stored list at all: findValue always delegates to the rebuilt R-tree-equivalent search structure, which re-sorts children at every level of build, and only its @VisibleForTesting-only brute-force scan visits entries in stored order. | `Climate.ParameterList` (Climate.java:174-177) keeps `values` in decoded order but immediately builds `RTree.create(values)`, and the production entry point `findValue` delegates to `findValueIndex` and thence to `RTree.search` (:183-212), never to the stored list; `RTree.build` re-sorts its children at every level (leaf fan-out sort :291-301, per-axis sorts :309-310, winning-bucket re-sort :325). Only `findValueBruteForce` (:188-204, marked @VisibleForTesting) scans in stored order. Correction: the STORED list preserves the order the codec decoded or the preset emitted (MultiNoiseBiomeSourceParameterList.java:56-64 for the nether, `OverworldBiomeBuilder.addBiomes` emission order for the overworld) — list order, not registry-load order — while the SEARCHED order is the rebuilt tree's and is deliberately re-sorted | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| quantize_climate is computed as (v * 10000.0) as i64 for an input f32 v. | `Climate.quantizeCoord` (Climate.java:80-82) multiplies the float argument by the 10000.0F QUANTIZATION_FACTOR (:28) and truncates the float product to long with a cast — a float multiply, then a truncate-toward-zero narrowing cast, exactly as claimed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's NoiseRouter climate-target density functions are six fields named temperature, vegetation, continents, erosion, depth, and ridges. | `NoiseRouter` (NoiseRouter.java:7-23) declares temperature, vegetation, continents, erosion, depth, ridges as its fifth through tenth components, with the identical codec field names (:30-35). They are interleaved: barrier, fluid_level_floodedness, fluid_level_spread and lava precede them, preliminary_surface_level, final_density, vein_toggle, vein_ridged and vein_gap follow | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Of those six NoiseRouter fields, three field names differ from the climate axis they actually feed: vegetation feeds the humidity axis, continents feeds the continentalness axis, and ridges feeds the weirdness axis; temperature, erosion, and depth feed the like-named axes. | `NoiseChunk.cachedClimateSampler` (NoiseChunk.java:169-179) constructs `Climate.Sampler` positionally from noises.temperature(), noises.vegetation(), noises.continents(), noises.erosion(), noises.depth(), noises.ridges(), spawnTarget, while `Climate.Sampler`'s own component order is temperature, humidity, continentalness, erosion, depth, weirdness, spawnTarget (Climate.java:485-493) — so vegetation lands in the humidity slot, continents in continentalness, ridges in weirdness, and the other three are like-named | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| QuartPos.toBlock converts a quart coordinate q to a block coordinate as q << 2, with no offset, no centering, and no interpolation. | `QuartPos.toBlock` (QuartPos.java:20-22) is a bare left shift by 2 of the quart coordinate, with BITS 2 and SIZE 4 (:4-5) — no addend, no centring, no interpolation | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's climate sampler has no "fuzz" or jitter offset anywhere in its quart-to-block coordinate conversion. | `Climate.Sampler.sample` (Climate.java:497-510) converts each axis with `QuartPos.toBlock` and adds no jitter term. Note for the blueprint: the fuzz does exist one level up, in `BiomeManager.getBiome` (BiomeManager.java:31-65), which chooses among the eight surrounding quart corners by a seeded `getFiddledDistance`; that is the gameplay block-to-biome query path, whereas generation fill (`ChunkAccess.fillBiomesFromNoise`, ChunkAccess.java:448-459) reaches `BiomeSource.getNoiseBiome` with no fuzz at all | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Climate.Sampler.sample(quartX, quartY, quartZ) evaluates its six climate density functions at the single exact block position (quartX<<2, quartY<<2, quartZ<<2), via a single-point, non-interpolated evaluation context. | Climate.java:497-510 — three `QuartPos.toBlock` conversions feeding one `DensityFunction.SinglePointContext(blockX, blockY, blockZ)` (DensityFunction.java:134-137, a bare three-int coordinate record with no interpolation or cell state), on which all six climate functions are computed | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla narrows each climate density function's f64 output to f32 before quantizing it to the integer climate value; the narrowing path is always f64 -> f32 -> quantize_climate, never f64 fed directly into the quantizer's f32 parameter. | Climate.java:502-509 applies a `(float)` cast to each `compute` result before passing it to `Climate.target` (:35-46), whose six parameters are float and which calls `quantizeCoord(float)` (:80-82) per axis — the double never reaches the quantizer unnarrowed, and the two casts happen in exactly the claimed order | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Rust's "as f32" narrowing cast on a finite f64 (round-to-nearest, ties-to-even) and its "as i64" narrowing cast on a finite f32 (truncate toward zero) both match Java's own narrowing-cast semantics exactly for this climate-sampling path. | JLS 5.1.3 narrowing primitive conversion: double-to-float is IEEE-754 round-to-nearest ties-to-even; float-to-long truncates toward zero, maps NaN to 0 and saturates out-of-range values. The repository's own reference digest docs/research/mc-26.2/18-float-determinism.md section 3.15 records both as confirmed non-hazards matching Rust's `as f32` and `as i64` for finite values on any 1.45-or-later toolchain, and flags the real neighbouring hazard as `Mth.floor`-before-cast (section 3.5), which this path does not use | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In a climate target point, the target value for the offset axis is always implicitly 0. | `Climate.TargetPoint` (Climate.java:574-582) has no offset component at all; `toParameterArray` (:578-581) appends a literal 0L as its seventh element, and `ParameterPoint.fitness` (:240-248) adds `Mth.square(this.offset)` with no target term subtracted | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Climate.Parameter.distance(min, max, t) is: above = t - max, below = min - t, distance = above if above > 0 else max(below, 0) -- i.e. exactly 0 when t is within [min, max], and never negative. | `Climate.Parameter.distance(long target)` (Climate.java:142-146) computes the overshoot above the span's max and the shortfall below its min, returns the overshoot when it is strictly positive, and otherwise the shortfall clamped up to zero via `Math.max` — zero exactly when the target lies within the span, never negative. The sibling overload for a span-valued target (:148-152) is used only by span-versus-span comparisons and not on this path | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Java's Mth.square(long) computes x*x using wrapping (unchecked) multiplication on overflow. | `Mth.square(long)` (Mth.java:653-655) is a plain long multiply of the argument by itself, with no overflow check; Java long multiplication is defined to wrap modulo 2^64 (JLS 15.17.1), so `x.wrapping_mul(x)` is the exact Rust equivalent | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Quantized climate values are bounded to roughly +/-20000 per axis. | `Climate.Parameter.CODEC` (Climate.java:103-112) builds every stored span from `Codec.floatRange(-2.0F, 2.0F)`, so a decoded axis span quantizes into -20000..20000, and `ParameterPoint.CODEC`'s offset field (:232) uses floatRange(0.0F, 1.0F), i.e. 0..10000. Note: that bound is enforced only on the stored parameter side. A `TargetPoint` carries whatever the climate density functions produce — data/minecraft/worldgen/noise_settings/overworld.json's noise_router has temperature and vegetation as shifted_noise and continents, erosion, depth, ridges as named functions, none explicitly clamped — so for targets the figure is a data-range observation, not a hard clamp. The widest value in the shipped data is continentalness -1.2 (mushroom_fields), i.e. -12000 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A biome-parameter point's fitness against a target is the sum of squared per-axis distances (via Climate.Parameter.distance) over the six named climate axes (temperature, humidity, continentalness, erosion, depth, weirdness) plus a seventh synthetic "offset" axis, whose distance to the target equals the point's own offset value directly, since the target's offset is implicitly 0. | `Climate.ParameterPoint.fitness` (Climate.java:240-248) sums `Mth.square` of each axis's `Parameter.distance` against the like-named target field over temperature, humidity, continentalness, erosion, depth, weirdness in that order, then adds `Mth.square(this.offset)` directly; `parameterSpace()` (:250-254) makes the seventh axis explicit as a point `Parameter(offset, offset)` and `toParameterArray` (:578-581) supplies 0 as its target, so the two formulations agree | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's real accelerated biome-search structure is not a classic balanced R-tree but a recursive k-d-tree-style bucketing structure over all 7 quantized axes, with a parameter count of 7 and 6 children per node. | `Climate.RTree` (Climate.java:257-483): PARAMETER_COUNT is 7 (:30), CHILDREN_PER_NODE is 6 (:258, though `build` and `bucketize` use the literal 6 at :291 and :350), `create` rejects any parameter space whose size is not 7 (:271-274), and `build` (:282-327) is the recursive per-axis bucketing described. Note on wording only: the class is literally named RTree and its internal nodes carry a per-axis union bounding box (`buildParameterSpace`, :377-396), i.e. a bounding-volume hierarchy bulk-loaded sort-tile-recursive style rather than a k-d tree with splitting planes — the claim's counts, axes and recursion are exact, the k-d-tree-style label is a loose gloss | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In vanilla's real search structure, each leaf holds one climate point, represented as seven axis spans (the six named axes' spans plus a seventh synthetic point-interval span for offset), paired with its biome-entry index. | `RTree.Leaf` (Climate.java:405-419) is built from one `ParameterPoint` and passes `parameterPoint.parameterSpace()` (:250-254) — the six named `Parameter` spans plus a point `Parameter(offset, offset)` — into `Node`'s seven-element parameterSpace array (:422-427). Note: vanilla's leaf carries the payload value T (the biome `Holder`) itself, not an index into the entry list; the blueprint's entry_index is an equivalent local re-shaping, not a vanilla field | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When building vanilla's real search structure, if a node has 6 or fewer children, they are sorted by their total absolute center-of-interval magnitude summed across all 7 axes (sum over axes of the absolute value of (span.min+span.max)/2, using integer division truncating toward zero) and wrapped directly into one internal node. | `RTree.build` (Climate.java:291-303): for six or fewer children it sorts by one key that sums, across all seven axes, the absolute value of that axis's integer centre `(min + max) / 2L` (long division, truncating toward zero) and returns a single `SubTree` wrapping them. Note: an earlier branch (:287-289) returns the lone child unchanged when there is exactly one, so the sort-and-wrap in practice covers two through six children | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When building vanilla's real search structure with more than 6 children (n = children.len()), the bucket size used is 6 raised to the power floor(log base 6 of (n - 0.01)). | `RTree.bucketize` (Climate.java:347-365) computes the bucket size as `Math.pow(6.0, Math.floor(Math.log(nodes.size() - 0.01) / Math.log(6.0)))` cast to int — the base-6 log taken as a ratio of natural logs, then a double power truncated to int. That matches the blueprint's Rust form, since `f64::log(6.0)` is defined as self.ln() divided by 6.0.ln() | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When building vanilla's real search structure with more than 6 children, for each of the 7 axes independently the children are sorted by that axis's own interval center, with ties broken by cycling through the remaining axes in rotation: (axis+1) mod 7, then (axis+2) mod 7, and so on through all 7 axes if needed. | `RTree.sort` (Climate.java:329-337) starts from `comparator(dimension, false)` — the raw signed interval centre `(min + max) / 2L` (:339-345) — and chains `thenComparing` over `(dimension + d) % dimensions` for d from 1 to 6, i.e. all seven axes in rotation; `build` (:309-310) calls it once per candidate axis. Note: vanilla re-sorts the SAME list in place for each axis rather than a copy, and `List.sort` is stable, so children tying on all seven axes retain the previous axis pass's relative order; `bucketize` copies each bucket into a fresh list (:347-364), so already-formed buckets are unaffected by later passes | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When building vanilla's real search structure with more than 6 children, the axis-sorted children are split into consecutive buckets of the computed bucket size, with the final bucket possibly shorter than the rest. | `RTree.bucketize` (Climate.java:352-364) appends children in the sorted list's order, closing a bucket as soon as it reaches the computed size, then flushes any remainder as a final, possibly shorter bucket | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each bucket's cost in vanilla's real search structure build is the sum, over all 7 axes, of the absolute difference between that axis's bucket-wide bounding maximum and minimum, not squared, unlike the fitness/distance metric. | `RTree.cost` (Climate.java:367-375) sums the absolute difference between max and min over the seven-element parameterSpace of the bucket's `SubTree`, which `buildParameterSpace` (:377-396) has already unioned across that bucket's children via `Parameter.span` (:154-156). No squaring, unlike `Node.distance` (:433-441) which does square each axis | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When building vanilla's real search structure, the axis whose buckets have the lowest total summed cost is chosen as that node's split axis. | `RTree.build` (Climate.java:305-323) tracks minCost, minDimension and minBuckets for the axis with the lowest sum of `cost(bucket.parameterSpace)` over that axis's buckets. Note: the update test is strict (minCost greater than totalCost), so on an exact cost tie the LOWEST axis index keeps the win | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| When building vanilla's real search structure, the winning axis's bucket set is re-sorted lexicographically by the absolute center magnitude of each bucket's own bounding box, using the same seven-axis comparator chain as the per-axis trial sort (starting at the winning axis and rotating through the rest), not the single summed-magnitude key used for leaf-level fan-out, before the build recurses into each bucket. | Climate.java:325 re-sorts with `sort(minBuckets, dimensions, minDimension, true)` — the same seven-axis comparator CHAIN as the per-axis pass, but with absolute set to true so each axis's key is the absolute value of that bucket bounding box's centre (:339-345), starting at the winning axis and rotating through the remaining six as tie-breakers. That is a different sort from the leaf fan-out, which uses a single key SUMMING the absolute centres of all seven axes (:291-301). Correction: re-sort the winning bucket set lexicographically by absolute bucket-bounding-box centre, winning axis first then (axis+1) mod 7 onward; only the two-to-six-children fan-out uses the summed-magnitude key | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| At an internal node of vanilla's real search structure, the search evaluates the same fitness-shaped distance metric against every child's own bounding 7-span, in the node's own stored child order. | `RTree.SubTree.search` (Climate.java:461-478) iterates its children array in stored order and calls `distanceMetric.distance(child, target)`, which for the production metric is `Node::distance` (:206-208, :433-441) — the sum over all seven axes of `Mth.square` of `Parameter.distance` against that axis's target, the same shape as `ParameterPoint.fitness`, applied to the node's union bounding spans | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's real search structure's branch-and-bound search recurses into a child only when that child's metric is strictly less than the current best-known fitness found so far. | Climate.java:469-471 — a child is recursed into only when the running minDistance is strictly greater than that child's bound, i.e. the child's metric must be strictly below the current best | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A leaf of vanilla's real search structure updates the running best-fitness candidate using the same strict-less-than (never less-than-or-equal) rule as a naive linear scan. | `RTree.Leaf.search` (Climate.java:413-418) simply returns itself; the running-best update lives in the parent `SubTree.search` (:472-476) and is a strict comparison, exactly like `findValueBruteForce`'s own strictly-less test (:197). Note: the value compared is the child's own bound when the child IS the returned leaf, and is otherwise recomputed on the returned leaf (:472) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla additionally seeds the very first "best" candidate of a real search from a thread-local cache of the last successfully found leaf (exploiting spatial coherence between consecutive queries); because the prune and best-update tests are both strict, a cached leaf whose distance equals the eventual minimum is never displaced and also blocks descent into any subtree whose bound equals that minimum, so the cache changes the final search result relative to a cold start whenever an exact fitness tie exists, and is result-neutral only on tie-free data. | The cache exists exactly as described — `ThreadLocal<Leaf<T>> lastResult` (Climate.java:260), read into `search` and written back afterwards (:398-403), supplying the initial candidate and hence the initial minDistance in `SubTree.search` (:465-466). But no effect on the final result does not hold: because both the prune test (:470) and the best-update test (:473) are strict, a cached leaf whose distance EQUALS the eventual minimum is never displaced and additionally suppresses descent into any subtree whose bound equals that minimum, whereas a cold start returns the first-encountered tied leaf. Correction: the cache is result-neutral only on tie-free data — the same caveat the blueprint already states for child order — so on an exact tie consecutive queries on one thread can differ from a cold run, and the blueprint's cold-start choice is a deliberate flagged deviation, not a provably invisible one | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Child iteration order in vanilla's real search structure is parity-relevant whenever two children tie exactly on distance, since the first-encountered child wins the tie, and the real tree's search must preserve the exact children ordering established by its own build step to reproduce that. | `SubTree.search` (Climate.java:461-478) walks this.children in the array order fixed at build time (:302, :326, array copied at :452-459 and never reordered afterwards), and both comparisons are strict (:470, :473), so on an exact tie the first child encountered keeps the win | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Adjacent Overworld climate bands are defined so that they share an exact edge value on at least one axis, for example a temperature value of -0.45. | `OverworldBiomeBuilder.temperatures` (OverworldBiomeBuilder.java:38-44): band 0 is span(-1.0F, -0.45F) and band 1 is span(-0.45F, -0.15F), sharing the exact edge -0.45, which quantizes to -4500 in both. The same shared-edge pattern holds on the other axes, e.g. humidities (:45-51) share -0.35, -0.1, 0.1 and 0.3, and erosions (:52-60) share -0.78, -0.375, -0.2225, 0.05, 0.45 and 0.55 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's spawn-point search (Climate.findSpawnPosition) searches against NoiseGeneratorSettings' own spawn-target list, a separate, small list of climate points, never the full biome-parameter list. | `NoiseGeneratorSettings` carries `List<Climate.ParameterPoint> spawnTarget` (NoiseGeneratorSettings.java:25) under the codec field spawn_target (:39); `NoiseBasedChunkGenerator.doCreateBiomes` (NoiseBasedChunkGenerator.java:95) passes `this.settings.value().spawnTarget()` into `NoiseChunk.cachedClimateSampler` (NoiseChunk.java:169-179), which stores it as `Climate.Sampler`'s spawnTarget component, and `Sampler.findSpawnPosition` (Climate.java:512-514) hands exactly that list to `Climate.findSpawnPosition` (:93-95). It really is small and separate: data/minecraft/worldgen/noise_settings/overworld.json has 2 spawn_target entries, while nether.json, end.json, caves.json and floating_islands.json have 0 (an empty list short-circuits to BlockPos.ZERO, Climate.java:513) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's spawn-point search walks an Archimedean-spiral radial pattern outward from a center point. | `Climate.SpawnFinder.radialSearch` (Climate.java:527-548). Note on the exact shape: the centre is `this.result.location()` captured ONCE at pass start (:532) and is not re-centred as better candidates appear within that pass; the radius is held constant through a full angular sweep with a constant arc-length step of radiusIncrement divided by radius (:542) and is bumped by radiusIncrement only once the angle passes 2 pi (:543-546) — concentric rings with a constant arc step, the discrete form of an Archimedean spiral rather than a continuously growing radius | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's spawn-point search samples climate at a fixed quart-Y of 0, since the search is Y-agnostic and the depth axis handles vertical placement quality instead. | `SpawnFinder.getSpawnPositionAndFitness` (Climate.java:550-566) samples at `sampler.sample(QuartPos.fromBlock(blockX), 0, QuartPos.fromBlock(blockZ))` — the quart-Y argument is the literal 0. Note: the trailing rationale is not supported by the reference. Depth contributes nothing at all, because the very next statement rebuilds the point with depth forced to 0 (:554-556) before any fitness is taken, so it is not the case that the depth axis handles vertical placement quality here | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's spawn-point search ranks each candidate position by the formula (minimum fitness over all spawn targets) * 2048^2 + (block_x^2 + block_z^2), so climate fitness dominates the ranking unless two candidates are within 1/2048^2 "worth" of origin-distance of each other. | Climate.java:563-564 — the origin bias is `Mth.square((long)blockX) + Mth.square((long)blockZ)` and the score is minFitness multiplied by `Mth.square(2048L)` plus that bias, with minFitness the `Math.min` of `point.fitness(zeroDepthTargetPoint)` across every spawn target (:557-561). Note: the squared distance is to the WORLD origin (absolute block x and z), not to the pass's own search centre | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's spawn-point search forces the queried climate point's own depth axis to 0 before computing fitness against the spawn targets (zeroDepthTargetPoint). | Climate.java:554-556 rebuilds the sampled point as a new `TargetPoint` carrying temperature, humidity, continentalness, erosion, a literal 0L for depth, and weirdness — the local is even named zeroDepthTargetPoint — and only that zeroed point is scored against the spawn targets (:559-561) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's spawn-point search truncates (does not round) the candidate x/z block coordinates computed from sin(angle)*radius and cos(angle)*radius. | Climate.java:535-536 — each candidate axis is an `(int)` cast of the double product, i.e. truncation toward zero, added to the search origin's own coordinate. Two notes: vanilla assigns x from sin(angle) times radius and z from cos(angle) times radius, the OPPOSITE of the blueprint's Context section G pseudocode (bx from cos, bz from sin), which mirrors the search path; and these are real double-precision `java.lang.Math` sin and cos, not `Mth`'s float sine table, so they carry the cross-JDK 1-ulp caveat of docs/research/mc-26.2/18-float-determinism.md section 3.7 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The first pass of vanilla's spawn-point radial search initializes radius to 512 (not 0) and sweeps up to and including 2048 in increments of 512, visiting only 512, 1024, 1536 and 2048; the origin is scored exactly once before either pass, never re-scored inside one. | `SpawnFinder.radialSearch` (Climate.java:527-548) initialises radius to radiusIncrement (:531) and loops while radius is at most maxRadius (:534), so the first pass — invoked as `radialSearch(targetClimates, sampler, 2048.0F, 512.0F)` (:523, argument order maxRadius then radiusIncrement) — visits radii 512, 1024, 1536 and 2048 only. Correction: the sweep starts at 512, not 0. The origin is scored exactly once, before the passes, by the constructor's own `getSpawnPositionAndFitness(..., 0, 0)` (:522), and is never re-scored inside a pass, so the blueprint's radius-0 iteration is an extra sample vanilla does not take | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The second pass of vanilla's spawn-point radial search is centered on the first pass's own best-found result and sweeps radius out to 512 in increments of 32. | The `SpawnFinder` constructor (Climate.java:521-525) runs `this.radialSearch(targetClimates, sampler, 512.0F, 32.0F)` after the first pass, and `radialSearch` reads its centre from `this.result.location()` (:532) — the best point found so far, i.e. the first pass's winner — then sweeps radii 32 up to and including 512 in 32-block steps (:531-546) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| QuartPos.fromBlock converts a block coordinate to a quart coordinate using a floor-toward-negative-infinity convention, i.e. an arithmetic right-shift by 2 rather than a truncating division. | `QuartPos.fromBlock` (QuartPos.java:12-14) is an arithmetic right shift by 2 of the signed int block coordinate, so it floors toward negative infinity (block -1 maps to quart -1, not 0, as a truncating divide would give); `QuartPos.quartLocal` (:16-18) masks with 3, consistent with that same convention | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's world height spans from Y = -64 up through a total height of 384 blocks, both exact multiples of 4 with no rounding needed when converting to quart coordinates. | data/minecraft/dimension_type/overworld.json gives min_y -64 and height 384 (logical_height 384); both are exact multiples of 4, so `QuartPos.fromBlock` on either is exact and no rounding is needed. Note: these are the OVERWORLD dimension type's values, not global constants — the_nether.json and the_end.json both give min_y 0 and height 256 — and `ChunkAccess.fillBiomesFromNoise` (ChunkAccess.java:448-459) iterates whatever the chunk's own LevelHeightAccessor reports, section by section, rather than a hardcoded range | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
