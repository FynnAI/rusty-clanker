# M5-B13c — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Ocean monument generation checks, before any piece generation is attempted, that every biome found in a 3-D quart-resolution volume of 15x16x15 = 3600 quart cells (x/z spanning 15 samples each from (16*chunk_x-20)>>2..=(16*chunk_x+38)>>2 around the chunk-min+9 center, y spanning 16 samples from (sea_level-29)>>2..=(sea_level+29)>>2 at sea level 63), collected into a hash set so duplicate quart cells collapse and iteration order is unspecified, is tagged #minecraft:required_ocean_monument_surrounding (MONUMENT_BIOME_CHECK_RADIUS = 29); if any sampled biome fails the tag check, generation returns NoValidPoint with zero RNG draws having occurred, and the gate's result is order-independent since it runs over that deduplicated set rather than a scan sequence. | `OceanMonumentStructure.findGenerationPoint` (OceanMonumentStructure.java:30-42) plus `BiomeSource.getBiomesWithin` (BiomeSource.java:44-68). The gate-before-any-RNG part is right — no draw occurs before the early return — but the geometry is wrong in four ways. (a) The center is `chunkPos.getBlockX(9)` / `getBlockZ(9)` = chunk min block + 9 (ChunkPos.java:159-161), NOT the +8 chunk center (`getMiddleBlockX`, ChunkPos.java:107). (b) The radius argument is 29, not 14: the sampled span is x-29..x+29 on every axis, i.e. 59 blocks wide, not 29. (c) The sample is a 3-D cube, not a single-Y plane: y also spans seaLevel-29..seaLevel+29. (d) Sampling is at quart (4-block biome) resolution via `QuartPos.fromBlock`, giving a 15x15x15 quart cube collected into a `Set`, not a per-block column scan. Correction for `MONUMENT_BIOME_CHECK_RADIUS`: 29, applied as a 3-D quart-resolution cube around (chunkMinX+9, seaLevel, chunkMinZ+9). The tag `minecraft:required_ocean_monument_surrounding` is correct (BiomeTags.REQUIRED_OCEAN_MONUMENT_SURROUNDING; datagen data/minecraft/tags/worldgen/biome/required_ocean_monument_surrounding.json = is_ocean plus is_river) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| After the ocean monument biome gate passes, its horizontal orientation is chosen by exactly one next_int_bounded(4) draw against the four-element horizontal Direction array [North, East, South, West] (not Rotation::all()) immediately after reseeding via set_large_feature_seed(world_seed, chunk_x, chunk_z), and the drawn Direction is mapped to a fixed (Rotation, Mirror) pair (North -> (None, None), East -> (Cw90, None), South -> (None, LeftRight), West -> (Cw90, LeftRight)) since a Mirror cannot be expressed by Rotation::all() alone - vanilla places ocean monuments with a random horizontal orientation. | `OceanMonumentStructure.createTopPiece` (OceanMonumentStructure.java:44-49) draws `Direction.Plane.HORIZONTAL.getRandomDirection(random)`, which is `Util.getRandom(faces, random)` = `faces[random.nextInt(4)]` (Util.java:826-828) over `HORIZONTAL`'s face array `NORTH, EAST, SOUTH, WEST` (Direction.java:577). One draw, bound 4 — correct. Two corrections: (a) the drawn value set is `Direction.Plane.HORIZONTAL`, a Direction, NOT `Rotation.values()`; the index-to-orientation mapping differs, so `Rotation::all()[nextInt(4)]` produces a different orientation for the same seed. (b) It is not "immediately after" the reseed: the reseed happens once when the context is built (`Structure.GenerationContext.makeRandom`, Structure.java:250-254), then the biome gate and `onTopOfChunkCenter`'s `OCEAN_FLOOR_WG` heightmap lookup (Structure.java:121-129, OceanMonumentStructure.java:41) run with zero draws, and the direction draw happens later when the pieces-builder consumer runs. It is nonetheless the family's first RNG draw | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| An ocean monument's core room, which doubles as its treasure room, is selected from the fixed z=2 row of its 5x5x3 room-index grid by one next_int_bounded(4) draw over the row's x-index (not zero-RNG); there are no distinct GuardianA/GuardianB room kinds - vanilla has two WingRoom pieces (whose shared mainDesign bit is derived from a single unbounded next_int() draw, the second wing's value always one more than the first) plus one Penthouse piece, and elder guardians are placed at world-placement time from three call sites, one inside the Penthouse and one inside each wing. | `OceanMonumentPieces.MonumentBuilding.generateRoomGraph` (OceanMonumentPieces.java:313) picks the core (treasure) room as `roomGrid[getRoomIndex(random.nextInt(4), 0, 2)]` — one `nextInt(4)` draw, so the treasure room's placement is NOT zero-RNG. The two wings are fixed boxes (:236-241) but the constructor draws one `random.nextInt()` at :239 to derive both wings' `mainDesign` variant (`OceanMonumentWingRoom`, :1845-1847). Vanilla has no `GuardianA`/`GuardianB` room kinds at all: there are two `OceanMonumentWingRoom` pieces (indices 1001, 1002, :304-308) plus one `OceanMonumentPenthouse` (1003), and elder guardians are spawned at world-placement time from three call sites (`spawnElder`, :1592-1603; called at :1436 penthouse, :1910 and :1949 wing) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| An ocean monument has two wings, each containing one elder guardian. | `OceanMonumentPieces.MonumentBuilding` (OceanMonumentPieces.java:236-241) builds exactly two `OceanMonumentWingRoom` pieces from the fixed boxes (1,1,1)-(23,8,21) and (34,1,1)-(56,8,21); `OceanMonumentWingRoom.postProcess` calls `spawnElder` exactly once per wing, at :1910 in the `mainDesign == 0` branch and :1949 in the other. Note for §B: a monument holds THREE elder guardians, not two — the penthouse spawns a third at :1436 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| An ocean monument's treasure room is located around the center of the structure. | `OceanMonumentPieces.MonumentBuilding.generateRoomGraph` (OceanMonumentPieces.java:313-321): the core room is `roomGrid[getRoomIndex(random.nextInt(4), 0, 2)]` in the 5x5x3 room index space (`GRID_WIDTH`/`GRID_DEPTH` 5, `GRID_HEIGHT` 3, :1457-1459) — bottom floor (y=0), middle depth row (z=2), x drawn from 0..3 — and it claims a 2x2x2 block of cells via its EAST/NORTH/UP connections (:314-321). `OceanMonumentCoreRoom.postProcess` (:748-833) is where the gold sits. So the treasure room is always in the middle depth band on the ground floor, horizontally near but not exactly at the center | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| An ocean monument's two WingRoom pieces connect directly to its room grid, each via one fixed SOUTH connection at a dedicated grid cell - there is no separate hallway, corridor, or connector piece class anywhere in this family. | No such hallway piece exists in 26.2. `OceanMonumentPieces.MonumentBuilding.generateRoomGraph` (OceanMonumentPieces.java:303-308) attaches `leftWing` (index 1001) and `rightWing` (1002) directly SOUTH of the two fixed grid rooms `GRIDROOM_LEFTWING_CONNECT_INDEX = getRoomIndex(0,1,0)` and `GRIDROOM_RIGHTWING_CONNECT_INDEX = getRoomIndex(4,1,0)` (:1464-1465). The wings are two fixed 23x8x21 boxes (:236-237) reached through the 5x5x3 room graph itself; the complete piece-class list (`OceanMonumentEntryRoom`, `OceanMonumentCoreRoom`, `OceanMonumentSimpleRoom`, `OceanMonumentSimpleTopRoom`, `OceanMonumentDoubleX/Y/Z/XY/YZRoom`, `OceanMonumentWingRoom`, `OceanMonumentPenthouse`) contains no hallway or connector kind. Correction: drop the `Hallway` slot and `MonumentRoomKind::Hallway` from Context §B | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A sponge room in an ocean monument is not guaranteed to generate - it appears only occasionally. | The sponge room is `OceanMonumentPieces.OceanMonumentSimpleTopRoom` (OceanMonumentPieces.java:1783-1840), which fills wet sponge at :1814-1815. It is created only by `FitSimpleTopRoom` (:171-191), whose `fits` requires the room definition to have NO opening on WEST, EAST, NORTH, SOUTH or UP — an outcome of the randomized opening-closing pass at :332-356 (each shuffled definition attempts up to 5 `random.nextInt(6)` draws, closing at most 2 openings, reverting any close that would disconnect the graph from the source room). So a monument can genuinely contain zero sponge rooms. Mechanism note for §B: there is no monument-level boolean roll and no probability constant — a `SimpleTopRoom` that IS placed always sponges, each of its 6x6 columns at 2-in-3 (`random.nextInt(3) != 0`, :1812) with a further `random.nextInt(4)` height pick, both at world-placement time | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| An ocean monument contains at least six rooms. | `OceanMonumentPieces.MonumentBuilding`'s constructor (OceanMonumentPieces.java:203-243) always emits the entry room (:208), the core room (:209), one fitted piece for every remaining unclaimed non-special room definition (:219-228, with `FitSimpleRoom.fits` returning true unconditionally, :157-160), two wing rooms and one penthouse (:240-242). The room graph is populated over 46 cells of the 5x5x3 index space (`GRID_SIZE = 75`, :1461): 5x4 at y=0 (:252-258), 5x4 at y=1 (:260-266), 3x2 at y=2 (:268-274). The piece count is therefore always far above six | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| An ocean monument's exterior footprint is 58x58 blocks. | `OceanMonumentPieces.MonumentBuilding.WIDTH = 58` and `DEPTH = 58` (OceanMonumentPieces.java:194, 196), with `HEIGHT = 22` (:195) and `TOP_POSITION = 61` (:198); the piece's bounding box is built as `makeBoundingBox(west, 39, north, direction, 58, 23, 58)` (:204). `OceanMonumentStructure.createTopPiece` (OceanMonumentStructure.java:45-46) anchors it at chunk min block minus 29 on both axes | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| An ocean monument's treasure room contains 8 gold blocks encased in dark prismarine, placed as a fixed block pattern rather than via a loot-table roll. | `OceanMonumentPieces.OceanMonumentCoreRoom.postProcess` (OceanMonumentPieces.java:793-794): the box (6,3,6)-(9,6,9) is filled with `BASE_BLACK` = `Blocks.DARK_PRISMARINE.defaultBlockState()` (:1443), then the box (7,4,7)-(8,5,8) is filled with `Blocks.GOLD_BLOCK.defaultBlockState()` — a 2x2x2 = exactly 8 gold blocks enclosed by the dark-prismarine shell. Both are unconditional `generateBox` calls with zero RNG and no loot-container hook | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| No vanilla loot table is attached to an ocean monument's treasure room. | `OceanMonumentPieces.java` contains no reference to `LootTable`, `BuiltInLootTables` or any `lootTable` field (grep over the whole 2009-line file returns nothing), and no monument piece implements `handleDataMarker`/chest placement. The datagen tree confirms it from the data side: no file under `datagen/generated/data/minecraft/loot_table/**` matches `*monument*`, and no `chests/monastery` table exists anywhere in the tree. Correction for Context §B: `MONUMENT_TREASURE_LOOT_TABLE = "minecraft:chests/monastery"` names a table that does not exist in 26.2 and should be removed rather than retained as a placeholder | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean monument room kinds, for definitions that are not one of the fixed specials, are assigned zero-RNG by a fixed, ordered list of geometric fitters (FitDoubleXYRoom, FitDoubleYZRoom, FitDoubleZRoom, FitDoubleXRoom, FitDoubleYRoom, FitSimpleTopRoom, FitSimpleRoom), each definition taking the first fitter whose predicate matches its own claimed/open-connection state, from the real kind set {SimpleRoom, SimpleTopRoom, DoubleXRoom, DoubleYRoom, DoubleZRoom, DoubleXYRoom, DoubleYZRoom} - not EmptyChamber/PillarRoom/WaterRoom/SmallConnector, none of which exists. | `OceanMonumentPieces.MonumentBuilding`'s constructor (OceanMonumentPieces.java:210-228) builds a FIXED, ORDERED fitter list — `FitDoubleXYRoom, FitDoubleYZRoom, FitDoubleZRoom, FitDoubleXRoom, FitDoubleYRoom, FitSimpleTopRoom, FitSimpleRoom` — and for each unclaimed non-special definition takes the FIRST fitter whose `fits(definition)` is true, breaking immediately. The room kind is therefore a deterministic function of that room's own six-way opening pattern, not an RNG draw per slot. The real kind set is `OceanMonumentSimpleRoom`, `SimpleTopRoom`, `DoubleXRoom`, `DoubleYRoom`, `DoubleZRoom`, `DoubleXYRoom`, `DoubleYZRoom` (plus the fixed `EntryRoom`, `CoreRoom`, two `WingRoom`s, `Penthouse`) — none of `EmptyChamber`, `PillarRoom`, `WaterRoom`, `SmallConnector` exists. Draws do occur inside some piece constructors (e.g. `OceanMonumentSimpleRoom` :1611 draws `random.nextInt(3)` for `mainDesign`), but they pick a decoration variant, never the room kind | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| There is no sponge-room probability roll or SPONGE_ROOM_PROBABILITY constant; sponge rooms arise structurally whenever a definition fits SimpleTopRoom (a fully closed-off room), and the family's complete structure-start draw sequence is: one next_int_bounded(4) for orientation, one next_int_bounded(4) for the core-room column, a 45-draw shuffle over the 46 room definitions, up to 5 next_int_bounded(6) draws per definition in the opening-closing pass, the per-room constructor draws produced by the fitter loop, and last a single unbounded next_int() from which both wings' design bit is derived - no next_float()/next_double() call occurs anywhere in this family. | There is no such roll and no such constant. `OceanMonumentPieces.java` contains no `nextFloat` or `nextDouble` call anywhere in the file (grep over all 2009 lines returns nothing), so no floating-point probability gate exists for this family. Sponge rooms arise structurally, from `FitSimpleTopRoom.fits` requiring a fully closed-off room definition (:176-182) after the randomized opening-closing pass (:332-356). Correction: delete `SPONGE_ROOM_PROBABILITY` and the `has_sponge_room` step from Context §B; the family's only structure-start draws are `nextInt(4)` for orientation, `nextInt(4)` for the core-room column, `Util.shuffle` over the room definitions, up to 5 `nextInt(6)` per definition in the closing pass, one `nextInt()` for the wing designs, and per-piece constructor draws | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Woodland mansion's individual rooms are generated as TemplateStructurePiece instances, the same base piece class used by igloo and shipwreck (desert pyramid and swamp hut instead extend ScatteredFeaturePiece and are not template-based). | The first half is right: `WoodlandMansionPieces.WoodlandMansionPiece extends TemplateStructurePiece` (WoodlandMansionPieces.java:1218-1240), as do `IglooPieces.IglooPiece` (IglooPieces.java:66) and `ShipwreckPieces.ShipwreckPiece` (ShipwreckPieces.java:90). The sibling list is wrong on desert pyramid: `DesertPyramidPiece extends ScatteredFeaturePiece` (DesertPyramidPiece.java:23) — it is procedural box-fill, not template-based; the same holds for `SwampHutPiece extends ScatteredFeaturePiece` (SwampHutPiece.java:25). Correction: cite igloo and shipwreck (or ocean ruin / end city / nether fossil / ruined portal) as the fellow `TemplateStructurePiece` families, never desert pyramid. `docs/research/mc-26.2/06-structures.md:87` repeats the same error and should be corrected with it | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Ocean monument generation is not template-based: its room-grid layout is procedural, internal to OceanMonumentPieces, and every room is genuine box-fill geometry rather than a template stamp. | Every monument piece class derives from `OceanMonumentPieces.OceanMonumentPiece extends StructurePiece` (OceanMonumentPieces.java:1440) and builds its geometry with `generateBox`, `generateWaterBox`, `generateBoxOnFillOnly`, `generateDefaultFloor`, `placeBlock` and `fillColumnDown` (:1528-1582). The file imports and references no `StructureTemplate`, `StructureTemplateManager` or `TemplateStructurePiece`, and the whole room graph is built in-file by `MonumentBuilding.generateRoomGraph` (:249-362) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Woodland mansion generation draws its horizontal rotation first (set_large_feature_seed, then one next_int_bounded(4) against Rotation::all()), since the gate's own 5x5-block box - offset 7 blocks from the chunk origin - has a rotation-dependent span (base axis offsets of 5, each negated for particular rotations); the minimum of the box's four corner heights, probed against the WORLD_SURFACE_WG heightmap (not MotionBlockingNoLeaves), must be at least 60, or generation is skipped outright. | `WoodlandMansionStructure.findGenerationPoint` (WoodlandMansionStructure.java:31-34) calls `getLowestYIn5by5BoxOffset7Blocks` and rejects `startPos.getY() < 60`. The offset-7 anchor and the 60 threshold are right: `Structure.getLowestYIn5by5BoxOffset7Blocks` (Structure.java:183-200) anchors at `chunkPos.getBlockX(7)` / `getBlockZ(7)`. Two corrections. (a) The heightmap is `Heightmap.Types.WORLD_SURFACE_WG`, NOT `MOTION_BLOCKING_NO_LEAVES` — `getLowestY` (:178-181) delegates to `getCornerHeights` (:157-164), which passes `WORLD_SURFACE_WG` to all four `getFirstOccupiedHeight` calls. (b) It is not a lowest-over-a-5x5 scan: only the FOUR CORNERS of the box are sampled and their minimum taken, and the box's span is a SIGNED plus-or-minus 5 whose sign depends on the rotation drawn one line earlier (Rotation.NONE gives (+5,+5); CLOCKWISE_90 gives (-5,+5); CLOCKWISE_180 gives (-5,-5); COUNTERCLOCKWISE_90 gives (+5,-5), :185-194) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla uses the same 5x5-lowest-ground-height gating check for both woodland mansion and end city structure generation. | Both call the identical helper with the identical threshold: `WoodlandMansionStructure.findGenerationPoint` (WoodlandMansionStructure.java:30-34) and `EndCityStructure.findGenerationPoint` (EndCityStructure.java:22-28) each draw `Rotation.getRandom(context.random())`, call `this.getLowestYIn5by5BoxOffset7Blocks(context, rotation)` (Structure.java:183-200, marked `@Deprecated`), and return `Optional.empty()` when `startPos.getY() < 60` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A woodland mansion's room-cell grid is 11x11 cells, planar, generated as one grid instance per floor. | `WoodlandMansionPieces.MansionGrid`'s constructor (WoodlandMansionPieces.java:153-156) allocates `floorRooms[0]`, `floorRooms[1]` and `floorRooms[2]`, each `new SimpleGrid(11, 11, 5)` — one planar 11x11 room-cell grid per floor, with `DEFAULT_SIZE = 11` (:107) and `houseSize = 11` (:132). Nuance for §C: the LAYOUT (corridor/room) grid is not per floor — floors 0 and 1 both classify against the shared `baseGrid` (:135, :157-158) while floor 2 classifies against a separately constructed `thirdFloorGrid` (:161-163) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A woodland mansion has three floors: ground, first, and second. | `WoodlandMansionPieces.MansionGrid.floorRooms` is a 3-element array (WoodlandMansionPieces.java:153-156), and `MansionPiecePlacer.createMansion` iterates `for (int floorNum = 0; floorNum < 3; floorNum++)` (:478) with `roomCollections = { FirstFloorRoomCollection, SecondFloorRoomCollection, ThirdFloorRoomCollection }` (:472-476) and floor origins at `origin.above(8 * floorNum + (floorNum == 2 ? 3 : 0))` (:479). Nuance for §C: the third floor is a conditional attic, not a full storey — `setupThirdFloor` (:249-305) blocks the entire third-floor grid when floor 1 offers no eligible door-flagged 1x2 room (:263-264) or when the chosen room has no free corridor direction (:294-296) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A woodland mansion's entrance region is carved by nine fixed SimpleGrid::set calls, not a uniform 3x3 foyer: a 2x2 start-room block at cells (7,4)-(8,5), a 1x2 room block at (6,4)-(6,5), a 2x6 blocked block at (9,2)-(10,7), two 1x2 corridor strips at (8,2)-(8,3) and (8,6)-(8,7), two single corridor cells at (6,3) and (6,6), and two border bands blocking the grid's own top and bottom edges at rows 0..=1 and 9..=10 (the set calls' own literal end-row argument is silently clipped by the grid's bounds-checked write). | The entrance cell is right, the 3x3 is not. `WoodlandMansionPieces.MansionGrid`'s constructor (WoodlandMansionPieces.java:133-142) sets `entranceX = 7`, `entranceY = 4`, then carves: a 2x2 `START_ROOM` (value 3) at cells (7..8, 4..5) via `baseGrid.set(entranceX, entranceY, entranceX+1, entranceY+1, 3)` (:136); a 1x2 `ROOM` (2) at (6, 4..5) (:137); a 2x6 `BLOCKED` (5) block at (9..10, 2..7) (:138); two 1x2 `CORRIDOR` (1) strips at (8, 2..3) and (8, 6..7) (:139-140); and single corridor cells at (6,3) and (6,6) (:141-142). Plus two blocked border bands at rows 0..1 and 9..10 (:143-144). `SimpleGrid.set(x0,y0,x1,y1,v)` is inclusive on both ends (:1190-1196). No 3x3 footprint exists. `docs/research/mc-26.2/06-structures.md:187`'s "3x3 foyer footprint carved by four baseGrid.set calls" is itself wrong and should be corrected with the blueprint | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A woodland mansion grows four recursive corridors outward from the foyer, biased toward the west. | `WoodlandMansionPieces.MansionGrid`'s constructor (WoodlandMansionPieces.java:145-148) makes exactly four `recursiveCorridor` calls, every one with `Direction.WEST`, seeded at cells (7, entranceY-2) = (7,2), (7, entranceY+3) = (7,7), (entranceX-2, entranceY-1) = (5,3) and (5, entranceY+2) = (5,6) — all adjacent to the entrance block. The west bias is doubly present: the four seed headings are literally WEST, and each recursion step additionally rejects an EAST turn on a coin flip (`nextDir != Direction.EAST` OR `!this.random.nextBoolean()`, :194) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The four woodland mansion corridors' base lengths are 6, 6, 3, and 3 blocks respectively. | `WoodlandMansionPieces.MansionGrid`'s constructor (WoodlandMansionPieces.java:145-148) passes `depth` values 6, 6, 3, 3 in exactly that order to the four `recursiveCorridor` calls. Unit note for §C: these are recursion-budget depths in GRID CELLS, not blocks — each cell is an 8-block room module (piece positions step by 8 in `MansionPiecePlacer`, e.g. :488-489), so the longest corridor spans at most 6 cells | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Woodland mansion orientation is chosen by one next_int_bounded(4) draw against Rotation::all(), taken immediately after the seed reseed and before any grid/corridor RNG draws. | `WoodlandMansionStructure.findGenerationPoint` (WoodlandMansionStructure.java:30) is `Rotation rotation = Rotation.getRandom(context.random());` as the very first statement; `Rotation.getRandom` (Rotation.java:112-114) is `Util.getRandom(values(), random)` = `values()[random.nextInt(4)]` (Util.java:826-828) over the 4-element `Rotation` enum, and `context.random()` was reseeded by `setLargeFeatureSeed(seed, chunkPos.x(), chunkPos.z())` in `Structure.GenerationContext.makeRandom` (Structure.java:250-254). Ordering correction for §C's pseudocode (not for this claim's own wording): the draw precedes the Y-GATE as well — the rotation is drawn before `getLowestYIn5by5BoxOffset7Blocks` and determines that gate's plus-or-minus 5 offset signs (Structure.java:185-194), so a mansion rejected by the Y-gate has still consumed the draw. §C's pseudocode runs the gate first and must be reordered | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Each attempt (up to 8 per recursive_corridor call) draws one next_int_bounded(4) direction candidate; a second next_bool() draw fires only when that candidate is East and East is not the current heading's own opposite; an accepted candidate whose two cells further ahead are both clear triggers exactly one recursive call with depth decremented by one, in that candidate's own direction, and the attempt loop then stops - there is no clockwise/counter-clockwise side-branch construction and no length halving, and a failed attempt still costs its own draws. | `WoodlandMansionPieces.MansionGrid.recursiveCorridor` (WoodlandMansionPieces.java:187-215) contains no float roll, no clockwise/counter-clockwise side branch, and no length halving. It returns immediately unless `depth > 0` (:188), sets the current cell to `CORRIDOR` and the cell one step ahead to corridor if it is `CLEAR` (:189-190), then loops UP TO 8 ATTEMPTS (:192-203): each attempt draws `Direction.from2DDataValue(this.random.nextInt(4))` (one `nextInt(4)`), plus one `nextBoolean()` whenever the drawn direction is EAST (:194); an attempt is accepted only if the direction is not the reverse of the heading and the two cells beyond the step-ahead cell in that direction are both `CLEAR` (:197-198), and on acceptance it recurses ONCE with `depth - 1` in the DRAWN direction and breaks (:199-200). The single recursion is both the branch and the continuation | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| There is no second, independent continue roll in woodland mansion corridor growth; recursive_corridor terminates when its depth reaches zero or when all 8 direction attempts fail to find a legal turn, and either way it unconditionally marks seven surrounding cells (the heading's clockwise and counter-clockwise neighbors, their step-ahead diagonals, and the two-cells-further positions in each of those three directions) as room cells with zero further RNG draws - these are what seed the room-classification pass. | There is no second roll and no separate continue step in `WoodlandMansionPieces.MansionGrid.recursiveCorridor` (WoodlandMansionPieces.java:187-215); see the preceding row. A corridor stops when `depth` reaches 0 (:188) or when all 8 direction attempts fail to find a legal turn (:192-203). Termination is also not draw-free at the tail: after the recursion attempt, the routine unconditionally runs seven `grid.setif(..., CLEAR, ROOM)` calls (:205-213) marking the clockwise, counter-clockwise, step-ahead-diagonal, two-ahead and two-sideways cells as `ROOM` — these are what seed the room-classification pass, and Context §C's reconstruction omits them entirely | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Woodland mansion corridor growth has no branch-probability threshold and performs no floating-point comparison at all; its only randomness per attempt is one next_int_bounded(4) direction draw plus a conditional next_bool() draw. | No such constant exists. `WoodlandMansionPieces.MansionGrid.recursiveCorridor` (WoodlandMansionPieces.java:187-215) takes no float threshold and performs no float comparison; its only randomness is `random.nextInt(4)` per attempt and a conditional `random.nextBoolean()` for the EAST rejection (:193-194). Correction: delete `CORRIDOR_BRANCH_PROBABILITY` from Context §C and the deliverable's constants | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Woodland mansion corridor growth has no continue-probability threshold and no hand-derived float vector; the algorithm's only randomness is the per-attempt next_int_bounded(4)/next_bool() pair described above. | No such constant exists; same source as the preceding row (WoodlandMansionPieces.java:187-215). Correction: delete `CORRIDOR_CONTINUE_PROBABILITY` from Context §C and the deliverable's constants, and drop the "first next_float 0.7310 / second 0.8314" hand-derived corridor vector, which tests a code path vanilla does not have | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| A woodland mansion's rooms are classified into three shapes: 1x1, 1x2, and 2x2. | `WoodlandMansionPieces.MansionGrid` declares exactly three room-type flags — `ROOM_1x1 = 65536`, `ROOM_1x2 = 131072`, `ROOM_2x2 = 262144` — under `ROOM_TYPE_MASK = 983040` (WoodlandMansionPieces.java:114-121). `identifyRooms` assigns one of the three per region (:329-369) and `MansionPiecePlacer.createMansion` dispatches on exactly those three values at :608-623 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Woodland mansion room classification collects every room-marked cell in row-major order, shuffles that list (consuming size-minus-one next_int draws), then for each still-unassigned cell tries, in fixed order, three 2x2 anchorings ((+x,+y), (-x,+y), (-x,-y) - there is no (+x,-y) anchoring) followed by four 1x2 neighbor directions, falling back to 1x1; it then picks a door cell via two next_bool() draws choosing one of the room's four corners, and if that cell does not edge a corridor it walks three further deterministic corner candidates (four candidates total) before clearing the door flag and placing the marker at the room's own origin corner if all four fail - the pass consumes RNG and is neither a flood fill nor a largest-first greedy split. | `WoodlandMansionPieces.MansionGrid.identifyRooms` (WoodlandMansionPieces.java:307-404) is neither a flood fill nor zero-RNG. It collects every cell whose base-grid value is `ROOM` (2) in row-major order (:310-316), SHUFFLES that list with `Util.shuffle(roomPos, this.random)` (:318; Util.java:1110-1117, consuming `size-1` `nextInt` draws), then walks the shuffled list assigning ids from 10 upward (:319-402). For each still-unassigned cell it tries, in this fixed order, three 2x2 anchorings — (+x,+y), (-x,+y), (-x,-y) (:330-356) — then the four 1x2 neighbours +x, +y, -x, -y (:357-368), falling back to 1x1. It then picks the room's door cell with TWO `nextBoolean()` draws (:371-372) and walks up to four corner fallbacks if that cell does not `edgesTo` a corridor (:374-389). Corrections: the input cells are `ROOM`-marked (written by `recursiveCorridor`'s trailing `setif` calls), not "empty"; region order is randomized, not scan order; there is no largest-first greedy split; and the pass consumes RNG, so the blueprint's "zero RNG" and its `identify_rooms` signature (no `rng` parameter) are both wrong | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Floors zero and one of a woodland mansion share the same underlying corridor-skeleton grid, but each floor's room classification is drawn independently (its own shuffle and door-direction draws), so the two floors' resulting room partitions differ from each other; the third floor does not share the skeleton at all - it is built from its own separately generated grid, choosing one eligible second-floor room as the stairs room via one next_int_bounded draw and a free corridor direction out of it via a second next_int_bounded draw, then growing its own corridor from there, with the whole floor left blocked and zero or one further draws consumed when no eligible room or corridor direction exists. | Only floors 0 and 1 do. `WoodlandMansionPieces.MansionGrid`'s constructor calls `identifyRooms(this.baseGrid, this.floorRooms[0])` and `identifyRooms(this.baseGrid, this.floorRooms[1])` (WoodlandMansionPieces.java:157-158) against the shared `baseGrid`, but floor 2 classifies against `thirdFloorGrid` (:161-163), a separate 11x11 grid built by `setupThirdFloor` (:249-305): it blocks every cell outside `baseGrid`'s house footprint, picks one random eligible door-flagged 1x2 room on floor 1 (`this.random.nextInt(potentialRooms.size())`, :266), picks a random free corridor direction out of it (`this.random.nextInt(potentialCorridors.size())`, :298), and grows one further `recursiveCorridor(..., 4)` (:299) followed by its own `cleanEdges` loop (:301-302). A second correction: floors 0 and 1 do NOT get identical room grids either — `identifyRooms` runs twice and consumes fresh RNG each time, so the two floors' room partitions differ. Test 9 (`three_floors_share_the_same_corridor_skeleton`) asserts behavior vanilla does not have | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Woodland mansion room selection draws each classified room cell's template from a per-floor, per-classification room-template pool (mirroring vanilla's FloorRoomCollection abstraction), but the draw count is not uniformly one per cell: a 1x1 cell always draws once from the plain 1x1 pool and, when it resolves to a secret room, draws a second time from a separate secret-1x1 pool with the first draw's result discarded. Per-floor pool sizes: first floor - 1x1: 5, secret 1x1: 4, 1x2 side-entrance: 9, 1x2 front-entrance: 5, secret 1x2: 2, 2x2: 4, secret 2x2: 0 (a fixed template); second and third floor - 1x1: 5, secret 1x1: 4, 1x2 side-entrance: 4 (0 when the room is the stairs room), 1x2 front-entrance: 5 (0 when the room is the stairs room), secret 1x2: 1, 2x2: 5, secret 2x2: 0. | The per-floor pools are real — `FirstFloorRoomCollection` (WoodlandMansionPieces.java:47-85), `SecondFloorRoomCollection` (:1131-1169) and `ThirdFloorRoomCollection` (:1213-1216, an empty subclass of the second), indexed as `roomCollections[floorNum]` (:472-479). The draw count is wrong. `get2x2Secret` returns the constant `"2x2_s1"` with ZERO draws (:82-84, :1166-1168); `SecondFloorRoomCollection.get1x2SideEntrance` and `get1x2FrontEntrance` return `"1x2_c_stairs"` / `"1x2_d_stairs"` with zero draws when `isStairsRoom` (:1146-1153); the pools have different sizes per getter (`nextInt(5)`, `nextInt(4)`, `nextInt(9)`, `nextInt(2)`, `nextInt(1)`); and each door-flagged room consumes an extra `this.random.nextInt(doorDirs.size())` outside the pool pick (:558). The getter is also selected by door direction and room orientation, not by shape class alone — `addRoom1x1` substitutes `get1x1Secret` when the door direction is neither EAST, NORTH, WEST nor SOUTH (:922-931). Test 8 (`room_template_pick_consumes_exactly_one_draw_per_classified_cell`) encodes the wrong invariant | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| After every woodland mansion room piece is placed, for every column across the whole chunk bounding box that does carry a placed piece at that piece's own top Y, the column is filled downward with cobblestone starting one block below that Y - through air and through liquid alike - stopping at the first solid, non-liquid block, and never writing at or below the world's own minimum Y (the strict loop bound means the lowest position ever written is one block above it). | `WoodlandMansionStructure.afterPlace` (WoodlandMansionStructure.java:45-75) exists and does fill cobblestone downward, but not under "exposed footprint edges". It scans EVERY column of the current chunk's bounding box (`chunkBB.minX()..maxX()` by `minZ()..maxZ()`, :60-61); for each it probes `y = pieces.calculateBoundingBox().minY()` and proceeds only when that block is non-empty AND the position is inside the pieces bounding box AND `pieces.isInsidePiece(pos)` (:62-63) — i.e. only under columns that DO carry a placed piece, the opposite of the blueprint's "no room above" test. It then walks `y` downward from `minY - 1` to `level.getMinY()`, placing `Blocks.COBBLESTONE.defaultBlockState()` with flag 2, breaking at the first block that is neither empty nor `liquid()` (:64-71) — so it fills through water and lava, not merely air, and there is no edge restriction. `WoodlandMansionBackfillData` must record every piece-covered column, not exposed-edge columns; test 10 (`backfill_never_touches_a_column_under_a_placed_room`) inverts the real condition | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The research corpus describes the per-floor woodland mansion room-cell grid as 11x11x5-cell; the 5 is the grid's own out-of-bounds sentinel value (matching the grid's BLOCKED marker), not a third spatial dimension - every mansion grid is 11x11, one instance per floor. | The reference resolves it, so the detail must not be left open. `WoodlandMansionPieces.SimpleGrid` (WoodlandMansionPieces.java:1171-1211) has the constructor `SimpleGrid(int width, int height, int valueIfOutside)`; the third argument is stored as `valueIfOutside` and returned by `get(x, y)` for any out-of-bounds coordinate (:1198-1200) — it is a sentinel, not a dimension. The literal 5 is `MansionGrid.BLOCKED` (:113), so every out-of-bounds probe reads as blocked. All mansion grids are built `new SimpleGrid(11, 11, 5)` (:135, :154-156, :161). `docs/research/mc-26.2/06-structures.md:187`'s "11x11x5-cell SimpleGrids" is a corpus transcription error of the constructor arguments and should be corrected; Context §C's open question and Constraint (d)'s deliberate non-resolution can both be dropped | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Secret-room variants in a woodland mansion are not a separate kind enumeration distinct from the 1x1/1x2/2x2 shape classification - each of the three shapes has exactly one secret variant (a per-floor room-template collection exposes seven getters total: 1x1, secret 1x1, 1x2 side-entrance, 1x2 front-entrance, secret 1x2, 2x2, secret 2x2), and the secret variant is selected inside the same shape dispatch that picks the plain variant. | Secret rooms are real but are NOT a distinct enumeration alongside the shape classification — they are a per-shape variant axis. `WoodlandMansionPieces.FloorRoomCollection` (WoodlandMansionPieces.java:87-104) declares one secret getter per shape: `get1x1Secret`, `get1x2Secret`, `get2x2Secret`, alongside `get1x1`, `get1x2SideEntrance`, `get1x2FrontEntrance`, `get2x2`. The secret variant is chosen inside the same shape dispatch: `addRoom1x1` substitutes `get1x1Secret` when the door direction is none of the four horizontals (:922-931), and `addRoom2x2Secret` is the `doorDir == Direction.UP` branch of the 2x2 case (:621-622, :1111-1119). Concrete template names are `1x1_as1..4`, `1x2_s1..2` / `1x2_se1`, `2x2_s1` (:57-59, :72-74, :82-84, :1141-1143, :1156-1158, :1166-1168). What minecraft.wiki says in passing is not settleable from the ASSET-D18(f) reference; the substantive claim about the classification is, and it is wrong | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
