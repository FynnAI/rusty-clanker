# M3.5-B01 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| `redstone_wire`'s state range is `4011..=5306` with default `5171`; `east/north/south/west` legal values are `[up, side, none]`; `power` is `0..=15`; property declaration order is `east, north, power, south, west`. | blocks.json, `minecraft:redstone_wire`: `states[]` min id 4011, max id 5306, default-flagged id 5171; `properties` key order `east, north, power, south, west`; `east/north/south/west` value lists each `["up","side","none"]`; `power` value list `["0".."15"]` (16 entries) | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| `repeater`'s state range is `7034..=7097` with default `7037`; property order `delay, facing, locked, powered`; `delay` legal values `["1","2","3","4"]`; `facing` legal values `[north, south, west, east]`. | blocks.json, `minecraft:repeater`: min 7034, max 7097, default 7037; `properties` key order `delay, facing, locked, powered`; `delay`=`["1","2","3","4"]`; `facing`=`["north","south","west","east"]` | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| `comparator`'s state range is `11263..=11278` with default `11264`; property order `facing, mode, powered`; `mode` legal values `[compare, subtract]`. | blocks.json, `minecraft:comparator`: min 11263, max 11278, default 11264; `properties` key order `facing, mode, powered`; `mode`=`["compare","subtract"]` | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| `piston`'s state range is `2257..=2268` with default `2263`; `sticky_piston`'s is `2235..=2246` with default `2241`; both share property order `extended, facing` and `facing` legal values `[north, east, south, west, up, down]`. | blocks.json, `minecraft:piston`: min 2257, max 2268, default 2263; `minecraft:sticky_piston`: min 2235, max 2246, default 2241; both `properties` key order `extended, facing`; `facing`=`["north","east","south","west","up","down"]` | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| `chest`'s state range is `3987..=4010` with default `3988`; property order `type, facing, waterlogged`; `type` legal values `[single, left, right]`. | blocks.json, `minecraft:chest`: min 3987, max 4010, default 3988; `properties` key order `type, facing, waterlogged`; `type`=`["single","left","right"]` | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| `furnace`'s state range is `5327..=5334` with default `5328`; property order `facing, lit`. | blocks.json, `minecraft:furnace`: min 5327, max 5334, default 5328; `properties` key order `facing, lit` | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| `hopper`'s state range is `11313..=11322` with default `11313`; property order `enabled, facing`; `facing` legal values `[down, north, south, west, east]`. | blocks.json, `minecraft:hopper`: min 11313, max 11322, default 11313; `properties` key order `enabled, facing`; `facing`=`["down","north","south","west","east"]` | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| `redstone_torch`'s state range is `6885..=6886` with default `6885` (property `lit`); `redstone_wall_torch`'s is `6887..=6894` with default `6887` (property order `facing, lit`). | blocks.json, `minecraft:redstone_torch`: min 6885, max 6886, default 6885, `properties`=`{lit}`; `minecraft:redstone_wall_torch`: min 6887, max 6894, default 6887, `properties` key order `facing, lit` | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| Every state of a given block shares an identical property-name set and order across all of that block's own states (no per-state property omission). | blocks.json, exhaustive scripted check over all 1196 blocks / 32366 states: for every block, every state's own `properties` key sequence compared positionally against that block's own top-level `properties` key sequence — 0 mismatches | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| `blocks.json`'s block-level `"properties"` object's key order always matches that block's own `states[].properties` key order. | Same exhaustive script as above (one pass computes both claims together) — 0 mismatches across all 1196 blocks / 32366 states | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| `minecraft:air` has zero properties and exactly one state (id `0`, flagged default). | blocks.json, `minecraft:air`: `properties` key omitted entirely; `states` has exactly one entry, `{"id":0,"default":true}` | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| The `REPLACEABLE_BLOCKS` candidate list (§3.5) — every individual member's presence in the real `minecraft:replaceable` block tag, and the list's completeness (no omitted real member) — is UNVERIFIABLE from this blueprint's own local access (no cached `tags/` export was available at authoring time) and must be checked against a real `<tags_root>/data/minecraft/tags/block/replaceable.json` before this blueprint's Changeset 2 lands. | `C:\Users\krank\mc-research\26.2\datagen\generated\data\minecraft\tags\block\replaceable.json` — real tag has 29 members (28 excluding `minecraft:snow`, which the blueprint correctly special-cases separately). Every one of the blueprint's 20 listed names is a true member (no false positives), but the list is INCOMPLETE — it omits 8 real members: `bush`, `short_dry_grass`, `tall_dry_grass`, `resin_clump`, `warped_roots`, `nether_sprouts`, `crimson_roots`, `leaf_litter`. Correct full list (28 entries, excluding snow): `air, water, lava, short_grass, fern, dead_bush, bush, short_dry_grass, tall_dry_grass, seagrass, tall_seagrass, fire, soul_fire, vine, glow_lichen, resin_clump, light, tall_grass, large_fern, structure_void, void_air, cave_air, bubble_column, warped_roots, nether_sprouts, crimson_roots, leaf_litter, hanging_roots` | WRONG — corrected | research role (TEST-D57 pass) | 2026-09-02 |
| `minecraft:snow`'s true vanilla replaceable rule being exactly "member of `minecraft:replaceable` AND `layers == 1`" (vs. some other or code-only condition the tag file alone cannot express) is UNVERIFIABLE from the tag file alone — needs cross-checking against decompiled `SnowLayerBlock` behavior (ASSET-D18(f)) or black-box observation (ASSET-D18(d)). | Decompiled `net/minecraft/world/level/block/entity` tree n/a; `net/minecraft/world/level/block/SnowLayerBlock.java:113-120` (`canBeReplaced` override) and `net/minecraft/world/level/block/Blocks.java:1931-1943` (`SNOW` properties, `.replaceable()`). Finding: the static `replaceable` flag backing the tag is set once per-block (`Blocks.java:1937`), so it is `true` uniformly for *every* snow layer state — it is not itself gated by `layers`. The `layers==1`-only behavior comes from a separate, item-context-dependent override (`SnowLayerBlock.canBeReplaced(state, context)`): when the item in hand is not snow (or the stack is already at 8 layers), `return layers == 1`; when the item in hand *is* snow and layers<8, the position accepts another layer regardless of current `layers` (up to 8) — a case no per-state-only boolean can express. The blueprint's own code (§4.2, `is_state_replaceable`'s snow branch, `layers=="1"`) matches exactly the default/non-snow-item path, which is the correct behavior for a generic state-only replaceable query; the tag-membership "AND" is vacuous (always true) rather than a real per-state filter, and the item-context extension case is out of reach of a per-state table by construction, not a gap this blueprint introduces | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
