# M3.5-B03 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| §3.8's per-packet masked-field list (which named field(s) each listed packet type carries that this harness must mask) and their exact byte offsets/widths within that packet's body. | Decompiled `net/minecraft/network/protocol/{common,game}/Clientbound*Packet.java`, pinned 26.2. Per packet, field order/encoding verified directly from each class's own read/write body: **`KeepAlive`** — sole field `id: long`, offset 0, width 8 (fixed, big-endian, whole body) — this is the only packet in the table with a truly static offset+width. **`Login`/join-game** — the masked value is not a top-level field at all: `ClientboundLoginPacket` has no `seed`/hash field of its own; it embeds `CommonPlayerSpawnInfo`, whose own 3rd field is `seed: long` (written via `output.writeLong`, constructed server-side as `BiomeManager.obfuscateSeed(level.getSeed())` — `ServerPlayer.java:2169-2179` — so it is functionally a seed hash despite the plain field name `seed`, not literally named "world-seed-hash"). `CommonPlayerSpawnInfo` itself sits after `playerId:i32`(4B, offset 0) + `hardcore:bool`(1B) + `levels:Set<ResourceKey>` (VarInt-counted, each a variable-length string) + `dimensionType:Holder<DimensionType>` (a variable-width registry-or-inline entry) — so `seed`'s own byte offset is data-dependent, never a fixed constant; its width is fixed (8B, plain long) once reached. **`PlayerPosition`** — `id` (teleport-id) is the packet's first field (`StreamCodec.composite`, `ByteBufCodecs.VAR_INT` first) — offset 0 (fixed), width variable (VarInt, `ClientboundPlayerPositionPacket.java:12-21`). **`AddEntity`** — `id` first field, offset 0, VarInt (variable width); `uuid` (16B fixed width) follows immediately after `id`, at an offset that varies with `id`'s own VarInt-encoded length (`ClientboundAddEntityPacket.java:95-121`) — both maskable, neither has one single universal fixed offset. **`SetEntityData`** — `id` is the sole leading field, offset 0, VarInt (variable width); the remainder is a self-terminating (0xFF-marker) metadata list, correctly left unmasked (`ClientboundSetEntityDataPacket.java`). **`MoveEntity*` family** (`Pos`/`PosRot`/`Rot` — three distinct wire packet ids) — `entityId` is the first field in all three variants, offset 0, VarInt (`ClientboundMoveEntityPacket.java:95-186`). **`RemoveEntities`** — the entire body *is* the entity-id list (`IntList` via `writeIntIdList`, VarInt count + VarInt ids, `ClientboundRemoveEntitiesPacket.java`) — masking "the entity-id field(s)" here means masking the whole body, since no other field exists. **`PlayerInfoUpdate`** — the latency sub-field is not statically positioned at all: it lives inside a per-player `Entry`, present on the wire *only if* the packet's own `actions: EnumSet<Action>` bitmask includes `UPDATE_LATENCY`, and when present its byte offset depends on (a) which of the other 7 actions are also active for that packet instance and their own variable-width per-entry payloads (`ADD_PLAYER`'s name+`PropertyMap`, `INITIALIZE_CHAT`'s nullable chat-session data, `UPDATE_GAME_MODE`'s VarInt, `UPDATE_LISTED`'s bool — enum declaration order: `ADD_PLAYER, INITIALIZE_CHAT, UPDATE_GAME_MODE, UPDATE_LISTED, UPDATE_LATENCY, UPDATE_DISPLAY_NAME, UPDATE_LIST_ORDER, UPDATE_HAT`), and (b) every preceding player entry's own total width in the same packet (`ClientboundPlayerInfoUpdatePacket.java:57-197`) — masking this field requires a structural, `actions`-driven decode, not a byte range. **`SetTime`** — 26.2 has replaced the classic two-`long` (`gameTime`,`dayTime`) shape entirely: the real record is `(gameTime: long, clockUpdates: Map<Holder<WorldClock>, ClockNetworkState>)` (`ClientboundSetTimePacket.java`) — there is no "day-time value" field any more. `gameTime` is still fixed offset 0 / width 8; the real-time-driven state formerly called "day time" now lives in the variable-size `clockUpdates` map, which the masking table must name instead of a nonexistent day-time field. | WRONG — corrected | research role (TEST-D57 pass) | 2026-09-02 |
| §3.8's claim that `LevelChunkWithLight`'s body begins with the chunk's own `(x, z)` position fields (needed by `normalize_chunk_batch_order`), and their exact encoding (raw `i32` pair vs. some other representation). | `net/minecraft/network/protocol/game/ClientboundLevelChunkWithLightPacket.java:34-45`: `write()` calls `output.writeInt(this.x)` then `output.writeInt(this.z)` as the packet's first two operations, before `chunkData`/`lightData`; both are plain (non-VarInt) big-endian 4-byte ints — `x` at offset 0, `z` at offset 4, both fixed-width. This is one of the few fields in the whole masking table with a genuinely static byte offset (no variable-width field precedes it) | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| §3.7's claim that the pinned azalea revision (TEST-D8) exposes a public entry point capable of dispatching a raw `(packet_id, body)` pair to its own `ClientboundGamePacket` variant name outside of a live connection's own internal routing — if false, `packet_name` resolution falls back to `None` universally (§3.7's own stated safe default), which changes only report readability, never pass/fail correctness, but should still be recorded as a finding either way. | Pinned checkout `~/.cargo/git/checkouts/azalea-55f499961895c5bc/6249c29/`. `azalea-protocol/src/packets/mod.rs:41-56`: `pub trait ProtocolPacket { fn id(&self)->u32; fn name(&self)->&'static str; fn read(id:u32, buf:&mut Cursor<&[u8]>) -> Result<Self, Box<ReadPacketError>>; fn write(...); }`. `azalea-protocol-macros/src/lib.rs:399-433` (the `declare_state_packets!` codegen invoked by `azalea-protocol/src/packets/game/mod.rs`) generates exactly this `ProtocolPacket` impl for the `ClientboundGamePacket` enum, with `read`/`name` both fully public and callable standalone: `<ClientboundGamePacket as ProtocolPacket>::read(id, &mut Cursor::new(&body))` yields a typed variant, and `.name()` on it returns the packet's own resource-name string directly (even simpler than reflecting off the variant) | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| §3.5's claim (from `docs/research/mc-26.2/02-network-protocol.md` §3.4/§3.7) that `online-mode=false` skips the AES/CFB8 encryption handshake entirely in the pinned 26.2 server, which this blueprint's whole capture mechanism (plaintext frame decode, no decryption step) depends on. | `net/minecraft/server/network/ServerLoginPacketListenerImpl.java:118-131` (`handleHello`): the encryption branch (`this.state = KEY; connection.send(new ClientboundHelloPacket(...))`) is entered only `if (this.server.usesAuthentication() && !this.connection.isMemoryConnection())`; otherwise `startClientVerification(UUIDUtil.createOfflineProfile(...))` runs directly, no `ClientboundHelloPacket` ever sent. `net/minecraft/server/MinecraftServer.java:1344-1349` (`usesAuthentication() { return this.onlineMode; }`) and `net/minecraft/server/dedicated/DedicatedServer.java:195` (`this.setUsesAuthentication(properties.onlineMode)`) confirm `usesAuthentication()` is a direct reflection of `server.properties`' `online-mode`. With `online-mode=false` (this harness's own setup on both sides), the AES/CFB8 key-exchange state is never entered | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
