# M3.5-B06 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| **CONFIRMED** — `SectionBlocksUpdate`'s wire shape (`section_pos` as a plain, non-VarInt `i64`; `((chunk_x & 0x3FFFFF) << 42) \| (section_y & 0xFFFFF) \| ((chunk_z & 0x3FFFFF) << 20)`; `states` as a VarInt-counted `Vec` of VarLong `(state_id << 12) \| (local_x << 8 \| local_z << 4 \| local_y)`): read directly from azalea's own `ChunkSectionPos::azalea_write`/`BlockStateWithPosition::azalea_write` at the pinned rev `6249c295`. | Independently re-read at `~/.cargo/git/checkouts/azalea-55f499961895c5bc/6249c29/` (matches the pinned rev exactly). `azalea-protocol/src/packets/game/c_section_blocks_update.rs`: `BlockStateWithPosition::azalea_write` computes `data = (state.id() << 12) \| (x<<8 \| z<<4 \| y)`, written via `u64::azalea_write_var` (VarLong). `azalea-core/src/position.rs:1036-1051`: `ChunkSectionPos::azalea_write` computes `long = ((x & 0x3FFFFF)<<42) \| (y & 0xFFFFF) \| ((z & 0x3FFFFF)<<20)`, written via `i64::azalea_write` (`azalea-buf/src/impls/primitives.rs:147-153`, a plain `write_i64::<BE>`, not variable-length). `Vec<T>`'s own `AzBuf` impl (`azalea-buf/src/impls/extra.rs:73-91,133`, `impl_for_list_type!(Vec<T>)`) length-prefixes with `u32::azalea_write_var` (VarInt) — all formulas match exactly | CONFIRMED | research role (TEST-D57 pass, independently re-verified) | 2026-09-02 |
| **UNVERIFIED** — `SectionBlocksUpdate`'s packet id `0x54`: derived via this project's own established azalea-declaration-order cross-check (self-validated against `BlockUpdate`'s own already-committed `0x08`), not against a locally-generated `reports/packets.json` or a real client capture. Confirm via `xtask fetch-data`/a real client capture at implementation time. | `C:\Users\krank\mc-research\26.2\datagen\generated\reports\packets.json` (now locally available to the research role, though not to the blueprint's original author): `data.play.clientbound["minecraft:section_blocks_update"].protocol_id` = 84 = `0x54`, exact match. Cross-checked in the same pass: `minecraft:block_update`=8=`0x08`, `minecraft:login`=49=`0x31`, `minecraft:player_position`=72=`0x48` — all match the blueprint's own azalea-declaration-order-derived values | CONFIRMED | research role (TEST-D57 pass, upgraded from UNVERIFIED using a source now available) | 2026-09-02 |
| **CONFIRMED** — the single-vs-batch coalescing rule (exactly 1 changed block in a section → `BlockUpdate`; ≥2 → one `SectionBlocksUpdate`): directly cited from `docs/research/mc-26.2/03-world-chunks.md`'s own restatement of `ChunkHolder.broadcastChanges`/`ServerChunkCache.broadcastChangedChunks`. | Independently re-read `net/minecraft/server/level/ChunkHolder.java:174-216` directly (not merely the research-doc restatement): `broadcastChanges` iterates `changedBlocksPerSection`; `if (changedBlocks.size() == 1)` → `new ClientboundBlockUpdatePacket(pos, state)`; `else` → `new ClientboundSectionBlocksUpdatePacket(sectionPos, changedBlocks, section)` — exact match | CONFIRMED | research role (TEST-D57 pass, independently re-verified against decompiled source, not just the research doc) | 2026-09-02 |
| **CONFIRMED** — `HopperBlock.checkPoweredState`'s exact rule (`ENABLED = !hasNeighborSignal(pos)`, called unconditionally from both `onPlace` and `neighborChanged`, no direction filter): `javap -c -p` disassembly of the pinned oracle jar's own `HopperBlock.class`, this session. | Independently re-disassembled this session: extracted `META-INF/versions/26.2/server-26.2.jar` from `oracle/26.2/server.jar`, `javap -p -c net/minecraft/world/level/block/HopperBlock.class`. `checkPoweredState`: `hasNeighborSignal` → `ifne`/push 0 else push 1 (i.e. `newEnabled = !hasNeighborSignal`); compares to `state.getValue(ENABLED)`; on mismatch, `setValue(ENABLED, newEnabled)` + `level.setBlock(pos, ..., 2)`. `onPlace` calls `checkPoweredState` unconditionally (after an unrelated same-block guard); `neighborChanged` calls it directly with no direction/source filtering at all | CONFIRMED | research role (TEST-D57 pass, independently re-disassembled) | 2026-09-02 |
| **CONFIRMED** — `HopperBlockEntity.tryMoveItems`'s own gate order (cooldown decrements unconditionally every tick; the push/pull attempt itself is additionally gated on `!isOnCooldown() && ENABLED`, checked together): `javap -c -p` disassembly of the pinned oracle jar's own `HopperBlockEntity.class`, this session. | Independently re-disassembled this session: `javap -p -c net/minecraft/world/level/block/entity/HopperBlockEntity.class`. Precision note on method attribution: the unconditional `cooldownTime--` and the game-time bookkeeping live in `pushItemsTick` (the actual per-tick entry point), which then only calls `tryMoveItems` when `!isOnCooldown()`; `tryMoveItems` itself re-checks `isOnCooldown()` (redundant at this call site, harmless) then checks `state.getValue(ENABLED)`, bailing (return `false`) if either fails, before any inventory push/pull logic runs — the blueprint's overall behavioral claim (decrement every tick; push/pull additionally gated on cooldown+ENABLED together, checked before any transfer) is exactly correct; only the specific method housing the decrement (`pushItemsTick`, not literally `tryMoveItems`) differs from the claim's own phrasing | CONFIRMED | research role (TEST-D57 pass, independently re-disassembled) | 2026-09-02 |
| **UNVERIFIED** — this blueprint's own choice to place the new `ENABLED` gate in `HopperBlockEntity::tick` immediately after the cooldown gate and before the pre-existing `is_locked_by_redstone` gate: consistent with the confirmed bytecode's own combined condition, but the *relative* order against `is_locked_by_redstone` specifically (a check this project added independently, with no vanilla bytecode counterpart identified in this session's own disassembly) is this blueprint's own design choice, not a verified vanilla fact. | `is_locked_by_redstone` is this project's own addition (`crates/mechanics/src/stage7/ecs.rs`'s own doc comment, restated in this blueprint's §3.2) with no corresponding vanilla mechanism found in this session's own `HopperBlockEntity.class`/`HopperBlock.class` disassembly — there is no vanilla fact to check the *relative* gate ordering against; this is an internal architectural decision, not a TEST-D57-applicable vanilla-behavior claim | UNVERIFIABLE | research role (TEST-D57 pass) | 2026-09-02 |
