# M3.5-B05 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Furnace block-entity NBT tag names (`BurnTime`/`CookTime`/`CookTimeTotal` vs. a possible 26.2 rename to `lit_time_remaining`/`cooking_time_spent`/`cooking_total_time`) — Context 2.4. | `net/minecraft/world/level/block/entity/AbstractFurnaceBlockEntity.java:114-136` (`saveAdditional`/`loadAdditional`): real vanilla 26.2 uses exactly the renamed, lower-`snake_case` keys — `output.putShort("cooking_time_spent", ...)`, `output.putShort("cooking_total_time", ...)`, `output.putShort("lit_time_remaining", ...)` — confirming the blueprint's own "possible rename" candidate exactly, not `BurnTime`/`CookTime`/`CookTimeTotal` (those are pre-26.2 names, no longer real vanilla). Additionally, real vanilla persists a **fourth** timing field, `lit_total_time: Short`, absent from both of the blueprint's candidate lists entirely | WRONG — corrected | research role (TEST-D57 pass) | 2026-09-02 |
| `minecraft:comparator`'s persisted analog-output tag name (`OutputSignal`) — Context 2.4/2.5. | `net/minecraft/world/level/block/entity/ComparatorBlockEntity.java:16-19`: `saveAdditional` writes exactly `output.putInt("OutputSignal", this.output)`; `loadAdditional` reads `input.getIntOr("OutputSignal", 0)`. No other type-specific field exists on this block entity | CONFIRMED | research role (TEST-D57 pass) | 2026-09-02 |
| Whether real vanilla 26.2 furnace/hopper/chest block-entity NBT carries any additional tier-1-relevant field this blueprint's own schema omits (e.g. a furnace's own `RecipesUsed` map) — not modeled here, not verified against `docs/research/mc-26.2/` at blueprint-writing time. | Furnace: `AbstractFurnaceBlockEntity.java:125,136` confirms `RecipesUsed` (a `Map<ResourceKey<Recipe<?>>, Integer>` via `RECIPES_USED_CODEC`) is real and present, matching the blueprint's own named example exactly — not modeled by this blueprint's schema. Furnace also carries the `lit_total_time: Short` field found under claim 1 above, which this project's own `crates/mechanics/src/block_entity/furnace.rs:113-114,230-233,268` already tracks in memory (`self.lit_total_time`) but its own doc comment explicitly declines to persist it, reasoning "vanilla's own furnace block-entity NBT schema lists only `BurnTime`/`CookTime`/`CookTimeTotal`" — a premise this pass shows is false for 26.2; on load it falls back to `lit_total_time: lit_time_remaining` (an approximation, not a true round trip). Hopper: `HopperBlockEntity.java:48-65` carries only `Items`+`TransferCooldown` beyond the shared container base — no additional field found. Secondary finding (surfaced while checking this claim, not itself one of the blueprint's listed sub-facts): the pre-existing, already-"CONFIRMED"-labelled `Lock: String?` field (§2.4, shared by chest/furnace/hopper) does not match real 26.2 either — `net/minecraft/world/level/block/entity/BaseContainerBlockEntity.java:30,40,152` delegates to `net/minecraft/world/LockCode.java`, whose real tag key is lower-case `"lock"` (`LockCode.TAG_LOCK`), and whose value is a full `ItemPredicate` compound via `LockCode.CODEC`, not a plain string | WRONG — corrected | research role (TEST-D57 pass) | 2026-09-02 |
