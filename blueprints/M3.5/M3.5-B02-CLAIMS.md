# M3.5-B02 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| CONFIRMED (verified directly in this session against the committed generated table, `crates/registries/generated/v776/block_states.rs`): every `default_state::<BLOCK>` literal cited in §3.6 (`AIR`=0, `BEDROCK`=85, `CHEST`=3988, `COMPARATOR`=11264, `CRYING_OBSIDIAN`=21820, `FURNACE`=5328, `HOPPER`=11313, `OBSIDIAN`=3369, `PISTON`=2263, `PISTON_HEAD`=2271, `REDSTONE_BLOCK`=11311, `REDSTONE_TORCH`=6885, `REDSTONE_WALL_TORCH`=6887, `REDSTONE_WIRE`=5171, `REINFORCED_DEEPSLATE`=32085, `REPEATER`=7037, `RESPAWN_ANCHOR`=21821, `SMOKER`=20755, `STICKY_PISTON`=2241, `STONE`=1, `DIRT`=10, `GRASS_BLOCK`=9, `BLAST_FURNACE`=20763) matches the literal each of `wire.rs`/`torch.rs`/`repeater.rs`/`comparator.rs`/`piston.rs`/`registration.rs`/`mining.rs` already hardcodes independently. | `crates/registries/generated/v776/block_states.rs` (`default_state` module, re-greped this session — all 23 values match exactly); spot-checked against `crates/mechanics/src/redstone/piston.rs:117,132-134` (`BEDROCK_ID`/`OBSIDIAN_ID`/`CRYING_OBSIDIAN_ID`/`REINFORCED_DEEPSLATE_ID`) and `crates/mechanics/src/redstone/registration.rs:161` (`REDSTONE_BLOCK_STATE_ID`) | CONFIRMED | research role (TEST-D57 pass, independently re-verified) | 2026-09-02 |
| UNVERIFIABLE without B01 merged: the exact module path and per-block constant/enum-variant names M3.5-B01 will actually emit (`state_ranges::<BLOCK>`, `BlockId::<BLOCK>` are this blueprint's own illustrative placeholders, §3.1) — the research role cannot confirm spelling that does not exist yet; re-verify against B01's actual merged output before Step 2 begins. | Checked this session: `crates/registries/generated/v776/` currently contains only `MANIFEST.json`, `block_states.rs`, `mod.rs`, `registries.rs`, `tags.rs` — no `block_state_properties.rs` exists yet anywhere under `crates/registries/`; M3.5-B01 has not been implemented/merged as of this pass | UNVERIFIABLE | research role (TEST-D57 pass, independently re-verified) | 2026-09-02 |
| CONFIRMED against `blocks.json` by the planning role (§3.7): `minecraft:chest` is 3987..=4010 (24 states, first `waterlogged=true`, default 3988); `replay.rs`'s `CHEST_RANGE = (3987, 4010)` is correct and `mining.rs`'s `CHEST.0..=CHEST.0+23` derivation is the defect; every one of the other seven `*_RANGE` constants matches its production counterpart exactly. | blocks.json `minecraft:chest`: states id 3987=`{type=single,facing=north,waterlogged=true}` (first, non-default), id 3988=default (`waterlogged=false`), id 4010=last (`type=right,facing=east,waterlogged=false`) — matches exactly. `crates/testing/gametest/src/replay.rs:450-464` (all 9 `*_RANGE` consts — note: the file actually declares 9, not 7 "others" plus chest; `WIRE_RANGE`,`TORCH_FLOOR_RANGE`,`TORCH_WALL_RANGE`,`REPEATER_RANGE`,`COMPARATOR_RANGE`,`PISTON_RANGE`,`STICKY_PISTON_RANGE`,`HOPPER_RANGE` all individually cross-checked against blocks.json — all match). `crates/server/src/play/mining.rs:1189-1218` (`chest_state_id`/`decode_chest_state`, anchored on `CHEST.0`=3988): manually traced — raw 3987 underflows to `None` (should decode to `single,north`); raw 3989 decodes to `(North, Single)` but the real state at 3989 is `(north,left,waterlogged=true)` — wrong; raw 4011 (`redstone_wire`'s own first state, not a real chest id at all) decodes as `(East, Right)` — all three defects reproduced exactly as claimed | CONFIRMED | research role (TEST-D57 pass, independently re-verified) | 2026-09-02 |
| CONFIRMED by the planning role against the committed corpus: no fixture under `crates/testing/gametest/corpus/redstone/` declares a chest at raw id 3987 or 3989 or any `waterlogged=true` state (grep over every `.ron`: zero hits), so `decode_chest_state`'s defect is dormant against `parity-check redstone` and `placement-diff` today; §5's `chest_decode_covers_waterlogged_ids` is the only test that exercises it. | `grep -rn "waterlogged=true" crates/testing/gametest/corpus/redstone/` → 0 hits; `grep -rln "state_id: 3987\|state_id: 3989" crates/testing/gametest/corpus/redstone/` → 0 hits (the only `waterlogged`/`398x`-adjacent hits found are unrelated `waterlogged=false` chest fixtures at id 3994) | CONFIRMED | research role (TEST-D57 pass, independently re-verified) | 2026-09-02 |
| CONFIRMED (verified directly in this session): `rc-mechanics/Cargo.toml` already depends on `rc-registries` normally (added by a prior M3 field-report changeset); `rc-physics/Cargo.toml` depends on only `rc-core` today; `rc-gametest/Cargo.toml` depends on neither `rc-registries` nor `rc-paritybot`/`azalea`. | `crates/mechanics/Cargo.toml` `[dependencies]` includes `rc-registries = { path = "../registries" }`; `crates/physics/Cargo.toml` `[dependencies]` is exactly `rc-core = { path = "../core" }`; `crates/testing/gametest/Cargo.toml` `[dependencies]`/`[dev-dependencies]` lists `rc-core, rc-chunk-storage, rc-messaging, rc-mechanics, serde, ron, postcard, thiserror` / `xtask` only — no `rc-registries`, `rc-paritybot`, or `azalea` anywhere | CONFIRMED | research role (TEST-D57 pass, independently re-verified) | 2026-09-02 |
| CONFIRMED (verified directly in this session against `xtask/src/lint_deps.rs`): the `SHARED`/`SIM`/`NETRENDER` lists (§3.4) are exactly as restated; a `rc-physics -> rc-registries` edge trips none of the four `check_rules` checks. | `xtask/src/lint_deps.rs:16-39`: `SHARED` = `[rc-core, rc-nbt, rc-registries, rc-protocol-macros, rc-protocol, rc-mod-api, rc-mod-host, rc-physics]`, `SIM` = `[rc-scheduler, rc-mechanics]`, `NETRENDER` = `[rc-render, rc-protocol, rc-transport-inproc, rc-transport-net, rc-auth, rc-cluster, rc-proxy]` — exact match. Rule 1 (`check_rule1`) only checks reachability, never forbids an extra edge; Rules 3/4 are scoped to `rc-messaging`/`rc-mod-api` only. `crates/registries/Cargo.toml` depends only on `rc-core`/`rc-nbt` (neither in `NETRENDER`), so a new `rc-physics -> rc-registries` edge opens no `SIM<->NETRENDER` path | CONFIRMED | research role (TEST-D57 pass, independently re-verified) | 2026-09-02 |
