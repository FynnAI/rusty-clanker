# M6-B07 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Vanilla's server tick rate is 20 ticks per second, i.e. a fixed 50ms per tick — restated as ARCH-D20's admission-deadline formula "each region's deadline = `last_tick_start + 50ms`" (§E) and as the `tick_budget_ms: 50.0` value the composition root's `MetricsRegistry` is constructed with (§C step 5). | `SharedConstants.TICKS_PER_SECOND = 20` and `SharedConstants.MILLIS_PER_TICK = 50` (SharedConstants.java:138-139); `TickRateManager` fields `tickrate = 20.0F` and `nanosecondsPerTick = TimeUtil.NANOSECONDS_PER_SECOND / 20L` (TickRateManager.java:9-10), read once per iteration of the server loop at MinecraftServer.java:733. One nuance worth knowing, though it does not contradict the claim: 50ms is vanilla's *default*, not an immutable constant — `TickRateManager.setTickRate` (TickRateManager.java:19-21) recomputes `nanosecondsPerTick` from an operator-set rate (the `/tick rate` command), and the sprint path (MinecraftServer.java:728-731) drops the inter-tick wait to zero entirely | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Anvil on-disk region-file save format groups chunks into 32×32-chunk regions — "Anvil's own on-disk region-file granularity (32×32 chunks)" (§C.2), independent of this project's own 16×16-chunk scheduling grid-cell size. | `ChunkPos.REGION_BITS = 5`, `REGION_SIZE = 32`, `REGION_MASK = 31`, `REGION_MAX_INDEX = 31` (ChunkPos.java:37-40), with `getRegionX`/`getRegionZ`/`getRegionLocalX`/`getRegionLocalZ` (ChunkPos.java:131-152) shifting and masking by those; `RegionFile.getOffsetIndex` (RegionFile.java:349-351) indexes the header as `regionLocalX + regionLocalZ * 32`, and the header table is sized `SECTOR_INTS = 1024` (RegionFile.java:28-29) = 32 × 32 entries; `RegionFileStorage` (RegionFileStorage.java:45) names each file `r.<regionX>.<regionZ>.mca` from those same region coordinates | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The vanilla dimension registry identifier for the overworld is the string `"minecraft:overworld"` — the one dimension name this blueprint's `--region-layout` parser and startup dimension resolution accept (§C step 2, §J.1). | `Level.OVERWORLD = ResourceKey.create(Registries.DIMENSION, Identifier.withDefaultNamespace("overworld"))` (Level.java:95), with `Identifier.DEFAULT_NAMESPACE = "minecraft"` (Identifier.java:23, used by `withDefaultNamespace` at :48-50) — so the key's identifier stringifies as `minecraft:overworld`. The same path/namespace pair is used by `LevelStem.OVERWORLD` (LevelStem.java:16) and `BuiltinDimensionTypes.OVERWORLD` (BuiltinDimensionTypes.java:8); datagen confirms the resource at data/minecraft/dimension_type/overworld.json | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Java Edition server listens by default on TCP port 25565 — the `--bind` flag's own default value `"0.0.0.0:25565"` this blueprint fixes as the process's own default bind address (§C step 14, §J). | `SharedConstants.DEFAULT_MINECRAFT_PORT = 25565` (SharedConstants.java:113); `DedicatedServerProperties.serverPort = this.get("server-port", 25565)` (DedicatedServerProperties.java:72), applied at DedicatedServer.java:207-209 before `startTcpServerListener(localAddress, getPort())` (:215). The wildcard half of the blueprint's default is faithful too: `DedicatedServerProperties.serverIp` defaults to the empty string (DedicatedServerProperties.java:58), and DedicatedServer.java:203-205 passes a `null` bind address when it is empty, i.e. binds every interface — logged as `*` at :212 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
