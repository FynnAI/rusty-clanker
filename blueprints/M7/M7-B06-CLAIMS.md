# M7-B06 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| Vanilla player names must be at most 16 UTF-16 code units long, with no minimum length, and every code unit must be a printable ASCII character in the range 33-126 (0x21-0x7E); on the wire the 16-unit cap is enforced first, by the string decoder rejecting an over-long frame before the validator's own length check would ever run. | Both halves differ. `StringUtil.isValidPlayerName` (StringUtil.java:69-71) returns false above length 16 and otherwise requires that no character of the name be at most 32 or at least 127 — an upper bound of 16 with NO lower bound (the empty string passes), and a character set of every UTF-16 code unit in 33..126, i.e. all printable ASCII 0x21..0x7E. Names such as `a-b`, `x.y` or `Hi!` are accepted by 26.2 and rejected by the blueprint's rule. The only other constraint on the login path is the wire cap `input.readUtf(16)` in `ServerboundHelloPacket`'s reader (ServerboundHelloPacket.java:15), which is the same 16-character maximum; the validator is invoked at `ServerLoginPacketListenerImpl.handleHello` (:120) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In online mode, vanilla's Login sequence sends an EncryptionRequest, awaits an EncryptionResponse, and byte-compares the returned verify token before proceeding. | `ServerLoginPacketListenerImpl.handleHello` (:126-128) sends `ClientboundHelloPacket` (login clientbound `minecraft:hello`, protocol_id 1 per reports/packets.json) and sets state KEY when `server.usesAuthentication() && !connection.isMemoryConnection()`; `handleKey` (:171-179) then calls `ServerboundKeyPacket.isChallengeValid` (ServerboundKeyPacket.java:47-53) = `Arrays.equals(challenge, Crypt.decryptUsingKey(privateKey, encryptedChallenge))` and throws "Protocol error" on mismatch, strictly before the shared secret is decrypted at :181. Nuance worth carrying into the proxy driver: the branch is also skipped when the requested name equals the singleplayer profile name (:122-124) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| In offline mode, vanilla's Login sequence exchanges no encryption packets at all. | `ServerLoginPacketListenerImpl.handleHello` (:129-131): when authentication is off (or the connection is in-memory) it goes straight to `startClientVerification(UUIDUtil.createOfflineProfile(name))`. No `ClientboundHelloPacket` is ever sent, and an unsolicited `ServerboundKeyPacket` would fail `handleKey`'s `state == KEY` precondition (:172) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Login-phase RSA decryption of the shared secret and verify token uses PKCS#1 v1.5 padding. | `Crypt.decryptUsingKey` / `cipherData` / `setupCipher` (Crypt.java:172-187) call `Cipher.getInstance(key.getAlgorithm())`, the algorithm string being "RSA" (keys produced by `KeyFactory.getInstance("RSA")`, :141 and :151) — used for both the shared secret (`decryptByteToSecretKey`, :158-166) and the verify token (ServerboundKeyPacket.java:49). The padding is not spelled anywhere in the Mojang source; it follows from the JCA standard-algorithm-names specification and the JDK provider default, under which the bare `RSA` transformation resolves to `RSA/ECB/PKCS1Padding`, i.e. PKCS#1 v1.5 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Login-phase stream cipher, once installed, is AES-128 in CFB8 mode. | `Crypt.getCipher` (Crypt.java:190-198) = `Cipher.getInstance("AES/CFB8/NoPadding")` initialised with `IvParameterSpec(key.getEncoded())` — the IV is the key bytes themselves. The key is the 128-bit AES secret the client generated (`Crypt.generateSecretKey`, :61-68, `keyGenerator.init(128)`), rebuilt server-side as `SecretKeySpec(key, "AES")` (:158-166) and installed via `connection.setEncryptionKey(decryptCipher, encryptCipher)` at ServerLoginPacketListenerImpl.java:184-186 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla sends the SetCompression packet itself without compression applied. | `ServerLoginPacketListenerImpl.verifyLoginAndFinishConnectionSetup` (:148-154) sends `ClientboundLoginCompressionPacket` (login clientbound `minecraft:login_compression`, protocol_id 3) with `PacketSendListener.thenRun(() -> connection.setupCompression(threshold, true))`. `Connection.setupCompression` (Connection.java:544-557) is what inserts the "compress"/"decompress" pipeline handlers, so at the moment this packet is written the pipeline still carries neither. The send is additionally gated on `getCompressionThreshold() >= 0 && !connection.isMemoryConnection()` | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla only applies compression to packets sent strictly after the SetCompression send completes. | `PacketSendListener.thenRun` (PacketSendListener.java:16-23) returns a Netty `ChannelFutureListener` whose runnable runs when the write future completes; that runnable is the `connection.setupCompression(...)` call passed at ServerLoginPacketListenerImpl.java:152. The compression encoder/decoder therefore enter the pipeline only after the compression packet has been written | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla sends LoginSuccess after SetCompression. | `verifyLoginAndFinishConnectionSetup` (ServerLoginPacketListenerImpl.java:140-163) sends the compression packet at :149-153 and only afterwards reaches `finishLoginAndWaitForClient` (:165-168), which sends `ClientboundLoginFinishedPacket` (login clientbound `minecraft:login_finished`, protocol_id 2 — vanilla's name for LoginSuccess). When a duplicate session must be evicted first, the send is deferred to a later tick through the WAITING_FOR_DUPE_DISCONNECT state (:78-81, :156-161) but still lands after the compression packet | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla only advances the connection to the Configuration state upon receiving LoginAcknowledged from the client. | `ServerLoginPacketListenerImpl.handleLoginAcknowledgement` (:237-246) asserts state PROTOCOL_SWITCHING and is the sole site that switches this connection to Configuration in both directions: `setupOutboundProtocol(ConfigurationProtocols.CLIENTBOUND)` (:240) and `setupInboundProtocol(ConfigurationProtocols.SERVERBOUND, configPacketListener)` (:243), followed by `startConfiguration()` (:244). The packet is `ServerboundLoginAcknowledgedPacket` = login serverbound `minecraft:login_acknowledged`, protocol_id 3 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's Configuration sequence is a branding PluginMessage, then UpdateEnabledFeatures, then known-packs negotiation, then RegistryData per the worldgen registries followed unconditionally by UpdateTags, then FinishConfiguration. | The five named steps occur in exactly that relative order, but the sequence as stated is incomplete — a mandatory packet inside the described span is missing. `ServerConfigurationPacketListenerImpl.startConfiguration` (:83-101) sends the brand `ClientboundCustomPayloadPacket(BrandPayload)` (:84), then — only if `server.serverLinks()` is non-empty — `ClientboundServerLinksPacket` (:86-88), then `ClientboundUpdateEnabledFeaturesPacket` (:96), then queues `SynchronizeRegistriesTask` (:97-98) whose `start` sends `ClientboundSelectKnownPacks` (SynchronizeRegistriesTask.java:31-33) and whose `handleResponse` sends the `ClientboundRegistryDataPacket` batch over `RegistryDataLoader.SYNCHRONIZED_REGISTRIES` resolved against `RegistryLayer.WORLDGEN` and then, unconditionally, `ClientboundUpdateTagsPacket` (:35-52, RegistrySynchronization.java:30-38) — that tags packet (`minecraft:update_tags`, configuration clientbound protocol_id 13) is required and is absent from the claim. After it come the optional code-of-conduct and resource-pack tasks (:110-128) and the packet-less `PrepareSpawnTask`, and only `JoinWorldTask` finally emits `ClientboundFinishConfigurationPacket` (JoinWorldTask.java:14-17) | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla only advances a connection to the Play state upon receiving AcknowledgeFinishConfiguration from the client. | `ServerConfigurationPacketListenerImpl.handleConfigurationFinished` (:159-183) is the only site: it switches outbound to `GameProtocols.CLIENTBOUND_TEMPLATE` (:163) and, via `prepareSpawnTask.spawnPlayer(...)` (:178) reaching `PlayerList.placeNewPlayer` (PlayerList.java:145-161), switches inbound to `GameProtocols.SERVERBOUND_TEMPLATE`. Vanilla's own name for the triggering packet is `ServerboundFinishConfigurationPacket` = configuration serverbound `minecraft:finish_configuration`, protocol_id 3 — the wiki's "Acknowledge Finish Configuration" | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's default network compression threshold is 256. | `DedicatedServerProperties.networkCompressionThreshold` (DedicatedServerProperties.java:107) = `this.get("network-compression-threshold", 256)`, surfaced by `DedicatedServer.getCompressionThreshold` (DedicatedServer.java:619) and consumed at ServerLoginPacketListenerImpl.java:148-152 | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla's default keep-alive interval is 15000 ms (15 seconds). | `ServerCommonPacketListenerImpl.LATENCY_CHECK_INTERVAL = 15000` (:35), applied by `keepConnectionAlive` (:117-131) as `now - keepAliveTime >= 15000L`, which either disconnects (an earlier keep-alive still unanswered) or sends `ClientboundKeepAlivePacket` and restarts the timer. Two notes for the proxy driver: the value is a hard-coded constant, not an operator-configurable default, and the same 15000 ms doubles as the unanswered-keep-alive timeout; the check is skipped for the singleplayer owner | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Vanilla Java Edition clients connect to the server over plain TCP, not QUIC/UDP. | `ServerConnectionListener.startTcpServerListener` (:56-93) binds a Netty `ServerBootstrap` on `EventLoopGroupHolder.remote(useNativeTransport)`'s server-channel class — `KQueueServerSocketChannel`, `EpollServerSocketChannel` or `NioServerSocketChannel` (EventLoopGroupHolder.java:27-68), all stream sockets — and sets `ChannelOption.TCP_NODELAY` on each accepted child (:65) plus a 30-second `ReadTimeoutHandler` (:70). No datagram or QUIC channel type exists anywhere on the player path; the only non-TCP channel in the tree is the in-JVM `LocalServerChannel` used by the integrated singleplayer server (EventLoopGroupHolder.java:45-50, ServerConnectionListener.java:96-99) | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Every vanilla protocol packet's payload begins with a VarInt packet id, followed by the packet's own fields. | `IdDispatchCodec.encode` (IdDispatchCodec.java:44-64) writes `VarInt.write(output, id)` and only then delegates to that packet type's own `StreamCodec`; `decode` (:25-42) mirrors it with `VarInt.read(input)` first, rejecting an out-of-range id as "Received unknown packet id". That dispatch codec is what every protocol state's codec is built from (`ProtocolCodecBuilder`, ProtocolCodecBuilder.java:9 and :25-27) and what `PacketEncoder.encode` invokes (PacketEncoder.java:24), so the rule holds for all four states in both directions | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| Session validation during vanilla Login calls Mojang's public hasJoined session-service endpoint. | `ServerLoginPacketListenerImpl.handleKey` (:191-197) spawns a "User Authenticator" thread that calls `server.services().sessionService().hasJoinedServer(name, digest, address)`, `digest` being `Crypt.digestData("", serverPublicKey, secretKey)` as a base-16 BigInteger (:182) and `address` only when prevent-proxy-connections is on (:221-226). The implementation is authlib's `YggdrasilMinecraftSessionService`, shipped as META-INF/libraries/com/mojang/authlib/9.0.75/authlib-9.0.75.jar inside server-bundler.jar: its constant pool carries `hasJoinedServer`, the path fragment `/session/minecraft/` and the literal `hasJoined`, against the base host `https://sessionserver.mojang.com` declared in `YggdrasilEnvironment` — the public session-service hasJoined endpoint, and the only remote endpoint the server contacts during Login | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
