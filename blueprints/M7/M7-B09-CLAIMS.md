# M7-B09 — Claims Verified (TEST-D57)

| Claim | Source location | Verdict | Verified by | Date |
|---|---|---|---|---|
| The Login-state packet LoginSuccess is sent to a client only during an actual protocol-level login, never during an ordinary in-world region/border crossing (§B.1, §D.2). | The clientbound login-completion packet has exactly one send site in the whole 26.2 server: `ServerLoginPacketListenerImpl.finishLoginAndWaitForClient` (ServerLoginPacketListenerImpl.java:167-170), reachable only from the login handshake with `state == PROTOCOL_SWITCHING`; nothing in the play or configuration listeners can emit it, so no movement, teleport, chunk change or dimension change can produce it. Naming correction for §B.1/§D.2, which the blueprint should adopt: 26.2 has no packet called `LoginSuccess` — the class is `ClientboundLoginFinishedPacket` (ClientboundLoginFinishedPacket.java:12-27) and the registry key is `minecraft:login_finished`, login/clientbound protocol_id 2 (reports/packets.json, login.clientbound). It carries a GameProfile plus a session UUID, not a bare success flag | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Login-state packet LoginAcknowledged is sent by the client to the server only, never by the server to a client, so it can never appear in the client's own observed inbound packet stream; the genuine inbound Login-state markers to check for during an ordinary in-world region/border crossing are LoginDisconnect, Hello, LoginFinished, LoginCompression, CustomQuery, and CookieRequest (§B.1, §D.2). | The direction is reversed. `minecraft:login_acknowledged` is SERVERBOUND-only, login state, protocol_id 3 (reports/packets.json, login.serverbound; there is no clientbound entry of that name), class `ServerboundLoginAcknowledgedPacket`, handled by `ServerLoginPacketListenerImpl.handleLoginAcknowledgement` (ServerLoginPacketListenerImpl.java:237-246). It is sent BY the client, once, in response to `minecraft:login_finished`, to move both directions onto `ConfigurationProtocols`. Correction with a real consequence for §D.2: the observed packet log there records the client's own INBOUND stream, which can never contain this packet at all, so asserting its absence is vacuous rather than load-bearing. The genuine inbound Login-state markers to scan for are `minecraft:login_disconnect` (0), `minecraft:hello` (1), `minecraft:login_finished` (2), `minecraft:login_compression` (3), `minecraft:custom_query` (4) and `minecraft:cookie_request` (5). Only if the harness also logs the client's OUTBOUND packets does a `login_acknowledged` assertion carry meaning | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The Configuration-state packet FinishConfiguration is sent to a client only during an actual configuration-phase re-entry, never during an ordinary in-world region/border crossing (§B.1, §D.2). | `ClientboundFinishConfigurationPacket` (`minecraft:finish_configuration`, configuration/clientbound protocol_id 3) has exactly one send site: `JoinWorldTask.start` (JoinWorldTask.java:14-18), the terminal configuration task; the configuration phase itself is entered only from `ServerLoginPacketListenerImpl.handleLoginAcknowledgement` (ServerLoginPacketListenerImpl.java:237-246, initial login) or from `ServerGamePacketListenerImpl.switchToConfig` (ServerGamePacketListenerImpl.java:1795-1800), whose sole caller in 26.2 is the `/debugconfig` command (DebugConfigCommand.java:81). No movement, chunk-boundary or teleport path reaches either, so the "never during an in-world crossing" half holds exactly. One scoping correction: the packet is not confined to a re-entry — the same `JoinWorldTask` ends the INITIAL, login-driven configuration phase too, which is the ordinary case; and note the serverbound `minecraft:finish_configuration` (protocol_id 3, ServerboundFinishConfigurationPacket) is a distinct packet the client sends back, so a phase tag alone cannot distinguish them | CONFIRMED | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
| The clientbound Play-state Respawn packet is sent to a client during an actual dimension change and also during an ordinary post-death respawn that need not change dimension, but a same-dimension in-world region/border crossing never emits it, since the underlying teleport path returns early, before constructing Respawn, whenever the target and current dimensions match (§B.1, §D.2). | Dimension change is not the only trigger. `ClientboundRespawnPacket` (`minecraft:respawn`, play/clientbound protocol_id 82) has exactly two send sites: `ServerPlayer.teleport` (ServerPlayer.java:1116-1118), with `dataToKeep = 3` (`KEEP_ALL_DATA`, ClientboundRespawnPacket.java:12-14), and `PlayerList.respawn` (PlayerList.java:387, send at :414) with `dataToKeep = (byte)(keepAllPlayerData ? 1 : 0)`. The second site is reached from `ServerGamePacketListenerImpl.handleClientCommand`'s PERFORM_RESPAWN branch (ServerGamePacketListenerImpl.java:1889-1903) for a post-death respawn, which does NOT require a dimension change — dying and respawning inside the overworld still sends it. The half that matters for AC1b is nevertheless confirmed, and confirmed strongly: `ServerPlayer.teleport` returns early at ServerPlayer.java:1109-1114 whenever the target level's dimension equals the current one, sending only an ordinary position teleport, so a same-dimension in-world move — which is what a region/border crossing is — provably never emits Respawn | WRONG — corrected | research role (TEST-D57 pass, claude-opus-5) | 2026-09-05 |
