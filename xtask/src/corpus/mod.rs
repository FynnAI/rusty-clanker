//! M3-B07: `xtask fetch-corpus`/`parity-check redstone` verb surface (WS-D9).
//! M3 field-report harness: `xtask placement-diff` (governance changeset, "a placement
//! differential harness") lives alongside them.

pub mod blocks_report;
pub mod fetch_corpus;
pub mod parity_check;
pub mod placement_diff;
pub mod protocol_diff;
