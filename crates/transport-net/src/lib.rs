//! `rc-transport-net` — `NetworkTransport`: `quinn`/QUIC + `postcard`-backed cluster-mode `Transport` impl. Gated behind `cluster` feature.
//!
//! M0 scaffold placeholder (M0-B01). Real types land in a later M0 blueprint.
