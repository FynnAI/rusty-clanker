//! `rc-scheduler` — RC-Executor, RC-WorkerPool, the 11-stage tick pipeline driver, region build/merge/split, Tokio<->RC-WorkerPool boundary types.
//!
//! M0 scaffold placeholder (M0-B01). Real types land in a later M0 blueprint.

pub mod pool;
