//! `rusty-clanker-server` — Server composition-root binary + embeddable library target (`run_embedded`). Wires every server-side crate, owns the Tokio runtime.
//!
//! M0 scaffold placeholder (M0-B01). The real `pub fn run_embedded(...)` lands
//! with the M0 blueprint that implements `rc-scheduler`'s tick loop.
