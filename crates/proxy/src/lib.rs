//! `rc-proxy` — Proxy-role logic: connection forwarding table, handoff buffering, proxy<->node control channel. A library, not a binary. Gated behind `cluster` feature.
//!
//! M0 scaffold placeholder (M0-B01). Real types land in a later M0 blueprint.
